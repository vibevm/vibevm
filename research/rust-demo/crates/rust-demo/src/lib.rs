//! rust-demo — the committed Rust consumer testbed of the AI-Native
//! discipline and the agentic type oracle (AGENTIC-TCG-RUST-PLAN v0.1
//! Phase 2). Cells register here and expose their seams; nothing else
//! is public.

specmark::scope!("spec://rust-demo/PROP-001#root");

pub mod cells;
pub mod core;
