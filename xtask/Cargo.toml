[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Project tooling — codegen, drift checks, and other build-time helpers"

[dependencies]
anyhow.workspace = true
clap.workspace = true
semver.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
specmap-core.workspace = true
rust-ai-native-specmap.workspace = true
walkdir.workspace = true
rust-ai-native-conform.workspace = true
conform-core.workspace = true
rust-ai-native-conform-frontend.workspace = true
rust-ai-native-cli.workspace = true
vibe-index.workspace = true
vibe-publish.workspace = true
zip.workspace = true
sha2.workspace = true
vibe-core.workspace = true
vibe-registry.workspace = true
# `embed-doc-shell` writes the shell's index and its pin, and the digest
# in both is measured by the crate that will re-measure it later — one
# algorithm, so the comparison means something (PROP-057 ##SHELL-PIN).
vibe-doc-shell.workspace = true
# The CLI path (not only the tests) needs a scratch directory to
# reproject into; the tests were using it as a dev-dependency before.
tempfile.workspace = true

[dev-dependencies]
# Test fixtures stamp journal records; DateTime is named, not inferred.
chrono.workspace = true
