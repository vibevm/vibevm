# FAQ

Answers to real questions developers ask about vibevm, written up as standalone pages so they can be searched, linked, and folded into the wider documentation over time. Each entry states the question, then the practical answer with worked examples and links into the spec.

| Question | Page |
| --- | --- |
| A dependency deep in my tree clashes on a version and the install fails — how do I fix it, and can I force a version? | [version-conflicts.md](version-conflicts.md) |

## Adding an entry

One question per page. Follow the shape of the existing entries: lead with the developer's question in bold, give a short answer up front, then the detail with copy-pasteable `vibe.toml` / CLI examples and cross-links to the relevant `docs/` pages and `spec/` PROPs. Add a row to the table above. Keep it in English, matching the rest of [`docs/`](../README.md).
