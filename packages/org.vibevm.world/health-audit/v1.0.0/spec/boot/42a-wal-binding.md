# Health audit — the WAL binding {#root}

@fact:AUDIT-WAL-BINDING With `flow:wal` installed, the volatile session
checkpoints of the finding rule are `spec/WAL.md` and `CONTINUE.md` —
reconciled against `AUDIT.md`, never its substitute. @status:impl/done
