# Conflict protocol — the WAL binding {#root}

@fact:CONFLICT-WAL-BINDING With `flow:wal` installed, the volatile state
file of the priority ladder is `spec/WAL.md`; the full ladder reads
Human > Spec > Tests > Code > WAL. @status:impl/done
