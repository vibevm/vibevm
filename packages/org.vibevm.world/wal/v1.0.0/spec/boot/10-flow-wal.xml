<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Flow: WAL (Write-Ahead Log)</title>
  <status stage="impl" state="done"/>
  <p><PROJECT-USES-WAL-DISCIPLINE-FOR-SESSION-CONTINUITY fact="true" status="impl/done">This project uses **WAL discipline** for session continuity.</PROJECT-USES-WAL-DISCIPLINE-FOR-SESSION-CONTINUITY></p>
  <p><two-files-carry-it-lead fact="true" status="impl/done">Two files
carry it:</two-files-carry-it-lead></p>
  <facts ordered="false">
    <FILE-WAL-IS-THE-CANONICAL-LIVING-CHECKPOINT fact="true" status="impl/done">`spec/WAL.xml` — the living checkpoint. **Canonical.**</FILE-WAL-IS-THE-CANONICAL-LIVING-CHECKPOINT>
    <FILE-CONTINUE-IS-THE-COLD-RESUME-SNAPSHOT fact="true" status="impl/done">`CONTINUE.md` (repo root) — the cold-resume snapshot. The WAL
  supersedes it wherever they diverge.</FILE-CONTINUE-IS-THE-COLD-RESUME-SNAPSHOT>
  </facts>
  <session-start title="At the start of every session">
    <facts ordered="true">
      <READ-THE-WAL-BEFORE-DOING-ANYTHING-ELSE fact="true" status="impl/done">Read `spec/WAL.xml` **before** doing anything else. The `wal-status`
   skill, where installed, is the fast form of this read.</READ-THE-WAL-BEFORE-DOING-ANYTHING-ELSE>
      <VERIFY-THE-UPDATED-LINE-IS-CURRENT fact="true" status="impl/done">Verify the `_Updated:` line is current. If it is older than 24
   hours, ask the user to confirm state before acting on anything the
   WAL claims — and before any destructive work.</VERIFY-THE-UPDATED-LINE-IS-CURRENT>
      <HONOUR-EVERY-CONSTRAINT-VERBATIM fact="true" status="impl/done">Honour every constraint listed in the WAL's **Constraints** section
   verbatim. These are the "do not touch" rules: violate them only
   after an explicit, in-session confirmation from the user.</HONOUR-EVERY-CONSTRAINT-VERBATIM>
    </facts>
  </session-start>
  <during title="During the session">
    <facts ordered="true">
      <PROPOSE-ADDING-A-DURABLE-DECISION-TO-THE-WAL fact="true" status="impl/done">If the user makes a decision that affects future sessions, propose
   adding it to the WAL (or the spec, if it's architectural). Do not
   silently file it as "remembered."</PROPOSE-ADDING-A-DURABLE-DECISION-TO-THE-WAL>
      <STOP-AND-SURFACE-BEFORE-VIOLATING-A-CONSTRAINT fact="true" status="impl/done">If you find yourself about to violate a Constraint, stop and surface
   the question explicitly. A violation snuck past in a diff is a
   future bug.</STOP-AND-SURFACE-BEFORE-VIOLATING-A-CONSTRAINT>
    </facts>
  </during>
  <session-end title="At the end of every session">
    <facts ordered="true">
      <REWRITE-THE-WAL-NEVER-APPEND-TO-IT fact="true" status="impl/done">Rewrite `spec/WAL.xml` per the protocol in
   @spec://org.vibevm.world/wal/flows/wal/session-end-hook#root.
   Rewrite, not append — the WAL must reflect the **current** state,
   not the history. History lives in `git log` and in milestone commit
   messages; the WAL is a checkpoint, not a journal.</REWRITE-THE-WAL-NEVER-APPEND-TO-IT>
    </facts>
  </session-end>
  <commands title="Session commands">
    <facts ordered="true">
      <RECOGNISE-THE-WIND-DOWN-PHRASES fact="true" status="impl/done">Recognise the **wind-down** phrases — `END SESSION`, `WRAP UP`,
   `CHECKPOINT AND CLOSE`, and any project-defined twins (recognise the
   intent, not the exact wording). A wind-down invokes the full
   session-end hook *plus* a wholesale overwrite of `CONTINUE.md`, per
   @spec://org.vibevm.world/wal/flows/wal/cold-resume#root.</RECOGNISE-THE-WIND-DOWN-PHRASES>
      <RECOGNISE-THE-RESUME-PHRASES fact="true" status="impl/done">Recognise the **resume** phrases — `RESUME SESSION`, `RESTORE
   CONTEXT`. Restore context, verify the repository state empirically,
   emit a status report — then **stop and wait for direction**. A
   recorded "next step" is a candidate, not authorisation.</RECOGNISE-THE-RESUME-PHRASES>
    </facts>
  </commands>
  <scope title="Scope of this flow">
    <facts ordered="false">
      <FLOW-OWNS-ONLY-ITS-OWN-FILES fact="true" status="impl/done">This flow owns only the protocol files under `spec/flows/wal/`, the
  `wal-status` skill, and this boot snippet.</FLOW-OWNS-ONLY-ITS-OWN-FILES>
      <WAL-AND-CONTINUE-ARE-PROJECT-STATE-NOT-PACKAGE-STATE fact="true" status="impl/done">`spec/WAL.xml` and `CONTINUE.md` are **project state**, not package
  state — the package never creates, deletes, or overwrites them as
  part of install or uninstall.</WAL-AND-CONTINUE-ARE-PROJECT-STATE-NOT-PACKAGE-STATE>
    </facts>
    <p><WAL-IS-THE-SINGLE-DEVELOPER-CONVENTION fact="true" status="impl/done">This flow is the
**single-developer, central-WAL convention**. A multi-developer project
chooses its own session-durability scheme — many WALs (the registered-
subprojects form is `flow:wal-specspaces`), or none — by not installing
this flow or by superseding it; nothing in VibeVM requires a WAL
(PROP-049).</WAL-IS-THE-SINGLE-DEVELOPER-CONVENTION></p>
    <p><full-protocol-pointer fact="true" status="impl/done">Full protocol: @spec://org.vibevm.world/wal/flows/wal/WAL-PROTOCOL#root.</full-protocol-pointer></p>
  </scope>
</spec>
