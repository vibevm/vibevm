# Redbook member — wal-specspaces {#root}

@fact:MEMBER-WAL-SPECSPACES `wal-specspaces` — nested projects (specspaces), each carrying its
own boot contract, checkpoint file, and cold-resume file. @status:impl/done
