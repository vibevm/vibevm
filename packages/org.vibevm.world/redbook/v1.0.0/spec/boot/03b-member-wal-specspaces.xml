<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Redbook member — wal-specspaces</title>
  <p><MEMBER-WAL-SPECSPACES fact="true" status="impl/done">`wal-specspaces` — nested projects (specspaces), each carrying its
own boot contract, checkpoint file, and cold-resume file.</MEMBER-WAL-SPECSPACES></p>
</spec>
