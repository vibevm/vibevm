<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">The Specspaces Protocol</title>
  <status stage="spec" state="done"/>
  <p><scope-of-this-document fact="true" status="impl/done">**Scope of this document.** This file defines *what* a specspace is,
the registry file that names them, how a session phrase resolves to a
target, the scoped session grammar, and the five laws that keep a
specspace's state from bleeding into its host.</scope-of-this-document></p>
  <p><WAL-MECHANICS-ARE-NOT-REDEFINED-HERE fact="true" status="impl/done">The per-specspace WAL
mechanics are not redefined here — each specspace runs the two-file
model owned by `flow:org.vibevm.world/wal`.</WAL-MECHANICS-ARE-NOT-REDEFINED-HERE></p>
  <what title="What a specspace is">
    <p><A-SPECSPACE-IS-WORKED-ON-AS-AN-INDEPENDENT-PROJECT fact="true" status="impl/done">A specspace is a sub-project that lives inside a host repository but
is **worked on as an independent project**: its own boot contract,
its own WAL, its own cold-resume file, its own plans and specs, its
own definition of a green floor.</A-SPECSPACE-IS-WORKED-ON-AS-AN-INDEPENDENT-PROJECT></p>
    <p><THE-HOST-CARRIES-IT-BUT-A-SPECSPACE-SESSION-IGNORES-HOST-CONTEXT fact="true" status="impl/done">The host carries it (one git
history, one set of repo-wide rules), but a session working on the
specspace neither loads nor scans the host's project context.</THE-HOST-CARRIES-IT-BUT-A-SPECSPACE-SESSION-IGNORES-HOST-CONTEXT></p>
    <p><A-SPECSPACE-IS-ITS-OWN-INSTALLABLE-PUBLISHABLE-THING fact="true" status="impl/done">A specspace is its own installable, publishable thing in the general
case; that a host repository happens to author one under `packages/`
(as vibevm does with `fractality`) is incidental.</A-SPECSPACE-IS-ITS-OWN-INSTALLABLE-PUBLISHABLE-THING></p>
    <p><PEOPLE-INSTALL-SPECSPACES-AS-ORDINARY-PACKAGES fact="true" status="impl/done">People install
specspaces as ordinary packages, and two of them coinciding inside
one host means nothing in general.</PEOPLE-INSTALL-SPECSPACES-AS-ORDINARY-PACKAGES></p>
    <p><the-problem-this-solves fact="true" status="spec/done">The problem this solves: a central WAL describes one project.</the-problem-this-solves></p>
    <p><sessions-otherwise-face-a-bad-choice fact="true" status="spec/done">The
moment a repository hosts a second, independently-evolving effort,
sessions face a bad choice — boot the whole host corpus to work on a
corner of it, or work blind.</sessions-otherwise-face-a-bad-choice></p>
    <p><SPECSPACES-MAKE-THE-SECOND-EFFORT-A-FIRST-CLASS-PROJECT fact="true" status="impl/done">Specspaces make the second effort a
first-class project with first-class session continuity, at the cost
of one registry file and one grammar extension.</SPECSPACES-MAKE-THE-SECOND-EFFORT-A-FIRST-CLASS-PROJECT></p>
  </what>
  <registry title="The registry: `SPECSPACES.md`">
    <p><ONE-FILE-AT-THE-HOST-ROOT-NAMES-EVERY-SPECSPACE fact="true" status="impl/done">One file at the host root names every specspace.</ONE-FILE-AT-THE-HOST-ROOT-NAMES-EVERY-SPECSPACE></p>
    <p><AN-OPTIONAL-DEFAULT-LINE-THEN-ONE-ROW-PER-SPECSPACE fact="true" status="impl/done">An optional
`default:` line may precede the table; then one table row per
specspace:</AN-OPTIONAL-DEFAULT-LINE-THEN-ONE-ROW-PER-SPECSPACE></p>
    <fence lang="markdown">default: host

| name | root | wal | continue | status |
|---|---|---|---|---|
| fractality | packages/org.vibevm.fractality/ | WAL.md | CONTINUE.md | 2026-07-09 — ignition PLANNED; next: Phase 0 |</fence>
    <facts ordered="false">
      <FIELD-DEFAULT fact="true" status="impl/done">**`default:`** (optional, above the table) — which target a **bare**
  session phrase resolves to (see [target resolution](#resolve)).
  `default: host` (or omitting the line entirely) makes a bare phrase
  target the host project; `default: &lt;name&gt;` makes a bare phrase
  target that specspace. Omit it unless the repository's primary work
  really lives in one specspace.</FIELD-DEFAULT>
      <FIELD-NAME fact="true" status="impl/done">**name** — the word used in session phrases. Short, unique,
  lowercase.</FIELD-NAME>
      <FIELD-ROOT fact="true" status="impl/done">**root** — the specspace root, relative to the host root. The
  specspace's `CLAUDE.md` (or equivalent boot contract) lives here.</FIELD-ROOT>
      <FIELD-WAL-AND-CONTINUE fact="true" status="impl/done">**wal**, **continue** — paths relative to root. Defaults are
  `spec/WAL.xml` and `CONTINUE.md` per the wal flow; a specspace
  without a spec tree at its root may keep both flat at the root, as
  the example does. The registry entry is the truth.</FIELD-WAL-AND-CONTINUE>
      <FIELD-STATUS fact="true" status="impl/done">**status** — one line, refreshed at every specspace wind-down:
  date, campaign/phase state, the next step. A pointer for the
  *host's* readers; never the specspace's canonical state.</FIELD-STATUS>
    </facts>
  </registry>
  <resolve title="Target resolution">
    <p><A-PHRASE-RESOLVES-TO-EXACTLY-ONE-TARGET fact="true" status="impl/done">A resume or wind-down phrase resolves to exactly **one** target — the
host project or a single specspace — by this order:</A-PHRASE-RESOLVES-TO-EXACTLY-ONE-TARGET></p>
    <facts ordered="true">
      <TARGET-EXPLICIT-WINS-ALWAYS fact="true" status="impl/done">**Explicit target wins, always.** A phrase that names a specspace
   (`RESUME SESSION &lt;name&gt;`), or that names an explicit directory,
   targets that specspace or directory — **regardless of any declared
   default or prior context**. This is the user's always-available
   escape hatch: an explicit command forces restoration from an
   arbitrary specspace or directory. A name matching no registry row
   is surfaced, not guessed.</TARGET-EXPLICIT-WINS-ALWAYS>
      <TARGET-DECLARED-DEFAULT fact="true" status="impl/done">**Declared default.** A **bare** phrase (no name) uses the
   specspace named by the `default:` line of `SPECSPACES.md`, when one
   is declared.</TARGET-DECLARED-DEFAULT>
      <TARGET-HOST-FALLBACK fact="true" status="impl/done">**Host fallback.** With no name and no declared default, a bare
   phrase targets the **host project** — restore the host root's own
   WAL and cold-resume file, per the host contract's session-command
   sections.</TARGET-HOST-FALLBACK>
    </facts>
    <p><A-BARE-PHRASE-NEVER-SILENTLY-SELECTS-A-SPECSPACE fact="true" status="impl/done">A bare phrase therefore **never silently selects a specspace**.</A-BARE-PHRASE-NEVER-SILENTLY-SELECTS-A-SPECSPACE></p>
    <p><AT-THE-HOST-ROOT-A-BARE-PHRASE-RESTORES-THE-HOST-WAL fact="true" status="impl/done">At the
host root, a bare `RESUME SESSION` / `ВОССТАНОВИ СЕССИЮ` restores the
**host** WAL — not a registered specspace such as `fractality`.</AT-THE-HOST-ROOT-A-BARE-PHRASE-RESTORES-THE-HOST-WAL></p>
    <p><the-whole-point-of-rule-three fact="true" status="spec/done">This
is the whole point of rule 3: a registry with rows in it must not
tempt a session into resuming the wrong project.</the-whole-point-of-rule-three></p>
  </resolve>
  <grammar title="The session grammar">
    <p><PHRASES-GAIN-AN-OPTIONAL-SPECSPACE-NAME fact="true" status="impl/done">The wal flow's wind-down and resume phrases gain an optional
specspace name.</PHRASES-GAIN-AN-OPTIONAL-SPECSPACE-NAME></p>
    <p><recognise-the-intent-not-the-exact-wording-lead fact="true" status="impl/done">Recognise the intent, not the exact wording:</recognise-the-intent-not-the-exact-wording-lead></p>
    <table>
      <tr>
        <td>Intent</td>
        <td>English</td>
        <td>Russian</td>
      </tr>
      <tr>
        <td><ROW-RESUME-INTO-SPECSPACE fact="true" status="impl/done">resume into specspace</ROW-RESUME-INTO-SPECSPACE></td>
        <td><ROW-RESUME-INTO-SPECSPACE-ENGLISH fact="true" status="impl/done">`RESUME SESSION &lt;name&gt;`, `RESTORE CONTEXT &lt;name&gt;`</ROW-RESUME-INTO-SPECSPACE-ENGLISH></td>
        <td><ROW-RESUME-INTO-SPECSPACE-RUSSIAN fact="true" status="impl/done">`ВОССТАНОВИ СЕССИЮ &lt;name&gt;`, `ПРОДОЛЖАЕМ &lt;name&gt;`</ROW-RESUME-INTO-SPECSPACE-RUSSIAN></td>
      </tr>
      <tr>
        <td><ROW-WIND-DOWN-SPECSPACE fact="true" status="impl/done">wind down specspace</ROW-WIND-DOWN-SPECSPACE></td>
        <td><ROW-WIND-DOWN-SPECSPACE-ENGLISH fact="true" status="impl/done">`END SESSION &lt;name&gt;`, `WRAP UP &lt;name&gt;`</ROW-WIND-DOWN-SPECSPACE-ENGLISH></td>
        <td><ROW-WIND-DOWN-SPECSPACE-RUSSIAN fact="true" status="impl/done">`ЗАВЕРШИ СЕССИЮ &lt;name&gt;`, `ФИКСИРУЕМ &lt;name&gt;`</ROW-WIND-DOWN-SPECSPACE-RUSSIAN></td>
      </tr>
    </table>
    <p><THE-NAME-IS-OPTIONAL fact="true" status="impl/done">The name is optional; [target resolution](#resolve) decides what a
phrase with no name means.</THE-NAME-IS-OPTIONAL></p>
    <p><RESUME-INTO-A-SPECSPACE fact="true" status="impl/done">**Resume into a specspace** (report-then-wait, as in the wal flow):
read the host's repo-wide rules, the specspace boot contract, the
specspace WAL, the specspace cold-resume file; verify the tree
empirically (branch, sync, working tree, recent commits touching the
specspace); report; stop and wait for direction.</RESUME-INTO-A-SPECSPACE></p>
    <p><WIND-DOWN-A-SPECSPACE fact="true" status="impl/done">**Wind down a specspace:** rewrite the specspace WAL; overwrite the
specspace cold-resume file wholesale; refresh the specspace's status
line in `SPECSPACES.md`; commit per the host's commit rules; update
the host WAL **only** if host files changed too.</WIND-DOWN-A-SPECSPACE></p>
  </grammar>
  <laws title="The five laws">
    <facts ordered="true">
      <LAW-BOOT-SCOPING fact="true" status="impl/done">**Boot scoping.** A specspace session loads the host's repo-wide
   rules and the specspace's own files — nothing else from the host.
   Crossing into host files mid-session is legal but announced.</LAW-BOOT-SCOPING>
      <LAW-STATE-LOCALITY fact="true" status="impl/done">**State locality.** A specspace's canonical state lives in the
   specspace (its WAL). The registry status line is a pointer, not
   state; the host WAL never carries specspace detail beyond "the
   specspace exists; see its WAL".</LAW-STATE-LOCALITY>
      <LAW-ONE-SESSION-ONE-FOCUS fact="true" status="impl/done">**One session, one focus.** A session serves the host or one
   specspace. Work for two projects in one session splits into
   commits per project and updates each project's WAL — and is the
   exception, said out loud, not the habit.</LAW-ONE-SESSION-ONE-FOCUS>
      <LAW-HOST-RULES-SURVIVE fact="true" status="impl/done">**Host rules survive.** Repo-wide non-negotiables (authorship,
   commit conventions, secrecy rules) bind specspace sessions in
   full. A specspace may add rules; it may not subtract the host's.</LAW-HOST-RULES-SURVIVE>
      <LAW-PACKAGE-STATE-STAYS-OUT fact="true" status="impl/done">**Package state stays out.** WAL, cold-resume, and registry files
   are project state. No installable package may create, overwrite,
   or delete them on install or uninstall — the same law the wal
   flow states for its two files.</LAW-PACKAGE-STATE-STAYS-OUT>
    </facts>
  </laws>
  <lifecycle title="Lifecycle">
    <facts ordered="false">
      <LIFECYCLE-REGISTER fact="true" status="impl/done">**Register:** create the specspace root with its boot contract,
  WAL, and cold-resume file; add the registry row; mention the
  registry in the host boot contract so sessions recognise the
  grammar. First wind-down validates the loop.</LIFECYCLE-REGISTER>
      <LIFECYCLE-RETIRE fact="true" status="impl/done">**Retire:** the specspace graduates to its own repository (its
  files move wholesale; its WAL goes with it) or closes (final
  status line says so; the row moves to a "retired" section — the
  name stays reserved so old phrases fail loudly, not silently).</LIFECYCLE-RETIRE>
      <LIFECYCLE-NESTING fact="true" status="impl/done">**Nesting:** one level. A specspace hosting its own specspaces is
  a sign it wants to be a repository.</LIFECYCLE-NESTING>
    </facts>
  </lifecycle>
  <re-derive title="Re-derive for your project">
    <p><run-this-prompt-once-lead fact="true" status="impl/done">Run this prompt once to adapt the protocol to a concrete host:</run-this-prompt-once-lead></p>
    <fence>Read SPECSPACES-PROTOCOL.xml. Adapt it to this repository:
1. Create SPECSPACES.md at the host root (empty table if no
   specspaces exist yet). Add a `default:` line only if a bare
   session phrase should target a specspace instead of the host.
2. Add a short "Specspaces" section to the host boot contract:
   the grammar, target resolution (bare → default else host), the
   boot-scoping law, and a pointer to the registry.
3. If a sub-project already behaves like a specspace, register it:
   boot contract, WAL, cold-resume file, registry row.
4. Record which host sections are "repo-wide rules" that specspace
   sessions must still load — name them explicitly.
Do not move any existing project state while adapting.</fence>
  </re-derive>
  <summary title="Summary">
    <facts ordered="false">
      <SUM-WHAT-A-SPECSPACE-IS fact="true" status="impl/done">A specspace is a sub-project with first-class session continuity:
  own boot contract, own WAL, own cold-resume file.</SUM-WHAT-A-SPECSPACE-IS>
      <SUM-THE-REGISTRY fact="true" status="impl/done">`SPECSPACES.md` at the host root is the registry; an optional
  `default:` line sets what a bare phrase targets; its status column
  is a pointer, never canonical state.</SUM-THE-REGISTRY>
      <SUM-TARGET-RESOLUTION fact="true" status="impl/done">Target resolution: an explicit name/directory always wins; a bare
  phrase takes the declared default, else the host — never a
  specspace by accident.</SUM-TARGET-RESOLUTION>
      <SUM-THE-SESSION-GRAMMAR fact="true" status="impl/done">Session phrases gain an optional specspace name; resume stays
  report-then-wait; wind-down operates on the specspace's files.</SUM-THE-SESSION-GRAMMAR>
      <SUM-THE-FIVE-LAWS fact="true" status="impl/done">Five laws: boot scoping, state locality, one focus, host rules
  survive, package state stays out.</SUM-THE-FIVE-LAWS>
      <SUM-NESTING-AND-GRADUATION fact="true" status="impl/done">One nesting level; a specspace that outgrows the host graduates
  to its own repository.</SUM-NESTING-AND-GRADUATION>
    </facts>
  </summary>
</spec>
