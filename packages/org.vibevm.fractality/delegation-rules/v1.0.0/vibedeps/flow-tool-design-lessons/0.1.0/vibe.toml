[package]
name = "tool-design-lessons"
group = "org.vibevm"
kind = "flow"
version = "0.1.0"
authors = ["Oleg Chirukhin"]
license = "UPL-1.0"
description = "Design lessons for self-updating tools and package systems: pointer-flip activation, immutable instances, identity-is-source, vendor the bootstrap"
keywords = ["tool-design", "version-manager", "installer", "packaging", "lessons"]

[compatibility]
min_vibe_version = "0.1.0"
requires_kinds = []

[boot_snippet]
source = "spec/boot/70-flow-tool-design-lessons.md"
category = "flow"
