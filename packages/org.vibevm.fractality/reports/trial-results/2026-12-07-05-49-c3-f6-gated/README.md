# C3 Ф6 — the RLM gated-boss trial (MT-C3-01)

_Fired 2026-12-07 (Campaign 3 Ф6, D-C3-9). Three paid GLM-5.2 cold-boss runs
over `mini_logfmt` + the RLM preamble (`preamble-g.md`), driving the Stage-B
need-gate + descent + escalation + acceptance machinery. Pre-registration:
MT-C3-01._

## What this measured

Whether the Stage-B RLM patterns behave as the plan's §7 predictions say when
a real cold boss drives them — the gated arm's delegation rate against the C2
naive baseline (16.7%), plus predictions P-C3-a..d.

## Summary results

- **Delegation 8/18 ≈ 44.4% vs the 16.7% naive baseline — ~2.7× more.**
- **fractality ran end to end as a product for the first time**: real GLM
  workers spawned under real pods, did work, wrote results, ran acceptance,
  and folded back into the journal (3 workers completed, 1 acceptance 1/1).
- **P-C3-c CONFIRMED** (no wall-budget overrun). **P-C3-a SUPPORTED**
  (window-fit → route, not over-decompose). **P-C3-b / P-C3-d INCONCLUSIVE**
  — the menu exercised neither the schema gate nor a Silo task.
- Caveat: boss (50) + worker (30) turn caps bit hard — `delegated-and-
  collected` (3) trailed `delegated` (8) purely on the caps, not capability.

## Follow-up

The inconclusive predictions + the caps drive **PP-004 → MT-C3-03** (the
gated re-run: raised caps, a schema task, a Silo task, the `decisions` verb).
Canonical verdicts: the MT-C3-01 "Recorded runs" section and
`reports/2026-12-07-06-44-campaign3-f6-trial.md`. The raw evidence (bus facts
+ gzipped transcripts + the decision/escalation journals) sits per-run beside
this file.
