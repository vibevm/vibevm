[package]
name = "decision-records"
group = "org.vibevm"
kind = "flow"
version = "0.1.0"
authors = ["Oleg Chirukhin"]
license = "UPL-1.0"
description = "Decisions, not facts: record why with data and a revisit trigger, at the spec anchor the decision governs"
keywords = ["decisions", "adr", "rationale", "spec-driven", "memory"]

[compatibility]
min_vibe_version = "0.1.0"
requires_kinds = []

[boot_snippet]
source = "spec/boot/25-flow-decision-records.md"
category = "flow"
