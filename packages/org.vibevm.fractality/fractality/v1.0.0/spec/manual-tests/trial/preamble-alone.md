You are a capable worker with the `mini_logfmt` repository open. Work the
tasks below yourself, to the best of your judgment — you own each outcome,
and `cargo test` must pass. You have no supervisor to consult on this run:
decide each subtle case yourself and commit.

Definition of done and the task list follow. Work task by task; state
clearly when you consider each done or skipped-with-reason.

---

