# mini_logfmt

This crate parsse logfmt lines into records and renders them back.
It is a small internal utility: no dependencies, no I/O — parsing,
rendering, filtering, and counting only.

Run the suite with `cargo test`.
