<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>MT-C2-03 — nudges and the question push, live (Campaign 2 Ф4 exit E2E)</title>
  <p>_Proves: the threshold nudge reaches a real session's next prompt and
goes quiet under its cooldown; a genuinely parked worker interrupts
exactly one Stop with its question and never a second one. The nudge
half is CLI-drivable; the question half needs one real parked worker
(the MT-04 pattern) and is therefore paid._</p>
  <p>**Paid:** one GLM worker turn that parks on an ask_boss question (small
slot). **Isolated:** scratch `--home` + scratch project; the real
`~/.fractality` untouched.</p>
  <section title="Prerequisites">
    <list ordered="false">
      <item>Built binaries (`cargo build --workspace`) — **the whole workspace:
  hooks talk to the sibling daemon binary, and a stale
  `fractality-mission-control.exe` folds session events with old rules**
  (caught live in the Ф4 smoke: the cooldown silently never engaged).</item>
      <item>`claude` ≥ 2.1.202; profiles for the paid half (MT-04 pattern).</item>
    </list>
  </section>
  <section title="Steps — nudge half (unpaid)">
    <list ordered="true">
      <item>Scratch home + adapter + seven work-tool events:</item>
    </list>
    <fence lang="sh">   cd packages/org.vibevm.fractality/fractality/v0.1.0
   MT_HOME=$(mktemp -d)/home; EXE="$PWD/target/debug/fractality.exe"
   export FRACTALITY_HOME="$MT_HOME"
   "$EXE" mc start
   for i in 1 2 3 4 5 6 7; do
     echo '{"session_id":"mt-c2-03","cwd":"C:/p","tool_name":"Bash","duration_ms":100}' \
       | "$EXE" hook post-tool-use
   done
   echo '{"session_id":"mt-c2-03","cwd":"C:/p"}' | "$EXE" hook user-prompt-submit</fence>
    <p>**Expected:** one JSON line whose `additionalContext` names the
   count («7 work-tool calls since your last delegation») and cites
   `fractality route`.</p>
    <list ordered="true">
      <item>The cooldown:</item>
    </list>
    <fence lang="sh">   echo '{"session_id":"mt-c2-03","cwd":"C:/p"}' | "$EXE" hook user-prompt-submit
   echo "EXIT=$?"</fence>
    <p>**Expected:** no output, `EXIT=0` (quiet inside the 300 s window;
   `session show mt-c2-03` shows `nudges=1`).</p>
    <list ordered="true">
      <item>The kill switch:</item>
    </list>
    <fence lang="sh">   FRACTALITY_INITIATIVE=off bash -c \
     'echo "{\"session_id\":\"mt-c2-03-b\",\"cwd\":\"C:/p\"}" | "'$EXE'" hook session-start'</fence>
    <p>**Expected:** no output at all (not even the scoreboard).</p>
  </section>
  <section title="Steps — question half (paid, the MT-04 pattern)">
    <list ordered="true">
      <item>Start a worker that asks a question (reuse
   `spec/manual-tests/mt-04` packet or equivalent), wait until
   `fractality questions` lists it. In a REAL Claude Code session over
   this home (adapter installed): finish any turn.</item>
    </list>
    <p>**Expected:** the turn does not stop silently — the Stop feedback
   quotes the worker's question and the exact
   `fractality answer &lt;id&gt; "..."` command. The NEXT turn's stop is
   quiet (the alert acked itself; `session show` counts
   `alerts=1`). `fractality answer` resumes the worker (MT-04
   semantics).</p>
  </section>
  <section title="Recorded runs">
    <list ordered="false">
      <item>2026-07-10 (agent pre-run, nudge half, steps 1–3): PASS — threshold
  text exact, cooldown quiet, kill switch silent. Question half:
  pre-ran in the Ф4 staged smoke (parked question interrupted exactly
  one stop; ack folded; `stop_hook_active` respected — Ф4 ledger).
  The Ф6 paid trial produced no parked questions (arm B delegated
  nothing — MT-C2-04), so the staged pre-run remains the recorded
  evidence for the question half.</item>
      <item>**Owner sign-off: RECORDED 2026-07-10** (verbatim: «подписываю
  MT-C2-01…04»).</item>
    </list>
  </section>
</spec>
