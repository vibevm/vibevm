<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Landscape note — where fractality sits</title>
  <p>_Ф0.s7 shallow scan, 2026-07-09. Positioning only — none of these is
studied for implementation (inventory S7). The point is to state what
fractality does that the field does not._</p>
  <section title="The two families that exist">
    <p>**Per-request routers/proxies** — e.g. `claude-code-router`
(musistudio): a local gateway (`localhost:8080`) that rewrites each API
call from one Claude Code process to whatever provider you configure,
by conditional rules. One process, many providers *per request*.
Verified from its README this session.</p>
    <p>**Session/instance orchestrators** — e.g. `claude-swarm` (parruda,
YAML-defined agent teams over MCP), `claude-squad` (TUI managing
several Claude/Codex sessions in tmux+worktrees). Many processes, one
provider each, coordinated by config or a terminal multiplexer.</p>
  </section>
  <section title="Why fractality is neither">
    <table>
      <tr>
        <td>Axis</td>
        <td>routers</td>
        <td>orchestrators</td>
        <td>**fractality**</td>
      </tr>
      <tr>
        <td>provider switch</td>
        <td>per request, in-proc</td>
        <td>per session, manual</td>
        <td>per **worker profile**, isolated env (I1)</td>
      </tr>
      <tr>
        <td>supervision</td>
        <td>none (it's a proxy)</td>
        <td>tmux / MCP wiring</td>
        <td>**mission-control daemon**: registry, call tree, budgets, recursive kill</td>
      </tr>
      <tr>
        <td>stdio / crashes</td>
        <td>n/a</td>
        <td>terminal-bound</td>
        <td>**pod** per worker owns stdio, survives MC restart (D3)</td>
      </tr>
      <tr>
        <td>boss↔worker channel</td>
        <td>HTTP proxy</td>
        <td>shared terminal</td>
        <td>**MC bus**; files are persistence, not medium (I2)</td>
      </tr>
      <tr>
        <td>non-yolo autonomy</td>
        <td>n/a</td>
        <td>usually skip-perms</td>
        <td>allowlist + **pod broker + ask_boss** (D18)</td>
      </tr>
      <tr>
        <td>metering</td>
        <td>none</td>
        <td>none</td>
        <td>**journal is the single telemetry store** (I3)</td>
      </tr>
      <tr>
        <td>bulk data</td>
        <td>n/a</td>
        <td>files ad hoc</td>
        <td>**claim-check FileRefs**, S3-range semantics, proven fs-identity (D19)</td>
      </tr>
      <tr>
        <td>distribution</td>
        <td>single box</td>
        <td>single box</td>
        <td>designed for **federation** (pods = machine-local runners)</td>
      </tr>
    </table>
    <p>The router answers "call a cheaper model." The orchestrators answer
"run several sessions at once." fractality answers a different
question: **"be the scheduler of an agent operating system"** — the
process table, call stack, quotas, kill, and accounting that a router
and a tmux grid both lack. The closest prior art in spirit is not in
this list at all; it is the young Linux kernel + GNU userland, which is
the analogy the foundation (PROP-001) builds on deliberately.</p>
  </section>
  <section title="What we take as validation, not code">
    <p>The field proves the *demand* (cheap-model routing and multi-agent runs
are wanted) and the *paved road* (the `ANTHROPIC_BASE_URL` env override
is how everyone connects GLM to Claude Code — confirmed by z.ai's own
docs this session). fractality's bet is that the missing layer is
supervision + accounting + a channel discipline, built as one
cross-platform Rust product rather than a proxy or a shell of scripts.</p>
  </section>
</spec>
