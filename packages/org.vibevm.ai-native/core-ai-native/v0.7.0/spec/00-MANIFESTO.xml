<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>The AI-Native Code Discipline — Manifesto</title>
  <p>**Discipline v0.2 · status: BETA · supersedes DISCIPLINE-CHARTER-v0.1**</p>
  <p>*This is the root document of the Discipline. The Discipline is a product in its own right: a language-independent set of principles, plus per-language projections, for writing code that is optimal for COMPREHENSION and SAFE MODIFICATION by AI agents — explicitly including weak readers. vibevm is the first pilot of this product, not its scope boundary.*</p>
  <p>*Maturity is marked everywhere. Some claims are benchmark-backed [E-strong], some are supported by adjacent evidence [E-mid], some are first-principles awaiting validation [E-hyp]. This document is a falsifiable beta, revised on pilot evidence only.*</p>
  <section title="0. What this is, in one paragraph">
    <p>Code generation by frontier models is already good. The unsolved problem is the rest of the lifecycle: a model — often a *different, weaker* model than the author — must later read, understand, and safely change that code. The Discipline optimizes for that. Its single design target is to **lower the minimum model capability required to maintain code safely**, so that a swarm of small agents can maintain code that a frontier model authored.</p>
  </section>
  <section title="1. The target function: author/reader asymmetry">
    <p>The economics that justify everything below: a strong author runs **once**; weak readers run **thousands of times**. So the discipline moves cost from maintenance-time to authoring-time. The strong author materializes expensive cognition into infrastructure — meta-layer, contracts, executable scaffolds, recorded rationale — and the weak swarm lives off that capital. **The Discipline is a capability-gap compensator.** A clever construct with no materialized rationale is theft from the reader pool.</p>
    <p>This is why "make the code simpler" is the wrong frame. We are not lowering the ceiling of what the code does; we are lowering the floor of who can safely touch it.</p>
  </section>
  <section title="2. The six axioms, projected to the language level">
    <p>The axioms are unchanged from the Charter; here is what each *means for how code and its metadata are written*.</p>
    <list ordered="false">
      <item>**A1 — Explainability.** Every artifact carries a machine-resolvable chain from code to requirement to rationale (`spec://` URIs, in-source `#[spec(...)]` edges). Unexplainable code is unmergeable. *Language-level:* identifiers, errors, and items are anchored to requirements, not free-floating.</item>
      <item>**A2 — Never pay twice.** Derived cognition is materialized content-addressed and dependency-tracked. *Language-level:* anything mechanically derivable (boilerplate, indexes, glue) is generated, not hand-maintained; the strong author's analysis is stored, not re-run.</item>
      <item>**A3 — Algorithmic floor.** Where a deterministic procedure exists, the LLM is forbidden; its outputs sink below the floor. *Language-level:* push as much intent as possible into machine-checkable form — types, exhaustiveness, contracts — so a checker, not a model, enforces it. **This is the engine of the central law (§3).**</item>
      <item>**A4 — Human accountability.** AI checks and proposes; the human is the accountable author; diffs stay human-reviewable. *Language-level:* no change is so clever a human cannot review it; determinism keeps diffs semantic.</item>
      <item>**A5 — Rule = code.** Every rule ships a checker or is explicitly a WISH. *Language-level:* a style rule with no linter is not a rule; it is documentation that decays (see §6). Rulebook health is the wish-ratio, not the page count.</item>
      <item>**A6 — Reality before aspiration.** Gates measure deltas against inventoried reality; debt, unimplemented intent, and contradiction are first-class tracked objects. *Language-level:* the code's actual state — not its intended state — is what tooling reasons over.</item>
    </list>
  </section>
  <section title="3. The central law: idiomatic inside the file, engineered around the file">
    <p>The strongest empirical result we found, dated and controlled: models collapse on out-of-distribution *surface syntax* (EsoLang-Bench, Mar 2026: frontier models 0–11% on esoteric languages they could solve trivially in Python) — **and** current agents largely *recover* that collapse through tools and in-session strategy (the Jun 2026 follow-up: the same tasks, 87–100% with file-editing + interpreter loops). In-context learning could **not** teach the unfamiliar surface (few-shot gave ~0 benefit); the recovery came from the verification loop and from building executable helpers, not from prose.</p>
    <p>The law that follows:</p>
    <quote>**Keep the code surface inside the training distribution. Put all the strictness into the meta-layer, the type system, and the verification loop — which sit AROUND the code, not in its syntax.**</quote>
    <p>Concretely: AI-Native Rust looks like *ordinary idiomatic Rust* at the token level (no invented notation, no exotic dialect — that would inherit the OOD penalty). What is unusual is everything around it: dense machine-checkable metadata, contract-bearing types, executable scaffolds, and a fast per-unit verification loop. We do not make the language stranger; we make its envelope stricter.</p>
  </section>
  <section title="4. Stricter, not simpler — and where the strictness lives">
    <p>"AI-native" is **more** discipline, not less — but the added strictness lives exclusively in machine-checkable form. More types, more contracts, more verification, more metadata — never more exotic syntax. Every constraint we add must be either enforced by a checker (A5) or it does not exist. A rule a model must *remember* is a rule that decays; a rule a compiler *enforces* is a rule that holds. The discipline's strictness is the compiler's strictness, extended.</p>
    <p>**Bans carry escape hatches.** Forbidden-by-default constructs (raw `unwrap` in domain logic, inline asm, proc-macro magic, stringly-typed protocols) remain legal *with machinery and a recorded reason* — the `unsafe` / `#[spec(deviates, reason)]` pattern. A ban with no escape hatch is a bug in the discipline; a deviation with no reason is a bug in the code.</p>
  </section>
  <section title="5. Runnable capital: explanation must be executable">
    <p>The second decisive result: weak agents given a *written* distillation of a strong agent's strategy barely improved; given an *executable* helper library carrying the same strategy, they leapt (Sonnet 4.6 on Brainfuck: 12→12 with text, 12→64 with runnable helpers). The mechanism: mid-tier models do not lack the idea; they cannot build the reusable code to carry it out.</p>
    <p>Therefore: **explanation capital must be runnable capital.** The meta-layer ships *executable scaffolds* — generators, typed builders, runnable contracts, differential oracles, compiled examples, local simulators (the nine classes; see `02-EXECUTABLE-SCAFFOLDS.xml`). Prose that *could* be a checker, a doctest, or a typed API is a WISH until it becomes one. A spec that says "use the registry pattern" is weaker than one that ships the registry macro plus a working example.</p>
    <p>**The honest boundary on this:** that result is about *generation* against an unfamiliar target. Transfer to *comprehension and modification* of in-distribution Rust is [E-mid] — plausible, not yet measured on our codebase. It is the primary thing the pilot must validate.</p>
  </section>
  <section title="6. Delivery: the discipline is not &quot;know N rules&quot;">
    <p>The owner's central worry is correct: a weak model cannot apply forty rules at once, in the right order. The resolution is that **rules are never all active at once.** Each rule/pattern is a *card* (`01-PATTERN-CARD-FORMAT.xml`) carrying a **Trigger** (when to switch on), a short **Routine** (≤7 steps), a **Checker** (machine verification), and a **Budget** (attention cost). The harness delivers only the cards whose triggers fire, as a small activation-matched set (lazy-push). Triggers escalate by cost:</p>
    <list ordered="true">
      <item>**Inline (edit-time)** — lint-detectable, fires in the per-cell loop. Most cards aim here.</item>
      <item>**Gate (merge-time)** — heavier checks (oracles, proofs) that need not run per keystroke.</item>
      <item>**Raid (scheduled)** — swept periodically across a layer when per-edit triggers cannot keep up (`03-RAID-PLAYBOOK.xml`).</item>
      <item>**Review (human/strong-agent)** — needs judgment a weak reader lacks.</item>
    </list>
    <p>This is grounded in the AGENTbench result (Feb 2026): bloated context *hurts* weak agents; minimal, sufficient context helps. The discipline therefore proselytizes minimal sufficiency — and obeys it: this package is a full authoring/review artifact, but runtime delivery to a weak reader is an *extract* (the card's ops block), never the whole corpus.</p>
  </section>
  <section title="7. The honest boundary (what we do not yet know)">
    <p>This is a beta. Stated plainly so the pilot can falsify it:</p>
    <list ordered="false">
      <item>**Transfer is unproven.** The executable-scaffold result is generation, not modification. [E-mid].</item>
      <item>**There is a floor.** Even with executable scaffolds, the weakest models (Haiku-4.5-class, and our target Qwen-32B may sit lower on some axes) did *not* recover — scaffolds amplify capability, they do not create it. The discipline lowers the floor; it does not remove it.</item>
      <item>**Surface-distribution is current.** The "stay in-distribution" law is tied to today's model generation; it carries a sunset (R-050) and must be re-checked as models change.</item>
      <item>**Measurement is deferred by design.** We build the core on internal logic plus others' published evidence, and instrument later, at a buyer's expense. Every card therefore carries a falsifiable `prediction` in place of a present measurement.</item>
    </list>
    <p>A discipline that names its own failure modes is more trustworthy than one that hides them. This one names them.</p>
  </section>
  <section title="8. The package map">
    <p>**Guiding layer (T1, language-independent):**</p>
    <list ordered="false">
      <item>`00-MANIFESTO.xml` — this document.</item>
      <item>`01-PATTERN-CARD-FORMAT.xml` — the format every pattern is written in (GoF × JEP × operational layer).</item>
      <item>`02-EXECUTABLE-SCAFFOLDS.xml` — the nine scaffold classes; the runnable-capital catalog.</item>
      <item>`03-RAID-PLAYBOOK.xml` — layered, scheduled refactoring campaigns (raids).</item>
      <item>`04-SWEEP-PLAYBOOK.xml` — the standing sweep that holds a tree inside the Discipline between campaigns.</item>
      <item>`05-CAMPAIGN-FORM.xml` — the campaign paper trail: cold-executable plans, baselines, predictions, logs, reports.</item>
      <item>`06-WAL-CONVENTION.xml` — session-durable project state (optional but preferred).</item>
    </list>
    <p>**Mechanisms (T1, language-independent; implemented per-stack):**</p>
    <list ordered="false">
      <item>`mechanisms/ENGINE-CONFORM-v0.1.xml` — the conformance engine: fact store, rules-as-queries, SARIF, ratchet baseline.</item>
      <item>`mechanisms/PROP-014-specmap-bidirectional-traceability.xml` — spec↔code traceability: anchors, revisions, tags, the index.</item>
      <item>`mechanisms/BROWNFIELD-PROTOCOL-v0.1.xml` — terraforming unfinished projects: inventory-not-gate, the registries, xfail-strict, characterization.</item>
      <item>`mechanisms/LEDGER-INTENT-v0.1.xml` — the intent ledger: facts vs interpretations, epoch-keyed cache.</item>
    </list>
    <p>Spec-unit URIs for this package read `spec://org.vibevm.ai-native/core-ai-native/&lt;docpath&gt;#&lt;anchor&gt;` (e.g. `spec://org.vibevm.ai-native/core-ai-native/mechanisms/ENGINE-CONFORM-v0.1#rules`); the Rust implementations ship in `stack:org.vibevm.ai-native/rust-ai-native-lang` (`rust-ai-native-conform`, `rust-ai-native-specmap`, `rust-ai-native`).</p>
    <p>**Language projections (T2):**</p>
    <list ordered="false">
      <item>`rust/GUIDE-AI-NATIVE-RUST.md` — the law and scaffolds projected onto Rust; supersedes GUIDE-RUST-v0.1. (Pilot language.)</item>
      <item>`rust/tools/rust-ai-native-tcg.md` — token-level type-aware constrained generation for Rust (very-far-future; carries the family prefix per PROP-028 §2.4). The AGENTIC delivery shipped first: `rust/tools/vibe-agentic-tcg-rust.md` — the consultation oracle over rust-analyzer.</item>
      <item>`typescript/GUIDE-AI-NATIVE-TYPESCRIPT.md` — projected onto TypeScript (typed language only; JS guide separate). The language where the generation-time type oracle already exists and codemods are mature.</item>
      <item>`typescript/tools/typescript-ai-native-tcg.md` — type-aware constrained generation for TypeScript (a wrap-and-extend of existing PLDI'25 work, not from scratch).</item>
      <item>Other languages (Python, C++, Go, Java, Kotlin) projected after Rust + TypeScript are validated.</item>
    </list>
    <p>**Cards (the patterns) — shipped per-language by each stack:**</p>
    <list ordered="false">
      <item>`cards/INDEX.md` — registry, axes, trigger modes (one per language stack).</item>
      <item>`cards/scaffold-{a..i}-*.md` — the nine scaffold patterns in their per-language card shape. The core (T1) defines the card FORMAT (`01-PATTERN-CARD-FORMAT.xml`) and the scaffold CATALOG (`02-EXECUTABLE-SCAFFOLDS.xml`), both language-neutral; each language stack ships the concrete `cards/` (Rust cards in `stack-rust-ai-native`, TypeScript cards in `stack-typescript-ai-native`), so the weak-reader runtime surface for an edit is a language-matched Band-3 block.</item>
    </list>
    <p>**Appendix (synthesis provenance):**</p>
    <list ordered="false">
      <item>`appendix/CONTRADICTION-MAP.xml` — where sources and hypotheses conflict, and the resolutions.</item>
      <item>`appendix/ATLAS.xml` — the findings ledger rendered for humans (generated from `findings.jsonl`).</item>
    </list>
    <p>The vibevm-specific adoption plan lives OUTSIDE this package, in `vibevm-terraform/`, because the Discipline is the product and vibevm is its pilot.</p>
  </section>
</spec>
