<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>Executable Scaffolds — The Runnable-Capital Catalog</title>
  <p>**Discipline v0.2 · status: BETA · T1**</p>
  <p>*The operational core of "explanation capital must be runnable capital" (Manifesto §5). Nine classes of artifact that carry a strong author's cognition in a form a weak reader CONSUMES rather than re-derives. Each pattern is also a card, shipped per-language in each stack's `cards/`; this catalog is the language-neutral overview and the build order.*</p>
  <section title="0. Definition">
    <p>An **executable scaffold** has two properties at once:</p>
    <list ordered="true">
      <item>**It runs or machine-checks** — compiles, executes, or evaluates as a checker, emitting a pass/fail (or typed) signal. Prose has no signal.</item>
      <item>**It carries cognition** — a weak reader USES it instead of re-deriving the understanding.</item>
    </list>
    <p>Prose about a pattern fails (1). A random utility fails (2). A scaffold is materialized understanding with a pulse.</p>
    <p>**Empirical basis.** EsoLang follow-up (Jun 2026): weak agents given a *written* strategy barely moved (Sonnet 12→12); given an *executable* helper library, they leapt (12→64). Mechanism: mid-tier models lack not the idea but the ability to build the reusable code that carries it (R2C-008).</p>
    <p>**Transfer tags.** [E-strong] = directly supported by that generation result; [E-mid] = supported by other ledger findings; [E-hyp] = first-principles, validate in pilot. The result is about *generation*; transfer to *modification* is the pilot's job.</p>
    <p>**The floor.** Even with the executable library, Haiku-4.5 stayed near the floor. Scaffolds amplify capability; they do not create it.</p>
  </section>
  <section title="1. The nine classes">
    <table>
      <tr>
        <td>Class</td>
        <td>Name</td>
        <td>Carries</td>
        <td>Transfer</td>
        <td>Card</td>
      </tr>
      <tr>
        <td>**A**</td>
        <td>Generators / codegen</td>
        <td>structural decisions, as named inputs to an emitter</td>
        <td>[E-strong]</td>
        <td>`scaffold-a-generators`</td>
      </tr>
      <tr>
        <td>**B**</td>
        <td>Typed builders / typestate</td>
        <td>protocol correctness, as types the compiler checks</td>
        <td>[E-mid]</td>
        <td>`scaffold-b-typed-builders`</td>
      </tr>
      <tr>
        <td>**C**</td>
        <td>Runnable contracts</td>
        <td>invariants, as executing assertions/proofs</td>
        <td>[E-mid]</td>
        <td>`scaffold-c-runnable-contracts`</td>
      </tr>
      <tr>
        <td>**D**</td>
        <td>Differential / characterization oracles</td>
        <td>behavior, as a runnable old-vs-new check</td>
        <td>[E-mid]</td>
        <td>`scaffold-d-differential-oracle`</td>
      </tr>
      <tr>
        <td>**E**</td>
        <td>Per-cell fast verification loop</td>
        <td>the substrate that makes all signals fast enough</td>
        <td>[E-strong]</td>
        <td>`scaffold-e-fast-loop`</td>
      </tr>
      <tr>
        <td>**F**</td>
        <td>Structured, REQ-citing diagnostics</td>
        <td>debugging cognition, in the error text</td>
        <td>[E-mid]</td>
        <td>`scaffold-f-structured-diagnostics`</td>
      </tr>
      <tr>
        <td>**G**</td>
        <td>Executable examples / doctests</td>
        <td>canonical usage, as compiled examples that cannot lie</td>
        <td>[E-strong]</td>
        <td>`scaffold-g-doctests`</td>
      </tr>
      <tr>
        <td>**H**</td>
        <td>Local simulators / reference models</td>
        <td>subsystem semantics, as a runnable model</td>
        <td>[E-strong]</td>
        <td>`scaffold-h-simulators`</td>
      </tr>
      <tr>
        <td>**I**</td>
        <td>Scaffolded edit operations / codemods</td>
        <td>a multi-file change, as one checked operation</td>
        <td>[E-hyp]</td>
        <td>`scaffold-i-codemods`</td>
      </tr>
    </table>
    <p>Each row's full Applicability/Routine/Checker is in its card.</p>
  </section>
  <section title="2. Build order (transfer-strength × weak-reader leverage)">
    <list ordered="true">
      <item>**E (fast loop)** — substrate; nothing pays off without it. First.</item>
      <item>**G (doctests) + F (diagnostics)** — cheapest runnable capital; guaranteed-truthful few-shot signal. [E-strong].</item>
      <item>**B (typed builders) + C (contracts)** — convert hallucinations to compile/assert failures at seams. [E-mid].</item>
      <item>**D (differential oracles)** — the modification-specific safety net; the class most worth validating for our actual task. [E-mid].</item>
      <item>**A (generators) + H (simulators)** — highest ceiling, highest cost. [E-strong].</item>
      <item>**I (codemods)** — potentially decisive for the swarm, but [E-hyp]; prototype and measure before the guide commits to it.</item>
    </list>
  </section>
  <section title="3. The scaffold-reality checklist (all four must hold)">
    <list ordered="false">
      <item>[ ] **Runs/checks:** emits pass/fail or typed signal, not prose.</item>
      <item>[ ] **Carries cognition:** encodes a decision a weak reader would otherwise re-derive.</item>
      <item>[ ] **Fast enough:** signal returns inside the per-cell loop budget (&lt;~60s).</item>
      <item>[ ] **Cannot silently lie:** if it drifts from reality it FAILS (compile error / assert / red test), never misleads. (The doctest-vs-comment distinction.)</item>
    </list>
  </section>
  <section title="4. The build/use boundary (a pilot hypothesis worth stating)">
    <p>There may be a sharp capability line between *building* a scaffold (Classes A, I — emit/parameterize) and *using* one (Classes G, H — consume). Evidence: Haiku did not improve even with the executable library, suggesting the barrier is not the scaffold's presence but the ability to wield it. If true, the weakest swarm tier should receive consume-only scaffolds (G/H) and invoke-only operations (I as a fixed command), never build-it-yourself scaffolds. Prime pilot question (R4).</p>
  </section>
</spec>
