<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">TCG-PROTOCOL v0.1 — the oracle wire protocol, both hops</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Status: v0.1 — authored with AGENTIC-TCG-TS-PLAN v0.1 (Phase 1),
implemented by its Phases 2–4.**</status-line></p>
  <p><companion-document fact="true" status="impl/done">The process model is
[`TCG-ORACLE-v0.1`](TCG-ORACLE-v0.1.xml).</companion-document></p>
  <p><DOCUMENT-OWNS-THE-MESSAGE-GRAMMAR fact="true" status="impl/done">This document owns the MESSAGE
GRAMMAR: framing, envelopes, every operation's request/response shape,
the enrichment fields the Rust middle layer adds, and the error
taxonomy.</DOCUMENT-OWNS-THE-MESSAGE-GRAMMAR></p>
  <p><ONE-SHAPE-SERVES-BOTH-HOPS fact="true" status="impl/done">One shape serves both hops — vibe-mcp ⇄ `typescript-ai-native-tcg
serve` and `typescript-ai-native-tcg` ⇄ `node oracle.ts` — the middle layer ADDS
fields, it never reshapes.</ONE-SHAPE-SERVES-BOTH-HOPS></p>
  <framing title="1. Framing and envelopes">
    <p><kind-line-framing fact="true" status="impl/done">`req r1`</kind-line-framing></p>
    <p><NDJSON-DUPLEX-FRAMING fact="true" status="impl/done">NDJSON duplex: one JSON object per line, UTF-8, `\n`-terminated, both
directions.</NDJSON-DUPLEX-FRAMING></p>
    <p><REQUESTS-AND-RESPONSES-CORRELATE-BY-ID fact="true" status="impl/done">Requests and responses correlate by caller-chosen `id`
(number, unique per in-flight request).</REQUESTS-AND-RESPONSES-CORRELATE-BY-ID></p>
    <p><version-rides-every-frame-lead fact="true" status="impl/done">The protocol version rides
every frame:</version-rides-every-frame-lead></p>
    <fence lang="jsonc">// request
{"proto": 1, "id": 7, "op": "validate", "params": { /* per-op */ }}
// success
{"proto": 1, "id": 7, "ok": true, "result": { /* per-op */ }}
// failure (op-grain, session survives)
{"proto": 1, "id": 7, "ok": false,
 "error": {"kind": "&lt;taxonomy §4&gt;", "detail": "…", "recipe": "…?"}}</fence>
    <p><ORACLE-PROTOCOL-IS-INDEPENDENT-OF-EXTRACT-PROTOCOL fact="true" status="impl/done">`ORACLE_PROTOCOL = 1` is independent of ts-extract's `PROTOCOL = 1`
(different channel, different message set; the constants version
independently).</ORACLE-PROTOCOL-IS-INDEPENDENT-OF-EXTRACT-PROTOCOL></p>
    <p><PROTO-MISMATCH-AND-UNKNOWN-IDS fact="true" status="impl/done">A `proto` mismatch is a `protocol` error; responses to
unknown `id`s are a bridge bug and dropped with a stderr note.</PROTO-MISMATCH-AND-UNKNOWN-IDS></p>
    <p><REQUESTS-MAY-BE-PIPELINED fact="true" status="impl/done">Requests
MAY be pipelined; responses come in completion order (the oracle is
single-threaded per op today, so in practice FIFO — callers must still
match by `id`, not order).</REQUESTS-MAY-BE-PIPELINED></p>
  </framing>
  <ops title="2. Operations">
    <p><kind-line-ops fact="true" status="impl/done">`req r2`</kind-line-ops></p>
    <p><POSITIONS-ARE-ONE-BASED-LINE-ZERO-BASED-CHARACTER fact="true" status="impl/done">Positions are `{line, character}`, 1-based line, 0-based character (the
TypeScript convention surfaced honestly).</POSITIONS-ARE-ONE-BASED-LINE-ZERO-BASED-CHARACTER></p>
    <p><PATHS-ARE-ROOT-RELATIVE-WITH-FORWARD-SLASHES fact="true" status="impl/done">Paths are project-root-
relative with forward slashes.</PATHS-ARE-ROOT-RELATIVE-WITH-FORWARD-SLASHES></p>
    <facts ordered="false">
      <OP-INIT fact="true" status="impl/done">**`init`** `{root, cells_dir?, seam?}` →
  `{ts_version, config_file, root_files}` — builds the service (ORACLE
  §2–3). `cells_dir`/`seam` are policy-derived DATA the Rust layer
  passes down (the node side never reads `conform.toml` itself); they
  feed the `scope` op's cell/seam/branded context and default to
  none/`"index"`. Re-`init` on a live oracle rebuilds config and
  policy; overlays are cleared.</OP-INIT>
      <OP-UPDATE fact="true" status="impl/done">**`update`** `{file, content | null}` → `{version}` — set/clear an
  overlay (ORACLE §3).</OP-UPDATE>
      <OP-VALIDATE fact="true" status="impl/done">**`validate`** `{file, content?}` →
  `{diagnostics: [{code, category, message, line, character}],
    facts: [/* ts-extract fact shapes */],
    markers: [/* §9 marker shapes */], degraded}` — the fact/marker
  arrays reuse the ts-extract record vocabulary verbatim (`ts_unsafe`,
  `import`, `item`, `file_metrics`; `{tag, uri, reason, symbol, line}`)
  so one serde vocabulary serves both tools.</OP-VALIDATE>
      <OP-SCOPE fact="true" status="impl/done">**`scope`** `{file, position?}` →
  `{symbols: [{name, kind, type_text}], cell, seam_file,
    branded: [{name, seam, heuristic}]}`.</OP-SCOPE>
      <OP-COMPLETE fact="true" status="impl/done">**`complete`** `{file, position, content?, prefix?, max?}` →
  `{entries: [{name, kind, type_text, unsafe}]}` — `prefix` filters by
  name prefix and `max` caps the set (default 50) BEFORE the per-entry
  checker details are computed: type text and the `unsafe` flag are
  entry-grain checker work, affordable only after the cut. A caller
  that wants the raw thousand-entry universe passes no prefix and a
  large `max`, and pays for it knowingly.</OP-COMPLETE>
      <OP-TYPE fact="true" status="impl/done">**`type`** `{file, position, content?}` →
  `{display, documentation}`.</OP-TYPE>
      <OP-SHUTDOWN fact="true" status="impl/done">**`shutdown`** `{}` → `{}` then exit 0.</OP-SHUTDOWN>
    </facts>
  </ops>
  <enrichment title="3. The enrichment hop (Rust adds, never reshapes)">
    <p><kind-line-enrichment fact="true" status="impl/done">`req r3`</kind-line-enrichment></p>
    <p><serve-widens-two-responses-lead fact="true" status="impl/done">`typescript-ai-native-tcg serve` speaks §1–§2 upward unchanged and widens two
responses with policy-derived fields (policy = the project's
`conform.toml`, read at init; ORACLE §4 keeps the node side
policy-free):</serve-widens-two-responses-lead></p>
    <facts ordered="false">
      <ENRICHMENT-VALIDATE-CONFORM-FINDINGS-AND-ADVICE fact="true" status="impl/done">`validate.result` gains
  `conform_findings: [{rule, message, line, baselined}]` — the REAL
  rule set (`ts-unsafe-in-domain`, `ts-cell-isolation`, file budget)
  run over the returned facts via `conform_core::check`, each finding
  flagged against the project's frozen ratchet baseline — and
  `advice: [string]` (Class-F strings citing `spec://` REQs).</ENRICHMENT-VALIDATE-CONFORM-FINDINGS-AND-ADVICE>
      <ENRICHMENT-SCOPE-BRANDED-COMPLETION fact="true" status="impl/done">`scope.result.branded` is completed from seam files per the policy's
  `cells_dir`/`seam`, and `advice` may name the branded constructor a
  bare primitive at this seam should use.</ENRICHMENT-SCOPE-BRANDED-COMPLETION>
      <ENRICHMENT-COMPLETE-UNSAFE-FINALISED fact="true" status="impl/done">`complete.result.entries[].unsafe` is finalised against the policy
  (the node side flags candidates; policy decides).</ENRICHMENT-COMPLETE-UNSAFE-FINALISED>
    </facts>
    <p><ENRICHMENT-FIELDS-ARE-ADDITIVE fact="true" status="impl/done">A consumer that talks to the oracle directly (no Rust layer) gets
well-formed §2 responses with no enrichment fields — the fields are
additive, and their absence means "no policy layer", not an error.</ENRICHMENT-FIELDS-ARE-ADDITIVE></p>
  </enrichment>
  <errors title="4. Error taxonomy">
    <p><kind-line-errors fact="true" status="impl/done">`req r4`</kind-line-errors></p>
    <p><five-error-kinds-lead fact="true" status="impl/done">Five kinds, each actionable, each carried in the §1 error object (and
mirrored as typed variants in `typescript-ai-native-tcg-bridge`):</five-error-kinds-lead></p>
    <table>
      <tr>
        <td>kind</td>
        <td>meaning</td>
        <td>recipe carried</td>
      </tr>
      <tr>
        <td><ROW-ERROR-NODE-MISSING fact="true" status="impl/done">`node-missing`</ROW-ERROR-NODE-MISSING></td>
        <td><ROW-ERROR-NODE-MISSING-MEANING fact="true" status="impl/done">node not spawnable</ROW-ERROR-NODE-MISSING-MEANING></td>
        <td><ROW-ERROR-NODE-MISSING-RECIPE-CARRIED fact="true" status="impl/done">install node &gt;= 22.6</ROW-ERROR-NODE-MISSING-RECIPE-CARRIED></td>
      </tr>
      <tr>
        <td><ROW-ERROR-TYPESCRIPT-UNRESOLVABLE fact="true" status="impl/done">`typescript-unresolvable`</ROW-ERROR-TYPESCRIPT-UNRESOLVABLE></td>
        <td><ROW-ERROR-TYPESCRIPT-UNRESOLVABLE-MEANING fact="true" status="impl/done">consumer install absent (ORACLE §2)</ROW-ERROR-TYPESCRIPT-UNRESOLVABLE-MEANING></td>
        <td><ROW-ERROR-TYPESCRIPT-UNRESOLVABLE-RECIPE-CARRIED fact="true" status="impl/done">`npm install -D typescript`</ROW-ERROR-TYPESCRIPT-UNRESOLVABLE-RECIPE-CARRIED></td>
      </tr>
      <tr>
        <td><ROW-ERROR-ORACLE-CRASHED fact="true" status="impl/done">`oracle-crashed`</ROW-ERROR-ORACLE-CRASHED></td>
        <td><ROW-ERROR-ORACLE-CRASHED-MEANING fact="true" status="impl/done">child died / stream closed mid-session</ROW-ERROR-ORACLE-CRASHED-MEANING></td>
        <td><ROW-ERROR-ORACLE-CRASHED-RECIPE-CARRIED fact="true" status="impl/done">respawn guidance; the bridge may retry once</ROW-ERROR-ORACLE-CRASHED-RECIPE-CARRIED></td>
      </tr>
      <tr>
        <td><ROW-ERROR-PROTOCOL fact="true" status="impl/done">`protocol`</ROW-ERROR-PROTOCOL></td>
        <td><ROW-ERROR-PROTOCOL-MEANING fact="true" status="impl/done">unparseable frame, `proto` mismatch, unknown op</ROW-ERROR-PROTOCOL-MEANING></td>
        <td><ROW-ERROR-PROTOCOL-RECIPE-CARRIED fact="true" status="impl/done">version/upgrade note; unknown-op errors list the known ops</ROW-ERROR-PROTOCOL-RECIPE-CARRIED></td>
      </tr>
      <tr>
        <td><ROW-ERROR-TIMEOUT fact="true" status="impl/done">`timeout`</ROW-ERROR-TIMEOUT></td>
        <td><ROW-ERROR-TIMEOUT-MEANING fact="true" status="impl/done">no response within the caller's budget</ROW-ERROR-TIMEOUT-MEANING></td>
        <td><ROW-ERROR-TIMEOUT-RECIPE-CARRIED fact="true" status="impl/done">the op and budget, for tuning</ROW-ERROR-TIMEOUT-RECIPE-CARRIED></td>
      </tr>
    </table>
    <p><ERRORS-ARE-OP-GRAIN fact="true" status="impl/done">Errors are OP-GRAIN wherever possible (the session survives, ORACLE
§5); only `oracle-crashed` is session-grain.</ERRORS-ARE-OP-GRAIN></p>
  </errors>
  <compat title="5. Compatibility rules">
    <p><kind-line-compat fact="true" status="impl/done">`req r5`</kind-line-compat></p>
    <p><COMPAT-ADDITIVE-EVOLUTION fact="true" status="impl/done">Additive evolution within a `proto`: new OPTIONAL request params, new
response fields, and new advice/finding entries are non-breaking; a
consumer ignores what it does not know.</COMPAT-ADDITIVE-EVOLUTION></p>
    <p><COMPAT-BREAKING-CHANGES-BUMP-THE-CONSTANT fact="true" status="impl/done">Renames, type changes, and
semantic changes to existing fields bump `ORACLE_PROTOCOL`, and the
bridge treats a mismatch as its own error class — the same
cache-retirement posture the extract bridge established.</COMPAT-BREAKING-CHANGES-BUMP-THE-CONSTANT></p>
    <p><REPLAY-GOLDENS-PIN-BOTH-SIDES fact="true" status="spec/done">Scripted
doubles pin the INNER side in this package's tests (the no-node double,
`crates/typescript-ai-native-tcg-bridge/src/transport.rs:312`); the
fact-parity test (ORACLE §1) pins the vocabulary shared with
ts-extract. The OUTER shape is not yet pinned — no recorded stream is
checked into the package and no test constructs an outer frame.</REPLAY-GOLDENS-PIN-BOTH-SIDES></p>
  </compat>
</spec>
