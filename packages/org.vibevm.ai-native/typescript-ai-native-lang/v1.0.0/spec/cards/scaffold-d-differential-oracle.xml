<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">CARD: scaffold-d-differential-oracle — Differential / Characterization Oracle (TypeScript)</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Discipline v0.2 · BETA · T2 · TypeScript**</status-line></p>
  <p><reference-instance-note fact="true" status="impl/done">*Reference instance of the AI-Native Pattern Card format, TypeScript projection.*</reference-instance-note></p>
  <p><demonstrates-all-three-bands fact="true" status="impl/done">*Demonstrates all three bands, especially the operational Band 3.*</demonstrates-all-three-bands></p>
  <p><card-is-beta fact="true" status="impl/done">*This card is itself BETA (its checker is specified but not yet implemented — there is no TypeScript pilot codebase yet).*</card-is-beta></p>
  <band-one-identity title="Band 1 — Identity &amp; Recognition">
    <p><CLASSIFICATION fact="true" status="impl/done">**Classification:** layer = E (Verification coupling); mechanism = scaffold class D.</CLASSIFICATION></p>
    <p><INTENT fact="true" status="impl/done">**Intent:** When code is replaced or refactored, pin its observable behavior with a runnable check that compares the new implementation against the old one (differential) or against a captured baseline (characterization), so that a reader — especially a weak one — can change code freely and receive a pass/fail signal on whether behavior moved.</INTENT></p>
    <p><ALSO-KNOWN-AS fact="true" status="spec/done">**Also Known As:** golden test; snapshot test; characterization test (Feathers); approval test; back-to-back test; differential testing; `fast-check` model-based / property-based test.</ALSO-KNOWN-AS></p>
    <p><applicability-recognition-lead fact="true" status="impl/done">**Applicability / Recognition:** Apply when ANY of these signals are present —</applicability-recognition-lead></p>
    <facts ordered="false">
      <SIGNAL-CELL-IS-BEING-REPLACED fact="true" status="impl/done">a cell is being *replaced* or its internals *rewritten* while its contract is meant to stay fixed (the replacement protocol, R-040, guide §11);</SIGNAL-CELL-IS-BEING-REPLACED>
      <SIGNAL-LEGACY-BEHAVIOR-IS-UNDERSTOOD-BY-NOBODY fact="true" status="impl/done">legacy behavior exists that nobody fully understands but must be preserved (no spec, only observed behavior);</SIGNAL-LEGACY-BEHAVIOR-IS-UNDERSTOOD-BY-NOBODY>
      <SIGNAL-REFACTOR-SPANS-MULTIPLE-FILES fact="true" status="impl/done">a refactor spans multiple files and the reader cannot prove by inspection that behavior is unchanged;</SIGNAL-REFACTOR-SPANS-MULTIPLE-FILES>
      <SIGNAL-WEAK-AGENT-NEEDS-A-SAFETY-NET fact="true" status="impl/done">a weak agent is assigned a modification task and needs a safety net it cannot derive itself.</SIGNAL-WEAK-AGENT-NEEDS-A-SAFETY-NET>
    </facts>
    <p><DETECTOR-SEED fact="true" status="impl/done">*Detector seed:* a diff that modifies the body of an item carrying `@implements spec://…` (or its sidecar edge) without a corresponding oracle artifact in the cell's test module → recognition fires.</DETECTOR-SEED></p>
  </band-one-identity>
  <band-two-justification title="Band 2 — Justification &amp; Tradeoffs">
    <p><MOTIVATION fact="true" status="spec/done">**Motivation:** A Qwen-32B-class agent is asked to optimize a parser cell authored by Opus. It rewrites the hot loop. By inspection, neither the agent nor a fast human reviewer can be sure the 200-line change preserved behavior across edge cases. With a differential oracle — `fast-check` feeding identical generated inputs to `oldParse` and `newParse` and asserting equal outputs — the agent gets an immediate, mechanical verdict: behavior held, or here is a minimized (shrunk) counterexample. The expensive cognition ("what are all the edge cases?") was materialized once, by the author, as a runnable harness; the weak agent consumes the verdict instead of re-deriving the edge-case analysis.</MOTIVATION></p>
    <p><structure-and-participants-lead fact="true" status="impl/done">**Structure &amp; Participants:**</structure-and-participants-lead></p>
    <facts ordered="false">
      <PARTICIPANT-SUBJECT-OLD fact="true" status="impl/done">*Subject-old* — the prior implementation (kept temporarily as `oldParse`, or captured as `vitest` snapshots).</PARTICIPANT-SUBJECT-OLD>
      <PARTICIPANT-SUBJECT-NEW fact="true" status="impl/done">*Subject-new* — the replacement.</PARTICIPANT-SUBJECT-NEW>
      <PARTICIPANT-INPUT-SOURCE fact="true" status="impl/done">*Input source* — a `fast-check` arbitrary, a recorded production-input set, or a snapshot corpus.</PARTICIPANT-INPUT-SOURCE>
      <PARTICIPANT-COMPARATOR fact="true" status="impl/done">*Comparator* — the equality/equivalence predicate (deep-equal, or domain-specific tolerance).</PARTICIPANT-COMPARATOR>
      <PARTICIPANT-ORACLE-HARNESS fact="true" status="impl/done">*Oracle harness* — the runnable `vitest`/`fast-check` test binding these, living in the cell's test module.</PARTICIPANT-ORACLE-HARNESS>
    </facts>
    <p><COLLABORATIONS fact="true" status="impl/done">**Collaborations:** Pairs with Class B (branded/typed surfaces shrink the input space the oracle must cover) and Class C (contracts define what "equivalent" means). Consumes Class E (the per-cell fast loop runs the oracle). Emits Class F diagnostics (a failure cites the violated REQ + the minimized counterexample). In a raid (§3 of the format), this card is the *differential-safety* gate that every behavior-changing card application must pass.</COLLABORATIONS></p>
    <p><goals-and-non-goals-lead fact="true" status="impl/done">**Goals / Non-Goals:**</goals-and-non-goals-lead></p>
    <facts ordered="false">
      <GOALS fact="true" status="impl/done">*Goals:* detect unintended behavior change during replacement/refactor; give weak readers a modification safety net; make "behavior preserved" a machine fact, not a claim.</GOALS>
      <NON-GOALS fact="true" status="impl/done">*Non-Goals:* NOT a correctness proof (it checks new-vs-old agreement, so it inherits any bug the old code had); NOT a substitute for the spec (it pins behavior, it does not justify it); NOT for greenfield code with no prior behavior to differ against.</NON-GOALS>
    </facts>
    <p><consequences-lead fact="true" status="impl/done">**Consequences:**</consequences-lead></p>
    <facts ordered="false">
      <CONSEQUENCE-REFACTORING-BECOMES-SAFE fact="true" status="spec/done">(+) The reader can refactor aggressively; the net catches behavior drift mechanically.</CONSEQUENCE-REFACTORING-BECOMES-SAFE>
      <CONSEQUENCE-IMPLEMENTATION-AND-CONTRACT-VARY-INDEPENDENTLY fact="true" status="spec/done">(+) Decouples "change the implementation" from "preserve the contract" — they vary independently.</CONSEQUENCE-IMPLEMENTATION-AND-CONTRACT-VARY-INDEPENDENTLY>
      <CONSEQUENCE-ARBITRARY-AND-COMPARATOR-COST-EFFORT fact="true" status="spec/done">(−) Cost: authoring the `fast-check` arbitrary and comparator; maintaining snapshots (which can rot — they must fail loudly when stale, run under `--ci`, never `--update` auto-rewriting silently).</CONSEQUENCE-ARBITRARY-AND-COMPARATOR-COST-EFFORT>
      <CONSEQUENCE-CHARACTERIZATION-ENSHRINES-CURRENT-BEHAVIOR fact="true" status="spec/done">(−) Characterization variant *enshrines current behavior including its bugs* — must be paired with a spec edge that says which behaviors are intentional vs incidental.</CONSEQUENCE-CHARACTERIZATION-ENSHRINES-CURRENT-BEHAVIOR>
    </facts>
    <p><alternatives-lead fact="true" status="impl/done">**Alternatives:**</alternatives-lead></p>
    <facts ordered="false">
      <ALTERNATIVE-FORMAL-PROOF fact="true" status="spec/done">*Full formal proof:* in Rust this is the Kani/Creusot option; **TypeScript has no comparable mainstream formal-verification tool**, so the differential/property oracle carries proportionally more of the modification-safety load here — a genuine TS-vs-Rust asymmetry, not a gap in this card.</ALTERNATIVE-FORMAL-PROOF>
      <ALTERNATIVE-MANUAL-REVIEW fact="true" status="spec/done">*Manual review:* the status quo; fails exactly where we need it (large multi-file edits, weak readers).</ALTERNATIVE-MANUAL-REVIEW>
      <ALTERNATIVE-FRESH-UNIT-TESTS fact="true" status="spec/done">*Unit tests written fresh:* test what the author thought to test; the differential oracle tests behavior the author never enumerated. Prefer differential when preserving opaque legacy behavior.</ALTERNATIVE-FRESH-UNIT-TESTS>
    </facts>
    <p><risks-and-assumptions-lead fact="true" status="impl/done">**Risks &amp; Assumptions:**</risks-and-assumptions-lead></p>
    <facts ordered="false">
      <RISK-OLD-IMPLEMENTATION-IS-AVAILABLE fact="true" status="spec/done">Assumes the old implementation is available or its behavior is capturable (as a snapshot).</RISK-OLD-IMPLEMENTATION-IS-AVAILABLE>
      <RISK-INPUTS-ARE-GENERATABLE-WITH-COVERAGE fact="true" status="spec/done">Assumes inputs are *generatable* with enough coverage; a weak `fast-check` arbitrary gives false confidence.</RISK-INPUTS-ARE-GENERATABLE-WITH-COVERAGE>
      <RISK-SUNSET fact="true" status="spec/done">*Sunset condition:* if generation-time tools (`typescript-ai-native-tcg`, renamed from `vibe-tcg-ts` by the family-prefix policy) plus full contracts ever make behavior-preservation statically provable for a class of cells, the differential oracle becomes redundant for that class and retires there. The condition is not met today: the binary of that name ships the *consultation* oracle (relay plus one-shot validate/scope/complete/type), while the token-level generation tier it turns on is still spec-stage in this stack's own tool brief.</RISK-SUNSET>
      <RISK-TRANSFER fact="true" status="spec/done">Transfer risk: the value of executable scaffolds for *modification* (vs generation) is [E-mid], not yet measured on a TypeScript codebase — this card is a prime pilot validation target on the forthcoming VibeVM TypeScript surface.</RISK-TRANSFER>
    </facts>
    <p><EVIDENCE-AND-TRANSFER-STRENGTH fact="true" status="spec/done">**Evidence &amp; Transfer-strength:** findings R-040 (replacement protocol, production), R2C-008 (executable scaffolds transformative for weak agents, benchmark), Feathers characterization method (production). Evidence class: production + benchmark. Transfer tag: **[E-mid]** (executable-scaffold value shown for generation; modification transfer to be validated in the TypeScript pilot).</EVIDENCE-AND-TRANSFER-STRENGTH></p>
  </band-two-justification>
  <band-three-operation title="Band 3 — Operation">
    <p><TRIGGER fact="true" status="impl/done">**Trigger:** WHEN a diff modifies the body of an item bearing `@implements spec://…`, OR a cell is marked for replacement, OR a refactor touches &gt; 1 file in a cell whose contract is unchanged — THEN apply this card before merge.</TRIGGER></p>
    <p><MODE fact="true" status="impl/done">**Mode:** gate (runs at the cell's verification gate, not per keystroke).</MODE></p>
    <p><routine-lead fact="true" status="impl/done">**Routine** (≤7 steps, each verifiable):</routine-lead></p>
    <facts ordered="true">
      <ROUTINE-IDENTIFY-THE-BEHAVIORAL-SURFACE fact="true" status="impl/done">Identify the behavioral surface to preserve (the seam's public functions).</ROUTINE-IDENTIFY-THE-BEHAVIORAL-SURFACE>
      <ROUTINE-KEEP-OLD-REACHABLE fact="true" status="impl/done">Keep `old` reachable (rename to `oldParse`, or capture `vitest` snapshots from it on a fixed input set).</ROUTINE-KEEP-OLD-REACHABLE>
      <ROUTINE-WRITE-THE-ARBITRARY fact="true" status="impl/done">Write/extend a `fast-check` arbitrary generating representative inputs for that surface.</ROUTINE-WRITE-THE-ARBITRARY>
      <ROUTINE-BIND-OLD-VS-NEW fact="true" status="impl/done">Bind `old` vs `new` (or `snapshot` vs `new`) under an equality/equivalence comparator.</ROUTINE-BIND-OLD-VS-NEW>
      <ROUTINE-RUN-IN-THE-LOOP fact="true" status="impl/done">Run under the per-cell loop; on a shrunk counterexample, fix `new` (NOT the oracle) until green.</ROUTINE-RUN-IN-THE-LOOP>
      <ROUTINE-REMOVE-OLD-ONCE-GREEN fact="true" status="impl/done">Once green, remove `old` (or commit the snapshots) and leave the oracle in the test module.</ROUTINE-REMOVE-OLD-ONCE-GREEN>
      <ROUTINE-CITE-THE-ORACLE fact="true" status="impl/done">Cite the oracle from the replacement's `@verifies spec://…` edge.</ROUTINE-CITE-THE-ORACLE>
    </facts>
    <p><CHECKER fact="true" status="spec/done">**Checker:** `@typescript-eslint`/conform rule `replacement-has-oracle` — flags any modification of an `@implements`-bearing item body whose cell lacks a differential/characterization test referencing it. Backed by `vitest run &lt;cell&gt;` running the `fast-check`/snapshot oracle. *(Status: specified, NOT yet implemented → this card is BETA.)*</CHECKER></p>
    <p><RAID-ROLE fact="true" status="impl/done">**Raid role:** layer = *behavior-preserving* phase (runs in any raid that rewrites implementations); order = applied AS A GATE around every other behavior-changing card (no ordering dependency of its own, but nothing that changes behavior may merge in a raid without it); batch = per-cell.</RAID-ROLE></p>
    <p><BUDGET fact="true" status="impl/done">**Budget:** competes with few rules (it is gate-time, not inline, so it does not crowd the edit-time active set); first-signal latency = one per-cell `fast-check` run (target &lt; 60s; tune the run count / `numRuns` to stay in budget).</BUDGET></p>
  </band-three-operation>
</spec>
