# rust-ai-native-tcg-bridge — the LSP client seam over the consumer's own
# rust-analyzer (TCG-ORACLE-RUST v0.1 / TCG-PROTOCOL-RUST v0.1): the
# family-prefixed twin of typescript-ai-native-tcg-bridge, per the
# GUIDE §2 naming rule (PROP-028 §2.4). Replay-tested without rust-analyzer;
# the live end-to-end suite requires the component and fails with the
# recipe when absent (a stack obligation, never a skip).
[package]
name = "rust-ai-native-tcg-bridge"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
specmark.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
