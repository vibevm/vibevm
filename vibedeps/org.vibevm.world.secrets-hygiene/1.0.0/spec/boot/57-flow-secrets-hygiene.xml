<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Flow: Secrets Hygiene</title>
  <status stage="impl" state="done"/>
  <p><fact id="the-repository-is-worked-by-a-recordable-agent-session" status="spec/done">This repository is worked by a coding agent that reads a lot and
whose sessions may be **recorded or logged**.</fact></p>
  <p><fact id="A-SECRET-HAS-NO-SAFE-MARGIN-ONE-ECHO-IS-A-LEAK" status="spec/done">Under those conditions
a secret has no safe margin: one echo of its value into chat, a
diff, or a log is a leak.</fact></p>
  <p><fact id="THE-FLOW-IS-THE-STANDING-RULE-FOR-CREDENTIALS" status="impl/done">This flow is the standing rule for
handling credentials — tokens, keys, passwords — so that no code
path and no session ever puts a secret value on a surface.</fact></p>
  <section id="surface-secret" title="What counts as a secret">
    <p><fact id="A-SURFACE-SECRET-IS-A-VALUE-THAT-MUST-NEVER-APPEAR" status="impl/done">A **surface secret** is any credential value that must never appear
on a surface the tooling or the session produces.</fact></p>
    <p><fact id="WHAT-COUNTS-AS-A-SURFACE-SECRET" status="impl/done">Publish and deploy
tokens, registry-API tokens, provider API keys, database passwords,
SSH passphrases — all surface secrets.</fact></p>
    <p><fact id="PRINT-THE-SOURCE-NEVER-THE-VALUE" status="impl/done">The rule is about the
**value**: you may freely print the *source* of a secret (an env-var
name, a file path, "explicit flag"); you never print the value it
resolves to.</fact></p>
  </section>
  <section id="laws" title="The four laws">
    <list ordered="true">
      <item><fact id="LAW-NEVER-PRINTED" status="impl/done">**Never printed.** Not to stdout, stderr, logs, a `--json` or
   event stream, error messages, panic or stack traces, telemetry,
   or a lockfile. Print the source, never the value.</fact></item>
      <item><fact id="LAW-NEVER-PERSISTED" status="impl/done">**Never persisted** outside the one sanctioned at-rest location:
   a per-user, permission-protected file in the tool's own config
   directory (or an environment variable, for CI). Never committed,
   never in the lockfile, never in a cache or the project tree.</fact></item>
      <item><fact id="LAW-SANCTIONED-PROCESS-BOUNDARIES-ONLY" status="impl/done">**Sanctioned process boundaries only.** A secret crosses a
   process boundary only by a narrow, audited path — a TLS
   `Authorization` header; a single child-process call with the
   credential embedded in a URL, relying on that child tool's own
   redaction of URL passwords. No other path.</fact></item>
      <item><fact id="LAW-REDACTION-IS-TESTED" status="impl/done">**Redaction is tested, not promised.** A wrapper type that
   redacts the value on display is backed by a unit test asserting
   the value never appears. A promise in a comment is not redaction.</fact></item>
    </list>
  </section>
  <section id="accidental" title="On accidental exposure">
    <p><fact id="SECRET-FILES-ARE-EDITED-NEVER-READ" status="impl/done">Secrets files are edited in an editor directly — never `cat`'d,
never read into the conversation with a file-reading tool.</fact></p>
    <p><fact id="ON-EXPOSURE-STOP-AND-DO-NOT-PROPAGATE" status="impl/done">If a
secret value nonetheless lands in context: **stop, do not
propagate.**</fact></p>
    <p><fact id="DO-NOT-QUOTE-ECHO-OR-SHOW-THE-VALUE" status="impl/done">Do not quote it back, do not echo it into a commit
message, do not show it in a diff.</fact></p>
    <p><fact id="TREAT-THE-VALUE-AS-COMPROMISED-AND-ROTATE" status="impl/done">Treat the value as compromised
and tell the human to rotate it — the value is dead the moment it
may have been seen.</fact></p>
  </section>
  <section id="never" title="Never">
    <list ordered="false">
      <item><fact id="NEVER-PRINT-A-SECRET-VALUE" status="impl/done">Never print, echo, quote, or paste a secret value — or the
  contents of a secret file — into chat, output, a log, a commit, or
  a diff. Print the source, never the value.</fact></item>
      <item><fact id="NEVER-READ-A-SECRET-FILE-INTO-THE-CONVERSATION" status="impl/done">Never read a secret file into the conversation with a file-reading
  or `cat`-style tool; edit it in an editor instead.</fact></item>
      <item><fact id="NEVER-PERSIST-OUTSIDE-THE-SANCTIONED-LOCATION" status="impl/done">Never commit or persist a secret anywhere but the one sanctioned
  per-user, permission-protected location.</fact></item>
      <item><fact id="NEVER-PUT-A-SECRET-IN-A-THIRD-PARTY-SCRIPTS-ENVIRONMENT" status="impl/done">Never place a secret in the environment of a spawned third-party
  script (install/build hooks run unseen third-party code).</fact></item>
      <item><fact id="NEVER-LET-AN-INTEGRATION-ESCALATE-SCOPE" status="impl/done">Never let an integration act outside its declared scope; refuse
  scope escalation — it is an error, not a warning.</fact></item>
      <item><fact id="ON-SUSPECTED-EXPOSURE-ROTATE-FIRST" status="impl/done">On any suspected exposure: rotate first, investigate second.</fact></item>
    </list>
    <p><fact id="sibling-document-pointers" status="impl/done">Full protocol:
@spec://org.vibevm.world/secrets-hygiene/flows/secrets-hygiene/SECRETS-HYGIENE-PROTOCOL#root.
Scope rules: @spec://org.vibevm.world/secrets-hygiene/flows/secrets-hygiene/scope-discipline#root.
Install-time code: @spec://org.vibevm.world/secrets-hygiene/flows/secrets-hygiene/third-party-code-consent#root.</fact></p>
  </section>
</spec>
