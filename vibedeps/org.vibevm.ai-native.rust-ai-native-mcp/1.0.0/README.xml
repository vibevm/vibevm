<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">mcp:org.vibevm.ai-native/rust-ai-native-mcp</title>
  <status stage="doc" state="done" audience="user"/>
  <p><fact id="SERVED-OVER-MCP-AS-ONE-STDIO-BINARY" status="impl/done">The AI-Native Rust discipline served over MCP: one stdio binary
(`rust-ai-native-mcp`), eighteen tools — the whole `rust-ai-native`
command surface (init, floor, the conform and specmap gates, trace,
test-gate, tripwire, health, fast-loop, codemod, ledger) plus the tcg
type oracle (validate / scope / complete / type / bench over a
persistent rust-analyzer session).</fact></p>
  <list ordered="false">
    <item><fact id="INSTALL-PULLS-THE-MATCHING-STACK-AT-THE-EXACT-PIN" status="impl/done">**Install:** `vibe install mcp:org.vibevm.ai-native/rust-ai-native-mcp` — the
  exact `=` pin pulls the matching `stack:org.vibevm.ai-native/rust-ai-native-lang`.</fact></item>
    <item><fact id="REGISTER-WRITES-THE-SERVER-INTO-THE-AGENT-CONFIG" status="impl/done">**Register:** `vibe mcp install` writes the server into your agent's
  config (a vibevm-managed entry; the command line is the slot's built
  artifact — `vibe bin build rust-ai-native-mcp` compiles it).</fact></item>
    <item><fact id="SERVE-NEEDS-NO-VIBE-IN-THE-RUNTIME-PATH" status="impl/done">**Serve:** the agent host launches the binary directly. No vibe in
  the runtime path — a consumer without vibe can build this package's
  workspace with cargo alone and wire the binary by hand.</fact></item>
  </list>
  <p><fact id="server-brief-pointer" status="impl/done">The brief (tool inventory, the tool↔CLI parity map, the session and
capture semantics): [`spec/tools/discipline-mcp-rust.md`](spec/tools/discipline-mcp-rust.md).</fact></p>
  <p><fact id="PREREQUISITE-THE-RUST-ANALYZER-COMPONENT" status="impl/done">Prerequisite (inherited from the stack): `rustup component add
rust-analyzer`.</fact></p>
</spec>
