<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">`flow:git-atomic-commits` — one commit, one idea</title>
  <status stage="doc" state="done" audience="user"/>
  <p><fact id="PACKAGE-INSTALLS-THE-ATOMIC-COMMITS-DISCIPLINE" status="impl/done">A vibevm `flow` package that installs the **atomic commits** Git
discipline into a project.</fact></p>
  <p><fact id="ONE-COMMIT-ONE-LOGICAL-CHANGE-EXPLAINED-BY-ITS-MESSAGE" status="impl/done">One commit carries exactly one logical
change, and the commit message explains *why* in
[Conventional Commits](https://www.conventionalcommits.org/) format.</fact></p>
  <p><fact id="in-a-pure-human-team-atomic-commits-are-quality-of-life" status="spec/done">In a pure-human team, atomic commits are a quality-of-life feature
(easier review, cleaner bisects, viable cherry-picks).</fact></p>
  <p><fact id="in-a-human-ai-team-they-are-load-bearing" status="spec/done">In a human-AI
team they are load-bearing: the human's primary verification mechanism
is reading the diff, and a commit that mixes three concerns across
eight files is not verifiable in one pass.</fact></p>
  <p><fact id="package-contents-lead" status="impl/done">This package ships the **atomicity** discipline (the message **format** is the separate
`flow:git-conventional-commits` package):</fact></p>
  <list ordered="false">
    <item><fact id="CONTENT-THE-FULL-PROTOCOL" status="impl/done">`spec/flows/atomic-commits/ATOMIC-COMMITS-PROTOCOL.md` — full
  protocol: what an atomic commit is, why it matters more in a
  human-AI team than elsewhere, when to split, when to batch, and the
  rule that pushed history is frozen.</fact></item>
    <item><fact id="CONTENT-THE-SPLITTING-PROCEDURE" status="impl/done">`spec/flows/atomic-commits/splitting-large-changes.md` — mechanical
  procedure for turning a messy working tree into a sequence of
  atomic commits using `git add -p`, including a prompt for
  delegating the split to the agent.</fact></item>
    <item><fact id="CONTENT-THE-BOOT-SNIPPET" status="impl/done">`spec/boot/30-flow-atomic-commits.md` — boot snippet loaded at
  session start, pointing the agent at the protocol and the never-do
  list.</fact></item>
  </list>
  <section id="install" title="Install">
    <fence lang="bash">vibe install flow:git-atomic-commits</fence>
  </section>
  <section id="uninstall" title="Uninstall">
    <fence lang="bash">vibe uninstall flow:git-atomic-commits</fence>
    <p><fact id="UNINSTALL-REMOVES-EVERY-FILE-THE-PACKAGE-WROTE" status="impl/done">Uninstalling removes every file the package wrote, including the boot
snippet.</fact></p>
    <p><fact id="USER-OWNED-FILES-ARE-NEVER-TOUCHED" status="impl/done">User-owned files (`00-core.md`, `90-user.md`, `WAL.md`) are
never touched.</fact></p>
  </section>
  <section id="composition" title="Composition">
    <list ordered="false">
      <item><fact id="COMPOSES-WAL-AND-SYNC-FROM-CODE-BY-DISTINCT-PREFIXES" status="impl/done">Works with `flow:wal` (`10-…`) and `flow:sync-from-code` (`20-…`):
  numeric boot-snippet prefixes are distinct by design.</fact></item>
      <item><fact id="COMPOSES-SYNC-FROM-CODE-FOR-THE-COMMIT-MESSAGE" status="impl/done">`flow:sync-from-code`'s final step is a `docs(spec)` commit; this
  flow is why the sync lands as its own commit and not folded into the
  code change. The *format* of that message is pinned by the sibling
  `flow:git-conventional-commits`, not here.</fact></item>
      <item><fact id="COMPOSES-WAL-FOR-THE-SESSION-END-COMMIT" status="impl/done">End-of-session WAL rewrite (from `flow:wal`) ends in a commit;
  git-atomic-commits is how that commit is shaped.</fact></item>
    </list>
  </section>
  <section id="background" title="Philosophical background">
    <p><fact id="extracted-from-the-books-second-chapter" status="spec/done">The rule is extracted from *AI-native development*, chapter 2
(*"Shared state: файлы как IPC"*, subsection "Атомарность"), together
with the Conventional Commits specification.</fact></p>
    <p><fact id="short-version-delegate-the-split-and-keep-the-log-as-an-archive" status="spec/done">Short version: humans
hate splitting messy trees, AI is happy to — delegate the split,
verify the plan, and use the commit log as the project's decision
archive.</fact></p>
  </section>
  <section id="license" title="License">
    <p><fact id="license-line" status="impl/done">UPL-1.0 — The Universal Permissive License, Version 1.0. See `LICENSE` and the surrounding registry for distribution terms.</fact></p>
  </section>
</spec>
