# The clean fixture: seam-only imports, zero unsafe-set tokens — the
# e2e test's green half.

[typescript]
roots = ["src"]
exclude_substrings = []
cells_dir = "src/cells"
gated = ["greet", "parse"]
seam = "index"
