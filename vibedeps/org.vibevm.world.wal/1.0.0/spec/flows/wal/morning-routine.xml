<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Morning routine — the human side of WAL</title>
  <status stage="spec" state="done"/>
  <p><fact id="scope-of-this-document" status="impl/done">**Scope of this document.** The five-minute human ritual at the start
of each day: read the WAL, reconcile it with your own memory, and only
then let an agent session begin.</fact></p>
  <p><fact id="WAL-IS-A-TWO-SIDED-PROTOCOL" status="impl/done">The WAL is a two-sided protocol — the
agent writes it at the end of each session, the human reads it at the
start of each day.</fact></p>
  <p><fact id="WITHOUT-THE-MORNING-READ-THE-WAL-ROTS" status="spec/done">Without the morning read, the WAL rots: the agent's
claim that "all tests pass" drifts from your memory that the timeout
test was flaky yesterday.</fact></p>
  <section id="read" title="Read before coding">
    <p><fact id="before-coding-lead" status="impl/done">Before opening any code, before starting any agent session:</fact></p>
    <list ordered="true">
      <item><fact id="STEP-OPEN-THE-WAL" status="impl/done">Open `spec/WAL.md`.</fact></item>
      <item><fact id="STEP-READ-IT-END-TO-END" status="impl/done">Read it end to end.</fact></item>
      <item><fact id="STEP-COMPARE-AGAINST-WHAT-YOU-REMEMBER" status="impl/done">Compare what it says against what you remember.</fact></item>
    </list>
  </section>
  <section id="head-wins" title="Head wins">
    <p><fact id="HEAD-WINS-EDIT-THE-WAL-TO-MATCH-MEMORY" status="impl/done">If the WAL and your memory disagree, **head wins** — edit the WAL to
match what you remember, *then* start the session.</fact></p>
    <p><fact id="why-head-wins" status="spec/done">Head wins because persistent human memory is the one authoritative
channel the agent cannot reach directly.</fact></p>
    <p><fact id="a-flaky-test-example" status="impl/done">If the WAL says "all tests
pass" and you remember the timeout test is flaky, the WAL is wrong:
fix it before the next agent session reads and trusts it.</fact></p>
    <p><fact id="top-of-the-conflict-hierarchy" status="impl/done">(This is
the top of the conflict hierarchy — Human &gt; Spec &gt; Tests &gt; Code &gt;
WAL.)</fact></p>
  </section>
  <section id="skim" title="Fast skim: wal-status">
    <p><fact id="the-daily-read-has-a-fast-form" status="impl/done">The daily read has a fast form.</fact></p>
    <p><fact id="FLOW-SHIPS-A-WAL-STATUS-SKILL" status="impl/done">This flow ships a `wal-status` skill:
install it into your agent and ask for a WAL status at session start.</fact></p>
    <p><fact id="SKILL-ANSWERS-IN-AT-MOST-TEN-LINES" status="impl/done">It reads `spec/WAL.md` end to end and answers in at most ten lines —
one line of phase and status, up to three attention bullets, one line
of next step — warning first when the WAL is older than 24 hours.</fact></p>
    <p><fact id="equivalent-prompt-lead" status="impl/done">For an agent without skill support, paste the equivalent prompt into
your project's agent instructions:</fact></p>
    <fence lang="markdown">## /wal-status
Read spec/WAL.md end to end. Emit in ≤ 10 lines:
- one line: current phase and status
- up to three bullets: what needs attention (blockers, risks)
- one line: next priority step
If the WAL `_Updated:` line is older than 24 hours, warn first.</fence>
    <p><fact id="the-summary-is-a-quick-daily-read" status="impl/done">The ten-line summary is a quick daily read.</fact></p>
    <p><fact id="THE-SKIM-DOES-NOT-REPLACE-THE-WEEKLY-RE-READ" status="impl/done">It does not replace the
end-of-week full re-read — the same way a `top` command does not
replace looking at dashboards.</fact></p>
  </section>
  <section id="cold" title="Cold starts">
    <p><fact id="cold-start-situations" status="impl/done">Coming back after a machine switch or a long gap, or arriving at a
repository that is not yours?</fact></p>
    <p><fact id="READ-CONTINUE-FIRST-THEN-THE-WAL" status="impl/done">Read `CONTINUE.md` at the repository
root first — it carries the tour, the commands, the map — then
`spec/WAL.md`, which is canonical wherever the two diverge.</fact></p>
    <p><fact id="cold-resume-contract-pointer" status="impl/done">The full
contract is [`cold-resume.md`](cold-resume.md).</fact></p>
  </section>
  <section id="weekly" title="Weekly re-read">
    <p><fact id="RE-READ-KEY-SPEC-DOCUMENTS-WEEKLY" status="impl/done">Once a week, re-read the key spec documents end-to-end. Watch for:</fact></p>
    <list ordered="false">
      <item><fact id="WATCH-FOR-INTERNAL-CONTRADICTIONS" status="impl/done">Internal contradictions (§2 says one thing, §5 says another).</fact></item>
      <item><fact id="WATCH-FOR-STALE-REVIEW-MARKERS" status="impl/done">REVIEW markers older than your configured threshold.</fact></item>
      <item><fact id="WATCH-FOR-ORPHAN-ANCHORS" status="impl/done">Orphan anchors (`{#something}` that nothing references).</fact></item>
    </list>
    <p><fact id="these-drift-silently" status="spec/done">These drift silently.</fact></p>
    <p><fact id="A-SCHEDULED-RE-READ-IS-THE-GARBAGE-COLLECTOR" status="spec/done">A scheduled re-read is the garbage collector for
the spec corpus.</fact></p>
  </section>
  <section id="stale" title="If the WAL is clearly stale">
    <p><fact id="A-STALE-DATE-LINE-MEANS-THE-LAST-SESSION-DID-NOT-CLOSE" status="spec/done">If you come back after a week and the WAL's `_Updated:` line is from
last Tuesday, the previous session clearly did not close cleanly. Your
move:</fact></p>
    <list ordered="true">
      <item><fact id="STALE-STEP-DO-NOT-START-A-SESSION-YET" status="impl/done">Do NOT start a new agent session yet.</fact></item>
      <item><fact id="STALE-STEP-RECONSTRUCT-FROM-GIT" status="impl/done">Look at `git log` and `git diff` for the interval. Reconstruct what
   actually happened.</fact></item>
      <item><fact id="STALE-STEP-REWRITE-THE-WAL-BY-HAND" status="impl/done">Rewrite the WAL yourself to reflect the true current state.</fact></item>
      <item><fact id="STALE-STEP-THEN-START-THE-NEXT-SESSION" status="impl/done">*Now* start the next session.</fact></item>
    </list>
    <p><fact id="THE-HUMAN-IS-THE-BACKUP-FOR-THE-WAL" status="impl/done">The human is the backup for the WAL.</fact></p>
    <p><fact id="where-the-human-is-irreplaceable" status="spec/done">This is one of the situations
where the human is irreplaceable in the system, and where ten minutes
of manual reconciliation prevents hours of agent confusion.</fact></p>
  </section>
  <section id="never" title="Never">
    <list ordered="false">
      <item><fact id="NEVER-START-ON-A-WAL-YOU-HAVE-NOT-READ-TODAY" status="impl/done">Never start an agent session on a WAL you have not read today.</fact></item>
      <item><fact id="NEVER-FIX-A-STALE-WAL-FROM-MEMORY-ALONE" status="impl/done">Never "fix" a stale WAL from memory alone — reconstruct from
  `git log` and `git diff` first; memory is reconstructive, not
  archival.</fact></item>
      <item><fact id="NEVER-LET-THE-SKIM-REPLACE-THE-WEEKLY-RE-READ" status="impl/done">Never let the skim replace the weekly full re-read.</fact></item>
      <item><fact id="NEVER-LEAVE-A-KNOWN-DIVERGENCE-FOR-LATER" status="impl/done">Never leave a known divergence in place "for later" — the very next
  session will read and trust it.</fact></item>
    </list>
  </section>
  <section id="summary" title="Summary">
    <list ordered="false">
      <item><fact id="SUM-READ-THE-WAL-EVERY-MORNING" status="impl/done">Read the WAL every morning, end to end, before any session.</fact></item>
      <item><fact id="SUM-HEAD-WINS" status="impl/done">Head wins: your memory corrects the WAL, never the other way around.</fact></item>
      <item><fact id="SUM-SKIM-AND-WEEKLY-RE-READ" status="impl/done">`wal-status` is the fast skim; the weekly full re-read still happens.</fact></item>
      <item><fact id="SUM-COLD-START-ORDER" status="impl/done">Cold start? `CONTINUE.md` first for the tour, the WAL for the truth.</fact></item>
      <item><fact id="SUM-STALE-WAL-PROCEDURE" status="impl/done">Stale WAL: reconstruct from git, rewrite by hand, then start.</fact></item>
    </list>
  </section>
</spec>
