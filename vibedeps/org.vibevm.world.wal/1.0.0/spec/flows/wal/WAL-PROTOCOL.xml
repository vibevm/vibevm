<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">WAL Protocol</title>
  <status stage="spec" state="done"/>
  <p><scope-of-this-document fact="true" status="impl/done">**Scope of this document.** What a WAL is in a spec-driven project,
which files carry the discipline, what belongs in the WAL, how big it
may get, when it must be updated, and who wins when it disagrees with
the rest of the system.</scope-of-this-document></p>
  <p><sibling-document-pointers fact="true" status="impl/done">The rituals around it live in the siblings:
[`session-end-hook.md`](session-end-hook.md), [`morning-routine.md`](morning-routine.md), [`cold-resume.md`](cold-resume.md).</sibling-document-pointers></p>
  <files title="The two files">
    <p><two-files-lead fact="true" status="impl/done">Session-durable state lives in two repository files:</two-files-lead></p>
    <list ordered="false">
      <item><FILE-WAL-IS-THE-CANONICAL-LIVING-CHECKPOINT fact="true" status="impl/done">**`spec/WAL.md` — the living checkpoint.** The *current* state:
  phase, constraints, done, open, next. Rewritten at every session end.
  **Canonical:** where it and any snapshot disagree, the WAL wins.</FILE-WAL-IS-THE-CANONICAL-LIVING-CHECKPOINT></item>
      <item><FILE-CONTINUE-IS-THE-SUBORDINATE-COLD-RESUME-SNAPSHOT fact="true" status="impl/done">**`CONTINUE.md` (repository root) — the cold-resume snapshot.**
  Written at session end for whoever picks up cold — another machine, a
  teammate, a long gap. Richer than the WAL, strictly subordinate.
  Contract: [`cold-resume.md`](cold-resume.md).</FILE-CONTINUE-IS-THE-SUBORDINATE-COLD-RESUME-SNAPSHOT></item>
    </list>
    <p><RESUMPTION-STATE-LIVES-IN-THE-REPOSITORY-NEVER-IN-A-SESSION fact="true" status="impl/done">One invariant governs both: **resumption state lives in the repository,
never in a session.**</RESUMPTION-STATE-LIVES-IN-THE-REPOSITORY-NEVER-IN-A-SESSION></p>
    <p><sessions-end-compact-and-crash fact="true" status="spec/done">Sessions end, compact, and crash; the repository
outlives them all.</sessions-end-compact-and-crash></p>
    <p><rest-of-the-document-specifies-the-wal fact="true" status="impl/done">The rest of this document specifies the WAL.</rest-of-the-document-specifies-the-wal></p>
  </files>
  <what title="What a WAL is">
    <p><WAL-STANDS-FOR-WRITE-AHEAD-LOG fact="true" status="spec/done">WAL stands for **Write-Ahead Log** — a name any developer, and any
coding agent from its training data alone, recognises on sight.</WAL-STANDS-FOR-WRITE-AHEAD-LOG></p>
    <p><THE-MECHANISM-IS-THE-CHECKPOINT-NOT-THE-LOG fact="true" status="impl/done">But the
mechanism that fits is not the ever-growing database log; it is the
database **checkpoint**: a snapshot of current state, *overwritten*,
not appended to — the old one is meaningless once a new one exists.</THE-MECHANISM-IS-THE-CHECKPOINT-NOT-THE-LOG></p>
    <p><picture-a-whiteboard-by-the-door fact="true" status="spec/done">Picture a whiteboard by the door.</picture-a-whiteboard-by-the-door></p>
    <p><a-good-whiteboard-and-a-bad-one fact="true" status="spec/done">A good one carries three lines:
where we are, what we are doing, what must not be touched; a bad one
carries three hundred lines of stale history, including tasks closed
months ago.</a-good-whiteboard-and-a-bad-one></p>
    <p><WAL-IS-A-WHITEBOARD-NOT-A-JOURNAL fact="true" status="impl/done">**The WAL is a whiteboard, not a journal.**</WAL-IS-A-WHITEBOARD-NOT-A-JOURNAL></p>
    <p><file-serves-both-processes fact="true" status="impl/done">The file serves both processes in the human-AI system.</file-serves-both-processes></p>
    <p><FOR-THE-AGENT-IT-IS-THE-ONLY-PERSISTENT-MEMORY fact="true" status="impl/done">For the
**agent** it is the canonical persistent memory: each session starts
blank, and the WAL is read first.</FOR-THE-AGENT-IT-IS-THE-ONLY-PERSISTENT-MEMORY></p>
    <p><FOR-THE-HUMAN-IT-IS-THE-SINGLE-MORNING-RE-READ fact="true" status="impl/done">For the **human** it is the single morning
re-read that synchronises your memory with the project's actual state.</FOR-THE-HUMAN-IT-IS-THE-SINGLE-MORNING-RE-READ></p>
    <p><THIS-FILE-ALONE-EXISTS-FOR-SESSION-CONTINUITY fact="true" status="impl/done">Everything else in `spec/` is medium-term memory or artefacts; this
file alone exists for session continuity.</THIS-FILE-ALONE-EXISTS-FOR-SESSION-CONTINUITY></p>
  </what>
  <not title="What a WAL is NOT">
    <list ordered="false">
      <item><NOT-A-LOG fact="true" status="impl/done">**Not a log.** Rewritten every session, always describing the
  *current* state; an append-only WAL rots into an archive nobody reads.</NOT-A-LOG></item>
      <item><NOT-A-CHANGELOG fact="true" status="impl/done">**Not a changelog.** History belongs in `git log` and spec changelogs.</NOT-A-CHANGELOG></item>
      <item><NOT-A-TO-DO-LIST fact="true" status="impl/done">**Not a to-do list.** The "Next" section names *one* next action; the
  backlog belongs in an issue tracker.</NOT-A-TO-DO-LIST></item>
      <item><NOT-DOCUMENTATION fact="true" status="impl/done">**Not documentation.** A fact stable enough to deserve a permanent
  home belongs in a spec document, not in the WAL.</NOT-DOCUMENTATION></item>
    </list>
  </not>
  <sections title="Required sections">
    <p><required-sections-lead fact="true" status="impl/done">A well-formed `spec/WAL.md` has:</required-sections-lead></p>
    <list ordered="true">
      <item><SECTION-UPDATED-LINE fact="true" status="impl/done">**`_Updated: &lt;ISO 8601 UTC&gt;_`** — the very first line after the
   title, always and without exception. An outdated WAL is worse than
   no WAL — false confidence; the date makes staleness detectable fast.</SECTION-UPDATED-LINE></item>
      <item><SECTION-CURRENT-PHASE fact="true" status="impl/done">**Current phase** — one or two lines naming what the project is
   working on right now.</SECTION-CURRENT-PHASE></item>
      <item><SECTION-CONSTRAINTS fact="true" status="impl/done">**Constraints** — the "do not touch" list; each entry carries a
   brief *why*, citing a spec anchor or issue: `match_by_hash(): DO NOT
   TOUCH — fragile reconnection logic, issue #12`. The most valuable
   content in the WAL: agents optimise locally, and without the written
   line an enthusiastic session "improves" what was deliberately left
   alone. Every constraint is a prevented regression.</SECTION-CONSTRAINTS></item>
      <item><SECTION-DONE fact="true" status="impl/done">**Done** — completed work collapsed to one-line summaries:
   "PROP-001 §§1-4 — complete, all tests pass", not five lines of notes.</SECTION-DONE></item>
      <item><SECTION-IN-PROGRESS fact="true" status="impl/done">**In progress** — what is open, with enough detail that the next
   session can pick it up. Cite spec anchors (`spec://…`).</SECTION-IN-PROGRESS></item>
      <item><SECTION-NEXT fact="true" status="impl/done">**Next** — the single next action. Several candidates? List them
   briefly and mark the default.</SECTION-NEXT></item>
      <item><SECTION-KNOWN-ISSUES fact="true" status="impl/done">**Known issues** — open problems not being worked on right now, so
   future sessions don't re-discover them.</SECTION-KNOWN-ISSUES></item>
      <item><SECTION-SESSION-CONTEXT fact="true" status="impl/done">**Session context** *(optional)* — one-paragraph orientation: what
   file to open first, what to run, what to avoid.</SECTION-SESSION-CONTEXT></item>
    </list>
  </sections>
  <precision title="Write for the autonomy you grant">
    <p><in-chat-vagueness-gets-a-question-back fact="true" status="spec/done">In chat, vague writing gets a question back.</in-chat-vagueness-gets-a-question-back></p>
    <p><IN-AGENT-MODE-THE-WAL-IS-EXECUTED-LITERALLY fact="true" status="spec/done">In agent mode — unattended
for hours — nobody asks; the WAL is executed literally. Compare:</IN-AGENT-MODE-THE-WAL-IS-EXECUTED-LITERALLY></p>
    <fence lang="markdown"># Chat-grade WAL (tolerable when a human steers every step)
Working on verification; open questions around the timeouts.

# Agent-grade WAL (executed literally, unattended)
## Constraints — do not violate
- Timeout: 600s, not 300s → spec://oproto/PROP-003#verification.timeout
- match_by_hash(): DO NOT TOUCH → issue #12, fragile reconnection logic
## Current task: src/verify.rs → degraded_mode_handler()
Run: cargo test --package oproto-verify. Done when: all tests green.</fence>
    <p><the-second-is-not-bureaucracy fact="true" status="spec/done">The second is not bureaucracy: it is the difference between an agent
that quietly "optimises" the timeout back to 300 s and one that does
what is needed.</the-second-is-not-bureaucracy></p>
    <p><MORE-AUTONOMY-DEMANDS-A-MORE-PRECISE-WAL fact="true" status="impl/done">**The more autonomy the agent has, the more precise the
WAL must be** — coarse context suffices for one supervised step; hours
unattended demand every constraint explicit and spec-anchored.</MORE-AUTONOMY-DEMANDS-A-MORE-PRECISE-WAL></p>
  </precision>
  <triggers title="Update triggers">
    <list ordered="false">
      <item><TRIGGER-END-OF-EVERY-SESSION fact="true" status="impl/done">**At the end of every session.** Non-negotiable; a session that ends
  without updating the WAL has partially broken the next session.
  Procedure: [`session-end-hook.md`](session-end-hook.md).</TRIGGER-END-OF-EVERY-SESSION></item>
      <item><TRIGGER-BEFORE-ANY-DESTRUCTIVE-OPERATION fact="true" status="impl/done">**Before any destructive operation.** A big refactor, a `git reset`,
  a migration — checkpoint first; a failure then restarts known-good.</TRIGGER-BEFORE-ANY-DESTRUCTIVE-OPERATION></item>
      <item><TRIGGER-WHEN-SWITCHING-CONTEXT-MID-SESSION fact="true" status="impl/done">**When switching context mid-session.** Pivoting from FEAT-007 to
  FEAT-010? Checkpoint FEAT-007 *before* pivoting, or its state is lost.</TRIGGER-WHEN-SWITCHING-CONTEXT-MID-SESSION></item>
    </list>
  </triggers>
  <freshness title="The freshness rule">
    <p><WAL-OLDER-THAN-24-HOURS-IS-PRESUMED-STALE fact="true" status="impl/done">A WAL older than **24 hours** is presumed stale: before trusting any
claim — and before *any* destructive work — verify against reality
(branch, tree, tests) and confirm divergences with the user.</WAL-OLDER-THAN-24-HOURS-IS-PRESUMED-STALE></p>
    <p><THE-RULE-BINDS-WHATEVER-THE-TOOLING-DOES fact="true" status="impl/done">Tooling
may warn advisorily; the rule binds regardless.</THE-RULE-BINDS-WHATEVER-THE-TOOLING-DOES></p>
  </freshness>
  <size title="Size budget">
    <p><WAL-IS-LOADED-AT-THE-START-OF-EVERY-SESSION fact="true" status="impl/done">The WAL is loaded into the agent's context at the start of every
session.</WAL-IS-LOADED-AT-THE-START-OF-EVERY-SESSION></p>
    <p><EVERY-TOKEN-IS-A-TAX-PAID-PER-SESSION fact="true" status="spec/done">Every token in the WAL is a tax paid per session.</EVERY-TOKEN-IS-A-TAX-PAID-PER-SESSION></p>
    <list ordered="false">
      <item><BUDGET-TARGET fact="true" status="impl/done">**Target:** ≤ 3 000 tokens (roughly one page of plain text).</BUDGET-TARGET></item>
      <item><BUDGET-HARD-LIMIT fact="true" status="impl/done">**Hard limit:** 5 000 tokens — past that the WAL hoards history:
  collapse done items, move stable facts to specs, split epics.</BUDGET-HARD-LIMIT></item>
      <item><BUDGET-RULE-OF-THUMB fact="true" status="impl/done">**Rule of thumb:** unreadable end-to-end in a minute means too long.</BUDGET-RULE-OF-THUMB></item>
    </list>
  </size>
  <conflicts title="Conflict resolution">
    <fence>Human  &gt;  Spec  &gt;  Tests  &gt;  Code  &gt;  WAL</fence>
    <p><WAL-IS-VOLATILE-BY-DESIGN fact="true" status="impl/done">The WAL is volatile by design — a *record* of the current state, not a
source of truth about intent.</WAL-IS-VOLATILE-BY-DESIGN></p>
    <p><SPEC-WINS-AND-THE-WAL-IS-STALE fact="true" status="impl/done">If the WAL says one thing and a spec
document says another, the spec wins and the WAL is stale.</SPEC-WINS-AND-THE-WAL-IS-STALE></p>
    <p><SURFACE-A-WRONG-WAL-ENTRY-NEVER-SILENTLY-REWRITE-IT fact="true" status="impl/done">An agent that believes a WAL entry is wrong does not silently rewrite
it: complete the task following the spec (not the stale WAL), add a
`&lt;!-- REVIEW: … --&gt;` marker where the mismatch matters, surface the
disagreement in the end-of-session report, and let the human reconcile.</SURFACE-A-WRONG-WAL-ENTRY-NEVER-SILENTLY-REWRITE-IT></p>
  </conflicts>
  <example title="A working example">
    <fence lang="markdown"># WAL — Project Continuation State
_Updated: 2026-04-16T18:23:00Z_

## Current phase
PROP-003 verification engine — IN PROGRESS (~70%)

## Constraints (do not violate without discussion)
- Timeout = 600s (spec://oproto/PROP-003#verification.timeout);
  300s caused false positives on VPN users.
- match_by_hash(): DO NOT TOUCH — fragile reconnection logic, #12.

## Done
- [x] PROP-001 OPROTO base; PROP-002 edge skeleton (47/52 tests).

## In progress
- PROP-003: hash matcher and basic timeout DONE; degraded mode TODO
  (spec://oproto/PROP-003#verification.degraded).

## Next
src/verify.rs → degraded_mode_handler().
Start with: `cargo test -p oproto-verify`.

## Known issues
- reconnection after net loss (#12); media_refs schema ambiguity (#15).</fence>
  </example>
  <acceptance title="The acceptance test">
    <p><ACCEPTANCE-TEST-IS-A-STRANGER-WITH-ONLY-THE-REPOSITORY fact="true" status="impl/done">One test decides whether the discipline works: **a stranger with only
the repository resumes work without asking.**</ACCEPTANCE-TEST-IS-A-STRANGER-WITH-ONLY-THE-REPOSITORY></p>
    <p><a-stranger-cold-from-files-alone fact="true" status="impl/done">A stranger, cold, from
files alone — not the author remembering, not the agent that wrote it
reconstructing.</a-stranger-cold-from-files-alone></p>
    <p><APPLY-THE-TEST-WHEN-IN-DOUBT fact="true" status="impl/done">When in doubt what to record or where, apply it.</APPLY-THE-TEST-WHEN-IN-DOUBT></p>
  </acceptance>
  <without title="Appendix: without a WAL">
    <p><A-PROJECT-MAY-DECLINE-THIS-CONVENTION fact="true" status="impl/done">A project may decline this convention.</A-PROJECT-MAY-DECLINE-THIS-CONVENTION></p>
    <p><THE-INVARIANT-BINDS-AND-THE-FALLBACK-IS-GIT-LOG fact="true" status="impl/done">The invariant still binds —
resumption state lives in the repository, never in a session — and the
fallback resume path is `git log` plus a fresh look at the tree: commit
messages carry session summaries, a plan's status line carries campaign
state, each session re-derives the rest.</THE-INVARIANT-BINDS-AND-THE-FALLBACK-IS-GIT-LOG></p>
    <p><the-cost-of-the-fallback-path fact="true" status="spec/done">That works; the cost is three
places instead of one, no single canonical "now", tokens burned every
session on re-derivation — and constraints missed first.</the-cost-of-the-fallback-path></p>
    <p><RE-DERIVING-STATE-IS-THE-SIGNAL-TO-ADOPT fact="true" status="spec/done">A project that
keeps re-deriving its own state has received the signal to adopt.</RE-DERIVING-STATE-IS-THE-SIGNAL-TO-ADOPT></p>
  </without>
  <re-derive title="Re-derive for your project">
    <p><COPY-THE-TASK-NOT-THE-IMPLEMENTATION fact="true" status="impl/done">Copy the task, not the implementation.</COPY-THE-TASK-NOT-THE-IMPLEMENTATION></p>
    <p><re-derive-prompt-lead fact="true" status="impl/done">Hand your agent this prompt and
review its drafts:</re-derive-prompt-lead></p>
    <fence lang="text">Read this flow's documents (your project installed them — typically `vibedeps/flow-wal/&lt;version&gt;/spec/flows/wal/`, check `vibe.lock`) WAL-PROTOCOL.md end to end, then adapt the WAL
discipline to this repository:
1. Draft spec/WAL.md with the required sections and a fresh
   _Updated: line, filled from the actual tree, not from guesses.
2. Propose this project's wind-down and resume trigger phrases, with
   native-language twins if the team is bilingual.
3. Draft boot lines (CLAUDE.md, AGENTS.md, or equivalent) so every
   session reads the WAL before anything else.
4. State the autonomy rule: may a wind-down commit and push, or does
   it stop at drafts? Default to drafts.
Show all four as proposals. Apply nothing until I approve.</fence>
  </re-derive>
  <summary title="Summary">
    <list ordered="false">
      <item><SUM-TWO-FILES fact="true" status="impl/done">Two files: `spec/WAL.md` — living checkpoint, canonical;
  `CONTINUE.md` — cold-resume snapshot, subordinate.</SUM-TWO-FILES></item>
      <item><SUM-WHITEBOARD-NOT-JOURNAL fact="true" status="impl/done">A whiteboard, not a journal: rewrite at session end, never append;
  date line first; older than 24 hours → verify before trusting.</SUM-WHITEBOARD-NOT-JOURNAL></item>
      <item><SUM-SIZE-AND-CONSTRAINTS fact="true" status="impl/done">Keep it under a page; stable facts move to specs; constraints are the
  highest-value content — more autonomy demands more precision.</SUM-SIZE-AND-CONSTRAINTS></item>
      <item><SUM-SPEC-BEATS-WAL fact="true" status="impl/done">Spec beats WAL; a stranger with only the repo resumes without asking.</SUM-SPEC-BEATS-WAL></item>
    </list>
  </summary>
</spec>
