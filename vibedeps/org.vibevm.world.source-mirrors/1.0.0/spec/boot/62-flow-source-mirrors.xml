<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Flow: Source Mirrors</title>
  <status stage="impl" state="done"/>
  <p><fact id="PROJECT-SOURCE-IS-MULTI-HOMED" status="impl/done">This project's source is **multi-homed**: the same history lives on
more than one git host.</fact></p>
  <p><fact id="COPIES-ARE-KEPT-IN-STEP-UNDER-A-SINGLE-WRITER-MODEL" status="impl/done">It is kept in step under a **single-writer**
model, so the copies never diverge.</fact></p>
  <section id="core-rule" title="Core rule">
    <p><fact id="THERE-IS-ONE-MAINLINE" status="impl/done">There is one **mainline** — the maintainer's integrated local `main`.</fact></p>
    <p><fact id="NO-HOST-IS-PRIMARY" status="impl/done">No host is primary.</fact></p>
    <p><fact id="EVERY-HOST-IS-A-DOWNSTREAM-READ-REPLICA" status="impl/done">Every host in the target manifest is a downstream
**read-replica** of mainline.</fact></p>
    <p><fact id="HISTORY-REACHES-A-HOST-ONLY-THROUGH-THE-FAN-OUT" status="impl/done">History reaches a host only through the
project's **fan-out** procedure, which is fast-forward-only and never
uses `--force`.</fact></p>
    <p><fact id="full-protocol-pointer" status="impl/done">Full protocol:
@spec://org.vibevm.world/source-mirrors/flows/source-mirrors/SOURCE-MIRRORS-PROTOCOL#root.</fact></p>
    <p><fact id="fanout-mechanics-pointer" status="impl/done">Fan-out mechanics and the reference script:
@spec://org.vibevm.world/source-mirrors/flows/source-mirrors/fanout-mechanics#root.</fact></p>
    <p><fact id="maintainers-day-pointer" status="impl/done">The maintainer's day:
@spec://org.vibevm.world/source-mirrors/flows/source-mirrors/daily-loop#root.</fact></p>
  </section>
  <section id="in-session" title="In session">
    <list ordered="false">
      <item><fact id="COMMIT-ON-MAINLINE-ROLLOUT-IS-A-SEPARATE-STEP" status="impl/done">Commit on mainline as usual. Rollout to the hosts is a **separate,
  deliberate** step — the fan-out procedure, run at a natural
  checkpoint, not a daemon and not `git push` to each host.</fact></item>
      <item><fact id="A-WEB-UI-MERGE-IS-NOT-INTEGRATED-UNTIL-BROUGHT-HOME" status="impl/done">A web-UI merge on a host (a clicked "Merge" button) is **not**
  integrated until it has been brought home into mainline first; only
  then does it fan out to the other hosts.</fact></item>
      <item><fact id="REPORTED-DRIFT-IS-A-SIGNAL-TO-INVESTIGATE" status="impl/done">If a host reports **drift** (it carries a `main` mainline does not),
  treat it as a signal to investigate — fetch, inspect, reconcile
  *into* mainline, then re-fan. Never overwrite the host to make the
  warning go away.</fact></item>
    </list>
  </section>
  <section id="never" title="Never">
    <list ordered="false">
      <item><fact id="NEVER-PUSH-DIRECTLY-TO-A-REPLICA-HOST" status="impl/done">Never push directly to a replica host — rollout goes through the
  fan-out procedure, which is the single source of truth for targets.</fact></item>
      <item><fact id="NEVER-FORCE-ANY-TARGET" status="impl/done">Never `--force` any target, for any ref, for any reason. The
  fan-out is fast-forward-only by law.</fact></item>
      <item><fact id="NEVER-RESOLVE-A-DIVERGENCE-BY-CLOBBERING" status="impl/done">Never resolve a divergence by clobbering the target. A diverged
  target is investigated and reconciled into mainline, never silently
  overwritten.</fact></item>
      <item><fact id="NEVER-TREAT-A-WEB-UI-MERGE-AS-INTEGRATED" status="impl/done">Never treat a web-UI merge as integrated until its commits are in
  mainline.</fact></item>
    </list>
  </section>
</spec>
