<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Addressable Specs Protocol</title>
  <status stage="spec" state="done"/>
  <p><fact id="scope-of-this-document" status="impl/done">**Scope of this document.** This file defines *what* addressability
is, *why* it is the first engineering requirement on a spec tree
shared between a human and a coding agent, the `spec://` URI scheme
and its anchor grammar, the single-source and placement rules that
follow, and the dependency graph addressable specs create for free.</fact></p>
  <p><fact id="sibling-document-pointers" status="impl/done">How to write the units is in [`authoring-rules.md`](authoring-rules.md);
where the files live is in [`spec-tree-layout.md`](spec-tree-layout.md).</fact></p>
  <section id="ipc" title="Specs are IPC, not documentation">
    <p><fact id="HUMAN-AND-AGENT-ARE-TWO-PROCESSES-SHARING-ONE-REPOSITORY" status="spec/done">A human and a coding agent are two processes sharing one repository.</fact></p>
    <p><fact id="THE-SPEC-TREE-IS-THE-ONLY-CHANNEL" status="spec/done">Between them there is no hallway conversation, no memory that
survives the session — the spec tree is the *only* channel through
which intent crosses the process boundary.</fact></p>
    <p><fact id="documentation-is-optional" status="spec/done">Documentation is
optional; projects limp along without it.</fact></p>
    <p><fact id="AN-IPC-CHANNEL-IS-NOT-OPTIONAL" status="spec/done">An IPC channel is not
optional: when it breaks, the system stops.</fact></p>
    <p><fact id="ADDRESSABILITY-IS-THE-FIRST-REQUIREMENT" status="impl/done">Treating spec files as
IPC imposes engineering requirements documentation never had, and
the first of them — ahead of atomicity, ahead of conflict rules — is
**addressability**: every element in every file must be precisely
pointable.</fact></p>
  </section>
  <section id="why" title="Why addressability is requirement #1">
    <p><fact id="THE-HUMANS-ROLE-IS-COHERENCE-MANAGEMENT" status="spec/done">The human's role in the two-process system is coherence management:
notice that the agent deviated from the spec, and correct it.</fact></p>
    <p><fact id="the-human-knows-instantly" status="spec/done">The
human knows *instantly* what is violated.</fact></p>
    <p><fact id="THE-BOTTLENECK-IS-TELLING-THE-MACHINE" status="spec/done">The bottleneck is telling
the machine — feedback latency is where working days go to die.</fact></p>
    <p><fact id="two-ways-lead" status="impl/done">Two ways to deliver the same correction:</fact></p>
    <fence>Way 1:  "You did the verification wrong."

Way 2:  "You are violating
         spec://com.example.shop/PROP-001#verification.timeout —
         the timeout must be 600 s, and you wrote 300 s."</fence>
    <table>
      <tr>
        <td/>
        <td>Way 1 — paraphrase</td>
        <td>Way 2 — URI citation</td>
      </tr>
      <tr>
        <td><fact id="ROW-AGENT-MUST" status="spec/done">Agent must</fact></td>
        <td><fact status="spec/done">guess what "verification" maps to here, guess what "wrong" means, form a hypothesis, attempt a fix</fact></td>
        <td><fact status="spec/done">open the file, jump to the anchor, read the value, compare, fix</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-TOKEN-COST" status="spec/done">Token cost</fact></td>
        <td><fact status="spec/done">hundreds, spent on search and hypothesis</fact></td>
        <td><fact status="spec/done">about twenty</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-RESULT" status="spec/done">Result</fact></td>
        <td><fact status="spec/done">may not match what the human meant</fact></td>
        <td><fact status="spec/done">exact hit</fact></td>
      </tr>
    </table>
    <p><fact id="the-difference-is-an-order-of-magnitude" status="spec/done">The difference is an order of magnitude, paid on *every* correction,
several times per session — in metered tokens and in minutes of a
short human day.</fact></p>
    <p><fact id="PARAPHRASE-KEEPS-A-NICHE" status="impl/done">Paraphrase keeps a niche: a sweeping refactor or a
philosophical re-orientation rightly starts from "re-read the whole
spec and rethink".</fact></p>
    <p><fact id="FOR-POINT-CORRECTIONS-THE-URI-WINS" status="impl/done">For point corrections — a wrong constant, a
missing parameter, a violated invariant — the URI wins every time.</fact></p>
  </section>
  <section id="uri-scheme" title="The URI scheme">
    <fence>spec://&lt;group&gt;/&lt;name&gt;[@&lt;version&gt;]/&lt;doc-path&gt;#&lt;section&gt;[.&lt;sub&gt;…][~r&lt;N&gt;]</fence>
    <p><fact id="URI-SCHEME-IS-THE-FULL-GRAMMAR" status="impl/done">This is the **whole** grammar — the section stopped publishing a subset of what implementations resolve (2026-08-04, closing the split the vibevm host measured): the authority is a package coordinate, the version and the revision pin are optional extensions, and a project's root is a package like any other (no host exception exists).</fact></p>
    <table>
      <tr>
        <td>Segment</td>
        <td>Meaning</td>
        <td>Example</td>
      </tr>
      <tr>
        <td><fact id="ROW-SEGMENT-GROUP-NAME" status="impl/done">`&lt;group&gt;/&lt;name&gt;`</fact></td>
        <td><fact status="impl/done">the package coordinate: reverse-DNS group, then package name, joined by `/` (never `.`) — the root project included, addressed by its own declared coordinate</fact></td>
        <td><fact status="impl/done">`org.vibevm.world/wal`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-SEGMENT-VERSION" status="impl/done">`[@&lt;version&gt;]`</fact></td>
        <td><fact status="impl/done">**optional — a feature, never an obligation** (owner-ruled 2026-08-04): absent, the address resolves against the **freshest installed version** (semver-newest); explicit, it picks its exact slot, including a non-newest one</fact></td>
        <td><fact status="impl/done">`@0.8.0`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-SEGMENT-MODULE" status="impl/done">`&lt;module&gt;`</fact></td>
        <td><fact status="impl/done">inside `&lt;doc-path&gt;`: the spec module — a directory under `spec/modules/`, or `common`</fact></td>
        <td><fact status="impl/done">`modules/vibe-registry`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-SEGMENT-DOC" status="impl/done">`&lt;doc&gt;`</fact></td>
        <td><fact status="impl/done">inside `&lt;doc-path&gt;`: the document name, extension dropped</fact></td>
        <td><fact status="impl/done">`PROP-001`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-SEGMENT-SECTION" status="impl/done">`&lt;section&gt;`</fact></td>
        <td><fact status="impl/done">the `{#anchor}` of a heading in that document</fact></td>
        <td><fact status="impl/done">`verification`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-SEGMENT-SUB" status="impl/done">`.&lt;sub&gt;`</fact></td>
        <td><fact status="impl/done">dotted hierarchy inside the anchor namespace</fact></td>
        <td><fact status="impl/done">`verification.timeout`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-SEGMENT-REVISION-PIN" status="impl/done">`[~r&lt;N&gt;]`</fact></td>
        <td><fact status="impl/done">pins a **spec-unit revision** for drift detection — never a package version (the `@` half owns that)</fact></td>
        <td><fact status="impl/done">`#verification~r2`</fact></td>
      </tr>
    </table>
    <p><fact id="why-a-uri-and-not-a-bespoke-notation" status="spec/done">Why a URI and not a bespoke notation?</fact></p>
    <p><fact id="the-model-already-knows-the-uri-shape" status="spec/done">Because the model already
knows, from billions of URLs and RFCs in its training data, that
`something://path/to/thing#anchor` points at a specific resource.</fact></p>
    <p><fact id="THE-SCHEME-EXPLOITS-SEMANTICS-THE-AGENT-CARRIES" status="spec/done">Nothing has to be taught — the scheme exploits semantics the agent
already carries.</fact></p>
  </section>
  <section id="anchors" title="Anchors">
    <p><fact id="ANCHORS-ARE-EXPLICIT-HEADING-IDS" status="impl/done">Inside a document, addressability is implemented with explicit
heading anchors — `{#id}` — a standard extended-Markdown syntax:</fact></p>
    <fence lang="markdown"># PROP-001: Payments protocol {#root}

## 5. Verification flow {#verification}

### 5.3 Timeout {#verification.timeout}
Unverified payments older than 600 seconds get status TIMEOUT.</fence>
    <p><fact id="the-third-heading-citation" status="impl/done">That third heading is cited as
`spec://com.example.shop/PROP-001#verification.timeout`.</fact></p>
    <p><fact id="RENDERERS-TURN-THE-ANCHOR-INTO-A-LINK-TARGET" status="spec/done">GitHub,
GitLab, and most Markdown renderers turn `{#id}` into a link target,
so the same URI that steers the agent is clickable for the human in
the web Git UI.</fact></p>
    <p><fact id="one-address-two-consumers" status="impl/done">One address, two consumers.</fact></p>
    <p><fact id="THE-DOT-IS-HIERARCHY" status="impl/done">The dot in `verification.timeout` is hierarchy: section `timeout`
inside section `verification`.</fact></p>
    <p><fact id="DOTS-NAMESPACE-THE-ANCHORS" status="impl/done">Dots namespace the anchors —
`#verification.timeout` and `#connection.timeout` coexist in one
document without collision.</fact></p>
  </section>
  <section id="modules" title="Module names: reverse DNS">
    <p><fact id="MODULE-NAMES-USE-REVERSE-DNS" status="impl/done">When specs may ever leave the project — shared across repositories,
published, or merely grep'd from a monorepo — module names use
reverse-DNS notation: `com.example.shop`, dots again available for
submodules (`com.example.shop.payments`).</fact></p>
    <p><fact id="the-convention-is-java-package-naming" status="spec/done">The convention is Java
package naming, introduced by Sun in the mid-1990s and fixed in the
Java Language Specification: global uniqueness for free, piggybacked
on a uniqueness system that already exists — domain names, written
backwards.</fact></p>
    <p><fact id="there-is-a-chat-shortcut" status="impl/done">There is a chat shortcut: tell the agent "we are working inside
module `com.example.shop`; resolve spec URIs relative to that base".</fact></p>
    <p><fact id="the-shortcut-costs-a-lookup" status="spec/done">It works, but it makes the model run a lookup akin to C++
argument-dependent lookup on every resolution.</fact></p>
    <p><fact id="IN-SPEC-FILES-WRITE-THE-FULL-ADDRESS" status="impl/done">Acceptable typed once
in a chat; inside spec files, re-read dozens of times per session,
write the full address.</fact></p>
    <p><fact id="PACKAGE-MODULE-AUTHORITY-IS-THE-FULL-COORDINATE" status="impl/done">**For a package, the module authority MUST be the package's full
coordinate `&lt;group&gt;/&lt;name&gt;`** — the name is the first path segment,
`/`-joined exactly as in a pkgref (e.g.
`org.vibevm.ai-native/rust-ai-native-lang`), never a bare
`rust-ai-native-lang`.</fact></p>
    <p><fact id="THE-SLASH-MAKES-THE-BOUNDARY-DETERMINISTIC" status="impl/done">Where the surface allows it
(`spec://` authority, pkgref), `/` is the joiner — in neither the group
(`[a-z0-9.-]`) nor the name (`[a-z0-9-]`), so the boundary splits
deterministically with no grammar to consult. The dot is not thereby
forbidden in general: the name alphabet `[a-z0-9-]` already excludes it,
so a dot-free name makes the last dot the boundary just as deterministically
— the old prohibition (groups are themselves dotted) was refuted by these
same alphabets, not only by external ruling.</fact></p>
    <p><fact id="A-JOINER-MUST-ADMIT-A-DETERMINISTIC-INVERSE-PARSE" status="impl/done">The requirement on a
joiner is not "not a dot" but that the coordinate admit a **deterministic
inverse parse**, and exactly two things satisfy it. Either the separator lies
outside both alphabets (`/` here, and `_` here) — then the split needs no
grammar at all; or the grammar guarantees that one half cannot contain the
separator, and then the *outermost* occurrence on that half's side is the
boundary: a separator-free second half is split at the last occurrence, a
separator-free first half at the first. An ecosystem whose grammar guarantees
neither half must take the first route.</fact></p>
    <p><fact id="A-BLANKET-DOT-BAN-COSTS-THE-FLAT-SURFACES" status="impl/done">Flat surfaces — repository
names, artifact names — commonly forbid `/` outright, so a blanket
dot-prohibition forces those surfaces onto a joiner their own domain rules
contradict, where a both-LDH `&lt;group&gt;.&lt;name&gt;` is itself a valid reversed
FQDN. vibevm moved its flat carrier onto the dot by owner ruling 2026-08-13
(`PROP-029` `##CARRIER-REPO-SWAP`, `##joiner-why`); `_` stays legal wherever
the name grammar gives no guarantee.</fact></p>
    <p><fact id="THE-FULL-COORDINATE-IS-MECHANICALLY-REFACTORABLE" status="impl/done">A
bare authority resolves only with ambient context (which package am I
in?); the full coordinate is a
self-contained global symbol, which is what makes every `spec://`
citation *mechanically* refactorable — an algorithm rewrites all
occurrences on a rename, no resolver and no model in the loop.</fact></p>
    <p><fact id="this-is-the-addressing-half-of-prop-029" status="impl/done">This is
the addressing half of vibevm's PROP-029 (fully-qualified addresses and
mechanical refactoring).</fact></p>
  </section>
  <section id="single-source" title="Single source of truth">
    <p><fact id="EVERY-FACT-HAS-EXACTLY-ONE-AUTHORITATIVE-ANCHOR" status="impl/done">Every fact has exactly one authoritative anchor.</fact></p>
    <p><fact id="copying-a-value-is-a-time-bomb" status="spec/done">Citing the anchor
is free; copying its value into a second file is a time bomb: one
copy gets edited, the other does not, and a later session finds
600 s in one file and 300 s in the other with no way to know which
binds.</fact></p>
    <p><fact id="it-fixes-the-wrong-copy" status="spec/done">It "fixes" the wrong copy — or worse, the code.</fact></p>
    <p><fact id="one-bug-becomes-three" status="spec/done">One bug
becomes three, and untangling them means replaying weeks of git
history.</fact></p>
    <p><fact id="DUPLICATION-IS-NOT-REDUNDANCY" status="spec/done">Duplication is not redundancy — redundancy implies a
reconciliation mechanism, and copies have none.</fact></p>
    <p><fact id="A-NORMATIVE-VALUE-LIVES-AT-EXACTLY-ONE-ANCHOR" status="impl/done">The rule: a normative value lives at exactly one anchor.</fact></p>
    <p><fact id="EVERY-OTHER-DOCUMENT-CITES-THE-URI" status="impl/done">Every other
document cites the URI and lets the reader — human or model —
resolve it.</fact></p>
    <p><fact id="A-RESTATEMENT-NAMES-ITS-ANCHOR" status="impl/done">If prose flow demands restating the value, the
restatement names its anchor in the same sentence, marking which
copy is the echo.</fact></p>
  </section>
  <section id="disposable-targets" title="Never cite something built to disappear">
    <p><fact id="A-PLAN-ROW-IS-NOT-AN-ADDRESS" status="impl/done">A plan, a backlog row, a campaign document — anything a project
creates in order to finish and delete — is **not an address**. Cite the
specification element the work established, never the row that
commissioned it.</fact></p>
    <p><fact id="WHY-A-DISPOSABLE-TARGET-IS-WORSE-THAN-A-MISSING-ONE" status="spec/done">This is stricter than it sounds, and the reason is that the
failure is silent. A citation to a deleted anchor at least breaks
loudly. A citation to a plan row that still exists keeps resolving —
to a row describing work as it was *imagined*, long after the work
shipped and changed shape. The reader finds a plausible, dated, wrong
answer and has no signal that it is wrong.</fact></p>
    <p><fact id="ON-CLOSURE-CONTENT-MOVES-INTO-THE-SPECS" status="impl/done">**On closure, significant content moves into the
specifications**, and every statement pointing at the closed item is
re-pointed at the spec element that now carries the fact. Where the
item established no contract — it was a finding, not a rule — the
citation becomes plain text and the row number survives only as
history.</fact></p>
    <p><fact id="REBUILDING-THE-CITATIONS-IS-PART-OF-CLOSING" status="impl/done">**Rebuilding the citations is part of closing, not tidying
afterwards.** An item is closed when no tail remains: content moved,
statements re-pointed, and the moved content judged in the same pass
that moved it. Deferring that step is how a project accumulates
addresses that outlive their targets.</fact></p>
    <p><fact id="THE-LIFECYCLE-HALF-LIVES-IN-CAMPAIGN-PLANS" status="impl/done">The other half of this rule — that a plan is a temporary
artifact and what its tombstones are for — belongs to the campaign-plans
flow, at
`spec://org.vibevm.world/campaign-plans/flows/campaign-plans/CAMPAIGN-PLAN-FORMAT#temporary`.
One law, two homes, one pointer: this half is about addressing, that
half is about a plan's life.</fact></p>
  </section>
  <section id="placement" title="Placement: Lost in the Middle">
    <p><fact id="MODELS-ATTEND-TO-THE-BEGINNING-AND-THE-END" status="spec/done">An empirical result, not a style preference: language models attend
most reliably to the beginning and the end of a long context; facts
placed in the middle lose retrieval accuracy, with drops of up to
thirty percent measured ("Lost in the Middle", Liu et al., arXiv
preprint 2023; TACL 2024).</fact></p>
    <p><fact id="a-spec-document-is-context" status="spec/done">A spec document is context; the same
U-curve applies.</fact></p>
    <p><fact id="INVARIANTS-GO-AT-THE-OPENING-OR-THE-END" status="impl/done">Constraints, acceptance criteria, and unbreakable
invariants therefore go in the opening paragraphs or a final
"Invariants" section — never buried mid-document.</fact></p>
    <p><fact id="A-MID-FILE-INVARIANT-WAS-NOT-READ" status="spec/done">A mid-file
invariant is an invariant the agent statistically did not read.</fact></p>
  </section>
  <section id="graph" title="The graph consequence">
    <p><fact id="ADDRESSABLE-SPECS-GIVE-A-DEPENDENCY-GRAPH-FOR-FREE" status="impl/done">Addressable specs give the project a dependency graph for free.</fact></p>
    <p><fact id="CODE-MARKS-WHAT-IT-IMPLEMENTS-THE-SPEC-WHAT-VERIFIES-IT" status="impl/done">Code
marks what it implements; the spec records what verifies it: *(the two
forms below are the plain-text ones, and they are what needs no tooling.
Where a project mechanizes the graph, both records are commonly authored
on the **code** side instead — the implements edge as a language-native
tag on the item, and the verification edge as the same kind of tag on
the **test** rather than as a `Test:` line in the document — and the
spec-side answer, "what verifies this unit", is then rendered from the
graph rather than maintained by hand. Either form yields the same
bidirectional edge; only the authoring side moves.)*</fact></p>
    <fence>// Implements: spec://com.example.shop/PROP-001#verification.timeout</fence>
    <fence lang="markdown">### 5.3 Timeout {#verification.timeout}
Test: payments_core::tests::timeout_marks_old_messages</fence>
    <p><fact id="THESE-ARE-BIDIRECTIONAL-EDGES" status="impl/done">These are bidirectional edges: when one side changes, the other must
be re-checked.</fact></p>
    <p><fact id="NO-TOOLING-IS-REQUIRED-TO-BENEFIT" status="impl/done">No tooling is required to benefit — a plain
`grep -rn "PROP-001#verification.timeout"` answers "which code
implements this unit", the `Test:` line answers "which test verifies
it", and a failing test carries the address of the violated unit. Where
the graph is mechanized, the same two questions are answered by the
rendered view over the code-side tags instead (§[graph](#graph)).</fact></p>
    <p><fact id="the-graph-pays-off-from-the-first-marker" status="impl/done">Tools can mechanize the check later; the graph is useful the day
the first marker lands.</fact></p>
  </section>
  <section id="re-derive" title="Re-derive for your project">
    <p><fact id="COPY-THE-PROMPT-TASK-NOT-THE-IMPLEMENTATION" status="impl/done">Do not transplant these documents as dogma — copy the prompt-task,
not the prompt-implementation.</fact></p>
    <p><fact id="re-derive-prompt-lead" status="impl/done">Paste this and review the plan:</fact></p>
    <fence>Read this flow's documents (your project installed them — typically `vibedeps/flow-addressable-specs/&lt;version&gt;/spec/flows/addressable-specs/`, check `vibe.lock`) in this repository — all three
documents. Adapt the addressable-specs practice to this concrete
project:
1. Propose the URI scheme instance: module names (reverse-DNS if
   these specs could ever be shared), document naming, anchor style.
2. Sweep the existing spec/docs tree: list every heading that states
   a decision, constraint, or contract but carries no {#anchor}.
3. List every invariant buried mid-file; propose moving each to the
   top of its document or into a final "Invariants" section.
4. Find normative values duplicated across files; for each, name the
   one authoritative anchor and the copies to replace with citations.
5. Output all of it as a migration plan (file, anchor, action) and
   stop. Do not edit anything until I approve.</fence>
  </section>
  <section id="summary" title="Summary">
    <list ordered="false">
      <item><fact id="SUM-SPECS-ARE-THE-IPC-CHANNEL" status="impl/done">Spec files are the IPC channel between human and agent;
  addressability is that channel's first requirement.</fact></item>
      <item><fact id="SUM-URI-CORRECTION-COSTS-TWENTY-TOKENS" status="spec/done">Correction by URI costs ~20 tokens and hits exactly; by
  paraphrase, hundreds — and it may miss.</fact></item>
      <item><fact id="SUM-THE-URI-SCHEME" status="impl/done">`spec://&lt;module&gt;/&lt;doc&gt;#&lt;section&gt;[.&lt;sub&gt;]`; anchors are `{#id}`,
  dots are hierarchy, modules reverse-DNS when specs can be shared.</fact></item>
      <item><fact id="SUM-ONE-FACT-ONE-ANCHOR" status="impl/done">One fact, one anchor. Copies diverge silently; cite instead.</fact></item>
      <item><fact id="SUM-INVARIANTS-AT-THE-EDGES" status="impl/done">Invariants live at the start or end of a file — the middle is
  where models stop reading.</fact></item>
      <item><fact id="SUM-THE-BIDIRECTIONAL-GRAPH" status="impl/done">`Implements:` markers plus `Test:` lines form a
  bidirectional graph that pays off with zero tooling — and where the graph is
  mechanized, the same two edges are authored as code-side tags and the
  spec-side view is rendered (§[graph](#graph)).</fact></item>
    </list>
  </section>
</spec>
