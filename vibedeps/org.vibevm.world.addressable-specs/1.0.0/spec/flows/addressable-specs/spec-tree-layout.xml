<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Spec tree layout</title>
  <status stage="spec" state="done"/>
  <p><fact id="scope-of-this-document" status="impl/done">**Scope of this document.** The reference directory layout for an
addressable spec tree, the PROP / FEAT distinction, the decision
table for where each kind of fact lives, and the `.human/` private
buffer with its physical-invisibility rule.</fact></p>
  <p><fact id="protocol-document-pointer" status="impl/done">The addressing scheme the
layout serves is defined in
[`ADDRESSABLE-SPECS-PROTOCOL.md`](ADDRESSABLE-SPECS-PROTOCOL.md).</fact></p>
  <section id="tree" title="The reference tree">
    <fence>project/
├── spec/                      # the IPC buffer (shared state)
│   ├── BOOT.md                # entry point — the agent reads this first
│   ├── WAL.md                 # continuation state between sessions
│   ├── SPEC-PROTOCOL.md       # how specs are updated (conflict rules)
│   ├── common/
│   │   ├── PROP-000.md        # foundational decisions
│   │   └── structure.md       # module map
│   └── modules/
│       ├── payments/
│       │   ├── PROP-001.md    # standing decisions for this module
│       │   └── FEAT-001.md    # one feature slice
│       └── client/
├── src/                       # artifacts — generated, verifiable, re-creatable
├── tests/                     # executable specs
├── .human/                    # human-only buffer, excluded from agent view
│   └── shortcuts.md           # copy-paste corrections, private notes
├── .&lt;agent&gt;ignore             # what the agent never sees (.human/ at minimum)
└── CLAUDE.md / AGENTS.md / …  # harness entry files redirecting to spec/BOOT.md</fence>
    <p><fact id="SPEC-OR-SPECS-PICK-ONE" status="impl/done">`spec/` or `specs/` — either; pick one and never mix.</fact></p>
    <p><fact id="three-zones-lead" status="impl/done">The tree
divides into three zones with different loss semantics:</fact></p>
    <list ordered="false">
      <item><fact id="ZONE-SHARED-STATE" status="impl/done">**shared
  state** (`spec/` — losing it is a catastrophe, it is the only channel
  between the two processes),</fact></item>
      <item><fact id="ZONE-ARTIFACTS" status="impl/done">**artifacts** (`src/`, `tests/` — losing
  a file is an inconvenience; it can be regenerated from the spec),</fact></item>
      <item><fact id="ZONE-PRIVATE-BUFFERS" status="impl/done">and
  **private buffers** (`.human/` — one process's memory, invisible to
  the other).</fact></item>
    </list>
    <p><fact id="HARNESS-ENTRY-FILES-ARE-THIN-REDIRECTS" status="impl/done">Harness entry files (`CLAUDE.md`, `AGENTS.md`, and whatever the next
tool demands) should be thin redirects into `spec/BOOT.md`.</fact></p>
    <p><fact id="one-boot-sequence-many-doors" status="spec/done">One boot
sequence, many doors — the alternative is N slowly diverging copies.</fact></p>
  </section>
  <section id="prop-vs-feat" title="PROP vs FEAT">
    <table>
      <tr>
        <td/>
        <td>PROP</td>
        <td>FEAT</td>
      </tr>
      <tr>
        <td><fact id="ROW-PROP-FEAT-HOLDS" status="impl/done">Holds</fact></td>
        <td><fact status="impl/done">standing decisions, contracts, protocol rules</fact></td>
        <td><fact status="impl/done">one feature slice: scope, plan, acceptance criteria</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-PROP-FEAT-LIFETIME" status="impl/done">Lifetime</fact></td>
        <td><fact status="impl/done">in force until superseded — never deleted</fact></td>
        <td><fact status="impl/done">completes and freezes when the slice ships</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-PROP-FEAT-CHANGES" status="impl/done">Changes</fact></td>
        <td><fact status="impl/done">rarely; every semantic change is a changelog line</fact></td>
        <td><fact status="impl/done">freely while active; frozen afterwards</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-PROP-FEAT-CITED-BY" status="impl/done">Cited by</fact></td>
        <td><fact status="impl/done">code markers, commits, other specs — long-lived anchors</fact></td>
        <td><fact status="impl/done">the WAL and the commits of its own campaign</fact></td>
      </tr>
    </table>
    <p><fact id="PROP-IS-THE-LAW-FEAT-IS-A-PROJECT-UNDER-IT" status="impl/done">PROP is the law; FEAT is a project under that law.</fact></p>
    <p><fact id="A-LASTING-DECISION-MOVES-UP-INTO-A-PROP" status="impl/done">When a FEAT
uncovers a lasting decision, the decision moves *up* into a PROP and
the FEAT cites it — the slice document never becomes the permanent
home of a permanent rule.</fact></p>
    <p><fact id="NUMBER-PER-MODULE-AND-NEVER-RENUMBER" status="impl/done">Number per module (`PROP-001`, `FEAT-003`, …) and never renumber:
the number is part of the URI, and URIs are forever
([authoring rules §anchor-stability](authoring-rules.md#anchor-stability)).</fact></p>
  </section>
  <section id="what-goes-where" title="What goes where">
    <table>
      <tr>
        <td>Fact</td>
        <td>Home</td>
      </tr>
      <tr>
        <td><fact id="ROW-HOME-ARCHITECTURE" status="impl/done">Architecture, stack, cross-module decision</fact></td>
        <td><fact status="impl/done">`spec/common/PROP-*`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-HOME-MODULE-CONTRACT" status="impl/done">One module's contract or invariant</fact></td>
        <td><fact status="impl/done">`spec/modules/&lt;m&gt;/PROP-*`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-HOME-FEATURE-SCOPE" status="impl/done">A feature's scope and acceptance criteria</fact></td>
        <td><fact status="impl/done">`spec/modules/&lt;m&gt;/FEAT-*` — or a campaign plan where the project runs slices as plans (`flow:campaign-plans`)</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-HOME-NEXT-SESSION-STEP" status="impl/done">What the next session must do first</fact></td>
        <td><fact status="impl/done">`spec/WAL.md`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-HOME-SPEC-PROTOCOL" status="impl/done">How specs are updated, conflict rules</fact></td>
        <td><fact status="impl/done">protocol docs at the spec root</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-HOME-BOOT-MINIMUM" status="impl/done">The session-boot minimum</fact></td>
        <td><fact status="impl/done">boot entry file (≤ 500 tokens)</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-HOME-IMPLEMENTATION-DETAIL" status="impl/done">Implementation detail (*how*)</fact></td>
        <td><fact status="impl/done">code and doc comments — never the spec</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-HOME-HUMAN-ONLY-NOTES" status="impl/done">Copy-paste snippets, human-only reminders</fact></td>
        <td><fact status="impl/done">`.human/`</fact></td>
      </tr>
    </table>
    <p><fact id="CHOOSING-BETWEEN-THE-TWO-HOMES" status="impl/done">**Choosing between the two homes.** The row above names two
carriers for a feature's plan; which one carries it is a choice with
consequences, because it decides whether the plan stays addressable
after the work ships and whether it outlives the effort that produced
it. Make the choice explicitly and say it to whoever asked for the
work.</fact></p>
    <p><fact id="A-FEATURE-DOCUMENT-CARRIES-A-FEATURE" status="impl/done">A **feature document** carries work that is a feature: it has a
name its user would recognise, it will be referred to again after it
ships, and it will not finish in one sitting. It is one file in the
feature's home, and it is addressable like any other specification
document — every anchored heading it carries becomes an address for
free, with nothing to register.</fact></p>
    <p><fact id="A-CAMPAIGN-PLAN-CARRIES-A-PROGRAMME" status="impl/done">A **campaign plan** carries a programme rather than a feature:
many documents or many packages, a unit of work that is a batch
rather than a change, and a lifetime that ends when the campaign
closes. Its zone is disposable by design, so nothing that must
outlive the campaign belongs inside it.</fact></p>
    <p><fact id="NEITHER-CARRIES-SMALL-WORK" status="impl/done">**Neither carries small work.** A three-line change planned into a
feature document is a document with more ceremony than content; the
current slice's checklist carries it, and that is the whole plan it
needs.</fact></p>
    <p><fact id="THE-THRESHOLD-STAYS-QUALITATIVE" status="impl/done">The threshold between them stays qualitative on purpose. A line
count would be a number nobody measured, and a rule that files work
by size rather than by kind puts a rename and a subsystem in one
bucket whenever the count happens to agree.</fact></p>
    <p><fact id="THE-CHOICE-BELONGS-TO-WHOEVER-ASKED-FOR-THE-WORK" status="impl/done">**The choice belongs to whoever asked for the work, not to the
agent doing it.** An agent that picks the carrier silently has
decided how long the work stays findable, which is not a decision it
was given: it proposes a carrier with its reason, and asks.</fact></p>
    <p><fact id="DEFAULT-A-HOMELESS-FACT-INTO-THE-SPEC-TREE" status="impl/done">When a fact has no obvious home, default it into the spec tree
rather than a wiki, a gist, or a head: a teammate — human or agent —
who clones the repository must receive everything the project knows.</fact></p>
  </section>
  <section id="human-buffer" title="The `.human/` private buffer">
    <p><fact id="HUMAN-BUFFER-HOLDS-TEXT-THE-AGENT-MUST-NEVER-SEE" status="impl/done">`.human/` holds text that must never enter the agent's context:
copy-paste correction snippets ("you are drifting, re-read the
spec"), negotiation notes, half-formed doubts.</fact></p>
    <p><fact id="two-reasons-lead" status="impl/done">Two reasons to keep
it out:</fact></p>
    <list ordered="false">
      <item><fact id="REASON-TOKEN-COST" status="spec/done">**Token cost.** Every file the agent reads is context spent; a
  shortcuts file is pure overhead for any task.</fact></item>
      <item><fact id="REASON-REACTION-RISK" status="spec/done">**Reaction risk.** An agent that reads "you are drifting" mid-task
  may *respond* to it — reflecting on drift it has not committed,
  polluting the session with a correction nobody issued.</fact></item>
    </list>
    <p><fact id="PHYSICAL-INVISIBILITY-BEATS-LOGICAL-PROHIBITION" status="impl/done">The enforcement rule: **physical invisibility beats logical
prohibition.**</fact></p>
    <p><fact id="DO-NOT-WRITE-A-NEVER-READ-LINE-IN-THE-BOOT-FILE" status="impl/done">Do not write "never read `.human/`" in the boot file
— that line itself costs tokens forever and invites the very
attention it forbids.</fact></p>
    <p><fact id="LIST-THE-BUFFER-IN-THE-IGNORE-MECHANISM" status="impl/done">Instead, list `.human/` in the agent's ignore
mechanism (`.claudeignore`, `.aiexclude`, `.cursorignore` — whatever
the harness supports), so the directory does not exist as far as the
agent can see.</fact></p>
    <p><fact id="a-firewall-beats-a-no-entry-sign" status="spec/done">A firewall beats a "no entry" sign.</fact></p>
    <p><fact id="NO-IGNORE-FILE-MEANS-KEEP-IT-OUTSIDE-THE-TREE" status="impl/done">If the harness supports no ignore file, keep the buffer outside the
repository working tree entirely.</fact></p>
    <p><fact id="the-principle-survives-the-mechanism" status="spec/done">The principle survives the
mechanism.</fact></p>
  </section>
  <section id="naming" title="Naming maps to addressing">
    <p><fact id="layout-is-the-uri-scheme-lead" status="impl/done">The layout is the URI scheme made physical:</fact></p>
    <list ordered="false">
      <item><fact id="SEGMENT-MODULE-IS-THE-DIRECTORY" status="impl/done">the directory name under `spec/modules/` is the `&lt;module&gt;` segment;</fact></item>
      <item><fact id="SEGMENT-DOC-IS-THE-FILE-NAME" status="impl/done">the file name minus `.md` is the `&lt;doc&gt;` segment;</fact></item>
      <item><fact id="SEGMENT-FRAGMENT-IS-THE-ANCHOR" status="impl/done">the `{#anchor}` in the file is the fragment.</fact></item>
    </list>
    <p><fact id="A-URI-RESOLVES-WITH-ZERO-INDEX" status="impl/done">`spec://com.example.shop/PROP-001#verification.timeout` resolves with
zero index: `spec/modules/com.example.shop/PROP-001.md`, then find
`{#verification.timeout}`.</fact></p>
    <p><fact id="KEEP-THE-MAPPING-ONE-TO-ONE" status="impl/done">Keep the mapping one-to-one — the moment
resolution needs a lookup table, every citation costs a search, and
the twenty-token correction stops being twenty tokens.</fact></p>
    <p><fact id="REVERSE-DNS-WHEN-SPECS-MAY-BE-SHARED" status="impl/done">Use reverse-DNS module directory names when the specs could ever be
shared beyond this repository; short local names are fine when they
provably cannot.</fact></p>
  </section>
  <section id="migrating" title="Migrating an existing project">
    <p><fact id="decisions-arrive-scattered" status="spec/done">Most projects arrive with decisions scattered across READMEs, wikis,
docstrings, and heads.</fact></p>
    <p><fact id="delegate-the-inventory-lead" status="impl/done">Delegate the inventory:</fact></p>
    <fence>Inventory every Markdown file in this repository that states a
decision, requirement, or plan. For each: current path, what kind
of fact it holds (per the what-goes-where table in
spec/flows/addressable-specs/spec-tree-layout.md), its proposed
home in the spec tree, and which headings need {#anchor}s.
Output a migration table. Move nothing yet.</fence>
    <p><fact id="MIGRATE-IN-SMALL-REVIEWABLE-STEPS" status="impl/done">Review the table, then migrate in small, reviewable steps — the tree
is load-bearing, so it deserves the same care as a schema migration.</fact></p>
  </section>
  <section id="summary" title="Summary">
    <list ordered="false">
      <item><fact id="SUM-THREE-ZONES" status="impl/done">Three zones: shared state (`spec/`), artifacts (`src/`, `tests/`),
  private buffers (`.human/`). Their loss semantics differ; treat
  them accordingly.</fact></item>
      <item><fact id="SUM-PROP-IS-LAW-FEAT-IS-A-SLICE" status="impl/done">PROP is standing law, FEAT is a slice under it; lasting decisions
  migrate up. Numbers are part of URIs — never renumber.</fact></item>
      <item><fact id="SUM-EVERY-FACT-HAS-A-DESIGNATED-HOME" status="impl/done">Every fact has a designated home; when in doubt, it goes into the
  spec tree, where a fresh clone can find it.</fact></item>
      <item><fact id="SUM-HUMAN-BUFFER-IS-ENFORCED-BY-INVISIBILITY" status="impl/done">`.human/` is enforced by ignore-file invisibility, not by a rule
  the agent must read to obey.</fact></item>
      <item><fact id="SUM-NAMES-ARE-THE-URI-SEGMENTS" status="impl/done">Directory and file names *are* the URI segments — resolution must
  work with zero index.</fact></item>
    </list>
  </section>
</spec>
