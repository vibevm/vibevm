[package]
name = "addressable-specs"
group = "org.vibevm.world"
kind = "flow"
version = "0.1.0"
authors = ["Oleg Chirukhin"]
license = "UPL-1.0"
description = "Addressable specifications: spec:// URIs, stable anchors, and the spec tree layout that lets a human correct an agent in twenty tokens"
keywords = ["specs", "addressing", "uri", "anchors", "spec-driven"]

[compatibility]
min_vibe_version = "0.1.0"
requires_kinds = []

[boot_snippet]
source = "spec/boot/15-flow-addressable-specs.md"
category = "flow"
