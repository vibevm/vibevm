<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Conflict Protocol</title>
  <status stage="spec" state="done"/>
  <p><fact id="scope-of-this-document" status="impl/done">**Scope of this document.** This file defines *how* two writers — a
human and a coding agent — share one file set without corrupting it:
the priority hierarchy that settles any disagreement between layers,
the REVIEW-marker protocol for disputing a spec without overriding
it, the marker lifecycle from placement to removal, and a worked
example of what one silent change costs when the protocol is skipped.</fact></p>
  <section id="two-writers" title="Two writers, one file set">
    <p><fact id="A-PROJECT-HAS-EXACTLY-ONE-SHARED-MEMORY-THE-FILES" status="spec/done">A spec-driven project has exactly one shared memory: the files.</fact></p>
    <p><fact id="the-human-edits-them" status="spec/done">The
human edits them.</fact></p>
    <p><fact id="the-agent-edits-them" status="spec/done">The agent edits them.</fact></p>
    <p><fact id="THERE-IS-NO-THIRD-CHANNEL" status="spec/done">There is no third channel —
no hallway conversation, no "as we discussed yesterday".</fact></p>
    <p><fact id="TWO-WRITERS-WILL-PRODUCE-CONTRADICTIONS" status="spec/done">Two writers
over one file set *will* produce contradictions:</fact></p>
    <list ordered="false">
      <item><fact id="CONTRADICTION-A-VALUE-CHANGED-IN-CODE-NOT-IN-THE-SPEC" status="spec/done">a value changed in
  code but not in the spec,</fact></item>
      <item><fact id="CONTRADICTION-A-TEST-ASSERTING-YESTERDAYS-RULE" status="spec/done">a test asserting yesterday's rule,</fact></item>
      <item><fact id="CONTRADICTION-A-STATE-FILE-DESCRIBING-REVERTED-WORK" status="spec/done">a state
  file describing work that was later reverted.</fact></item>
    </list>
    <p><fact id="CONTRADICTION-IS-NORMAL-COOPERATION-NOT-AN-ERROR" status="impl/done">This is normal cooperation, not an error.</fact></p>
    <p><fact id="treating-contradiction-as-shameful-hides-it" status="spec/done">Treating each contradiction
as a failure to be ashamed of is counterproductive — it trains both
writers to hide disagreement, and hidden disagreement is the only
kind that does damage.</fact></p>
    <p><fact id="THE-FAILURE-MODE-IS-THE-UNRESOLVED-CONFLICT" status="spec/done">The failure mode is the *unresolved* conflict,
found weeks later by a reader who cannot tell which side was right.</fact></p>
    <p><fact id="concurrent-programming-met-this-problem-long-ago" status="spec/done">Concurrent programming met this problem long ago: two processes
writing shared memory without an ordering rule produce a race
condition, and no post-hoc reading can reconstruct the intent.</fact></p>
    <p><fact id="the-fix-there-is-a-memory-fence" status="spec/done">The
fix there is a memory fence — an explicit ordering both processes
obey.</fact></p>
    <p><fact id="THE-FIX-HERE-IS-THE-SAME-FENCE-ADAPTED-FOR-PROSE" status="impl/done">The fix here is the same fence, adapted for prose.</fact></p>
  </section>
  <section id="hierarchy" title="The hierarchy">
    <fence>Human  &gt;  Spec  &gt;  Tests  &gt;  Code  &gt;  WAL</fence>
    <table>
      <tr>
        <td>Relation</td>
        <td>Operational meaning</td>
      </tr>
      <tr>
        <td><fact id="ROW-HUMAN-BEATS-SPEC" status="impl/done">**Human &gt; Spec**</fact></td>
        <td><fact status="impl/done">The human may change the spec — that is what winning means here. A human instruction that contradicts the spec is not a conflict; it is a spec change that has not been written down yet. Write it down, then follow it.</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-SPEC-BEATS-CODE" status="impl/done">**Spec &gt; Code**</fact></td>
        <td><fact status="impl/done">Code must conform to the spec. When they disagree, the code is wrong *by definition* until the human rules otherwise. Fix the code, or dispute the spec with a REVIEW marker — never by editing the spec.</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-TESTS-ARE-THE-SPEC-EXECUTABLE" status="impl/done">**Tests = Spec, executable**</fact></td>
        <td><fact status="impl/done">Tests are the spec in executable form (next section).</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-CODE-BEATS-WAL" status="impl/done">**Code &gt; WAL**</fact></td>
        <td><fact status="impl/done">The volatile state file (WAL or equivalent) is a record of where work stands, not a source of truth about intended behaviour. When it disagrees with anything above it, it is stale — correct the record, not the truth.</fact></td>
      </tr>
    </table>
    <p><fact id="THE-ORDER-IS-TOTAL-AND-ACYCLIC" status="impl/done">Unlike rock-paper-scissors — or rock-paper-scissors-lizard-Spock —
this order is total and acyclic.</fact></p>
    <p><fact id="predictability-is-the-entire-point" status="spec/done">Every pairing has a predetermined
winner, and that predictability is the entire point: two writers who
both know the resolution table never negotiate mid-write, and a
reader who finds a contradiction knows instantly which side to trust.</fact></p>
    <p><fact id="newer-appears-nowhere-in-the-table" status="impl/done">Note what appears nowhere in the table: "newer".</fact></p>
    <p><fact id="RECENCY-IS-EVIDENCE-OF-WHEN-NOT-OF-AUTHORITY" status="impl/done">Recency is evidence
about *when* a change happened, not whether it was authorized.</fact></p>
    <p><fact id="THE-MOST-DAMAGING-MOVE-IS-SYNCING-THE-SPEC-TO-NEWER-CODE" status="spec/done">The
most damaging move available to an agent is resolving a spec-vs-code
disagreement by assuming the code is newer and "syncing" the spec to
match — see the worked example below.</fact></p>
  </section>
  <section id="tests" title="Tests are the spec in executable form">
    <p><fact id="TESTS-SIT-BESIDE-THE-SPEC-NOT-BELOW-IT" status="impl/done">Tests sit beside the spec, not below it: the same rules, phrased in a
language a machine can run.</fact></p>
    <p><fact id="the-corollary-is-a-sharp-diagnostic-rule" status="impl/done">The corollary is a sharp diagnostic rule:</fact></p>
    <quote><fact id="A-TEST-CONTRADICTING-THE-SPEC-IS-A-BUG-IN-EXACTLY-ONE" status="impl/done">A test that contradicts the spec is a bug in exactly one of the two
— never both.</fact></quote>
    <p><fact id="EITHER-THE-TEST-IS-STALE-OR-THE-SPEC-PROSE-HAS-DECAYED" status="impl/done">Either the test encodes yesterday's spec (fix the test), or the test
is right and the spec prose has decayed (dispute it with a REVIEW
marker; the human rules).</fact></p>
    <p><fact id="THE-FORBIDDEN-THIRD-PATH-IS-WEAKENING-THE-TEST" status="impl/done">The forbidden third path is weakening the
test until the code passes without consulting the spec at all — that
deletes the project's only automatic tripwire for divergence.</fact></p>
  </section>
  <section id="review-protocol" title="The REVIEW-marker protocol">
    <p><fact id="the-agent-will-sometimes-believe-the-spec-is-wrong" status="spec/done">The agent will sometimes believe the spec is wrong — and sometimes it
will *be* wrong.</fact></p>
    <p><fact id="the-protocol-when-that-happens" status="impl/done">The protocol when that happens:</fact></p>
    <list ordered="true">
      <item><fact id="REVIEW-STEP-IMPLEMENT-WHAT-THE-SPEC-SAYS" status="impl/done">**Implement what the spec says.** The spec wins the current cycle,
   including when you disagree with it.</fact></item>
      <item><fact id="REVIEW-STEP-ADD-A-MARKER-WITH-A-REASON" status="impl/done">**Add a REVIEW marker at the point of disagreement**, always with
   a reason:</fact></item>
    </list>
    <fence>   &lt;!-- REVIEW: §5.3 would be better served by exponential backoff
        than a fixed 600s timeout, because retries cluster at the
        boundary under VPN latency --&gt;</fence>
    <list ordered="true">
      <item><fact id="REVIEW-STEP-SURFACE-IT-IN-THE-END-OF-SESSION-REPORT" status="impl/done">**Surface it in the end-of-session report:** "Implemented the
   fixed timeout per §5.3; propose exponential backoff instead — see
   REVIEW in PROP-001 §5.3."</fact></item>
      <item><fact id="REVIEW-STEP-THE-HUMAN-DECIDES-IN-THE-NEXT-CYCLE" status="impl/done">**The human decides in the next cycle** — accepts (and changes the
   spec) or rejects (and the marker comes out).</fact></item>
    </list>
    <p><fact id="three-lines-of-text" status="spec/done">Three lines of text.</fact></p>
    <p><fact id="seconds-to-write-a-minute-to-read" status="spec/done">Seconds to write, a minute to read.</fact></p>
    <p><fact id="that-is-the-entire-price-of-no-silent-override" status="spec/done">That is the
entire price of never having a silent override in the repository.</fact></p>
    <section id="placement" title="Where markers go">
      <list ordered="false">
        <item><fact id="MARKER-GOES-IN-THE-SPEC" status="impl/done">**In the spec**, next to the contested clause, when the dispute is
  about what the rule should *be*.</fact></item>
        <item><fact id="MARKER-GOES-IN-THE-CODE" status="impl/done">**In the code**, next to the implementing lines, when the dispute
  is about how the rule lands in practice — in the language's own
  comment syntax (`// REVIEW: …`); the HTML form is for Markdown.</fact></item>
        <item><fact id="MARKER-ALWAYS-CARRIES-A-REASON" status="impl/done">**Always with a reason.** A bare `REVIEW: check this` cannot be
  ruled on; markers without reasons are noise wearing a uniform.</fact></item>
      </list>
    </section>
    <section id="bureaucracy" title="The bureaucracy objection">
      <p><fact id="on-first-contact-it-reads-as-bureaucracy" status="spec/done">On first contact this protocol reads as bureaucracy.</fact></p>
      <p><fact id="the-protocol-is-the-opposite-on-all-three-axes" status="spec/done">But real
bureaucracy's sin is being slow, expensive, and useless; this is the
opposite on all three axes — seconds to execute, cheaper in tokens
than one reasoning session over an undocumented disagreement, and it
turns every dispute into a one-line decision the human makes at a
glance.</fact></p>
      <p><fact id="CHEAP-PROCESS-THAT-PREVENTS-EXPENSIVE-ARCHAEOLOGY-IS-A-FENCE" status="spec/done">Cheap process that prevents expensive archaeology is a fence.</fact></p>
    </section>
  </section>
  <section id="silent-change" title="What one silent change costs">
    <p><fact id="a-worked-example-needing-no-malice" status="spec/done">A worked example. No step in it requires malice or incompetence —
only the absence of the protocol.</fact></p>
    <fence>Day 0   The agent changes TIMEOUT from 600s to 300s while touching
        nearby code ("300 is more responsive"). No marker, no report
        line. The human misses it: the diff is long, the change is
        two characters.

Day 7   A fresh session finds code = 300s, spec = 600s. No record
        says which is right. It reasons "the code is newer, the spec
        must be stale" and rewrites the spec to 300s. The forbidden
        move — resolving by recency — now looks like diligence.

Day 14  The human remembers *why* 600s existed (300s produced false
        timeouts for users on slow links), opens the spec, finds
        300s, changes it back — but by now two other code paths
        assume 300s.

Result  One unauthorized edit became three bugs, plus a two-week git
        archaeology dig to establish who changed what, when, and
        which value was ever actually intended.</fence>
    <p><fact id="THIS-IS-A-DATA-RACE-AT-FILE-LEVEL" status="spec/done">This is a data race at file level: two writers, no fence, an outcome
no one chose.</fact></p>
    <p><fact id="WITH-THE-PROTOCOL-THE-CHAIN-NEVER-STARTS" status="impl/done">Run the same Day 0 *with* the protocol and the chain
never starts — the agent implements 600s, places one REVIEW marker
proposing 300s, and the human rules on it next morning in a minute.</fact></p>
  </section>
  <section id="lifecycle" title="Marker lifecycle">
    <p><fact id="A-MARKER-IS-A-MESSAGE-IN-FLIGHT-NOT-A-PERMANENT-ANNOTATION" status="impl/done">A REVIEW marker is a message in flight, not a permanent annotation.</fact></p>
    <list ordered="true">
      <item><fact id="LIFECYCLE-PLACED" status="impl/done">**Placed** — during work, with a reason, at the point of
   disagreement.</fact></item>
      <item><fact id="LIFECYCLE-SURFACED" status="impl/done">**Surfaced** — named in the end-of-session report with its file
   and section, so the human never has to grep for surprises.</fact></item>
      <item><fact id="LIFECYCLE-RESOLVED" status="impl/done">**Resolved** — the human accepts (the spec changes) or rejects
   (the proposal dies; record one line of *why* if it will recur).</fact></item>
      <item><fact id="LIFECYCLE-REMOVED-IN-THE-RESOLVING-CHANGE" status="impl/done">**Removed in the same change that resolves it.** A resolved marker
   left in place is indistinguishable from an open one, and the next
   reader will re-litigate it.</fact></item>
    </list>
    <p><fact id="AGING-MARKERS-ARE-AUDIT-INPUT" status="impl/done">Aging markers are audit input.</fact></p>
    <p><fact id="A-LONG-LIVED-MARKER-MEANS-SURFACING-FAILED" status="impl/done">A marker that survives more than a few
sessions means step 2 failed — the surfacing channel is broken or
reports go unread.</fact></p>
    <p><fact id="SWEEP-FOR-REVIEW-MARKERS-PERIODICALLY" status="impl/done">Sweep for `REVIEW:` periodically; every hit is an
open decision the human owes, or a removal someone forgot.</fact></p>
    <p><fact id="two-standing-prohibitions" status="impl/done">Two standing prohibitions:</fact></p>
    <list ordered="false">
      <item><fact id="NEVER-REMOVE-ANOTHER-WRITERS-MARKER-WITHOUT-RESOLVING-IT" status="impl/done">never remove another writer's marker
  without resolving it (that is deleting mail unread),</fact></item>
      <item><fact id="NEVER-RESOLVE-YOUR-OWN-MARKER-BY-ASSUMING-AGREEMENT" status="impl/done">and never
  "resolve" your own marker by deciding the human would probably agree.</fact></item>
    </list>
  </section>
  <section id="re-derive" title="Re-derive for your project">
    <p><fact id="this-document-states-the-practice-in-project-neutral-terms" status="impl/done">This document states the practice in project-neutral terms.</fact></p>
    <p><fact id="ADAPT-IT-BY-HANDING-YOUR-AGENT-THE-TASK" status="impl/done">Adapt it
by handing your agent the task, not a copied template:</fact></p>
    <fence>Read this flow's documents (your project installed them — typically `vibedeps/flow-conflict-protocol/&lt;version&gt;/spec/flows/conflict-protocol/`, check `vibe.lock`) end to end. Then adapt the
practice to this project:
1. Name the writers: which humans and which agents edit the spec
   tree, the tests, and the code.
2. Name the layers: list our spec documents, test suites, and any
   volatile state files, and restate the hierarchy in those names.
3. Fix the marker syntax per file type (HTML comment in Markdown,
   line comment in code) and the exact grep pattern for sweeps.
4. Fix the surfacing channel: where end-of-session reports live and
   where open markers get listed.
5. Write the result to spec/flows/conflict-protocol/local.md, and
   add the hierarchy one-liner plus the Never list to the boot file.
Show me the draft before writing anything.</fence>
  </section>
  <section id="summary" title="Summary">
    <list ordered="false">
      <item><fact id="SUM-CONTRADICTION-IS-COOPERATION-ONLY-SILENCE-DAMAGES" status="spec/done">Two writers on one file set will contradict each other; that is
  cooperation working. Only *silent* contradiction does damage.</fact></item>
      <item><fact id="SUM-FIXED-PRIORITY-SETTLES-EVERYTHING" status="impl/done">Fixed priority settles everything: Human &gt; Spec &gt; Tests &gt; Code &gt;
  WAL. Recency settles nothing.</fact></item>
      <item><fact id="SUM-THE-REVIEW-CYCLE" status="impl/done">Disagree with the spec? Implement it anyway, mark `REVIEW:` with a
  reason, surface it in the report, let the human rule next cycle.</fact></item>
      <item><fact id="SUM-A-TEST-CONTRADICTING-THE-SPEC-IS-A-BUG-IN-ONE-OF-THE-TWO" status="impl/done">A test contradicting the spec is a bug in exactly one of the two —
  never both, and never fixed by weakening the test blind.</fact></item>
      <item><fact id="SUM-MARKERS-ARE-MESSAGES-IN-FLIGHT" status="impl/done">Markers are messages in flight: placed, surfaced, resolved, removed
  in the resolving change. Aging markers mean the channel is broken.</fact></item>
    </list>
  </section>
</spec>
