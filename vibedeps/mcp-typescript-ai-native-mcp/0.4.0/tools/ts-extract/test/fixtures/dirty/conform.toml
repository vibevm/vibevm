# The dirty fixture's conform policy: everything gated, so the e2e test
# proves each TS rule fires. NOT a template — `discipline-typescript
# init` writes the starting policy for real projects.
roots = []

[typescript]
roots = ["src"]
exclude_substrings = []
cells_dir = "src/cells"
seam = "index"
