<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">The Two-Process Model</title>
  <status stage="spec" state="done"/>
  <p><fact id="scope-of-this-document" status="impl/done">**Scope of this document.** This file defines the mental model the
whole redbook collection rests on: *what* the human and the AI each
are as computational processes, *why* the two dominant metaphors for
working with an AI fail on real projects, *how* a productive cycle
between the two processes looks, and *what* standing consequences
follow.</fact></p>
  <p><fact id="every-other-flow-is-a-consequence" status="spec/done">Every other flow in the collection is a consequence of this
model.</fact></p>
  <section id="wrong-metaphors" title="The two wrong metaphors">
    <p><fact id="everyone-begins-with-one-of-two-mental-models" status="spec/done">Everyone who starts working with a language model on a production
project begins with one of two mental models.</fact></p>
    <p><fact id="BOTH-FAIL-AND-BOTH-FAIL-THE-SAME-WAY" status="spec/done">Both fail, and both fail the same way.</fact></p>
    <section id="boss" title="Boss and subordinate">
      <p><fact id="the-boss-and-subordinate-loop" status="spec/done">The human formulates the task, the AI executes, the human inspects,
the AI fixes.</fact></p>
      <p><fact id="the-loop-is-familiar" status="spec/done">Familiar — most of the industry runs on it.</fact></p>
      <p><fact id="THE-AI-IS-A-BAD-SUBORDINATE" status="spec/done">But the AI
is a *bad subordinate*: it remembers nothing between sessions, it
does not ask clarifying questions when the task is vague — it
guesses, confidently and often wrongly — and it does not learn from
last week's corrections, because for it there was no last week.</fact></p>
      <p><fact id="THE-DEEPER-FAILURE-IS-LOAD-DISTRIBUTION" status="spec/done">The deeper failure is load distribution.</fact></p>
      <p><fact id="THE-HUMAN-CARRIES-ALL-OF-THE-THINKING" status="spec/done">In this model the human
carries all of the thinking: planning, decomposition, verification,
the big picture.</fact></p>
      <p><fact id="the-ai-contributes-fast-hands" status="spec/done">The AI contributes fast hands.</fact></p>
      <p><fact id="fast-hands-are-an-expensive-goal" status="spec/done">If fast hands were
the goal, the strongest model on the market would be an expensive
way to buy them.</fact></p>
    </section>
    <section id="tool" title="Human and tool">
      <p><fact id="the-ai-as-a-very-good-autocomplete" status="spec/done">The AI as a very good autocomplete: start a function, it finishes;
describe a component, it generates.</fact></p>
      <p><fact id="A-TOOL-HAS-NO-NOTION-OF-THE-PROJECT" status="spec/done">Fine for small tasks, corrosive
for a months-long project — a tool has no notion of the project.</fact></p>
      <p><fact id="A-TOOL-OPTIMIZES-LOCALLY-AND-DESTROYS-GLOBAL-CONSISTENCY" status="spec/done">It
optimizes locally (this file, this function) and can quietly destroy
global consistency doing so.</fact></p>
      <p><fact id="EVERY-INVOCATION-IS-A-DISCONNECTED-ACT" status="spec/done">Every invocation is a disconnected act.</fact></p>
    </section>
    <section id="shared-mistake" title="The shared mistake">
      <p><fact id="THE-HUMAN-TAKES-ON-ONE-HUNDRED-PERCENT-OF-THE-COGNITIVE-LOAD" status="spec/done">In both models the human takes on **one hundred percent of the
cognitive load**, and the human is not built for sustained 100%
load on non-routine work.</fact></p>
      <p><fact id="BOTH-MODELS-WASTE-THE-ACTUAL-OPPORTUNITY" status="spec/done">Both models also waste the actual
opportunity: distributing *thinking* — not typing — across two
processes with different architectures.</fact></p>
    </section>
  </section>
  <section id="coprocessors" title="Coprocessors">
    <p><fact id="picture-a-cpu-and-a-gpu" status="spec/done">Picture a system with two processors of *different architectures*
working one task — a CPU and a GPU.</fact></p>
    <p><fact id="the-cpu-and-the-gpu-each-win-elsewhere" status="spec/done">The CPU wins on sequential logic
with deep dependencies, the GPU on massive parallelism of simple
operations.</fact></p>
    <p><fact id="NEITHER-IS-BETTER-THE-POWER-IS-THE-SPLIT" status="spec/done">Neither is "better"; the system's power is the split.</fact></p>
    <table>
      <tr>
        <td/>
        <td>Strong at</td>
        <td>Weak at</td>
      </tr>
      <tr>
        <td><fact id="ROW-HUMAN" status="spec/done">**Human**</fact></td>
        <td><fact status="spec/done">persistent memory (weeks, years); intent and the "spirit" of a decision; intuition ahead of formalization; deep verification across the whole project; decisions under uncertainty; taste — aesthetic, ethical, UX</fact></td>
        <td><fact status="spec/done">throughput (reads and writes slowly); mechanical consistency (typos, forgotten twin files); holding many details at once (7±2); routine repetition; work under fatigue</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-AI" status="spec/done">**AI**</fact></td>
        <td><fact status="spec/done">throughput (thousands of consistent lines per minute); mechanical consistency within a session; broad shallow erudition (syntaxes, APIs); routine transforms and boilerplate; formal structure (parse, transform, generate); tirelessness within the session budget</fact></td>
        <td><fact status="spec/done">persistent memory (none across sessions); the spirit of a decision (follows the letter); long-range coherence; decisions needing context beyond the window; catching its own errors; volition — it cannot want the project to succeed</fact></td>
      </tr>
    </table>
    <p><fact id="THE-TWO-COLUMNS-ARE-COMPLEMENTARY" status="spec/done">The key observation: **the two columns are complementary**.</fact></p>
    <p><fact id="THE-WEAKNESSES-OF-EACH-ARE-THE-STRENGTHS-OF-THE-OTHER" status="spec/done">The weaknesses of each process are the strengths of the other.</fact></p>
    <p><fact id="the-human-holds-the-picture-the-ai-holds-the-details" status="spec/done">The human
is slow but remembers everything and holds the picture; the AI is
fast but forgets everything and holds the details.</fact></p>
    <p><fact id="ROUTING-WITH-THE-GRAIN-BEATS-EITHER-PARTICIPANT-ALONE" status="spec/done">A system that
routes work with the grain of this table gets more than either
participant alone.</fact></p>
    <p><fact id="ROUTING-AGAINST-THE-GRAIN-EXHAUSTS-THE-HUMAN" status="spec/done">A system that routes against it gets an exhausted
human supervising a drifting machine.</fact></p>
  </section>
  <section id="boundary" title="The boundary moves; one thing does not">
    <p><fact id="the-line-shifts-with-capability-criticality-and-the-hour" status="spec/done">Where exactly the line sits between "human work" and "AI work"
shifts with model capability, with the project's criticality, even
with the hour of the day.</fact></p>
    <p><fact id="DO-NOT-FREEZE-THE-TABLE-INTO-DOGMA" status="impl/done">Do not freeze the table into dogma —
recalibrate it per project and per year.</fact></p>
    <p><fact id="ONE-ASSIGNMENT-NEVER-MOVES-THE-HUMAN-OWNS-COHERENCE" status="impl/done">One assignment never moves: **the human owns coherence** — the
agreement between iterations, between modules, between what the
system does and what it is for.</fact></p>
    <p><fact id="the-ai-cannot-own-coherence" status="spec/done">The AI cannot own it, because
coherence lives across sessions and the AI does not.</fact></p>
    <p><fact id="EVERYTHING-ELSE-EXISTS-TO-MAKE-THAT-JOB-CHEAP" status="impl/done">Everything else
in this collection — checkpoint files, addressable specs, conflict
rules — exists to make that one human job cheap enough to actually
perform.</fact></p>
  </section>
  <section id="cycle" title="What a productive cycle looks like">
    <p><fact id="the-cycle-lead" status="impl/done">A working session is a loop in which each side does what the other
cannot:</fact></p>
    <list ordered="true">
      <item><fact id="CYCLE-THE-HUMAN-DECIDES" status="impl/done">**The human decides.** Reads the current state (the checkpoint
   file), makes the one pending decision (a timeout value, an
   approach), updates the spec. Minutes, not hours.</fact></item>
      <item><fact id="CYCLE-THE-AI-GENERATES" status="impl/done">**The AI generates.** Receives a precise task with an address
   into the spec ("implement §5.3; the timeout changed yesterday —
   re-read it; do not touch the matcher"). Produces code, tests,
   and the updated shared state.</fact></item>
      <item><fact id="CYCLE-THE-HUMAN-VERIFIES" status="impl/done">**The human verifies.** Reads the diff, not the codebase. Checks
   that the tests cover what the spec says. Commits.</fact></item>
      <item><fact id="CYCLE-THE-STATE-CARRIES-OVER" status="impl/done">**The state carries over.** The checkpoint file and the spec —
   not anyone's memory — carry the result into the next session.</fact></item>
    </list>
    <p><fact id="the-unproductive-mirror-image" status="spec/done">The same task given as "finish the verification module" produces the
unproductive mirror image: the AI re-reads everything, guesses the
intent, "improves" adjacent code, and the human spends two hours
partitioning a diff that mixes the asked-for change with three
unasked ones.</fact></p>
    <p><fact id="THE-DIFFERENCE-IS-WHETHER-THE-HUMAN-DID-STEPS-ONE-AND-THREE" status="spec/done">Same model, same tooling — the difference is whether the human did
steps 1 and 3.</fact></p>
  </section>
  <section id="dated" title="The model is dated, deliberately">
    <p><fact id="the-model-fits-the-current-generation" status="spec/done">This is the model for the current generation of AI: strong enough to
be a real partner, not yet reliable enough to be autonomous.</fact></p>
    <p><fact id="the-model-will-change" status="spec/done">It will
change.</fact></p>
    <p><fact id="RE-DERIVE-THE-SPLIT-WHEN-CAPABILITIES-MOVE" status="impl/done">Re-derive the split when the underlying capabilities move —
that is what the revisit discipline (flow:decision-records) is for.</fact></p>
  </section>
  <section id="re-derive" title="Re-derive for your project">
    <p><fact id="COPY-THE-PROMPT-TASK-NOT-THE-PROMPT-IMPLEMENTATION" status="impl/done">Copy the prompt-task, not the prompt-implementation.</fact></p>
    <p><fact id="re-derive-lead" status="impl/done">Paste this to
your agent in a fresh session:</fact></p>
    <fence>Read this flow's documents (your project installed them — typically `vibedeps/flow-two-process-model/&lt;version&gt;/spec/flows/two-process-model/`, check `vibe.lock`) end to end. Then look at THIS
project: its criticality, team size, test maturity, and how capable
the models we use actually are. Produce a one-page project-specific
responsibility split: which decisions are human-only here, which
work is delegated to the AI by default, and where the boundary is
deliberately different from the generic table (say why). Propose it
as a draft for the project's boot file. Do not apply until I
approve.</fence>
  </section>
  <section id="summary" title="Summary">
    <list ordered="false">
      <item><fact id="SUM-BOTH-METAPHORS-FAIL-THE-SAME-WAY" status="spec/done">Boss/subordinate and human/tool both fail the same way: the human
  carries 100% of the thinking.</fact></item>
      <item><fact id="SUM-COPROCESSORS-WITH-COMPLEMENTARY-PROFILES" status="spec/done">Human and AI are coprocessors with complementary profiles; route
  work with the grain.</fact></item>
      <item><fact id="SUM-THE-BOUNDARY-MOVES-BUT-COHERENCE-DOES-NOT" status="impl/done">The boundary between zones moves; human ownership of coherence
  does not.</fact></item>
      <item><fact id="SUM-THE-PRODUCTIVE-CYCLE" status="impl/done">A productive cycle is decide → generate → verify → carry over, and
  the human's steps are the cheap ones — if the shared state is
  maintained.</fact></item>
      <item><fact id="SUM-EVERYTHING-ELSE-KEEPS-THAT-CYCLE-CHEAP" status="impl/done">Everything else in this collection exists to keep that cycle cheap.</fact></item>
    </list>
  </section>
</spec>
