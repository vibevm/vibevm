<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">`flow:decision-records` — record why, not just what</title>
  <status stage="doc" state="done" audience="user"/>
  <p><fact id="PACKAGE-INSTALLS-THE-DECISIONS-NOT-FACTS-DISCIPLINE" status="impl/done">A `flow` package that installs the **decisions, not facts**
discipline into a project.</fact></p>
  <p><fact id="FACT-IS-RECOVERABLE-REASON-IS-NOT" status="spec/done">A fact ("timeout is 600 s") is
recoverable from the code in a second; the *reason* cannot be
recovered at all.</fact></p>
  <p><fact id="FOUR-FIELD-RECORD-AT-THE-GOVERNING-ANCHOR" status="impl/done">So every choice a future reader could plausibly
re-open gets a four-field record — Decision / Why / Considered and
rejected / When to revisit — at the spec anchor that governs the
value.</fact></p>
  <p><fact id="tribal-knowledge-in-a-pure-human-team" status="spec/done">In a pure-human team, unrecorded reasoning limps along as tribal
knowledge — someone can still ask Vasya why the library was chosen.</fact></p>
  <p><fact id="AGENT-CANNOT-ASK-VASYA" status="spec/done">The agent cannot ask Vasya.</fact></p>
  <p><fact id="re-derivation-ends-in-re-litigation" status="spec/done">It re-derives from the code, the code
shows the value but not the constraint, and the decision gets
re-litigated: "600 s looks arbitrary, propose 300 s for
performance."</fact></p>
  <p><fact id="RECORD-IS-IMMUNITY-TRIGGER-KEEPS-IT-HONEST" status="spec/done">A recorded decision is immunity from re-litigation;
the revisit trigger keeps the immunity from hardening into dogma.</fact></p>
  <p><fact id="NO-ADR-DIRECTORY-NO-NUMBERED-LOG" status="impl/done">There is deliberately no `adr/` directory and no immutable numbered
log: the governing spec section IS the record, evolution is an edit
plus a changelog line, and history lives in git.</fact></p>
  <p><fact id="package-contents-lead" status="impl/done">This package ships three pieces of content plus a boot snippet:</fact></p>
  <list ordered="false">
    <item><fact id="CONTENT-THE-PROTOCOL" status="impl/done">`spec/flows/decision-records/DECISION-RECORDS-PROTOCOL.md` — full
  protocol: facts versus decisions, why the asymmetry is load-bearing
  in a human-AI team, the four-field record, placement at the
  governing anchor (contrasted with classic ADR), when to write a
  record, the rejected-alternatives and invariants section patterns,
  and a re-derive prompt for adapting the practice to any project.</fact></item>
    <item><fact id="CONTENT-THE-RECORD-TEMPLATE" status="impl/done">`spec/flows/decision-records/record-template.md` — the copy-ready
  template, two fully worked examples (a timeout constant, a library
  choice), and the anti-pattern table.</fact></item>
    <item><fact id="CONTENT-THE-REVISIT-TRIGGERS" status="impl/done">`spec/flows/decision-records/revisit-triggers.md` — trigger anatomy
  (metric + threshold + observation point), good/bad trigger table,
  the periodic sweep that actually fires triggers, and the reopening
  procedure.</fact></item>
    <item><fact id="CONTENT-THE-BOOT-SNIPPET" status="impl/done">`spec/boot/25-flow-decision-records.md` — boot snippet loaded at
  session start: the core rule, the in-session recording duty, and
  the never-do list.</fact></item>
  </list>
  <section id="install" title="Install">
    <fence lang="bash">vibe install flow:decision-records</fence>
  </section>
  <section id="uninstall" title="Uninstall">
    <fence lang="bash">vibe uninstall flow:decision-records</fence>
    <p><fact id="UNINSTALL-REMOVES-EVERY-FILE-THE-PACKAGE-WROTE" status="impl/done">Uninstalling removes every file the package wrote, including the boot
snippet.</fact></p>
    <p><fact id="USER-OWNED-FILES-ARE-NEVER-TOUCHED" status="impl/done">User-owned files are never touched.</fact></p>
  </section>
  <section id="composition" title="Composition">
    <list ordered="false">
      <item><fact id="COMPOSES-ADDRESSABLE-SPECS" status="impl/done">`flow:addressable-specs` — records live at anchors; the stable
  `spec://…#anchor` address is what makes a record citable from code
  comments, commits, and the session journal.</fact></item>
      <item><fact id="COMPOSES-SYNC-FROM-CODE" status="impl/done">`flow:sync-from-code` — its spec delta (value + reason + revisit
  trigger) is precisely a decision record born from a code-first
  change; this package generalises that trio to *all* decisions,
  whichever direction they arrive from.</fact></item>
      <item><fact id="COMPOSES-WAL" status="impl/done">`flow:wal` — WAL Constraints entries cite recorded decisions
  ("timeout is 600 s, not 300 → §verification.timeout") instead of
  restating the reasoning.</fact></item>
      <item><fact id="COMPOSES-ATOMIC-COMMITS" status="impl/done">`flow:git-atomic-commits` — commit bodies cite the record, never
  replace it: the spec carries the why, the commit points at the
  anchor.</fact></item>
    </list>
  </section>
  <section id="background" title="Philosophical background">
    <p><fact id="practice-extracted-from-the-book" status="spec/done">The practice is extracted from *AI-native development*, chapter 3
(*«Архитектура памяти»*, subsection *«Решения, а не факты»*) and the
memory-architecture conclusions of chapter 1.</fact></p>
    <p><fact id="BOOK-SHIPS-IN-RUSSIAN-INSIDE-REDBOOK" status="impl/done">The book ships in
Russian inside `flow:redbook` at `spec/book/ru/`.</fact></p>
    <p><fact id="AGENT-HAS-NO-MEMORY-BETWEEN-SESSIONS" status="spec/done">Short version: the
agent has no memory between sessions — a recorded decision is the
only kind of memory it can ever have.</fact></p>
  </section>
  <section id="license" title="License">
    <p><fact id="license-line" status="impl/done">UPL-1.0. See [`LICENSE.md`](LICENSE.md).</fact></p>
  </section>
</spec>
