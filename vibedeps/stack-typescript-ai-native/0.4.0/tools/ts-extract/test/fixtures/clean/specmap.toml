# The clean fixture's specmap policy — every export tagged or scoped;
# the TS orphan gate's green half.
namespace = "fixture"
scan_roots = ["src"]
spec_roots = ["spec"]
root_spec_docs = []
exempt = []
dispositioned = []
