<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">`flow:spec-genres` — what goes where, and who wins</title>
  <status stage="doc" state="done" audience="user"/>
  <p><fact id="PACKAGE-INSTALLS-A-GENRE-TAXONOMY" status="impl/done">A `flow` package that installs a **genre taxonomy** for a project's
documents.</fact></p>
  <p><fact id="pile-rots-three-ways" status="spec/done">One undifferentiated pile of markdown rots three ways:</fact></p>
  <list ordered="false">
    <item><fact id="ROT-CONTRACTS-BLOAT-WITH-NARRATIVE" status="spec/done">contracts bloat with narrative,</fact></item>
    <item><fact id="ROT-NARRATIVE-GETS-TREATED-AS-BINDING" status="spec/done">narrative gets treated as binding,</fact></item>
    <item><fact id="ROT-NOBODY-KNOWS-WHAT-WINS" status="spec/done">and nobody knows what wins.</fact></item>
  </list>
  <p><fact id="PACKAGE-SORTS-FIXES-AND-PINS-THE-TWO-LAWS" status="impl/done">This package sorts every document into a
genre, fixes what each genre may do, and pins the two laws that keep
the pile honest — contract wins over lore, and the two-way link that
lets a cold reader find the lore behind a contract.</fact></p>
  <p><fact id="genres-lead" status="impl/done">The genres:</fact></p>
  <list ordered="false">
    <item><fact id="GENRE-GROUP-BINDING" status="impl/done">binding (boot files, foundational decisions, module
  contracts),</fact></item>
    <item><fact id="GENRE-GROUP-NON-BINDING" status="impl/done">non-binding (design docs, research docs),</fact></item>
    <item><fact id="GENRE-GROUP-VOLATILE" status="impl/done">and volatile
  (campaign plans, the checkpoint).</fact></item>
  </list>
  <p><fact id="CONVENTION-NAMES-MODULE-CONTRACTS-PROP-FEAT" status="spec/done">The convention here names module
contracts PROP / FEAT — that is a naming choice this collection
carries from its origin, and you can rename it for your project; the
taxonomy is what matters, not the labels.</fact></p>
  <p><fact id="package-contents-lead" status="impl/done">This package ships three pieces of content plus a boot snippet:</fact></p>
  <list ordered="false">
    <item><fact id="CONTENT-THE-PROTOCOL" status="impl/done">`spec/flows/spec-genres/SPEC-GENRES-PROTOCOL.md` — the taxonomy:
  why genres exist, the full genre table (charter, mutability,
  reader, authority), the precedence law, the two-way linking law,
  and a re-derive prompt for adapting the map to any project.</fact></item>
    <item><fact id="CONTENT-THE-DESIGN-DOCS-GUIDE" status="impl/done">`spec/flows/spec-genres/design-docs.md` — the contract/lore split
  in practice: what spills out of a contract, what never leaves, the
  fork-by-fork record skeleton, the orthogonal-decomposition lesson,
  and how a design doc grows stale honestly.</fact></item>
    <item><fact id="CONTENT-THE-ROUTING-GUIDE" status="impl/done">`spec/flows/spec-genres/when-to-write-what.md` — the routing table
  (situation → genre), the misfiling-smells table, and one worked
  example of the linking law with both ends wired.</fact></item>
    <item><fact id="CONTENT-THE-BOOT-SNIPPET" status="impl/done">`spec/boot/17-flow-spec-genres.md` — boot snippet: the genre map,
  the name-the-genre-first rule, and the never-do list.</fact></item>
  </list>
  <section id="install" title="Install">
    <fence lang="bash">vibe install flow:spec-genres</fence>
  </section>
  <section id="uninstall" title="Uninstall">
    <fence lang="bash">vibe uninstall flow:spec-genres</fence>
    <p><fact id="UNINSTALL-REMOVES-EVERY-FILE-THE-PACKAGE-WROTE" status="impl/done">Uninstalling removes every file the package wrote, including the boot
snippet.</fact></p>
    <p><fact id="USER-OWNED-FILES-ARE-NEVER-TOUCHED" status="impl/done">User-owned files are never touched.</fact></p>
  </section>
  <section id="composition" title="Composition">
    <list ordered="false">
      <item><fact id="COMPOSES-ADDRESSABLE-SPECS" status="impl/done">`flow:addressable-specs` — the spec tree layout and stable anchors
  live there; genres classify *what* the tree holds, that package
  fixes *how* it is addressed.</fact></item>
      <item><fact id="COMPOSES-DECISION-RECORDS" status="impl/done">`flow:decision-records` — a decision record lives at the contract
  anchor; a design doc is where its long-form story goes. This
  package draws the line between the two.</fact></item>
      <item><fact id="COMPOSES-COMPARATIVE-RESEARCH" status="impl/done">`flow:comparative-research` — the research genre has its own
  package; this taxonomy points at it rather than duplicating it.</fact></item>
      <item><fact id="COMPOSES-CAMPAIGN-PLANS" status="impl/done">`flow:campaign-plans` — the campaign-plan genre has its own
  package; here it is one row in the genre table (execution, not
  truth).</fact></item>
    </list>
  </section>
  <section id="background" title="Philosophical background">
    <p><fact id="genre-model-crystallized-from-the-origin-project" status="spec/done">The genre model is crystallized from the origin project's own
design-doc genre law: the load-bearing rationale stays inside each
contract, the narrative rationale — the lore — moves into a linked
design doc, and the link is the mechanism that makes the lore survive
a cold start.</fact></p>
    <p><fact id="collection-spirit-is-the-book" status="spec/done">The collection's spirit is the book *AI-native
development* (in Russian inside `flow:redbook` at `spec/book/ru/`).</fact></p>
    <p><fact id="AGENT-READS-THE-TREE-COLD-EVERY-SESSION" status="spec/done">Short version: an agent reads the tree cold every session, so a
sentence's genre — and therefore its authority — must be legible
without asking anyone.</fact></p>
  </section>
  <section id="license" title="License">
    <p><fact id="license-line" status="impl/done">UPL-1.0. See [`LICENSE.md`](LICENSE.md).</fact></p>
  </section>
</spec>
