[package]
name = "two-process-model"
group = "org.vibevm.world"
kind = "flow"
version = "0.1.0"
authors = ["Oleg Chirukhin"]
license = "UPL-1.0"
description = "The two-process model: human and AI as coprocessors with complementary architectures, files as the only shared memory"
keywords = ["coprocessors", "cognitive-load", "collaboration", "foundations", "ipc"]

[compatibility]
min_vibe_version = "0.1.0"
requires_kinds = []

[boot_snippet]
source = "spec/boot/05-flow-two-process-model.md"
category = "flow"
