# tcg-oracle-bridge-rust — the LSP client seam over the consumer's own
# rust-analyzer (TCG-ORACLE-RUST v0.1 / TCG-PROTOCOL-RUST v0.1): the
# stem of the TS twin (tcg-oracle-bridge) + the language suffix the
# GUIDE §2 naming rule mandates. Replay-tested without rust-analyzer;
# the live end-to-end suite requires the component and fails with the
# recipe when absent (a stack obligation, never a skip).
[package]
name = "tcg-oracle-bridge-rust"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
specmark.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
