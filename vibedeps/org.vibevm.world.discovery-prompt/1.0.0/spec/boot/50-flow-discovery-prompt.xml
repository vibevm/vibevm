<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Flow: Discovery Prompt</title>
  <status stage="impl" state="done"/>
  <p><fact id="PROJECT-SHIPS-THE-DISCOVERY-PROMPT" status="impl/done">This project ships the **DISCOVERY collaborative-research prompt**, a
distributable user-level prompt that reshapes an LLM session into
structured co-inquiry.</fact></p>
  <p><fact id="ARTIFACT-LIVES-UNDER-THE-FLOW-DIRECTORY" status="impl/done">The artifact lives at
@spec://org.vibevm.world/discovery-prompt/flows/discovery-prompt/DISCOVERY-PROMPT#root.</fact></p>
  <p><fact id="ARTIFACT-IS-A-PAYLOAD-NOT-STANDING-INSTRUCTIONS" status="impl/done">It is a **payload for a fresh session**, not standing instructions —
do not load it into context outside an explicit deployment request.</fact></p>
  <section id="when" title="When to deploy">
    <p><fact id="REACH-FOR-THE-ARTIFACT-ON-A-RESEARCH-REQUEST" status="impl/done">When the user asks for a research, discovery, or exploration session
— comparing designs, mapping an unknown problem space, stress-testing
a hypothesis — reach for the artifact: fill its `&lt;VARIABLES&gt;` block
and paste the whole text as the first message of a fresh
conversation.</fact></p>
    <p><fact id="deployment-walkthrough-pointer" status="impl/done">Deployment steps and a piece-by-piece walkthrough:
@spec://org.vibevm.world/discovery-prompt/flows/discovery-prompt/usage#root.</fact></p>
  </section>
  <section id="what" title="What it does">
    <p><fact id="DEPLOYMENT-REPLACES-THE-HELPFUL-ASSISTANT-FRAME" status="impl/done">Once deployed, the prompt replaces the default helpful-assistant
frame with a response grammar (PrimaryHypothesis carrying an explicit
0.0–1.0 confidence, AlternativeInterpretations, MetaReflection), a
mandatory adversarial self-objection before any answer is finalized,
and hashtag intensity knobs (`#hot`, `#cold`, `#reboil`,
`#superthink`).</fact></p>
    <p><fact id="STANCE-UNCERTAINTY-IS-DATA-NOT-FAILURE" status="impl/done">Its stance in one line: uncertainty is data, not
failure.</fact></p>
  </section>
  <section id="never" title="Never">
    <list ordered="false">
      <item><fact id="NEVER-MIX-THE-GRAMMAR-INTO-A-CODING-SESSION" status="impl/done">Never mix the discovery grammar into a coding session uninvited —
  its overhead pays off only when the answer space is genuinely open.</fact></item>
      <item><fact id="NEVER-EDIT-THE-ARTIFACT-IN-PLACE" status="impl/done">Never edit the artifact in place; adapt a copy via the re-derive
  prompt in @spec://org.vibevm.world/discovery-prompt/flows/discovery-prompt/usage#re-derive.</fact></item>
      <item><fact id="NEVER-TREAT-CONFIDENCE-NUMBERS-AS-GUARANTEES" status="impl/done">Never treat the confidence numbers as guarantees — they are
  calibration aids, not measurements.</fact></item>
    </list>
  </section>
</spec>
