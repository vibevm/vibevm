<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">CARD: scaffold-e-fast-loop — Per-Cell Fast Verification Loop</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Discipline v0.2 · BETA**</status-line></p>
  <band-one-identity title="Band 1 — Identity &amp; Recognition">
    <p><CLASSIFICATION fact="true" status="impl/done">Classification: layer=E (verification) + H (weak-reader); mechanism=scaffold E.</CLASSIFICATION></p>
    <p><INTENT fact="true" status="impl/done">Intent: Guarantee every cell is independently compilable and testable in seconds, so an agent's edit→check→read-error→edit loop gets a first signal fast enough to steer — the substrate that makes every other scaffold's pass/fail usable.</INTENT></p>
    <p><ALSO-KNOWN-AS fact="true" status="spec/done">Also Known As: tight feedback loop; incremental check; per-package test; smoke loop; agent-computer interface.</ALSO-KNOWN-AS></p>
    <p><APPLICABILITY-RECOGNITION fact="true" status="impl/done">Applicability / Recognition: Apply when — a cell cannot be checked without building the whole repo; the only verification is a multi-minute CI run; an agent must wait minutes for a signal. *Detector seed:* `cargo test -p &lt;cell&gt;` fails to run in isolation, OR cell has no fast local check → recognition fires (verification locality beats capability, R3-007).</APPLICABILITY-RECOGNITION></p>
  </band-one-identity>
  <band-two-justification title="Band 2 — Justification &amp; Tradeoffs">
    <p><MOTIVATION fact="true" status="spec/done">Motivation: A weak agent in a bounded loop gets one signal per 30-min CI run — useless. With `rust-ai-native fast-loop --cell &lt;crate&gt;` + `cargo test -p &lt;cell&gt;` returning in seconds, the same agent iterates ten times in the budget that previously bought zero feedback. The interpreter-budget result confirms: more local runs help agents that can use feedback (Opus, Sonnet); the loop is the amplifier.</MOTIVATION></p>
    <p><STRUCTURE-AND-PARTICIPANTS fact="true" status="impl/done">Structure &amp; Participants: *Cell isolation* (independently buildable package) · *Fast checker* (conform tiers + per-cell tests) · *Structured error* (Class F) · *Budget guard* (first signal &lt; ~60s).</STRUCTURE-AND-PARTICIPANTS></p>
    <p><COLLABORATIONS fact="true" status="impl/done">Collaborations: Runs Classes C/D/G checks; consumes Class B's compiler checks; emits Class F diagnostics. The raid executor runs this per batch.</COLLABORATIONS></p>
    <p><GOALS-AND-NON-GOALS fact="true" status="impl/done">Goals / Non-Goals: *Goals:* sub-minute first signal per cell; make the loop the standard agent workflow. *Non-Goals:* NOT replacing full CI (still runs at merge); does NOT create capability — Haiku didn't improve with more runs (it amplifies, not creates).</GOALS-AND-NON-GOALS></p>
    <p><CONSEQUENCES fact="true" status="spec/done">Consequences: (+) every other scaffold becomes loop-usable; (+) iteration count rises within budget. (−) requires cells to be genuinely isolable (drives §1 cell design); (−) fast checks may miss what full CI catches — gate-mode checks backstop.</CONSEQUENCES></p>
    <p><ALTERNATIVES fact="true" status="spec/done">Alternatives: whole-repo CI only (too slow for an agent loop); manual testing (not mechanical). Neither is a substitute.</ALTERNATIVES></p>
    <p><RISKS-AND-ASSUMPTIONS fact="true" status="spec/done">Risks &amp; Assumptions: assumes cells are isolable; god-files break this (anti-pattern). *Sunset:* none — the loop is foundational.</RISKS-AND-ASSUMPTIONS></p>
    <p><EVIDENCE-AND-TRANSFER-STRENGTH fact="true" status="spec/done">Evidence &amp; Transfer-strength: R3-007 (verification locality, theory), R2C interpreter-budget result (more runs amplify capable agents, benchmark), BLD-010 (30–60s practical threshold). Class: benchmark + theory. Tag: **[E-strong]**.</EVIDENCE-AND-TRANSFER-STRENGTH></p>
  </band-two-justification>
  <band-three-operation title="Band 3 — Operation">
    <fence lang="card-ops">trigger: WHEN a cell lacks a sub-minute isolated check, OR verification requires a full-repo build THEN apply
mode: gate            # a structural precondition, verified at merge
routine:
  1. Make the cell an independently buildable unit (own package or test target).
  2. Provide `rust-ai-native conform check --scope &lt;crate&gt;` + `rust-ai-native fast-loop --cell &lt;crate&gt;` for it.
  3. Ensure `cargo test -p &lt;cell&gt;` runs in isolation, &lt; ~60s.
  4. Wire the cell's Class C/D/G checks into that command.
  5. Confirm first-signal latency is within budget; if not, reduce test case counts.
checker: harness assertion `cell-fast-loop-present` (cell builds+tests in isolation under budget)
raid_role: layer=infrastructure; order=after:cell-boundaries; batch=cell
budget: active_rules=1; first_signal=&lt;60s by definition</fence>
  </band-three-operation>
</spec>
