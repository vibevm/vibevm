<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">TCG-PROTOCOL-RUST v0.1 — the rust relay's wire contract</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Status: v0.1 — authored with AGENTIC-TCG-RUST-PLAN v0.1 (Phase 1),
implemented by its Phases 3–4.**</status-line></p>
  <p><companion-document fact="true" status="impl/done">The process model is
[`TCG-ORACLE-RUST-v0.1`](TCG-ORACLE-RUST-v0.1.md).</companion-document></p>
  <p><DOCUMENT-OWNS-THE-OUTER-HOP-GRAMMAR fact="true" status="impl/done">This document owns
the OUTER hop's message grammar — host (vibe-tcg / one-shot CLI) ⇄
`rust-ai-native-tcg serve` — and the mapping of each operation onto the INNER
hop, which is not a bespoke protocol at all but LSP 3.17 spoken to the
consumer's rust-analyzer.</DOCUMENT-OWNS-THE-OUTER-HOP-GRAMMAR></p>
  <parity title="1. Wire parity with the TS protocol">
    <p><kind-line-parity fact="true" status="impl/done">`req r1`</kind-line-parity></p>
    <p><outer-hop-is-wire-parity-lead fact="true" status="impl/done">The outer hop is WIRE-PARITY with the TS stack's TCG-PROTOCOL v0.1 §1:</outer-hop-is-wire-parity-lead></p>
    <list ordered="false">
      <item><PARITY-NDJSON-DUPLEX fact="true" status="impl/done">NDJSON duplex,</PARITY-NDJSON-DUPLEX></item>
      <item><PARITY-ONE-JSON-OBJECT-PER-LINE fact="true" status="impl/done">one JSON object per line,</PARITY-ONE-JSON-OBJECT-PER-LINE></item>
      <item><PARITY-REQUEST-AND-RESPONSE-ENVELOPES fact="true" status="impl/done">`{proto, id, op, params}`
  requests and `{proto, id, ok, result | error}` responses,</PARITY-REQUEST-AND-RESPONSE-ENVELOPES></item>
      <item><PARITY-CALLER-CHOSEN-CORRELATION-IDS fact="true" status="impl/done">caller-chosen
  correlation ids,</PARITY-CALLER-CHOSEN-CORRELATION-IDS></item>
      <item><PARITY-ORACLE-PROTOCOL-ON-EVERY-FRAME fact="true" status="impl/done">`ORACLE_PROTOCOL = 1` on every frame,</PARITY-ORACLE-PROTOCOL-ON-EVERY-FRAME></item>
      <item><PARITY-ADDITIVE-ONLY-EVOLUTION fact="true" status="impl/done">additive-only evolution within a proto (new optional params, new response fields —
  non-breaking; renames/semantic changes bump the constant).</PARITY-ADDITIVE-ONLY-EVOLUTION></item>
    </list>
    <p><ONE-PRODUCT-CLIENT-DRIVES-BOTH-RELAYS fact="true" status="spec/done">One
language-generic product client (`vibe-tcg`'s `OracleRegistry` link)
drives BOTH relays with the same frames — the parity is enforced at
the product level by the two live-chain tests, and pinned per-package
by outer-frame replay goldens. *Specified, not built — the client named
is gone; one of the two proofs survives. The `vibe-tcg` registry crate
was retired with the whole multiplexed-product topology and DELETED
(PROP-026 in vibe-mcp, `##TOPOLOGY-RETIRED` and `##TCG-CRATE-DELETED`);
`OracleRegistry` now appears in no `.rs` file anywhere, only in that
document's own retired-design section and in historical plans under
`legacy-spec/`. Its posture survives one layer over and per-language, not
generic: each `mcp`-kind package keeps a server-local shared session
(`TcgSession`). **The two live-chain tests do exist** —
`rust-ai-native-mcp/.../tests/live_chain.rs` and its TypeScript sibling —
but they run per package, not "at the product level", because there is
no product. The outer-frame replay goldens do not exist at all: no
golden or snapshot fixture is present in any ai-native package.*</ONE-PRODUCT-CLIENT-DRIVES-BOTH-RELAYS></p>
    <p><RESTATES-RATHER-THAN-INCLUDES-THE-TS-TEXT fact="true" status="impl/done">This document restates rather than
includes the TS text (cross-package spec inclusion is not a mechanism
we have); every DELIBERATE delta from the TS shape is listed in §3–§4,
and anything not listed there is parity by definition — drift outside
that list is a bug.</RESTATES-RATHER-THAN-INCLUDES-THE-TS-TEXT></p>
  </parity>
  <ops title="2. Operations">
    <p><kind-line-ops fact="true" status="impl/done">`req r2`</kind-line-ops></p>
    <p><POSITIONS-ARE-ONE-BASED-LINE-ZERO-BASED-CHARACTER fact="true" status="impl/done">Positions are `{line, character}`, 1-based line, 0-based character —
UNCHANGED from the TS shape for parity; the bridge converts to LSP's
0-based lines and, when utf-8 encoding was not granted, to UTF-16 code
units through the line's text.</POSITIONS-ARE-ONE-BASED-LINE-ZERO-BASED-CHARACTER></p>
    <p><PATHS-ARE-PROJECT-ROOT-RELATIVE fact="true" status="impl/done">Paths are project-root-relative with
forward slashes.</PATHS-ARE-PROJECT-ROOT-RELATIVE></p>
    <list ordered="false">
      <item><OP-INIT fact="true" status="impl/done">**`init`** `{}` → `{ra_version, position_encoding,
  pull_diagnostics, quiescent}` — resolves and spawns the analyzer
  (ORACLE-RUST §1), negotiates capabilities (§2), applies §3 config,
  waits for quiescence bounded by a deadline. Re-`init` on a live
  session restarts the child; overlays are cleared. The relay
  self-inits at `serve` start, so a host's first frame may be any op
  (client init frames remain re-init). The relay serves ONE project,
  so `init` takes no parameters — the root is `run_serve`'s own
  canonicalized process root.</OP-INIT></item>
      <item><OP-UPDATE fact="true" status="impl/done">**`update`** `{file, content | null}` → `{version}` — set/replace an
  overlay (`didOpen`/`didChange`, monotonic version) or clear it
  (`didClose`).</OP-UPDATE></item>
      <item><OP-VALIDATE fact="true" status="impl/done">**`validate`** `{file, content?}` → `{diagnostics: [{code, category,
  message, line, character}], facts: [/* serde `Fact` records */],
  markers: [], conform_findings: [{rule, message, line, baselined}],
  advice: [string], degraded}` — diagnostics from the pull channel
  (`textDocument/diagnostic`) for the ONE document; facts from the
  in-process `RustFrontend::extract` over the effective text;
  `conform_findings`/`advice` per §4. `markers` is always present and
  always empty in v0.1 — reserved for parity with the TS shape (Rust's
  marker analog, the specmark tag stream, lives in the specmap engine
  and is not re-extracted here; the field exists so consumers written
  against either relay see one shape).</OP-VALIDATE></item>
      <item><OP-SCOPE fact="true" status="impl/done">**`scope`** `{file, position?}` → `{symbols: [{name, kind,
  type_text}], cell, seam_file, branded: [{name, seam, heuristic}]}` —
  symbols via a completion sweep at the position (or a neutral
  top-level position); `cell` is the module path derived from the
  file's location under `src/` (there is no `[rust]` cells topology in
  conform.toml — derivation, never invented policy); `seam_file` is
  the enclosing `mod.rs`/`lib.rs`; `branded` carries the RUST brand
  analog — seam NEWTYPES (pub tuple structs with a single private
  field), syn-detected, every entry `heuristic: true`.</OP-SCOPE></item>
      <item><OP-COMPLETE fact="true" status="impl/done">**`complete`** `{file, position, content?, prefix?, max?}` →
  `{entries: [{name, kind, type_text, unsafe}]}` — LSP completion;
  `prefix` filters and `max` caps (default 50) BEFORE per-entry detail
  work; `unsafe: true` flags entries that would land a §6-banned form
  in domain code (v0.1: `unwrap`/`expect` on `Option`/`Result`
  receivers outside test files; name+receiver heuristic, honestly
  labelled in the brief).</OP-COMPLETE></item>
      <item><OP-TYPE fact="true" status="impl/done">**`type`** `{file, position, content?}` → `{display,
  documentation}` — LSP hover, markdown stripped to text.</OP-TYPE></item>
      <item><OP-SHUTDOWN fact="true" status="impl/done">**`shutdown`** `{}` → `{}` then exit 0 (the LSP shutdown/exit dance
  toward the child, kill-on-drop as backstop).</OP-SHUTDOWN></item>
    </list>
  </ops>
  <enrichment title="3. The enrichment fields (in-process, same engine as the gate)">
    <p><kind-line-enrichment fact="true" status="impl/done">`req r3`</kind-line-enrichment></p>
    <p><RELAY-READS-CONFORM-TOML-AND-BUILDS-THE-GATES-RULES fact="true" status="impl/done">`rust-ai-native-tcg serve` reads the project's `conform.toml` once per init
(`rust_ai_native_conform::load_config_or_default`, origin printed to
stderr) and assembles THE GATE'S OWN rule set through the
`rust_ai_native_conform::build_rules` pub seam.</RELAY-READS-CONFORM-TOML-AND-BUILDS-THE-GATES-RULES></p>
    <p><VALIDATE-PIPELINE-AND-ADVICE fact="true" status="impl/done">On `validate`: the effective
text → `RustFrontend::extract(file, crate, module, text)` →
`conform_core::check` → `conform_findings`, each flagged `baselined`
against the project's frozen ratchet baseline (the same file
`run_check` reads), plus `advice` strings in Class-F form citing GUIDE
REQs (`.unwrap()` in domain → §6 + the `#[spec(deviates)]` recipe;
missing doctest on a new pub seam → §3 Class G; ambient `std::env`
reads outside `env_roots` → the R-001 rule; a file over
`max_file_lines` → §2).</VALIDATE-PIPELINE-AND-ADVICE></p>
    <p><CRATE-MODULE-MAPPING-AND-FINDING-PARITY-TEST fact="true" status="impl/done">The crate/module strings for the single file
are computed by a relay-local mapping mirroring the engine's, and a
finding-parity test diffs the relay's finding set against
`rust-ai-native-conform check` on the same demo file — drift is a red test, not
a silent lie.</CRATE-MODULE-MAPPING-AND-FINDING-PARITY-TEST></p>
    <p><ENRICHMENT-FIELDS-EXIST-ONLY-ON-THIS-HOP fact="true" status="impl/done">A consumer that talks to the oracle-side directly gets
LSP; the enrichment fields exist only on this hop, and their absence
in a §2 response means "no policy layer", not an error.</ENRICHMENT-FIELDS-EXIST-ONLY-ON-THIS-HOP></p>
  </enrichment>
  <errors title="4. Error taxonomy">
    <p><kind-line-errors fact="true" status="impl/done">`req r4`</kind-line-errors></p>
    <p><five-error-kinds-lead fact="true" status="impl/done">Five kinds, each actionable, mirrored as typed variants in
`rust-ai-native-tcg-bridge`; the two environment rows are the DELIBERATE
renames against the TS table (§1):</five-error-kinds-lead></p>
    <table>
      <tr>
        <td>kind</td>
        <td>meaning</td>
        <td>recipe carried</td>
      </tr>
      <tr>
        <td><ROW-ERROR-RUST-ANALYZER-MISSING fact="true" status="impl/done">`rust-analyzer-missing`</ROW-ERROR-RUST-ANALYZER-MISSING></td>
        <td><fact status="impl/done">no analyzer resolvable (ORACLE-RUST §1)</fact></td>
        <td><fact status="impl/done">`rustup component add rust-analyzer`</fact></td>
      </tr>
      <tr>
        <td><ROW-ERROR-WORKSPACE-UNLOADABLE fact="true" status="impl/done">`workspace-unloadable`</ROW-ERROR-WORKSPACE-UNLOADABLE></td>
        <td><fact status="impl/done">the project failed to load (no Cargo.toml, cargo metadata failed)</fact></td>
        <td><fact status="impl/done">check `cargo metadata` standalone</fact></td>
      </tr>
      <tr>
        <td><ROW-ERROR-ORACLE-CRASHED fact="true" status="impl/done">`oracle-crashed`</ROW-ERROR-ORACLE-CRASHED></td>
        <td><fact status="impl/done">child died / stream closed mid-session</fact></td>
        <td><fact status="impl/done">respawn guidance; the host registry retries once</fact></td>
      </tr>
      <tr>
        <td><ROW-ERROR-PROTOCOL fact="true" status="impl/done">`protocol`</ROW-ERROR-PROTOCOL></td>
        <td><fact status="impl/done">unparseable frame, proto mismatch, unknown op</fact></td>
        <td><fact status="impl/done">version note; unknown-op errors list the known ops</fact></td>
      </tr>
      <tr>
        <td><ROW-ERROR-TIMEOUT fact="true" status="impl/done">`timeout`</ROW-ERROR-TIMEOUT></td>
        <td><fact status="impl/done">no response within the caller's budget</fact></td>
        <td><fact status="impl/done">the op and the budget, for tuning</fact></td>
      </tr>
    </table>
    <p><PRODUCT-LINK-LAYER-SPECIAL-CASES-ONLY-ORACLE-CRASHED fact="true" status="spec/done">The product's link layer special-cases only `oracle-crashed`
(session-grain) and passes every other kind through as a
recipe-carrying detail — verified against the shipped `vibe-tcg`
before this protocol was authored, so the renames ride WITHOUT a
product edit. *Specified, not built — the behaviour outlived the layer
this sentence names. The special-casing is real and still exactly this
narrow: `crates/rust-ai-native-tcg/src/serve.rs:208` singles out
`TcgBridgeError::OracleCrashed` and passes every other kind through, and
the per-family MCP server respawns once on it. But there is no "product
link layer" to hold it — `vibe-tcg` was deleted (PROP-026 in vibe-mcp,
`##TCG-CRATE-DELETED`), so nothing was verified against a shipped
product, and "the renames ride WITHOUT a product edit" is true only
because no product remains to edit.*</PRODUCT-LINK-LAYER-SPECIAL-CASES-ONLY-ORACLE-CRASHED></p>
  </errors>
  <compat title="5. Compatibility rules">
    <p><kind-line-compat fact="true" status="impl/done">`req r5`</kind-line-compat></p>
    <p><compatibility-is-identical-to-the-ts-protocol-lead fact="true" status="impl/done">Identical to the TS protocol's §5, restated:</compatibility-is-identical-to-the-ts-protocol-lead></p>
    <list ordered="false">
      <item><COMPAT-ADDITIVE-EVOLUTION fact="true" status="impl/done">additive evolution within
  a `proto` (optional params, new response fields, new advice/finding
  entries — consumers ignore the unknown);</COMPAT-ADDITIVE-EVOLUTION></item>
      <item><COMPAT-BREAKING-CHANGES-BUMP-THE-CONSTANT fact="true" status="impl/done">renames, type changes, and
  semantic changes bump `ORACLE_PROTOCOL` and the bridge treats the
  mismatch as its own error class.</COMPAT-BREAKING-CHANGES-BUMP-THE-CONSTANT></item>
    </list>
    <p><REPLAY-GOLDENS-AND-RECORDED-TRANSCRIPTS-PIN-BOTH-HOPS fact="true" status="spec/done">Recorded LSP
transcripts pin the INNER hop in this package's tests (r-a-free in the
unit suite: `crates/rust-ai-native-tcg-bridge/src/client/tests.rs`,
`src/oracle/tests.rs`). The OUTER shape is not yet pinned — no test
drives `run_serve` over recorded frames; `crates/rust-ai-native-tcg`
carries `tests/finding_parity.rs` and unit tests only.</REPLAY-GOLDENS-AND-RECORDED-TRANSCRIPTS-PIN-BOTH-HOPS></p>
    <p><MARKERS-RESERVATION-MAY-BE-FILLED-IN-A-FUTURE-MINOR fact="true" status="spec/done">The `markers: []`
reservation (§2) may be filled in a future minor by the specmark tag
stream — that is the additive path, planned for, not promised.</MARKERS-RESERVATION-MAY-BE-FILLED-IN-A-FUTURE-MINOR></p>
  </compat>
</spec>
