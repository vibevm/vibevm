<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">AI-Native TypeScript (stack:org.vibevm.ai-native/typescript-ai-native-lang)</title>
  <status stage="doc" state="done" audience="user"/>
  <p><TYPESCRIPT-PROJECTION-SHIPS-A-RUNNABLE-TOOLCHAIN fact="true" status="impl/done">The TypeScript projection of the AI-Native Code Discipline — and the
**runnable toolchain** that enforces it (PROP-024 code-bearing packages):
installing this stack yields working checkers and procedures, not
descriptions of them.</TYPESCRIPT-PROJECTION-SHIPS-A-RUNNABLE-TOOLCHAIN></p>
  <p><NEUTRAL-METHOD-COMES-FROM-THE-CORE-DEPENDENCY fact="true" status="impl/done">The language-neutral method (manifesto, playbooks, mechanism specs)
comes from its dependency `flow:org.vibevm.ai-native/core-ai-native`.</NEUTRAL-METHOD-COMES-FROM-THE-CORE-DEPENDENCY></p>
  <what-ships title="What ships">
    <list ordered="false">
      <item><SHIPS-FOUR-BINARIES fact="true" status="impl/done">**Four binaries** (this package's own Cargo workspace, `crates/`,
  declared as `[[binary]]` in `vibe.toml` for PROP-025 lockfile
  dispatch):</SHIPS-FOUR-BINARIES></item>
      <item><SHIPS-TYPESCRIPT-AI-NATIVE-UMBRELLA fact="true" status="impl/done">`typescript-ai-native` — the umbrella tool: `init` (bootstrap
    policies + registries), `floor` (the portable verification floor —
    prettier → tsc → tests → eslint → conform → specmap → test-gate, one
    exit code), `conform`, `specmap`, `trace`, `test-gate`, `tripwire`,
    `health`, `fast-loop`, `codemod`.</SHIPS-TYPESCRIPT-AI-NATIVE-UMBRELLA></item>
      <item><SHIPS-TYPESCRIPT-AI-NATIVE-CONFORM fact="true" status="impl/done">`typescript-ai-native-conform` — the conformance gate alone (ENGINE-CONFORM).</SHIPS-TYPESCRIPT-AI-NATIVE-CONFORM></item>
      <item><SHIPS-TYPESCRIPT-AI-NATIVE-SPECMAP fact="true" status="impl/done">`typescript-ai-native-specmap` — the traceability engine alone (PROP-014).</SHIPS-TYPESCRIPT-AI-NATIVE-SPECMAP></item>
      <item><SHIPS-TYPESCRIPT-AI-NATIVE-TCG fact="true" status="impl/done">`typescript-ai-native-tcg` — the agentic type oracle (TCG-ORACLE-v0.1 /
    TCG-PROTOCOL-v0.1): a persistent enriching `serve` relay for MCP
    hosts plus one-shot `validate` / `scope` / `complete` / `type` /
    `bench`, answered by the CONSUMER's own `typescript` install over
    in-memory overlays with the gate's own conform rules merged in.
    **Prerequisite:** node ≥ 22.6 and the project's own `typescript`
    devDependency — the same install the `tsc` floor step needs, so the
    oracle adds no new dependency. The floor stays the truth; the oracle
    exists so the floor stays green on the first try.</SHIPS-TYPESCRIPT-AI-NATIVE-TCG></item>
      <item><SHIPS-GUIDE-AND-CARDS fact="true" status="impl/done">**The TypeScript guide and cards**
  (`spec/typescript/GUIDE-AI-NATIVE-TYPESCRIPT.md`, `spec/cards/` — the
  nine scaffolds A–I in their TypeScript shape, Band-3 ops blocks for
  weak readers).</SHIPS-GUIDE-AND-CARDS></item>
      <item><SHIPS-TWO-AGENT-SKILLS fact="true" status="impl/done">**Two agent skills** (`vibe skill install` projects them):
  `/typescript-ai-native-terraform` (brownfield adoption per BROWNFIELD-PROTOCOL)
  and `/typescript-ai-native-sweep` (the recurring sweep per the Sweep
  Playbook).</SHIPS-TWO-AGENT-SKILLS></item>
      <item><SHIPS-TWO-NODE-SIDE-TOOLS fact="true" status="impl/done">**Two node-side tools** (`tools/`, run directly under node ≥ 22.6
  type-stripping): `ts-extract`, the Compiler-API fact extractor the
  conform frontend and the specmap scanner drive; and `ts-oracle`, the
  long-lived language-service oracle behind `typescript-ai-native-tcg`.
  Both resolve the CONSUMER project's `typescript` at runtime.</SHIPS-TWO-NODE-SIDE-TOOLS></item>
      <item><SHIPS-MECHANISM-SPECS fact="true" status="impl/done">**The TypeScript mechanism specs and tool briefs** —
  `spec/typescript/mechanisms/TCG-ORACLE-v0.1.md` and
  `TCG-PROTOCOL-v0.1.md`, plus `spec/typescript/tools/`.</SHIPS-MECHANISM-SPECS></item>
      <item><NEUTRAL-ENGINES-RIDE-ALONG-VENDORED fact="true" status="impl/done">**The neutral engines ride along as vendored copies**
  (`crates/vendor/core-ai-native-{conform,specmap,specmark,specmark-grammar}`),
  so the slot is its own Cargo workspace and builds standalone.</NEUTRAL-ENGINES-RIDE-ALONG-VENDORED></item>
    </list>
  </what-ships>
  <running-the-tools title="Running the tools">
    <p><running-forms-lead fact="true" status="impl/done">Three supported forms, from your project root (where `vibedeps/` is):</running-forms-lead></p>
    <fence lang="sh"># (a) vibe-native (PROP-025) — build once in the slot, dispatch through
#     the project's lockfile:
vibe bin build            # or straight to:
vibe bin exec typescript-ai-native -- floor

# (b) install once onto PATH — then just `typescript-ai-native …`
cargo install --path vibedeps/&lt;stack-slot&gt;/crates/typescript-ai-native-cli

# (c) zero-install, run in place
cargo run --manifest-path vibedeps/&lt;stack-slot&gt;/Cargo.toml \
    -p typescript-ai-native-cli --bin typescript-ai-native -- floor</fence>
    <p><STACK-SLOT-IS-THE-MATERIALISED-DIRECTORY fact="true" status="impl/done">`&lt;stack-slot&gt;` is this package's materialised directory (e.g.
`stack-typescript-ai-native-lang/1.0.0` — check your `vibe.lock`).</STACK-SLOT-IS-THE-MATERIALISED-DIRECTORY></p>
    <p><SLOT-BUILD-DROPS-A-TARGET-DIRECTORY fact="true" status="impl/done">Building in the
slot drops a `target/` there; add `vibedeps/**/target/` to your
`.gitignore` (build output is already excluded from the package's content
hash, PROP-024 §2.2). A repo that also carries Rust keeps
`[workspace] exclude = ["vibedeps"]`.</SLOT-BUILD-DROPS-A-TARGET-DIRECTORY></p>
  </running-the-tools>
  <the-lifecycle title="The lifecycle">
    <fence lang="sh">vibe install                          # materialise this stack into vibedeps/
npm install -D typescript prettier eslint typescript-eslint
typescript-ai-native init             # policies + registries + external spec resolution
# … write spec units, tag exports (GUIDE §9), adopt cell by cell …
typescript-ai-native floor            # the gate panel, one exit code
/typescript-ai-native-sweep           # the recurring sweep (agent skill)
/typescript-ai-native-terraform       # brownfield adoption (agent skill)</fence>
    <p><wiring-and-sweep-pointers fact="true" status="impl/done">The wiring recipe — install, binaries, project toolchain, bootstrap,
the generation-time oracle — is GUIDE §15; the sweep idioms are GUIDE
§16.</wiring-and-sweep-pointers></p>
    <p><POLICIES-STAY-WITH-THE-CONSUMER-PROJECT fact="true" status="impl/done">The policies (`conform.toml`, `specmap.toml`) stay with YOUR project:
this package ships engines, never policy (PROP-024 §2.2).</POLICIES-STAY-WITH-THE-CONSUMER-PROJECT></p>
  </the-lifecycle>
</spec>
