<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">CARD: scaffold-f-structured-diagnostics — Structured, Requirement-Citing Diagnostics (TypeScript)</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Discipline v0.2 · BETA · T2 · TypeScript**</status-line></p>
  <band-one-identity title="Band 1 — Identity &amp; Recognition">
    <p><CLASSIFICATION fact="true" status="impl/done">Classification: layer=E (verification) + C (meta); mechanism=scaffold F.</CLASSIFICATION></p>
    <p><INTENT fact="true" status="impl/done">Intent: Engineer linter/checker output as agent input — stable, structured, citing the violated requirement and the fix surface — because error text is the highest-leverage prompt in the loop.</INTENT></p>
    <p><ALSO-KNOWN-AS fact="true" status="spec/done">Also Known As: actionable diagnostics; SARIF output; fix-it hints; structured errors; machine-readable lint; ESLint `messageId`.</ALSO-KNOWN-AS></p>
    <p><APPLICABILITY-RECOGNITION fact="true" status="impl/done">Applicability / Recognition: Apply when — a custom `@typescript-eslint` rule (or a thrown domain error) emits free text; an error states what failed but not which REQ or where to fix; tool output is unstable across runs. *Detector seed:* a custom eslint/check message or a domain error without a `spec://` REQ URI and a fix-surface hint → recognition fires (tool output is the agent's percept, R3-011). Note: `tsc`'s own diagnostics (TS2322 etc.) are already coded — wrap them with REQ context, do not replace them.</APPLICABILITY-RECOGNITION></p>
  </band-one-identity>
  <band-two-justification title="Band 2 — Justification &amp; Tradeoffs">
    <p><MOTIVATION fact="true" status="spec/done">Motivation: A weak agent sees `Type 'string' is not assignable to type 'never'` from a `satisfies never` exhaustiveness check — true but opaque. With the custom rule emitting `violates REQ spec://errors/r6: error union not exhaustively handled; fix surface: add a case for variant 'Timeout' before assertNever`, the agent acts directly. The strong author's "what to do when this fails" is materialized in the message.</MOTIVATION></p>
    <p><STRUCTURE-AND-PARTICIPANTS fact="true" status="impl/done">Structure &amp; Participants: *Diagnostic* (an eslint report with a stable `messageId`) · *REQ citation* (`spec://` URI) · *Fix-surface hint* (where/what) · *Stable format* (SARIF / fixed grammar).</STRUCTURE-AND-PARTICIPANTS></p>
    <p><COLLABORATIONS fact="true" status="impl/done">Collaborations: Carries failures from Classes C/D/E and the §8 bans; feeds the agent loop's next prompt; in raids, structured diagnostics let the orchestrator triage misfires.</COLLABORATIONS></p>
    <p><GOALS-AND-NON-GOALS fact="true" status="impl/done">Goals / Non-Goals: *Goals:* every custom check is agent-actionable. *Non-Goals:* NOT rewriting `tsc`'s own diagnostics (already good — wrap them); does NOT replace the contract that defines correctness.</GOALS-AND-NON-GOALS></p>
    <p><CONSEQUENCES fact="true" status="spec/done">Consequences: (+) iterations-to-green drop, more for weaker models; (+) diagnostics double as a navigable requirement map. (−) message authoring cost; (−) verbosity vs token budget — keep a compact grammar.</CONSEQUENCES></p>
    <p><ALTERNATIVES fact="true" status="spec/done">Alternatives: free-text errors (wasted conditioning); silent failure (worst). Neither acceptable for an agent loop.</ALTERNATIVES></p>
    <p><RISKS-AND-ASSUMPTIONS fact="true" status="spec/done">Risks &amp; Assumptions: assumes a stable REQ namespace exists (it does — specmap, guide §9). *Sunset:* none material.</RISKS-AND-ASSUMPTIONS></p>
    <p><EVIDENCE-AND-TRANSFER-STRENGTH fact="true" status="spec/done">Evidence &amp; Transfer-strength: R3-011 (tool output is highest-leverage prompt, theory), R2C-004 (agent conditions on tool text, benchmark). Class: benchmark + theory. Tag: **[E-mid]**.</EVIDENCE-AND-TRANSFER-STRENGTH></p>
  </band-two-justification>
  <band-three-operation title="Band 3 — Operation">
    <fence lang="card-ops">trigger: WHEN a custom eslint/check message or a domain error lacks a spec:// REQ URI + fix-surface hint THEN apply
mode: inline
routine:
  1. Add the violated REQ's spec:// URI to the message.
  2. Add a one-line fix surface: where to change and what.
  3. Emit in a stable structured form (eslint `messageId` + SARIF; fixed grammar for domain errors).
  4. Keep it compact (one line of why + one of where).
checker: `diagnostic-cites-req` in the flat-config plugin `@org.vibevm/eslint-plugin-ai-native` (rule key `ai-native/diagnostic-cites-req`; source `typescript-ai-native-lang/v1.0.0/tools/eslint-plugin-ai-native/`) — custom messages must match the grammar `violates REQ &lt;uri&gt;: &lt;why&gt;; fix surface: &lt;where&gt;`; wired via the project's own eslint config (demo: `research/ts-demo/eslint.config.js` at `error`)
raid_role: layer=tooling; order=after:none; batch=package
budget: active_rules=1; first_signal=lint pass (&lt;60s)</fence>
  </band-three-operation>
</spec>
