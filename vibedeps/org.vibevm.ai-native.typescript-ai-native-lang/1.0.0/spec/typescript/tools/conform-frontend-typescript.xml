<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Tool Spec (high-level): `typescript-ai-native-conform-frontend` — the TypeScript frontend for the language-neutral conform engine</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">*Status: **SHIPPED** (0.3.0, the deferrals-closeout campaign) — this package's `crates/typescript-ai-native-conform-frontend` (`id = "ts-tsc"`) + `crates/typescript-ai-native-conform` (binary **`typescript-ai-native-conform`**), fed by the Compiler-API extractor at `tools/ts-extract/` through the `typescript-ai-native-extract-bridge` NDJSON protocol.*</status-line></p>
  <p><TYPESCRIPT-COUNTERPART-OF-THE-RUST-FRONTEND fact="true" status="impl/done">*The TypeScript counterpart to `rust-ai-native-conform-frontend`: it gives `.ts` code the SAME structural discipline gate that `.rs` code gets from `rust-ai-native-conform check` — by feeding TypeScript facts into the language-neutral conform engine, not by re-implementing the rules in ESLint.*</TYPESCRIPT-COUNTERPART-OF-THE-RUST-FRONTEND></p>
  <p><SECTION-THREE-QUESTION-WAS-ANSWERED fact="true" status="impl/done">*The §3 question below was answered by the engine consolidation: option (a) happened — `conform-core` now lives in `flow:org.vibevm.ai-native/core-ai-native`, both stacks vendor it (`cargo xtask sync-engines`).*</SECTION-THREE-QUESTION-WAS-ANSWERED></p>
  <p><brief-remains-as-design-rationale fact="true" status="impl/done">*This brief remains as the design rationale.*</brief-remains-as-design-rationale></p>
  <quote><WHY-A-TS-FRONTEND-IS-NOW-POSSIBLE fact="true" status="impl/done">**Why this is now possible (PROP-024).** Until the code-bearing-packages refactor, the conform engine was hardcoded inside the vibevm workspace — a Rust-only, vibevm-only tool. PROP-024 made a package a project that ships runnable code, and relocated `conform-core` + `rust-ai-native-conform-frontend` into `stack:org.vibevm.ai-native/rust-ai-native-lang`. `conform-core` is **language-neutral** by construction: it owns the `Fact` model, the rules-as-queries, the SARIF renderer, and the ratchet baseline, and it consumes facts through a `Frontend` trait that carries nothing Rust-specific (`rust-ai-native-conform-frontend` is just one implementation, over `syn`). So adding TypeScript to the discipline's *structural* gate is a new frontend, not a second engine.</WHY-A-TS-FRONTEND-IS-NOW-POSSIBLE></quote>
  <division title="1. The division of labour with the native TS tooling">
    <p><TS-TOOLING-CARRIES-THE-TYPE-GENERATION-HALF fact="true" status="spec/done">The TypeScript cards already specify a rich, mature **type / generation** checker set — `@typescript-eslint`, `tsc --noEmit`, `tsd` / `expectTypeOf`, Twoslash, `fast-check` (GUIDE §14; the cards' Band-3 ops).</TS-TOOLING-CARRIES-THE-TYPE-GENERATION-HALF></p>
    <p><THAT-HALF-IS-WELL-TYPED-AND-EXAMPLES-TYPE-CHECK fact="true" status="spec/done">Those answer *"is this well-typed, and do the examples type-check?"* — the half TypeScript's compiler does natively and does superbly.</THAT-HALF-IS-WELL-TYPED-AND-EXAMPLES-TYPE-CHECK></p>
    <p><frontend-answers-the-structural-half-lead fact="true" status="impl/done">`typescript-ai-native-conform-frontend` answers the **other** half — the *structural / architectural* rules no type checker expresses, the ones `conform check` already enforces for Rust:</frontend-answers-the-structural-half-lead></p>
    <list ordered="false">
      <item><RULE-FILE-LENGTH-BUDGET fact="true" status="impl/done">the file-length budget (position is a resource), carried by the root `conform.toml` key `max_file_lines`, with two sibling root keys feeding `invariant-comment-position` (R3-003) — `invariant_comment_markers` and `invariant_comment_min_file_lines`; a fourth, `sarif_reports`, feeds the SARIF-ingest path described below. All four are root keys, language-neutral, NOT under `[typescript]`, and what they mean and default to is described once, in `ENGINE-CONFORM §6` (the policy file) — this surface names which ones the TS rules read, not their values;</RULE-FILE-LENGTH-BUDGET></item>
      <item><RULE-CELL-ISOLATION fact="true" status="impl/done">cell isolation (a cell imports seams + core only, never sibling cells);</RULE-CELL-ISOLATION></item>
      <item><RULE-BANS-AS-FACTS fact="true" status="impl/done">the bans-as-facts (`any` / unchecked `as` / `!` / `@ts-ignore` in domain code — the §8 set) surfaced as conform findings in the Class-F `violates REQ …; fix surface: …` grammar, navigable back to the governing card;</RULE-BANS-AS-FACTS></item>
      <item><RULE-DEVIATION-ESCAPE-HATCH fact="true" status="impl/done">the deviation escape hatch (`@ts-expect-error -- reason`, a recorded `deviates`), honoured the way `#[spec(deviates)]` is for Rust.</RULE-DEVIATION-ESCAPE-HATCH></item>
    </list>
    <p><THE-SARIF-CITATION-PATH-IS-FED-BY-FOREIGN-LINTERS-NOT-BY-TS-EXTRACT fact="true" status="impl/done">One more rule is mounted in this gate and is deliberately absent from the list above: `lint-suppression-needs-reason`, whose facts come from SARIF **ingest** — eslint and its plugins — rather than from this frontend. It is fed by the fourth root key, `sarif_reports`, and it is the T-sem citation path: a Discipline rule citing a foreign linter's diagnosis as its own evidence. Listing it beside the extractor's own rules would claim a fact source this frontend does not have.</THE-SARIF-CITATION-PATH-IS-FED-BY-FOREIGN-LINTERS-NOT-BY-TS-EXTRACT></p>
    <p><eslint-could-express-some-of-these fact="true" status="spec/done">ESLint *could* express some of these as custom rules.</eslint-could-express-some-of-these></p>
    <p><ONE-ENGINE-ONE-GRAMMAR-ONE-BASELINE fact="true" status="impl/done">Routing them through conform instead keeps **one rule engine, one finding grammar, one ratchet baseline** across both languages, with the rules defined once in `conform-core` and fed by either frontend — so a rule cannot drift between the Rust and TypeScript projections. The TypeScript gate carries the same coverage invariant as the sibling stacks: every cell the `[typescript]` roots enumerate is either `[typescript] gated` or carries a `[[typescript.exempt]]` entry with its reason (`{unit, reason}` — the cell is TS's gate unit), enforced by the engine in `check` and `freeze` — an unclassified cell, a duplicate, a both-listed cell, a reasonless or a ghost exemption all fail loudly with cell-naming messages. A present config whose TS roots resolve to zero cells warns instead of passing silently, and a gated cell the scan attributed no file to is flagged vacuous.</ONE-ENGINE-ONE-GRAMMAR-ONE-BASELINE></p>
  </division>
  <what-it-is title="2. What it is">
    <p><TS-FRONTEND-IS-A-FACT-PRODUCER fact="true" status="impl/done">A fact producer: parse a `.ts` / `.tsx` file and emit the language-neutral `conform_core::Fact` stream the rules consume.</TS-FRONTEND-IS-A-FACT-PRODUCER></p>
    <list ordered="false">
      <item><PARSER-IS-THE-COMPILER-API fact="true" status="impl/done">**Parser:** the TypeScript Compiler API (or `ts-morph` over it) — the most analyzable AST of any mainstream language (GUIDE §0, Advantage 2). Items with their decorators / attribute text, imports, construction sites, the `unsafe`-set tokens, whole-file metrics — the same fact shapes `rust-ai-native-conform-frontend` extracts from `syn`.</PARSER-IS-THE-COMPILER-API></item>
      <item><IMPLEMENTS-THE-FRONTEND-TRAIT fact="true" status="impl/done">**Implements `Frontend`:** an `id()` such as `"ts-tsc"`, a `version()` that bumps when the fact schema grows (retiring cache slots wholesale, exactly as the Rust frontend does), and `extract(file, crate, module, text) -&gt; Vec&lt;Fact&gt;`.</IMPLEMENTS-THE-FRONTEND-TRAIT></item>
      <item><UNPARSEABLE-FILE-YIELDS-ZERO-FACTS fact="true" status="impl/done">**Unparseable file → zero facts, never an error** (the B5 rule), so one malformed file does not blind the gate to the rest of the tree.</UNPARSEABLE-FILE-YIELDS-ZERO-FACTS></item>
    </list>
    <p><ENGINE-PARTS-ARE-REUSED-UNCHANGED fact="true" status="impl/done">The rules, the content-addressed store, the SARIF, the baseline — all reused unchanged from `conform-core`.</ENGINE-PARTS-ARE-REUSED-UNCHANGED></p>
    <p><only-the-fact-source-is-new fact="true" status="impl/done">Only the fact source is new.</only-the-fact-source-is-new></p>
  </what-it-is>
  <open-question title="3. The architectural question this brief leaves open">
    <p><CONFORM-CORE-HOMES-IN-THE-RUST-STACK fact="true" status="spec/done">`conform-core` is language-neutral but currently **homes in `stack:org.vibevm.ai-native/rust-ai-native-lang`** — it moved there with `rust-ai-native-conform-frontend`, its first and only consumer at the time (PROP-024 phase 4: the clean move set was conform-core + rust-ai-native-conform-frontend + rust-ai-native-env-audit).</CONFORM-CORE-HOMES-IN-THE-RUST-STACK></p>
    <p><A-TS-FRONTEND-NEEDS-CONFORM-CORE-REACHABLE fact="true" status="spec/done">A TypeScript frontend that reuses it needs `conform-core` reachable from a *different* package.</A-TS-FRONTEND-NEEDS-CONFORM-CORE-REACHABLE></p>
    <p><two-clean-resolutions-lead fact="true" status="spec/done">Two clean resolutions, deferred to when this tool is built:</two-clean-resolutions-lead></p>
    <list ordered="false">
      <item><RESOLUTION-PROMOTE-CONFORM-CORE fact="true" status="spec/done">**(a) Promote `conform-core` to `flow:org.vibevm.ai-native/core-ai-native`** (the language-neutral package): `rust-ai-native-conform-frontend` stays in the Rust stack, `typescript-ai-native-conform-frontend` ships in this one, both depend on the neutral core. This is the principled end-state — the engine is language-neutral, so it belongs in the language-neutral package.</RESOLUTION-PROMOTE-CONFORM-CORE></item>
      <item><RESOLUTION-CROSS-PACKAGE-DEPENDENCY fact="true" status="spec/done">**(b) Cross-package dependency:** this package depends on the Rust stack's `conform-core`. Simpler, but couples the TypeScript stack to the Rust one for no reason beyond where the code happens to sit today.</RESOLUTION-CROSS-PACKAGE-DEPENDENCY></item>
    </list>
    <p><option-a-is-the-better-shape fact="true" status="spec/done">(a) is the better shape.</option-a-is-the-better-shape></p>
    <p><a-follow-up-move-not-a-blocker fact="true" status="spec/done">It is a follow-up move, not a blocker for the cards — and it is exactly the kind of relocation PROP-024 made cheap.</a-follow-up-move-not-a-blocker></p>
  </open-question>
  <code-root title="4. The code-root">
    <p><FRONTEND-SHIPS-IN-THIS-PACKAGE-AS-ITS-CODE-ROOT fact="true" status="impl/done">When implemented, `typescript-ai-native-conform-frontend` ships **in this package** — `stack:org.vibevm.ai-native/typescript-ai-native-lang` — as the package's own code-root: the TypeScript mirror of how `rust-ai-native-lang` now carries `crates/rust-ai-native-conform-frontend`.</FRONTEND-SHIPS-IN-THIS-PACKAGE-AS-ITS-CODE-ROOT></p>
    <p><INSTALLING-THE-STACK-YIELDS-A-WORKING-GATE fact="true" status="impl/done">Installing the stack would then yield a working `.ts` structural gate, not a description of one (the PROP-024 promise, applied to TypeScript).</INSTALLING-THE-STACK-YIELDS-A-WORKING-GATE></p>
    <p><SHIPPABLE-TREE-EXCLUDES-BUILD-OUTPUT fact="true" status="impl/done">The shippable tree excludes build output (`node_modules/`, `dist/`, build caches) the same way it excludes `target/` for Rust (PROP-024 §2.2).</SHIPPABLE-TREE-EXCLUDES-BUILD-OUTPUT></p>
    <p><BINARY-IS-LANGUAGE-SUFFIXED fact="true" status="impl/done">Its binary is **`typescript-ai-native-conform`** — language-suffixed like the Rust stack's `rust-ai-native-conform`, so several discipline checkers can share a `PATH` without shadowing one another.</BINARY-IS-LANGUAGE-SUFFIXED></p>
  </code-root>
  <honesty title="5. The honest note">
    <p><SPECIFIED-NOT-BUILT fact="true" status="unknown/hold">This is **specified, not built** — like the TypeScript checker cards, status `specified`, awaiting the forthcoming VibeVM TypeScript surface as its pilot. @status:unknown</SPECIFIED-NOT-BUILT></p>
    <p><NATIVE-TYPE-TOOLING-IS-REAL-TODAY fact="true" status="unknown/hold">The native type tooling (the Class-E `tsc` loop, the type-level test tools) is real and usable today; what waits is the *structural* gate — the language-neutral rules applied to TypeScript through this frontend. @status:unknown</NATIVE-TYPE-TOOLING-IS-REAL-TODAY></p>
    <p><NOT-ON-THE-CRITICAL-PATH-FOR-TYPE-CHECKS-ONLY fact="true" status="spec/done">Nothing here is on the critical path for a TypeScript consumer who only wants the type checks; it is the path to giving TypeScript the *same architectural discipline* Rust has, through the *same engine*, once there is TypeScript code to hold to it.</NOT-ON-THE-CRITICAL-PATH-FOR-TYPE-CHECKS-ONLY></p>
  </honesty>
</spec>
