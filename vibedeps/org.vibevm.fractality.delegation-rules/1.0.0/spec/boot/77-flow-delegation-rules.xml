<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Flow: Delegation Rules</title>
  <p>This project runs a boss–worker delegation fabric. The routing policy
is one law plus a decidable procedure:</p>
  <p>**Delegate when verification is cheaper than generation.**</p>
  <p>Before doing any bulk, mechanical, or read-and-summarize work
yourself, score the task on four axes — error cost, context
transferability, verifiability, size — and run the verdict procedure
in
[`spec/flows/delegation-rules/DECISION-MATRIX.md`](../flows/delegation-rules/DECISION-MATRIX.md).
A task that survives the three KEEP-gates is delegated: small ×
mechanical → the `small` model slot; everything else delegable → the
`big` slot, as a coarse one-shot. Per-model behavior (budgets, task
shapes, blind spots) lives in the
[playbooks](../flows/delegation-rules/playbooks/); routing names
slots, never vendors.</p>
  <section id="worker-choice" title="The axis also picks the worker">
    <p>Verifiability decides not just *whether* to delegate but
*which* worker gets it. A high-verifiability delegable task
goes to the `big` GLM slot — the review is cheap, so the
cheap, already-paid-for worker wins. A
delegable-but-low-verifiability task, where reviewing a
foreign-model diff would cost as much as regenerating it, may
use a same-model subagent for context-offload. The harness's
native sub-agent tool (Claude Code's `Agent` / `Task` /
`Workflow`, which spawn same-model workers) is **never** the
reflex default for delegable execution: it spends the
expensive process without the cost saving the `big` slot
gives (delegation-first `#route`).</p>
  </section>
  <section id="never-delegate" title="The never-delegate set">
    <p>Always the boss's own work: secrets and credential surfaces;
destructive or irreversible operations; architecture, spec, and plan
authoring; tasks whose ambiguity IS the design; **review of delegated
output**; tiny edits.</p>
  </section>
  <section id="scenarios" title="The two work-order scenarios">
    <p>Every delegated packet picks one, explicitly: **(1)** compile the
context in — exact files, patterns, commands, self-verify; or **(2)**
order the worker to boot from named corpus files first. A big task
with a thin prompt and no boot order is banned — it produces plausible
wrong output that costs more to review than to rewrite.</p>
  </section>
  <section id="never" title="Never">
    <list ordered="false">
      <item>Never delegate anything in the never-delegate set — the economics
  never justify it.</item>
      <item>Never skip the review loop: delegated output is advisory until the
  diff is read and the acceptance/gates are green.</item>
      <item>Never retry a failed packet more than twice (small → big → boss
  reclaims); past that the economics have inverted.</item>
      <item>Never let a delegation surprise die in chat — it belongs in the
  producing model's playbook.</item>
    </list>
  </section>
</spec>
