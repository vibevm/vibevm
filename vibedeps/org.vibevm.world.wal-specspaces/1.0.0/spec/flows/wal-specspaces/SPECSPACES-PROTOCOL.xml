<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">The Specspaces Protocol</title>
  <status stage="spec" state="done"/>
  <p><fact id="scope-of-this-document" status="impl/done">**Scope of this document.** This file defines *what* a specspace is,
the registry file that names them, how a session phrase resolves to a
target, the scoped session grammar, and the five laws that keep a
specspace's state from bleeding into its host.</fact></p>
  <p><fact id="WAL-MECHANICS-ARE-NOT-REDEFINED-HERE" status="impl/done">The per-specspace WAL
mechanics are not redefined here — each specspace runs the two-file
model owned by `flow:org.vibevm.world/wal`.</fact></p>
  <section id="what" title="What a specspace is">
    <p><fact id="A-SPECSPACE-IS-WORKED-ON-AS-AN-INDEPENDENT-PROJECT" status="impl/done">A specspace is a sub-project that lives inside a host repository but
is **worked on as an independent project**: its own boot contract,
its own WAL, its own cold-resume file, its own plans and specs, its
own definition of a green floor.</fact></p>
    <p><fact id="THE-HOST-CARRIES-IT-BUT-A-SPECSPACE-SESSION-IGNORES-HOST-CONTEXT" status="impl/done">The host carries it (one git
history, one set of repo-wide rules), but a session working on the
specspace neither loads nor scans the host's project context.</fact></p>
    <p><fact id="A-SPECSPACE-IS-ITS-OWN-INSTALLABLE-PUBLISHABLE-THING" status="impl/done">A specspace is its own installable, publishable thing in the general
case; that a host repository happens to author one under `packages/`
(as vibevm does with `fractality`) is incidental.</fact></p>
    <p><fact id="PEOPLE-INSTALL-SPECSPACES-AS-ORDINARY-PACKAGES" status="impl/done">People install
specspaces as ordinary packages, and two of them coinciding inside
one host means nothing in general.</fact></p>
    <p><fact id="the-problem-this-solves" status="spec/done">The problem this solves: a central WAL describes one project.</fact></p>
    <p><fact id="sessions-otherwise-face-a-bad-choice" status="spec/done">The
moment a repository hosts a second, independently-evolving effort,
sessions face a bad choice — boot the whole host corpus to work on a
corner of it, or work blind.</fact></p>
    <p><fact id="SPECSPACES-MAKE-THE-SECOND-EFFORT-A-FIRST-CLASS-PROJECT" status="impl/done">Specspaces make the second effort a
first-class project with first-class session continuity, at the cost
of one registry file and one grammar extension.</fact></p>
  </section>
  <section id="registry" title="The registry: `SPECSPACES.md`">
    <p><fact id="ONE-FILE-AT-THE-HOST-ROOT-NAMES-EVERY-SPECSPACE" status="impl/done">One file at the host root names every specspace.</fact></p>
    <p><fact id="AN-OPTIONAL-DEFAULT-LINE-THEN-ONE-ROW-PER-SPECSPACE" status="impl/done">An optional
`default:` line may precede the table; then one table row per
specspace:</fact></p>
    <fence lang="markdown">default: host

| name | root | wal | continue | status |
|---|---|---|---|---|
| fractality | packages/org.vibevm.fractality/ | WAL.md | CONTINUE.md | 2026-07-09 — ignition PLANNED; next: Phase 0 |</fence>
    <list ordered="false">
      <item><fact id="FIELD-DEFAULT" status="impl/done">**`default:`** (optional, above the table) — which target a **bare**
  session phrase resolves to (see [target resolution](#resolve)).
  `default: host` (or omitting the line entirely) makes a bare phrase
  target the host project; `default: &lt;name&gt;` makes a bare phrase
  target that specspace. Omit it unless the repository's primary work
  really lives in one specspace.</fact></item>
      <item><fact id="FIELD-NAME" status="impl/done">**name** — the word used in session phrases. Short, unique,
  lowercase.</fact></item>
      <item><fact id="FIELD-ROOT" status="impl/done">**root** — the specspace root, relative to the host root. The
  specspace's `CLAUDE.md` (or equivalent boot contract) lives here.</fact></item>
      <item><fact id="FIELD-WAL-AND-CONTINUE" status="impl/done">**wal**, **continue** — paths relative to root. Defaults are
  `spec/WAL.md` and `CONTINUE.md` per the wal flow; a specspace
  without a spec tree at its root may keep both flat at the root, as
  the example does. The registry entry is the truth.</fact></item>
      <item><fact id="FIELD-STATUS" status="impl/done">**status** — one line, refreshed at every specspace wind-down:
  date, campaign/phase state, the next step. A pointer for the
  *host's* readers; never the specspace's canonical state.</fact></item>
    </list>
  </section>
  <section id="resolve" title="Target resolution">
    <p><fact id="A-PHRASE-RESOLVES-TO-EXACTLY-ONE-TARGET" status="impl/done">A resume or wind-down phrase resolves to exactly **one** target — the
host project or a single specspace — by this order:</fact></p>
    <list ordered="true">
      <item><fact id="TARGET-EXPLICIT-WINS-ALWAYS" status="impl/done">**Explicit target wins, always.** A phrase that names a specspace
   (`RESUME SESSION &lt;name&gt;`), or that names an explicit directory,
   targets that specspace or directory — **regardless of any declared
   default or prior context**. This is the user's always-available
   escape hatch: an explicit command forces restoration from an
   arbitrary specspace or directory. A name matching no registry row
   is surfaced, not guessed.</fact></item>
      <item><fact id="TARGET-DECLARED-DEFAULT" status="impl/done">**Declared default.** A **bare** phrase (no name) uses the
   specspace named by the `default:` line of `SPECSPACES.md`, when one
   is declared.</fact></item>
      <item><fact id="TARGET-HOST-FALLBACK" status="impl/done">**Host fallback.** With no name and no declared default, a bare
   phrase targets the **host project** — restore the host root's own
   WAL and cold-resume file, per the host contract's session-command
   sections.</fact></item>
    </list>
    <p><fact id="A-BARE-PHRASE-NEVER-SILENTLY-SELECTS-A-SPECSPACE" status="impl/done">A bare phrase therefore **never silently selects a specspace**.</fact></p>
    <p><fact id="AT-THE-HOST-ROOT-A-BARE-PHRASE-RESTORES-THE-HOST-WAL" status="impl/done">At the
host root, a bare `RESUME SESSION` / `ВОССТАНОВИ СЕССИЮ` restores the
**host** WAL — not a registered specspace such as `fractality`.</fact></p>
    <p><fact id="the-whole-point-of-rule-three" status="spec/done">This
is the whole point of rule 3: a registry with rows in it must not
tempt a session into resuming the wrong project.</fact></p>
  </section>
  <section id="grammar" title="The session grammar">
    <p><fact id="PHRASES-GAIN-AN-OPTIONAL-SPECSPACE-NAME" status="impl/done">The wal flow's wind-down and resume phrases gain an optional
specspace name.</fact></p>
    <p><fact id="recognise-the-intent-not-the-exact-wording-lead" status="impl/done">Recognise the intent, not the exact wording:</fact></p>
    <table>
      <tr>
        <td>Intent</td>
        <td>English</td>
        <td>Russian</td>
      </tr>
      <tr>
        <td><fact id="ROW-RESUME-INTO-SPECSPACE" status="impl/done">resume into specspace</fact></td>
        <td><fact status="impl/done">`RESUME SESSION &lt;name&gt;`, `RESTORE CONTEXT &lt;name&gt;`</fact></td>
        <td><fact status="impl/done">`ВОССТАНОВИ СЕССИЮ &lt;name&gt;`, `ПРОДОЛЖАЕМ &lt;name&gt;`</fact></td>
      </tr>
      <tr>
        <td><fact id="ROW-WIND-DOWN-SPECSPACE" status="impl/done">wind down specspace</fact></td>
        <td><fact status="impl/done">`END SESSION &lt;name&gt;`, `WRAP UP &lt;name&gt;`</fact></td>
        <td><fact status="impl/done">`ЗАВЕРШИ СЕССИЮ &lt;name&gt;`, `ФИКСИРУЕМ &lt;name&gt;`</fact></td>
      </tr>
    </table>
    <p><fact id="THE-NAME-IS-OPTIONAL" status="impl/done">The name is optional; [target resolution](#resolve) decides what a
phrase with no name means.</fact></p>
    <p><fact id="RESUME-INTO-A-SPECSPACE" status="impl/done">**Resume into a specspace** (report-then-wait, as in the wal flow):
read the host's repo-wide rules, the specspace boot contract, the
specspace WAL, the specspace cold-resume file; verify the tree
empirically (branch, sync, working tree, recent commits touching the
specspace); report; stop and wait for direction.</fact></p>
    <p><fact id="WIND-DOWN-A-SPECSPACE" status="impl/done">**Wind down a specspace:** rewrite the specspace WAL; overwrite the
specspace cold-resume file wholesale; refresh the specspace's status
line in `SPECSPACES.md`; commit per the host's commit rules; update
the host WAL **only** if host files changed too.</fact></p>
  </section>
  <section id="laws" title="The five laws">
    <list ordered="true">
      <item><fact id="LAW-BOOT-SCOPING" status="impl/done">**Boot scoping.** A specspace session loads the host's repo-wide
   rules and the specspace's own files — nothing else from the host.
   Crossing into host files mid-session is legal but announced.</fact></item>
      <item><fact id="LAW-STATE-LOCALITY" status="impl/done">**State locality.** A specspace's canonical state lives in the
   specspace (its WAL). The registry status line is a pointer, not
   state; the host WAL never carries specspace detail beyond "the
   specspace exists; see its WAL".</fact></item>
      <item><fact id="LAW-ONE-SESSION-ONE-FOCUS" status="impl/done">**One session, one focus.** A session serves the host or one
   specspace. Work for two projects in one session splits into
   commits per project and updates each project's WAL — and is the
   exception, said out loud, not the habit.</fact></item>
      <item><fact id="LAW-HOST-RULES-SURVIVE" status="impl/done">**Host rules survive.** Repo-wide non-negotiables (authorship,
   commit conventions, secrecy rules) bind specspace sessions in
   full. A specspace may add rules; it may not subtract the host's.</fact></item>
      <item><fact id="LAW-PACKAGE-STATE-STAYS-OUT" status="impl/done">**Package state stays out.** WAL, cold-resume, and registry files
   are project state. No installable package may create, overwrite,
   or delete them on install or uninstall — the same law the wal
   flow states for its two files.</fact></item>
    </list>
  </section>
  <section id="lifecycle" title="Lifecycle">
    <list ordered="false">
      <item><fact id="LIFECYCLE-REGISTER" status="impl/done">**Register:** create the specspace root with its boot contract,
  WAL, and cold-resume file; add the registry row; mention the
  registry in the host boot contract so sessions recognise the
  grammar. First wind-down validates the loop.</fact></item>
      <item><fact id="LIFECYCLE-RETIRE" status="impl/done">**Retire:** the specspace graduates to its own repository (its
  files move wholesale; its WAL goes with it) or closes (final
  status line says so; the row moves to a "retired" section — the
  name stays reserved so old phrases fail loudly, not silently).</fact></item>
      <item><fact id="LIFECYCLE-NESTING" status="impl/done">**Nesting:** one level. A specspace hosting its own specspaces is
  a sign it wants to be a repository.</fact></item>
    </list>
  </section>
  <section id="re-derive" title="Re-derive for your project">
    <p><fact id="run-this-prompt-once-lead" status="impl/done">Run this prompt once to adapt the protocol to a concrete host:</fact></p>
    <fence>Read SPECSPACES-PROTOCOL.md. Adapt it to this repository:
1. Create SPECSPACES.md at the host root (empty table if no
   specspaces exist yet). Add a `default:` line only if a bare
   session phrase should target a specspace instead of the host.
2. Add a short "Specspaces" section to the host boot contract:
   the grammar, target resolution (bare → default else host), the
   boot-scoping law, and a pointer to the registry.
3. If a sub-project already behaves like a specspace, register it:
   boot contract, WAL, cold-resume file, registry row.
4. Record which host sections are "repo-wide rules" that specspace
   sessions must still load — name them explicitly.
Do not move any existing project state while adapting.</fence>
  </section>
  <section id="summary" title="Summary">
    <list ordered="false">
      <item><fact id="SUM-WHAT-A-SPECSPACE-IS" status="impl/done">A specspace is a sub-project with first-class session continuity:
  own boot contract, own WAL, own cold-resume file.</fact></item>
      <item><fact id="SUM-THE-REGISTRY" status="impl/done">`SPECSPACES.md` at the host root is the registry; an optional
  `default:` line sets what a bare phrase targets; its status column
  is a pointer, never canonical state.</fact></item>
      <item><fact id="SUM-TARGET-RESOLUTION" status="impl/done">Target resolution: an explicit name/directory always wins; a bare
  phrase takes the declared default, else the host — never a
  specspace by accident.</fact></item>
      <item><fact id="SUM-THE-SESSION-GRAMMAR" status="impl/done">Session phrases gain an optional specspace name; resume stays
  report-then-wait; wind-down operates on the specspace's files.</fact></item>
      <item><fact id="SUM-THE-FIVE-LAWS" status="impl/done">Five laws: boot scoping, state locality, one focus, host rules
  survive, package state stays out.</fact></item>
      <item><fact id="SUM-NESTING-AND-GRADUATION" status="impl/done">One nesting level; a specspace that outgrows the host graduates
  to its own repository.</fact></item>
    </list>
  </section>
</spec>
