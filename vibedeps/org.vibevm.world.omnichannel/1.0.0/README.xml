<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">`flow:omnichannel` — a capability lives in a library, not in a surface</title>
  <status stage="doc" state="done" audience="user"/>
  <p><PACKAGE-INSTALLS-THE-SURFACE-DISCIPLINE fact="true" status="impl/done">A vibevm `flow` package that installs one architectural rule and the
vocabulary it needs: a capability lives in a **library**, and the command
line, the TUI, the MCP server and the HTTP API are **surfaces** over it —
thin, replaceable, and none of them the base.</PACKAGE-INSTALLS-THE-SURFACE-DISCIPLINE></p>
  <p><the-cost-appears-at-the-second-surface fact="true" status="spec/done">The cost never shows up when the first surface is written; it shows
up when the second one is, because the logic is already inside the first.</the-cost-appears-at-the-second-surface></p>
  <p><TWO-SURFACES-THAT-DISAGREE-CANNOT-BE-ADJUDICATED fact="true" status="impl/done">Two surfaces of one capability that answer the same question
differently cannot be adjudicated: nothing says which is wrong. The
library is that third place.</TWO-SURFACES-THAT-DISAGREE-CANNOT-BE-ADJUDICATED></p>
  <p><package-contents-lead fact="true" status="impl/done">This package ships:</package-contents-lead></p>
  <facts ordered="false">
    <CONTENT-THE-PROTOCOL fact="true" status="impl/done">`spec/flows/omnichannel/OMNICHANNEL-PROTOCOL.xml` — the rule, the surface
  vocabulary and its three classes, what "thin" means per class, how a
  project declares its floor, and why the coverage table must be derived.</CONTENT-THE-PROTOCOL>
    <CONTENT-THE-BOOT-SNIPPET fact="true" status="impl/done">`spec/boot/68-flow-omnichannel.xml` — the boot snippet read at session
  start: the rule, the vocabulary, the obligations and the never-do list.</CONTENT-THE-BOOT-SNIPPET>
  </facts>
  <not title="What it is not">
    <p><NOT-A-CODE-STYLE-RULE fact="true" status="impl/done">It is not a code-style rule and belongs to no language discipline. A
project writing no AI-native code still has surfaces and still pays for
letting them diverge.</NOT-A-CODE-STYLE-RULE></p>
    <p><NOT-A-CHECKLIST-OF-SURFACES-EVERY-PROJECT-OWES fact="true" status="impl/done">It is not a checklist of surfaces every project owes. The vocabulary
names them so two projects mean the same thing; **the floor is the
project's own declaration**, and an undeclared surface is a choice rather
than a debt.</NOT-A-CHECKLIST-OF-SURFACES-EVERY-PROJECT-OWES></p>
  </not>
  <test title="The test that settles arguments">
    <p><DELETE-THE-SURFACE-ON-PAPER fact="true" status="impl/done">Delete the surface on paper. If anything but presentation and
transport is lost, the split is wrong — the capability had leaked into the
surface.</DELETE-THE-SURFACE-ON-PAPER></p>
  </test>
</spec>
