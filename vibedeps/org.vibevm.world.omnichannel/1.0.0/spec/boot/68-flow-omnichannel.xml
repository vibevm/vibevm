<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Flow: Omnichannel</title>
  <status stage="impl" state="done"/>
  <p><fact id="A-CAPABILITY-LIVES-IN-A-LIBRARY-NOT-IN-A-SURFACE" status="impl/done">In this project a capability lives in a **library**. The command
line, the TUI, the MCP server, the HTTP API are **surfaces** over it —
thin, interchangeable, and none of them the base.</fact></p>
  <p><fact id="NAILING-LOGIC-TO-ONE-SURFACE-IS-THE-DEFECT" status="impl/done">Nailing the logic to
whichever surface was written first is the defect this flow exists to
prevent: the second surface then reimplements it, and the two answer the
same question differently.</fact></p>
  <section id="vocabulary" title="The surface vocabulary">
    <p><fact id="SURFACES-ARE-NAMED-FROM-ONE-LIST" status="impl/done">Surfaces are named from one list, so that every project means the
same thing by them:</fact></p>
    <fence>Library  ─── not a surface, but what every surface sits on
             (mandatory once there is more than one surface)
    │
    ├── local, synchronous:   CLI · TUI · GUI
    ├── agent-facing:         MCP · LSP · IDE extension
    └── networked:            REST · GraphQL · Queue</fence>
    <p><fact id="THE-CLASS-DECIDES-WHAT-REPEATS" status="impl/done">The grouping earns its place because the class decides **what
repeats**. Local surfaces share the problem of rendering one dataset into
different views; agent-facing ones share tool description and schema;
networked ones share contract versioning and compatibility. "Logic in a
library" is one law for all three — what counts as a *thin* surface differs
per class.</fact></p>
  </section>
  <section id="obligations" title="What a project owes">
    <list ordered="false">
      <item><fact id="A-PROJECT-DECLARES-ITS-OWN-SET" status="impl/done">**A project declares its own set** of surfaces. A surface that is
  not declared is not a debt: an undeclared LSP is a choice, not a gap.</fact></item>
      <item><fact id="A-NEW-CAPABILITY-IS-BORN-WITH-EVERY-DECLARED-SURFACE" status="impl/done">**A new capability is born with all declared surfaces**, or with a
  recorded reason why one sufficed. The reason is the artifact; "we will
  add it later" is not one.</fact></item>
      <item><fact id="THE-SURFACE-TABLE-IS-DERIVED-NEVER-HAND-MAINTAINED" status="spec/plan">**The table of which capability has which surface is derived, never
  hand-maintained.** A hand-kept table rots exactly like every other
  restated fact — so the vocabulary is machine-readable, and the table is
  a query.</fact></item>
    </list>
  </section>
  <section id="never" title="Never">
    <list ordered="false">
      <item><fact id="NEVER-PUT-THE-LOGIC-IN-THE-SURFACE" status="impl/done">Never put a capability's logic in a surface. The surface parses
  input, calls the library, renders output.</fact></item>
      <item><fact id="NEVER-LET-ONE-SURFACE-BECOME-THE-REFERENCE-IMPLEMENTATION" status="impl/done">Never let one surface become the reference implementation that
  others are ported from — that is the same defect wearing a plan.</fact></item>
      <item><fact id="NEVER-HAND-MAINTAIN-THE-COVERAGE-TABLE" status="impl/done">Never hand-maintain the surface-coverage table; derive it.</fact></item>
    </list>
    <p><fact id="full-protocol-pointer" status="impl/done">Full protocol, including the declaration form and the thinness test
per class:
@spec://org.vibevm.world/omnichannel/flows/omnichannel/OMNICHANNEL-PROTOCOL#root.</fact></p>
  </section>
</spec>
