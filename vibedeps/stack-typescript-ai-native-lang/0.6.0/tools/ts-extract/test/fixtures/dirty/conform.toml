# The dirty fixture's conform policy: everything gated, so the e2e test
# proves each TS rule fires. NOT a template — `typescript-ai-native
# init` writes the starting policy for real projects.

[typescript]
roots = ["src"]
exclude_substrings = []
cells_dir = "src/cells"
gated = ["greet", "parse"]
seam = "index"
