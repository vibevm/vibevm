<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">`flow:git-conventional-commits` — the commit message format</title>
  <status stage="doc" state="done" audience="user"/>
  <p><fact id="PACKAGE-INSTALLS-THE-CONVENTIONAL-COMMITS-MESSAGE-FORMAT" status="impl/done">A vibevm `flow` package that installs the [Conventional Commits](https://www.conventionalcommits.org/)
message format: a typed header (`type(scope): subject`) and a body that explains *why* a change
was made, not *what* the diff already shows.</fact></p>
  <p><fact id="THIS-IS-THE-MESSAGE-FORMAT-ONLY" status="impl/done">This is the message **format** only.</fact></p>
  <p><fact id="ATOMICITY-IS-THE-SEPARATE-ATOMIC-COMMITS-PACKAGE" status="impl/done">The complementary discipline — **atomicity**, one commit =
one logical idea — is the separate `flow:git-atomic-commits` package.</fact></p>
  <p><fact id="EACH-DISCIPLINE-IS-ADOPTABLE-ON-ITS-OWN" status="impl/done">You can follow this format and
still write a non-atomic commit (`feat: add foo, bar, and baz`), and you can be atomic without
this format; the two run together, and each is its own package so a project can adopt either.</fact></p>
  <p><fact id="package-contents-lead" status="impl/done">This package ships:</fact></p>
  <list ordered="false">
    <item><fact id="CONTENT-THE-FULL-FORMAT" status="impl/done">`spec/flows/conventional-commits/conventional-commits.md` — the full format: header shape,
  the allowed-type table, scope convention, body structure, worked examples, and anti-patterns.</fact></item>
    <item><fact id="CONTENT-THE-BOOT-SNIPPET" status="impl/done">`spec/boot/31-flow-conventional-commits.md` — the boot snippet loaded at session start.</fact></item>
  </list>
  <section id="install" title="Install">
    <fence lang="bash">vibe install flow:git-conventional-commits</fence>
  </section>
  <section id="composition" title="Composition">
    <list ordered="false">
      <item><fact id="COMPOSES-ATOMIC-COMMITS" status="impl/done">Pairs with `flow:git-atomic-commits` (atomicity) — together they are the commit-message half of a
  `git-practices` posture.</fact></item>
    </list>
  </section>
  <section id="license" title="License">
    <p><fact id="license-line" status="impl/done">UPL-1.0 — see `LICENSE`.</fact></p>
  </section>
</spec>
