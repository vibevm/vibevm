<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>GUIDE — TypeScript/JavaScript under the Discipline, v0.1</title>
  <p>**Status.** Beta; second T2 guide. Section structure is deliberately isomorphic to `GUIDE-RUST-v0.1.md` — cross-language diffing of the guides is a feature of the discipline, not a coincidence. Scope: only what the axioms require; formatting and generic style live in tooling.</p>
  <p>Framing note — the inverse of Rust's. Rust *enforces*; TypeScript *permits everything*, because underneath it is JavaScript: mutable, dynamic, side-effectful, with an erased type system. The primary instrument of this guide is therefore **subset selection**, enforced by compiler flags and type-aware lints (the C++ situation, with a far better enforcement toolkit). Classical patterns dissolve here too, differently: Strategy = interface + composition root; Visitor = discriminated union + exhaustive `switch`; Decorator = wrapper object; Observer = an explicit event seam, never `EventEmitter` inheritance; Singleton = forbidden, pass it.</p>
  <section id="baseline" title="0. Language baseline">
    <list ordered="false">
      <item>**Cells are TypeScript (`.ts`).** Plain JS is tolerated only in scripts/config under `// @ts-check` with JSDoc types. Seam declarations are always TS.</item>
      <item>**tsconfig (load-bearing flags):** `strict`, `noUncheckedIndexedAccess` (indexing can miss), `exactOptionalPropertyTypes`, `noImplicitOverride`, `noFallthroughCasesInSwitch`, `noPropertyAccessFromIndexSignature`, `isolatedModules`, `verbatimModuleSyntax`; `module: NodeNext` (server / sidecar cells) or `bundler` (front-end cells).</item>
      <item>**ESM-only:** `"type": "module"`; `require`/CJS interop is quarantined in boundary modules.</item>
      <item>**Lint baseline:** typescript-eslint `strict-type-checked` + `stylistic-type-checked`; ESLint runs as a conform **evidence provider** via SARIF (ENGINE §2) — we cite its checks, we do not reimplement them.</item>
      <item>**Type-suppression policy:** `@ts-expect-error` (with a reason comment) is the **only** permitted suppression — it is xfail-strict by construction: the directive itself errors when the underlying error disappears, forcing promotion (BROWNFIELD §4 at the type level). `@ts-ignore` is banned. `any`, bare `as`, and non-null `!` are banned in cells; they are legal only in **boundary modules** (§4) with a one-line justification.</item>
      <item>**Workspace:** pnpm workspaces; tsc project references (`composite: true`) for incremental type-checking — the facts-class cache at the type level (axiom A2).</item>
      <item>**Tests:** vitest; property testing: fast-check (the proptest analog). All MIT/Apache-class tooling.</item>
    </list>
  </section>
  <section id="cells" title="1. Cells">
    <p>A cell is a directory module behind one seam: single public entry (`index.ts`) exporting the seam implementation and nothing else.</p>
    <list ordered="false">
      <item>**Side-effect-free on import.** In JS, importing a module *executes* it — the language's most underrated landmine. Cell modules perform no top-level work (no I/O, no registration, no global mutation); `"sideEffects": false` in `package.json` must be honest. Enforced: lint + bundler audit.</item>
      <item>**No cross-cell barrels.** A barrel that re-exports across cells silently fuses them (and wrecks tree-shaking and circular-import analysis). The cell's `index.ts` is its only surface; sibling-cell imports are forbidden (R-002) — including via path aliases, which is why this check needs T-sem module resolution, not grep.</item>
      <item>**Platform capabilities are injected.** Cells never touch `process.env`, `fs`, `fetch`, or other ambient platform globals directly — those are seams, passed in at construction (ports-and-adapters; the sans-io instinct). Time and randomness SHOULD also be injected (determinism in tests). Consequence: cell tests need no module interception at all.</item>
      <item>**Promotion to a workspace package** when: heavy optional dependencies, independent publish boundary, or ~2 kLoC — mirrors the Rust crate-promotion procedure and is equally pilot-measured.</item>
    </list>
    <p>Cell manifest (JSDoc carrier, §5):</p>
    <fence lang="ts">/**
 * @spec implements spec://org.vibevm.core/vibevm/modules/vibe-resolver/PROP-003#solver-upgrade r2
 * @cell seam=DepSolver variant=sat replaces=naive flag=solver
 */
export class SatDepSolver implements DepSolver { /* … */ }</fence>
  </section>
  <section id="seams" title="2. Seams">
    <list ordered="false">
      <item>A seam is a TS `interface` living in core/seams — never inside a cell.</item>
      <item>**Composition over inheritance is a MUST at seams:** no abstract base classes with behavior (template methods are hidden control flow, R-021). A seam exposes functions; state stays inside the cell.</item>
      <item>Structural typing caveat: TS interfaces match by shape, so accidental conformance is possible. For identity-critical seams, a phantom brand (`readonly __seam?: "DepSolver"`) MAY be used; the registry remains the real gatekeeper.</item>
      <item>Seam methods that can fail in expected ways return `Result` (§4) — the failure surface is part of the interface text, visible to the compiler.</item>
    </list>
  </section>
  <section id="flags" title="3. Registry and flags">
    <p>R-001 binding — flag at the seam, never in the veins:</p>
    <fence lang="ts">// src/registry.ts — the only module reading selection flags
// and the only legal site of computed dynamic import().
export async function depSolver(flags: Flags, provider: DepProvider): Promise&lt;DepSolver&gt; {
  switch (flags.get("solver")) {              // provenance: default | env | cli | lockfile
    case "sat":  return new (await import("./cells/sat-dep-solver/index.js")).SatDepSolver(provider);
    default:     return new NaiveDepSolver(provider);   // eager: imported statically above
  }
}</fence>
    <list ordered="false">
      <item>**Two tiers, never confused:** bundle-time constants (`define` / `import.meta.env` → dead-code elimination) answer *"is the code in the bundle"* — the cargo-feature analog; runtime flags answer *"is the cell selected"*. A runtime flag must not change the type surface; a bundle constant must not encode product choice.</item>
      <item>Eager vs lazy cell loading (static import vs `await import()` in the registry) is the code-level mirror of vibevm's delivery modes; lazy is a registry decision, never a cell's own.</item>
      <item>**No DI containers.** Inversify/tsyringe/Nest-style `reflect-metadata` injection is link-time magic: it violates R-021 and blinds T-syn analysis. Explicit constructor injection + the hand-written registry `switch` is the system's table of contents.</item>
    </list>
  </section>
  <section id="errors" title="4. Errors as contract">
    <p>TypeScript cannot type `throws`; an exception is invisible in the seam's signature — an A1 hole the language will never close. Therefore:</p>
    <list ordered="false">
      <item>**Expected failures are values.** Seams return a discriminated Result — the minimal core type, no framework dependency (`neverthrow` MAY be adopted later; the shape is what matters):</item>
    </list>
    <fence lang="ts">  type Result&lt;T, E&gt; = { ok: true; value: T } | { ok: false; error: E };</fence>
    <p>Error objects carry `code` and the violated REQ URI (`static spec = "spec://…#req-…" as const` on error classes/factories) — conform checks coverage via the index; user-facing rendering appends the URI (PROP-014 §2.6).</p>
    <list ordered="false">
      <item>**`throw` is for invariant violations only** — the panic analog. Never throw strings or bare `Error` in cells; chain with `cause` (ES2022).</item>
      <item>**Async hygiene (MUST, type-aware lint):** no floating promises; no unhandled rejections; no `async` functions passed where `void`-returns are expected.</item>
      <item>**Exhaustiveness:** closed sets are discriminated unions with compiler-checked exhaustive `switch` (`assertNever` in `default`, or `satisfies`-based patterns); the type-aware switch-exhaustiveness lint gates it.</item>
      <item>**Source maps are the release map.** Here the discipline's founding analogy becomes literal: production builds ship (or retain server-side) `.map` files keyed by build id; the A1 chain *minified frame → original source → item → REQ* must resolve end-to-end. An error report that dies at `chunk-7f3a.js:1:48211` is an explainability defect, not a cosmetic one.</item>
    </list>
  </section>
  <section id="specmark" title="5. specmark carrier">
    <p>JSDoc tags — zero runtime cost, valid in `.ts` and `.js`, parseable by both tree-sitter and the TS compiler API, and (a pleasant simplification over Rust) the carrier is already the hover-doc surface, so no doc-injection machinery is needed:</p>
    <fence>/** @spec implements &lt;uri&gt; r&lt;N&gt; */            one edge per line; tags repeat
/** @spec deviates &lt;uri&gt; r&lt;N&gt; reason="…" */   reason mandatory
/** @verifies &lt;uri&gt; r&lt;N&gt; */                   on tests
/** @specScope &lt;uri&gt; r&lt;N&gt; */                  file-top block: module-level inheritance</fence>
    <p>Standard (TC39) decorators were considered and rejected as carrier: runtime weight, `.js` incompatibility, and no analytical gain over JSDoc. ≤3 edges per item or split (same lint as Rust).</p>
  </section>
  <section id="naming" title="6. Naming (R-020/R-021 bindings)">
    <list ordered="false">
      <item>Canonical cell type name is computed from the manifest: `{Variant}{Seam}` → `SatDepSolver`. Hand-written names are linted against the computation. Length free, ambiguity not — `MultiRegistryResolverWithRedirectFollowing` is fine *iff* every token is manifest-backed.</item>
      <item>The TS ecosystem loves both verbose names and hidden magic; this guide keeps the first and bans the second. **Forbidden in cells regardless of elegance:** Proxy-based domain APIs; getters/setters with side effects; prototype or builtin patching; behavior-bearing decorators; `Symbol.toPrimitive`/`valueOf` coercion tricks; module-graph interception in tests (`vi.mock`/`jest.mock` — inject capabilities instead, §1); computed dynamic `import()` outside the registry.</item>
    </list>
  </section>
  <section id="replacement" title="7. Replacement protocol (R-040 binding)">
    <p>A cell with `replaces=…` ships a differential oracle: fast-check property tests asserting agreement with the old cell across the seam (documented-divergence list otherwise), `@verifies`-tagged. **Snapshot/golden discipline (brownfield binding):** snapshots are characterization artifacts; CI never updates them; local updates require the promotion protocol — a debt/intent reference in the commit body. Blanket `--update` is the snapshot ecosystem's graveyard mode and is banned.</p>
  </section>
  <section id="risks" title="8. Risk table (what conform must cover for TS/JS)">
    <table>
      <tr>
        <td>Footgun</td>
        <td>Rule</td>
        <td>Tier</td>
      </tr>
      <tr>
        <td>`any` / `as` / `!` / `@ts-ignore` outside boundary modules</td>
        <td>§0</td>
        <td>T-syn</td>
      </tr>
      <tr>
        <td>floating promise / unhandled rejection</td>
        <td>§4</td>
        <td>T-sem</td>
      </tr>
      <tr>
        <td>top-level side effects in a cell module</td>
        <td>§1</td>
        <td>T-sem</td>
      </tr>
      <tr>
        <td>cell importing a sibling cell (incl. via aliases/barrels)</td>
        <td>R-002</td>
        <td>T-syn + T-sem</td>
      </tr>
      <tr>
        <td>expected failure thrown across a seam / thrown string</td>
        <td>§4</td>
        <td>T-sem</td>
      </tr>
      <tr>
        <td>non-exhaustive switch on a discriminated union</td>
        <td>§4</td>
        <td>T-sem</td>
      </tr>
      <tr>
        <td>Proxy / prototype patching / behavior decorators in cells</td>
        <td>§6</td>
        <td>T-syn</td>
      </tr>
      <tr>
        <td>direct `process.env` / `fs` / `fetch` in cells</td>
        <td>§1</td>
        <td>T-syn</td>
      </tr>
      <tr>
        <td>computed dynamic `import()` outside the registry</td>
        <td>§3, §6</td>
        <td>T-syn</td>
      </tr>
      <tr>
        <td>`require` / CJS interop inside cells</td>
        <td>§0</td>
        <td>T-syn</td>
      </tr>
      <tr>
        <td>flag read outside the registry</td>
        <td>R-001</td>
        <td>T-syn</td>
      </tr>
      <tr>
        <td>snapshot updated without a promotion reference</td>
        <td>§7</td>
        <td>process check</td>
      </tr>
      <tr>
        <td>public export without own/inherited spec edge</td>
        <td>PROP-014 §3.2-6</td>
        <td>T-syn + index</td>
      </tr>
    </table>
  </section>
  <section id="docs" title="9. Doc layer">
    <p>TSDoc on every tagged export states the practically-important behavior: error codes and their REQ URIs, async semantics (cancellation, ordering, retry expectations), edge cases, performance traps. TSDoc is the human-facing detail layer; the spec stays thin; the ledger renders machine explanations from both. Duplication between TSDoc and spec is a defect on the spec side.</p>
    <p>**First carrier note.** vibevm has no TypeScript today; the first TS cell under this guide is already designated by the architecture itself — the conform engine's TypeScript sidecar (ENGINE §2), the process that asks the TS compiler about its own language. Self-hosting, again.</p>
    <p>*Any rule binding here without a corresponding conform check (or explicit `WISH` mark in the Charter rule record) by the first TS carrier milestone is removed rather than carried as aspiration.*</p>
  </section>
</spec>
