<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">PROP-014 — specmap: bidirectional spec↔code traceability and the source metamodel</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Status.** Design proposal v0.1 — not implementation-locked.</status-line></p>
  <p><drafted-for-review-open-until-ratified fact="true" status="impl/done">Drafted for review; every decision below is open to challenge until ratified.</drafted-for-review-open-until-ratified></p>
  <p><companions-lead fact="true" status="impl/done">**Companions** (vibevm-hosted, the pilot project's spec tree — cited as context, not shipped here):</companions-lead></p>
  <list ordered="false">
    <item><COMPANION-PROP-000 fact="true" status="impl/done">PROP-000 (foundation, license policy §3),</COMPANION-PROP-000></item>
    <item><COMPANION-PROP-003 fact="true" status="impl/done">PROP-003 (the LLM-boundary philosophy this PROP extends),</COMPANION-PROP-003></item>
    <item><COMPANION-PROP-009 fact="true" status="impl/done">PROP-009 (boot/loading model — specmap becomes its intra-project counterpart),</COMPANION-PROP-009></item>
    <item><COMPANION-PROP-013 fact="true" status="impl/done">PROP-013 (category C "drift" — specmap mechanizes its detection),</COMPANION-PROP-013></item>
    <item><COMPANION-RED-BOOK fact="true" status="impl/done">Red Book ch. 2 (files as IPC) and ch. 3 (Sync-from-Code — specmap is its instrumentation).</COMPANION-RED-BOOK></item>
  </list>
  <p><HOME-SHIPS-WITH-THE-DISCIPLINE fact="true" status="impl/done">**Home.** `flow:org.vibevm.ai-native/core-ai-native`, `spec/mechanisms/` — this mechanism ships with the Discipline (URIs `spec://org.vibevm.ai-native/core-ai-native/mechanisms/PROP-014#…`); its Rust implementation ships in `stack:org.vibevm.ai-native/rust-ai-native-lang` (`specmap-core` + the `rust-ai-native-specmap` binary).</HOME-SHIPS-WITH-THE-DISCIPLINE></p>
  <p><TAG-SYNTAX-SHOWN-IS-THE-RUST-PROJECTION fact="true" status="impl/done">The tag *syntax* shown throughout (`#[spec]`, `scope!`) is the Rust projection; other language stacks ship their own projection of the same model.</TAG-SYNTAX-SHOWN-IS-THE-RUST-PROJECTION></p>
  <problem title="1. Problem statement">
    <p><vibevm-scale-and-its-governing-documents fact="true" status="spec/done">vibevm is ~52K lines of Rust governed by ~20 PROP documents plus a 99KB owner-frozen spec.</vibevm-scale-and-its-governing-documents></p>
    <p><linkage-exists-today-only-in-three-weak-forms fact="true" status="spec/done">The linkage between the two layers exists today only as (a) prose cross-references in doc comments, (b) `spec://` URIs cited in commit bodies per the Rule-2 discipline, and (c) the maintainer's head.</linkage-exists-today-only-in-three-weak-forms></p>
    <p><drift-is-detected-by-periodic-human-sweep fact="true" status="spec/done">AUDIT category C (drift) is detected by periodic human sweep.</drift-is-detected-by-periodic-human-sweep></p>
    <p><the-two-questions-become-unanswerable fact="true" status="spec/done">As the codebase grows, the question "which code realises this decision?" and its inverse "which decision justifies this code?" become unanswerable at acceptable cost.</the-two-questions-become-unanswerable></p>
    <p><desired-end-state-is-bidirectional-reach fact="true" status="spec/done">The desired end-state, by analogy with JS/TS source maps: from any compiled artefact you can reach the source; from any source you can reach the artefacts.</desired-end-state-is-bidirectional-reach></p>
    <p><many-to-many-is-the-real-shape fact="true" status="spec/done">One spec unit may touch many code items; one code item may serve many spec units.</many-to-many-is-the-real-shape></p>
    <p><SYSTEM-REPRESENTS-MANY-TO-MANY-AND-LINTS-ITS-GROWTH fact="true" status="spec/done">Many-to-many is undesirable but real; the system must represent it while linting against its growth. *Half built: the representation ships and is exercised — the host index carries 912 edges over 898 code items, so many-to-many is real in practice, not just permitted. Nothing lints its growth: no edges-per-item or fan-out check exists in the specmap ratchet, in `vibe check`'s roster, or anywhere else, so the fan-out can rise without a signal.*</SYSTEM-REPRESENTS-MANY-TO-MANY-AND-LINTS-ITS-GROWTH></p>
    <analogy title="1.1 Where the source-map analogy holds, and where it breaks">
      <p><source-maps-work-because-a-compiler-emits-them fact="true" status="spec/done">Source maps work because a **compiler emits them as a free, deterministic byproduct of every build**.</source-maps-work-because-a-compiler-emits-them></p>
      <p><between-spec-and-realisation-there-is-no-compiler fact="true" status="spec/done">Between a vibevm spec and its Rust realisation there is no compiler — there is a human or an LLM session.</between-spec-and-realisation-there-is-no-compiler></p>
      <p><consequences-lead fact="true" status="impl/done">Consequences:</consequences-lead></p>
      <list ordered="true">
        <item><CONSEQUENCE-MAPPING-IS-CARRIED-NOT-GENERATED fact="true" status="impl/done">The mapping cannot be *generated*; it can only be *carried and verified*. Until M1.5's `vibe build` exists, every edge is authored metadata.</CONSEQUENCE-MAPPING-IS-CARRIED-NOT-GENERATED></item>
        <item><CONSEQUENCE-AUTHORED-METADATA-ROTS-UNLESS-THREE-FORCES-HOLD fact="true" status="impl/done">Authored metadata rots unless three forces hold simultaneously:</CONSEQUENCE-AUTHORED-METADATA-ROTS-UNLESS-THREE-FORCES-HOLD></item>
        <item><FORCE-EDGES-TRAVEL-WITH-THE-ARTEFACTS fact="true" status="impl/done">**Edges travel with the artefacts.** Code-side links live *in the code* (attributes on items) and survive any refactor that moves the item. Spec-side links live *in the spec* (stable anchors). External sidecar maps are rejected (§5.1).</FORCE-EDGES-TRAVEL-WITH-THE-ARTEFACTS></item>
        <item><FORCE-INVARIANTS-ARE-MACHINE-CHECKED fact="true" status="impl/done">**Invariants are machine-checked.** Dangling references, uncovered requirements, orphan code, and — the load-bearing one — **staleness**: a spec unit carries a revision + content hash; when it changes, every edge pinned to the old revision flips to *suspect* until re-affirmed.</FORCE-INVARIANTS-ARE-MACHINE-CHECKED></item>
        <item><FORCE-MAP-IS-LOAD-BEARING-IN-DAILY-WORK fact="true" status="impl/done">**The map is load-bearing in daily work.** A map that is only audited dies (the classical requirements-traceability graveyard). specmap must feed (i) agent context paging — working on a REQ pulls its code, editing an item pulls its specs; (ii) `vibe explain`; (iii) error provenance — failures cite the violated REQ. *Two of the three feeds ship. (ii) is real, under the name `trace explain` rather than `vibe explain` (`specmap/src/explain.rs:199,209`, driven by `cargo xtask trace explain` and `rust-ai-native trace explain`). (iii) is real and enforced: the conform rules `error-message-cites-req` and `error-enum-cites-req` require the `spec://` REQ URI in the Display text itself — "errors are agent food" — and host errors carry it, e.g. `vibe-core/src/error.rs:34` emits `(violates spec://org.vibevm.core/vibevm/modules/vibe-registry/PROP-008#pkgref; …)`. **(i) agent context paging has no implementation** — nothing pulls code from a REQ or specs from an item.*</FORCE-MAP-IS-LOAD-BEARING-IN-DAILY-WORK></item>
        <item><CONSEQUENCE-M1-5-CONVERGENCE fact="true" status="impl/done">**M1.5 convergence.** Once `vibe build` generates code from specs, the generator emits specmap edges as a true compiler byproduct — the analogy becomes literal. Hand-authored tags remain as the human-override lane. This PROP defines the format that the future generator will target.</CONSEQUENCE-M1-5-CONVERGENCE></item>
      </list>
    </analogy>
    <runtime-vision title="1.2 The runtime vision (AI-native open source)">
      <p><RUNTIME-EXPOSES-THE-METAMODEL-TO-CONSUMERS fact="true" status="impl/plan">For an open-source project, the metamodel (and, on demand, the source behind it) is exposed to *consumers* of the tool at runtime: an agent driving `vibe` can ask not just `--help` but "why does `vibe install` behave this way, under which decisions, realised where, with which known deviations" — and receive a structured subgraph or a rendered explanation. *Specified, not built (→ B-018): there is no runtime channel. The subgraph and the prose render both exist (`explain_json`, `explain_text`) but are reachable only by running a CLI against a checkout — `specmap_query` / `specmap_explain` / `specmap_source` return no hit anywhere, and the MCP servers that would carry it do not offer it: `core-ai-native-mcp` is a transport that ships no tools at all (its `echo` is a `#[cfg(test)]` fixture, `server.rs`), and the host's `vibe-mcp` `explain` op is PROP-018's README relay, unrelated to this map. The stacks' `trace_explain` is the discipline's per-project channel over a checkout, not this metamodel. An agent driving `vibe` cannot ask the running tool anything.*</RUNTIME-EXPOSES-THE-METAMODEL-TO-CONSUMERS></p>
      <p><DISTRIBUTION-RIDES-THE-EXISTING-REGISTRY fact="true" status="impl/plan">Distribution rides the existing registry: the metamodel index ships with the package; source fragments are fetched by content hash. *Specified, not built (→ B-016): no package ships an index (no `vibe.toml` lists `specmap.json` in a payload), and there is no fetch-by-content-hash path — `content_hash` hashes, it does not retrieve.*</DISTRIBUTION-RIDES-THE-EXISTING-REGISTRY></p>
      <p><CLOSED-SOURCE-PROJECTS-SHIP-A-REDACTED-PROFILE fact="true" status="impl/plan">Closed-source projects ship a redacted profile (§2.8.3). *Specified, not built (→ B-017): the profile has no manifest key, no parser and no redaction path. `[metamodel]` appears in no `vibe.toml`, `conform.toml` or `specmap.toml` in the repository, and no code reads an open/contract/none setting. Nothing is redacted because nothing is exposed at runtime to redact — this fact cannot land before `#RUNTIME-EXPOSES-THE-METAMODEL-TO-CONSUMERS` above it does.*</CLOSED-SOURCE-PROJECTS-SHIP-A-REDACTED-PROFILE></p>
    </runtime-vision>
  </problem>
  <decisions title="2. Decisions">
    <quote><DECISIONS-SECTION-IS-THE-BINDING-MODEL fact="true" status="impl/done">Deliverable (в) of the design brief — the binding model — is this section in its entirety.</DECISIONS-SECTION-IS-THE-BINDING-MODEL></quote>
    <addressing-spec title="2.1 Addressing: spec-side">
      <p><kind-line-addressing-spec fact="true" status="impl/done">`req r1`</kind-line-addressing-spec></p>
      <p><DECISION-EXTEND-THE-SPEC-URI-SCHEME fact="true" status="impl/done">**Decision.** Extend the existing `spec://` URI scheme into the canonical spec-side address:</DECISION-EXTEND-THE-SPEC-URI-SCHEME></p>
      <fence>spec://&lt;package&gt;/&lt;doc-path&gt;#&lt;anchor&gt;            — a spec unit
spec://&lt;package&gt;/&lt;doc-path&gt;#&lt;anchor&gt;~r&lt;N&gt;       — a unit at revision N</fence>
      <list ordered="false">
        <item><URI-PACKAGE-COMPONENT fact="true" status="impl/done">`&lt;package&gt;` is today the repo name (`vibevm`); the grammar reserves group-qualification (`spec://org.vibevm.world/wal/...`) for cross-package tracing per PROP-008, deferred (§7.1).</URI-PACKAGE-COMPONENT></item>
        <item><HEADING-ANCHOR-GRAMMAR-AND-IMMUTABILITY fact="true" status="impl/done">`&lt;anchor&gt;` is the explicit `{#anchor}` on a heading. Its grammar is `[A-Za-z][A-Za-z0-9_-]*` — **one law, shared with fact ids** (owner ruling 2026-07-26; it was kebab-only until then, on no recorded reason beyond «already used by every PROP heading»). Kebab remains the convention every existing anchor follows and the one to keep writing; the wider set exists so a heading anchor and a fact id can never be legal in one position and illegal in the other. **Anchors are immutable once published and never reused.** Renaming a unit keeps its anchor; retiring a unit tombstones the anchor (`&lt;!-- RETIRED: superseded by #new-anchor --&gt;`) rather than deleting it.</HEADING-ANCHOR-GRAMMAR-AND-IMMUTABILITY></item>
        <item><ANCHORS-ARE-CASE-SENSITIVE-AT-EVERY-LEVEL fact="true" status="impl/done">**Anchors are case-SENSITIVE**, at every level: what may be written, duplicate detection, and resolution. `##FOO` and `##foo` are two units; `spec://…#FOO` resolves only to `FOO` and never to `foo`. *Considered and rejected (owner, 2026-07-26): folding case for duplicate detection.* It would flag **29 published pairs across 12 documents** — a section heading `{#kebab-slug}` beside that section's lead normative fact `##KEBAB-SLUG`, which is the convention the two id registers exist for. Byte-exact detection already catches every real duplicate, including `{#FOO}` written beside `##FOO`. **Revisit when:** a case-only collision is observed to mislead a reader in practice.</ANCHORS-ARE-CASE-SENSITIVE-AT-EVERY-LEVEL></item>
        <item><SPEC-UNIT-SPAN-AND-FACT-UNIT-GRAIN fact="true" status="impl/done">A **spec unit** is the span from an anchored heading (or an explicit `REQ` block, §2.2) to the next same-or-higher heading / next unit marker; **fact units** (below) are the sub-heading grain.</SPEC-UNIT-SPAN-AND-FACT-UNIT-GRAIN></item>
        <item><FACT-ANCHORS-MINT-THE-FINEST-GRAIN fact="true" status="impl/done">**Fact anchors (fact amendment, 2026-07-24 — owner-directed).** A `##&lt;ID&gt;`
  written as the **first token of a paragraph or a list item** (at any
  nesting depth, outside fenced/inline code) mints a **fact unit** — the
  finest addressable grain. `&lt;ID&gt;` follows `[A-Za-z][A-Za-z0-9_-]*` and
  carries two registers by convention: `UPPER-SLUG` names a normative
  fact, `kebab-case` a service one (the grammar originates in the host's
  Progress Control amendment — the PROP-043 §3.8 twin of this clause).
  The unit's span is the carrying paragraph or item, continuation lines
  included; an anchored nested item is its own unit. Fact ids share **one
  address space with heading anchors per document** — the same
  `spec://&lt;package&gt;/&lt;doc-path&gt;#&lt;ID&gt;` form cites either, and a duplicate id
  (fact-vs-fact or fact-vs-heading) is an extraction warning, and it is
  **byte-exact** — case distinguishes two addresses, it does not merge them.
  Heading anchors were kebab-only when this clause was written; since the
  owner ruling of 2026-07-26 (§2.1) there is **one grammar for both**, so the
  register is convention rather than a boundary the parser enforces.
  Immutability and tombstoning bind fact anchors exactly as
  heading anchors.</FACT-ANCHORS-MINT-THE-FINEST-GRAIN></item>
        <item><FACT-UNITS-CARRY-NO-KIND-LINE fact="true" status="impl/done">Fact units carry no `kind:` line (§2.2 typing stays a heading-unit
  discipline); their normativity signal is the id register. Edges cite
  them exactly as heading units (`implements`, `verifies`, `documents`),
  so code can bind to a single statement instead of a whole section.</FACT-UNITS-CARRY-NO-KIND-LINE></item>
        <item><MERGE-BEHAVIOUR-IS-OWNED-BY-THE-HOST-CONTRACT fact="true" status="impl/done">**Merge behaviour** — the host spec-compiler contract (its PROP-035
  §7.3 fact-inheritance clause, owner-ratified 2026-07-24) owns it:
  facts follow their section's fate under the contract↔source merge; a
  source fact redeclaring a contract fact's id is a **per-fact
  override**; the merged view re-gates id uniqueness as a build error.
  This PROP only cites that law — one source of truth.</MERGE-BEHAVIOUR-IS-OWNED-BY-THE-HOST-CONTRACT></item>
      </list>
    </addressing-spec>
    <spec-units title="2.2 Spec units, normativity, and the two-tier revision discipline">
      <p><kind-line-spec-units fact="true" status="impl/done">`req r1`</kind-line-spec-units></p>
      <p><DECISION-FOUR-UNIT-KINDS fact="true" status="impl/done">**Decision.** Four unit kinds, each with a different default edge semantics:</DECISION-FOUR-UNIT-KINDS></p>
      <table>
        <tr>
          <td>Kind</td>
          <td>Carries</td>
          <td>Typical edges</td>
        </tr>
        <tr>
          <td><ROW-KIND-PROP fact="true" status="impl/done">`prop`</ROW-KIND-PROP></td>
          <td><fact status="impl/done">a decision + rationale ("why")</fact></td>
          <td><fact status="impl/done">`decides`, referenced by REQs — *Specified, not built: `decides` is not a verb this system can emit. The `Verb` enum in `specmark-grammar` is `Implements · Verifies · Documents · Deviates · Informs`, and its own doctest states the verb set is closed (`Verb::parse("fulfills") == None`); `decides` returns zero hits across every crate. The `prop` kind itself parses fine (`mdspec.rs:78`) — it is the edge verb that has no producer, so a decision can be a node and never the tail of the edge this cell names.* @status:spec/done</fact></td>
        </tr>
        <tr>
          <td><ROW-KIND-REQ fact="true" status="impl/done">`req`</ROW-KIND-REQ></td>
          <td><fact status="impl/done">a normative contract (RFC-2119 MUST/SHOULD/MAY)</fact></td>
          <td><fact status="impl/done">`implements`, `verifies`</fact></td>
        </tr>
        <tr>
          <td><ROW-KIND-DESIGN fact="true" status="impl/done">`design`</ROW-KIND-DESIGN></td>
          <td><fact status="impl/done">shape of a solution ("how", non-binding)</fact></td>
          <td><fact status="impl/done">`informs`</fact></td>
        </tr>
        <tr>
          <td><ROW-KIND-GUIDE fact="true" status="impl/done">`guide`</ROW-KIND-GUIDE></td>
          <td><fact status="impl/done">usage documentation</fact></td>
          <td><fact status="impl/done">`documents`</fact></td>
        </tr>
      </table>
      <p><unit-declares-normativity-with-a-kind-line fact="true" status="impl/done">A unit declares normativity with a one-line marker directly under its heading:</unit-declares-normativity-with-a-kind-line></p>
      <fence lang="markdown">### Conditional dependencies resolve to a fixed point {#req-conditional-fixpoint}
`req r2` — predicates are evaluated against resolved project state; each
pass MUST only add requirements (monotone), guaranteeing convergence.</fence>
      <p><revisions-are-two-tier-lead fact="true" status="impl/done">**Revisions are two-tier:**</revisions-are-two-tier-lead></p>
      <list ordered="false">
        <item><REVISION-R-IS-AN-AUTHOR-ASSERTED-SEMANTIC-BUMP fact="true" status="impl/done">`r&lt;N&gt;` is an **author-asserted semantic revision**. Bump it only when the *meaning* changes. Editorial edits (typos, wording) do not bump.</REVISION-R-IS-AN-AUTHOR-ASSERTED-SEMANTIC-BUMP></item>
        <item><REVISION-CONTENT-HASH-AUDITS-THE-BUMP fact="true" status="impl/done">The indexer computes a **content hash** of the unit text. Hash changed while `r` did not → `vibe trace` warns: *"editorial-or-forgot-to-bump — confirm."* This catches the human failure mode without making typo fixes expensive. (Prior art: OpenFastTrace's `~rev` integers; Doorstop's reviewed-hash stamps — ideas only, see §6.)</REVISION-CONTENT-HASH-AUDITS-THE-BUMP></item>
      </list>
      <p><asymmetric-invalidation-rule-lead fact="true" status="impl/done">**Asymmetric invalidation rule (load-bearing).**</asymmetric-invalidation-rule-lead></p>
      <list ordered="false">
        <item><INVALIDATION-SPEC-BUMP-MAKES-EDGES-SUSPECT fact="true" status="impl/done">Spec unit `r` bumps → every edge pinned to the old `r` becomes **suspect**; CI gate lists them; each is cleared by re-affirming (updating the pin) after review.</INVALIDATION-SPEC-BUMP-MAKES-EDGES-SUSPECT></item>
        <item><INVALIDATION-CODE-CHANGE-KEEPS-EDGES-VALID fact="true" status="spec/done">Code item changes → linked edges stay **valid** (the contract didn't move; implementation detail is free to change). Exception: edges of type `deviates` flip to *review* on either side changing, because a deviation is a statement about both sides. *The rule holds, structurally and by accident; the exception does not. `CodeItem` in the committed index carries `crate_name`, `file`, `item_kind`, `line`, `symbol` and **no content hash**, so a code change is invisible to the edge and cannot invalidate it — the stated behaviour is what the data model can do rather than a decision it enforces. Nothing implements the exception: `deviates` exists as a `Verb` variant with a grammar-mandated reason, and no code on either side treats a deviates edge differently or flips anything to review.*</INVALIDATION-CODE-CHANGE-KEEPS-EDGES-VALID></item>
      </list>
    </spec-units>
    <addressing-code title="2.3 Addressing: code-side — tags that travel">
      <p><kind-line-addressing-code fact="true" status="impl/done">`req r1`</kind-line-addressing-code></p>
      <p><DECISION-CODE-LINKS-ARE-SPECMARK-ATTRIBUTES fact="true" status="impl/done">**Decision.** Code-side links are inert attributes provided by a tiny `specmark` crate (workspace-internal at first; publishable later).</DECISION-CODE-LINKS-ARE-SPECMARK-ATTRIBUTES></p>
      <p><ATTRIBUTE-IS-A-NO-OP-WITH-TWO-CONSUMERS fact="true" status="impl/done">The attribute is a no-op for the compiler — its consumers are (a) the source scanner and (b) rustdoc, into which the macro injects a rendered "Spec:" line so the link is visible in generated docs.</ATTRIBUTE-IS-A-NO-OP-WITH-TWO-CONSUMERS></p>
      <fence lang="rust">use specmark::spec;

/// Parses the `context(&lt;key&gt;)` predicate grammar.
#[spec(implements = "spec://org.vibevm.core/vibevm/modules/vibe-resolver/PROP-003#conditional-deps", r = 2)]
pub enum ConditionalPredicate { /* … */ }

#[spec(deviates = "spec://org.vibevm.core/vibevm/modules/vibe-resolver/PROP-003#conditional-deps", r = 2,
       reason = "boolean composition (`and`/`or`/`not`) intentionally unimplemented; \
                 surfaces as PredicateError::Unsupported pending PROP-014-pilot decision")]
impl ConditionalPredicate {
    pub fn parse(raw: &amp;str) -&gt; Result&lt;Self, PredicateError&gt; { /* … */ }
}

#[cfg(test)]
mod tests {
    use specmark::verifies;

    #[test]
    #[verifies("spec://org.vibevm.core/vibevm/modules/vibe-resolver/PROP-003#conditional-deps", r = 2)]
    fn fixed_point_is_monotone() { /* … */ }
}</fence>
      <p><grammar-one-edge-per-attribute-lead fact="true" status="impl/done">Grammar (one edge per attribute; attributes repeat for multiple edges):</grammar-one-edge-per-attribute-lead></p>
      <fence>#[spec( &lt;verb&gt; = "&lt;spec-uri&gt;" [, r = &lt;N&gt;] [, reason = "&lt;text&gt;"] )]
#[verifies("&lt;spec-uri&gt;" [, r = &lt;N&gt;])]            // sugar for tests
specmark::scope!("&lt;spec-uri&gt;" [, r = &lt;N&gt;]);      // module-level inheritance marker</fence>
      <p><rules-lead fact="true" status="impl/done">Rules:</rules-lead></p>
      <list ordered="false">
        <item><RULE-VERBS-AND-MANDATORY-REASON fact="true" status="impl/done">**Verbs:** `implements`, `verifies`, `documents`, `deviates`, `informs`. `deviates` REQUIRES `reason`.</RULE-VERBS-AND-MANDATORY-REASON></item>
        <item><RULE-IMPLEMENTS-IS-A-CLAIM-ABOUT-CODE-THAT-RUNS fact="true" status="impl/done">**`implements` is a claim about code that RUNS, and a bare declaration does not carry one.** A trait, an abstract signature or any other declared shape that no production code implements gets no `implements` edge, however faithfully it describes the requirement. The edge arrives with the implementation.</RULE-IMPLEMENTS-IS-A-CLAIM-ABOUT-CODE-THAT-RUNS></item>
        <item><RULE-WHY-A-DECLARED-SHAPE-MUST-NOT-CLAIM-COVERAGE fact="true" status="impl/done">**Why, and it is the whole value of the map:** an edge from a declaration is indistinguishable in the index from an edge from working code, so a reader asking «what implements this REQ» is told a shape and hears a build. The perverse gradient is the point — **the more carefully a project declares its shapes ahead of time, the more false coverage it accumulates**, which punishes exactly the discipline this model exists to reward. Measured instance, 2026-08-05: `vibe_settings::events::Watcher` carried the edge with zero implementors in the tree, and `vibe explain` reported two.</RULE-WHY-A-DECLARED-SHAPE-MUST-NOT-CLAIM-COVERAGE></item>
        <item><RULE-DROPPING-THE-EDGE-SILENCES-NOTHING fact="true" status="impl/done">**Dropping the edge does not hide the declaration**, which is the objection to expect: the module's `specmark::scope!` still covers the item for the orphan gate (`##RULE-SCOPE-INHERITANCE`), the requirement keeps whatever honest edges it has from real code, and the docblock is where «this is promised here» belongs. So the choice is not between a false edge and silence — it is between a false edge and an accurate absence.</RULE-DROPPING-THE-EDGE-SILENCES-NOTHING></item>
        <item><RULE-UNIT-OF-CODE-IS-THE-ITEM fact="true" status="impl/done">**Unit of code = the item** (fn, struct, enum, trait, impl block, mod). Never lines, never expressions. Line/column spans appear only in the *derived* index (§2.5), where volatility is harmless because the index is regenerated.</RULE-UNIT-OF-CODE-IS-THE-ITEM></item>
        <item><RULE-SCOPE-INHERITANCE fact="true" status="impl/done">**Scope inheritance.** `specmark::scope!(…)` at the top of a module gives every item inside a default `implements` edge unless the item carries its own `#[spec]` (own tags **replace** the inherited set in v0.1; merge syntax is an open question, §7.2). Private helpers therefore usually need no annotation. Rust note: a true inner attribute (`#![spec(…)]`) on modules is unstable for proc-macros, hence the macro-invocation form.</RULE-SCOPE-INHERITANCE></item>
        <item><RULE-GENERATED-CODE-IS-EXCLUDED fact="true" status="impl/done">**Generated code** (e.g. `vibe-wire/src/generated/`) is excluded from orphan checks via a directory marker; the *generator input* (JTD schema file) is the taggable unit instead. *BUILT 2026-08-05 — both halves are now real. The exclusion half always was: `rscan` and `ratchet` skip a path containing `/generated/`. The designation half was a decision nobody could act on, because no scanner opened a `.json` at all and the edge model hangs an address off a code SYMBOL, which a JSON document has no obvious equivalent of. Both are answered: a schema's units are its ROOT and each `definitions` entry (`symbol` = `&lt;stem&gt;` and `&lt;stem&gt;::&lt;def&gt;`, `item_kind` = `schema` / `schema-def`), and the tag lives in JTD's own `metadata.spec` block as a verb → URI map. The scanner is opt-in per project through `schema_roots`, empty by default. Measured on the host that raised this: seven wire contracts tagged, 16 units and 7 edges in the map, and `vibe explain` now answers «what implements PROP-000 §16» with all seven, by file and line.*</RULE-GENERATED-CODE-IS-EXCLUDED></item>
        <item><RULE-ERROR-ENUMS-ARE-CONTRACT fact="true" status="impl/done">**Error enums are contract.** Every public error variant whose meaning comes from a REQ carries `implements` on the variant's enum (or `#[spec]` on the variant where precision pays). This is what lets a failing command cite the violated requirement (§2.6).</RULE-ERROR-ENUMS-ARE-CONTRACT></item>
        <item><RULE-MULTIPLICITY-LINT fact="true" status="impl/plan">**Multiplicity lint.** An item carrying more than **3** spec edges is flagged by `vibe check`: either the item does too much or the spec units are cut too fine. (Threshold configurable; mirrors the activation-conflict lint philosophy of PROP-003 §2.10.) *Specified, not built (→ B-021): no checker in any layer counts edges per item; `vibe check`'s checks do not include a multiplicity lint.*</RULE-MULTIPLICITY-LINT></item>
      </list>
    </addressing-code>
    <edges title="2.4 The edge model">
      <p><kind-line-edges fact="true" status="impl/done">`req r1`</kind-line-edges></p>
      <p><edge-model-is-a-typed-directed-multigraph-lead fact="true" status="impl/done">A **typed, directed property multigraph**:</edge-model-is-a-typed-directed-multigraph-lead></p>
      <list ordered="false">
        <item><EDGE-MODEL-NODES fact="true" status="impl/plan">**Nodes:** `SpecUnit { uri, kind, r, content_hash }`, `CodeItem { symbol, item_kind, crate_name, file, line }`, plus derived `Command`, `ErrorVariant` views. *Specified, not built (→ B-019): `CodeItem` carries no content hash (see §2.2's own note), and there are no derived `Command` or `ErrorVariant` node views. (`ErrorVariant` exists as a conform **fact** — `conform/src/facts.rs:66` — which is a different graph.)*</EDGE-MODEL-NODES></item>
        <item><EDGE-MODEL-EDGES fact="true" status="spec/done">**Edges:** `(CodeItem) --implements/verifies/documents/deviates/informs--&gt; (SpecUnit @ r)`, each with provenance (`authored` | `generated` | `proposed`) and, for `deviates`, the mandatory reason. *(Brownfield amendment:)* spec units additionally carry a lifecycle status (`planned` | `disputed`; `ratified` is the absent default, `retired` a tombstone), and a `disputed` unit names the other anchor of its pair in a `disputes` field. *Specified, not built: the pairing is a unit field and not yet a spec↔spec `conflicts_with` edge; edges into `disputed` units are not frozen — suspect detection reads only the pinned revision (`index.rs:118-131`); and no coverage math over spec units exists, so `planned` scope is neither reported separately nor penalized.*</EDGE-MODEL-EDGES></item>
        <item><EDGE-MODEL-DIRECTION-OF-AUTHORITY fact="true" status="impl/done">**Direction of authority:** spec → code (the Red Book's top-down flow). The reverse direction is *computed* (the index inverts edges) plus one social channel: a `proposed` edge pool (§4, Phase 2) feeding the Sync-from-Code protocol when code grows meaning the spec lacks.</EDGE-MODEL-DIRECTION-OF-AUTHORITY></item>
      </list>
    </edges>
    <index title="2.5 The index: `specmap.json`">
      <p><kind-line-index fact="true" status="impl/done">`req r1`</kind-line-index></p>
      <p><DECISION-INDEX-IS-A-DERIVED-COMMITTED-ARTEFACT fact="true" status="impl/done">**Decision.** A derived, deterministic, committed artefact, regenerated by `cargo xtask specmap` and gated by `cargo xtask specmap --check` in CI — the exact idiom `check-codegen` already established.</DECISION-INDEX-IS-A-DERIVED-COMMITTED-ARTEFACT></p>
      <list ordered="false">
        <item><INDEX-IS-BUILT-BY-A-SOURCE-SCANNER fact="true" status="impl/done">Built by a source scanner (syn or tree-sitter over the workspace; markdown parser over `spec/**`). No macro expansion needed — `#[spec]` is read as text/AST, which also makes the JS/Python bindings (§2.9) uniform.</INDEX-IS-BUILT-BY-A-SOURCE-SCANNER></item>
        <item><INDEX-IS-CANONICAL-JSON-WITH-A-SCHEMA fact="true" status="impl/done">Canonical JSON (stable ordering), schema under `schemas/specmap.jtd.json` → `vibe-wire` types via the existing codegen pipeline.</INDEX-IS-CANONICAL-JSON-WITH-A-SCHEMA></item>
        <item><INDEX-CONTENTS fact="true" status="impl/done">Contents: all nodes (with content hashes and *current* file:line spans), all edges, plus computed tables: coverage per REQ (`{implemented, verified, documented}` bits), orphans (public items with no edge own-or-inherited), suspects (edges whose pinned `r` &lt; unit's current `r`), unbumped-hash warnings. *Re-measured 2026-08-05, and one third of this note had gone stale. The host's committed index has exactly six keys — `code_items`, `edges`, `schema`, `spec_units`, `suspects`, `warnings` — so nodes, edges, suspects and the warnings table are real (5 652 spec units, 915 code items, 932 edges, 205 warnings; the earlier reading of 5 266 / 898 / 912 / 265 was a different tree). **Coverage per REQ is still absent** and **the orphans table is still absent** — orphan coverage is computed at gate time by the ratchet and deliberately never serialised (`ratchet.rs`, `index.rs`). What is no longer true is the content hash: `code_item` now carries `fingerprint`, a `tok1:&lt;sha256&gt;` over the element's TOKEN STREAM (doc comments count as code, ordinary comments and whitespace do not), and it stands on 915 of 915 items — so the map is no longer blind to a change in the code under a link, and a formatter run does not move it.*</INDEX-CONTENTS></item>
        <item><INDEX-DETERMINISM-IS-A-TESTED-PROPERTY fact="true" status="impl/done">**Determinism is a tested property**, same as the resolver's (PROP-003 §3.3): index twice, assert byte-identical.</INDEX-DETERMINISM-IS-A-TESTED-PROPERTY></item>
      </list>
    </index>
    <queries title="2.6 Query surface">
      <p><kind-line-queries fact="true" status="impl/done">`req r1`</kind-line-queries></p>
      <fence>vibe trace coverage [--crate X] [--kind req]      # matrix: REQ × {impl, test, doc}
vibe trace impact &lt;spec-uri&gt;                      # all items/tests reachable from a unit
vibe trace orphans [--ratchet-file …]             # unjustified public items
vibe trace stale                                  # suspect edges + unbumped-hash warnings
vibe explain &lt;command|symbol|spec-uri&gt; [--json|--text|--prose]</fence>
      <list ordered="false">
        <item><QUERY-OUTPUT-FORMATS fact="true" status="impl/done">`--json` emits the raw subgraph (agent-friendly); `--text` a deterministic structured rendering; `--prose` an LLM rendering of the same subgraph. **The tool MUST be fully useful without an LLM** — `--prose` is a presentation layer, never the data layer.</QUERY-OUTPUT-FORMATS></item>
        <item><QUERY-ERROR-PROVENANCE fact="true" status="impl/plan">**Error provenance:** `vibe`'s error rendering looks up the failing error variant in the index and appends `violates spec://…#req-… (r2) — run: vibe explain &lt;uri&gt;`. This is the single highest-leverage consumer: every failure becomes a doorway into the metamodel. *Specified, not built as an index lookup (→ B-019/B-018): error renderings cite `violates spec://…` from compile-time constants — `core-ai-native-mcp/src/error.rs:25`, `:37`, `:48`, pinned by a doctest, and conform's `req_message` (`rules/mod.rs:49`) — with no revision and no `run: vibe explain` hint. The doorway is real; the lookup is not.*</QUERY-ERROR-PROVENANCE></item>
        <item><QUERIES-LIVE-BEHIND-XTASK-DURING-THE-PILOT fact="true" status="impl/done">During the pilot these live behind `cargo xtask trace …` to avoid touching the CLI surface prematurely; promotion to `vibe trace` / `vibe explain` is a Phase 4 decision (§4).</QUERIES-LIVE-BEHIND-XTASK-DURING-THE-PILOT></item>
      </list>
    </queries>
    <llm-boundary title="2.7 The LLM boundary">
      <p><llm-boundary-continues-prop-003-lead fact="true" status="impl/done">Continuation of PROP-003 §2.5.3's philosophy — *the LLM emits facts and renderings; deterministic machinery decides*:</llm-boundary-continues-prop-003-lead></p>
      <list ordered="true">
        <item><LLM-AS-PROPOSER fact="true" status="impl/done">**LLM as proposer.** Link mining (Phase 2) produces edges with provenance `proposed`, stored in `specmap-proposals.json`, never in code. A human (or an explicitly delegated agent session) *affirms* a proposal by writing the actual `#[spec]` attribute — the affirmation IS the code change, reviewed like any diff.</LLM-AS-PROPOSER></item>
        <item><LLM-AS-RENDERER fact="true" status="impl/plan">**LLM as renderer.** `vibe explain --prose` feeds the subgraph (spec unit texts + rustdoc of linked items + deviation reasons) to the provider behind `vibe-llm`. The subgraph is the ground truth; the prose cites URIs; hallucination risk is bounded by retrieval, and the `--json` form is always available for verification. *Specified, not built (→ B-020): the prose producer is a deterministic template (`ledger.rs:168`) and the crate's own header says an LLM producer slots in later.*</LLM-AS-RENDERER></item>
        <item><LLM-NEVER-SILENTLY-WRITES-EDGES fact="true" status="impl/done">**LLM never silently writes edges, bumps revisions, or clears suspects.** Those are state transitions with audit cost; they pass through diffs.</LLM-NEVER-SILENTLY-WRITES-EDGES></item>
      </list>
    </llm-boundary>
    <runtime title="2.8 Runtime exposure — the AI-native OSS channel">
      <list ordered="true">
        <item><RUNTIME-TRANSPORT fact="true" status="impl/plan">**Transport.** Each stack's discipline MCP server (`rust-ai-native-mcp`, `typescript-ai-native-mcp`, `go-ai-native-mcp`, all built on this package's `core-ai-native-mcp`) exposes `trace_explain(target, {json|prose})` — the subgraph and the prose render — alongside `specmap_check` and `specmap_write`. An agent that drives the stack CLI gets the same via `&lt;lang&gt;-ai-native trace &lt;target&gt; --json`. *Specified, not built (→ B-018): there is no `specmap_source(content_hash) -&gt; fragment` tool and no general `specmap_query`.*</RUNTIME-TRANSPORT></item>
        <item><RUNTIME-DISTRIBUTION fact="true" status="impl/plan">**Distribution.** The index ships inside the published package (it is small); source fragments resolve by content hash against the package's git registry — the content-addressed identity from PROP-002 already guarantees fetch integrity. *Specified, not built (→ B-016): no published package ships an index. No `vibe.toml` in the repository lists `specmap.json` in its payload, and the only `specmap.json` files under `packages/` are `go-extract` / `ts-extract` test fixtures plus one project's own working index. Content-hash fragment resolution does not exist either — `content_hash` computes hashes for the ledger's cache keys, and nothing resolves a source fragment by one. PROP-002's fetch integrity is real and is not this.*</RUNTIME-DISTRIBUTION></item>
        <item><RUNTIME-PROFILES fact="true" status="impl/plan">**Profiles.** `open` (full graph + source), `contract` (spec units + signatures of items, no bodies — the closed-source tier), `none`. Declared in `vibe.toml` `[metamodel] profile = "open"`. *Specified, not built (→ B-017): `[metamodel]` is in no manifest, no schema and no parser; the three profile values have no representation.*</RUNTIME-PROFILES></item>
        <item><RUNTIME-SECURITY-IS-NON-OPTIONAL fact="true" status="spec/done">**Security (non-optional).** The exposed content is *instructions-shaped prose* delivered into a consuming agent's context — a prompt-injection distribution channel by construction. Therefore: (a) the shipped index and fragments are **signed**; consumers verify before use (scheme TBD, §7.6 — sigstore-class is the default candidate); (b) the MCP tool descriptions explicitly frame returned content as *reference data, not instructions*; (c) `vibe check` lints spec units for imperative second-person phrasing outside `guide` kind. This PROP takes the position that the trust layer ships **with** the runtime channel, not after it. *Specified, not built — all three clauses. (a) Nothing is signed: no signing or verification path exists in `vibe-publish`, `vibe-registry` or any engine crate, and this document's own `#OPEN-SIGNING-SCHEME` still calls the scheme undecided and blocking. (b) No MCP tool description carries that framing; the phrase appears nowhere. (c) `vibe check`'s roster has no imperative-phrasing or second-person lint. The position the sentence takes is the right one, and it is why the marker moves rather than the text: the trust layer has **not** shipped, and by this PROP's own standard the runtime channel must not ship until it does — which is consistent with `#RUNTIME-EXPOSES-THE-METAMODEL-TO-CONSUMERS` being unbuilt too.*</RUNTIME-SECURITY-IS-NON-OPTIONAL></item>
      </list>
    </runtime>
    <languages title="2.9 Language neutrality">
      <p><GRAMMAR-IS-LANGUAGE-NEUTRAL fact="true" status="impl/done">The grammar (URIs, verbs, `r`, reasons) is language-neutral; only the carrier syntax is per-language.</GRAMMAR-IS-LANGUAGE-NEUTRAL></p>
      <p><SPEC-SIDE-IS-ONE-SHARED-ENGINE fact="true" status="impl/done">The **spec side is one shared engine** (`core-ai-native-specmap`), so fact-unit extraction (§2.1) reaches every language family — rust, typescript, go — identically through the vendored engine; adding a language never re-implements the spec scanner.</SPEC-SIDE-IS-ONE-SHARED-ENGINE></p>
      <p><RUST-SHIPS-FIRST fact="true" status="impl/done">Rust ships first.</RUST-SHIPS-FIRST></p>
      <p><sketches-normative-later-lead fact="true" status="spec/done">Sketches, normative later:</sketches-normative-later-lead></p>
      <list ordered="false">
        <item><SKETCH-JAVASCRIPT-TYPESCRIPT fact="true" status="spec/done">**JavaScript/TypeScript:** JSDoc carrier — `/** @spec implements spec://… r2 */` on declarations; scanner = tree-sitter.</SKETCH-JAVASCRIPT-TYPESCRIPT></item>
        <item><SKETCH-PYTHON fact="true" status="spec/done">**Python:** decorator `@spec(implements="spec://…", r=2)` from a `specmark` package; module-level `__specmap_scope__ = "spec://…"` (NB: not `__spec__`, which importlib owns).</SKETCH-PYTHON></item>
      </list>
    </languages>
  </decisions>
  <principles title="3. Principles">
    <spec-principles title="3.1 (а) Writing specifications">
      <list ordered="true">
        <item><SPEC-PRINCIPLE-EVERY-STATEMENT-IS-ADDRESSABLE fact="true" status="impl/done">**Every normative statement is addressable.** It lives in a unit with a stable `{#anchor}`; anchors are immutable and never reused; retirement is a tombstone, not a deletion.</SPEC-PRINCIPLE-EVERY-STATEMENT-IS-ADDRESSABLE></item>
        <item><SPEC-PRINCIPLE-ONE-UNIT-ONE-DECISION fact="true" status="impl/done">**One unit, one decision.** If a unit needs "and also", it is two units. The unit is the page of the context-memory hierarchy: it must make sense *alone* when paged into an agent's window.</SPEC-PRINCIPLE-ONE-UNIT-ONE-DECISION></item>
        <item><SPEC-PRINCIPLE-NORMATIVITY-IS-MARKED fact="true" status="impl/done">**Normativity is marked, not implied.** RFC-2119 verbs inside `req` units; everything else is `prop` rationale, `design`, or `guide`. A reader (human or model) must never guess whether a sentence binds.</SPEC-PRINCIPLE-NORMATIVITY-IS-MARKED></item>
        <item><SPEC-PRINCIPLE-NORM-AND-RATIONALE-ARE-SEPARATED fact="true" status="impl/done">**Norm and rationale are separated.** The MUST changes rarely and bumps `r`; the "why" evolves freely without invalidating implementations. PROPs hold rationale; REQs hold contract.</SPEC-PRINCIPLE-NORM-AND-RATIONALE-ARE-SEPARATED></item>
        <item><SPEC-PRINCIPLE-SEMANTIC-EDITS-BUMP-R fact="true" status="impl/done">**Semantic edits bump `r`; editorial edits don't; the hash audits the difference.** Forgetting to bump is detected, not punished.</SPEC-PRINCIPLE-SEMANTIC-EDITS-BUMP-R></item>
        <item><SPEC-PRINCIPLE-SPEC-NEVER-RESTATES-HOW fact="true" status="impl/done">**Spec states *what* and *why* — never restates *how*.** Implementation detail belongs in rustdoc next to the code (where it cannot drift from the code); the metamodel joins the two layers at query time. A spec that mirrors code is shadow code and drift fuel.</SPEC-PRINCIPLE-SPEC-NEVER-RESTATES-HOW></item>
        <item><SPEC-PRINCIPLE-WRITE-TESTABLY fact="true" status="impl/done">**Write testably.** A `req` should imply its verification; if you cannot imagine the `#[verifies]` test, it is `design`, not `req`.</SPEC-PRINCIPLE-WRITE-TESTABLY></item>
        <item><SPEC-PRINCIPLE-DEVIATIONS-ARE-FIRST-CLASS fact="true" status="impl/done">**Deviations are first-class and honest.** When reality intentionally differs, the code says `deviates` + reason — the generalisation of the existing `&lt;!-- REVIEW: … --&gt;` discipline. An undocumented deviation found by audit is a defect.</SPEC-PRINCIPLE-DEVIATIONS-ARE-FIRST-CLASS></item>
        <item><SPEC-PRINCIPLE-CROSS-REFERENCE-BY-URI-ONLY fact="true" status="impl/done">**Cross-reference by URI only.** No "see above", no relative prose pointers — they don't survive paging or reorganisation.</SPEC-PRINCIPLE-CROSS-REFERENCE-BY-URI-ONLY></item>
        <item><SPEC-PRINCIPLE-UNITS-FIT-A-PAGE fact="true" status="impl/plan">**Units fit a page.** Soft target ≤ 120 lines per unit; `vibe check` warns beyond. Long units page badly and hash-churn often. *Specified, not built (→ B-021): no checker warns on spec-unit length; the 120-line figure is a target with no enforcement.*</SPEC-PRINCIPLE-UNITS-FIT-A-PAGE></item>
      </list>
    </spec-principles>
    <rust-principles title="3.2 (б) Writing Rust under specmap">
      <quote><RUST-PRINCIPLES-ARE-NOT-A-GENERAL-STYLE-GUIDE fact="true" status="impl/done">Deliberately *not* a general style guide — only what traceability and the metamodel require. House rules (clippy `-D warnings`, `forbid(unsafe_code)`, etc.) stay where they are.</RUST-PRINCIPLES-ARE-NOT-A-GENERAL-STYLE-GUIDE></quote>
      <list ordered="true">
        <item><RUST-PRINCIPLE-ITEM-IS-THE-UNIT-OF-MEANING fact="true" status="impl/done">**The item is the unit of meaning.** Shape code so each public item serves few spec units (≤ 3 edges; lint beyond). If an item needs more, split the item or merge the units.</RUST-PRINCIPLE-ITEM-IS-THE-UNIT-OF-MEANING></item>
        <item><RUST-PRINCIPLE-TAGS-TRAVEL-WITH-CODE fact="true" status="impl/done">**Tags travel with code.** `#[spec]` on items; `scope!` per module for inheritance; private helpers inherit silently. Moving a function moves its link; that is the entire point.</RUST-PRINCIPLE-TAGS-TRAVEL-WITH-CODE></item>
        <item><RUST-PRINCIPLE-TYPED-VERBS-NO-BARE-LINKS fact="true" status="impl/done">**Typed verbs, no bare links.** `implements` ≠ `documents` ≠ `deviates`; the verb is what makes the graph queryable.</RUST-PRINCIPLE-TYPED-VERBS-NO-BARE-LINKS></item>
        <item><RUST-PRINCIPLE-TESTS-DECLARE-WHAT-THEY-VERIFY fact="true" status="impl/done">**Tests declare what they verify.** `#[verifies(uri, r)]` on the test, not a comment. Coverage = REQ × {impl, test} computed, not estimated.</RUST-PRINCIPLE-TESTS-DECLARE-WHAT-THEY-VERIFY></item>
        <item><RUST-PRINCIPLE-RUSTDOC-IS-THE-DETAIL-LAYER fact="true" status="impl/plan">**Rustdoc is the detail layer.** Every tagged public item's doc comment states the *practically important* behaviour — errors, edge cases, performance traps. `vibe explain` composes spec (contract) + rustdoc (detail); neither duplicates the other. *Specified, not built (→ B-020): `explain` cannot compose rustdoc — `CodeItem` carries no doc field and the renderer emits symbol, kind, crate, file, line and edges only.*</RUST-PRINCIPLE-RUSTDOC-IS-THE-DETAIL-LAYER></item>
        <item><RUST-PRINCIPLE-NO-ORPHAN-PUBLIC-API fact="true" status="impl/done">**No orphan public API.** Every `pub` item is reachable from an edge, own or inherited. Ratcheted: warn → error per crate as migration lands (§4).</RUST-PRINCIPLE-NO-ORPHAN-PUBLIC-API></item>
        <item><RUST-PRINCIPLE-GENERATOR-INPUT-IS-TAGGED fact="true" status="impl/done">**Generated code is excluded; its generator input is tagged.** Schema files and macro definitions carry the edges; expansion output is marked generated.</RUST-PRINCIPLE-GENERATOR-INPUT-IS-TAGGED></item>
        <item><RUST-PRINCIPLE-ERRORS-ARE-CONTRACT-SURFACE fact="true" status="impl/done">**Errors are contract surface.** Public error types/variants that signal a requirement carry its edge, enabling error-message provenance. An error no spec explains is an undocumented behaviour.</RUST-PRINCIPLE-ERRORS-ARE-CONTRACT-SURFACE></item>
      </list>
    </rust-principles>
    <binding-principles title="3.3 (в) Binding principles">
      <p><SECTION-2-IS-THE-DELIVERABLE fact="true" status="impl/done">Section 2 *is* deliverable (в).</SECTION-2-IS-THE-DELIVERABLE></p>
      <p><five-load-bearing-invariants-lead fact="true" status="impl/done">For reading convenience, the five load-bearing invariants:</five-load-bearing-invariants-lead></p>
      <list ordered="false">
        <item><INVARIANT-EDGES-TRAVEL-WITH-ARTEFACTS fact="true" status="impl/done">edges travel with artefacts (§2.3);</INVARIANT-EDGES-TRAVEL-WITH-ARTEFACTS></item>
        <item><INVARIANT-TWO-TIER-REVISIONS fact="true" status="impl/done">two-tier revisions with asymmetric invalidation (§2.2);</INVARIANT-TWO-TIER-REVISIONS></item>
        <item><INVARIANT-DERIVED-INDEX-WITH-A-CI-GATE fact="true" status="impl/done">derived deterministic committed index with a CI gate (§2.5);</INVARIANT-DERIVED-INDEX-WITH-A-CI-GATE></item>
        <item><INVARIANT-TOOL-IS-FUNCTIONAL-WITHOUT-AN-LLM fact="true" status="impl/done">the tool is fully functional without an LLM, and the LLM only proposes and renders (§2.6–2.7);</INVARIANT-TOOL-IS-FUNCTIONAL-WITHOUT-AN-LLM></item>
        <item><INVARIANT-RUNTIME-CHANNEL-SHIPS-SIGNED fact="true" status="impl/done">the runtime channel ships signed or not at all (§2.8.4).</INVARIANT-RUNTIME-CHANNEL-SHIPS-SIGNED></item>
      </list>
    </binding-principles>
  </principles>
  <migration title="4. (г) Migration playbook — transforming vibevm with Claude Code">
    <p><MIGRATION-STRATEGY-EASY-WINS-FIRST fact="true" status="spec/done">**Strategy: easy wins first, ratchet always, never gate the whole repo on day one.**</MIGRATION-STRATEGY-EASY-WINS-FIRST></p>
    <p><maximum-perfection-horizon-is-phase-5-plus fact="true" status="spec/done">(The maximum-perfection horizon — full backfill of all 12 crates, JS/Py bindings, signed runtime channel — is Phase 5+, listed for honesty, not for scheduling.)</maximum-perfection-horizon-is-phase-5-plus></p>
    <phase-0-tooling-skeleton title="Phase 0 — tooling skeleton (≈ half a day)">
      <list ordered="false">
        <item><PHASE-0-SPECMARK-CRATE fact="true" status="spec/done">`crates/specmark/`: the no-op attribute + `scope!` + `verifies` macros (syn parse of the grammar, rustdoc line injection, zero runtime cost).</PHASE-0-SPECMARK-CRATE></item>
        <item><PHASE-0-XTASK-SPECMAP-SUBCOMMAND fact="true" status="spec/done">`xtask specmap` subcommand: markdown unit parser + syn-based item scanner + canonical JSON emitter; `--check` mode (regenerate-and-diff, the `check-codegen` idiom).</PHASE-0-XTASK-SPECMAP-SUBCOMMAND></item>
        <item><PHASE-0-SCHEMA-AND-CODEGEN fact="true" status="spec/done">`schemas/specmap.jtd.json` + codegen.</PHASE-0-SCHEMA-AND-CODEGEN></item>
        <item><PHASE-0-ACCEPTANCE fact="true" status="spec/done">Acceptance: index builds deterministically twice on the untouched repo (zero edges, full node inventory); CI job wired but non-blocking.</PHASE-0-ACCEPTANCE></item>
      </list>
    </phase-0-tooling-skeleton>
    <phase-1-pilot title="Phase 1 — pilot: PROP-003 §2.6.1 × `vibe-resolver/src/conditional.rs`">
      <p><phase-1-is-the-smallest-real-loop fact="true" status="spec/done">The smallest real loop, chosen deliberately: fresh spec, ~130-line module, and it carries a live design question (the `not`/monotonicity issue) that becomes the first officially traceable REQ with a recorded `deviates`.</phase-1-is-the-smallest-real-loop></p>
      <list ordered="true">
        <item><PHASE-1-UNIT-IFY-THE-SPEC fact="true" status="spec/done">Unit-ify §2.6.1: add `req` markers + anchors for (i) the fixed-point/monotonicity contract, (ii) the predicate grammar, (iii) the host-invariance rule. *Anchors added, no prose rewritten* — additions only, owner-frozen text untouched pending sign-off.</PHASE-1-UNIT-IFY-THE-SPEC></item>
        <item><PHASE-1-TAG-CONDITIONAL-RS fact="true" status="spec/done">Tag `conditional.rs`: `implements` on the enum and `parse`, `deviates` (+ reason) for unimplemented boolean composition, `verifies` on its tests.</PHASE-1-TAG-CONDITIONAL-RS></item>
        <item><PHASE-1-DRIFT-DRILL fact="true" status="spec/done">**Drift drill (the acceptance that matters):** semantically edit the fixed-point unit, bump `r` → gate flags the suspect edges; re-affirm → gate clears. Then edit a typo *without* bumping → hash warning fires. Both behaviours demonstrated in one PR description.</PHASE-1-DRIFT-DRILL></item>
        <item><PHASE-1-TRACE-EXPLAIN-EMITS-A-SUBGRAPH fact="true" status="spec/done">`xtask trace explain conditional::ConditionalPredicate::parse --text` emits a correct subgraph.</PHASE-1-TRACE-EXPLAIN-EMITS-A-SUBGRAPH></item>
      </list>
    </phase-1-pilot>
    <phase-2-backfill title="Phase 2 — backfill `vibe-resolver` with Claude Code">
      <p><phase-2-two-link-sources-lead fact="true" status="spec/done">Two link sources, both flowing through the `proposed` pool (§2.7), affirmed by diff review:</phase-2-two-link-sources-lead></p>
      <p><PHASE-2-LATENT-CORPUS-MINING fact="true" status="spec/done">**(a) Latent corpus mining** — the repo already cites `spec://` in commit bodies (Rule 2):</PHASE-2-LATENT-CORPUS-MINING></p>
      <fence>git log --all --pretty='%H %s' --grep='spec://' | …</fence>
      <p><PHASE-2-MINED-TRIPLES-SEED-PROPOSED-EDGES fact="true" status="spec/done">Each (commit → files touched → URIs cited) triple seeds proposed edges with evidence pointers.</PHASE-2-MINED-TRIPLES-SEED-PROPOSED-EDGES></p>
      <p><PHASE-2-CRATE-SWEEP-PROMPT fact="true" status="spec/done">**(b) Crate sweep.** Claude Code prompt (guardrails included):</PHASE-2-CRATE-SWEEP-PROMPT></p>
      <fence lang="text">Read spec/modules/vibe-resolver/*.md and crates/vibe-resolver/src/.
For every public item, propose at most 3 specmap edges using the
PROP-014 §2.3 grammar. For each proposal output: item path, verb,
spec URI + r, a one-line evidence quote from BOTH sides, and a
confidence (high/medium/low). Do NOT edit any file. Do NOT propose
edges where you cannot quote evidence from the spec side — mark the
item "candidate orphan" instead. Emit specmap-proposals.json only.</fence>
      <p><phase-2-affirmation-session-prompt-lead fact="true" status="spec/done">Affirmation session prompt:</phase-2-affirmation-session-prompt-lead></p>
      <fence lang="text">Take specmap-proposals.json entries marked APPROVED in the review
file. Write the corresponding #[spec]/#[verifies]/scope! annotations.
One commit per module, Conventional Commits, body citing the spec://
URIs added. Run `cargo xtask specmap --check` and `cargo test -p
vibe-resolver` before each commit. Touch nothing outside
crates/vibe-resolver and the proposals file.</fence>
      <list ordered="false">
        <item><PHASE-2-ACCEPTANCE fact="true" status="spec/done">Acceptance: `vibe-resolver` coverage report ≥ 90% of `req` units implemented-and-verified; orphan list for the crate empty or dispositioned in AUDIT; gate flipped to blocking *for this crate only* (the ratchet file lists exempt crates).</PHASE-2-ACCEPTANCE></item>
      </list>
    </phase-2-backfill>
    <phase-3-expansion-and-metrics title="Phase 3 — expansion + metrics">
      <list ordered="false">
        <item><PHASE-3-CRATE-BY-CRATE fact="true" status="spec/done">Crate-by-crate (suggested order: `vibe-core` → `vibe-install` → `vibe-registry` → CLI last), each flipping its ratchet entry.</PHASE-3-CRATE-BY-CRATE></item>
        <item><PHASE-3-INSTRUMENT-THE-ECONOMICS fact="true" status="spec/done">**Instrument the economics** — the empirical answer to "will this rot": stale-edge half-life after a normal refactor week; proposals-to-affirmation lag; % of PRs touching tagged items that also touch their pins. Targets set after two weeks of data, recorded in AUDIT.</PHASE-3-INSTRUMENT-THE-ECONOMICS></item>
      </list>
    </phase-3-expansion-and-metrics>
    <phase-4-surfaces title="Phase 4 — surfaces">
      <list ordered="false">
        <item><PHASE-4-PROMOTE-XTASK-TO-VIBE fact="true" status="spec/done">Promote `xtask trace`/`explain` → `vibe trace` / `vibe explain` (`--json/--text/--prose`).</PHASE-4-PROMOTE-XTASK-TO-VIBE></item>
        <item><PHASE-4-ERROR-PROVENANCE-WIRING fact="true" status="spec/done">Error provenance wiring in `vibe-cli` error rendering.</PHASE-4-ERROR-PROVENANCE-WIRING></item>
        <item><PHASE-4-MCP-TOOLS-BLOCKED-ON-SIGNING fact="true" status="spec/done">`vibe-mcp` tools per §2.8 — **blocked on the signing decision (§7.6)**; ships signed or not at all.</PHASE-4-MCP-TOOLS-BLOCKED-ON-SIGNING></item>
      </list>
    </phase-4-surfaces>
  </migration>
  <rejected title="5. Rejected alternatives">
    <list ordered="true">
      <item><rejected-external-sidecar-map-only fact="true" status="spec/done">**External sidecar map only** (a `specmap.toml` maintained by hand or by tool, no in-source tags). Rots immediately: without a compiler regenerating it, every refactor silently invalidates spans and symbol paths. Kept only as the *derived* index (§2.5), where regeneration is the lifecycle.</rejected-external-sidecar-map-only></item>
      <item><rejected-line-range-anchors fact="true" status="spec/done">**Line/range anchors** ("PROP-003 lines 410–462", `src/naive.rs:118-160`). Maximally precise and maximally fragile; every upstream edit shifts them. Spans are demoted to derived-index decoration.</rejected-line-range-anchors></item>
      <item><rejected-embedding-similarity-as-ground-truth fact="true" status="spec/done">**Embedding-similarity recovered links as ground truth.** Non-deterministic core, unexplainable diffs, silent drift. Allowed exactly once in the lifecycle: as a *proposer* in Phase 2, behind human affirmation.</rejected-embedding-similarity-as-ground-truth></item>
      <item><rejected-literate-programming fact="true" status="spec/done">**Literate programming / tangle** (spec is the single source; code is extracted). Inverts authority correctly but destroys the entire Rust toolchain experience (rust-analyzer, incremental compile, grep-ability) and forces the spec to carry *how*. The Red Book's layer model (spec=meaning, code=detail) is the opposite bet, deliberately.</rejected-literate-programming></item>
      <item><rejected-external-requirements-database fact="true" status="spec/done">**External requirements database** (DOORS/Doorstop-style items outside the repo). Violates "project facts live in the repo" (CLAUDE.md memory discipline) and splits the review surface. Everything here is files in git — the book's ch. 2 thesis.</rejected-external-requirements-database></item>
      <item><rejected-full-formal-specification fact="true" status="spec/done">**Full formal specification** (TLA+/Kani/Dafny for the contracts). Wrong genre for prose contracts and process disciplines; *complementary* for isolated algorithmic kernels — the conditional-deps fixed point is a natural first candidate if we ever want a machine-checked model, and the specmap edge type for it would be `verifies`.</rejected-full-formal-specification></item>
    </list>
  </rejected>
  <prior-art title="6. Prior art and license posture">
    <p><CONVENTIONS-ARE-FREE-CODE-IS-NOT fact="true" status="impl/done">Conventions and ideas are free; code is not.</CONVENTIONS-ARE-FREE-CODE-IS-NOT></p>
    <p><ROLES-ARE-EXPLICIT-PER-PROP-000 fact="true" status="impl/done">Per PROP-000 §3 (permissive only; GPL/AGPL/LGPL forbidden as dependencies), roles below are explicit.</ROLES-ARE-EXPLICIT-PER-PROP-000></p>
    <p><LICENSE-FIELDS-ARE-RE-VERIFIED-BEFORE-REUSE fact="true" status="impl/done">License fields to be re-verified before any code-level reuse.</LICENSE-FIELDS-ARE-RE-VERIFIED-BEFORE-REUSE></p>
    <table>
      <tr>
        <td>System</td>
        <td>License (verify)</td>
        <td>Role here</td>
      </tr>
      <tr>
        <td><ROW-PRIOR-ART-OPENFASTTRACE fact="true" status="spec/done">OpenFastTrace</ROW-PRIOR-ART-OPENFASTTRACE></td>
        <td><fact status="spec/done">GPL-3.0</fact></td>
        <td><fact status="spec/done">**Study only.** Borrowed *ideas*: artifact-type chains (req→dsn→impl→utest), `~rev` semantics, coverage states. No code, no linkage.</fact></td>
      </tr>
      <tr>
        <td><ROW-PRIOR-ART-STRICTDOC fact="true" status="spec/done">strictdoc</ROW-PRIOR-ART-STRICTDOC></td>
        <td><fact status="spec/done">Apache-2.0</fact></td>
        <td><fact status="spec/done">Friendly. Grammar/UI patterns for requirement documents.</fact></td>
      </tr>
      <tr>
        <td><ROW-PRIOR-ART-DOORSTOP fact="true" status="spec/done">Doorstop</ROW-PRIOR-ART-DOORSTOP></td>
        <td><fact status="spec/done">LGPL-3.0</fact></td>
        <td><fact status="spec/done">Wrapper-zone per policy if ever executed; borrowed *idea*: reviewed-hash stamps (our two-tier revisions).</fact></td>
      </tr>
      <tr>
        <td><ROW-PRIOR-ART-SPHINX-NEEDS fact="true" status="spec/done">Sphinx-needs</ROW-PRIOR-ART-SPHINX-NEEDS></td>
        <td><fact status="spec/done">MIT</fact></td>
        <td><fact status="spec/done">Friendly. Typed needs/links, filter queries.</fact></td>
      </tr>
      <tr>
        <td><ROW-PRIOR-ART-DO-178C fact="true" status="spec/done">DO-178C / DOORS culture</ROW-PRIOR-ART-DO-178C></td>
        <td><fact status="spec/done">n/a (standards)</fact></td>
        <td><fact status="spec/done">The cautionary tale §1.1 is built on: traceability that is audited but not load-bearing dies.</fact></td>
      </tr>
      <tr>
        <td><ROW-PRIOR-ART-SOURCE-MAPS fact="true" status="spec/done">JS source maps / DWARF</ROW-PRIOR-ART-SOURCE-MAPS></td>
        <td><fact status="spec/done">n/a</fact></td>
        <td><fact status="spec/done">The analogy and its precise failure point (§1.1).</fact></td>
      </tr>
      <tr>
        <td><ROW-PRIOR-ART-SYN-TREE-SITTER fact="true" status="spec/done">syn / tree-sitter *(`syn` is the live scanner dependency — `Cargo.toml:44`; tree-sitter is in no manifest in this repository.)*</ROW-PRIOR-ART-SYN-TREE-SITTER></td>
        <td><fact status="spec/done">MIT/Apache-2.0; MIT</fact></td>
        <td><fact status="spec/done">Implementation dependencies for the scanner.</fact></td>
      </tr>
      <tr>
        <td><ROW-PRIOR-ART-SIGSTORE fact="true" status="spec/done">sigstore</ROW-PRIOR-ART-SIGSTORE></td>
        <td><fact status="spec/done">Apache-2.0</fact></td>
        <td><fact status="spec/done">Default candidate for §2.8.4 signing.</fact></td>
      </tr>
    </table>
    <p><differentiators-lead fact="true" status="impl/done">**Differentiators vs. classical requirements traceability:**</differentiators-lead></p>
    <list ordered="false">
      <item><DIFFERENTIATOR-CONSUMED-AT-RUNTIME fact="true" status="impl/done">(i) the map is consumed at *runtime by agents using the tool*, not only at audit time;</DIFFERENTIATOR-CONSUMED-AT-RUNTIME></item>
      <item><DIFFERENTIATOR-LLM-PARTICIPATES-BEHIND-A-DETERMINISTIC-CORE fact="true" status="impl/done">(ii) an LLM participates — strictly as proposer and renderer behind a deterministic core;</DIFFERENTIATOR-LLM-PARTICIPATES-BEHIND-A-DETERMINISTIC-CORE></item>
      <item><DIFFERENTIATOR-DOUBLES-AS-THE-CONTEXT-PAGING-TABLE fact="true" status="impl/done">(iii) the map doubles as the context-paging table for agent sessions (PROP-009's intra-project counterpart);</DIFFERENTIATOR-DOUBLES-AS-THE-CONTEXT-PAGING-TABLE></item>
      <item><DIFFERENTIATOR-SPECS-ARE-PACKAGE-DISTRIBUTED fact="true" status="impl/done">(iv) specs are package-distributed artefacts (vibevm itself), so tracing composes across the registry.</DIFFERENTIATOR-SPECS-ARE-PACKAGE-DISTRIBUTED></item>
    </list>
  </prior-art>
  <open title="7. Open questions">
    <list ordered="true">
      <item><OPEN-CROSS-PACKAGE-URIS fact="true" status="spec/done">**Cross-package URIs.** Group-qualified `spec://org.vibevm.world/wal/...` grammar and resolution against installed packages — after PROP-008 settles live.</OPEN-CROSS-PACKAGE-URIS></item>
      <item><OPEN-INHERITANCE-MERGE fact="true" status="spec/done">**Inheritance merge.** v0.1: item tags replace `scope!` defaults. Is a `+implements` extend form needed? Decide on Phase 2 evidence.</OPEN-INHERITANCE-MERGE></item>
      <item><OPEN-UNIT-MOVES-ACROSS-DOCUMENTS fact="true" status="spec/done">**Unit moves across documents.** Anchor immutability covers renames-in-place; moving a unit between files needs either URI redirect stubs (PROP-012 flavour) or doc-path-free unit IDs. Lean: redirect stubs.</OPEN-UNIT-MOVES-ACROSS-DOCUMENTS></item>
      <item><OPEN-EXPLANATION-CACHING fact="true" status="spec/done">**Explanation caching.** `--prose` renderings keyed by (subgraph hash, model id) — where cached, when invalidated.</OPEN-EXPLANATION-CACHING></item>
      <item><OPEN-THRESHOLDS fact="true" status="spec/done">**Thresholds.** 3 edges/item, 120 lines/unit — placeholders until Phase 3 metrics.</OPEN-THRESHOLDS></item>
      <item><OPEN-SIGNING-SCHEME fact="true" status="spec/done">**Signing scheme.** sigstore vs. minisign-class vs. registry-native git signatures; decide before Phase 4's MCP exposure; blocking for §2.8.</OPEN-SIGNING-SCHEME></item>
      <item><OPEN-NON-OSS-CONTRACT-PROFILE fact="true" status="spec/done">**Non-OSS `contract` profile.** Exactly which item metadata (signatures? doc comments?) is safe to ship; needs a real closed-source consumer to decide.</OPEN-NON-OSS-CONTRACT-PROFILE></item>
      <item><OPEN-COMMIT-MESSAGE-INTEGRATION fact="true" status="spec/done">**Commit-message integration.** Rule 2 already cites `spec://`; should commits citing a REQ auto-link into the index as `informs` provenance? Cheap, probably yes; confirm noise level on Phase 2 history.</OPEN-COMMIT-MESSAGE-INTEGRATION></item>
    </list>
    <p><this-prop-is-a-design-proposal fact="true" status="impl/done">*This PROP is a design proposal.*</this-prop-is-a-design-proposal></p>
    <p><RATIFICATION-HAPPENS-THROUGH-PR-REVIEW fact="true" status="impl/done">*Ratification — and the `specmark`/xtask implementation start — happens through PR review against this document.*</RATIFICATION-HAPPENS-THROUGH-PR-REVIEW></p>
    <p><UNEXERCISED-MECHANISM-IS-REMOVED-FROM-THE-SPEC fact="true" status="impl/done">*Any mechanism specified here that is not exercised by the end of Phase 2 is either removed from the spec or annotated in place as **specified, not built** — never carried as unmarked aspiration.*</UNEXERCISED-MECHANISM-IS-REMOVED-FROM-THE-SPEC></p>
  </open>
</spec>
