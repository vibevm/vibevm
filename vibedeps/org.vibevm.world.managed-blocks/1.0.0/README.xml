<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">`flow:managed-blocks` — write into files you do not own</title>
  <status stage="doc" state="done" audience="user"/>
  <p><fact id="PACKAGE-INSTALLS-THE-MANAGED-BLOCKS-DISCIPLINE" status="impl/done">A `flow` package that installs one discipline: **how a tool writes
into a file it does not own** — an agent-instruction file, a shell rc,
an ssh config, a shared project config — without destroying what the
other tenants put there.</fact></p>
  <p><fact id="FIRST-INSTALL-INTO-A-NON-TRIVIAL-HOST-FILE-IS-A-DATA-LOSS-EVENT" status="spec/done">The first install of a whole-file-overwriting
tool into a project with a non-trivial host file is a data-loss event;
this practice is how it stops being one.</fact></p>
  <p><fact id="AUDIENCE-IS-TOOL-AUTHORS" status="impl/done">**Audience: tool authors.** Anyone whose software writes into files it
does not solely own.</fact></p>
  <p><fact id="IF-YOUR-TOOL-WRITES-A-FILE-OTHERS-EDIT-THIS-IS-FOR-YOU" status="impl/done">If your tool `write()`s a file a human or another
tool also edits, this is for you.</fact></p>
  <p><fact id="the-law-fits-on-one-line" status="impl/done">The law fits on one line:</fact></p>
  <fence>Own exactly one delimited block; never touch a byte outside it.</fence>
  <p><fact id="package-contents-lead" status="impl/done">This package ships three pieces of content plus a boot snippet:</fact></p>
  <list ordered="false">
    <item><fact id="CONTENT-THE-PROTOCOL" status="impl/done">`spec/flows/managed-blocks/MANAGED-BLOCKS-PROTOCOL.md` — the full
  protocol: the co-tenant law, marker design (unique, greppable,
  paired, self-documenting, versioned), the absent / present /
  malformed state machine, the three verbs (create / update / remove),
  plan-time classification, the byte-identical no-op, and multi-tool
  cohabitation.</fact></item>
    <item><fact id="CONTENT-THE-REJECTED-DESIGNS" status="impl/done">`spec/flows/managed-blocks/rejected-designs.md` — four designs that
  look reasonable and are wrong (sidecar, model-based detection,
  auto-repair, whole-file ownership), each with its full why, plus the
  malformed-state hard-stop drill.</fact></item>
    <item><fact id="CONTENT-THE-ADOPTION-GUIDE" status="impl/done">`spec/flows/managed-blocks/adoption-guide.md` — migrating an
  overwriting tool onto a block, a fixture table that pins the state
  machine, and what belongs inside the block versus a tool-owned file
  it points at.</fact></item>
    <item><fact id="CONTENT-THE-BOOT-SNIPPET" status="impl/done">`spec/boot/65-flow-managed-blocks.md` — boot snippet loaded at
  session start: the one-line law, when to read the protocol, and the
  never-do list.</fact></item>
  </list>
  <section id="install" title="Install">
    <fence lang="bash">vibe install flow:managed-blocks</fence>
  </section>
  <section id="uninstall" title="Uninstall">
    <fence lang="bash">vibe uninstall flow:managed-blocks</fence>
    <p><fact id="UNINSTALL-REMOVES-EVERY-FILE-THE-PACKAGE-WROTE" status="impl/done">Uninstalling removes every file the package wrote, including the boot
snippet.</fact></p>
    <p><fact id="USER-OWNED-FILES-ARE-NEVER-TOUCHED" status="impl/done">User-owned files are never touched — which is, fittingly, the
whole point of the practice.</fact></p>
  </section>
  <section id="composition" title="Composition">
    <list ordered="false">
      <item><fact id="COMPOSES-TOOL-DESIGN-LESSONS" status="impl/done">`flow:tool-design-lessons` — the sibling package for tool authors;
  managed blocks is one deep-dive lesson from it, extracted so it can
  be installed on its own.</fact></item>
      <item><fact id="COMPOSES-ATTRIBUTION-POLICY-AND-WAL" status="impl/done">`flow:git-attribution-policy` and `flow:wal` — their instruction files
  are exactly the kind of shared, hand-authored file this protocol
  protects. A tool that writes an attribution snippet or a WAL redirect
  into `CLAUDE.md` must do it as a block, not an overwrite.</fact></item>
      <item><fact id="COMPOSES-CONFLICT-PROTOCOL" status="impl/done">`flow:conflict-protocol` — a malformed block is a conflict surfaced
  to the human, never silently resolved; both practices refuse to guess
  when two writers disagree, and both hard-stop instead of auto-fixing.</fact></item>
    </list>
  </section>
  <section id="background" title="Philosophical background">
    <p><fact id="practice-crystallized-from-the-origin-projects-law" status="spec/done">The practice is crystallized from the origin project's
managed-redirect-block law — the rule that a tool owns one delimited,
machine-findable region of a shared file and never touches a byte
outside it, with a hard stop on any malformed state.</fact></p>
    <p><fact id="ORIGIN-MARKERS-ARE-ONE-EXAMPLE-AND-THE-RULE-IS-GENERIC" status="impl/done">The origin's own
markers (`&lt;vibevm&gt;`) and host file (`CLAUDE.md`) appear here only as
one worked example among others (shell rc, ssh config); the rule is
generic to any tool and any shared file.</fact></p>
    <p><fact id="collections-spirit-is-the-redbook" status="spec/done">The collection's spirit is the
book *AI-native development*, which ships in Russian inside
`flow:redbook` at `spec/book/ru/`.</fact></p>
  </section>
  <section id="license" title="License">
    <p><fact id="license-line" status="impl/done">UPL-1.0. See `LICENSE.md`.</fact></p>
  </section>
</spec>
