<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Flow: Managed Blocks</title>
  <status stage="impl" state="done"/>
  <p><fact id="PROJECT-SHIPS-THE-MANAGED-BLOCKS-PRACTICE" status="impl/done">This project ships the **managed blocks** practice — a discipline
for tool authors: how a tool writes into a file it does not own (an
agent-instruction file, a shell rc, an ssh config, a shared project
config) without destroying what the other tenants wrote.</fact></p>
  <p><fact id="the-law-fits-on-one-line" status="impl/done">The law
fits on one line:</fact></p>
  <fence>Own exactly one delimited block; never touch a byte outside it.</fence>
  <section id="when" title="When to read the protocol">
    <p><fact id="READ-THE-PROTOCOL-BEFORE-WRITING-INTO-A-SHARED-FILE" status="impl/done">**Before** designing or reviewing any feature that writes into a
file the tool does not fully own, read
@spec://org.vibevm.world/managed-blocks/flows/managed-blocks/MANAGED-BLOCKS-PROTOCOL#root
first: marker design, the absent / present / malformed state
machine, the three verbs (create / update / remove), plan-time
classification.</fact></p>
    <p><fact id="shortcuts-are-already-catalogued" status="impl/done">The shortcuts you are tempted by — a sidecar file, a
smart detector, auto-repair, "just regenerate the file" — are
already catalogued with their failure modes in
@spec://org.vibevm.world/managed-blocks/flows/managed-blocks/rejected-designs#root.</fact></p>
    <p><fact id="adoption-guide-pointer" status="impl/done">Migrating an existing overwriting tool, the fixture table for the
state machine, and what belongs inside the block:
@spec://org.vibevm.world/managed-blocks/flows/managed-blocks/adoption-guide#root.</fact></p>
  </section>
  <section id="never" title="Never">
    <list ordered="false">
      <item><fact id="NEVER-WRITE-OUTSIDE-YOUR-OWN-BLOCK" status="impl/done">Never write outside your own block. Every byte beyond your markers
  is another tenant's property.</fact></item>
      <item><fact id="NEVER-GATE-A-DESTRUCTIVE-WRITE-ON-A-NONDETERMINISTIC-DETECTOR" status="impl/done">Never gate a destructive write on a nondeterministic detector —
  the block is found by a deterministic byte scan or not at all.</fact></item>
      <item><fact id="NEVER-AUTO-REPAIR-A-MALFORMED-BLOCK" status="impl/done">Never auto-repair a malformed block. Hard stop, precise report;
  the human decides.</fact></item>
      <item><fact id="NEVER-REWRITE-A-FILE-WHEN-THE-RESULT-IS-BYTE-IDENTICAL" status="impl/done">Never rewrite a file when the result is byte-identical.</fact></item>
    </list>
  </section>
</spec>
