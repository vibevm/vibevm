<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">`flow:git-attribution-policy` — choose your authorship surface deliberately</title>
  <status stage="doc" state="done" audience="user"/>
  <p><PACKAGE-INSTALLS-A-DELIBERATE-ATTRIBUTION-POSTURE fact="true" status="impl/done">A vibevm `flow` package that installs a **deliberate attribution
posture** for an AI-assisted repository.</PACKAGE-INSTALLS-A-DELIBERATE-ATTRIBUTION-POSTURE></p>
  <p><agents-self-attribute-and-an-undecided-project-drifts fact="true" status="spec/done">Coding agents attribute
themselves by default — trailers in commits, "generated by" lines —
and a project that never decides ends up with an accidental,
inconsistent record: neither a clean human-authored surface nor a
reliable provenance trail.</agents-self-attribute-and-an-undecided-project-drifts></p>
  <p><PACKAGE-MAKES-THE-CHOICE-EXPLICIT-AND-ENFORCED fact="true" status="spec/done">This package makes the choice explicit
and enforced. *Explicit ships; enforced does not. `vibe.toml` declares one
`[boot_snippet]` and no executable, so an install writes the snippet and three
documents; the enforcement is `enforcement-checklist.xml`'s pre-push scan — a
fenced shell snippet each project must wire for itself — beside
tool-configuration advice and a periodic-audit line. Searched the whole
consuming tree (`packages/`, `vibedeps/`, `crates/`, `xtask/`, `tools/`,
`spec/`, `discipline/`, `terraform/`, `campaigns/`, `legacy-spec/`, root
scripts) and by name for `.git/hooks/`, `.githooks/`, `.github/` and
`core.hooksPath`: no hook, no CI file, no trailer setting. By this flow's own
law that a policy with no checker is a wish, the «enforced» half is a wish. The
half that does ship is the configuration the checklist calls cheaper than
catching — the boot snippet as the agent's standing instruction — and on this
consumer it holds: zero attribution trailers and one author across 400
commits.*</PACKAGE-MAKES-THE-CHOICE-EXPLICIT-AND-ENFORCED></p>
  <p><THE-DEFAULT-POSTURE-IS-CONCEALMENT fact="true" status="impl/done">**The default posture is concealment**: the repository presents a
human-authored surface on every artifact.</THE-DEFAULT-POSTURE-IS-CONCEALMENT></p>
  <p><the-honest-rationale-ships-with-the-rule fact="true" status="spec/done">The honest rationale ships
with the rule — some jurisdictions regulate or may regulate machine
authorship of software, and an owner not subject to such regulation
may proactively remove the hook.</the-honest-rationale-ships-with-the-rule></p>
  <p><DISCLOSURE-IS-A-FIRST-CLASS-ALTERNATIVE fact="true" status="spec/done">The open-disclosure posture is
documented as a first-class alternative, and is the *mandatory*
choice where law, employer, or upstream projects require disclosure.</DISCLOSURE-IS-A-FIRST-CLASS-ALTERNATIVE></p>
  <p><VERIFY-BOTH-BEFORE-ADOPTING-THE-DEFAULT fact="true" status="impl/done">Verify both before adopting the default; the protocol tells you how.</VERIFY-BOTH-BEFORE-ADOPTING-THE-DEFAULT></p>
  <p><package-contents-lead fact="true" status="impl/done">This package ships three pieces of content plus a boot snippet:</package-contents-lead></p>
  <facts ordered="false">
    <CONTENT-THE-FULL-POLICY fact="true" status="impl/done">`spec/flows/attribution-policy/ATTRIBUTION-POLICY.xml` — the full
  policy: what attribution governs, the concealment posture and its
  honest rationale, the single-place law, how to instruct an agent,
  and the slip drill.</CONTENT-THE-FULL-POLICY>
    <CONTENT-THE-DISCLOSURE-ALTERNATIVE fact="true" status="impl/done">`spec/flows/attribution-policy/disclosure-alternative.xml` — the
  open posture: when it is mandatory, when it is the better fit,
  what it costs, and how to switch postures forward-only.</CONTENT-THE-DISCLOSURE-ALTERNATIVE>
    <CONTENT-THE-ENFORCEMENT-CHECKLIST fact="true" status="impl/done">`spec/flows/attribution-policy/enforcement-checklist.xml` — the
  eight leak surfaces, a pre-push scan, tool configuration, and the
  periodic audit line.</CONTENT-THE-ENFORCEMENT-CHECKLIST>
    <CONTENT-THE-BOOT-SNIPPET fact="true" status="impl/done">`spec/boot/55-flow-attribution-policy.xml` — boot snippet loaded at
  session start: the rule, the honest reason, the scope carve-outs,
  and the never-do list. Under the single-place law, this snippet is
  the one place in a consuming project where the topic lives.</CONTENT-THE-BOOT-SNIPPET>
  </facts>
  <install title="Install">
    <fence lang="bash">vibe install flow:git-attribution-policy</fence>
  </install>
  <uninstall title="Uninstall">
    <fence lang="bash">vibe uninstall flow:git-attribution-policy</fence>
    <p><UNINSTALL-REMOVES-EVERY-FILE-THE-PACKAGE-WROTE fact="true" status="impl/done">Uninstalling removes every file the package wrote, including the
boot snippet.</UNINSTALL-REMOVES-EVERY-FILE-THE-PACKAGE-WROTE></p>
    <p><USER-OWNED-FILES-ARE-NEVER-TOUCHED fact="true" status="impl/done">User-owned files are never touched.</USER-OWNED-FILES-ARE-NEVER-TOUCHED></p>
  </uninstall>
  <composition title="Composition">
    <facts ordered="false">
      <COMPOSES-ATOMIC-COMMITS fact="true" status="impl/done">`flow:git-atomic-commits` (`30-…`) pins the commit-message format this
  policy scans, and its frozen-history rule governs the
  pushed-slip drill: no history rewrite without owner approval.</COMPOSES-ATOMIC-COMMITS>
      <COMPOSES-DECISION-RECORDS fact="true" status="impl/done">`flow:decision-records` (`25-…`): the posture choice is a recorded
  decision with a revisit trigger tied to regulation changes.</COMPOSES-DECISION-RECORDS>
      <COMPOSES-WAL fact="true" status="impl/done">`flow:wal` (`10-…`): AI-workflow documents like the WAL protocol
  are explicitly carved out — they organize work and never mark
  artifacts as machine-authored.</COMPOSES-WAL>
    </facts>
  </composition>
  <background title="Philosophical background">
    <p><extracted-from-the-books-aside-on-commit-metadata fact="true" status="spec/done">Extracted from *AI-native development*, chapter 2 («Shared state:
файлы как IPC»), the aside on commit metadata: decide consciously
whether commits may say a model made them, and if the answer is no,
tell the model the true reason.</extracted-from-the-books-aside-on-commit-metadata></p>
    <p><collections-spirit-is-the-redbook fact="true" status="spec/done">The book ships in Russian inside
`flow:redbook` at `spec/book/ru/`.</collections-spirit-is-the-redbook></p>
  </background>
  <license title="License">
    <p><license-line fact="true" status="impl/done">UPL-1.0. See `LICENSE.md`.</license-line></p>
  </license>
</spec>
