<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">`flow:git-attribution-policy` — choose your authorship surface deliberately</title>
  <status stage="doc" state="done" audience="user"/>
  <p><fact id="PACKAGE-INSTALLS-A-DELIBERATE-ATTRIBUTION-POSTURE" status="impl/done">A vibevm `flow` package that installs a **deliberate attribution
posture** for an AI-assisted repository.</fact></p>
  <p><fact id="agents-self-attribute-and-an-undecided-project-drifts" status="spec/done">Coding agents attribute
themselves by default — trailers in commits, "generated by" lines —
and a project that never decides ends up with an accidental,
inconsistent record: neither a clean human-authored surface nor a
reliable provenance trail.</fact></p>
  <p><fact id="PACKAGE-MAKES-THE-CHOICE-EXPLICIT-AND-ENFORCED" status="spec/done">This package makes the choice explicit
and enforced. *Explicit ships; enforced does not. `vibe.toml` declares one
`[boot_snippet]` and no executable, so an install writes the snippet and three
documents; the enforcement is `enforcement-checklist.md`'s pre-push scan — a
fenced shell snippet each project must wire for itself — beside
tool-configuration advice and a periodic-audit line. Searched the whole
consuming tree (`packages/`, `vibedeps/`, `crates/`, `xtask/`, `tools/`,
`spec/`, `discipline/`, `terraform/`, `campaigns/`, `legacy-spec/`, root
scripts) and by name for `.git/hooks/`, `.githooks/`, `.github/` and
`core.hooksPath`: no hook, no CI file, no trailer setting. By this flow's own
law that a policy with no checker is a wish, the «enforced» half is a wish. The
half that does ship is the configuration the checklist calls cheaper than
catching — the boot snippet as the agent's standing instruction — and on this
consumer it holds: zero attribution trailers and one author across 400
commits.*</fact></p>
  <p><fact id="THE-DEFAULT-POSTURE-IS-CONCEALMENT" status="impl/done">**The default posture is concealment**: the repository presents a
human-authored surface on every artifact.</fact></p>
  <p><fact id="the-honest-rationale-ships-with-the-rule" status="spec/done">The honest rationale ships
with the rule — some jurisdictions regulate or may regulate machine
authorship of software, and an owner not subject to such regulation
may proactively remove the hook.</fact></p>
  <p><fact id="DISCLOSURE-IS-A-FIRST-CLASS-ALTERNATIVE" status="spec/done">The open-disclosure posture is
documented as a first-class alternative, and is the *mandatory*
choice where law, employer, or upstream projects require disclosure.</fact></p>
  <p><fact id="VERIFY-BOTH-BEFORE-ADOPTING-THE-DEFAULT" status="impl/done">Verify both before adopting the default; the protocol tells you how.</fact></p>
  <p><fact id="package-contents-lead" status="impl/done">This package ships three pieces of content plus a boot snippet:</fact></p>
  <list ordered="false">
    <item><fact id="CONTENT-THE-FULL-POLICY" status="impl/done">`spec/flows/attribution-policy/ATTRIBUTION-POLICY.md` — the full
  policy: what attribution governs, the concealment posture and its
  honest rationale, the single-place law, how to instruct an agent,
  and the slip drill.</fact></item>
    <item><fact id="CONTENT-THE-DISCLOSURE-ALTERNATIVE" status="impl/done">`spec/flows/attribution-policy/disclosure-alternative.md` — the
  open posture: when it is mandatory, when it is the better fit,
  what it costs, and how to switch postures forward-only.</fact></item>
    <item><fact id="CONTENT-THE-ENFORCEMENT-CHECKLIST" status="impl/done">`spec/flows/attribution-policy/enforcement-checklist.md` — the
  eight leak surfaces, a pre-push scan, tool configuration, and the
  periodic audit line.</fact></item>
    <item><fact id="CONTENT-THE-BOOT-SNIPPET" status="impl/done">`spec/boot/55-flow-attribution-policy.md` — boot snippet loaded at
  session start: the rule, the honest reason, the scope carve-outs,
  and the never-do list. Under the single-place law, this snippet is
  the one place in a consuming project where the topic lives.</fact></item>
  </list>
  <section id="install" title="Install">
    <fence lang="bash">vibe install flow:git-attribution-policy</fence>
  </section>
  <section id="uninstall" title="Uninstall">
    <fence lang="bash">vibe uninstall flow:git-attribution-policy</fence>
    <p><fact id="UNINSTALL-REMOVES-EVERY-FILE-THE-PACKAGE-WROTE" status="impl/done">Uninstalling removes every file the package wrote, including the
boot snippet.</fact></p>
    <p><fact id="USER-OWNED-FILES-ARE-NEVER-TOUCHED" status="impl/done">User-owned files are never touched.</fact></p>
  </section>
  <section id="composition" title="Composition">
    <list ordered="false">
      <item><fact id="COMPOSES-ATOMIC-COMMITS" status="impl/done">`flow:git-atomic-commits` (`30-…`) pins the commit-message format this
  policy scans, and its frozen-history rule governs the
  pushed-slip drill: no history rewrite without owner approval.</fact></item>
      <item><fact id="COMPOSES-DECISION-RECORDS" status="impl/done">`flow:decision-records` (`25-…`): the posture choice is a recorded
  decision with a revisit trigger tied to regulation changes.</fact></item>
      <item><fact id="COMPOSES-WAL" status="impl/done">`flow:wal` (`10-…`): AI-workflow documents like the WAL protocol
  are explicitly carved out — they organize work and never mark
  artifacts as machine-authored.</fact></item>
    </list>
  </section>
  <section id="background" title="Philosophical background">
    <p><fact id="extracted-from-the-books-aside-on-commit-metadata" status="spec/done">Extracted from *AI-native development*, chapter 2 («Shared state:
файлы как IPC»), the aside on commit metadata: decide consciously
whether commits may say a model made them, and if the answer is no,
tell the model the true reason.</fact></p>
    <p><fact id="collections-spirit-is-the-redbook" status="spec/done">The book ships in Russian inside
`flow:redbook` at `spec/book/ru/`.</fact></p>
  </section>
  <section id="license" title="License">
    <p><fact id="license-line" status="impl/done">UPL-1.0. See `LICENSE.md`.</fact></p>
  </section>
</spec>
