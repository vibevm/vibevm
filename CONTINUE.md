# CONTINUE — cold-resume snapshot (2026-08-03, wind-down №3: добро на фазу E)

**Не цитируй числа из этого файла — меряй:**
`python campaigns/packages-2026-09/tasks/summary.py` ·
`python campaigns/packages-2026-09/tasks/drift-registry.py`.
`spec/WAL.md` переписан той же сессией и **суперсидит** этот снапшот.

**TL;DR.** Фаза D **закрыта** на зелёном гейте (корпус 98.0 %, owed 17 — все
на рулингах-стройках). **Фаза E АВТОРИЗОВАНА** — «даю добро» владельца
2026-08-03 записано в §7 LOG (пятый обмен) и в статусе плана. Исполнители —
**claudez-воркеры**, босс — **Fable**, и экономика задана дословно:
«минимизация нагрузки на Fable и максимизация кодинговой работы на claudez
субагенты»; «работа по сшивке-приемке-суждениям высокоуровневым никто лучше
не сделает». Транспорт для этого построен и проверен вживую в тот же день:
изоляция `-c`-тредов (матрица ALPHA/BRAVO), параллельность в worktree,
stream-json-логи сразу в архив, опрос ~30 с, WORKER-REPORT как карта приёмки.
Фазы **T/F/G этим добром НЕ покрыты** — ждут своего слова.

## ПЕРВОЕ ДЕЛО НОВОЙ СЕССИИ — исполнять мандат фазы E

Мандат (авторизован, повторный вопрос владельцу НЕ нужен):

1. **Волна А — B-011** (Самый Высокий Приоритет владельца: детерминированная
   загрузка). Направления его дизайна — дословно в `BACKLOG.md` B-011:
   rename-on-splice с сохранением валидности всех ссылок, `#use spec://… as
   X` + `@!X`, аналогия ADL из C++, кейс динамической дозагрузки библиотек со
   своими STATIC.md. **Дизайн — работа босса (Fable), НЕ делегируется, и
   предъявляется владельцу ДО имплементации** (в формате подачи: суть →
   дерево решений → точные имена). Имплементация одобренного дизайна, спайки
   и прототипы — claudez-воркерами.
2. **Параллельная линия — исследования B-022 и B-023** (по образцу B-012):
   evidence-свипы можно воркерам, суждение и рекомендация — босс.
3. После B-011 внутри волны А: B-006, B-031, B-028. Волны Б/В/Г — после.
   Release-события (в т.ч. пере-минт `-lang`-слотов) — к владельцу ДО
   публикации. Пересуд якорей шести deferred-строк — по мере посадки их
   строек (mirror → merge-verdicts → seal, не сцеплять).

**Подготовка сессии:** boot по контракту → обе команды замера → `bash
tools/self-check.sh` → 0 (шаг 6b требует локальный jtd-codegen; vibe-команды
параллельно панели запрещены).

## Экономика Fable/claudez (владелец, дословно, 2026-08-03)

Fable минимален и незаменим: нарезка пакетов, дизайн-суждения, ревью по
карте WORKER-REPORT, сшивка/мерджи, пересуды, коммиты, разговор с владельцем.
**Весь кодинг — воркерам**; Fable не печатает код, который можно отдать.
Три закрывающих уточнения владельца (шестой обмен): высокоуровневые драфты
делает Fable — **скаффолдом с вшитыми точками уточнения/проверки** для
воркера («уточни здесь», «проверь, что»); отчёт воркера обязан нести
**«Decisions taken (each with why)»** — каждое решение с обоснованием;
у Fable — **явное право НЕ ПРИНЯТЬ**: вердикты ПРИНЯТО · НЕ ПРИНЯТО →
доработка через `-c` с точным именованием неверного решения/реализации ·
re-commission (потолок — две доработки) · discard; каждый цикл — в meta.md.

## Транспорт воркеров — закон (читать SUBAGENT-LAUNCHERS.md ЦЕЛИКОМ)

`campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` + переключатель
`SUBAGENT-MODE.toml` (сейчас `claudez`; перечитывать перед КАЖДЫМ fan-out —
рычаг владельца). Скелет: запускаторы `C:\Users\olegc\opt\bin\{claudez,claudez2}`
(две линии, у каждого свой каталог состояния — `-c` продолжает свой тред;
effort=max вшит); изолированные периметры → две линии в worktree, конфликтоопасные
многоместные правки → один поток; `--output-format stream-json --verbose`,
лог СРАЗУ в `C:\Users\olegc\git\v\cache\agents\sorted\<task-id>\`; heartbeat
`echo "PROGRESS: …"`/`TASK-DONE` обязателен в пакете (первичный сигнал —
рост лога; стойло ≈ mtime ≳5 мин); закрывающий `WORKER-REPORT-<task-id>.md`
по вшитому шаблону; финализация — meta.md + штампованный перенос отчёта;
приёмка по артефактам, не по финальной строке; воркерам git-глаголы не
выдаются; фикс в крейтах пакета → `cargo xtask sync-engines` тем же заходом.

## Очередь владельца

Пусто по фазе E (добро дано). Не срочное: открытые строки аудита
(`AUDIT.md` §2026-08-03), DBT-0023 (quinn-proto у fractality), MT-02/MT-03,
стоячий known-issues в WAL. Развилки карты (единица гейта, computed-имена,
отпечаток, язык запросов, композиция) — приносить по одной по мере подхода.

## Где стоит работа

- Ветка `main`, оба зеркала синхронны; роллаут — ТОЛЬКО `cargo xtask mirror`.
  Дерево чистое.
- Панель зелёная (с шагом 6b); exhaustive `progress check --campaign` clean,
  261 файл; `--campaign` ВСЕГДА.
- Реестр: 90 / 190, owed 17 — все на стройках B-022/023/025/026/029/031/033/034
  (+B-032); resolved 139. Ledger: `PHASE-D-HOST-OBLIGATIONS.md#close-2026-08-03`.

## Карта

`campaigns/packages-2026-09/`: **SUBAGENT-LAUNCHERS.md + SUBAGENT-MODE.toml**
(закон транспорта), PHASE-D-BATCH-PLAN.md (§3.6/3.7/3.8, §6.1 — уроки),
PHASE-D-HOST-OBLIGATIONS.md (#close-2026-08-03), PHASE-T-SPEC.md (транспорт
конкретизирован), tasks/*.py, run/mirror/. План:
`spec/terraforms/PACKAGES-ACTUALIZATION-CAMPAIGN-v0.1.md` — статус в шапке
(PHASE E AUTHORIZED), §5 Phase E, §7 LOG с конца (пять обменов 2026-08-03).
`BACKLOG.md` (B-011 — первый; B-013 done) + `TOOLING-MAP.md` (волны, рамка,
развилки). `AUDIT.md` §2026-08-03. Архив логов:
`C:\Users\olegc\git\v\cache\agents\` (+README). `spec/WAL.md` — канон.

## Недавняя цепочка коммитов (хвост)

```
915be44* docs(wal): контракт воркеров дополнен обязательным отчётом
<этот wind-down> docs(campaign): мандат фазы E + статус; docs(wal); docs(continue)
2351141* docs(campaign): 30-секундный контракт наблюдаемости и архив логов
add26863 docs(wal): констрейнт делегирования переведён на переключатель владельца
235a304b docs(campaign): E/T получают переключаемый claudez-транспорт воркеров
9c965514 docs(campaign): phase D closes — the remainder, and who owns each row
dcc23250 chore(campaign): the phase boundary's baseline
755d664a feat(campaign): the routing record closes, and every survivor carries an owner ruling
```

## Quick-start

```sh
python campaigns/packages-2026-09/tasks/summary.py
python campaigns/packages-2026-09/tasks/drift-registry.py
bash tools/self-check.sh   # реальный exit-код; vibe-команды параллельно запрещены
```

_WAL — канон живого состояния; при расхождении верить ему, не этому файлу._
