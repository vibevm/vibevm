<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">PROP-018 — Agentic and standalone modes</title>
  <status stage="impl" state="done" action="continue" comment="C 2026-07-25: the §4 MVP is implemented end to end (23 implements / 8 verifies across 8 sections); §6&apos;s heavier surface stays parked; fact grain 2026-07-24"/>
  <p><status-line fact="true" status="impl/done">**Status: the §4 MVP is IMPLEMENTED** (specified 2026-06-16 in an
owner-requested design session; verified against the tree 2026-07-25 by the
spec-actualization campaign — `vibe agentic explain`, `vibe command`, the
`[[skill]]` section and the `agentic_explain` MCP tool are all live, with 23
specmap `implements` and 8 `verifies` edges across eight sections). Everything
heavier stays parked in §6 (far backlog). This is the spec home for vibevm's
*product modes* — a cross-cutting concept, distinct from PROP-006's *session*
postures (see §1.3).</status-line></p>
  <p><related fact="true" status="spec/done">**Related:** [PROP-015](../modules/vibe-mcp/PROP-015-mcp-integration.xml)
(the MCP server + agent-integration machinery — the `Agent` enum, the
per-agent config/skill writers, and `vibe mcp install` — that this PROP
reuses and extends), [`VIBEVM-SPEC.md` §3.2](../../VIBEVM-SPEC.md) (the
committed *CLI-first, agent-agnostic* posture and the
deterministic/probabilistic split this PROP formalises into modes),
[`VIBEVM-SPEC.md` §10.4](../../VIBEVM-SPEC.md) (the future `vibe-llm`
provider layer — the standalone built-in inference backend §2.2 names),
[PROP-006](PROP-006-operating-modes.xml) (session operating postures — a
*different* concept, §1.3), [PROP-003 §2.5](../modules/vibe-resolver/PROP-003-dep-evolution.xml)
(subskill *delivery* into the project tree — distinct from agent-skill
*projection*, §2.5), [PROP-017 §8](../modules/vibe-resolver/PROP-017-resolvo-resolver.xml)
(a sibling far-backlog).</related></p>
  <motivation title="1. Motivation">
    <problem title="1.1 The problem">
      <facts ordered="false">
        <commands-split fact="true" status="spec/done">vibevm has commands that are pure algorithm (`install`, `check`, `list`)
  and commands that genuinely need reasoning (explain, build, review). The
  algorithmic ones already run from a bare terminal with no LLM
  (`VIBEVM-SPEC.md` §3.2).</commands-split>
        <reasoning-question fact="true" status="spec/done">The reasoning ones raise a question of *who does
  the reasoning, and how*.</reasoning-question>
      </facts>
      <facts ordered="false">
        <invoked-by-agent fact="true" status="spec/done">vibevm is almost always invoked *by* a coding agent (Claude Code,
  OpenCode, Codex) that already holds a capable LLM, the live context, and
  the tools.</invoked-by-agent>
        <executor-vs-author fact="true" status="spec/done">For reasoning work in that setting the agent is the right
  **executor** — but vibevm is the right **author** of the task.</executor-vs-author>
        <domain-knowledge fact="true" status="spec/done">vibevm carries stable, algorithmic knowledge of its own domain (the
  spec-driven model, the dependency and package rules, the discipline), so an
  instruction it composes is more informative and more trustworthy than one
  the agent would improvise from scratch.</domain-knowledge>
        <roles-named fact="true" status="spec/done">This PROP gives the two their natural roles — vibevm composes the
  domain-grounded instruction, the agent carries it out — and names the
  operating contexts so the codebase branches on them cleanly.</roles-named>
        <standalone-aside fact="true" status="spec/done">vibevm can also reason with *no* agent present, via a
  built-in `vibe-llm` engine — `VIBEVM-SPEC.md` §10.4, far-backlog §6 — but
  that is standalone mode; it is not what makes agentic mode worthwhile.</standalone-aside>
      </facts>
    </problem>
    <axis title="1.2 The two modes — one axis">
      <p><one-axis fact="true" status="spec/done">The modes are not two codebases. They are one question: **where does an
operation's reasoning happen?**</one-axis></p>
      <facts ordered="false">
        <MODE-AGENTIC fact="true" status="spec/done">**agentic** — vibevm is driven by a host agent during that agent's own
  work. For a step that needs reasoning, vibevm composes a domain-grounded
  instruction and *delegates it back* to the agent, which executes it on
  its LLM with the live context vibevm lacks. The split is by strength, not
  a workaround: the agent is the better executor in-session, vibevm the
  better author of the instruction. (Pure-algorithm operations still run
  directly — agentic is about the *reasoning* steps.)</MODE-AGENTIC>
        <MODE-STANDALONE fact="true" status="spec/done">**standalone** — vibevm stands on its own. Reasoning runs on vibevm's
  *own* backend: algorithmic where the work allows, and — when `vibe-llm`
  lands — a built-in inference engine. Today the standalone backend has no
  LLM, so the only standalone functionality this PROP ships is the
  **non-reasoning** one: projecting skills into agents (§2.6).</MODE-STANDALONE>
      </facts>
      <facts ordered="false">
        <UNIFYING-STATEMENT fact="true" status="spec/done">The unifying statement (§2.1): **a mode is a choice of inference backend.**
  Non-reasoning operations behave identically in both modes; reasoning
  operations branch on the backend.</UNIFYING-STATEMENT>
        <THE-SEAM fact="true" status="spec/done">This is the seam everything else hangs off.</THE-SEAM>
      </facts>
    </axis>
    <not-prop-006 title="1.3 What this is NOT — PROP-006">
      <facts ordered="false">
        <P006-SESSION-POSTURES fact="true" status="spec/done">[PROP-006](PROP-006-operating-modes.xml) defines *session operating
  postures*: codewords ("move fast and break things") that change **how an
  agent behaves within one work session** (whether to pause for
  confirmation, how freely to spend tokens). Those govern the *agent's*
  conduct.</P006-SESSION-POSTURES>
        <P018-INFERENCE-SOURCE fact="true" status="spec/done">PROP-018 modes govern **where vibevm gets inference** — a property
  of *vibevm's* execution, orthogonal to any session posture.</P018-INFERENCE-SOURCE>
        <orthogonal-example fact="true" status="spec/done">A session can be in "move fast" posture while vibevm runs in agentic
  mode; the two never collide.</orthogonal-example>
        <NO-OVERLOAD fact="true" status="spec/done">Do not overload one onto the other.</NO-OVERLOAD>
      </facts>
    </not-prop-006>
  </motivation>
  <decisions title="2. Decisions">
    <mode-is-backend title="2.1 A mode is a choice of inference backend">
      <p><req-mode-backend fact="true" status="spec/done">`req r1`</req-mode-backend></p>
      <facts ordered="false">
        <MODE-INFERRED fact="true" status="spec/done">**Decision.** Mode is not a global flag the user sets; it is **inferred per
  operation from how vibevm was reached and what backend is available.**</MODE-INFERRED>
        <OP-DECLARES fact="true" status="spec/done">An operation declares whether it needs inference; if it does, the active
  backend decides the realisation:</OP-DECLARES>
      </facts>
      <facts ordered="false">
        <REACH-SUBPROCESS fact="true" status="spec/done">reached as a **subprocess of an agent** (CLI one-shot or MCP call) →
  the **relay backend** (§2.7): delegate the intent back to that agent.</REACH-SUBPROCESS>
        <REACH-STANDALONE-ENGINE fact="true" status="spec/done">reached **standalone** with a built-in engine available (future) → the
  **built-in backend**: run inference in-process via `vibe-llm`.</REACH-STANDALONE-ENGINE>
        <REACH-STANDALONE-NO-ENGINE fact="true" status="spec/done">reached **standalone** with no engine (today) → a reasoning operation
  **fails loud** with "this needs an inference backend; run me under an
  agent, or wait for the built-in engine," and a non-reasoning operation
  runs normally.</REACH-STANDALONE-NO-ENGINE>
        <mode-inferred-why fact="true" status="spec/done">**Why:** §1.2 `##UNIFYING-STATEMENT` fixes what a mode is — *"a mode is a choice of inference backend"* — and §2.3 `##AFFINITY-OF-WORK` fixes who chooses: *"Affinity is a property of the work, not a user choice."* Mode-by-inference is that same principle one level up: which backend can serve a call is a fact about the call's reach, not a preference.</mode-inferred-why>
        <mode-inferred-rejected fact="true" status="spec/done">**Considered and rejected:** **a global mode flag the user sets** (`--mode agentic|standalone` or a `vibe.toml` key) — rejected: a user could then name a backend the operation has no affinity for, which the dispatcher must refuse anyway (`##DISPATCHER-REFUSES`), or name one that does not exist on this machine, which today is every standalone reasoning call (`##REACH-STANDALONE-NO-ENGINE`). The flag would be a way to ask for a refusal.</mode-inferred-rejected>
        <mode-inferred-revisit fact="true" status="spec/done">**Revisit when:** `BuiltinBackend` ships over `vibe-llm` (`##FB-BUILTIN-BACKEND`, `VIBEVM-SPEC.md` §10.4) — from that day two backends can both serve one standalone call, and *"what backend is available"* stops determining the answer on its own. Observation point: the far-backlog item closing, i.e. a `vibe-llm` inference path in the workspace. Second clause: a reach appears that the inference cannot classify — a persistent console (`##FB-CONSOLE`) or an invocation through a wrapper that hides the agent parentage — observed as a mis-chosen backend in a bug report.</mode-inferred-revisit>
      </facts>
    </mode-is-backend>
    <pluggable-backend title="2.2 The pluggable inference backend">
      <p><req-pluggable fact="true" status="spec/done">`req r2`</req-pluggable></p>
      <facts ordered="false">
        <BACKEND-TRAIT fact="true" status="spec/done">**Decision.** Inference sits behind one trait, `InferenceBackend`, so an
  operation never names a provider.</BACKEND-TRAIT>
        <INTENT-CONSTRUCT fact="true" status="spec/done">An operation that needs reasoning constructs an `Intent` (a structured
  prompt + the inputs it needs) and hands it to the active backend.</INTENT-CONSTRUCT>
        <two-backends-lead fact="true" status="spec/done">Two backends are foreseen:</two-backends-lead>
      </facts>
      <facts ordered="false">
        <RELAY-BACKEND fact="true" status="spec/done">**`RelayBackend`** (agent mode) — vibevm authors the `Intent` and
  *parks* it for the calling agent to execute, returning "delegated"
  (§2.7). Not a stopgap: in agent mode the agent is the right executor.</RELAY-BACKEND>
        <BUILTIN-BACKEND fact="true" status="spec/done">**`BuiltinBackend`** (standalone mode; far backlog §6) — runs the
  `Intent` on `vibe-llm` in-process, for when no agent is present.</BUILTIN-BACKEND>
      </facts>
      <facts ordered="false">
        <NOT-OVERBUILT fact="true" status="spec/done">This is deliberately **not** over-built: the trait exists so reasoning
  operations are written once against an abstract backend, and the standalone
  engine slots in later without touching them.</NOT-OVERBUILT>
        <SINGLE-HOME-OPS fact="true" status="spec/done">Operations with a single
  natural home (skill-install is standalone-only; a "rewrite this spec
  section" op may be agentic-only until the engine exists) simply do not
  offer the other backend (§2.3).</SINGLE-HOME-OPS>
      </facts>
    </pluggable-backend>
    <affinity title="2.3 Per-operation backend affinity">
      <p><req-affinity fact="true" status="spec/done">`req r2`</req-affinity></p>
      <facts ordered="false">
        <AFFINITY-DECL fact="true" status="spec/done">**Decision.** Each operation declares an **affinity**: `agentic-only`,
  `standalone-only`, or `both`.</AFFINITY-DECL>
        <AFFINITY-OF-WORK fact="true" status="spec/done">Affinity is a property of the *work*, not a
  user choice — scanning a manifest is `standalone-only` (pure algorithm,
  needs no agent); a free-form "explain this project in prose" is
  `agentic-only` until the built-in engine exists; a task expressible either
  as a deterministic pass or as reasoning is `both`.</AFFINITY-OF-WORK>
        <DISPATCHER-REFUSES fact="true" status="spec/done">The dispatcher refuses
  an operation invoked through a backend it has no affinity for, with a
  message naming the right one.</DISPATCHER-REFUSES>
      </facts>
    </affinity>
    <skill-decl title="2.4 Agent-installable artifacts are declared separately from the package kind">
      <p><req-skill-decl fact="true" status="spec/done">`req r3`</req-skill-decl></p>
      <facts ordered="false">
        <SKILL-SECTION-NOT-KIND fact="true" status="spec/done">**Decision.** A package declares which of its files are **skills** for
  agents in a dedicated manifest section — **not** by introducing a
  package kind of its own. The kind register (`package_ref.rs`,
  `VIBEVM-SPEC.md` §4.1) stays closed to skills.</SKILL-SECTION-NOT-KIND>
        <ANY-KIND-RATIONALE fact="true" status="spec/done">**Why:** skills can
  live inside a package of *any* kind and be structured any way. A `tool`
  package `vim` can ship the tool itself **plus** a skill for driving vim
  — one self-contained package, two artefact classes. Kind answers "what
  is this package"; the new section answers "what does it project into an
  agent."</ANY-KIND-RATIONALE>
        <MCP-HALF-SUPERSEDED fact="true" status="spec/done">This unit's original text sketched MCP servers as a second
  any-kind section; that half is SUPERSEDED — MCP servers became their own
  `mcp` kind with their own laws, owner resolution 2026-07-07:
  [PROP-027](../modules/vibe-mcp/PROP-027-mcp-packages.xml). The skill law
  here is unchanged.</MCP-HALF-SUPERSEDED>
        <skill-decl-rejected fact="true" status="spec/done">**Considered and rejected:** **a fifth package kind for skills** — explicitly rejected (`##OOS-FIFTH-KIND`): kind answers *"what is this package"* while the section answers *"what does it project into an agent"*, and skills can live inside a package of any kind (`##ANY-KIND-RATIONALE`). **MCP servers as a second any-kind section** — proposed in this unit's original text and **superseded**: they became their own `mcp` kind with their own laws, owner resolution 2026-07-07 ([PROP-027](../modules/vibe-mcp/PROP-027-mcp-packages.xml), `##MCP-HALF-SUPERSEDED`). The skill law is unchanged; the MCP half is the counter-example that shows where the line falls.</skill-decl-rejected>
        <skill-decl-revisit fact="true" status="spec/done">**Revisit when:** an agent-installable artefact class arrives that needs **its own lifecycle laws** — install / uninstall semantics, resolution or conflict rules of its own — rather than only a projection path. That is exactly the state that fired for MCP servers on 2026-07-07 (`##MCP-HALF-SUPERSEDED`), so the trigger has a worked precedent. Observation point: the kind register in `crates/vibe-core` (`package_ref.rs`) and `VIBEVM-SPEC.md` §4.1 — whose `##INV-VOCABULARY` already anticipates `app`; the register growing is the fired state.</skill-decl-revisit>
      </facts>
      <p><SKILL-TABLE-SHAPE fact="true" status="spec/done">The MVP section is an array-of-tables, matching the manifest's existing
`[[requires_any]]` / `[[registry]]` / `[[mirror]]` shape:</SKILL-TABLE-SHAPE></p>
      <fence lang="toml">[[skill]]
name        = "vim"                 # becomes the skill dir name in the agent
path        = "skills/vim"          # file or dir (relative to package root) = the skill body
description = "Drive vim from an agent"   # optional; shown in listings
agents      = ["claude", "opencode"]      # optional; default = all skill-supporting agents</fence>
      <p><MCP-TABLE-RESERVED fact="true" status="spec/done">A sibling `[[mcp]]` table (command / args / target agents) is specified the
same way but is **near-term, not MVP** (§6) — the schema is reserved here so
the vim-style "tool + mcp + skill" package is expressible end to end.</MCP-TABLE-RESERVED></p>
    </skill-decl>
    <projection title="2.5 Skills are an orthogonal projection, not a delivery mode">
      <p><req-projection fact="true" status="spec/done">`req r3`</req-projection></p>
      <facts ordered="false">
        <PROJECTION-DEF fact="true" status="spec/done">**Decision.** Installing a skill into an agent is a **projection**: read the
  declared skill body from the package (in `vibedeps/…` once installed) and
  write it into each target agent's skill directory in that agent's own
  convention (`.claude/skills/&lt;name&gt;/…`, `.opencode/skills/&lt;name&gt;/…`,
  `.agents/skills/&lt;name&gt;/…` — the paths PROP-015 §2.6 already resolves).</PROJECTION-DEF>
        <DISTINCT-FROM-DELIVERY fact="true" status="spec/done">This
  is distinct from PROP-003 §2.5 subskill *delivery* (which materialises
  content into the **project tree**). Skill projection materialises **out of**
  the workspace, into the **agent**.</DISTINCT-FROM-DELIVERY>
        <NO-SHARED-CODE fact="true" status="spec/done">The two share no code path beyond the
  `Agent` skill-path resolver.</NO-SHARED-CODE>
      </facts>
    </projection>
    <vibe-skill title="2.6 Standalone MVP — `vibe skill install`">
      <p><req-vibe-skill fact="true" status="spec/done">`req r3`</req-vibe-skill></p>
      <p><SKILL-CMD-FAMILY fact="true" status="spec/done">**Decision.** A new command family projects package-declared skills into
agents, reusing PROP-015's agent machinery (`Agent` enum, detection, the
idempotent skill writer, the per-(agent, scope) report records):</SKILL-CMD-FAMILY></p>
      <facts ordered="false">
        <CMD-SKILL-LIST fact="true" status="spec/done">**`vibe skill list`** — skills declared by installed packages.</CMD-SKILL-LIST>
        <CMD-SKILL-INSTALL fact="true" status="spec/done">**`vibe skill install [--agent …] [--scope project|user|both] [&lt;pkgref&gt;] [&lt;skill&gt;…]`**
  — project skills into agents. **Default: all declared skills**; narrow
  with explicit skill names or a pkgref. Idempotent, `--dry-run`, confirm
  (or `--assume-yes`), per-(agent, scope) report — the same lifecycle and
  merge discipline as `vibe mcp install` (PROP-015 §2.7).</CMD-SKILL-INSTALL>
        <CMD-SKILL-UNINSTALL fact="true" status="spec/done">**`vibe skill uninstall …`** — the inverse; strips only vibevm-projected
  skills, leaves foreign skill dirs untouched.</CMD-SKILL-UNINSTALL>
      </facts>
      <p><ONLY-STANDALONE-V1 fact="true" status="spec/done">This is the **only standalone functionality v1 of this PROP ships.** It
needs no LLM, so it works today, agent-present or not.</ONLY-STANDALONE-V1></p>
    </vibe-skill>
    <relay title="2.7 The agentic relay — delegate intent back to the caller">
      <p><req-relay fact="true" status="spec/done">`req r4`</req-relay></p>
      <facts ordered="false">
        <RELAY-PARKS fact="true" status="spec/done">**Decision.** When a reasoning operation runs under the relay backend, it
  does not act. It writes an `Intent` — a markdown prompt with light
  frontmatter (id, source command, created-at, status) — to a **single-slot
  mailbox**, the project-local `.vibe/agentic/command.md` (§3), and returns a
  pointer telling the caller to drain it.</RELAY-PARKS>
        <DRAIN-VERB fact="true" status="spec/done">The **consumer seam is one
  command**, `vibe command`: it prints the pending `Intent` to stdout and
  clears the slot (consume-on-read; the spent intent is archived to
  `.vibe/agentic/command.done.md`).</DRAIN-VERB>
        <EMPTY-SLOT-OK fact="true" status="spec/done">Re-running with an empty slot prints "no
  pending command" and exits `0`.</EMPTY-SLOT-OK>
      </facts>
      <p><two-step-lead fact="true" status="spec/done">Two properties make the two-step (produce → `vibe command`) worth its
seam rather than just printing the intent from the producer:</two-step-lead></p>
      <facts ordered="true">
        <SEAM-UNIFORMITY fact="true" status="spec/done">**Uniformity.** *Any* vibevm command that discovers mid-run it needs
   reasoning parks an intent the same way — not only `vibe agentic …`
   commands. The agent learns one drain verb, not per-command stdout
   parsing.</SEAM-UNIFORMITY>
        <SEAM-DECOUPLING fact="true" status="spec/done">**Decoupling.** Producer and consumer need not be the same invocation,
   which is what lets a future deterministic command (`vibe build`) park a
   reasoning step and exit, the agent draining it afterward.</SEAM-DECOUPLING>
      </facts>
      <facts ordered="false">
        <NO-WRITE-BACK fact="true" status="spec/done">**MVP carries no write-back** (`req r4`): the relay is fire-and-forget.</NO-WRITE-BACK>
        <AGENT-ORCHESTRATES fact="true" status="spec/done">The calling agent orchestrates the conversation — if it wants vibevm to
  see the result, *it* arranges that with a follow-up command.</AGENT-ORCHESTRATES>
        <SKILL-STATES-NO-CHANNEL fact="true" status="spec/done">The installed skill (§2.9) states this contract explicitly so agents do
  not wait for a channel that is not there. (Full bidirectional
  conversations are §6.)</SKILL-STATES-NO-CHANNEL>
        <relay-why fact="true" status="spec/done">**Why:** the two-step seam buys two properties a direct print cannot, both stated at `##two-step-lead`: **uniformity** — any command that discovers mid-run it needs reasoning parks an intent the same way, so an agent learns one drain verb rather than per-command stdout parsing (`##SEAM-UNIFORMITY`); and **decoupling** — producer and consumer need not be the same invocation, which is what lets a future deterministic command park a reasoning step and exit (`##SEAM-DECOUPLING`).</relay-why>
        <relay-rejected fact="true" status="spec/done">**Considered and rejected:** **printing the intent directly from the producer**, with no `vibe command` seam — rejected for the two reasons above; it is cheaper by one command and forfeits both. **A return channel (write-back)** — **deferred, not rejected**: the MVP relay is fire-and-forget (`##NO-WRITE-BACK`), the calling agent orchestrates (`##AGENT-ORCHESTRATES`), and full bidirectional conversations are parked at §6 `##FB-CONVERSATIONS`.</relay-rejected>
        <relay-revisit fact="true" status="spec/done">**Revisit when:** either the **single slot overflows** — a producer runs while `.vibe/agentic/command.md` already holds an undrained intent, which the mailbox's own shape makes mechanically observable (`##FILE-COMMAND-MD`) — **or** the fire-and-forget contract starts costing a round trip, observed as an agent issuing a follow-up `vibe …` command whose only purpose is to hand a result back (the case `##SKILL-STATES-NO-CHANNEL` tells agents not to expect). Either fires §6's `##FB-CONVERSATIONS`.</relay-revisit>
      </facts>
    </relay>
    <transports title="2.8 One operation, two transports">
      <p><req-transports fact="true" status="spec/done">`req r5`</req-transports></p>
      <p><ONE-OP-TWO-TRANSPORTS fact="true" status="spec/done">**Decision.** A reasoning/agentic operation is defined **once**, as a
transport-agnostic core (the `Intent`-producing function over a project
context), and exposed by **two thin adapters**:</ONE-OP-TWO-TRANSPORTS></p>
      <facts ordered="false">
        <TRANSPORT-CLI fact="true" status="spec/done">**One-shot CLI** (`vibe agentic &lt;op&gt;`) — stateless, one process per call.
  An intent is delivered through the §2.7 file relay. Best when vibevm is
  touched once and discarded — e.g. an agent scanning a directory of
  hundreds of vibevm projects for a quick fact. All per-session state
  (future conversation/context, §6) is lost on exit, by design.</TRANSPORT-CLI>
        <TRANSPORT-MCP fact="true" status="spec/done">**MCP server** (`vibe mcp serve`, PROP-015 §2.1) — persistent,
  zero-latency, can hold session context. The same op is registered as an
  MCP tool; an intent is returned **synchronously in the tool result**, so
  no file mailbox is needed on this path. Best for sustained work inside
  one project.</TRANSPORT-MCP>
      </facts>
      <facts ordered="false">
        <CHOICE-IS-AGENTS fact="true" status="spec/done">The choice is the **agent's**, by situation, and the skill (§2.9) teaches
  the heuristic.</CHOICE-IS-AGENTS>
        <CORE-ADAPTER-BLIND fact="true" status="spec/done">The core never knows which adapter called it.</CORE-ADAPTER-BLIND>
      </facts>
    </transports>
    <usage-skill title="2.9 The vibevm-usage skill teaches the protocol">
      <p><req-usage-skill fact="true" status="spec/done">`req r5`</req-usage-skill></p>
      <p><USAGE-SKILL-TEACHES fact="true" status="spec/done">**Decision.** The skill `vibe mcp install` already projects
(`skill_template.md`) gains a section that teaches an agent:</USAGE-SKILL-TEACHES></p>
      <facts ordered="false">
        <TEACH-TRANSPORT fact="true" status="spec/done">the transport heuristic (one-shot CLI for a quick/one-off or a wide scan;
  MCP server for sustained in-project work) — §2.8;</TEACH-TRANSPORT>
        <TEACH-RELAY fact="true" status="spec/done">the relay contract: some `vibe …` commands park reasoning instead of
  doing it; after such a command, run `vibe command`, then **carry out the
  returned instruction yourself**;</TEACH-RELAY>
        <TEACH-NO-WRITEBACK fact="true" status="spec/done">there is **no automatic write-back** — if the result should reach
  vibevm, the agent issues the follow-up itself.</TEACH-NO-WRITEBACK>
      </facts>
      <p><SKILL-DATA-NOT-CODE fact="true" status="spec/done">The skill stays *data, not code* (PROP-015 §2.6).</SKILL-DATA-NOT-CODE></p>
    </usage-skill>
    <explain title="2.10 `vibe agentic explain` — the MVP demonstrator">
      <p><req-explain fact="true" status="spec/done">`req r4`</req-explain></p>
      <p><EXPLAIN-DEMONSTRATOR fact="true" status="spec/done">**Decision.** The first `vibe agentic` operation, `explain`, exercises the
whole relay with zero real risk. Run under an agent, it parks an `Intent`
to `.vibe/agentic/command.md` of roughly:</EXPLAIN-DEMONSTRATOR></p>
      <fence>Task — explain this project. In ≤3 short paragraphs, tell the reader
what this project is and does. Sources, in priority order: (1) `README.md`
at the project root — summarise it; (2) if `vibe.toml` is present, fold in
what its structure reveals (the package `kind`, what it `requires`, what
it `provides`). If `README.md` is absent, say so and explain from
`vibe.toml` alone. Write for a developer seeing the repo for the first
time. Do not invent features the sources do not support.</fence>
      <facts ordered="false">
        <EXPLAIN-NO-LLM fact="true" status="spec/done">`vibe agentic explain` does no LLM work and reads no file content itself; it
  only composes the intent (it *may* check which of `README.md` / `vibe.toml`
  exist to tailor the prompt).</EXPLAIN-NO-LLM>
        <EXPLAIN-FLOW fact="true" status="spec/done">The agent then runs `vibe command`, gets this
  instruction, and produces the explanation on its own LLM.</EXPLAIN-FLOW>
        <EXPLAIN-AFFINITY fact="true" status="spec/done">Affinity: `agentic-only` until the built-in backend exists (§2.3).</EXPLAIN-AFFINITY>
      </facts>
    </explain>
  </decisions>
  <vibevm-dir title="3. The `.vibe/agentic/` relay directory">
    <p><req-relay-dir fact="true" status="spec/done">`req r4`</req-relay-dir></p>
    <facts ordered="false">
      <RELAY-DIR fact="true" status="spec/done">**Decision.** Agentic relay state lives under the existing project-local
  `.vibe/` scratch root, in a dedicated **`.vibe/agentic/`** subdirectory
  (created on demand) — one dot-dir, not two.</RELAY-DIR>
      <INHERITS-IGNORE fact="true" status="spec/done">`.vibe/` is already vibevm's
  project-local scratch space (`init.rs` scaffolds `.vibe/cache/` for the
  package cache) and is already git-ignored by its own `.vibe/.gitignore`
  (`*`), so the relay inherits that ignore for free: no `vibe init` change,
  and no second near-homonym dot-dir sitting beside `.vibe/`.</INHERITS-IGNORE>
      <SUBDIR-DISAMBIGUATION fact="true" status="spec/done">Subdirectories
  disambiguate the two concerns — `.vibe/cache/` is the package cache,
  `.vibe/agentic/` is the agent↔vibevm relay channel (and the future home of
  the §6 conversation state).</SUBDIR-DISAMBIGUATION>
    </facts>
    <p><mvp-contents-lead fact="true" status="spec/done">MVP contents:</mvp-contents-lead></p>
    <facts ordered="false">
      <FILE-COMMAND-MD fact="true" status="spec/done">`.vibe/agentic/command.md` — the single pending intent (absent when none).</FILE-COMMAND-MD>
      <FILE-COMMAND-DONE-MD fact="true" status="spec/done">`.vibe/agentic/command.done.md` — the last consumed intent (archive aid).</FILE-COMMAND-DONE-MD>
    </facts>
    <facts ordered="false">
      <PATH-INTERNAL fact="true" status="spec/done">The relay path is an internal detail: the installed skill (§2.9) teaches
  the agent the `vibe command` verb, never the path, so the location carries
  no external contract and can move freely.</PATH-INTERNAL>
      <CACHE-CLEAN-SCOPE fact="true" status="spec/done">A future `vibe cache clean` must
  scope to `.vibe/cache/` — never the whole `.vibe/` — so cache eviction
  cannot nuke an in-flight relay intent.</CACHE-CLEAN-SCOPE>
    </facts>
  </vibevm-dir>
  <mvp title="4. MVP scope — what this PROP authorises now">
    <facts ordered="true">
      <MVP-MANIFEST fact="true" status="spec/done">**Manifest** — the `[[skill]]` section in `vibe-core` (§2.4), parsed,
   validated, round-tripped; `[[mcp]]` schema reserved but not wired.</MVP-MANIFEST>
      <MVP-STANDALONE fact="true" status="spec/done">**Standalone** — `vibe skill list` / `install` / `uninstall` (§2.6)
   over the existing agent machinery.</MVP-STANDALONE>
      <MVP-AGENTIC-CORE fact="true" status="spec/done">**Agentic core** — `InferenceBackend` + `Intent` + `RelayBackend`
   (§2.2, §2.7); affinity (§2.3).</MVP-AGENTIC-CORE>
      <MVP-RELAY fact="true" status="spec/done">**Agentic relay** — `.vibe/agentic/command.md` mailbox (§3); `vibe command`
   consumer (§2.7); `vibe agentic explain` producer (§2.10).</MVP-RELAY>
      <MVP-DUAL-TRANSPORT fact="true" status="spec/done">**Dual transport** — the explain op exposed as both `vibe agentic
   explain` (CLI) and an MCP tool (§2.8).</MVP-DUAL-TRANSPORT>
      <MVP-SKILL fact="true" status="spec/done">**Skill** — `skill_template.md` updated to teach the protocol (§2.9).</MVP-SKILL>
    </facts>
    <p><CRATE-PLACEMENT fact="true" status="spec/done">Crate placement (flagged to owner): a dedicated **`vibe-agentic`** crate for
§2.2/§2.3/§2.7 core (it will grow per §6), with adapters in `vibe-cli` and
`vibe-mcp`. Lighter alternative: fold the core into `vibe-mcp` for the MVP
and extract later.</CRATE-PLACEMENT></p>
  </mvp>
  <out-of-scope title="5. Out of scope (now)">
    <facts ordered="false">
      <OOS-FIFTH-KIND fact="true" status="spec/done">**A fifth package kind** — explicitly rejected (§2.4).</OOS-FIFTH-KIND>
      <OOS-MCP-WIRING fact="true" status="spec/done">**`[[mcp]]` bundled-server install** — schema reserved (§2.4), wiring is
  near-term, not MVP.</OOS-MCP-WIRING>
      <OOS-BUILTIN fact="true" status="spec/done">**Built-in inference** — `BuiltinBackend` waits on `vibe-llm`
  (`VIBEVM-SPEC.md` §10.4); MVP relay-only.</OOS-BUILTIN>
      <OOS-WRITE-BACK fact="true" status="spec/done">**Write-back / conversations** — §6.</OOS-WRITE-BACK>
      <OOS-PROP-006 fact="true" status="spec/done">**Changing PROP-006** — untouched (§1.3).</OOS-PROP-006>
    </facts>
  </out-of-scope>
  <far-backlog title="6. Far backlog">
    <p><far-backlog-lead fact="true" status="spec/done">Parked deliberately; recorded so the MVP's seams are cut to admit them:</far-backlog-lead></p>
    <facts ordered="false">
      <FB-CONVERSATIONS fact="true" status="spec/done">**Full vibevm↔agent conversations.** A request/response protocol shaped
  like the OpenAI Chat/Responses API: write-back, multi-turn, and full
  multi-agency — calling agents open any number of conversations; vibevm
  keeps a fast cache and the context each conversation needs. This is where
  the §2.7 relay grows a return channel and the §2.8 MCP transport grows
  session state.</FB-CONVERSATIONS>
      <FB-CONSOLE fact="true" status="spec/done">**An OpenCode-style console.** A persistent vibevm session with
  `--resume &lt;id&gt;`, reachable both from an agent (e.g. Claude Code) and
  interactively by a human at a terminal.</FB-CONSOLE>
      <FB-BUILTIN-BACKEND fact="true" status="spec/done">**`BuiltinBackend`** over `vibe-llm` (§2.2) — the standalone inference
  engine that lets reasoning operations run with no agent present.</FB-BUILTIN-BACKEND>
      <FB-MCP-PROJECTION fact="true" status="spec/done">**`[[mcp]]` bundled-server projection** (§2.4) — install a package's
  bundled MCP server into agents alongside its skills.</FB-MCP-PROJECTION>
    </facts>
    <p><sibling-backlogs fact="true" status="spec/done">(Sibling far-backlogs: PROP-017 §8. If these lists keep growing, a
consolidated backlog doc may be warranted — not today.)</sibling-backlogs></p>
  </far-backlog>
  <acceptance title="7. Acceptance">
    <facts ordered="false">
      <ACC-MANIFEST fact="true" status="spec/done">`vibe-core` parses and round-trips `[[skill]]`; an unknown key fails
  (`deny_unknown_fields`); a `[[skill]]` with a missing `path` is a typed
  manifest error citing this PROP.</ACC-MANIFEST>
      <ACC-SKILL-INSTALL fact="true" status="spec/done">`vibe skill install` projects a fixture package's declared skill into each
  skill-supporting agent under the right path, preserves foreign skill dirs,
  is idempotent, and reports per-(agent, scope); `uninstall` is its inverse;
  `list` writes nothing.</ACC-SKILL-INSTALL>
      <ACC-EXPLAIN-RELAY fact="true" status="spec/done">`vibe agentic explain`, run with a fixture project, writes a well-formed
  `.vibe/agentic/command.md` (frontmatter + the §2.10 prompt) and writes no other
  state; `vibe command` prints it, archives it to `command.done.md`, and
  empties the slot; a second `vibe command` reports "no pending command"
  and exits `0`.</ACC-EXPLAIN-RELAY>
      <ACC-MCP-TRANSPORT fact="true" status="spec/done">The same explain op invoked through the MCP transport returns the intent
  in the tool result and touches no mailbox file.</ACC-MCP-TRANSPORT>
      <ACC-FAIL-LOUD fact="true" status="spec/done">A reasoning operation invoked standalone with no engine fails loud with
  the §2.1 message; `vibe skill install` invoked standalone succeeds.</ACC-FAIL-LOUD>
      <ACC-SKILL-SECTION fact="true" status="spec/done">The projected `SKILL.md` contains the §2.9 protocol section; the existing
  PROP-015 acceptance still holds.</ACC-SKILL-SECTION>
    </facts>
  </acceptance>
</spec>
