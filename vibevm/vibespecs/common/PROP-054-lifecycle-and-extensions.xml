<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">PROP-054 — The vibe lifecycle and the extension machine</title>
  <status stage="spec" state="plan" comment="commissioned by the owner 2026-08-24 (mandate quoted in §1); a DESIGN awaiting owner review — nothing here is built, and the roadmap in §12 is the proposed build order"/>
  <p><related fact="true" status="spec/done">**Related:** [PROP-009](../modules/vibe-workspace/PROP-009-loading-model.xml) (install orchestration — becomes phase 2 of the default lifecycle), [PROP-011](../modules/vibe-workspace/PROP-011-incremental-install.xml) (skip-when-fresh — §9 revises its materialise step from slot-wipe to per-file diff), [PROP-020](../modules/vibe-workspace/PROP-020-install-hooks.xml) (the two install hooks whose `##REJ-GENERAL-LIFECYCLE` deferral this document un-defers; its trust gate is adopted whole), [PROP-022](../modules/vibe-workspace/PROP-022-materialization-modes.xml) (slot reset semantics — §9 refines them), [PROP-024](PROP-024-code-bearing-packages.xml) (code in packages; §8 amends its §2.3 build-output home), [PROP-025 §3–§4](../modules/vibe-workspace/PROP-025-binary-delivery.xml#dispatch) (`[[binary]]` build-in-slot — the precedent §8 generalises), [PROP-035](../modules/vibe-workspace/PROP-035-spec-compiler.xml) (the compile pipeline §7 attaches extension points to), [PROP-045](PROP-045-xml-spec-sources.xml) (the derived manifest — the per-file record §9 generalises), [PROP-053](../modules/vibe-workspace/PROP-053-clean-verb.xml) (verb chaining — §4.4 is the "own semantics ruling" its `##CHAIN-ONLY-INSTALL` demanded before any wider grammar).</related></p>
  <mandate title="1. The owner's mandate">
    <p><LIFE-MANDATE-VERBATIM fact="true" status="spec/done">The owner's words (2026-08-24), verbatim, abridged only of the courtesy framing: «Я хочу продумать аналог Lifecycle как то сделано в Maven. И сделать хуки в этот Lifecycle как это сделано в Maven и Babel. […] Ложится ли наш Lifecycle на Maven Lifecycle? Каких фаз у нас еще нет? Я хочу чтобы движок лайфсайклов был встроен прямо в Vibe, и так же как в Maven если мы делаем стадию более высокого уровня, то все предыдущие тоже выполняются. Но надо понимать разницу между нашим install и их install. Их install это компиляция, наш - это материализация в vibedeps. Поэтому наш install запускается в самом начале, первой фазой (при этом он должен быть строго инкрементальным - если vibe.toml не поменялся, инсталлировать пакеты не нужно). Наш аналог стадии install будет называться build и create. На стадии build будут собирать алгоритмически собираемые исходники (например, Rust). На стадии create - в этот момент будет активироваться какой-нибудь внешний агент или vibe-llm (если запущено из командной строки) либо если vibe вызван из агента - будут нашим хитрым механизмом передаваться промты для обеспечения агентного режима. Собственно, vibe build это довольно понятная детерминированная стадия, а вот create - это долго и непонятно, поэтому она одна из последних (но до package и deploy по аналогии с Maven). На этапе package мы соответственно будем собирать что-то типа статически собираемых скиллов, плагинов для Claude, инсталляторов для локально запускаемых утилит, и так далее. Deploy - это обычно раскатка на сервера. Я хочу сразу запроектировать две вещи. Во-первых, стандартный ритуал вызова стандартных методов как в Maven. Второе - так же как в Maven, возможность сделать пакет-плагин и добавить новую функциональность в этот ритуал.»</LIFE-MANDATE-VERBATIM></p>
    <p><LIFE-MANDATE-FULL-IR fact="true" status="spec/done">The owner's second ruling (2026-08-25), verbatim, on reviewing the first draft: «Я бы хотел, чтобы плагины компилятора имели полный доступ к IR пакетов и могли его изменять как хочешь. То есть, чтобы это был полноценное компиляторное расширение - так же как ты можешь писать новые оптимизации на бэкенде и новые фронтенды для LLVM. Да, это небезопасно, поэтому я предлагаю сделать отдельный флаг в vibe.toml который явно покажет, что плагин попросил доступ к такому низкоуровневому API. Я хочу чтобы пользователи могли прямо достроить компилятор поверх этого. Вероятно, под это нужно отрефакторить переходы в нашем IR и сделать всё высоко композируемым.» This ruling shapes §7's two-tier model, §7.5's IR refactor, and the pass tier's `compiler_internals` flag.</LIFE-MANDATE-FULL-IR></p>
    <p><LIFE-MANDATE-NO-CONSENT fact="true" status="spec/done">The owner's third ruling (2026-08-25), verbatim: «Я не хочу консентов. Если пользователь установил пакет - это само по себе уже консент. Есть множество компиляторов от Gradle и Webpack до Clang которые поддерживают всевозможные нативные расширения. Где ты видел, чтобы пользователь сидел и подтверждал эти разрешения? На модификацию графа? Да обычный пользователь вообще не разбирается, как это всё внутри работает, он пришел за работающими промтами. Что полезно сделать: полную прозрачность observability, чтобы отладчики, сканеры, специалисты по internals мгновенно могли понять, что там происходит.» And, on binary distribution: «…если кто-то захочет создать полностью закрытый плагин и не поставлять исходников, ему придется скомпилировать этот плагин подо все существующие в природе версии Rust, что кажется очень сложной задачей» — the fear this document answers by NOT being ABI-bound to Rust (§8.1: the C-ABI/VST posture, and §8.2's prebuilt closed-source form). These rulings shape §3.3's installation-is-consent law, §3.5's observability surface, and closed `##OPEN-PHASE-NAMES` (the nine-phase table stands as drafted) and `##OPEN-NATIVE-TRUST`.</LIFE-MANDATE-NO-CONSENT></p>
    <p><LIFE-MANDATE-VERBATIM-2 fact="true" status="spec/done">Continued, verbatim: «В качестве тестового сценария обработки стадии vibevm lifecycle я предлагаю какой-то механизм, который может определить плагин в своем манифесте, который программно будет что-то печатать - например, название стадии. […] Сейчас стоит посмотреть на то, какая информация попадает в обработку стадий в Maven и сделать это. Вторая иерархия мест расширения - это процесс компиляции, который аналогичен Babel. […] В данном случае копипастить бессмысленно с Babel не нужно, нужно только в общих чертах делать не хуже. В качестве тестового сценария плагина компилятора я предлагаю какой-нибудь простейший ТЕСТОВЫЙ алгоритм компрессии, например - в результирующем XML удалить все пробелы и переносы строк между тэгами […]. На самом деле, убирать пробелы и переносы - это плохая идея […], но это хороший пример для тестирования механики. В дальнейшем мы например, сделаем туда плагин компрессии по Caveman […]. Почему я рассказываю тебе про обе эти системы вместе? Потому что их дизайн должен быть сделан похоже и синергично, чтобы у пользователей не было ощущения разброда и нелогичности в разных форматах расширения VibeVM. Они должны быть взаимодополняющими, удобными, понятными. Также важная техническая деталь. Поскольку мы пишем не на Java или JavaScript как Maven или Babel, мы не можем просто так легко обрабатывать стадии скриптами. […] Должен быть еще один и самый продвинутый способ - что для lifecycle что для компилятора - это вызов нативного Rust кода. Я предлагаю систему, в которой библиотеки могут поставить код, который соберется в динамическую библиотеку, которую vibevm подхватит. Такие библиотеки, могут быть специфичны для проекта, поэтому собранные расширения нужно собирать inplace прямо внутри vibedeps либо внутри packages (если это "наш" пакет). Крайне важно, чтобы vibevm делала install инкрементально, потому что иначе она рискует стереть уже собранные нативные расширения […]. Нужна ревизия инкрементальности утилиты install, чтобы она не стирала всё перед установкой пакета, а накатывала только реально обновленную разницу. Эта инкрементальность - супер важный кусок, без которого всё будет слишком сильно тормозить, чтобы этим можно было пользоваться.»</LIFE-MANDATE-VERBATIM-2></p>
  </mandate>
  <prior-art title="2. Prior art, and where we stand against it">
    <maven-model title="2.1 The Maven model — what we adopt and what we do not">
      <p><MAVEN-MODEL fact="true" status="spec/done">Maven's model, from the primary source (the lifecycle introduction, read 2026-08-24): three built-in lifecycles — `default` (23 phases: validate, initialize, generate-sources … compile … test … package … verify, install, deploy), `clean` (pre-clean, clean, post-clean), `site`. Invoking a phase executes every prior phase of that lifecycle plus the phase itself; `mvn clean deploy` runs two lifecycles in sequence. A **phase** is a slot; a **plugin goal** is a task bound to a slot; *«if a build phase has no goals bound to it, that build phase will not execute»* — an empty phase is a free no-op. The `&lt;packaging&gt;` element selects a default goal-to-phase binding table (jar binds `compiler:compile` to compile, `surefire:test` to test, `jar:jar` to package…; `pom` packaging binds almost nothing). Plugins add goals via `&lt;executions&gt;` with per-execution `&lt;phase&gt;`, `&lt;configuration&gt;` and an id; multiple goals on one phase run in POM declaration order, packaging-bound goals first.</MAVEN-MODEL></p>
      <facts ordered="false">
        <ADOPT-FROM-MAVEN fact="true" status="spec/plan">**Adopted:** the phase line as the user-facing ritual (one short vocabulary that builds any vibe project); invoke-runs-all-priors; clean as a separate lifecycle composed by `vibe clean &lt;phase&gt;`; empty-phase-is-a-no-op; packaging-style default bindings selected by the manifest (§4.5); plugin executions with per-execution phase, id and config (§6).</ADOPT-FROM-MAVEN>
        <NOT-FROM-MAVEN fact="true" status="spec/plan">**Not adopted:** the 23-phase granularity (our default lifecycle has nine phases — the hyphenated `pre-*`/`process-*` intermediate slots are exactly the ones Maven itself says are *«not usually directly called from the command line»*, and we start without them; a future phase can be inserted behind its own ruling because phase order is a table, not an enum in stone); the `site` lifecycle (our "site" — the boot lane and generated docs — is already the product of install/compile, not a third lifecycle); Maven's unconditional re-execution (§4.3 makes every phase skip-when-fresh — the Gradle/Bazel property, and the owner's hard requirement).</NOT-FROM-MAVEN>
      </facts>
    </maven-model>
    <babel-model title="2.2 The Babel model — what we adopt and what we do not">
      <p><BABEL-MODEL fact="true" status="spec/done">Babel's model, from the primary source (the plugins reference, read 2026-08-24): compilation is parse → transform → generate; plugins are functions returning a `visitor` keyed by AST node type (`Identifier(path) { … }`), enabled by listing them in configuration; options ride as `["plugin-name", { …opts }]`; ordering is *«plugins run before presets; plugin ordering is first to last; preset ordering is reversed»*; a transform plugin auto-enables its syntax plugin; presets are named plugin groups.</BABEL-MODEL></p>
      <facts ordered="false">
        <ADOPT-FROM-BABEL fact="true" status="spec/plan">**Adopted:** transforms as declared, configured, ordered pipeline entries over the compiler's own IR; per-plugin options in the declaration; presets as named groups (our kind-presets, §4.5, serve both hierarchies).</ADOPT-FROM-BABEL>
        <NOT-FROM-BABEL fact="true" status="spec/plan">**Not adopted (per the mandate — "не хуже в общих чертах, без копипаста"):** the reversed-preset ordering rule (one rule for everything: declaration order, §3.4 — Babel's reversal is a compat artefact users must memorise); the per-node-type visitor as the FIRST-wave external plugin API — our tier-1 transform stages are document-grained (§7.2), because our compiler already runs its own visitors internally (qualify, the absorbed filter) and a document-grained transform crosses the native ABI in one call instead of thousands (§8.5); full-depth mutation is served by the pass tier (§7.4) as whole-IR-in/whole-IR-out, not by an interactive per-node visitor API — that interactive surface stays deferred behind this anchor.</NOT-FROM-BABEL>
      </facts>
    </babel-model>
    <llvm-model title="2.3 The LLVM/MLIR model — the pass tier's ancestry">
      <p><LLVM-MODEL fact="true" status="spec/done">The 2026-08-25 ruling names a third ancestor: LLVM — where an extension is not a consumer of fixed hook points but a **pass** over the compiler's own IR, ordered by a pass manager among the built-in passes, with custom frontends lowering new source languages INTO the IR and custom backends lowering it OUT. The closest structural match for us is actually **MLIR's multi-level shape**: our compilation is a progressive lowering through named representations (§7.4.1), transforms run *within* a level, lowerings step *between* levels, and a verifier runs between passes so a broken invariant fails at its author, not three passes later.</LLVM-MODEL></p>
      <p><ADOPT-FROM-LLVM fact="true" status="spec/plan">**Adopted (tier 2, §7.4):** the pass as the unit of extension; the pass manager with position-by-name (after/before/replace a built-in pass); frontends and backends as lowering passes anyone can add; the inter-pass verifier. **Not adopted:** LLVM's in-process C++ API surface as the plugin contract (no stable Rust ABI exists — §8.1's law stands; a pass crosses the C ABI as whole-IR-in / whole-IR-out JSON, §7.4.4) and LLVM's unversioned IR churn (our IR wire schema is versioned and gated, §7.4.5).</ADOPT-FROM-LLVM></p>
    </llvm-model>
    <webpack-model title="2.4 The Webpack model — the audit that found three gaps">
      <p><WEBPACK-MODEL fact="true" status="spec/done">Webpack (owner-supplied source, 2026-08-25): an **event-driven plugin-based compiler** — a lifecycle of hook events (compile → resolve → parse → bundle → post-process) where even core functionality ships as default plugins on the same hooks; **loaders** transform individual files (selected per-file by `test`/include/exclude rules, chained) while **plugins** work on the whole bundle at the end; the dependency graph is built by recursive resolution from an entry, topologically sorted, merged, then tree-shaken and minified; watch mode and persistent caching make rebuilds incremental; `stats`/bundle-analyzer attribute bundle size to modules.</WEBPACK-MODEL></p>
      <facts ordered="false">
        <WEBPACK-ALREADY-COVERED fact="true" status="spec/done">**Already ours, checked point by point:** core-as-plugins ⇔ §7.5's «built-ins are not privileged»; loaders-vs-plugins ⇔ tier 1's per-document stages (`compile:source`/`document`) versus whole-lane stages (`compile:lane`/`emitted`) — one machine where Webpack has two vocabularies; entry ⇔ the boot-snippet seed; import-order/cycles ⇔ the topo order and three-colour cycle walker PROP-035 already ships; code-splitting/lazy chunks ⇔ static-vs-dynamic lanes plus lazy-pull subskills, already the loading model; minification ⇔ §10.2; stats/profiling ⇔ §3.5. **Tree shaking we have by construction, not as an optimisation:** the closure is computed FROM the seed over authored edges, so unreachable content never enters the lane at all — Webpack prunes after the fact what our loading semantics never admits.</WEBPACK-ALREADY-COVERED>
        <WEBPACK-GAPS-TAKEN fact="true" status="spec/plan">**Three things the audit found missing, now specified:** per-contribution SELECTORS (the loader `test`-rule genre — §3.2 `##CONTRIB-SELECTOR`); the compile cache the refactor must not preclude (the persistent-cache/Turbopack lesson — §7.5 `##COMPILE-CACHE-READY`); and the lane analyzer (the bundle-analyzer genre bound to our tokenomics law — §3.5 `##OBS-LANE-ANALYZER`).</WEBPACK-GAPS-TAKEN>
      </facts>
    </webpack-model>
    <gap-map title="2.5 Does our lifecycle map onto Maven's? Which phases are missing?">
      <p><GAP-ANSWER fact="true" status="spec/done">The owner's direct question, answered. It maps — with three systematic shifts, not one-to-one. **Shift 1:** Maven's dependency resolution is not a phase (it happens before/lazily); ours IS the first real phase, because our `target/`-equivalent is the materialised prompt world itself. **Shift 2:** Maven's single `compile` splits in two for us — deterministic `build` and agentic `create` — because we have a producer Maven does not: the LLM. **Shift 3:** Maven re-runs everything; every phase of ours must be skip-when-fresh or the agentic/native costs make the ritual unusable.</GAP-ANSWER></p>
      <p><GAP-TABLE fact="true" status="spec/done">The correspondence, phase by phase (their name → ours → what exists today):</GAP-TABLE></p>
      <fence>Maven               vibe          exists today?
validate            validate      partially — `vibe check` is a rich lint VERB, not a phase;
                                  the phase is its cheap preflight subset (§4.2.1)
(resolution, no     install       YES — the whole PROP-009/011 machine; becomes phase 2
 phase in Maven)                  and gains the §9 diff-materialise revision
generate-sources    generate      NO as a phase — the genre exists (spec-driven codegen)
compile             build         partially — `vibe bin build` builds package binaries
                                  per-binary; no project build, no phase
test                test          NO as a phase — panels/conform/oracles exist as host
                                  tooling and package code, nothing project-ritualised
(no equivalent)     create        NO — the agentic producer; vibe-llm crate is an M0 stub
verify              verify        NO
package             package       partially — skill projection (`vibe skill`, mcp
                                  pkgskill) exists as verbs, not as a phase
install (their      —             not a vibe phase: their `install` publishes into the
 local repo)                      local repo; our machine cache (~/.vibe/cache,
                                  PROP-010) is fed by fetch, not by a build phase
deploy              deploy        NO
clean lifecycle     clean         YES — PROP-053, one phase, no pre-/post-
site lifecycle      —             deliberately absent (##NOT-FROM-MAVEN)</fence>
    </gap-map>
  </prior-art>
  <synergy title="3. One extension machine — the synergy law">
    <p><ONE-MACHINE fact="true" status="spec/plan">**Decision.** The lifecycle hooks and the compiler plugins are ONE machine with two families of extension points, not two machines. One declaration grammar, one context-envelope shape, one handler taxonomy, one trust gate, one ordering rule. A user who has written a lifecycle contribution knows how to write a compiler transform, and vice versa — this is the mandate's «похоже и синергично» made structural.</ONE-MACHINE></p>
    <points title="3.1 Extension points — one namespace, three families">
      <p><POINT-GRAMMAR fact="true" status="spec/plan">An **extension point** is a string `&lt;family&gt;:&lt;name&gt;`. Three families: **`phase:`** — the default-lifecycle phases and `clean` (project-scoped moments: `phase:build`, `phase:create`, …); **`compile:`** — the compiler's two tiers: the §7.2 staged positions (`compile:source`, `compile:document`, `compile:lane`, `compile:emitted`) and the §7.4 pass tier's single point `compile:pass`, whose position inside the pipeline is carried by the contribution's `pass = { … }` declaration; **`slot:`** — the per-package materialisation moments that already exist as PROP-020 (`slot:pre-install`, `slot:post-install`), re-stated in this vocabulary without changing their semantics, timing, or manifest spelling ([`[hooks]`](../modules/vibe-workspace/PROP-020-install-hooks.xml#manifest) keeps working verbatim; it is now sugar for two `slot:` contributions).</POINT-GRAMMAR></p>
    </points>
    <contributions title="3.2 Contributions — the [[extension]] table">
      <p><CONTRIB-GRAMMAR fact="true" status="spec/plan">A **contribution** binds a handler to a point. It is declared as an `[[extension]]` table — in a **package** manifest (the package ships and offers the behaviour) or in the **project** manifest (the host adds its own). The shape is one grammar for every family:</CONTRIB-GRAMMAR></p>
      <fence lang="toml">[[extension]]
id      = "announce"                  # unique within the declaring manifest
point   = "phase:build"               # family:name
handler = { kind = "builtin", name = "log" }
config  = { message = "phase={phase} in {project}" }   # free TOML, passed through

[[extension]]
id      = "xml-squeeze"
point   = "compile:emitted"
handler = { kind = "native", crate_dir = "ext/xml-squeeze" }
auto    = false                       # compile-family default; see ##AUTO-BY-FAMILY</fence>
      <facts ordered="false">
        <CONTRIB-FIELDS fact="true" status="spec/plan">Fields: `id` (execution identity — Maven's execution id; also the disable/override key), `point`, `handler` (§6), `config` (free TOML delivered to the handler verbatim — Babel's plugin options), `auto` (§3.3), and optionally `when` (a guard: `kind = "rust-stack"`-style applicability conditions, future work behind this anchor).</CONTRIB-FIELDS>
        <MULTI-EXEC fact="true" status="spec/plan">The same handler may be bound more than once with different `id`s and configs — Maven's multiple-executions property, verbatim need (e.g. `log` at `phase:validate` and again at `phase:package`).</MULTI-EXEC>
        <CONTRIB-SELECTOR fact="true" status="spec/plan">**Selectors** (the Webpack loader `test`-rule genre, 2026-08-25 audit): a contribution may carry `applies_to = { packages = ["org.x/*"], paths = ["vibevm/vibespecs/prompts/**"] }` — declarative include/exclude scoping evaluated by the ENGINE, so a per-document transform (tier-1 `compile:source`/`document`) or a per-slot moment runs only where declared, visible to §3.5's registry, instead of every plugin re-implementing filtering privately in `config`. Absent selector = applies everywhere its point fires, today's behaviour.</CONTRIB-SELECTOR>
      </facts>
    </contributions>
    <activation title="3.3 Activation — installation is consent; configuration is not a permission slip">
      <facts ordered="false">
        <INSTALL-IS-CONSENT fact="true" status="spec/plan">**Installing a package IS the consent to run its extensions** (owner ruling 2026-08-25, `##LIFE-MANDATE-NO-CONSENT`). No allow-lists, no first-run prompts, no `--allow-…` flags — the Gradle/Webpack/Clang posture: an ecosystem of native extensions where the trust decision is made once, at dependency selection, like `build.rs` and `postinstall` before us. The safety model is not permission dialogs but **total observability** (§3.5). At build time this obligates an amendment at PROP-020 §2.3 (its allow-list + first-run-consent + non-interactive-abort machinery is superseded by this law for all handler kinds; statuses moved at their anchors, nothing renamed).</INSTALL-IS-CONSENT>
        <AUTO-BY-FAMILY fact="true" status="spec/plan">**A dependency's `phase:`/`slot:` contributions are active once the package is installed** — the owner's test scenario verbatim: a plugin declares in ITS OWN manifest and prints. **A dependency's `compile:` contributions default to `auto = false` — they activate by a host manifest line.** The asymmetry is CONFIGURATION, not a permission gate: a compile transform shapes the host's own artifact (which transforms produce MY lane is the host's choice, like `spec_format` is), while a phase contribution performs the package's own declared job. A package MAY declare `auto = true` on a compile contribution; it is ignored with a warning — the shape of the host's lane is the host's word.</AUTO-BY-FAMILY>
        <HOST-ACTIVATION fact="true" status="spec/plan">**Host activation and override.** The consuming manifest activates a dependency's non-auto contribution, or re-configures an auto one, by reference: `[[extension.use]] ref = "org.vibevm.x/y#xml-squeeze"` with optional `config` override; `[extensions] disable = ["org.vibevm.x/y#announce"]` turns any contribution off. The reference key is `&lt;group&gt;/&lt;name&gt;#&lt;id&gt;` — stable, printable, greppable.</HOST-ACTIVATION>
        <SURFACE-THE-RITUAL fact="true" status="spec/plan">**Nothing runs invisibly.** Every phase run prints the contributions it will execute (id, point, handler kind, providing package) before running them — the narrator genre the tooling already speaks (`[k/n]` lines). A ritual whose steps are secret is not a ritual. The full instrument rack is §3.5.</SURFACE-THE-RITUAL>
      </facts>
    </activation>
    <ordering title="3.4 Ordering — one rule everywhere">
      <p><ORDER-LAW fact="true" status="spec/plan">Within one extension point, contributions run in this order: **(1)** built-in bindings of the effective kind-preset (§4.5) — Maven's «packaging first»; **(2)** dependency-declared contributions, in lockfile package order (deterministic, no filesystem enumeration), then declaration order within a package; **(3)** host-manifest contributions and `[[extension.use]]` activations, in declaration order. First-to-last, no reversals anywhere — one rule a user can hold in their head, against Babel's plugins-vs-presets special case (`##NOT-FROM-BABEL`).</ORDER-LAW></p>
    </ordering>
    <observability title="3.5 Observability — the safety model is glass, not gates">
      <p><OBS-LAW fact="true" status="spec/plan">**The owner's trade (2026-08-25): no consent dialogs, total transparency instead** — debuggers, scanners, and internals specialists must be able to see instantly what the extension machine is doing. Observability is therefore a REQUIREMENT of the machine, specified with it, not a later courtesy:</OBS-LAW></p>
      <facts ordered="false">
        <OBS-REGISTRY fact="true" status="spec/plan">**The extension registry is a query surface:** `vibe extensions` (with `--json`) lists every declared contribution in the installed world — id, point, handler kind, providing package and version, config, `compiler_internals` flag, auto/host-activated state, disabled state, and for natives the artifact path + build state + content hash. One command answers "what runs on my project and who brought it". `vibe list`/`show` mark internals-bearing packages, as §7.4.3 already requires.</OBS-REGISTRY>
        <OBS-STATIC-SCANNABLE fact="true" status="spec/plan">**Everything a plugin CAN do is declared, so a scanner needs no execution:** points, pass positions, formats, the internals flag, prebuilt artifact paths — all in manifests, greppable in the materialised world. A security scanner (the PROP-020 §4 "LLM antivirus" future) reads the whole capability surface statically; the machine adds no runtime-discovered powers.</OBS-STATIC-SCANNABLE>
        <OBS-TRACE fact="true" status="spec/plan">**Compilation is traceable, LLVM-style:** `--trace-compile` (or `[compile] trace = true`) dumps the IR snapshot after every pass — built-in and plugin alike — to `.vibe/trace/&lt;run&gt;/NN-&lt;pass&gt;.json`, the `-print-after-all` genre; a per-pass timing table (the `-time-passes` genre) prints with the run. Diffing two adjacent snapshots answers "what did THIS pass change" in seconds — the debugger's and the internals specialist's first instrument.</OBS-TRACE>
        <OBS-LANE-ANALYZER fact="true" status="spec/plan">**The lane analyzer** (the bundle-analyzer genre, 2026-08-25 audit): a report attributing the compiled lane's weight — bytes and estimated tokens — per node, per package, per pass-delta (what each transform added or saved), answering «кто сколько приносит в лейн». This is [PROP-048](PROP-048-tokenomics.xml)'s tokenomics law given its instrument: the token budget is a design constraint, so its spend must be measurable at a glance. Surfaced as `vibe extensions analyze` (or a `vibe show lane` view) with `--json`.</OBS-LANE-ANALYZER>
        <OBS-RUN-RECORD fact="true" status="spec/plan">**Every run leaves a record:** the phase narration (`##SURFACE-THE-RITUAL`), the generated artifact's transforms/pass header (§7.1, §7.4.3), and `.vibe/lifecycle.toml`'s per-execution fingerprints, timings and statuses — so "what ran, in what order, on what inputs, how long" is answerable after the fact without re-running.</OBS-RUN-RECORD>
      </facts>
    </observability>
  </synergy>
  <lifecycle title="4. The vibe lifecycle">
    <shape title="4.1 Two lifecycles, nine default phases">
      <p><LIFECYCLES fact="true" status="spec/plan">vibe has **two** lifecycles. **`clean`** — one phase, exactly PROP-053. **`default`** — nine phases, in this fixed order:</LIFECYCLES></p>
      <fence>validate → install → generate → build → test → create → verify → package → deploy</fence>
      <p><INVOKE-RUNS-PRIORS fact="true" status="spec/plan">`vibe &lt;phase&gt;` executes every default-lifecycle phase up to and including the named one, in order — the Maven ritual verbatim. `vibe clean &lt;phase&gt;` runs the clean lifecycle first, then the default lifecycle up to `&lt;phase&gt;` (§4.4). A phase with no contributions and no built-in binding **completes as a no-op** — Maven's empty-phase law, which is what makes nine slots cost nothing for a prompt-only project (its `deploy` chain degenerates to validate+install).</INVOKE-RUNS-PRIORS></p>
      <p><ENGINE-ALGORITHM fact="true" status="spec/plan">**The engine, as an algorithm** (normative; written out so no implementer has to reverse-engineer it from prose):</ENGINE-ALGORITHM></p>
      <fence>run(requested_phase, pkgrefs, flags):
  1  root      = resolve_project_root(flags.path)          # refuse outside a vibe project
  2  manifest  = parse vibe.toml; world = read vibe.lock   # both may be absent for validate
  3  chain     = [validate .. requested_phase]             # §4.1 order; clean prepended if asked
  4  contribs  = collect():                                #   for EVERY phase in chain
       a. kind-preset built-ins (§4.5)                     #   order rule §3.4: (a) then (b) then (c)
       b. [[extension]] of installed packages, lock order
       c. host [[extension]] + [[extension.use]], decl order
       drop disabled; apply selectors (##CONTRIB-SELECTOR)
  5  print the ritual: every (phase, execution) to run     # ##SURFACE-THE-RITUAL
  6  for phase in chain:
       state = read .vibe/lifecycle.toml
       for exec in contribs[phase]:
         fp = fingerprint(exec)                            # §4.3: inputs hash
         if !flags.force and state[exec].fingerprint == fp:
             report "fresh"; continue
         reply = dispatch(exec, envelope(...))             # §5, §6 by handler kind
         record fingerprint, timing, status, artifacts
         if reply.status == fail: stop per ##FAILURE-BY-PHASE
         if reply.status == delegated: park + exit         # §6.5, agent mode only
  7  write .vibe/lifecycle.toml; exit 0</fence>
    </shape>
    <phases title="4.2 What each phase means">
      <facts ordered="false">
        <PHASE-VALIDATE fact="true" status="spec/plan">**`validate`** — the cheap preflight: the manifest parses, the role/epoch/format are supported, declared extension references resolve to installed packages. Seconds, no network, no LLM. It is NOT `vibe check` (which stays a standalone diagnostic verb, our `mvn site`-grade tool); the phase is the subset whose failure makes every later phase meaningless.</PHASE-VALIDATE>
        <PHASE-INSTALL fact="true" status="spec/plan">**`install`** — the whole existing PROP-009/PROP-011 machine: resolve, fetch, materialise `vibevm/vibedeps/`, regenerate the boot lane (which is where the `compile:` family fires, §7.1), write the lock. **Strictly incremental by the owner's law:** manifest unchanged + lock satisfied + sources fresh ⇒ the phase reports fresh and does nothing (`##EMPTY-REQUIRES-IS-A-NO-OP` and the PROP-011 freshness gate already carry most of this; §9 closes the remaining hole — the per-file diff). Runs FIRST among real phases because everything downstream reads the world it materialises.</PHASE-INSTALL>
        <PHASE-GENERATE fact="true" status="spec/plan">**`generate`** — derived *sources* from specs/prompts (the spec-driven codegen genre the host already practises as `cargo xtask codegen`): K6.5-table→constants, schema→types, prompt-catalog→registry. Empty by default; stack presets and plugins populate it. Sits between install (needs the world) and build (produces what build compiles).</PHASE-GENERATE>
        <PHASE-BUILD fact="true" status="spec/plan">**`build`** — the deterministic, algorithmic build. Built-in binding: build every installed package's `[[binary]]` (PROP-025's `build_binary`, today per-name behind `vibe bin build`) and every declared native extension (§8) — in-slot, incremental via cargo's own fingerprinting (which §9's mtime preservation makes work). Stack presets add the project's own build (`cargo build` for the rust stack, `npm`/`tsc` for typescript, `go build` for go). Understandable, repeatable, cacheable — the owner's words.</PHASE-BUILD>
        <PHASE-TEST fact="true" status="spec/plan">**`test`** — deterministic checks: stack preset binds the language's test runner; discipline packages contribute their conform/oracle/tcg gates as plugin contributions (this is exactly the owner's «на вызов фазы типа vibe test должно быть возможно повесить алгоритмический хук или вызов агента»). No LLM by default.</PHASE-TEST>
        <PHASE-CREATE fact="true" status="spec/plan">**`create`** — the agentic producer, our phase Maven does not have. Long, non-deterministic, expensive — therefore LATE (after test: the deterministic world is proven before tokens are spent) but before package/deploy (its outputs are what gets packaged). Contributions are `handler = { kind = "agent" }` executions (§6.4): from a terminal, `vibe-llm` drives them; under an agent host, the §6.5 handshake delegates them to the hosting agent. Skip-when-fresh is VITAL here — a create whose inputs (prompt + spec fingerprint) are unchanged must not re-spend tokens (§4.3).</PHASE-CREATE>
        <PHASE-VERIFY fact="true" status="spec/plan">**`verify`** — the quality gate over build+create outputs, before anything is packaged: checksums, contract assertions, and (as plugins) LLM-judge checks. Empty by default; exists so that create's non-determinism has a named gate in front of package, the same reason Maven puts verify before install/deploy.</PHASE-VERIFY>
        <PHASE-PACKAGE fact="true" status="spec/plan">**`package`** — assemble distributables: statically-assembled skills, Claude plugins/skill bundles, installers for locally-run utilities, zips of the §C9 genre. Built-in bindings arrive with the packaging work (skill projection is the first candidate); plugins extend.</PHASE-PACKAGE>
        <PHASE-DEPLOY fact="true" status="spec/plan">**`deploy`** — push the packaged artifacts out: servers, registries, marketplaces. Empty by default, plugin-populated; red lines (publishing, tokens, PROP-000 §20) bind exactly as they bind everywhere else.</PHASE-DEPLOY>
      </facts>
    </phases>
    <freshness title="4.3 The incremental law — every phase is skip-when-fresh">
      <facts ordered="false">
        <PHASE-FINGERPRINT fact="true" status="spec/plan">**Every phase execution owns a fingerprint** — a hash over its declared inputs (for install: manifest+sources, already built; for build: the slot sources + project sources the preset names; for create: the prompt documents + their spec closure). A phase whose fingerprint matches the recorded one **skips**, reporting `fresh` per contribution — the Gradle up-to-date property grafted onto the Maven ritual. This is the design answer to «если vibe.toml не поменялся, инсталлировать пакеты не нужно», generalised to all nine phases because create (tokens) and build (native compile time) are even more expensive than install.</PHASE-FINGERPRINT>
        <PHASE-STATE-HOME fact="true" status="spec/plan">Fingerprints and last-run records live in `.vibe/lifecycle.toml` at the workspace root — machine state beside the project settings that already live in `.vibe/`, inside the shippable-tree denylist (PROP-024 §2.2), never committed, never hashed. `vibe clean` does NOT remove it (it describes work, it is not derived prompt state); a `--force` flag on any phase ignores it for one run.</PHASE-STATE-HOME>
        <FRESHNESS-IS-PER-CONTRIBUTION fact="true" status="spec/plan">Freshness is judged **per contribution**, not only per phase: one stale execution re-runs alone; its peers stay skipped. A handler MAY declare extra inputs in its manifest entry (`inputs = ["spec/prompts/**"]`); a handler that declares none is conservatively re-run whenever the phase's own inputs move.</FRESHNESS-IS-PER-CONTRIBUTION>
      </facts>
    </freshness>
    <chaining title="4.4 The command grammar — PROP-053's chain, generalised by its own rule">
      <p><CHAIN-GENERAL fact="true" status="spec/plan">**`vibe [clean] &lt;phase&gt; [&lt;pkgref&gt;…] [flags]`.** Any default-lifecycle phase may be named; `clean` before it runs the clean lifecycle first. `vibe clean install org.x/y --offline` keeps its exact PROP-053 semantics (wipe → world from lock → refresh the named) as the special case it already was. This section IS the «own semantics ruling» PROP-053 `##CHAIN-ONLY-INSTALL` required before any wider grammar — that anchor is superseded by this one **at build time, by a status move and a successor note beside it, never by deleting or renaming it**. Pkgrefs remain meaningful only to the install phase (they scope what refreshes); other phases take flags, not pkgrefs, until a ruling says otherwise.</CHAIN-GENERAL></p>
      <p><EXISTING-VERBS-STAY fact="true" status="spec/plan">Existing verbs are untouched: `vibe check`, `vibe bin build`, `vibe skill`, `vibe cache …` keep their meanings as directly-invoked tools — Maven's «a goal not bound to any build phase could be executed outside of the build lifecycle by direct invocation». `vibe install` the verb IS the install phase invoked directly (one implementation, two spellings of the same thing). Mixing direct verbs into a phase chain on one command line (Maven's `mvn clean dependency:copy-dependencies package`) is **not** adopted now — each verb keeps its own invocation; revisit behind this anchor if the need shows.</EXISTING-VERBS-STAY></p>
    </chaining>
    <presets title="4.5 Kind-presets — our packaging element">
      <facts ordered="false">
        <PRESET-LAW fact="true" status="spec/plan">The manifest's existing `kind` (flow, feat, stack, tool, mcp, lang) plus the installed stack select a **binding preset** — the table of built-in contributions, exactly Maven's packaging→bindings table. A prompt-only flow package binds nothing beyond validate/install (the `pom`-packaging analogue); a project with the rust stack installed gets generate/build/test bound to the cargo genre; a skill-bearing package gets a package-phase binding. Presets are DATA (a table shipped in vibe or contributed by a stack package), not code branches.</PRESET-LAW>
        <STACK-CONTRIBUTES-PRESET fact="true" status="spec/plan">A stack package may ship its preset as a set of `[[extension]]` contributions in its own manifest (auto, trust-gated like everything else) — this is how `rust-ai-native-lang` teaches `vibe build`/`vibe test` to drive cargo without vibe hardcoding cargo, the OOS-AUTODETECT posture of PROP-024 kept intact: the package declares, vibe never infers.</STACK-CONTRIBUTES-PRESET>
      </facts>
    </presets>
  </lifecycle>
  <context title="5. The context envelope — what a contribution receives">
    <p><ENVELOPE-LAW fact="true" status="spec/plan">Every handler, in every family, receives one **context envelope** — a versioned JSON document (the wire form for `script`/`binary`/`native` handlers; builtin handlers get the same data as a Rust struct; agent handlers get its prose projection). Modelled on what Maven hands a Mojo — MavenProject, MavenSession, MojoExecution, per-parameter configuration — translated to our world and extended with what the owner predicted: «в стадии будет попадать много больше, чем их просто название».</ENVELOPE-LAW></p>
    <fence lang="json">{
  "envelope": 1,
  "point": "phase:build",
  "execution": { "id": "announce", "package": "org.vibevm.x/y", "config": { } },
  "project": { "root": "…", "name": "…", "version": "…", "kind": "…",
               "manifest": "vibe.toml", "spec_roots": ["vibevm/vibespecs"] },
  "world": { "lockfile": "vibe.lock", "deps_root": "vibevm/vibedeps",
             "packages": [ { "group": "…", "name": "…", "version": "…",
                             "kind": "…", "slot": "vibevm/vibedeps/…" } ] },
  "run": { "requested": "package", "chain": ["validate","install","…"],
           "phase": "build", "offline": false, "assume_yes": false,
           "agent_mode": "cli", "force": false },
  "artifacts": [ { "id": "…", "phase": "build", "path": "…", "kind": "…" } ],
  "io": { "scratch": ".vibe/lifecycle/&lt;run&gt;/&lt;execution&gt;/" }
}</fence>
    <facts ordered="false">
      <ENVELOPE-MAPPING fact="true" status="spec/done">The Maven correspondence, so the translation is checkable: `project` ↔ MavenProject (coordinates, dirs, model); `world` ↔ the resolved dependency set + session repository view; `run` ↔ MavenSession (request, properties, offline flag); `execution` ↔ MojoExecution (id + configuration); `artifacts` ↔ attached artifacts; `io.scratch` ↔ the plugin's work directory convention.</ENVELOPE-MAPPING>
      <ARTIFACT-REGISTRY fact="true" status="spec/plan">**The artifact registry is how phases talk.** A contribution's reply may declare artifacts (`{"artifacts":[{"id","path","kind"}]}`); later phases receive the accumulated list (create's outputs are what package packs). The registry is part of the run, persisted into `.vibe/lifecycle.toml` for freshness accounting — never a hidden channel.</ARTIFACT-REGISTRY>
      <REPLY-SHAPE fact="true" status="spec/plan">The **reply envelope** (same versioning): `{"status": "ok"|"fail"|"skip", "message": "…", "artifacts": […], "tasks": […]}` — `tasks` is non-empty only from agent handlers under the §6.5 handshake. `script` handlers reply by exit code + a `VIBE_REPLY` file in scratch (streams stay human); `binary`/`native` reply with the JSON itself.</REPLY-SHAPE>
      <COMPILE-ENVELOPE fact="true" status="spec/plan">The `compile:` family rides the SAME envelope with a `payload` member instead of `artifacts`/`run`-chain: `{"payload": {"stage": "emitted", "format": "xml", "bytes_b64"| "text": …, "doc": …}}`, reply `{"status", "payload"}` — one shape, two families, which is the synergy law §3 made mechanical.</COMPILE-ENVELOPE>
      <FAILURE-BY-PHASE fact="true" status="spec/plan">**Failure semantics generalise PROP-020 §2.5:** a failing contribution in validate/install/generate/build/test **stops the chain** (the phase reports which execution failed); a failing create/verify contribution stops before package (nothing half-created is packaged); package/deploy failures stop the chain trivially (they are last). `slot:pre-install` keeps its rollback; `slot:post-install` keeps installed-but-flagged. A `skip` reply is bookkept, never an error.</FAILURE-BY-PHASE>
    </facts>
  </context>
  <handlers title="6. The handler taxonomy — five kinds, two of them already ours">
    <p><HANDLER-KINDS fact="true" status="spec/plan">A contribution's `handler.kind` is one of five. Two wrap machinery that exists; three are new. All five are legal at every `phase:`/`slot:` point; the `compile:` family accepts `builtin` and `native` only (§8.5 explains why).</HANDLER-KINDS></p>
    <facts ordered="false">
      <H-BUILTIN fact="true" status="spec/plan">**`builtin`** — a named handler compiled into vibe (`log`, the §10.1 test bearer; future: `copy`, `zip`, `checksum`). Zero trust surface, zero build cost; the vocabulary grows only by shipping vibe.</H-BUILTIN>
      <H-SCRIPT fact="true" status="spec/plan">**`script`** — a base path resolved `.sh`/`.ps1` with the PROP-020 §2.2 interpreter ladder, environment (`VIBE_*`) and streams, unchanged — plus the envelope on `VIBE_CONTEXT` (a file path in scratch, not an env blob). The existing `[hooks]` table compiles to two `script` contributions at `slot:` points; nothing published breaks.</H-SCRIPT>
      <H-BINARY fact="true" status="spec/plan">**`binary`** — a `[[binary]]` name from an installed package (PROP-025): built if missing (PROP-025's build flow; its build-consent prompt is superseded by `##INSTALL-IS-CONSENT` at build time — narrated, never asked), then invoked with the envelope on stdin, reply on stdout. The right kind for per-run tools where process-spawn cost is irrelevant.</H-BINARY>
      <H-NATIVE fact="true" status="spec/plan">**`native`** — a cdylib loaded in-process (§8): built from `handler.crate_dir` inside the providing package, or shipped prebuilt per platform for closed-source plugins (§8.2 `##PREBUILT-CLOSED`). The advanced tier the mandate names; required for `compile:` (per-lane call frequency) and available to `phase:` (long-lived state, no spawn cost).</H-NATIVE>
      <H-AGENT fact="true" status="spec/plan">**`agent`** — a prompt-bearing execution: `handler = { kind = "agent", prompt = "spec://…" }`, the prompt being a spec document the package ships. Executed by §6.4/§6.5 depending on `run.agent_mode`. The only handler kind that may spend tokens, and the reason create is late in the line.</H-AGENT>
    </facts>
    <agent-cli title="6.4 Agent handlers from a terminal — vibe-llm">
      <p><AGENT-CLI fact="true" status="spec/plan">In `agent_mode = "cli"` (a human at a terminal, no agent host detected), agent executions run through **`vibe-llm`** (the workspace crate; today an M0 stub, its `LLMProvider` trait the seam): resolve the prompt address against the materialised world, assemble the envelope's prose projection, call the configured provider, write outputs to the declared paths, validate the declared artifacts exist. No provider configured ⇒ the execution fails with remediation («configure [llm] or run under an agent»), never a silent skip — the PROP-020 no-silent-skip posture.</AGENT-CLI></p>
    </agent-cli>
    <agent-handshake title="6.5 Agent handlers under an agent — the delegation handshake">
      <p><AGENT-HANDSHAKE fact="true" status="spec/plan">In `agent_mode = "agent"` (detected via env — the CLAUDECODE/VIBE_AGENT genre — or forced by `--agent-mode`), vibe does NOT call an LLM. It **parks the work and hands it to the hosting agent**: for each agent execution it writes a task file under `.vibe/agentic/outbox/&lt;run&gt;/&lt;execution&gt;.md` — the resolved prompt, the envelope's prose projection, and the output contract (paths + acceptance) — then prints a fenced `vibe-agent-tasks` block naming the run id and every task file, and exits the phase with status `delegated` (chain pauses; nothing downstream runs). The hosting agent performs the tasks, writes the outputs, and re-invokes the same phase: vibe finds the outputs, validates them against the contract, marks the executions done and the chain continues — resumability falls out of §4.3's fingerprints, no special resume verb. The parking place and the relay posture are [PROP-018](PROP-018-agentic-standalone-modes.xml)'s agentic-relay genre (`.vibe/agentic/`, `vibe agentic explain`, the dual CLI/MCP transport) extended from one command to a phase's worth of tasks; this gives that genre a wire contract. An MCP surface for the same handshake (the vibevm MCP server exposing `lifecycle_run`/`lifecycle_tasks`) is the second consumer of the same files, not a second mechanism.</AGENT-HANDSHAKE></p>
    </agent-handshake>
  </handlers>
  <compiler title="7. The compiler extension points — two tiers: staged transforms and full passes">
    <where title="7.1 Where the compiler runs, and whose word activates a transform">
      <p><COMPILE-SITE fact="true" status="spec/done">The spec compiler (PROP-035 §8) runs inside boot-lane regeneration — today a step of install — with the fixed internal pipeline: parse/topo of the `#use` graph → `#source` merge → `#embed` expansion → per-node qualification + absorbed-node filtering + cross-node short-link resolution → marker-wrapped emit. Those internal passes ARE visitors in the Babel sense; they stay internal and ordered by the spec, not pluggable — determinism of the boot lane is a PROP-009 constitutional property.</COMPILE-SITE></p>
      <p><COMPILE-ACTIVATION fact="true" status="spec/plan">Transforms are activated by the manifest of the workspace whose lane is being compiled (host manifest for the host lane, package manifest for a package's own lane) — `##AUTO-BY-FAMILY`'s host-opt-in rule. The activated list, in §3.4 order, is recorded in the generated artifact's header comment (`&lt;!-- vibe:transforms … --&gt;`) so a reader of STATIC.xml can see what touched it — the same honesty rule as `##SURFACE-THE-RITUAL`.</COMPILE-ACTIVATION></p>
    </where>
    <stages title="7.2 Tier 1 — the four staged transforms">
      <facts ordered="false">
        <STAGE-SOURCE fact="true" status="spec/plan">**`compile:source`** — one document's raw text before parsing (the preprocessor seam; Babel's parse-options analogue). Payload: text + its spec address. May rewrite text; may not change the address.</STAGE-SOURCE>
        <STAGE-DOCUMENT fact="true" status="spec/plan">**`compile:document`** — one document's parsed tree (the specdoc/DocTree JSON projection) after parse, before it enters the closure. The transform-plugin analogue at document grain: rewrite prose, drop or annotate nodes, normalise vocabulary. One call per document, whole-tree payload.</STAGE-DOCUMENT>
        <STAGE-LANE fact="true" status="spec/plan">**`compile:lane`** — the assembled lane as an ordered node list (origin, slug, body per node) after the internal pipeline, before serialisation. Where cross-cutting compression genres (the future Caveman pass) live: they see everything that will ship, in final order, still structured.</STAGE-LANE>
        <STAGE-EMITTED fact="true" status="spec/plan">**`compile:emitted`** — the serialised artifact bytes (per artifact: STATIC.xml, STATIC.md, INLINE.*) as the last word before the file is written. Byte-to-byte transforms; the §10.2 minifier's home.</STAGE-EMITTED>
        <NO-CLOSURE-HOOK fact="true" status="spec/plan">**Within tier 1 there is deliberately NO closure/graph access** — a staged transform cannot add or remove `#use` edges or change which nodes enter the lane. What loads stays decided by authored directives, auditable from sources alone; a transform that could silently widen the closure would be a prompt-injection lever. Full IR access — closure included — EXISTS, but only in tier 2, behind the `compiler_internals` flag, host activation, and the header record (§7.4.3): the capability is not denied, it is priced.</NO-CLOSURE-HOOK>
      </facts>
    </stages>
    <oracle title="7.3 Transforms versus the oracles">
      <p><TRANSFORM-VS-ORACLE fact="true" status="spec/plan">The byte-stability oracles (convert-source round-trips, `check-codegen`-style diffs) judge the UNtransformed compile — transforms apply after the oracle's reference output is taken, and the recorded transform list (§7.1) is part of the artifact's identity (a changed transform set ⇒ the lane is stale, regenerate). Otherwise every transform would read as corruption to the very instruments that keep the compiler honest.</TRANSFORM-VS-ORACLE></p>
    </oracle>
    <pass-tier title="7.4 Tier 2 — the pass tier: full IR access behind compiler_internals">
      <p><PASS-TIER-LAW fact="true" status="spec/plan">**Decision (owner ruling 2026-08-25).** A plugin may be a **full compiler extension** — a pass with complete access to the packages' IR, free to change it arbitrarily: new optimisations, new frontends, new backends, the LLVM posture verbatim. Users can build the compiler onward from here. The price of the power is explicitness: the pass tier exists only behind a dedicated manifest flag, only under host activation, only in-process (`builtin`/`native`), and always on the record (§7.4.3).</PASS-TIER-LAW></p>
      <ir-levels title="7.4.1 The IR — five named levels, lowered progressively">
        <p><IR-LEVELS fact="true" status="spec/plan">The compiler's IR is **multi-level** (the MLIR shape, because that is what our compilation already is — a progressive lowering): **`source`** — one document's raw text plus its spec address; **`document`** — one document's parsed tree (sections, facts, directives, anchors); **`closure`** — the whole compilation unit: every node reachable from the seed, topo-ordered, with the `#use`/`#source`/`#embed` graph, origins and rename state — *this is "the packages' IR" of the mandate: every package's contribution, visible and mutable at once*; **`lane`** — the assembled artifact as an ordered node list with marker/provenance metadata, ready to serialise; **`emitted`** — the serialised bytes per artifact. Today these representations exist implicitly inside `compile_static_inner`; §7.5 makes them explicit types with named transitions.</IR-LEVELS></p>
      </ir-levels>
      <pass-kinds title="7.4.2 Pass kinds — transform, lowering, frontend, backend">
        <facts ordered="false">
          <PASS-TRANSFORM fact="true" status="spec/plan">**Transform pass** — level N → level N (an optimisation): `pass = { kind = "transform", level = "closure", after = "qualify" }`. Position is by built-in pass name (`after` / `before`), exactly LLVM's pass ordering; tier 1's four stages are, mechanically, transform passes pinned to canonical positions — one machine, two doors.</PASS-TRANSFORM>
          <PASS-LOWERING fact="true" status="spec/plan">**Lowering pass** — level N → level N+1. The built-in chain IS a lowering sequence: `parse` (source→document), `close` (documents→closure, the topo/merge/embed complex), `assemble` (closure→lane), `emit` (lane→emitted). A plugin lowering **replaces or augments** a named built-in: `pass = { kind = "lowering", replace = "emit", artifact = "static-xml" }`.</PASS-LOWERING>
          <PASS-FRONTEND fact="true" status="spec/plan">**Frontend** — a `parse`-position lowering registered for new source formats: `pass = { kind = "frontend", formats = ["adoc"] }` receives `source`, returns `document`. The address grammar treats a registered extension exactly as it treats `.md`/`.xml` (canonical doc paths strip it), so a new format's documents are first-class `spec://` citizens.</PASS-FRONTEND>
          <PASS-BACKEND fact="true" status="spec/plan">**Backend** — an `emit`-position lowering: receives `lane`, returns artifact bytes — a new lane serialisation (JSON lane, compact lane, HTML projection) or a replacement of a built-in emitter for one named artifact. Built-in emit passes carry stable names (`emit:static-xml`, `emit:static-md`, `emit:index`, `emit:inline`) so replacement is precise.</PASS-BACKEND>
        </facts>
      </pass-kinds>
      <the-flag title="7.4.3 The flag, and the trust posture of full access">
        <p><COMPILER-INTERNALS-FLAG fact="true" status="spec/plan">A contribution enters the pass tier by declaring **`compiler_internals = true`** on its `[[extension]]` table — the owner-mandated flag, a single conspicuous boolean whose presence means "this plugin asked for the low-level API". Without it, `pass = …` is a validation error. With it: **host activation is always required** (`auto` is ignored for this tier — stricter than tier 1's compile rule, same direction); the activated pass list, positions included, is recorded in the generated artifact's header (`&lt;!-- vibe:transforms … --&gt;` gains the pass entries); and the install/build narration names each internals-plugin before it runs. The flag rides the package manifest, so `vibe list`/`show` can surface "this dependency contains compiler-internals extensions" before anything activates.</COMPILER-INTERNALS-FLAG></p>
        <p><INTERNALS-TRUST fact="true" status="spec/done">The tier is `builtin`/`native` only (§8.5's reasons apply with more force). **The flag is a declaration, not a permission dialog** (owner ruling 2026-08-25, `##INSTALL-IS-CONSENT`): no consent prompt guards it — its job is to make the capability VISIBLE (to `vibe extensions`, to `vibe list`, to static scanners, to a human reading the manifest) before anything runs. Installation plus the host's activation line are the whole authorisation, the Clang-plugin posture; the safety spend goes into §3.5's observability instead of gates.</INTERNALS-TRUST></p>
      </the-flag>
      <the-wire title="7.4.4 The wire — whole IR in, whole IR out">
        <p><WHOLE-IR-WIRE fact="true" status="spec/plan">A pass crosses the C ABI as **one call per pass per compilation**: the request carries the full serialised IR of its level (`payload.ir`, schema-versioned JSON), the reply carries the rewritten IR (or bytes, for a backend). No handle API, no callbacks into vibe, no partial views — composability comes from the pass MANAGER (many small passes in sequence), not from an interactive surface. This keeps §8.1's ABI honest, makes a pass trivially testable (a pure JSON→JSON function), and postpones the LLVM-C-API tarpit until a real pass cannot be written this way (recorded as a deferral in §11).</WHOLE-IR-WIRE></p>
      </the-wire>
      <ir-contract title="7.4.5 The IR schema is a versioned contract, and the verifier runs between passes">
        <facts ordered="false">
          <IR-SCHEMA-VERSIONED fact="true" status="spec/plan">The IR wire schema carries `ir_schema` (starts at 1); additive growth within a version, a bump when a shape breaks; a plugin's `vibe_ext_manifest` declares the schema it speaks, and a mismatch refuses with remediation before any invoke — the anti-LLVM-churn stance `##ADOPT-FROM-LLVM` promises.</IR-SCHEMA-VERSIONED>
          <INTER-PASS-VERIFIER fact="true" status="spec/plan">**The verifier is what makes arbitrary mutation survivable:** after every pass-tier pass, the level's invariants are re-checked (well-formed trees, anchor-namespace uniqueness — the existing `DuplicateId` gate — closure acyclicity, lane/marker integrity), so a pass that breaks the IR fails AT THAT PASS, by name, not three passes later as a mystery. This is LLVM's `-verify-each` practice adopted as a default that cannot be switched off from a manifest.</INTER-PASS-VERIFIER>
          <REFERENCE-STAYS-ORACLE fact="true" status="spec/plan">§7.3's oracle rule extends unchanged: the reference semantics are the built-in pipeline with zero plugin passes, and the recorded transform/pass list is part of the artifact's identity.</REFERENCE-STAYS-ORACLE>
        </facts>
      </ir-contract>
    </pass-tier>
    <ir-refactor title="7.5 The IR refactor — the composability precondition">
      <p><IR-REFACTOR fact="true" status="spec/plan">**The mandate's closing sentence is a work item:** today the transitions live as one monolithic phase loop (`compile_static_inner` — parse/topo, merge, embed, qualify, absorb, link, emit in fixed code order) over implicit representations. Before any pass-tier API exists, the compiler is refactored so that: **(1)** the five §7.4.1 levels are explicit types; **(2)** every built-in phase is a named first-class pass over them (`parse`, `close`, `merge`, `embed`, `qualify`, `absorb`, `link`, `assemble`, `emit:*`) registered in the same pass manager plugins will use — built-ins are not privileged, which is the composability test; **(3)** the default pipeline is a declared pass LIST, not control flow. **The refactor's gate is byte-identity:** the default pipeline over the whole existing corpus must emit byte-identical artifacts before and after — the compiler's own oracles (round-trip, byte-stable lanes) judge it, and no external API ships until they are green.</IR-REFACTOR></p>
      <p><COMPILER-CODE-MAP fact="true" status="spec/done">**Where the compiler lives TODAY** (verified against the tree 2026-08-24/25; the refactor's raw material, so an implementer starts from addresses, not archaeology): the phase loop — `crates/vibe-spec/src/pipeline.rs` (`compile_static_inner`, with `compile_static` / `compile_static_qualified` as its two modes); the `#use` graph and topo order — `use_graph.rs` (`topo_order_from`, `source_fold_order`); the `#source` fold — `merge.rs` (`fold_sources`) plus `pipeline.rs`'s `fold` module; `#embed` — `embed.rs` (`expand_embeds`, the `SectionSource` trait every resolver implements); qualification and rename maps — `qualify.rs` (`qualify_contribution`, `origin_slug`); reversible markers — `markers.rs` (`open`/`close`/`decompile`); addresses and routing — `address.rs` / `resolver.rs`; the doc IR — `doctree.rs` (`DocTree`) with the format pivot in the separate `vibe-specdoc` crate (`from_markdown`/`from_xml`/`to_xml`/`to_markdown`). The lane is INVOKED from `vibe-workspace`'s boot_artifacts (`boot_artifacts.rs`, `boot_artifacts/normal.rs` — the normal-lane seed) during install's boot regeneration.</COMPILER-CODE-MAP></p>
      <p><COMPILE-CACHE-READY fact="true" status="spec/plan">**The refactor must leave the door open to per-level memoization** (the Webpack persistent-cache / Turbopack lesson, 2026-08-25 audit): once every pass is a pure function of typed IR, a level's output is cacheable by its input hashes — parse results keyed by source hash (one edited document re-parses one document, not ninety-eight), pass outputs keyed by (input IR hash, pass identity, config). Not built in R3 — today's corpus compiles fast enough — but the pass signatures are designed so that adding the cache later is a wrapper, not a rewrite; the day an expensive plugin pass (an LLM-driven compression) joins the pipeline, this is what keeps the ritual usable.</COMPILE-CACHE-READY></p>
    </ir-refactor>
  </compiler>
  <native title="8. Native extensions — one ABI for both families">
    <why-c-abi title="8.1 The ABI is C + JSON, never the Rust ABI">
      <p><C-ABI-LAW fact="true" status="spec/plan">A native handler is a **cdylib** exposing a four-symbol C ABI: `vibe_ext_abi() -&gt; u32` (contract version, starts at 1); `vibe_ext_manifest() -&gt; *const c_char` (JSON: the ids and points it implements); `vibe_ext_invoke(req_ptr, req_len, resp_ptr, resp_len) -&gt; i32` (request = context envelope JSON bytes, response = reply envelope JSON bytes); `vibe_ext_free(ptr, len)`. **The Rust ABI is not a wire:** it is unstable across compiler versions, and a plugin built by yesterday's rustc against today's vibe would be undefined behaviour — the «compile it for every rustc in existence» hell the owner named (2026-08-25). C ABI + JSON costs one serialisation per call and buys total version independence — and §7.2's document-grained stages exist precisely so "per call" means "per artifact", making that cost invisible. **This is the VST posture the owner asked for:** a plugin compiles ONCE, with any toolchain that can emit a C-ABI cdylib (Rust, C, C++, Zig, Go — authorship is not Rust-bound), and keeps working across vibe releases; migrations happen when OUR plugin API moves (`vibe_ext_abi`, the envelope, `ir_schema` — all versioned, additive within a version, a break announced as a `formats/breaks/` note), never when a compiler does.</C-ABI-LAW></p>
      <facts ordered="false">
        <ABI-CRATE fact="true" status="spec/plan">vibe ships a small **`vibe-ext` helper crate** (published with the rust stack) so a plugin author writes a safe `fn handle(ctx: Context) -&gt; Reply` and a `vibe_extension!` macro emits the four symbols, the catch_unwind guard, and the (de)serialisation. Authors never touch `unsafe`.</ABI-CRATE>
        <PANIC-AND-VERSION fact="true" status="spec/plan">The loader (libloading) checks `vibe_ext_abi` before any invoke — a mismatch refuses with remediation («rebuild: `vibe build`»), never a crash. Every invoke crosses a catch_unwind at the plugin's own boundary (the macro's job); a panicking plugin fails its execution, reported like any failing contribution — it does not take vibe down.</PANIC-AND-VERSION>
      </facts>
    </why-c-abi>
    <build-home title="8.2 Where native extensions build — in place, amending PROP-024 §2.3">
      <p><IN-SLOT-BUILD fact="true" status="spec/plan">Per the mandate («собирать inplace прямо внутри vibedeps либо внутри packages»), and per the precedent PROP-025 already set (`build_binary` runs `cargo build --release` in the slot): **a dependency's native extension builds inside its own slot** (`&lt;slot&gt;/&lt;crate_dir&gt;` workspace, `target/` in the slot), and an authored package of ours builds inside its `vibevm/vibepacks/...` live slot with its own workspace. This AMENDS PROP-024 §2.3's «build output is directed to a gitignored location e.g. `.vibe/&lt;pkg&gt;-target/`, never the slot» — the amendment is recorded here and lands at that anchor at build time: build output in the slot was already reality for PROP-025 binaries, it stays outside the content hash and the shippable tree by the §2.2 denylist (`target/`, `node_modules/`), and what makes it SAFE is §9 (materialisation stops deleting what it did not write). vibe maintains the ignore entries (a generated `.gitignore` inside the vibedeps root covering `**/target/` and `**/node_modules/` — the vendored world stays clean in git without per-slot files).</IN-SLOT-BUILD></p>
      <p><BUILD-PHASE-OWNS-IT fact="true" status="spec/plan">The **build phase** builds native extensions (and `[[binary]]`s) — with cargo's own incrementality and `vibe bin build`'s narration genre (announced per artifact; PROP-025's build-consent prompt dies with the rest of the prompts, `##INSTALL-IS-CONSENT` — that amendment rides §9's plan too). Lazily building on first use stays available for `binary` handlers (as today); `native` handlers used by the COMPILER are never built lazily mid-compile — see the bootstrap order.</BUILD-PHASE-OWNS-IT></p>
      <p><PREBUILT-CLOSED fact="true" status="spec/plan">**Closed-source plugins ship prebuilt** (owner case 2026-08-25): a package may carry ready cdylib artifacts instead of (or beside) sources — `handler = { kind = "native", prebuilt = { "windows-x86_64" = "ext/bin/squeeze.dll", "linux-x86_64" = "ext/bin/libsqueeze.so", "macos-aarch64" = "ext/bin/libsqueeze.dylib" } }`, keyed by the PROP-000 platform trio's target names. A prebuilt artifact is ordinary package content: hashed into `content_hash`, materialised verbatim, integrity for free. Loading order: prebuilt for the current platform if declared, else build from `crate_dir` if sources ship, else refuse naming the missing platform — never a silent skip. The build phase skips prebuilt-only natives (nothing to build); `vibe_ext_abi`/`ir_schema` gates apply to a prebuilt exactly as to a built one, which is what makes closed-source distribution SAFE to version: the C-ABI wire (§8.1) is the whole contract, so one artifact per platform serves every vibe release the schema allows.</PREBUILT-CLOSED></p>
      <p><BOOTSTRAP-ORDER fact="true" status="spec/plan">**The bootstrap order, stated because install precedes build:** if an activated `compile:` transform's cdylib artifact is absent when install compiles the lane, install compiles WITHOUT it, marks the lane `transforms-pending` in the artifact header, and says so; the build phase, having built the extension, re-runs the (cheap) lane compile so the chain `vibe build` converges in one invocation. A `vibe install` alone with a missing transform artifact is thus honest and usable, never wedged — and never silently different from what the manifest declares.</BOOTSTRAP-ORDER></p>
    </build-home>
    <wasm title="8.3 The road not taken now">
      <p><WASM-DEFERRED fact="true" status="spec/plan">**WASM handlers — deferred, not rejected:** a wasm component would buy sandboxing (the PROP-020 §4 accepted-risk posture could tighten) and cross-platform artifacts, at the cost of a runtime dependency and awkward filesystem/process access — exactly the operations phase handlers exist to do. When the sandboxing need fires (third-party plugin ecosystems beyond the allow-list), this anchor is where the design resumes. Until then the trust gate is the boundary, exactly as PROP-020 records it.</WASM-DEFERRED></p>
    </wasm>
    <compile-native-only title="8.5 Why compile: takes only builtin and native">
      <p><COMPILE-NATIVE-ONLY fact="true" status="spec/plan">A `compile:` transform runs on every lane regeneration — inside install, potentially per document. A process spawn per document (script/binary kinds) is a Windows-hostile cost multiplier and an interpreter-availability hazard inside the innermost loop the whole product depends on; and an agent in the compiler would make the boot lane non-deterministic, which PROP-009 forbids constitutionally. Hence: `builtin` and `native` only — the two in-process kinds.</COMPILE-NATIVE-ONLY></p>
    </compile-native-only>
  </native>
  <incremental title="9. The incremental-install revision — materialise becomes a diff">
    <problem title="9.1 The defect, named from the code">
      <p><WIPE-TODAY fact="true" status="spec/done">Today `materialise_with` (vibe-workspace `vibedeps.rs`) clears the whole slot (`remove_dir_all`) and re-copies on every re-materialisation, and a **mutable local source is NEVER presence-trusted** (`install.rs`: `!dep.source_mutable` in the trust gate) — so every `vibe install` over an edited-in-workspace package wipes its slot, and with it the `target/` PROP-025 just built there. The owner's fear is already a live defect for package binaries; native compile («может идти очень долго») raises the stakes, it does not create them.</WIPE-TODAY></p>
      <p><MATERIALISE-CODE-MAP fact="true" status="spec/done">**Where materialisation lives TODAY** (verified 2026-08-24/25): the copy machine — `crates/vibe-workspace/src/vibedeps.rs` (`materialise` / `materialise_with` — the `remove_dir_all` + `copy_tree` pair this section replaces with a diff; `slot_rel_path`/`slot_abs_path`; the returned footprint `Vec&lt;PathBuf&gt;` this section's record persists); the transformed-slot variant — `vibedeps/derived.rs` (`materialise_with_spec_format`, the `.vibe-derived.toml` manifest §9.2 absorbs, `LEGACY0_EXCLUDES`); the trust gate and per-dep loop — `install.rs` (`materialise_resolution_with_spec_format`: the `present`/`format_current`/`!dep.source_mutable`/`slot_integrity` decision, `SlotIntegrity::TrustPresence|Verify`, the `slot_verifier` seam); freshness — `freshness.rs` (`hold_pins` and the fresh-check the install verb consults); hooks that must re-run only on real change — `hooks.rs` + `install/hooks_run.rs`; the in-slot binary precedent — `bins.rs` (`build_binary`, `DeclaredBinary::artifact`).</MATERIALISE-CODE-MAP></p>
    </problem>
    <record title="9.2 The slot record — the footprint becomes a file">
      <p><SLOT-RECORD fact="true" status="spec/plan">Every materialised slot gains a **slot record** — `.vibe-slot.toml` at the slot root: schema, the source `content_hash`, the spec-format identity (absorbing PROP-045's `.vibe-derived.toml` fields for transformed slots — ONE record file, not two), and `[[file]]` rows `{path, sha256}` for every file the materialiser wrote. `materialise` already returns exactly this footprint («and, in a later phase, record» — its own docstring names this phase); the record is that promise kept. The record itself, like the derived manifest today, is outside the content hash.</SLOT-RECORD></p>
    </record>
    <diff title="9.3 The diff algorithm — write what changed, delete what was ours, touch nothing else">
      <p><DIFF-MATERIALISE fact="true" status="spec/plan">Re-materialising a slot that carries a record: **(1)** compute the incoming footprint (path→hash) from the source; **(2)** for each incoming file — recorded hash equal ⇒ **leave the file untouched** (no write, no mtime change); differing or unrecorded ⇒ write it; **(3)** for each RECORDED file absent from the incoming footprint ⇒ delete it (and now-empty parent dirs, best-effort); **(4)** any on-disk path NOT in the record — `target/`, `node_modules/`, any build output — is **never touched**; **(5)** write the new record. A slot with no record (legacy) pays one last full wipe-and-copy and comes out recorded. `vibe clean` remains the only whole-slot wipe (PROP-053 unchanged).</DIFF-MATERIALISE></p>
      <facts ordered="false">
        <MTIME-LAW fact="true" status="spec/plan">**Step 2's "untouched" is a law, not an optimisation:** unchanged files keep their bytes AND their mtimes, so cargo's fingerprinting sees an unchanged workspace and the native/binary rebuild inside the slot is a no-op. This single property is what makes «инкрементальность — супер важный кусок» hold end-to-end: edit one prompt file in a package → one file rewritten in the slot → cargo rebuilds nothing.</MTIME-LAW>
        <MUTABLE-GETS-A-GATE fact="true" status="spec/plan">**Mutable sources get a freshness gate instead of a wipe:** for a PROP-011 §2.6 `file://` working-tree source, install re-hashes the source's shippable tree (cheap — read-and-hash, no copy) and compares against the record's `content_hash`: equal ⇒ the slot is fresh, skip entirely (this is the missing half of «vibe.toml не поменялся — не инсталлировать»); differing ⇒ the §9.3 diff. The never-trust-presence rule stays true in spirit — presence alone is still not trusted; the record's hash is what earns the skip.</MUTABLE-GETS-A-GATE>
        <SELF-HEAL fact="true" status="spec/plan">**Drift repair gets cheaper, not weaker.** PROP-020's reset guarantee (hook edits never compound) is re-based: "reset" = the §9.3 diff restores every RECORDED file to footprint state, byte-exact, whenever the slot re-materialises; `verify`-integrity mode compares per-file against the record and heals only divergent files. Hooks re-run only when the diff actually changed something — re-running them over an untouched slot would compound effects, the exact disease PROP-020 §2.1 exists to prevent.</SELF-HEAL>
        <AMENDMENT-PLAN fact="true" status="spec/plan">**Amendments this section obligates at build time** (recorded here so the build session lands them at the owning anchors, statuses moved, nothing renamed): PROP-011 gains the diff-materialise ruling and the mutable-source freshness gate; PROP-020 §2.1 `##RESET-THEN-RERUN` gains the record-diff-restore successor note, and its §2.3 trust gate (allow-list, first-run consent, non-interactive abort, `--allow-hooks`) is superseded by `##INSTALL-IS-CONSENT` + §3.5 observability; PROP-022 §2.2's snapshot reset language likewise; PROP-045's manifest is absorbed into the slot record (its facts re-pointed, file format superseded); PROP-024 §2.3 per §8.2; PROP-025's build-consent prompt likewise yields to `##INSTALL-IS-CONSENT`.</AMENDMENT-PLAN>
      </facts>
    </diff>
  </incremental>
  <scenarios title="10. The two commissioning test scenarios">
    <lifecycle-test title="10.1 Lifecycle: the phase-announcer plugin">
      <p><TEST-LOG-PLUGIN fact="true" status="spec/plan">A fixture package declares in its own manifest: `[[extension]] id="announce" point="phase:build" handler={kind="builtin", name="log"} config={message="hello from {phase} in {project}"}` (plus a second execution at `phase:test` — the multi-execution property under test). E2E: install the fixture into a scratch project, run `vibe test`, assert stdout carries both rendered lines in phase order, each attributed to the providing package (§3.3 surface rule); run `vibe test` again and assert the executions report `fresh` (§4.3 under test); assert `vibe extensions --json` lists both executions with their provider (§3.5 under test), and that a host `[extensions] disable = [...]` line silences exactly one of them (§3.3 under test). The `log` builtin renders `{phase}`, `{project}`, `{package}` from the envelope — proving the envelope reaches handlers, which is the scenario's real point.</TEST-LOG-PLUGIN></p>
    </lifecycle-test>
    <compiler-test title="10.2 Compiler: the XML whitespace squeezer — a TEST vehicle, not a shipping practice">
      <p><TEST-XML-MINIFY fact="true" status="spec/plan">A `compile:emitted` transform (builtin first — proving the declaration/activation/ordering tract; then re-shipped as a `native` cdylib in a fixture package — proving the §8 build/load/invoke tract with byte-identical output, which is itself the assertion). **The algorithm, XML-aware and safe by parent-shape:** parse the emitted XML; delete every text node consisting solely of whitespace whose parent element has at least one ELEMENT child and no non-whitespace text child (pure element containers lose their pretty-printing; mixed-content elements — anything carrying prose — are untouched byte-for-byte); comments (the compiled lane's `vibe:` markers) and CDATA survive verbatim; serialise. Applies to `.xml` artifacts only.</TEST-XML-MINIFY></p>
      <p><MINIFY-NOT-FOR-PROD fact="true" status="spec/done">**The owner's own caveat, recorded as a norm:** inter-tag whitespace stripping is a poor production idea — token savings are marginal and the readability/diffability of the lane (and its reversibility oracle, §7.3) suffer. The transform ships as a test fixture and a mechanics-proof, default-off everywhere; the real tenant of these stages is the future **Caveman compression** pass at `compile:lane`, which compresses prose content deliberately rather than shaving markup accidentally.</MINIFY-NOT-FOR-PROD></p>
    </compiler-test>
  </scenarios>
  <rejected title="11. Rejected and deferred alternatives">
    <facts ordered="false">
      <REJ-TWO-GRAMMARS fact="true" status="spec/plan">**Two separate extension grammars** (a `[hooks]`-style table for lifecycle, a Babel-style list for the compiler) — rejected: the mandate's core is one coherent machine; divergence here is the «разброд» the owner named as the failure mode.</REJ-TWO-GRAMMARS>
      <REJ-FULL-MAVEN-PHASES fact="true" status="spec/plan">**Adopting Maven's full 23-phase table** — rejected: the hyphenated phases exist to sequence JVM build minutiae we do not have; nine phases cover the mandate, and the phase table is extensible behind a ruling when a real between-slot need arrives.</REJ-FULL-MAVEN-PHASES>
      <REJ-RUST-ABI fact="true" status="spec/plan">**`dylib` with Rust-native symbols** (handing `&amp;mut DocTree` across the boundary) — rejected: no stable Rust ABI exists; every plugin would be pinned to the exact compiler and vibe build, which is undistributable. C ABI + JSON (§8.1) is the only honest wire.</REJ-RUST-ABI>
      <REJ-SUBPROCESS-COMPILER fact="true" status="spec/plan">**Script/binary handlers for `compile:` stages** — rejected (§8.5): spawn-per-document inside install's innermost loop, interpreter availability as a compile dependency, and agent handlers there would break boot-lane determinism.</REJ-SUBPROCESS-COMPILER>
      <REJ-CLOSURE-HOOKS fact="true" status="spec/plan">**Closure/graph access in the STAGED tier** (a tier-1 transform adding/removing `#use` edges) — rejected as a prompt-injection lever and an auditability break (§7.2 `##NO-CLOSURE-HOOK`). Full access moved tiers rather than dying: the 2026-08-25 ruling prices it into the pass tier behind `compiler_internals` + host activation + the header record (§7.4.3) — an explicit capability, never a silent one.</REJ-CLOSURE-HOOKS>
      <DEFER-HANDLE-API fact="true" status="spec/plan">**Deferred: an interactive handle/callback C API for passes** (the LLVM-C-API genre — the plugin calls back into vibe to walk and mutate nodes piecemeal) — the pass wire is whole-IR-in/whole-IR-out (§7.4.4) until a real pass demonstrably cannot be written that way; the deferral lives behind this anchor.</DEFER-HANDLE-API>
      <REJ-INLINE-COMMANDS-STAND fact="true" status="spec/plan">**Inline command strings in the manifest** — stays rejected exactly as PROP-020 §3 ruled; the `script` kind keeps the reviewable-file form, and `builtin`/`binary`/`native` are the structured alternatives.</REJ-INLINE-COMMANDS-STAND>
      <DEFER-GOAL-MIXING fact="true" status="spec/plan">**Deferred:** direct-verb mixing in a chain (`##EXISTING-VERBS-STAY`); WASM handlers (`##WASM-DEFERRED`); a per-node visitor API for external transforms (`##NOT-FROM-BABEL`); pkgrefs scoping non-install phases (`##CHAIN-GENERAL`).</DEFER-GOAL-MIXING>
    </facts>
  </rejected>
  <roadmap title="12. The proposed build order — six self-standing waves">
    <facts ordered="true">
      <R1-DIFF fact="true" status="impl/plan">**R1 — the materialise diff (§9).** Slot record, diff algorithm, mutable-source freshness gate, mtime law; amendments landed at PROP-011/020/022/024/045 anchors. Ships alone and pays immediately: `vibe install` stops killing slot `target/`s (a today-bug for PROP-025 binaries), and edit-one-prompt installs become near-instant. **First because everything after it builds artifacts the old wipe would destroy.**</R1-DIFF>
      <R2-ENGINE fact="true" status="impl/plan">**R2 — the lifecycle engine (§3–§6 minus agent).** The phase table, chain grammar (superseding PROP-053 `##CHAIN-ONLY-INSTALL` at its anchor), `[[extension]]` parsing, trust-gate extension, envelope, `builtin`/`script`/`binary` handlers, `.vibe/lifecycle.toml` freshness, the `log` builtin, and the §10.1 e2e. `vibe build`/`vibe test` become real (preset: binaries + stack commands).</R2-ENGINE>
      <R3-IR-REFACTOR fact="true" status="impl/plan">**R3 — the IR refactor (§7.5).** Explicit level types, built-in phases as named passes, the declared default pipeline, the inter-pass verifier skeleton — no external API yet. Gate: byte-identical artifacts over the whole corpus, judged by the existing oracles. Before both compiler tiers, so they are born on the pass manager instead of retrofitted onto the monolith.</R3-IR-REFACTOR>
      <R4-STAGED fact="true" status="impl/plan">**R4 — tier 1, the staged transforms (§7.2).** The four canonical transform positions, host activation, the transforms header, the oracle rule, and the §10.2 builtin minifier e2e.</R4-STAGED>
      <R5-NATIVE fact="true" status="impl/plan">**R5 — the native tier (§8).** `vibe-ext` crate, the four-symbol ABI, loader + catch_unwind + version gate, build-phase integration, bootstrap order, and the §10.2 native-parity e2e.</R5-NATIVE>
      <R6-PASS-TIER fact="true" status="impl/plan">**R6 — tier 2, the pass tier (§7.4).** The `compiler_internals` flag, pass declarations (transform/lowering/frontend/backend), the whole-IR wire with `ir_schema`, the verifier turned on between plugin passes, `vibe list`/`show` surfacing. E2E: a fixture frontend for a trivial format whose documents resolve as `spec://` citizens, and a fixture JSON backend for the lane — both behind the flag, both refused without it.</R6-PASS-TIER>
      <R7-AGENT fact="true" status="impl/plan">**R7 — create and the agent tier (§6.4–§6.5).** The agent handler, vibe-llm provider seam made real, the outbox handshake, `delegated` status and fingerprint-resume, MCP surface.</R7-AGENT>
      <R8-PACKAGE-DEPLOY fact="true" status="impl/plan">**R8 — package/deploy substance.** Skill/plugin/installer bindings for package; first deploy plugins. Shaped by what exists when its turn comes; deliberately last.</R8-PACKAGE-DEPLOY>
    </facts>
  </roadmap>
  <open title="13. Open questions — the owner's forks">
    <facts ordered="false">
      <OPEN-PHASE-NAMES fact="true" status="spec/done">**Naming — CLOSED (owner, 2026-08-25: «окей»):** the nine-phase table stands as drafted — `generate` separate from `build`, `verify` in the table from day one, the name `create` confirmed.</OPEN-PHASE-NAMES>
      <OPEN-NATIVE-TRUST fact="true" status="spec/done">**Native trust — CLOSED (owner, 2026-08-25):** no consent tier of any kind — installation is the consent (`##INSTALL-IS-CONSENT`), for in-process cdylibs exactly as for scripts, the Gradle/Webpack/Clang norm. The safety budget is spent on §3.5 observability. §8.3's wasm sandboxing note stays the eventual escape if a hostile-ecosystem era ever demands one.</OPEN-NATIVE-TRUST>
      <OPEN-CREATE-BUDGET fact="true" status="spec/plan">**Create economics:** whether agent executions carry a declared token budget in `config` and whether vibe enforces it (refuse vs warn) — policy, not mechanics; the envelope has room either way.</OPEN-CREATE-BUDGET>
      <OPEN-DEPLOY-TARGETS fact="true" status="spec/plan">**Deploy first targets:** what R8 should actually push to first (the vibespecs registry? a server genre? Claude marketplace?) — pure owner-roadmap territory.</OPEN-DEPLOY-TARGETS>
    </facts>
  </open>
  <reference title="14. Reference — the exact shapes (normative for the build)">
    <p><REF-PURPOSE fact="true" status="spec/done">This section exists so that an implementer — human or model — never has to guess a field name, a file layout, or a call sequence from the prose above. Where a shape is deliberately unfrozen it says so; everything else here is normative.</REF-PURPOSE></p>
    <ext-grammar title="14.1 The [[extension]] table, field by field">
      <fence>field               req?  type      applies to        meaning / rules
id                  yes   string    all               unique within the declaring manifest;
                                                      the reference key is &lt;group&gt;/&lt;name&gt;#&lt;id&gt;
point               yes   string    all               "phase:&lt;name&gt;" | "slot:pre-install" |
                                                      "slot:post-install" | "compile:&lt;stage&gt;" |
                                                      "compile:pass"
handler             yes   table     all               { kind = "builtin"|"script"|"binary"|
                                                        "native"|"agent", ... } — see 14.4
config              no    table     all               free TOML, delivered to the handler verbatim
auto                no    bool      compile:* only    default false for compile:*; true elsewhere
                                                      and not writable there (§3.3)
inputs              no    [string]  phase:*           extra fingerprint inputs, globs (§4.3)
applies_to          no    table     compile:source/   { packages = [globs], paths = [globs] } —
                                    document, slot:*  engine-evaluated selectors (##CONTRIB-SELECTOR)
compiler_internals  no    bool      compile:pass      REQUIRED true to use point "compile:pass";
                                                      forbidden elsewhere (§7.4.3)
pass                no    table     compile:pass      { kind = "transform"|"lowering"|"frontend"|
                                                        "backend", level|from/to, after|before|
                                                        replace, formats, artifact } (§7.4.2)
when                no    table     all               applicability guard — UNFROZEN, future ruling</fence>
      <p><REF-HANDLER-TABLES fact="true" status="spec/plan">Handler sub-shapes: `builtin` → `{ kind, name }`; `script` → `{ kind, base }` (base path without extension, PROP-020 §2.2 resolution); `binary` → `{ kind, name }` (a `[[binary]]` name); `native` → `{ kind, crate_dir? , prebuilt? }` (at least one of the two, §8.2); `agent` → `{ kind, prompt }` (a `spec://` address the package resolves).</REF-HANDLER-TABLES></p>
    </ext-grammar>
    <state-files title="14.2 .vibe/lifecycle.toml — the run/freshness record">
      <fence>schema = 1
[run]                       # last run, whole-chain
requested = "build"
chain = ["validate", "install", "build"]
started = "2026-08-25T12:00:00Z"        # clock is an input, injected — never Date::now in tests

[execution."org.vibevm.x/y#announce"]   # one table per execution, keyed by reference key
phase = "build"
fingerprint = "sha256:…"                # §4.3: hash over the execution's declared inputs
status = "ok"                           # ok | fail | skip | fresh | delegated
duration_ms = 12
artifacts = [ { id = "…", path = "…", kind = "…" } ]</fence>
      <p><REF-LIFECYCLE-TOML fact="true" status="spec/plan">Machine state: inside `.vibe/` (shippable-tree denylist), never committed, never hashed, survives `vibe clean` (§4.3), erasable at will — losing it costs one full re-run, nothing else. Schema-versioned like every state file we own.</REF-LIFECYCLE-TOML></p>
    </state-files>
    <slot-record title="14.3 .vibe-slot.toml — the materialised-slot record (§9.2)">
      <fence>schema = 1
source_hash = "sha256:…"     # content_hash of the shippable tree that was materialised
spec_format = "xml"          # mixed | markdown | xml — absorbs .vibe-derived.toml's role
converter_recipe = "…"       # present iff transformed (PROP-045 genre)
derived_hash = "sha256:…"    # present iff transformed
overlay_hash = "sha256:…"    # present iff a facts overlay applied

[[file]]                     # one row per file the MATERIALISER wrote — the footprint
path = "vibevm/vibespecs/boot/20-stack.md"    # slot-relative, forward-slashed, sorted
sha256 = "…"
# for transformed slots rows also carry: source = "…", disposition = "converted"|"copied"</fence>
      <p><REF-SLOT-RECORD fact="true" status="spec/plan">Lives at the slot root; excluded from the slot's content identity exactly as `.vibe-derived.toml` is today; unknown fields refuse (deny_unknown, schema bump to extend). The §9.3 diff reads the old record, computes the incoming footprint, and writes the new record LAST — a crash between steps leaves a slot the next run heals (recorded files re-diff; unrecorded paths were never touched).</REF-SLOT-RECORD></p>
    </slot-record>
    <wire-details title="14.4 Handler wires, end to end">
      <facts ordered="false">
        <REF-WIRE-SCRIPT fact="true" status="spec/plan">**script:** interpreter per PROP-020 §2.2 (`.sh` via bash first, `.ps1` via PowerShell on Windows fallback); cwd = slot for `slot:` points, project root for `phase:`; env = the `VIBE_*` set PROP-020 documents plus `VIBE_CONTEXT` (absolute path of the envelope JSON written into scratch) and `VIBE_REPLY` (absolute path where a reply JSON MAY be written); exit 0 + no reply file = `{"status":"ok"}`; non-zero exit = fail regardless of reply file; streams go to the user live.</REF-WIRE-SCRIPT>
        <REF-WIRE-BINARY fact="true" status="spec/plan">**binary:** the artifact is resolved via PROP-025 (`DeclaredBinary::artifact`, built if absent); envelope JSON on stdin (closed after write); reply JSON expected on stdout (whole stdout parsed as one JSON document; stderr streams to the user); non-zero exit = fail; a malformed reply = fail naming the execution.</REF-WIRE-BINARY>
        <REF-WIRE-NATIVE fact="true" status="spec/plan">**native, one invocation, seven steps:** (1) resolve artifact — prebuilt-for-platform, else built cdylib, else refuse; (2) load (libloading), once per run, cached by path; (3) `vibe_ext_abi()` — must equal a supported ABI version or refuse with «rebuild: vibe build»; (4) `vibe_ext_manifest()` — parse, check the execution's id/point/`ir_schema` are declared; (5) `vibe_ext_invoke(req, req_len, &amp;resp, &amp;resp_len)` under `catch_unwind` at the plugin macro's boundary — request/response are UTF-8 JSON byte buffers, envelope in, reply out; non-zero return or panic = the execution fails, vibe survives; (6) parse reply; (7) `vibe_ext_free(resp, resp_len)`. The `vibe-ext` helper crate's `vibe_extension!` macro generates all four symbols from a safe `fn handle(Context) -&gt; Reply`.</REF-WIRE-NATIVE>
      </facts>
    </wire-details>
    <agent-wire title="14.5 The agent handshake, on the wire (§6.5)">
      <fence># .vibe/agentic/outbox/&lt;run-id&gt;/&lt;execution-id&gt;.md   — one task file per agent execution
---
run: "&lt;run-id&gt;"
execution: "org.vibevm.x/y#draft-guide"
phase: "create"
outputs:                       # the contract vibe validates on resume
  - path: "docs/guide.md"
    accept: "non-empty file"
---
&lt;the resolved prompt document, then the envelope's prose projection&gt;

# printed to stdout, then the phase exits with status `delegated`:
```vibe-agent-tasks
run: &lt;run-id&gt;
tasks:
  - .vibe/agentic/outbox/&lt;run-id&gt;/draft-guide.md
resume: vibe &lt;requested-phase&gt;
```</fence>
      <p><REF-AGENT-RESUME fact="true" status="spec/plan">Resume is the SAME phase command re-run: the engine finds the run id in `.vibe/lifecycle.toml` (status `delegated`), validates each task's declared outputs, marks satisfied executions done, re-parks unsatisfied ones (idempotent), and continues the chain. No dedicated resume verb; `--force` re-parks everything.</REF-AGENT-RESUME></p>
    </agent-wire>
    <ir-skeletons title="14.6 IR level skeletons (tier-2 wire; frozen at R6, sketched now)">
      <fence>source:   { "ir_schema": 1, "level": "source",
            "doc": { "address": "spec://grp/pkg/path#", "format": "md", "text": "…" } }
document: { "level": "document",
            "doc": { "address": "…", "tree": { "title": …, "sections": [ … ],
                     "facts": [ { "id": "…", "status": "…", "text": "…" } ],
                     "directives": [ { "kind": "use|source|embed|spec", "target": "…" } ] } } }
closure:  { "level": "closure", "seed": "spec://…",
            "nodes": [ { "key": "…", "origin": "grp/name", "address": "…",
                         "body": "…", "renames": [ … ] } ],        # topo order
            "edges": [ { "from": "…", "to": "…", "kind": "use|source|embed" } ] }
lane:     { "level": "lane", "artifact": "static-xml",
            "entries": [ { "origin": "…", "slug": "…", "body": "…", "markers": … } ] }
emitted:  { "level": "emitted", "artifact": "static-xml", "bytes_b64": "…" }</fence>
      <p><REF-IR-UNFROZEN fact="true" status="spec/plan">These skeletons fix the VOCABULARY (level names, the node/edge/entry framing); exact field sets freeze in R6 when the wire ships, derived from the R3 types — deliberately, so the refactor is not forced to mirror a JSON guess made before it ran. `ir_schema` starts at 1 the day the first external pass can load.</REF-IR-UNFROZEN></p>
    </ir-skeletons>
  </reference>
  <glossary title="15. Glossary — the words this document leans on">
    <fence>slot            one package's materialised directory: vibevm/vibedeps/&lt;group&gt;.&lt;name&gt;/&lt;version&gt;/
world           all resolved packages of a workspace, as pinned by vibe.lock
materialise     copy a package's shippable tree into its slot (today: wipe+copy; after R1: diff)
shippable tree  a package's directory minus build output (.git/.vibe/target/node_modules/.vibeignore)
boot lane       the generated agent-entry artifacts: vibevm/vibespecs/boot/STATIC.* + INDEX.md + INLINE.*
seed            the spec address a normal-format package's boot snippet points the compiler at
closure         every spec node reachable from the seed via directives; what the lane contains
lane            the assembled single-document projection of the closure, marker-wrapped, reversible
origin          the package (or host) a node was authored in; qualification prefixes slugs with it
qualify         rewrite a node's anchors/links under its origin so merged nodes cannot collide
absorbed node   a node whose text is contained in another included node; emitted once (READ-ONCE)
fingerprint     a hash over an execution's declared inputs; equality means skip ("fresh")
contribution    one [[extension]] row: a handler bound to an extension point, with config
execution       one scheduled run of a contribution (Maven's execution; the id is its name)
handler         how a contribution runs: builtin | script | binary | native | agent
envelope        the versioned JSON context every handler receives (§5)
pass            tier-2 unit of compiler extension: whole-IR-in → whole-IR-out at a named position
lowering        a pass that moves the IR down one level (source→document→closure→lane→emitted)
frontend        a parse-position lowering registered for a new source format
backend         an emit-position lowering producing artifact bytes
kind-preset     the built-in contribution table selected by manifest kind + installed stack (§4.5)
slot record     .vibe-slot.toml — the per-file footprint the R1 diff materialiser keeps
machine cache   ~/.vibe/cache — the per-machine package store (PROP-010); never touched by clean</fence>
  </glossary>
</spec>
