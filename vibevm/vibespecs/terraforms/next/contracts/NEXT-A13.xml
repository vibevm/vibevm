<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">NEXT temporary contract 3.13 — Environment management: one package system, explicit targets</title>
  <status stage="impl" state="plan"/>
  <p><NEXT-A13-SCOPE fact="true" status="spec/done">Temporary normative staging unit. Its body is the accepted architecture from refined plan revision 4, section 3.13. Before production code cites this law, promote the relevant clauses into permanent specifications. This file is consumed only after semantic coverage, implemented behavior and retained evidence are accepted. The task/retirement checker cannot perform semantic acceptance.</NEXT-A13-SCOPE></p>
  <p><NEXT-A13-U001 fact="true" status="spec/done">**Owner mandate D-020/D-021:** VibeVM развивается в менеджер пакетов и окружений: личная система пользователя в $HOME и сборка Docker/OCI-образов обязательны в одной отдельной фазе. Будущий Linux-дистрибутив — направление дизайна; tools/install interfaces должны подходить и для него. Это расширяет прежний non-goal OS management. Собственный privilege broker, ядро, загрузчик и замена всех сторонних package engines сейчас не требуются. Архитектурные seams M-14 входят раньше consumers; практическая фаза M-15 не прячется в defer table.</NEXT-A13-U001></p>
  <p><NEXT-A13-U002 fact="true" status="spec/done">Один authored environment manifest использует существующие package identities, requires и feature/choice algebra. Он может жить в репозитории образа или в пользовательском каталоге без Git и без coding project. Environment — deployable consumer context, не новый package kind, change или personal stewardship plan. tool уже существует как kind; installable payload, service и OCI image — capabilities/artifacts. Не добавлять kind=os/docker для каждой формы доставки и не заменять npm/Cargo resolution их внутренностей.</NEXT-A13-U002></p>
  <p><NEXT-A13-U003 fact="true" status="spec/done">Разделить пять объектов:</NEXT-A13-U003></p>
  <p><NEXT-A13-U004 fact="true" status="spec/done">Requirement: | Object | Meaning / identity | |---|---| | EnvironmentDefinition | Переносимый desired composition: roots, choices, target constraints, tool/service exports, runtime bindings по именам; без machine paths/credentials | | EnvironmentLock | Exact selected Vibe captures/configuration + external backend closures + build/runtime dependency roles + target ABI; derived once per environment domain | | ExecutionContext | Где исполняется конкретная операция: Windows host, Linux BuildKit worker, local Linux process; admitted executor/tool/provider identity, network/mount/principal capabilities | | TargetBinding | Stable environment instance ID, generation, target OS/arch/ABI/libc constraints, offline/live mode, filesystem roots/prefix, target principal/home и storage backend; local physical binding отдельно от portable definition | | EnvironmentGeneration | Realized payload inventory/metadata, dependency closure, generated boot/adapters/service catalog и activation links, связанные с lock/recipes; mutable data и secrets не часть immutable generation |</NEXT-A13-U004></p>
  <fence lang="text">| Object | Meaning / identity |
|---|---|
| EnvironmentDefinition | Переносимый desired composition: roots, choices, target constraints, tool/service exports, runtime bindings по именам; без machine paths/credentials |
| EnvironmentLock | Exact selected Vibe captures/configuration + external backend closures + build/runtime dependency roles + target ABI; derived once per environment domain |
| ExecutionContext | Где исполняется конкретная операция: Windows host, Linux BuildKit worker, local Linux process; admitted executor/tool/provider identity, network/mount/principal capabilities |
| TargetBinding | Stable environment instance ID, generation, target OS/arch/ABI/libc constraints, offline/live mode, filesystem roots/prefix, target principal/home и storage backend; local physical binding отдельно от portable definition |
| EnvironmentGeneration | Realized payload inventory/metadata, dependency closure, generated boot/adapters/service catalog и activation links, связанные с lock/recipes; mutable data и secrets не часть immutable generation |</fence>
  <p><NEXT-A13-U005 fact="true" status="spec/done">Для compiler toolchain отдельны build machine, machine where produced tool runs и, если сам tool является compiler, emitted-code target. Native Vibe extension ABI всегда относится к executor, а не устанавливаемому Linux binary. OS-only when guard не описывает архитектуру/libc. Existing host-based guard migrates to an explicit compatibility interpretation; target predicates не переключаются молча. Feature selected world и boot conditions получают target context. Windows host не должен выбирать Windows launcher для Linux image.</NEXT-A13-U005></p>
  <p><NEXT-A13-U006 fact="true" status="spec/done">Target kinds: workspace projection, user profile, offline Linux rootfs/image и future live Linux system. Поддержка описывается capabilities; M-14 implements existing local binding and explicit unsupported responses for future operations, M-15 delivers home/rootfs/OCI execution. Не требуется умение всех providers работать со всеми target kinds. Unsupported target/ABI/metadata отказывает при plan, не после partial installation.</NEXT-A13-U006></p>
  <p><NEXT-A13-U007 fact="true" status="spec/done">Root paths передаёт trusted surface; provider не читает ambient HOME/PATH, чтобы заново выбрать destination. Root-relative resource identity включает environment instance/root/principal и case/path policy. Physical locks additionally recognize aliased roots across instances. Clone/image instantiation получает новую instance binding; generation content identity переиспользуется. Move/relocate/adopt — explicit validated operation, прежняя receipt не даёт права на случайный новый каталог по тому же path.</NEXT-A13-U007></p>
  <p><NEXT-A13-U008 fact="true" status="spec/done">Layout remains centralized per PROP-052. Existing project vibevm/ tree не переезжает. Новая environment layout table владеет отдельными configuration/store/profile/state/cache roots; proposed home defaults under existing Vibe settings/data configuration фиксируются M-14-A, а не размазываются literals по providers. Пользователь может выбрать весь prefix под своим HOME. Stewardship state не становится environment package DB.</NEXT-A13-U008></p>
</spec>
