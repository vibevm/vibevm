<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>vibe tree — the TUI application (PROP-037) visual sign-off</title>
  <p><purpose fact="true" status="impl/done">**Purpose.** `vibe tree` is now a full TUI application (PROP-037, TREE-TUI-PLAN
v0.2): a formal visual language (five palettes, glyph vocabulary, rendering
tiers), a reusable `ui::` component library, the tree filter/shape pipeline,
trees in every mode, a keymap-driven action dispatch, a detail card, settings
persistence, and a copy system. The automated suite (241 vibe-cli tests,
`self-check` all green) proves the *model + the rendering fns*; it cannot drive
a real terminal and confirm the TUI *looks and reads right* — that the Unicode
box-drawing aligns, the palette is beautiful and switchable, the windows float,
the card wraps, the modals cascade at depth 2. That is what a human signs off
here.</purpose></p>
  <p><settings-mutation-note fact="true" status="impl/done">Unlike MT-01, this TUI **writes user settings** (`~/.vibe/` via the vibe-settings
system, Шаг 2) — palette/tier/mode/sort/shape choices persist. The test mutates
only machine-global user prefs (never project files); back them up or accept the
delta.</settings-mutation-note></p>
  <section title="Preconditions">
    <facts ordered="false">
      <PRE-TTY-TRUECOLOR fact="true" status="impl/done">A real interactive terminal (a tty), ideally truecolor (`echo $COLORTERM` →
  `truecolor`/`24bit`) so Tier 3 shows; a 256-colour or 16-colour terminal is the
  degradation path and is worth a second pass.</PRE-TTY-TRUECOLOR>
      <PRE-BINARY fact="true" status="impl/done">`cargo build -p vibe-cli` (invoke `./target/debug/vibe`).</PRE-BINARY>
      <PRE-REPO-ROOT fact="true" status="impl/done">Run from the vibevm repo root.</PRE-REPO-ROOT>
    </facts>
  </section>
  <section title="Steps">
    <facts ordered="true">
      <STEP-1-DEFAULT-LOOK fact="true" status="impl/done">**Launch + the default look (Rosé Pine, Tier 3).**</STEP-1-DEFAULT-LOOK>
    </facts>
    <fence>   ./target/debug/vibe tree</fence>
    <facts ordered="false">
      <S1-GLYPHS fact="true" status="impl/done">Tree connectors `│├└─` align; fold indicator is `▾`/`▸` (not `+`/`-`); DAG
     re-occurrence is `↩` (not `(*)`); flags are `●`/`○` (not `x`/`.`).</S1-GLYPHS>
      <S1-FOOTER fact="true" status="impl/done">The footer lists `F1 search · F2 sort · F3 mode · F4 settings · F6 copy
     · ↑↓ move · ←→ pan · Space fold · Enter details · Esc quit` (Tabs mode adds
     `Shift+←/→ tab`).</S1-FOOTER>
      <S1-STATUS-LINE fact="true" status="impl/done">The status line shows ordering · mode · STATIC.md size · package count.</S1-STATUS-LINE>
    </facts>
    <facts ordered="true">
      <STEP-2-NAVIGATION fact="true" status="impl/done">**Navigation + fold (every mode is a tree).** `↑`/`↓` move, `←`/`→` pan,
   `Space` folds a node (▾↔▸), `Enter` opens the detail card.</STEP-2-NAVIGATION>
    </facts>
    <facts ordered="true">
      <STEP-3-DETAIL-CARD fact="true" status="impl/done">**The detail card (§8).** `Enter` on a package → a paper panel, **bold field
   headers**, wrapped long values (a 64-char hash wraps, never truncates), a `✕`
   close affordance top-right; `Esc`/`✕` closes.</STEP-3-DETAIL-CARD>
    </facts>
    <facts ordered="true">
      <STEP-4-MODES fact="true" status="impl/done">**Modes (§4) — all trees.** `F3` → the mode menu → SubTables (stacked trees per
   load partition, each under a subheader), Tabs (`Shift+←`/`Shift+→` switch
   tabs, each tab a tree). Fold a package in SubTables — it folds in every block.</STEP-4-MODES>
    </facts>
    <facts ordered="true">
      <STEP-5-SORT-SHAPE fact="true" status="impl/done">**Sort &amp; shape (§7.2).** `F2` → a multi-group dialog: Sort by (alphabetical/
   topological) + Shape (members-as-roots / load-type-forest / pruned-tree) +
   Block order (sub-tables only). Pick a shape — the tree re-forms. The menu
   stays open (sticky); `Esc` closes.</STEP-5-SORT-SHAPE>
    </facts>
    <facts ordered="true">
      <STEP-6-SEARCH fact="true" status="impl/done">**Search Everywhere (§7.3).** `F1` → the hybrid "All" + per-category tabs;
   type a query — packages, card fields, and `vibe.tree` actions match; `Enter`
   on an action runs it in place.</STEP-6-SEARCH>
    </facts>
    <facts ordered="true">
      <STEP-7-COPY fact="true" status="impl/done">**Copy (§10).** `F6` → copies the current screen (tree or card) as Markdown
   to the clipboard (footer flash `✓ copied`). `Shift+F6` → copy-settings
   (format Markdown/PNG + dest clipboard/file). PNG → ComingSoon. dest=file →
   the FileDest modal (TextField + Save/Cancel) **over** copy-settings (depth-2);
   `Esc` returns to copy-settings, not the base.</STEP-7-COPY>
    </facts>
    <facts ordered="true">
      <STEP-8-QUIT-CONFIRM fact="true" status="impl/done">**Quit-confirm (§7.4).** At the base, `Esc` → "Really quit?" dialog (not an
   instant quit); `Enter` quits, `Esc`/`No` cancels.</STEP-8-QUIT-CONFIRM>
    </facts>
    <facts ordered="true">
      <STEP-9-PALETTE fact="true" status="impl/done">**Switchable palette (§2.2.1, the owner vision).** Quit, set the palette:</STEP-9-PALETTE>
    </facts>
    <fence>   # edit directly, or pick it in the F4 settings menu    ~/.vibe/settings.toml
   [vibe.tree]
   palette = "catppuccin-mocha"   # or -macchiato / -frappe / -latte (light) / rose-pine</fence>
    <p><S9-RELAUNCH fact="true" status="impl/done">Relaunch — the whole UI (tree, windows, card, menus, search) is now in the
   chosen palette; Latte is light. Tier override: `tier = 1` (16-colour) or
   `0` (ASCII fallback) to see the degradation.</S9-RELAUNCH></p>
    <facts ordered="true">
      <STEP-10-PERSISTENCE fact="true" status="impl/done">**Persistence.** Change mode/sort/shape via the menus; quit; relaunch — the
    choices are restored.</STEP-10-PERSISTENCE>
    </facts>
  </section>
  <section title="Pass">
    <p><pass-criteria fact="true" status="impl/done">Every step reads as described: the Unicode aligns, the palettes are beautiful
and distinct, the windows float, the card wraps, the depth-2 cascade works, and
the five palettes all render. The owner signs the date below.</pass-criteria></p>
  </section>
  <section title="Sign-off">
    <facts ordered="false">
      <SIGNOFF-OWNER fact="true" status="impl/work">[ ] Owner visual sign-off (date / initials): ______</SIGNOFF-OWNER>
      <SIGNOFF-TIER-3 fact="true" status="impl/work">Tier 3 (truecolor) checked on terminal: ______</SIGNOFF-TIER-3>
      <SIGNOFF-DEGRADATION fact="true" status="impl/work">A degradation tier (256 / 16 / ASCII) also checked: ______</SIGNOFF-DEGRADATION>
    </facts>
  </section>
</spec>
