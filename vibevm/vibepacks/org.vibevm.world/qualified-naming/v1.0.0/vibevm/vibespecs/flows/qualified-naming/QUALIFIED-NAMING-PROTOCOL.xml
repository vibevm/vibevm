<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Qualified Naming Protocol</title>
  <status stage="spec" state="done"/>
  <p><scope-of-this-document fact="true" status="impl/done">**Scope of this document.** This file defines the laws that make a
package namespace scale: why a flat namespace fails, the mandatory
*group*, the identity tuple, why a rename is a new identity, why short
names may live only at the human boundary, and why a *collision* and a
*conflict* are two different failures.</scope-of-this-document></p>
  <p><written-for-ecosystem-designers fact="true" status="impl/done">It is written for **ecosystem
designers** — anyone minting the namespace for packages, plugins,
extensions, or artifacts.</written-for-ecosystem-designers></p>
  <p><sibling-document-pointers fact="true" status="impl/done">Reference grammar: [`ref-grammar.xml`](ref-grammar.xml);
the fork-by-fork rationale: [`naming-forks.xml`](naming-forks.xml).</sibling-document-pointers></p>
  <problem title="The problem: flat names">
    <p><A-FLAT-NAMESPACE-ADDRESSES-BY-A-SINGLE-BARE-NAME fact="true" status="spec/done">A flat namespace is one where an artifact is addressed by a single
bare `name`, unique across the whole registry.</A-FLAT-NAMESPACE-ADDRESSES-BY-A-SINGLE-BARE-NAME></p>
    <p><it-fails-in-three-predictable-ways fact="true" status="spec/done">It reads beautifully on
day one (`install wal`) and fails in three predictable ways:</it-fails-in-three-predictable-ways></p>
    <facts ordered="false">
      <FAILURE-SQUATTING fact="true" status="spec/done">**Squatting.** The good short names are a finite commons. The first
  arrival takes `http`, `json`, `auth`; everyone after fights over
  `http2`, `json-fast`, `auth-real`. The namespace rewards land-grab
  speed, not quality.</FAILURE-SQUATTING>
      <FAILURE-TRUST-AMBIGUITY fact="true" status="spec/done">**Trust ambiguity.** In a flat namespace `logger` has no owner you
  can name. Is this the `logger` you audited last week, or a
  same-named replacement someone else published? Nothing in the
  coordinate answers, so trust cannot be delegated — it must be
  re-established per artifact.</FAILURE-TRUST-AMBIGUITY>
      <FAILURE-TRANSITIVE-COLLISIONS fact="true" status="spec/done">**Transitive collisions.** Two dependencies deep in your graph both
  want the bare name `utils`, meaning different things. A flat resolver
  cannot satisfy both; the graph is unbuildable, and the failure
  surfaces far from either author.</FAILURE-TRANSITIVE-COLLISIONS>
    </facts>
    <p><EVERY-LAW-BELOW-IS-A-STRUCTURAL-FIX fact="true" status="impl/done">Every law below is a structural fix for one of these, not a
convenience.</EVERY-LAW-BELOW-IS-A-STRUCTURAL-FIX></p>
    <p><A-GROUPED-NAMESPACE-FIXES-ALL-THREE fact="true" status="impl/done">A group-qualified namespace makes squatting local, trust
delegable, and transitive collisions impossible by construction.</A-GROUPED-NAMESPACE-FIXES-ALL-THREE></p>
  </problem>
  <group title="Law 1 — every artifact carries a group">
    <p><IDENTITY-BEGINS-WITH-A-MANDATORY-GROUP fact="true" status="impl/done">Identity begins with a **mandatory** `group`: a dot-separated string
of lowercase segments (`com.example.shop`, `io.acme`, `org.vibevm`).</IDENTITY-BEGINS-WITH-A-MANDATORY-GROUP></p>
    <p><THE-GROUP-IS-THE-UNIT-OF-OWNERSHIP fact="true" status="impl/done">The group is the unit of ownership; `name` is unique *within* it, so
two owners may both ship a `wal` without colliding.</THE-GROUP-IS-THE-UNIT-OF-OWNERSHIP></p>
    <facts ordered="false">
      <GROUP-UNIQUENESS-IS-DELEGATED fact="true" status="impl/done">**Uniqueness is delegated.** The registry guarantees groups are
  distinct; each group's owner guarantees names are distinct inside
  it. Global uniqueness falls out of two local guarantees — no central
  arbiter of every short name.</GROUP-UNIQUENESS-IS-DELEGATED>
      <GROUP-REVERSE-FQDN-IS-RECOMMENDED-NOT-ENFORCED fact="true" status="impl/done">**Reverse-FQDN is recommended, not enforced.** Writing the group as
  a reversed domain (`org.example` for `example.org`) piggybacks on
  DNS's existing global uniqueness — a convention Sun introduced for
  Java packages in 1995 for exactly this reason. But whether a group
  *looks* like a reversed domain is **style**: a matter for humans and
  linters. The resolver checks only two things — that the group is
  well-formed grammar, and that it is unique. It never demands you own
  the domain.</GROUP-REVERSE-FQDN-IS-RECOMMENDED-NOT-ENFORCED>
      <GROUP-GRAMMAR-IS-THE-ONLY-HARD-RULE fact="true" status="impl/done">**Grammar is the only hard rule.** Segments are `[a-z0-9_-]+`, ASCII
  lowercase, dot-separated. That is enforced. Taste is not.</GROUP-GRAMMAR-IS-THE-ONLY-HARD-RULE>
    </facts>
    <p><A-MANDATORY-GROUP-REMOVES-A-GREY-ZONE fact="true" status="impl/done">Making the group mandatory (rather than optional) removes a grey zone:
there is no "has a group" versus "no group" fork to reason about — every
artifact is qualified, always.</A-MANDATORY-GROUP-REMOVES-A-GREY-ZONE></p>
  </group>
  <identity title="Law 2 — identity is a tuple">
    <p><AN-ARTIFACTS-IDENTITY-IS-THE-TUPLE fact="true" status="impl/done">An artifact's identity is the tuple **`(group, name, version,
content-hash)`**.</AN-ARTIFACTS-IDENTITY-IS-THE-TUPLE></p>
    <p><two-consequences-carry-the-whole-system fact="true" status="impl/done">Two consequences carry the whole system:</two-consequences-carry-the-whole-system></p>
    <facts ordered="false">
      <GROUP-NAME-IS-GLOBALLY-UNIQUE fact="true" status="impl/done">**`(group, name)` is globally unique.** It names *the package* across
  all its versions. Any type tag (a `kind` such as `flow`, `plugin`,
  `lib`) is metadata — it may help placement or filtering, but it is
  **not** part of identity and never disambiguates two packages.</GROUP-NAME-IS-GLOBALLY-UNIQUE>
      <CONTENT-HASH-PINS-THE-BYTES fact="true" status="impl/done">**`content-hash` pins the bytes.** `(group, name, version)` names a
  release; the hash proves which bytes that release is. A mirror in a
  different registry serving the same bytes is the *same* identity; the
  registry URL is a fetch detail, not part of who the artifact is.</CONTENT-HASH-PINS-THE-BYTES>
    </facts>
    <p><THE-COORDINATE-CARRIES-ITS-OWN-OWNERSHIP-AND-INTEGRITY fact="true" status="impl/done">Because identity is a tuple and not a string, the coordinate carries
its own ownership (`group`) and its own integrity (`content-hash`).</THE-COORDINATE-CARRIES-ITS-OWN-OWNERSHIP-AND-INTEGRITY></p>
    <p><THAT-IS-WHAT-LETS-TRUST-BE-DELEGATED fact="true" status="impl/done">That is what lets trust be delegated: you trust `io.acme`, so you trust
every `io.acme/*` name, without re-auditing each one.</THAT-IS-WHAT-LETS-TRUST-BE-DELEGATED></p>
  </identity>
  <rename title="Law 3 — a rename is a new identity">
    <p><A-CHANGED-GROUP-OR-NAME-IS-A-NEW-PACKAGE fact="true" status="impl/done">Change the `group` or the `name`, and you have a **new package** — not
a renamed one.</A-CHANGED-GROUP-OR-NAME-IS-A-NEW-PACKAGE></p>
    <p><THIS-FOLLOWS-FROM-LAW-2-NOT-FROM-POLICY fact="true" status="impl/done">This is not a policy choice; it follows from Law 2: the
identity tuple changed, so the identity changed.</THIS-FOLLOWS-FROM-LAW-2-NOT-FROM-POLICY></p>
    <facts ordered="false">
      <RENAME-VERSIONS-NEVER-TRANSFER fact="true" status="impl/done">**Versions never transfer.** `io.acme/logger` at `2.3.0` does not
  make `io.acme/log` start at `2.3.0`. The new name starts its own
  version line. History stays attached to the coordinate that earned
  it.</RENAME-VERSIONS-NEVER-TRANSFER>
      <RENAME-NO-COORDINATE-IS-EVER-REUSED fact="true" status="impl/done">**No coordinate is ever reused for different content — ever.** Once
  `com.example.shop/cart@1.4.0` has meant one artifact, that exact
  `name@version` must never resolve to different bytes for anyone,
  forever. A consumer who locked `1.4.0` locked a specific meaning;
  silently repointing it is the one betrayal a package system must make
  impossible. Yank a bad release, publish a `1.4.1` — but never let the
  old coordinate mean something new.</RENAME-NO-COORDINATE-IS-EVER-REUSED>
    </facts>
    <p><the-alias-table-alternative-is-examined-in-naming-forks fact="true" status="impl/done">The rejected alternative — an alias table mapping the old name to the
new — is examined in [`naming-forks.xml` §rename](naming-forks.xml#rename).</the-alias-table-alternative-is-examined-in-naming-forks></p>
    <p><AN-ALIAS-RE-INTRODUCES-THE-AMBIGUITY-THE-GROUP-REMOVED fact="true" status="spec/done">It loses because an alias re-introduces exactly the ambiguity the group
removed: now two coordinates name one artifact, and every reader must
know the mapping to trust what they read.</AN-ALIAS-RE-INTRODUCES-THE-AMBIGUITY-THE-GROUP-REMOVED></p>
  </rename>
  <short-names title="Law 4 — short names live only at the boundary">
    <p><A-SHORT-NAME-IS-THE-BARE-UNQUALIFIED-NAME-A-HUMAN-TYPES fact="true" status="impl/done">A **short name** is the bare, unqualified `name` a human types
(`install wal`).</A-SHORT-NAME-IS-THE-BARE-UNQUALIFIED-NAME-A-HUMAN-TYPES></p>
    <p><A-SHORT-NAME-IS-LEGAL-IN-EXACTLY-ONE-PLACE fact="true" status="impl/done">It is a convenience, and it is legal in exactly one
place: **the human-typed CLI input boundary**, resolved **once** against
an index of `(group, name)` candidates.</A-SHORT-NAME-IS-LEGAL-IN-EXACTLY-ONE-PLACE></p>
    <facts ordered="false">
      <SHORT-NAMES-ARE-NEVER-STORED fact="true" status="impl/done">**Never stored.** The moment a short name is resolved, the tool
  writes the *qualified* form (`org.vibevm.world/wal`) into the manifest and
  lockfile. Persisted state is qualified-only.</SHORT-NAMES-ARE-NEVER-STORED>
      <SHORT-NAMES-ARE-NEVER-RESOLVED-RECURSIVELY fact="true" status="impl/done">**Never resolved recursively.** Resolution happens for a human's
  argument and nothing else. The dependency graph is built entirely
  from qualified names, because every author published through the same
  boundary and stored the qualified form.</SHORT-NAMES-ARE-NEVER-RESOLVED-RECURSIVELY>
    </facts>
    <p><THE-BOUNDARY-RULE-MAKES-TRANSITIVE-COLLISIONS-IMPOSSIBLE fact="true" status="impl/done">This single rule is what makes **transitive collisions impossible by
construction**.</THE-BOUNDARY-RULE-MAKES-TRANSITIVE-COLLISIONS-IMPOSSIBLE></p>
    <p><AMBIGUITY-CAN-ONLY-ARISE-WHERE-A-HUMAN-IS-PRESENT fact="true" status="impl/done">A short name can only be ambiguous at the one place a
human is present to disambiguate it; it can never be ambiguous three
levels deep in a graph, because no short name ever reaches that far.</AMBIGUITY-CAN-ONLY-ARISE-WHERE-A-HUMAN-IS-PRESENT></p>
    <p><the-cargo-npm-pattern-is-the-same-instinct fact="true" status="spec/done">The
cargo/npm pattern is the same instinct: `add serde` on the command
line, `serde = "1"` in the manifest.</the-cargo-npm-pattern-is-the-same-instinct></p>
  </short-names>
  <collision title="Law 5 — collision and conflict are distinct failures">
    <p><two-failures-look-similar-and-must-never-be-merged fact="true" status="impl/done">Two failures look similar and must never be merged:</two-failures-look-similar-and-must-never-be-merged></p>
    <table>
      <tr>
        <td>Failure</td>
        <td>Cause</td>
        <td>Resolution</td>
      </tr>
      <tr>
        <td><ROW-FAILURE-COLLISION fact="true" status="impl/done">**Collision**</ROW-FAILURE-COLLISION></td>
        <td><ROW-FAILURE-COLLISION-CAUSE fact="true" status="impl/done">one short name matches two *different* packages (different groups)</ROW-FAILURE-COLLISION-CAUSE></td>
        <td><ROW-FAILURE-COLLISION-RESOLUTION fact="true" status="impl/done">the human picks a group and records the qualified form</ROW-FAILURE-COLLISION-RESOLUTION></td>
      </tr>
      <tr>
        <td><ROW-FAILURE-CONFLICT fact="true" status="impl/done">**Conflict**</ROW-FAILURE-CONFLICT></td>
        <td><ROW-FAILURE-CONFLICT-CAUSE fact="true" status="impl/done">version requirements cannot all be satisfied — contradictory constraints, a declared incompatibility, an unsatisfiable diamond</ROW-FAILURE-CONFLICT-CAUSE></td>
        <td><ROW-FAILURE-CONFLICT-RESOLUTION fact="true" status="impl/done">the human relaxes a constraint or drops a dependency</ROW-FAILURE-CONFLICT-RESOLUTION></td>
      </tr>
    </table>
    <facts ordered="false">
      <FAILURES-HAVE-DISTINCT-MACHINE-READABLE-IDENTITIES fact="true" status="impl/done">**Distinct machine-readable identities.** Each failure gets its own
  exit code and its own error type, so a script — or an agent — can
  branch on *which* failure occurred without parsing prose. The specific
  numbers are an implementation's choice; the law is only that the two
  differ and are stable.</FAILURES-HAVE-DISTINCT-MACHINE-READABLE-IDENTITIES>
      <NO-INTERACTIVE-PICK-ON-A-COLLISION fact="true" status="impl/done">**No interactive pick on a collision.** When a short name is
  ambiguous, the tool prints *all* candidates with their exact
  qualified forms and **fails**. It does not offer an arrow-key menu.
  The choice must be *recorded deliberately* — edited into the manifest
  by a human — not clicked once and forgotten. A clicked choice leaves
  no trace of why; a recorded qualified name is self-documenting.</NO-INTERACTIVE-PICK-ON-A-COLLISION>
    </facts>
    <p><error-shape-detail-pointer fact="true" status="impl/done">Error-shape detail — what a good collision message must contain — is in
[`ref-grammar.xml` §errors](ref-grammar.xml#errors).</error-shape-detail-pointer></p>
  </collision>
  <re-derive title="Re-derive for your project">
    <p><re-derive-lead fact="true" status="impl/done">Do not copy this document's example groups — copy the *task*, and let
the agent derive the namespace your ecosystem actually needs:</re-derive-lead></p>
    <fence>Read this flow's documents (your project installed them — typically `vibedeps/flow-qualified-naming/&lt;version&gt;/spec/flows/qualified-naming/`, check `vibe.lock`) in full, then design the namespace
for THIS ecosystem:
1. Name the artifacts it distributes (packages, plugins, extensions)
   and who owns each — the owner set is your group set.
2. Choose a group grammar and a recommended style (reverse-FQDN or
   other). State plainly what is enforced vs merely recommended.
3. Define the identity tuple and the reference grammar: separators for
   type / group / version, and which forms are legal where.
4. State the storage rule (qualified-only in manifests and locks) and
   the boundary rule (short names resolved once, at CLI input).
5. Specify collision vs conflict as two failures with two distinct,
   stable machine identities, each with an example message.
Show me the design as a short spec. Change nothing in code yet.</fence>
  </re-derive>
  <summary title="Summary">
    <facts ordered="false">
      <SUM-FLAT-NAMES-FAIL-THREE-WAYS fact="true" status="spec/done">Flat names fail three ways: squatting, trust ambiguity, transitive
  collisions. Groups fix all three structurally.</SUM-FLAT-NAMES-FAIL-THREE-WAYS>
      <SUM-EVERY-ARTIFACT-CARRIES-A-MANDATORY-GROUP fact="true" status="impl/done">Every artifact carries a mandatory **group**; identity is
  `(group, name, version, content-hash)` and `(group, name)` is unique.</SUM-EVERY-ARTIFACT-CARRIES-A-MANDATORY-GROUP>
      <SUM-REVERSE-FQDN-IS-RECOMMENDED-STYLE fact="true" status="impl/done">Reverse-FQDN is recommended style, not enforced law — the resolver
  checks grammar and uniqueness, nothing about taste.</SUM-REVERSE-FQDN-IS-RECOMMENDED-STYLE>
      <SUM-A-RENAME-IS-A-NEW-IDENTITY fact="true" status="impl/done">A rename is a **new identity**: versions never transfer, and no
  `name@version` coordinate is ever reused for different content.</SUM-A-RENAME-IS-A-NEW-IDENTITY>
      <SUM-SHORT-NAMES-LIVE-ONLY-AT-THE-BOUNDARY fact="true" status="impl/done">Short names live **only** at the human CLI boundary, resolved once;
  manifests and locks store the qualified form — so transitive
  collisions cannot exist.</SUM-SHORT-NAMES-LIVE-ONLY-AT-THE-BOUNDARY>
      <SUM-COLLISION-AND-CONFLICT-ARE-DISTINCT fact="true" status="impl/done">**Collision** and **conflict** are distinct failures with distinct
  machine identities; a collision fails with candidates, never an
  interactive pick.</SUM-COLLISION-AND-CONFLICT-ARE-DISTINCT>
    </facts>
  </summary>
</spec>
