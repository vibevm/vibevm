<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">FRACTALITY-RLM-RESEARCH-PLAN v0.1 — Campaign 3 · Stage A: the RLM research</title>
  <p>_Status: **CLOSED 2026-07-11 00:59 — all phases complete, all
four predictions CONFIRMED** (P-R2 at the exact 60% boundary;
verdicts: [`RLM-SYNTHESIS.xml §5`](../refs/notes/RLM-SYNTHESIS.xml)).
Deliverables: `rlm-source-selection.xml` (5/5/5 + runners-up), 11
study notes, `RLM-SYNTHESIS.xml` (RD-1…21), and the Stage B draft
[`FRACTALITY-RLM-PLAN-v0.1.xml`](FRACTALITY-RLM-PLAN-v0.1.xml)
(RP-C3-1 OPEN — not commissioned). Executed 2026-07-10 22:27 →
2026-07-11 00:59, one session. Drafted 2026-07-10 22:15; RP-R1
resolved 22:27 («План одобряю, начинай исследование!» + «Goal
set: довести исследование до конца»)._</p>
  <mandate title="1. The mandate (owner, 2026-07-10, verbatim)">
    <p>First message (the commission):</p>
    <quote>Перед тем как что либо делать с планированием RLM процесса, я
хочу чтобы ты провел полный рисёч темы. Например, нашел 5 самых
важных репозиториев на GitHub, посвященным RLM-подобным проектам
которые рекурсивно запускают агентов, 5 самых важных научных
исследований, и 5 самых крутых статей в интернете. Я хочу чтобы
ты сделал это в 3 волны. Одна волна — это Deep Research. Вторая
волна — поиск того же самого через web search без deep research.
Третье — объединение результатов. Дальше ты все эти проекты и
документы качаешь в /refs и изучаешь. Результаты изучения
записываешь в /refs/notes по каждому проекту или документу. Цель
изучения — понять всё про RLM и то, какие идеи нам нужно забрать
себе. Ну и конечно, ты знаешь, есть один самый главный проект и
документ про это: Recursive Language Models | Alex L. Zhang (есть
документ на arxiv, есть код на github). Важное: мы именно
вычленяем и понимаем идеи, мы не должны копировать код. Если нам
хочется скопировать какой-то код — нужно понять его смысл и
реализовать Clean Room Implementation. Это очень-очень важно,
потому что копирование кода приведет к юридическим последствиям.
Всё вышенаписанное это должно быть частью некоторого плана
исследования.</quote>
    <p>Second message (the plan order):</p>
    <quote>Теперь я хочу чтобы ты написал финальный большой план, как мы
будем всё это делать. Ты можешь улучшить уже существующий план
Campaign 3, или написать новый. Как лучше реши сама. В любом
случае, после исследования RLM, план Campaign 3 нужно будет
улучшить нашими идеями.</quote>
    <p>Third message (the execution gate): «Не исполняй план, вначале дай
посмотреть на него» — encoded as RP-R1 (§10).</p>
  </mandate>
  <goal title="2. Goal and non-goals">
    <p>**Goal:** understand everything about RLM — Recursive Language
Models and RLM-like systems that recursively launch agents — well
enough to name, with reasons, the ideas fractality takes for
itself; and exit with the Campaign 3 implementation plan seeded by
those ideas.</p>
    <p>**Non-goals (named so their absence is not oversight):**</p>
    <list ordered="false">
      <item>**No implementation in this stage.** Not one line of product
  code. The stage ends at a drafted Stage-B plan, itself RP-gated.</item>
      <item>**No code copying, ever** — the owner's clean-room order (§4).</item>
      <item>**No exhaustive literature survey.** The cut is 5+5+5 by the §5
  criteria, with a runners-up ledger so nothing found is lost.</item>
    </list>
  </goal>
  <section id="facts" title="3. Current-state facts (verified 2026-07-10; do not re-discover)">
    <list ordered="false">
      <item>**No Campaign 3 plan exists.** `spec/plans/` holds exactly two
  closed plans (IGNITION, INITIATIVE). "Campaign 3 (RLM, DEF-2)"
  lives only as the INITIATIVE §15 deferral. Hence D-R1: this is a
  new plan, and "improve the Campaign 3 plan with our ideas" means
  *authoring* Stage B from this stage's synthesis.</item>
      <item>**The anchor source is already inventoried and license-cleared:**
  [`refs/INVENTORY.xml`](../refs/INVENTORY.xml) S3
  (github.com/alexzhang13/rlm, MIT, pinned `72d6940`) and S4 (arXiv
  2512.24601 PDF, 9.9 MB, downloaded) — deep study deliberately
  deferred to this stage; `notes/rlm-study.md` is reserved as its
  note. The author's blog post (alexzhang13 site) is the third face
  of the same project and joins T1.</item>
      <item>**The refs tree exists and is gitignored wholesale** (verified
  `git check-ignore`): host `&lt;repo&gt;/refs/` with `src/`, `papers/`,
  `book/`, `study/`, `ts/`. `refs/articles/` does not exist yet —
  Ф3 creates it. Study notes are **committed** and live in
  `fractality/v0.1.0/spec/refs/notes/` (3 exist). Mapping of the
  owner's words: «/refs» = host `refs/` (downloads, never
  committed); «/refs/notes» = `spec/refs/notes/` (committed) — the
  established split from IGNITION Ф0.</item>
      <item>**Harness capabilities:** a deep-research skill (multi-agent
  fan-out + verification + cited report) is available for Wave 1;
  plain WebSearch/WebFetch tools for Wave 2. GitVerse over https
  hangs from this box; **GitHub answers fine** — all clones from
  GitHub. Glob tool proved unreliable on this tree today — recipes
  below use PowerShell/git listings.</item>
      <item>**Standing laws that bind this stage:** the clean-room register
  rules (license verdict BEFORE deep study; notes record decisions,
  never text/code shapes; implementation sessions open the note,
  not the source), the delegation law + live-observation protocol,
  the specmap drift law (anchored spec edits re-mint
  `specmap.json` in the same commit), machine quirks (editor-tool
  edits only; commit via `git commit -F - &lt;&lt;'MSG'`).</item>
      <item>**Context this research serves:**
  [`VISION-RECURSIVE-FABRIC.xml`](../VISION-RECURSIVE-FABRIC.xml) —
  V2 (the whole RLM procedure) plus the §5 open questions; the
  INITIATIVE §15 deferrals; PP-002 (credibility facts) as a
  candidate C3 companion.</item>
    </list>
  </section>
  <decisions title="4. Decisions">
    <d-r1 title="D-R1 — a new staged plan; Stage B is the exit deliverable">
      <p>**Decision:** the research is its own plan (this document, Campaign
3 · Stage A). The Campaign 3 implementation plan
(`FRACTALITY-RLM-PLAN-v0.1.xml`, Stage B) is *authored by Ф5* from
the synthesis — that is how the owner's «план Campaign 3 нужно
будет улучшить нашими идеями» lands, given no such plan exists yet.
**Rejected:** embedding the research as Stage B's Phase 0 — the
Phase-0-no-commits law conflicts with committed study notes, and
implementation decisions written before the study would violate the
clean-room order (decisions flow FROM notes). **Revisit:** if Ф5
finds the field too thin to justify a full campaign, Stage B may
shrink to a slice inside another campaign — owner call at RP-R3.</p>
    </d-r1>
    <d-r2 title="D-R2 — three waves, first two independent">
      <p>**Decision:** Wave 1 = the deep-research harness; Wave 2 = plain
web search executed directly, **without reading Wave 1's output
first**; Wave 3 = merge. Independence is the point: two different
methods surface different corners, and their overlap is itself a
measurement (P-R2). **Rejected:** a single deep-research pass
(cheaper, but self-confirming — no second modality to catch what
the harness misses); seeding W2 queries from W1 results (kills
independence). **Revisit:** never — this is the owner's protocol
verbatim.</p>
    </d-r2>
    <d-r3 title="D-R3 — what &quot;most important&quot; means (selection criteria)">
      <p>**Decision:** rank candidates per category by, in order:</p>
      <list ordered="true">
        <item>**Mechanism relevance** — recursion is load-bearing: the system
   recursively spawns sub-agents / sub-LM-calls over tasks or
   context. Generic agent frameworks qualify only if recursive
   delegation is a core, documented mechanism.</item>
        <item>**Idea density for our fabric** — what transfers to
   mission-control + pods + packets (descent, escalation, advisors,
   promotion, budgets/depth, context paging).</item>
        <item>**Influence** — citations, stars, discourse footprint, who
   builds on it.</item>
        <item>**Recency-adjusted** — it is mid-2026; the RLM wave dates late</item>
        <item>Follow-ups, reimplementations, and critiques count.</item>
      </list>
      <p>Repos additionally carry a license check before study (INVENTORY
rule 1). The articles list **must include at least one strong
counterpoint** (anti-recursive/anti-multi-agent argument) — the
two-way-gap law of flow:comparative-research applied to selection.
**Rejected:** ranking by stars/citations alone (popularity is not
mechanism relevance).</p>
    </d-r3>
    <d-r4 title="D-R4 — the intake pipeline (clean-room, verbatim law)">
      <p>**Decision:** every adopted source passes, in order: (1) an
INVENTORY row with source, local path, license verdict, class —
**before any study deeper than LICENSE + README**; (2) download
into the gitignored host tree — repos → `refs/src/&lt;name&gt;/` (clone,
record HEAD sha as pin), papers → `refs/papers/`, articles →
`refs/articles/&lt;slug&gt;.md` snapshot with URL + access date; (3)
study; (4) a committed note in `spec/refs/notes/`. The owner's
clean-room order, operative verbatim: «мы именно вычленяем и
понимаем идеи, мы не должны копировать код. … нужно понять его
смысл и реализовать Clean Room Implementation. … копирование кода
приведет к юридическим последствиям». Methods described in papers
are implementable freely; reference *code* is inspiration-only
regardless of its license; article text is cite-only. **Never:**
port lines, adapt file-by-file, or paste source text into notes.</p>
    </d-r4>
    <d-r5 title="D-R5 — tiered study depth + delegation posture">
      <p>**Decision:** three tiers. **T1 (deep, boss-only):** the anchor
project — S4 paper + S3 repo + the author's blog post → one note
(`rlm-study.xml`). **T2 (medium):** the remaining top-5 papers and
top-5 repos → one note per project. **T3 (survey):** the articles
and any runner-up worth a paragraph → grouped notes allowed. Per
the delegation law, T2/T3 *first-pass surveys* SHOULD be delegated
to GLM over sandboxed copies (S2 precedent: survey delegated, boss
spot-checks load-bearing claims) under the live-observation
protocol, inputs under the launch cwd, `.git` stripped from copies.
**The boss authors every note** — notes are decisions, not
summaries; delegation produces raw material only. **Rejected:**
boss-reads-everything (misappropriates boss tokens — the law);
delegate-writes-notes (a delegate cannot decide what fractality
adopts).</p>
    </d-r5>
    <d-r6 title="D-R6 — note and synthesis formats">
      <p>**Decision:** per-source notes follow the INVENTORY house form —
*what the source achieves, which decisions we take* (and which we
explicitly do not), no text/code shapes from repos; short dated
verbatim quotes are allowed from papers/articles (cite, never
copy into product surfaces). The closing synthesis
(`notes/RLM-SYNTHESIS.md`) follows flow:comparative-research:
quote-first, **two-way gaps** (where the field is ahead of the
fabric / where the fabric is ahead of the field), **numbered
deltas** `RD-1..N` each with priority and target home (Stage B
phase, VISION section, or a PROP anchor), and a **re-fetch list**
(every URL + access date + subject version). The synthesis
proposes; acceptance happens in Stage B (deltas, not decrees).</p>
    </d-r6>
    <d-r7 title="D-R7 — one project, one note">
      <p>**Decision:** a paper + its code + its blog post are **one project
→ one note** (the owner's «по каждому проекту или документу»
resolved toward projects; S3+S4+blog → `rlm-study.xml` is the
worked example). Standalone documents get their own notes.
**Rejected:** one note per artifact — triples the files, splits
one project's decisions across three places.</p>
    </d-r7>
  </decisions>
  <waves title="5. The three waves — frozen protocol">
    <wave-1 title="Wave 1 — deep research">
      <p>Fire the deep-research harness once, with this question (frozen;
edits to it are a plan change):</p>
      <quote>Recursive Language Models (RLM) and RLM-like recursive agent
systems — systems where a language model or agent recursively
spawns sub-LM calls or sub-agents over context or tasks. Anchor,
already known: "Recursive Language Models" by Alex L. Zhang and
Omar Khattab (arXiv 2512.24601; github.com/alexzhang13/rlm; the
author's blog post). It is mid-2026: include follow-ups,
reimplementations, critiques, successors. Deliver three ranked
lists with per-item justification and access-dated URLs: (1) the
5 most important GitHub repositories where recursive spawning of
agents/LM-calls is a core mechanism — reference implementations
and toolkits, not generic agent frameworks unless recursion is
load-bearing — each with its license; (2) the 5 most important
scientific papers on recursive LM inference/decomposition
(recursive delegation, divide-and-conquer over long context or
tasks, context folding/paging); (3) the 5 best practitioner
articles/blog posts, including at least one strong counterpoint
to recursive/multi-agent designs. For every item: which ideas
transfer to a mission-control + pod agent fabric (recursive boss
promotion, escalation, advisor calls, context descent, budgets).</quote>
      <p>Output lands raw in `refs/study/rlm-waves/wave1-deep-research.md`
(gitignored) — kept verbatim for the merge audit.</p>
    </wave-1>
    <wave-2 title="Wave 2 — plain web search, independent">
      <p>Executed directly with the WebSearch tool, **before reading Wave
1's output**. The query battery (frozen; ~top-10 organic results
per query, candidates recorded with URL and date; fetching beyond
snippets only to disambiguate a candidate):</p>
      <fence>1.  "recursive language models"
2.  RLM Zhang Khattab arxiv recursive
3.  github recursive LLM agent spawns sub-agents recursion
4.  recursive agent task decomposition paper 2025 2026
5.  ReDel recursive multi-agent delegation
6.  LLM context folding paging virtual context paper
7.  "chain of agents" OR "LLM x MapReduce" long context
8.  anthropic multi-agent research system orchestrator
9.  don't build multi-agents context engineering critique
10. recursive language model reimplementation OR benchmark 2026
11. best articles recursive LLM agents delegation 2026
12. site:github.com recursive LLM REPL context environment</fence>
      <p>Output lands raw in `refs/study/rlm-waves/wave2-web-search.md`
(gitignored): per-query candidate rows, then one independent
candidate table (category, URL, one-line why).</p>
    </wave-2>
    <wave-3 title="Wave 3 — the merge">
      <p>Union W1+W2; dedup by canonical URL/repo; score against D-R3;
produce the **5/5/5 verdict table** (item, category, score
rationale one paragraph each, license for repos, W1/W2/both
provenance) plus the **runners-up ledger** (every rejected
candidate with its why-not, so the cut is auditable). Deliverable:
[`spec/refs/notes/rlm-source-selection.xml`](../refs/notes/) —
committed; it doubles as the re-fetch list seed. Overlap metric
recorded for P-R2.</p>
    </wave-3>
  </waves>
  <phases title="6. Phases and gates">
    <p>Every phase boundary is a safe stop; the gate panel for a docs-only
phase is "specmap green + the named artifacts committed".</p>
    <list ordered="false">
      <item>**Ф0 — plan lands (this session).** This document + the drafted
  dashboard in `reports/`, specmap re-minted. **Gate: RP-R1 OPEN —
  execution stops here until the owner's word.**</item>
      <item>**Ф1 — the waves.** Fire Wave 1 (background); execute Wave 2
  meanwhile (independence per D-R2); raw outputs to
  `refs/study/rlm-waves/`. No commits (gitignored artifacts only).</item>
      <item>**Ф2 — the merge.** Wave 3 → `rlm-source-selection.xml` +
  INVENTORY rows (license verdicts) for every adopted source.
  One commit. **Gate: RP-R2 — the 5/5/5 table shown to the owner
  in chat; objections rearrange the cut before heavy study.**</item>
      <item>**Ф3 — intake.** Clones @recorded pins into `refs/src/`, PDFs
  into `refs/papers/`, access-dated article snapshots into
  `refs/articles/`; INVENTORY pins/paths finalized. One commit
  (INVENTORY only — downloads are gitignored).</item>
      <item>**Ф4 — study.** T1 first (`rlm-study.xml`), then T2, then T3 per
  D-R5 (delegated surveys under the live-observation law; boss
  authors notes). Commits grouped per tier. **Gate: every adopted
  source is covered by a committed note.**</item>
      <item>**Ф5 — synthesis and exit.** `RLM-SYNTHESIS.xml` (D-R6 form,
  RD-deltas numbered) → **author the Stage B draft**
  (`FRACTALITY-RLM-PLAN-v0.1.xml`) seeded by the deltas + VISION
  §V2/§5 + INITIATIVE §15 leftovers → close this plan (prediction
  verdicts, ledger, WAL, completed-plan dashboard). **Gate: RP-R3 —
  Stage B execution is a separate owner decision; drafting it is
  this plan's last act.**</item>
    </list>
  </phases>
  <predictions title="7. Predictions (checked at close)">
    <list ordered="false">
      <item>**P-R1** — the waves jointly surface ≥ 12 qualifying repos and
  ≥ 12 qualifying papers before the cut. (Falsifier: the field is
  thinner than the vision assumes — itself a Stage-B-shaping fact.)</item>
      <item>**P-R2** — W1∩W2 agreement on the final 5/5/5 is ≥ 60% but
  &lt; 100%. (Falsifier low: one method would have sufficed — the
  3-wave design was overkill; falsifier high: the methods are
  redundant. Either way, recorded for the next research stage.)</item>
      <item>**P-R3** — after study, the Zhang RLM project (S3+S4+blog)
  remains the single most load-bearing source for Stage B. 
  (Falsifier: something supersedes it — a headline finding.)</item>
      <item>**P-R4** — the counterpoint article generates at least one
  RD-delta (a constraint we adopt). (Falsifier: the critique
  teaches us nothing — suspicious; re-read it.)</item>
    </list>
  </predictions>
  <baseline title="8. Baseline and exit state">
    <p>**Baseline (2026-07-10):** INVENTORY 9 rows (S1–S9); S3/S4 status
"study deferred to Campaign 3"; `spec/refs/notes/`: 3 notes;
`refs/src/`: 11 dirs; `refs/articles/`: absent; Campaign 3 plan:
none; VISION §V2 open questions: unanswered.</p>
    <p>**Exit:** INVENTORY carries a row for every adopted source (up to
+13 rows; S3/S4 statuses flipped to studied); `refs/` holds all 15
adopted sources locally; `spec/refs/notes/` holds one note per
adopted project (D-R7) + `rlm-source-selection.xml` +
`RLM-SYNTHESIS.xml` with numbered RD-deltas;
`FRACTALITY-RLM-PLAN-v0.1.xml` exists as a draft; this plan carries
the close record and prediction verdicts; WAL and dashboards
current.</p>
  </baseline>
  <risks title="9. Risks and fallbacks">
    <list ordered="false">
      <item>**Deep-research harness unavailable/degraded** → fallback: Wave 1
  as a manual multi-query fan-out with verification passes;
  recorded as a deviation in the ledger.</item>
      <item>**Link rot / paywalls** → snapshots at first touch (Ф3);
  paywalled papers studied at abstract+blog level and flagged in
  the note; the re-fetch list keeps access dates.</item>
      <item>**License surprises** (a top-5 repo is GPL/AGPL or unlicensed) →
  no crisis: class stays inspiration-only (rule 4), code is never
  adopted from ANY source (D-R4); the license column still gets
  filled for the record.</item>
      <item>**arXiv id sanity** — INVENTORY pins S4 as 2512.24601; verify the
  live id/version at Ф3 and correct the row if the record drifted.</item>
      <item>**Wave overlap too high** (searches collapse onto the same 10
  URLs) → widen W2 with category-specific follow-ups (allowed:
  *adding* queries is not a plan change; *editing* frozen ones is).</item>
      <item>**Volume blowout** (15 sources × deep study exceeds sessions) →
  the tiers ARE the fallback; T3 depth may shrink to grouped
  paragraphs, never below "every source has a recorded verdict".</item>
      <item>**Delegate derailment** on T2/T3 surveys → the live-observation
  law (heartbeats, watcher, kill-correct-relaunch); inputs under
  launch cwd; two cwd strikes already on record — pin cwd in every
  command.</item>
      <item>**Tool quirk** — Glob is unreliable on this tree; recipes use
  PowerShell/git listings.</item>
    </list>
  </risks>
  <review-points title="10. Review points">
    <list ordered="false">
      <item>**RP-R1 — plan review (RESOLVED 2026-07-10 22:27, owner verbatim:
  «План одобряю, начинай исследование!», followed the same minute
  by the execution ruling «Goal set: довести исследование до
  конца»).** The gate order («Не исполняй план, вначале дай
  посмотреть на него») was honored: the plan landed at `82dc31c`,
  the owner reviewed it (asked where cloning/study lives — §6
  Ф3/Ф4, D-R4, §11 — and what preparation remained: none), then
  launched. Posture per the goal ruling: the stage executes
  autonomously through Ф5; RP-R2 becomes show-and-proceed; RP-R3
  still blocks Stage B *execution*.</item>
      <item>**RP-R2 — the 5/5/5 cut (opens at Ф2; show-and-proceed under the
  goal ruling).** The verdict table is shown in chat; the owner may
  swap items at any time and the cut is rearranged — but study does
  not block on a reply. T1 study never waits (the owner already
  named the anchor project primary in the mandate).</item>
      <item>**RP-R3 — Stage B launch (opens at Ф5).** Drafting the Stage B
  plan is in scope; executing it is not — separate owner word.</item>
    </list>
  </review-points>
  <quick-start title="11. Quick-start for the executing session">
    <fence lang="sh"># From the workspace root (cwd law — pin it in the command):
cd /c/Users/olegc/gits/vibevm/packages/org.vibevm.fractality

# State: this plan §6 + the ledger §12; then:
#   Ф1: fire the deep-research skill with §5.1's frozen question;
#       run §5.2's frozen queries via WebSearch; raw → refs/study/rlm-waves/
#   Ф2: merge → fractality/v0.1.0/spec/refs/notes/rlm-source-selection.xml
#       + INVENTORY rows; commit; show the table (RP-R2)
#   Ф3 clone form (record the pin):
git clone --depth 1 &lt;url&gt; /c/Users/olegc/gits/vibevm/refs/src/&lt;name&gt; \
  &amp;&amp; git -C /c/Users/olegc/gits/vibevm/refs/src/&lt;name&gt; rev-parse HEAD
#   Ф4: T1 note first: fractality/v0.1.0/spec/refs/notes/rlm-study.xml
#   Ф5: RLM-SYNTHESIS.xml → spec/plans/FRACTALITY-RLM-PLAN-v0.1.xml (draft)

# Specmap re-mint rides every commit that adds anchored spec files:
cd fractality/v0.1.0 &amp;&amp; /c/Users/olegc/gits/vibevm/packages/org.vibevm.ai-native/rust-ai-native-lang/v0.7.0/target/debug/rust-ai-native.exe specmap</fence>
  </quick-start>
  <acceptance title="12. Whole-stage acceptance">
    <p>At close, all of the following hold: (1) `rlm-source-selection.xml`
exists with 5/5/5 + runners-up + provenance + overlap metric; (2)
every adopted source has an INVENTORY row with pin + license
verdict recorded **before** its deep study began (git history
proves order); (3) all adopted sources present under `refs/`; (4)
one committed note per adopted project; (5) `RLM-SYNTHESIS.xml`
carries numbered RD-deltas each with priority + target home + the
re-fetch list; (6) `FRACTALITY-RLM-PLAN-v0.1.xml` draft exists and
cites the deltas it adopts; (7) P-R1…P-R4 carry verdicts; (8) zero
product-code changes in the whole stage; (9) specmap green at every
phase boundary.</p>
  </acceptance>
  <ledger title="13. Execution ledger">
    <p>_(filled per phase; empty at draft)_</p>
    <table>
      <tr>
        <td>phase</td>
        <td>commits</td>
        <td>confirmed / falsified</td>
        <td>notes</td>
      </tr>
      <tr>
        <td>Ф0</td>
        <td>`82dc31c`, `12f02da`</td>
        <td>—</td>
        <td>plan drafted 22:15; RP-R1 RESOLVED 22:27 (approve + goal ruling); execution begins</td>
      </tr>
      <tr>
        <td>Ф1</td>
        <td>— (gitignored artifacts)</td>
        <td>P-R1 CONFIRMED: ≥16 qualifying repos, ≥17 papers before the cut</td>
        <td>W1: deep-research, 103 agents / 710 tool calls / 24 claims verified 3-0, 31 min (raw: `refs/study/rlm-waves/wave1-deep-research.md`). W2: 12 frozen queries, executed before reading W1 (raw: `wave2-web-search.md`). Independence held.</td>
      </tr>
      <tr>
        <td>Ф2</td>
        <td>`a4c9611`</td>
        <td>overlap exactly 60% (9/15; membership = name in wave record) — P-R2 verdict at close</td>
        <td>5/5/5 cut + runners-up ledgered in `notes/rlm-source-selection.md`; INVENTORY S10–S26 with license verdicts BEFORE study (rule 1). W1-only finds: ROMA, 2506.16411, SRLM; W2-only: ReDel, RAO, minRLM + the practitioner shelf.</td>
      </tr>
      <tr>
        <td>Ф3</td>
        <td>INVENTORY pin commit</td>
        <td>—</td>
        <td>Clones @depth-1 with pins: roma `a6e3bb4`, fast-rlm `f25f310`, redel `79eb988`, recursive-llm `cc7a826`. 7 PDFs valid (incl. anchor v3 9.9 MB). 6 article snapshots + ACCESS-DATES in `refs/articles/`. Real `~` untouched; all downloads gitignored.</td>
      </tr>
      <tr>
        <td>Ф4</td>
        <td>T1 commit + T2 commit + T3 commit</td>
        <td>—</td>
        <td>11 notes / 15 sources + 4 runners-up. Boss read all 7 papers (pdftotext; PDF-render absent on box) + anchor repo; 5 GLM survey delegates over sandboxed copies (2 stalled silently ≥14 min — killed and relaunched per the live-observation law; relaunches clean); boss spot-checked load-bearing claims verbatim before authoring notes. Delegated 7 (5 surveys + 2 relaunches count as re-delegation) / kept: papers (judgment), all note authorship, spot-checks.</td>
      </tr>
      <tr>
        <td>Ф5</td>
        <td>synthesis+draft commit; close commit</td>
        <td>**P-R1 ✓, P-R2 ✓ (exactly 60%), P-R3 ✓, P-R4 ✓**</td>
        <td>`RLM-SYNTHESIS.xml`: two-way gaps + RD-1…21 (6×P1, 8×P2, 7×P3) + VISION §5 answers. Stage B draft authored with scope options A/B/C, decisions D-C3-1…9 citing deltas, RP-C3-1 OPEN. Acceptance §12: (1)–(9) all hold; zero product-code changes in the stage.</td>
      </tr>
    </table>
  </ledger>
  <log title="14. Log">
    <list ordered="false">
      <item>2026-07-10 22:15 — drafted in-session from the owner's three
  messages (mandate, plan order, execution gate); RP-R1 OPEN;
  dashboard `reports/2026-10-07-22-15-rlmresearch-drafted-plan.xml`.</item>
      <item>2026-07-10 22:27 — RP-R1 RESOLVED (approve + «Goal set: довести
  исследование до конца»); Ф1 fired: Wave 1 (deep research,
  background) + Wave 2 (independent web search); dashboard
  `reports/2026-10-07-22-27-rlmresearch-started-plan.xml`.</item>
      <item>2026-07-11 00:59 — CLOSED. W1: 103 agents / 3.74M tokens /
  31 min / 24 claims verified 3-0. W2: 12 queries. Merge 60%
  overlap. Intake: 4 clones pinned, 7 PDFs, 6 snapshots. Study:
  11 notes, 2 delegate stalls killed+relaunched. Synthesis
  RD-1…21; Stage B drafted (RP-C3-1 OPEN). Dashboard
  `reports/2026-11-07-00-59-rlmresearch-completed-plan.xml`.</item>
    </list>
  </log>
</spec>
