<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>MT-04 — a live worker parks on ask_boss and resumes (Phase 4b, D18)</title>
  <p>_Proves: the interaction layer end to end — a headless GLM worker calls
the `ask_boss` MCP tool, the run parks (`waiting_on_boss`, question on
the bus and on the plane), the boss triages with `fractality questions`
and replies with `fractality answer`, the worker receives the text as
its tool result and finishes the task with it. Manual-test #4._</p>
  <p>**Paid:** one GLM (`model = "small"`) worker turn, most of it idle
waiting on the answer. **Isolated:** scratch `--home`; a dedicated
`mt4` profile switches the broker on (`ask_boss = true`).</p>
  <section title="Prerequisites">
    <list ordered="false">
      <item>`~/.fractality/profiles.toml` with the `glm` profile;
  `~/.vibe/zai.api.token` present.</item>
      <item>Built binaries: from `fractality/v1.0.0/`, `cargo build --workspace`.</item>
    </list>
  </section>
  <section title="Steps">
    <list ordered="true">
      <item>Scratch home with the `mt4` profile and the packet:</item>
    </list>
    <fence lang="sh">   cd vibevm/vibepacks/org.vibevm.fractality/fractality/v1.0.0
   MT04_BASE=$(cygpath -m "$(mktemp -d)")
   MT04_HOME="$MT04_BASE/fractality-home"
   mkdir -p "$MT04_HOME"
   cp ~/.fractality/profiles.toml "$MT04_HOME/profiles.toml"
   cat &gt;&gt; "$MT04_HOME/profiles.toml" &lt;&lt;'EOF'

   [profile.mt4]
   backend = "claude-code"
   base_url = "https://api.z.ai/api/anthropic"
   token_file = "~/.vibe/zai.api.token"
   [profile.mt4.models]
   big = "glm-5.2[1m]"
   small = "glm-5-turbo"
   haiku_slot = "glm-5-turbo"
   [profile.mt4.env]
   API_TIMEOUT_MS = "3000000"
   CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC = "1"
   [profile.mt4.permissions]
   mode = "acceptEdits"
   ask_boss = true
   EOF
   cat &gt; "$MT04_BASE/ask.toml" &lt;&lt;'EOF'
   schema = 1
   [task]
   title = "mt4-ask-roundtrip"
   goal = """You must NOT decide the greeting yourself. Step 1: call the ask_boss tool with exactly this question: `Greeting A or greeting B?` Step 2: create the file `greeting.txt` containing ONLY the answer text you received, nothing else. Step 3: stop."""
   [workspace]
   mode = "dir"
   [budget]
   wall_secs = 600
   [routing]
   profile = "mt4"
   model = "small"
   EOF</fence>
    <p>**Expected:** profile block and packet in place.</p>
    <list ordered="true">
      <item>Spawn, wait for the park, read the question:</item>
    </list>
    <fence lang="sh">   R=$(./target/debug/fractality.exe --home "$MT04_HOME" spawn --packet "$MT04_BASE/ask.toml")
   echo "run=$R"
   while ! ./target/debug/fractality.exe --home "$MT04_HOME" questions | grep -q "$R"; do
     sleep 3
   done
   ./target/debug/fractality.exe --home "$MT04_HOME" questions
   ./target/debug/fractality.exe --home "$MT04_HOME" ps --state waiting_on_boss
   cat "$MT04_HOME"/runs/$R/question.md</fence>
    <p>**Expected:** `questions` lists the run with age and the question
   text (`Greeting A or greeting B?`); `ps --state waiting_on_boss`
   shows exactly this run; `question.md` holds the same text (I2: the
   bus carried it, the plane recorded it).</p>
    <list ordered="true">
      <item>Answer; the worker resumes and finishes:</item>
    </list>
    <fence lang="sh">   ./target/debug/fractality.exe --home "$MT04_HOME" answer "$R" "Greeting B - and mention the fractal."
   ./target/debug/fractality.exe --home "$MT04_HOME" wait "$R"
   echo "WAIT_EXIT=$?"
   WORK=$("$MT04_HOME"/../ 2&gt;/dev/null; ./target/debug/fractality.exe --home "$MT04_HOME" show $R --json | grep '"run_dir"' | cut -d'"' -f4)
   cat "$WORK/work/greeting.txt"
   cat "$MT04_HOME"/runs/$R/answer.md</fence>
    <p>**Expected:** `answer` prints `&lt;id&gt; running`; `wait` settles
   `completed exit=0` (`WAIT_EXIT=0`); `greeting.txt` contains the
   answer text (the worker used the tool result, not its own guess);
   `answer.md` persisted the reply.</p>
    <list ordered="true">
      <item>Teardown:</item>
    </list>
    <fence lang="sh">   ./target/debug/fractality.exe --home "$MT04_HOME" mc stop
   rm -rf "$MT04_BASE"</fence>
    <p>**Expected:** `mc stopped`; the real `~/.fractality` untouched.</p>
  </section>
  <section title="Recorded run">
    <p>_(Agent pre-run output is appended below on each execution; the pass is
signed by a human — the pre-run only flags divergence.)_</p>
    <section title="Pre-run 2026-07-10 — GREEN first firing, no divergence">
      <p>**PASS — signed off by the owner, 2026-07-10** (covers the 11-minute
park addendum below).</p>
      <fence lang="text">MT04-RUN=01KX4QEE308PBK98RHHHWJRVHY
questions: 01KX4QEE308PBK98RHHHWJRVHY 6s  Greeting A or greeting B?
ps --state waiting_on_boss: exactly this run
question.md: Greeting A or greeting B?
answer "Greeting B - and mention the fractal."  -&gt;  &lt;id&gt; running
wait: completed exit=0
greeting.txt: Greeting B - and mention the fractal.   (verbatim tool result)
answer.md:    Greeting B - and mention the fractal.
status.json:  "state": "completed"</fence>
      <p>The worker parked ~6 s after boot, resumed the moment the answer
landed, and wrote the reply verbatim — it decided nothing itself. The
phase prediction (a parked worker survives &gt;= 10 minutes idle and
resumes cleanly) was checked by a separate firing with a 30-minute
wall budget and a deliberate 11-minute silence before the answer; see
the addendum below.</p>
    </section>
    <section title="Addendum — the 11-minute park (same day): prediction CONFIRMED">
      <p>Run `01KX4QG4PSNXENAS5HPEK7XP9R` (wall budget 1800 s): parked at ~6 s,
left in `waiting_on_boss` for 11 minutes of deliberate silence
(SILENCE-OVER at 03:51:40), answered, resumed, `completed exit=0`,
`greeting.txt` verbatim. A parked worker burns no tokens — the CC
process blocks on the one MCP tool result. **The phase prediction
holds with a minute to spare over its 10-minute bar.**</p>
    </section>
  </section>
</spec>
