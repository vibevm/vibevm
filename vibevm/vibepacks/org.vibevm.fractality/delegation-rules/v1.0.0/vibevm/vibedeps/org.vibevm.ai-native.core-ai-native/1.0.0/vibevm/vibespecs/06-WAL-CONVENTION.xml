<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">The WAL Convention — session-durable project state</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Discipline v0.2 · status: BETA · T1 · language-neutral · OPTIONAL but preferred**</status-line></p>
  <p><sessions-end-a-project-outlives-them fact="true" status="spec/done">*Agent sessions end, compact, and crash; a project outlives all of them.*</sessions-end-a-project-outlives-them></p>
  <p><CONVENTION-KEEPS-STATE-IN-TWO-FILES fact="true" status="impl/done">*This
convention makes session boundaries cheap by keeping the project's living
state in two repository files.*</CONVENTION-KEEPS-STATE-IN-TWO-FILES></p>
  <p><WAL-IS-OPTIONAL fact="true" status="impl/done">*It is **optional**: every Discipline procedure
that touches it ([Sweep §4](04-SWEEP-PLAYBOOK.xml#output),
[Campaign Form §4](05-CAMPAIGN-FORM.xml#resume), the shipped terraform/sweep
skills) carries an explicit without-WAL branch.*</WAL-IS-OPTIONAL></p>
  <p><WAL-IS-PREFERRED fact="true" status="impl/done">*It is **preferred** because the alternative — resumption
state scattered across commit messages and
plan-status lines — degrades as a project grows.*</WAL-IS-PREFERRED></p>
  <p><ADOPT-WHEN-MORE-THAN-ONE-SESSION fact="true" status="impl/done">*Adopt it when more than one
session (or more than one operator) will ever touch the tree.*</ADOPT-WHEN-MORE-THAN-ONE-SESSION></p>
  <files title="1. The two files">
    <p><WAL-FILE-DESCRIBES-THE-CURRENT-STATE fact="true" status="impl/done">**`vibevm/vibespecs/WAL.xml` — the living checkpoint.** Describes the *current* state:
a dated standing line (what landed, gate-panel state, what's next, known
issues), plus a per-session section for the active campaign.</WAL-FILE-DESCRIBES-THE-CURRENT-STATE></p>
    <p><two-hard-rules-lead fact="true" status="impl/done">Two hard rules:</two-hard-rules-lead></p>
    <facts ordered="false">
      <RULE-REWRITE-NOT-APPEND fact="true" status="impl/done">**Rewrite, not append.** The WAL is a checkpoint, not a log — its lead
  always describes *now*. History lives in git; an append-only WAL rots into
  an archive nobody reads. (Prior standing lines may be demoted into a
  PRIOR-tail as they age; the git log is the authoritative per-item record.)</RULE-REWRITE-NOT-APPEND>
      <RULE-THE-WAL-IS-CANONICAL fact="true" status="impl/done">**The WAL is canonical.** Where the WAL and any snapshot (CONTINUE, a plan's
  status line, a README) disagree, the WAL wins.</RULE-THE-WAL-IS-CANONICAL>
    </facts>
    <p><CONTINUE-FILE-IS-THE-COLD-RESUME-SNAPSHOT fact="true" status="impl/done">**`CONTINUE.md` (repo root) — the cold-resume snapshot.** Written at session
end for whoever picks up cold: TL;DR, where work stands (branch, sync,
tree state), the active blocker and the exact action that unblocks it, the
next-steps recipe with paths and commands, non-obvious findings, and the
recent commit chain.</CONTINUE-FILE-IS-THE-COLD-RESUME-SNAPSHOT></p>
    <p><CONTINUE-IS-OVERWRITTEN-WHOLESALE fact="true" status="impl/done">Overwritten wholesale each time — staleness compounds
otherwise.</CONTINUE-IS-OVERWRITTEN-WHOLESALE></p>
    <p><CONTINUE-IS-A-SNAPSHOT-THE-WAL-SUPERSEDES-IT fact="true" status="impl/done">It is a *snapshot*; the WAL supersedes it.</CONTINUE-IS-A-SNAPSHOT-THE-WAL-SUPERSEDES-IT></p>
  </files>
  <freshness title="2. The freshness rule">
    <p><WAL-OLDER-THAN-24-HOURS-IS-STALE fact="true" status="impl/done">A WAL older than **24 hours** is stale: verify the recorded state against
reality (branch, gates, tree) before any destructive work, and say so to the
owner when the divergence matters.</WAL-OLDER-THAN-24-HOURS-IS-STALE></p>
    <p><FRESHNESS-ENFORCEMENT-IS-ADVISORY fact="true" status="impl/done">Tooling may enforce this advisorily (the
pilot's project linter warns on a stale WAL); the sweep's Tier-2 drift pass
checks it weekly regardless.</FRESHNESS-ENFORCEMENT-IS-ADVISORY></p>
  </freshness>
  <boundaries title="3. Session boundaries">
    <facts ordered="false">
      <BOUNDARY-SESSION-END fact="true" status="impl/done">**Session end (wind-down):** update the WAL's standing line + session
  section; rewrite CONTINUE.md; commit both as their own topic commits.
  The test: a stranger with only the repository resumes without asking.</BOUNDARY-SESSION-END>
      <BOUNDARY-SESSION-RESUME fact="true" status="impl/done">**Session resume:** boot per the project's boot sequence, read the WAL,
  read CONTINUE.md, verify empirically, **report and wait** — a recorded
  "next step" is the candidate, not an authorisation; the owner steers.</BOUNDARY-SESSION-RESUME>
      <BOUNDARY-MID-WORK-CHECKPOINTS fact="true" status="impl/done">**Mid-work checkpoints:** campaigns bump the WAL at phase boundaries
  ([Campaign Form §3–4](05-CAMPAIGN-FORM.xml#gates)); sweeps bump it at
  milestone moves ([Sweep §4](04-SWEEP-PLAYBOOK.xml#output)).</BOUNDARY-MID-WORK-CHECKPOINTS>
    </facts>
  </boundaries>
  <without title="4. Without a WAL">
    <p><OPT-OUT-STILL-OWES-THE-SAME-INVARIANT fact="true" status="impl/done">A project that opts out still owes the same invariant — **resumption state
lives in the repository, never in a session**.</OPT-OUT-STILL-OWES-THE-SAME-INVARIANT></p>
    <p><the-fallbacks-lead fact="true" status="impl/done">The fallbacks the procedures
use:</the-fallbacks-lead></p>
    <facts ordered="false">
      <FALLBACK-PLAN-STATUS-LINE fact="true" status="impl/done">a campaign PLAN carries a status line at its top, updated with each
  phase's commits;</FALLBACK-PLAN-STATUS-LINE>
      <FALLBACK-SWEEP-CLOSING-COMMIT fact="true" status="impl/done">a sweep's closing commit message carries the summary, and the committed
  health snapshot is the trend record;</FALLBACK-SWEEP-CLOSING-COMMIT>
      <FALLBACK-TERRAFORM-REGISTRIES fact="true" status="impl/done">the terraform skill's inventory registries (BROWNFIELD §3) hold what a WAL
  would have held about debt and intent.</FALLBACK-TERRAFORM-REGISTRIES>
    </facts>
    <p><fallbacks-work-but-are-weaker fact="true" status="spec/done">These fallbacks work; they are simply weaker — three places instead of one,
no single canonical "now".</fallbacks-work-but-are-weaker></p>
    <p><SIGNAL-TO-ADOPT-THE-TWO-FILES fact="true" status="impl/done">When a without-WAL project notices it keeps
re-deriving its own state, that is the signal to adopt §1.</SIGNAL-TO-ADOPT-THE-TWO-FILES></p>
  </without>
  <scope title="5. Scope discipline">
    <p><WAL-RECORDS-PROJECT-FACTS fact="true" status="impl/done">The WAL records *project* facts.</WAL-RECORDS-PROJECT-FACTS></p>
    <p><MACHINE-SCOPED-QUIRKS-BELONG-ELSEWHERE fact="true" status="impl/done">Machine-scoped quirks (shell behaviors, OS
footguns of one contributor's box) belong in that machine's user-scoped
notes or the project's boot user-override file — not in the WAL and not in
the Discipline's method documents.</MACHINE-SCOPED-QUIRKS-BELONG-ELSEWHERE></p>
    <p><KEEP-THE-THREE-LAYERS-APART fact="true" status="impl/done">Keep the three layers apart: method
(this package), project (WAL/CONTINUE), machine (user-owned boot snippet).</KEEP-THE-THREE-LAYERS-APART></p>
  </scope>
</spec>
