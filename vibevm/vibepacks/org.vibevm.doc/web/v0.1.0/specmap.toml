# specmap.toml — this project's traceability policy
# (PROP-014; generated by `typescript-ai-native init`, then owned by you).

# The spec:// namespace this project's units are minted under.
namespace = "org.vibevm.doc/web"

scan_roots = ["design/src", "site/src"]
spec_roots = ["vibevm/vibespecs"]
root_spec_docs = []
exempt = []
dispositioned = []

# No [[external_specs]] on purpose. Every unit here cites a requirement of
# PROP-057, which lives in the host repository — and the only way to name
# that tree from inside this package is a path relative to one checkout's
# layout. The package is published as source and built elsewhere, so such
# a path would resolve on exactly one machine. The citations stay whole
# and unresolved here; the host's own gates resolve them.
