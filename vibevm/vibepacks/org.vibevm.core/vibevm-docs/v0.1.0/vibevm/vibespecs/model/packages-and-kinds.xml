<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Packages and their kinds</title>
  <status stage="doc" state="work" audience="user"/>
  <p>Everything vibe installs is a package: a folder with a small description file and the text or tools it delivers. Packages come in eight kinds, and the kind tells you what a package is for before you open it: a way of working, a feature, a technology, a tool, a language guide, an agent server, documentation, or an application.</p>
  <example id="list" fixture="hello-vibe">
    <run>vibe list --path hello-vibe</run>
    <expect></expect>
  </example>
  <a-package title="What a package is">
    <p>A package is a project made installable. It has the same layout as a project, with its own `vibe.toml` and its own `vibevm/vibespecs/`, and it may carry code beside them. When a project installs it, the package's published tree is copied verbatim into the project's dependency tree; nothing is extracted, rewritten or merged.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-052#PACKAGES-CARRY-THE-LAYOUT-TOO"/>
    <p>A package is named by a *[coordinate](../glossary/index.xml#coordinate)*: a group, a slash, and a name, as in `org.vibevm.world/wal`. The group looks like a reversed domain name and says who publishes; the name is unique within the group. A version completes the address when one is needed: `org.vibevm.world/wal@1.0.0`. The kind is not part of the name. It may be written as a prefix on the command line, `flow:org.vibevm.world/wal`, and then vibe checks that the package really is of that kind.</p>
    <rule ref="spec://org.vibevm.core/vibevm/modules/vibe-registry/PROP-008#IDENTITY-TUPLE"/>
  </a-package>
  <the-kinds title="The eight kinds">
    <table>
      <tr><td>Kind</td><td>What it delivers</td><td>Example</td></tr>
      <tr><td>`flow`</td><td>a way of working: commit rules, session notes, review conventions; usually a boot snippet the agent reads every session</td><td>`org.vibevm.world/wal`</td></tr>
      <tr><td>`feat`</td><td>a description of what to build, without saying how</td><td>a welcome page, e-mail sign-in</td></tr>
      <tr><td>`stack`</td><td>a technology context that says how a feature is built with it, or a bundle of a family's members at one version</td><td>`org.vibevm.ai-native/rust-ai-native`</td></tr>
      <tr><td>`tool`</td><td>a script or utility a build step can invoke</td><td>a formatter wrapper</td></tr>
      <tr><td>`lang`</td><td>guidance on how to write in a language or notation</td><td>`org.vibevm.ai-native/rust-ai-native-lang`</td></tr>
      <tr><td>`mcp`</td><td>a server an agent talks to, built from the package's own code</td><td>`org.vibevm.ai-native/rust-ai-native-mcp`</td></tr>
      <tr><td>`doc`</td><td>documentation of other packages: read, never installed</td><td>`org.vibevm.core/vibevm-docs`</td></tr>
      <tr><td>`app`</td><td>a standalone product with its own deployment</td><td>`org.vibevm.doc/web`</td></tr>
    </table>
    <p>The set is closed and grows only by an amendment to the specification; a [manifest](../glossary/index.xml#manifest) with an unknown kind is rejected rather than guessed.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-000#KIND-SET"/>
    <p>The kind is metadata about the package, not part of its identity: it decides where content is placed, what the `--kind` filter of `vibe list` and `vibe search` shows, and whether a kind-prefixed name is accepted. Two packages of different kinds may not share a coordinate, because the coordinate alone is the identity.</p>
    <rule ref="spec://org.vibevm.core/vibevm/modules/vibe-registry/PROP-008#KIND-METADATA"/>
  </the-kinds>
  <families title="Families and companions">
    <p>Some [capabilities](../glossary/index.xml#capability) arrive as several packages that share a name stem: the language guide, the server that serves its tools, and a small bundle that pins both at one version. This is a *family*. Requiring the bundle installs the family, and a change to any member bumps every member to one shared version, so the pieces can never drift apart.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-028#UNISON-LAW"/>
    <p>Documentation is the exception. A package's manual is its *[companion](../glossary/index.xml#companion)*, named with the suffix `-docs` in the same group, and it keeps its own version line: a fixed typo in the manual does not release the tools, and a new tool version does not demand a new manual. The manual says which versions of its subject it describes, and the site picks the newest manual that fits.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-028#ROLE-DOCS"/>
  </families>
  <edge-cases title="Edge cases and rules">
    <p>Changing a package's group or name creates a new package, not a rename: versions do not carry across, and old coordinates are never reused for different content.</p>
    <rule ref="spec://org.vibevm.core/vibevm/modules/vibe-registry/PROP-008#GROUP-CHANGE-NEW-PACKAGE"/>
    <p>A documentation package cannot be installed into a project. `vibe install` refuses it and names the command that fetches it for reading instead.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-057#KIND-DOC-NOT-INSTALLED"/>
    <p>A short name without a group, such as `wal`, is accepted on the command line and resolved through the [registry](../glossary/index.xml#registry) [index](../glossary/index.xml#index-registry); it is convenience, and the manifest always records the full coordinate.</p>
    <rule ref="spec://org.vibevm.core/vibevm/modules/vibe-registry/PROP-008#ROW-SHORT-BEHAVIOUR"/>
  </edge-cases>
</spec>
