<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Что лежит в проекте</title>
  <status stage="doc" state="work" audience="user"/>
  <p>После первой установки в проекте лежит горстка файлов, которые написали вы, и набор побольше, который vibe написал за вас. Эта страница называет каждый из них и говорит, кому можно его менять, чтобы ничего из вашего не было перезаписано и ничего из сгенерированного не правили руками.</p>
  <example ref="tree"/>
  <the-files title="Файлы, один за другим">
    <p>Начните с двух файлов в корне, потому что всё остальное выводится из них.</p>
    <p>`vibe.toml` — это *[манифест](../glossary/index.xml#manifest)*. Его пишете вы, или `vibe init` пишет первую версию за вас. Он называет проект, перечисляет нужные пакеты с диапазоном версий для каждого и перечисляет [реестры](../glossary/index.xml#registry), откуда их брать. Это единственный файл, который нужен коллеге, чтобы воспроизвести вашу настройку, вместе с [лок-файлом](../glossary/index.xml#lock-file) рядом.</p>
    <p>`vibe.lock` — это *лок-файл*. Его пишет vibe, вы его коммитите и никогда не правите. Он записывает точную версию каждого установленного пакета, включая те, что притянули ваши пакеты, и [отпечаток](../glossary/index.xml#fingerprint) (*fingerprint*) содержимого каждого. С ним свежий клон ставит те же байты на любой машине.</p>
    <p>Ниже лежит один каталог, `vibevm/`, а в нём три подкаталога. Эта раскладка одинакова в каждом проекте и каждом пакете, и она не настраивается.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-052#THE-LAYOUT"/>
    <p>`vibevm/vibespecs/` — *ваше* дерево: [спецификации](../glossary/index.xml#specification) и правила, которые пишет сам этот проект, в Markdown или в XML-диалекте проекта. vibe читает его и никогда в него не пишет, с одним исключением, описанным ниже.</p>
    <p>`vibevm/vibedeps/` — дерево *vibe*: по папке на установленный пакет и версию, с опубликованными файлами этого пакета как есть. Вы его коммитите, чтобы свежий клон читался без запуска чего бы то ни было, но никогда не правите. Правка там исчезает при следующей установке.</p>
    <rule ref="spec://org.vibevm.core/vibevm/modules/vibe-workspace/PROP-009#VIBEDEPS-COMMITTED"/>
    <p>В `vibevm/vibepacks/` лежат пакеты, которые этот репозиторий разрабатывает на месте: проект, который и сам публикует пакеты, держит их исходники здесь, и vibe считает этот каталог маленьким локальным реестром. У большинства проектов его нет.</p>
  </the-files>
  <the-boot-files title="Стартовые файлы">
    <p>Исключение в вашем дереве — `vibevm/vibespecs/boot/`. Два файла там ваши: `00-core` держит основы проекта, `90-user` — ваши личные переопределения, и vibe не трогает ни тот, ни другой. Два файла там сгенерированы: `INDEX.md`, манифест того, что читает агент, есть всегда, а `STATIC.md`, текст, который он читает первым и целиком, есть только когда какой-то пакет попросил читать себя именно так. Проект, который закрепил в манифесте `spec_format = "xml"`, получает вместо него `STATIC.xml`. Оба несут заголовок о том, что они сгенерированы; этот заголовок не украшение.</p>
    <rule ref="spec://org.vibevm.core/vibevm/modules/vibe-workspace/PROP-009#ARTIFACTS-GENERATED"/>
    <p>Агент находит стартовые файлы через короткий [управляемый блок](../glossary/index.xml#managed-block) (*managed block*) в конце `CLAUDE.md`, `AGENTS.md` и `GEMINI.md`, между строками `&lt;vibevm&gt;` и `&lt;/vibevm&gt;`. vibe переписывает то, что между двумя маркерами, и ничего больше в этих файлах; остальной файл ваш, и место блока, раз он появился, — тоже ваше.</p>
    <rule ref="spec://org.vibevm.core/vibevm/modules/vibe-workspace/PROP-012#OUTSIDE-PRESERVED"/>
  </the-boot-files>
  <who-writes-what title="Кто что пишет">
    <table>
      <tr><td>Файл или каталог</td><td>Кто пишет</td><td>Коммитится</td><td>Правится руками</td></tr>
      <tr><td>`vibe.toml`</td><td>вы (первую версию — `vibe init`)</td><td>да</td><td>да</td></tr>
      <tr><td>`vibe.lock`</td><td>vibe</td><td>да</td><td>никогда</td></tr>
      <tr><td>`vibevm/vibespecs/`</td><td>вы</td><td>да</td><td>да</td></tr>
      <tr><td>`vibevm/vibespecs/boot/00-core`, `90-user`</td><td>вы</td><td>да</td><td>да</td></tr>
      <tr><td>`vibevm/vibespecs/boot/INDEX.md`, `STATIC.md` или `STATIC.xml`</td><td>vibe</td><td>да</td><td>никогда</td></tr>
      <tr><td>`vibevm/vibedeps/`</td><td>vibe</td><td>да</td><td>никогда</td></tr>
      <tr><td>`vibevm/vibepacks/`</td><td>вы, когда репозиторий разрабатывает пакеты</td><td>да</td><td>да</td></tr>
      <tr><td>блок `&lt;vibevm&gt;` в файлах инструкций для агентов</td><td>vibe</td><td>да</td><td>только его место</td></tr>
      <tr><td>`.vibe/`</td><td>vibe</td><td>нет</td><td>никогда</td></tr>
    </table>
    <p>Последняя строка — черновая область проекта: кэши и внутреннее состояние, которые git игнорирует и которые можно спокойно удалить. Машинное [хранилище](../glossary/index.xml#store) (*store*) скачанных пакетов лежит в другом месте, в вашем домашнем каталоге, и его делят все проекты на машине.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-000#VIBE-DIR-IGNORED"/>
    <p>Ещё одна папка появляется, когда проект начинает вести учёт, какие правила своих пакетов он принял: `vibefacts/`, коммитится, по маленькому TOML-файлу на пакет. Установка пакета не копирует туда ни одного статуса автора; принять их — осознанное действие, `vibe facts adopt`, а `vibe facts` — рычаг для всего остального: перечислить, задать статус по адресу, синхронизировать и отчитаться. Удаление пакета спрашивает, оставить ли его файл принятия или убрать; `vibe facts clean` удаляет файлы исчезнувших пакетов, а после обновления `vibe facts sync` сообщает о [якорях](../glossary/index.xml#anchor), которые пропали или переехали.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-046#REGISTRY-HOME"/>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-046#LAW-SOVEREIGNTY"/>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-046#CLI-FACTS"/>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-046#LAW-LIFECYCLE"/>
  </who-writes-what>
  <edge-cases title="Особые случаи и правила">
    <p>Если вы удалите `vibevm/vibedeps/` или сгенерированные стартовые файлы, `vibe reinstall` восстановит их из лок-файла и хранилища, не трогая сеть.</p>
    <rule ref="spec://org.vibevm.core/vibevm/modules/vibe-workspace/PROP-009#REINSTALL-NO-FORCE"/>
    <p>Если проект из времён до нынешней раскладки несёт корневую папку `spec/`, vibe останавливается с рецептом миграции, а не гадает. Старые раскладки молча не читаются.</p>
    <rule ref="spec://org.vibevm.core/vibevm/common/PROP-052#NO-LEGACY-LAYOUT"/>
    <p>Если вы случайно напишете в `vibevm/vibedeps/`, сразу ничего не сломается; следующая установка перезапишет правку, потому что папка пакета там — дословная копия опубликованного.</p>
  </edge-cases>
</spec>
