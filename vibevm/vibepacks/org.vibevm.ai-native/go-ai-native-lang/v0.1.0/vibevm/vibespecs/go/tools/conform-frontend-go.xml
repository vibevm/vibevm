<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Tool Spec: `go-ai-native-conform-frontend` + `go-extract` — the Go frontend for the language-neutral conform engine</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">*Status: **SHIPPED with this package** (GO-AI-NATIVE-PLAN Phases 4–5) —
`crates/go-ai-native-conform-frontend` (`id = "go-extract"`) +
`crates/go-ai-native-conform` (binary **`go-ai-native-conform`**), fed by
the stdlib-only extractor at `tools/go-extract/` through the
`go-ai-native-extract-bridge` NDJSON protocol.*</status-line></p>
  <p><GO-COUNTERPART-OF-THE-SIBLING-FRONTENDS fact="true" status="impl/done">*The Go counterpart of
`rust-ai-native-conform-frontend` (in-process syn) and
`typescript-ai-native-conform-frontend` (node sidecar): it gives `.go`
code the SAME structural discipline gate, by feeding Go facts into the
language-neutral engine — never by re-implementing the rules in a Go
linter.*</GO-COUNTERPART-OF-THE-SIBLING-FRONTENDS></p>
  <division title="1. The division of labour with the native Go tooling">
    <p><kind-line-division fact="true" status="impl/done">`req r1`</kind-line-division></p>
    <p><GO-TOOLCHAIN-CARRIES-THE-TYPE-CORRECTNESS-HALF fact="true" status="spec/done">Go's own toolchain already carries the **type / correctness** half:
`go build` (the compile gate), `go vet` (shipped correctness census),
staticcheck and the `exhaustive` linter as evidence providers (guide
§1, §5).</GO-TOOLCHAIN-CARRIES-THE-TYPE-CORRECTNESS-HALF></p>
    <p><THAT-HALF-IS-WELL-TYPED-AND-LOCALLY-SANE fact="true" status="spec/done">Those answer *"is this well-typed and locally sane?"* — the
half the language does natively and well.</THAT-HALF-IS-WELL-TYPED-AND-LOCALLY-SANE></p>
    <p><frontend-answers-the-structural-half-lead fact="true" status="impl/done">This frontend answers the **other** half — the *structural /
architectural* rules no Go tool expresses, the ones `conform check`
already enforces for Rust and TypeScript:</frontend-answers-the-structural-half-lead></p>
    <facts ordered="false">
      <RULE-FILE-LENGTH-BUDGET fact="true" status="impl/done">the file-length budget (position is a resource, guide §3);</RULE-FILE-LENGTH-BUDGET>
      <RULE-CELL-ISOLATION fact="true" status="impl/done">cell isolation (a cell imports seams + core only, never sibling
  cells; the registry is the only cell importer — guide §2/§6);</RULE-CELL-ISOLATION>
      <RULE-BAN-CENSUS-AS-FACTS fact="true" status="impl/done">the ban census as facts (`init()` in a cell, blank imports, ambient
  defaults, naked `go` statements, error-string matching, reasonless
  suppressions, `t.Skip` — guide §7's theater list) surfaced as conform
  findings in the Class-F `violates REQ …; fix surface: …` grammar;</RULE-BAN-CENSUS-AS-FACTS>
      <RULE-SEAM-ERROR-CONTRACT fact="true" status="impl/done">the seam-error contract (`go-seam-error-cites-req` — a seam's closed
  error set carries its REQ URI, guide §5): one rule, both halves — the structure half flags a `*Error` type that owns an `Error()` but has no `Spec` field; the message half flags an `Error()` whose rendered text carries no REQ, where a REQ is counted as cited by the literal `spec://` OR the `violates REQ` marker (Go renders the URI out of the `Spec` field, while the format string itself carries the `violates REQ %s` marker);</RULE-SEAM-ERROR-CONTRACT>
      <RULE-DEVIATION-ESCAPE-HATCH fact="true" status="impl/done">the deviation escape hatch (`//spec:deviates … reason="…"`), honoured
  the way `#[spec(deviates)]` is for Rust.</RULE-DEVIATION-ESCAPE-HATCH>
    </facts>
    <p><ONE-ENGINE-ONE-GRAMMAR-ONE-BASELINE fact="true" status="impl/done">Routing these through conform keeps **one rule engine, one finding
grammar, one ratchet baseline** across all three languages, with the
rules defined once over `conform_core::Fact` and fed by any frontend —
a rule cannot drift between projections.</ONE-ENGINE-ONE-GRAMMAR-ONE-BASELINE></p>
  </division>
  <extractor title="2. What go-extract is">
    <p><kind-line-extractor fact="true" status="impl/done">`req r1`</kind-line-extractor></p>
    <p><GO-EXTRACT-IS-A-FACT-PRODUCER fact="true" status="impl/done">A fact producer: parse a `.go` file and emit the language-neutral fact
stream the rules consume.</GO-EXTRACT-IS-A-FACT-PRODUCER></p>
    <p><GO-EXTRACT-IS-STDLIB-ONLY fact="true" status="impl/done">**Stdlib-only by construction** — `go/parser`,
`go/ast`, `go/token`, `encoding/json`, nothing else — so
`go run extract.go` works with no module context, no `go.mod`, no
network, on any machine that carries the toolchain the floor already
requires.</GO-EXTRACT-IS-STDLIB-ONLY></p>
    <p><MINIMUM-EXTERNAL-TOOLING-MADE-STRUCTURAL fact="true" status="impl/done">This is the owner's "minimum external tooling" ideal made
structural: the language parses itself, one file, zero dependencies.</MINIMUM-EXTERNAL-TOOLING-MADE-STRUCTURAL></p>
    <facts ordered="false">
      <DELIVERY-EMBEDDED-AND-CONTENT-ADDRESSED fact="true" status="impl/done">**Delivery:** embedded in the Rust bridge crate (`include_str!`),
  materialised content-addressed to
  `&lt;project&gt;/target/conform/go-extract/extract-&lt;hash16&gt;.go` before
  spawn — the proven ts-extract mechanism. Because exactly one file is
  materialised, the source stays import-free of sibling tool files.</DELIVERY-EMBEDDED-AND-CONTENT-ADDRESSED>
      <PROTOCOL-NDJSON-ON-STDIO fact="true" status="impl/done">**Protocol:** NDJSON on stdio — a `{proto, files: [...]}` request, one
  `{file, facts: [...], markers: [...]}` record per input, `PROTOCOL =
  1`, additive evolution, unparseable file → zero facts + a `degraded`
  note, never a crash (B5).</PROTOCOL-NDJSON-ON-STDIO>
      <FACT-KINDS fact="true" status="impl/done">**Fact kinds** (the ts-extract vocabulary, Go-shaped): `item`
  (func / method / type / const / var; exported flag; receiver;
  attached `//spec:` directives), `import` (path, blank flag),
  `go_unsafe` (the ban census sites with their kind:
  `init_decl` / `blank_import` / `ambient_call` / `naked_go` /
  `error_string_match` / `t_skip` / `reasonless_suppression`),
  `file_metrics` (physical lines), and `marker` (the `//spec:`
  directive stream: tag, uri, r, reason, attached symbol, line).</FACT-KINDS>
      <ONE-EXTRACTION-TWO-CONSUMERS fact="true" status="impl/done">**One extraction, two consumers:** the conform frontend consumes
  `facts`; the specmap scanner consumes `markers`; the tcg relay
  consumes both (TCG-PROTOCOL-GO §3). One parser, one vocabulary, no
  drift.</ONE-EXTRACTION-TWO-CONSUMERS>
    </facts>
  </extractor>
  <frontend title="3. The frontend crate">
    <p><kind-line-frontend fact="true" status="impl/done">`req r1`</kind-line-frontend></p>
    <p><FRONTEND-IMPLEMENTS-THE-ENGINE-TRAIT fact="true" status="impl/done">`go-ai-native-conform-frontend` implements the engine's `Frontend`
trait: an `id()` of `"go-extract"`, a `version()` that bumps when the
fact schema grows (retiring cache slots wholesale, exactly as the
sibling frontends do), and `extract(file, package, module, text) -&gt;
Vec&lt;Fact&gt;` that round-trips through the bridge.</FRONTEND-IMPLEMENTS-THE-ENGINE-TRAIT></p>
    <p><FACTS-ARE-CONTENT-ADDRESSED fact="true" status="impl/done">Facts are keyed
`(file content-hash, frontend id+version)` in the engine's
content-addressed store — a 1-file diff re-extracts 1 file (A2).</FACTS-ARE-CONTENT-ADDRESSED></p>
  </frontend>
  <topology title="4. Topology: the `[go]` policy section">
    <p><kind-line-topology fact="true" status="impl/done">`req r1`</kind-line-topology></p>
    <p><CONFORM-TOML-GAINS-A-GO-SECTION fact="true" status="impl/done">`conform.toml` gains a `[go]` section, written by `go-ai-native init`
from the module layout: `roots` (source roots to scan), `cells_dir`
(default `internal/cells`), `seams_pkg` (default `internal/seams`),
`registry_pkg` (default `internal/registry`), `[go] gated` /
`[[go.exempt]]` `{unit, reason}` (the package is Go's gate unit — the
expand-as-you-conform ratchet: a package enters `gated` only at zero
findings, every other package carrying an exempt reason). The file-length and `invariant-comment-position` rules are fed by **root**
`conform.toml` keys (NOT under `[go]` — they are language-neutral, beside each
other): `max_file_lines` feeds `file-length`; `invariant_comment_markers` /
`invariant_comment_min_file_lines` feed `invariant-comment-position`; and
`sarif_reports` feeds the SARIF ingest whose `LintDiagnosis` facts the
`lint-suppression-needs-reason` rule cites. What those root keys mean, and what
they default to, is described once, in `ENGINE-CONFORM §6` (the policy file) —
this surface names which ones the Go rules read, not their values.</CONFORM-TOML-GAINS-A-GO-SECTION></p>
    <p><THE-SARIF-CITATION-PATH-IS-FED-BY-FOREIGN-LINTERS-NOT-BY-GO-EXTRACT fact="true" status="impl/done">The
`lint-suppression-needs-reason` rule is mounted in this gate too, and it is
deliberately absent from the rule list above: its facts come from SARIF
ingest — `go vet`, `staticcheck`, `golangci-lint` — not from go-extract. It is
the T-sem citation path (§5), where a Discipline rule cites a foreign linter's
diagnosis as its own evidence, and it is the one rule here the extractor does
not feed.</THE-SARIF-CITATION-PATH-IS-FED-BY-FOREIGN-LINTERS-NOT-BY-GO-EXTRACT></p>
    <p><EVERY-PACKAGE-GATED-OR-EXEMPT fact="true" status="impl/done">The
every-package-gated-or-exempt invariant is enforced by the engine on
every check, exactly as for the sibling stacks.</EVERY-PACKAGE-GATED-OR-EXEMPT></p>
  </topology>
  <honesty title="5. The honest note">
    <p><GO-EXTRACT-IS-A-PARSER-NOT-A-TYPE-CHECKER fact="true" status="impl/done">The structural gate is only as good as its facts, and go-extract is a
PARSER, not a type checker: it sees syntax and directives, not
resolved types.</GO-EXTRACT-IS-A-PARSER-NOT-A-TYPE-CHECKER></p>
    <p><TYPE-DEPENDENT-RULES-ARE-OUT-OF-SCOPE fact="true" status="impl/done">Rules that would need type information (e.g. "this
call's receiver is a seam type") are out of this tool's scope and
belong to the vet/staticcheck/exhaustive evidence tier or the oracle.</TYPE-DEPENDENT-RULES-ARE-OUT-OF-SCOPE></p>
    <p><THE-DIVISION-IS-THE-THREE-TIER-SPLIT fact="true" status="impl/done">The division is deliberate — the same three-tier split (T-lex/T-syn/
T-sem) ENGINE-CONFORM §1 defines; go-extract is the T-syn tier, and
Go's T-sem tier is the toolchain itself.</THE-DIVISION-IS-THE-THREE-TIER-SPLIT></p>
  </honesty>
</spec>
