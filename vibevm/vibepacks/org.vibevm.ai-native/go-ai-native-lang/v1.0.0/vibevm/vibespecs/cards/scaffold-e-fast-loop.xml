<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">CARD: scaffold-e-fast-loop — Per-Cell Fast Verification Loop (Go)</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Discipline v0.2 · BETA · T2 · Go**</status-line></p>
  <band-one-identity title="Band 1 — Identity &amp; Recognition">
    <p><CLASSIFICATION fact="true" status="impl/done">Classification: layer=E (verification) + H (weak-reader); mechanism=scaffold E.</CLASSIFICATION></p>
    <p><INTENT fact="true" status="impl/done">Intent: Guarantee every cell is independently buildable and testable in seconds (`go test ./internal/cells/&lt;name&gt;/ -race`), so an agent's edit→check→read-error→edit loop gets a first signal fast enough to steer — the substrate that makes every other scaffold's pass/fail usable. Go hands this scaffold the strongest substrate of the three stacks: per-package testing needs no project references (TS) and no shared cold `target/` (Rust); the compiler was engineered for build speed as a headline feature.</INTENT></p>
    <p><ALSO-KNOWN-AS fact="true" status="spec/done">Also Known As: tight feedback loop; per-package test; incremental check; smoke loop; agent-computer interface.</ALSO-KNOWN-AS></p>
    <p><APPLICABILITY-RECOGNITION fact="true" status="impl/done">Applicability / Recognition: Apply when — a cell cannot be checked without building/testing the whole module set; the only verification is a multi-minute CI run; an agent must wait minutes for a signal. *Detector seed:* `go test ./&lt;cell&gt;/` fails to run in isolation (cross-package test coupling, missing fakes forcing integration setup), OR the cell has no tests at all → recognition fires (verification locality beats capability, R3-007).</APPLICABILITY-RECOGNITION></p>
  </band-one-identity>
  <band-two-justification title="Band 2 — Justification &amp; Tradeoffs">
    <p><MOTIVATION fact="true" status="spec/done">Motivation: A weak agent in a bounded loop gets one signal per multi-minute CI run — useless. With `go test ./internal/cells/batchplanner/ -race` returning in ~1–5 s, the same agent iterates dozens of times in the budget that previously bought zero feedback. The interpreter-budget result confirms: more local runs help agents that can use feedback; the loop is the amplifier.</MOTIVATION></p>
    <p><STRUCTURE-AND-PARTICIPANTS fact="true" status="impl/done">Structure &amp; Participants: *Cell isolation* (a package with injected capabilities — no ambient setup) · *Fast checker* (`go vet` + per-package `go test -race`) · *Structured error* (Class F messages + Go's file:line panics) · *Budget guard* (first signal &lt; ~60s; in practice far under).</STRUCTURE-AND-PARTICIPANTS></p>
    <p><COLLABORATIONS fact="true" status="impl/done">Collaborations: Runs Classes C/D/G checks (invariants, fuzz seeds, Examples all live in the same per-package run); consumes Class B's compile checks; emits Class F diagnostics. The raid executor runs this per batch. Capability injection (§2) is what MAKES the isolation real — a cell touching ambient state needs integration scaffolding and falls out of the loop.</COLLABORATIONS></p>
    <p><GOALS-AND-NON-GOALS fact="true" status="impl/done">Goals / Non-Goals: *Goals:* sub-minute first signal per cell; make the loop the standard agent workflow. *Non-Goals:* NOT replacing full CI (module-wide `go test ./...` still runs at merge); does NOT create capability — scaffolds amplify, they do not create.</GOALS-AND-NON-GOALS></p>
    <p><CONSEQUENCES fact="true" status="spec/done">Consequences: (+) every other scaffold becomes loop-usable; (+) `-race` rides along at per-package cost, so the concurrency discipline (§5) is checked in the same loop. (−) requires cells genuinely isolable (drives §2 cell design); a cell with no tests has NO loop — that is a finding, not a neutral state.</CONSEQUENCES></p>
    <p><ALTERNATIVES fact="true" status="spec/done">Alternatives: whole-module CI only (too slow for an agent loop); manual testing (not mechanical). Neither is a substitute.</ALTERNATIVES></p>
    <p><RISKS-AND-ASSUMPTIONS fact="true" status="spec/done">Risks &amp; Assumptions: assumes cells are isolable; ambient coupling breaks this (anti-pattern). *Sunset:* none — the loop is foundational.</RISKS-AND-ASSUMPTIONS></p>
    <p><EVIDENCE-AND-TRANSFER-STRENGTH fact="true" status="spec/done">Evidence &amp; Transfer-strength: R3-007 (verification locality, theory), R2C interpreter-budget result (more runs amplify capable agents, benchmark), BLD-010 (30–60s practical threshold). Class: benchmark + theory. Tag: **[E-strong]**.</EVIDENCE-AND-TRANSFER-STRENGTH></p>
  </band-two-justification>
  <band-three-operation title="Band 3 — Operation">
    <fence lang="card-ops">trigger: WHEN a cell lacks a sub-minute isolated check, OR has no tests, OR verification requires a whole-module run THEN apply
mode: gate            # a structural precondition, verified at merge
routine:
  1. Make the cell a self-contained package: capabilities injected, no ambient setup (§2).
  2. Ensure `go test ./&lt;cell&gt;/ -race` runs in isolation, &lt; ~60s (in practice seconds).
  3. Wire the cell's Class C/D/G artifacts (invariants, fuzz seeds, Examples) into that same run.
  4. Give the loop a one-line entry: `go-ai-native fast-loop --cell &lt;package&gt;` (asserts the budget).
  5. Confirm first-signal latency is within budget; if not, cut corpus/case counts.
checker: `go-ai-native fast-loop` (cell tests in isolation under budget; a test-less cell fails)
raid_role: layer=infrastructure; order=after:cell-boundaries; batch=cell
budget: active_rules=1; first_signal=&lt;60s by definition</fence>
  </band-three-operation>
</spec>
