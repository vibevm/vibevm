<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">Steward status</title>
  <status stage="impl" state="done"/>
  <p><STATUS-CENTRAL-ONLY fact="true" status="impl/done">This skill is for an owner-facing central session.
If the current task is a worker/reviewer packet, report that the packet—not
central custody—governs and stop.</STATUS-CENTRAL-ONLY></p>
  <procedure title="Procedure">
    <facts ordered="true">
      <STATUS-RESOLVE fact="true" status="impl/done">Resolve the exact repository/worktree/revision binding
   under `~/.vibe/steward/contexts/*/binding.toml`. An explicit context named by
   the owner wins. Do not guess between duplicate exact matches.</STATUS-RESOLVE>
      <STATUS-READ fact="true" status="impl/done">Read global/context settings, custody, the whole
   `plan.toml`, and the repository's actual branch, HEAD, ahead/behind and dirty
   state. Read handoff material only when the owner explicitly requests formal
   handoff inspection or receipt. Session/handoff/takeover records are not
   prerequisites for ordinary status or continuation.</STATUS-READ>
      <STATUS-VERIFY fact="true" status="impl/done">Compare stored claims with the tree. Separate
   project-accepted evidence from local claims and candidates; warn first when
   a valid explicit formal offer is active, an actual conflicting writer is
   observed, or inputs move. `vacant`, `held`, stale heartbeat and mismatched
   holder/session are advisory diagnostics, not authority or a write gate.</STATUS-VERIFY>
      <STATUS-BACKSCAN fact="true" status="impl/done">Check every non-terminal mandate item has a plan node
   and every candidate artifact named by the plan still exists. Do not pay or
   repair the debt during this status-only skill.</STATUS-BACKSCAN>
    </facts>
  </procedure>
  <output title="Output">
    <p><STATUS-OUTPUT fact="true" status="impl/done">Report: context and effective modes; custody holder/epoch;
repository state; accepted boundary; candidate/unaccepted work; full remaining
epic route in compressed form; expanded current frontier; blockers and the
candidate next atom. Then stop and wait for owner direction. Do not edit, claim
custody, run implementation or declare acceptance.</STATUS-OUTPUT></p>
    <p><STATUS-VERSUS-CONTINUE fact="true" status="impl/done">The wait above belongs to a status-only request.
When the owner's current request is to continue work, ordinary continuation may
proceed after the same resolution and verification without a custody claim or
handoff ritual; an observed live conflicting writer or moving input still
stops writes.</STATUS-VERSUS-CONTINUE></p>
  </output>
</spec>
