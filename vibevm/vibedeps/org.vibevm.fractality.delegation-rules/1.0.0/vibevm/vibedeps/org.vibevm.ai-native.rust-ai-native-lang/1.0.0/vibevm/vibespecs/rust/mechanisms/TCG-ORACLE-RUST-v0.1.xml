<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">TCG-ORACLE-RUST v0.1 — the rust-analyzer oracle process model</title>
  <status stage="spec" state="done"/>
  <p><status-line fact="true" status="impl/done">**Status: v0.1 — authored with AGENTIC-TCG-RUST-PLAN v0.1 (Phase 1),
implemented by its Phases 3–4.**</status-line></p>
  <p><companion-documents fact="true" status="impl/done">The component brief is
[`tools/vibe-agentic-tcg-rust.xml`](../tools/vibe-agentic-tcg-rust.xml);
the message grammar is
[`TCG-PROTOCOL-RUST-v0.1`](TCG-PROTOCOL-RUST-v0.1.xml).</companion-documents></p>
  <p><DOCUMENT-OWNS-THE-ORACLE-PROCESS fact="true" status="impl/done">This document
owns the oracle PROCESS: resolution, LSP lifecycle, configuration,
overlays, quiescence, the approximation posture, and latency.</DOCUMENT-OWNS-THE-ORACLE-PROCESS></p>
  <p><SPIKE-FACTS-MEASURED-AGAINST-1-93-1 fact="true" status="impl/done">Spike
facts cited here were measured against rust-analyzer 1.93.1 on
2026-07-07 (AGENTIC-TCG-RUST-PLAN Phase 0).</SPIKE-FACTS-MEASURED-AGAINST-1-93-1></p>
  <resolution title="1. The process and its resolution">
    <p><kind-line-resolution fact="true" status="impl/done">`req r1`</kind-line-resolution></p>
    <p><ORACLE-IS-THE-CONSUMERS-OWN-RUST-ANALYZER fact="true" status="impl/done">The oracle process is the CONSUMER's own `rust-analyzer` binary — the
stack never bundles, links, or vendors an analyzer.</ORACLE-IS-THE-CONSUMERS-OWN-RUST-ANALYZER></p>
    <p><resolution-order-lead fact="true" status="impl/done">Resolution order,
run from the project root so `rust-toolchain.toml` pinning is
honoured, each failure recipe-carrying and never silently skipped:</resolution-order-lead></p>
    <facts ordered="true">
      <RESOLUTION-RUSTUP-WHICH fact="true" status="impl/done">`rustup which rust-analyzer` (the toolchain's component);</RESOLUTION-RUSTUP-WHICH>
      <RESOLUTION-PATH fact="true" status="impl/done">`rust-analyzer` on PATH;</RESOLUTION-PATH>
      <RESOLUTION-HARD-FAILURE fact="true" status="impl/done">hard failure: the bridge's `rust-analyzer-missing` error with the
   recipe `rustup component add rust-analyzer`.</RESOLUTION-HARD-FAILURE>
    </facts>
    <p><STACK-OBLIGES-THE-MACHINE-TO-CARRY-RUST-ANALYZER fact="true" status="impl/done">Installing this stack OBLIGES the machine to carry rust-analyzer (the
same posture as node ≥ 22.6 for the TS stack): inside the stack's own
test suite an absent analyzer is a recipe-carrying FAILURE, never a
skip; outside the stack no obligation exists — a project without
rust-ai-native gets the product's not-installed recipe and owes
nothing.</STACK-OBLIGES-THE-MACHINE-TO-CARRY-RUST-ANALYZER></p>
    <p><RESOLVED-PATH-AND-VERSION-LAND-IN-INIT fact="true" status="spec/done">The resolved path and the server's reported version land in
the `init` result. *Specified, not built — half of it. The version half ships:
`init_result` emits `ra_version` (`crates/rust-ai-native-tcg/src/serve.rs:76-86`),
beside `position_encoding`, `pull_diagnostics` and `quiescent`. The path half
does not: the path IS resolved — `resolve_rust_analyzer`
(`crates/rust-ai-native-tcg-bridge/src/lib.rs:146`) returns it — but it is never
put into the result, and `ra_path` occurs in this package only as the shape
`TCG-PROTOCOL-RUST-v0.1.xml#OP-INIT` promises, never as a field the code emits.*</RESOLVED-PATH-AND-VERSION-LAND-IN-INIT></p>
  </resolution>
  <session title="2. LSP session and capabilities">
    <p><kind-line-session fact="true" status="impl/done">`req r2`</kind-line-session></p>
    <p><BRIDGE-SPEAKS-LSP-3-17-OVER-STDIO fact="true" status="impl/done">The bridge speaks LSP 3.17 over the child's stdio (Content-Length
framing).</BRIDGE-SPEAKS-LSP-3-17-OVER-STDIO></p>
    <p><initialize-declares-lead fact="true" status="impl/done">The `initialize` request declares:</initialize-declares-lead></p>
    <facts ordered="false">
      <DECLARES-UTF8-POSITION-ENCODING fact="true" status="impl/done">utf-8 in
  `general.positionEncodings` (granted by 1.93.1 — positions then need
  line-base conversion only; the utf-16 fallback converts through the
  line's text and is unit-tested on non-ASCII content),</DECLARES-UTF8-POSITION-ENCODING>
      <DECLARES-PULL-DIAGNOSTICS fact="true" status="impl/done">pull diagnostics
  (`textDocument.diagnostic`),</DECLARES-PULL-DIAGNOSTICS>
      <DECLARES-WORK-DONE-PROGRESS fact="true" status="impl/done">`window.workDoneProgress`,</DECLARES-WORK-DONE-PROGRESS>
      <DECLARES-SERVER-STATUS-NOTIFICATION fact="true" status="impl/done">and the
  experimental `serverStatusNotification`.</DECLARES-SERVER-STATUS-NOTIFICATION>
    </facts>
    <p><DOWNSTREAM-FEATURES-KEY-OFF-THE-GRANTED-SET fact="true" status="impl/done">Every downstream feature keys
off the GRANTED set — a capability the server did not grant degrades
per §6 into a well-formed error or a documented fallback, never a
crash.</DOWNSTREAM-FEATURES-KEY-OFF-THE-GRANTED-SET></p>
    <p><BRIDGE-ANSWERS-THE-SERVERS-OWN-REQUESTS fact="true" status="impl/done">The bridge answers the server's own requests:
`workspace/configuration` (with §3's config object),
`window/workDoneProgress/create` and `client/registerCapability` (null
results).</BRIDGE-ANSWERS-THE-SERVERS-OWN-REQUESTS></p>
  </session>
  <config title="3. Configuration: experimental diagnostics, deliberately on">
    <p><kind-line-config fact="true" status="impl/done">`req r3`</kind-line-config></p>
    <p><SPIKE-FINDING-EXPERIMENTAL-DIAGNOSTICS-ARE-DEFAULT-OFF fact="true" status="spec/done">The spike's central finding: rust-analyzer's most valuable native
diagnostics — type-mismatch (E0308), unresolved-name (E0425) — sit
behind the DEFAULT-OFF `diagnostics.experimental.enable`.</SPIKE-FINDING-EXPERIMENTAL-DIAGNOSTICS-ARE-DEFAULT-OFF></p>
    <p><NULL-CONFIG-ORACLE-ANSWERS-SILENCE fact="true" status="spec/done">A
null-config oracle answers silence for the very classes the tool
exists to catch.</NULL-CONFIG-ORACLE-ANSWERS-SILENCE></p>
    <p><BRIDGE-SHIPS-ONE-CONFIG-OBJECT fact="true" status="impl/done">The bridge therefore ships one config object —
`{"diagnostics": {"experimental": {"enable": true}}}` — passed BOTH as
`initializationOptions` and as every `workspace/configuration` answer.</BRIDGE-SHIPS-ONE-CONFIG-OBJECT></p>
    <p><ENABLING-EXPERIMENTAL-IS-A-DOCUMENTED-POSTURE fact="true" status="spec/done">This is a deliberate, documented posture: the enabled set is
experimental by rust-analyzer's own naming, which is one more reason
§5's approximation statement is spec, not fine print.</ENABLING-EXPERIMENTAL-IS-A-DOCUMENTED-POSTURE></p>
    <p><FUTURE-CONFIG-NEEDS-EXTEND-ONE-OBJECT fact="true" status="spec/done">Future config
needs (feature flags, cargo target selection) extend this object in
one place.</FUTURE-CONFIG-NEEDS-EXTEND-ONE-OBJECT></p>
  </config>
  <overlays title="4. Overlays and versions">
    <p><kind-line-overlays fact="true" status="impl/done">`req r4`</kind-line-overlays></p>
    <p><OVERLAY-IS-AN-LSP-OWNED-TEXT-DOCUMENT fact="true" status="impl/done">An overlay is an LSP-owned text document: `didOpen {uri, version: 1,
text}` claims the document (the server stops reading disk for it),
`didChange` with full-text sync and a MONOTONICALLY increasing
per-document version replaces it, `didClose` releases it back to disk.</OVERLAY-IS-AN-LSP-OWNED-TEXT-DOCUMENT></p>
    <p><rules-are-lsp-native-law-lead fact="true" status="impl/done">The rules the TS campaign learned the hard way are LSP-native law
here and the bridge enforces them structurally:</rules-are-lsp-native-law-lead></p>
    <facts ordered="false">
      <OVERLAY-RULE-VERSIONS-NEVER-REPEAT fact="true" status="impl/done">versions never repeat within an
  overlay's lifetime (a monotonic counter per open document, never derived
  from content); clearing an overlay closes the document and a later
  reopen starts again at 1 —
  `crates/rust-ai-native-tcg-bridge/src/oracle.rs:184` (`docs.remove`);</OVERLAY-RULE-VERSIONS-NEVER-REPEAT>
      <OVERLAY-RULE-VALIDATE-WITHOUT-CONTENT-READS-DISK fact="true" status="impl/done">`validate` WITHOUT inline content reads the disk file and opens it
  with that text, so version bookkeeping has exactly one owner (the
  bridge) and a later disk edit is picked up by the next validate's
  `didChange`;</OVERLAY-RULE-VALIDATE-WITHOUT-CONTENT-READS-DISK>
      <OVERLAY-RULE-FILE-NEED-NOT-EXIST-ON-DISK fact="true" status="impl/done">an overlaid file need not exist on disk — a hypothetical new module
  participates via `didOpen` alone (spike-proven: a seeded error in a
  pure overlay is diagnosed with zero disk writes);</OVERLAY-RULE-FILE-NEED-NOT-EXIST-ON-DISK>
      <OVERLAY-RULE-NULL-CONTENT-MAPS-TO-DIDCLOSE fact="true" status="impl/done">`update {content: null}` maps to `didClose`.</OVERLAY-RULE-NULL-CONTENT-MAPS-TO-DIDCLOSE>
    </facts>
  </overlays>
  <approximation title="5. The approximation posture (r-a is not rustc)">
    <p><kind-line-approximation fact="true" status="impl/done">`req r5`</kind-line-approximation></p>
    <p><THIS-ORACLE-ANSWERS-WITH-INDEPENDENT-ANALYSIS fact="true" status="spec/done">The TS oracle answers with tsc's own engine; THIS oracle answers with
rust-analyzer's independent analysis, which is deliberately partial.</THIS-ORACLE-ANSWERS-WITH-INDEPENDENT-ANALYSIS></p>
    <p><consequences-all-normative-lead fact="true" status="impl/done">Consequences, all normative:</consequences-all-normative-lead></p>
    <facts ordered="false">
      <CLEAN-VALIDATE-DOES-NOT-CERTIFY-A-CLEAN-FLOOR fact="true" status="impl/done">A clean `validate` does NOT certify a clean floor. The floor
  (`rust-ai-native floor` — the seven steps: cargo fmt → cargo test →
  clippy → conform → specmap → test-gate → fast-loop; the compile rides
  inside the test step) remains the truth; consumer-facing docs repeat
  it.</CLEAN-VALIDATE-DOES-NOT-CERTIFY-A-CLEAN-FLOOR>
      <DIFFERENTIAL-CORPUS-CURATES-NATIVE-COMPETENCE fact="true" status="impl/done">The differential corpus curates classes INSIDE r-a's native
  competence; each class is pinned to cargo check through the
  committed mapping table (1.93.1 rows: E0308↔E0308, E0425↔E0425,
  E0107↔E0061 arity, E0559↔E0609 unknown-field, E0063↔E0063,
  E0599↔E0599). Diagnostic CODES may differ for the same defect;
  existence-grain agreement is the claim, through the table.</DIFFERENTIAL-CORPUS-CURATES-NATIVE-COMPETENCE>
      <KNOWN-SILENCES-ARE-DOCUMENTED-GAP-CASES fact="true" status="impl/done">Known silences are DOCUMENTED-GAP corpus cases, not omissions:
  privacy at 1.93.1 is the standing exhibit — the oracle answers
  nothing while cargo check speaks (rustc's code depends on the
  reference shape: E0423 for a use-imported tuple constructor, E0603
  for the module-path form — one defect class, two codes; the corpus
  pins the E0423 shape). The case asserts exactly that asymmetry so a
  future r-a flips it red and the gap list never rots.</KNOWN-SILENCES-ARE-DOCUMENTED-GAP-CASES>
      <OPEN-DELTA-CLASS-IS-NAMED fact="true" status="impl/done">Borrow-check subtleties, trait-solver edges, and macro-heavy code
  are named as the open delta class; no corpus case claims them.</OPEN-DELTA-CLASS-IS-NAMED>
    </facts>
  </approximation>
  <degradation title="6. Quiescence, degradation, never crashes">
    <p><kind-line-degradation fact="true" status="impl/done">`req r6`</kind-line-degradation></p>
    <p><SERVER-LOADS-THE-WORKSPACE-AFTER-INITIALIZED fact="true" status="spec/done">After `initialized`, the server loads the workspace (cargo metadata,
cache priming).</SERVER-LOADS-THE-WORKSPACE-AFTER-INITIALIZED></p>
    <p><BRIDGE-WAITS-FOR-QUIESCENT-SERVER-STATUS fact="true" status="impl/done">The bridge waits for `experimental/serverStatus` with
`quiescent: true`, bounded by a deadline — and that flag is the ONLY
trusted signal.</BRIDGE-WAITS-FOR-QUIESCENT-SERVER-STATUS></p>
    <p><two-live-chain-findings-lead fact="true" status="impl/done">Two live-chain findings harden this (2026-07-07,
Phase 3):</two-live-chain-findings-lead></p>
    <facts ordered="false">
      <FINDING-A-NO-SERVER-STATUS-ECHO fact="true" status="spec/done">(a) rust-analyzer does NOT echo `serverStatusNotification`
  in its InitializeResult even though it honours the declared client
  capability, so there is nothing to key a capability check off — the
  bridge declares and trusts the channel;</FINDING-A-NO-SERVER-STATUS-ECHO>
      <FINDING-B-PROGRESS-DRAIN-HEURISTIC-FALSIFIED fact="true" status="spec/done">(b) a progress-drain
  heuristic ("initial workDoneProgress tokens ended") was tried and
  FALSIFIED twice — a fast first token drains while indexing continues,
  yielding confident empty answers — so it is deliberately ABSENT, and
  a replay test pins that progress noise never satisfies the wait.</FINDING-B-PROGRESS-DRAIN-HEURISTIC-FALSIFIED>
    </facts>
    <p><DEADLINE-PASS-DEGRADES fact="true" status="impl/done">A
deadline pass degrades: answers carry `degraded: true`, so callers
can distinguish warm truth from cold best-effort.</DEADLINE-PASS-DEGRADES></p>
    <p><b5-extends-to-the-whole-session-lead fact="true" status="impl/done">B5 extends to the whole
session:</b5-extends-to-the-whole-session-lead></p>
    <facts ordered="false">
      <B5-UNKNOWN-OP-ANSWERS-A-PROTOCOL-ERROR fact="true" status="impl/done">an op the relay does not know answers a protocol error naming
  the known set;</B5-UNKNOWN-OP-ANSWERS-A-PROTOCOL-ERROR>
      <B5-ANALYZER-CRASH-ENDS-THE-SESSION fact="true" status="impl/done">an analyzer crash surfaces `oracle-crashed` op-grain
  and ends the session (the product registry owns respawn-once);</B5-ANALYZER-CRASH-ENDS-THE-SESSION>
      <B5-NO-INPUT-MAY-POISON-THE-SESSION fact="true" status="impl/done">no input may poison the session.</B5-NO-INPUT-MAY-POISON-THE-SESSION>
    </facts>
  </degradation>
  <lifecycle title="7. Process lifecycle and Windows discipline">
    <p><kind-line-lifecycle fact="true" status="impl/done">`req r7`</kind-line-lifecycle></p>
    <p><ONE-LONG-LIVED-CHILD-PER-ROOT-SESSION fact="true" status="impl/done">One long-lived child per (root, session).</ONE-LONG-LIVED-CHILD-PER-ROOT-SESSION></p>
    <p><GRACEFUL-EXIT-AND-THE-NO-ZOMBIE-PROPERTY fact="true" status="impl/done">Graceful exit is the LSP
dance — `shutdown` request, `exit` notification — with kill-on-drop as
the backstop; the no-zombie property is test-asserted (spike-proven:
clean exit code 0, no surviving pid). *Built 2026-08-05 — the proof caught up
to the mechanism, which had shipped long before it. The dance ships
(`shutdown` at `crates/rust-ai-native-tcg-bridge/src/oracle.rs:356`) and so
does the backstop (`Drop for ChildTransport` → `kill()` then `wait()`,
`client.rs:346-350`). The assertion now ships too:
`dropping_the_oracle_kills_the_child_process_no_zombie`
(`tests/live_oracle.rs`) reads the child PID through
`ChildTransport::child_pid()`, proves that process is alive, drops the wrapper
— deliberately NOT the graceful path, so the kill-on-drop backstop is what is
exercised — and polls the OS process table until the PID is gone, treating a
recycled PID as dead by comparing `start_time`. Between 2026-08-04 and this
build the sentence was a promise; it is now a description.*</GRACEFUL-EXIT-AND-THE-NO-ZOMBIE-PROPERTY></p>
    <p><PATHS-BECOME-URIS-AFTER-PREFIX-STRIPPING fact="true" status="impl/done">Paths become URIs only after
verbatim-prefix stripping (`\\?\` breaks child argv and URI builders —
the standing lesson's fourth home).</PATHS-BECOME-URIS-AFTER-PREFIX-STRIPPING></p>
    <p><STDOUT-CARRIES-LSP-FRAMES-ONLY fact="true" status="spec/done">stdout carries LSP frames only;
rust-analyzer's own stderr chatter is drained and discarded by the
reader (surfaced only in bridge debug logging), so protocol streams
stay clean. *Specified, not built — the conclusion holds, the mechanism
described does not. Protocol streams do stay clean, but not by draining:
the child is spawned `.stderr(std::process::Stdio::null())`
(`crates/rust-ai-native-tcg-bridge/src/client.rs:303`, and again at
`lib.rs:163`), so the OS discards the chatter at the pipe and no reader
ever sees it. There is no bridge debug logging to surface it in — the
bridge crate has no logging facility at all.*</STDOUT-CARRIES-LSP-FRAMES-ONLY></p>
  </lifecycle>
  <latency title="8. Latency posture">
    <p><kind-line-latency fact="true" status="impl/done">`req r8`</kind-line-latency></p>
    <p><TARGETS-ARE-POSTED-AND-MEASURED-NEVER-GATED fact="true" status="impl/done">Targets are POSTED and MEASURED, never CI-gated (the standing split:
gate what cannot flake, record what can).</TARGETS-ARE-POSTED-AND-MEASURED-NEVER-GATED></p>
    <p><spike-facts-lead fact="true" status="impl/done">Spike facts on a minimal
crate, this box:</spike-facts-lead></p>
    <facts ordered="false">
      <SPIKE-INIT-HANDSHAKE fact="true" status="impl/done">init handshake ~10 ms;</SPIKE-INIT-HANDSHAKE>
      <SPIKE-INIT-TO-QUIESCENT fact="true" status="impl/done">init-to-quiescent 14.7 s
  cache-COLD (sysroot indexing dominates) and 2.5 s warm;</SPIKE-INIT-TO-QUIESCENT>
      <SPIKE-WARM-PULL-DIAGNOSTICS fact="true" status="impl/done">warm pull
  diagnostics 1–2 ms;</SPIKE-WARM-PULL-DIAGNOSTICS>
      <SPIKE-HOVER fact="true" status="impl/done">hover ~1 ms;</SPIKE-HOVER>
      <SPIKE-COMPLETION fact="true" status="impl/done">completion ~19 ms at 118 entries.</SPIKE-COMPLETION>
    </facts>
    <p><posted-targets-lead fact="true" status="impl/done">Posted targets for demo-class trees:</posted-targets-lead></p>
    <facts ordered="false">
      <TARGET-WARM-VALIDATE fact="true" status="impl/done">warm `validate` p50 &lt; 500 ms,</TARGET-WARM-VALIDATE>
      <TARGET-WARM-COMPLETE fact="true" status="spec/done">`complete` p50 &lt; 300 ms — posted, not yet
  measured: the bench harness times `validate` only
  (`crates/rust-ai-native-tcg/src/bench.rs` emits `cold_init_ms`,
  `validate_p50_ms`, `validate_p95_ms` and no `complete` field); the
  measurement corpus is deliberately far-future work (`BACKLOG.md`
  B-042),</TARGET-WARM-COMPLETE>
      <TARGET-COLD-INIT-TO-QUIESCENT fact="true" status="impl/done">cold init-to-quiescent &lt; 15 s.</TARGET-COLD-INIT-TO-QUIESCENT>
    </facts>
    <p><BENCH-HARNESS-RECORDS-DISTRIBUTIONS fact="true" status="impl/done">The bench
harness records distributions per run; a target that moves, moves in a
committed REPORT with a reason — and per the owner's resolution a miss
CANCELS NOTHING: the campaign proceeds and the miss is reported
prominently.</BENCH-HARNESS-RECORDS-DISTRIBUTIONS></p>
    <p><LARGE-WORKSPACE-CONSUMERS-ARE-WARNED fact="true" status="impl/done">Large-workspace consumers are warned —
the tcg brief's `##RISK-COLD-INIT-ON-LARGE-WORKSPACES` carries the
spec-layer warning (14.7 s cache-cold on a minimal crate; a big consumer
tree may exceed the first-request budget) — and the relay's eager init at
`serve` start (before the host's first frame) spends the cold cost as
early as possible. The shipped ceiling is the **45 s** quiescence budget
(`QUIESCENCE_BUDGET`, `crates/rust-ai-native-tcg/src/lib.rs:33`, used by
`spawn_oracle` and `serve`); this document's own
`##TARGET-COLD-INIT-TO-QUIESCENT` posts &lt; 15 s for demo-class trees.</LARGE-WORKSPACE-CONSUMERS-ARE-WARNED></p>
  </latency>
</spec>
