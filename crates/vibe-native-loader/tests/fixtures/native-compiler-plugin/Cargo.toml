[package]
name = "vibe-native-loader-compiler-fixture"
version = "1.0.0"
edition = "2024"
publish = false

[lib]
name = "vibe_native_loader_compiler_fixture"
crate-type = ["rlib", "cdylib"]

[features]
default = ["abi"]
abi = []

[dependencies]
base64.workspace = true
quick-xml.workspace = true
sha2.workspace = true
vibe-ext = { path = "../../../../vibe-ext" }
