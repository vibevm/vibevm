[package]
name = "vibe-facts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Consumer-owned adoption-facts registry for vibevm projects"

[dependencies]
specmark.workspace = true
progress-core.workspace = true
serde.workspace = true
sha2.workspace = true
thiserror.workspace = true
toml.workspace = true
# The spec:// router's forward law (canonical_doc_path) — sync mints
# addresses in the same form the router resolves.
vibe-core.workspace = true
vibe-spec.workspace = true
# The PROP-045 pivot IR is the one shared traversal for authored package
# facts used by both adopt and the adoption report.
vibe-specdoc.workspace = true

[dev-dependencies]
tempfile.workspace = true
