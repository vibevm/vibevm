# Example Thing {#root}

A host module doc whose filename carries a descriptive slug, so resolving
`spec://org.vibevm.core/vibevm/modules/demo/PROP-042` must invert the `PROP-NNN` truncation.

## Detail {#detail}

Detail body.
