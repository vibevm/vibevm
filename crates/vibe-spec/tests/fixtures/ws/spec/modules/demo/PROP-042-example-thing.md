# Example Thing {#root}

A host module doc whose filename carries a descriptive slug, so resolving
`spec://vibevm/modules/demo/PROP-042` must invert the `PROP-NNN` truncation.

## Detail {#detail}

Detail body.
