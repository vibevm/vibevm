// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Wire format for `vibe-index get <data-dir> <group> <name> --json` (PROP-
/// 008 §2.2): one package's usable versions by its `(group, name)` identity,
/// with the versions this build refused named beside them. Read by scripts
/// and agents (PROP-044 §6.5 — a CLI `--json` report is a format plain text;
/// `foreign_parsers = many`). Source of truth for the writer: `crates/vibe-
/// index/src/cli/get.rs` (`GetEnvelope`); the generated reader this schema owns
/// lives in `crates/vibe-wire/src/generated/index_cli/e1/get_report/`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct GetReport {
    /// Always `"get"` for this report.
    pub command: String,

    /// Whether the asked-for fact came back: `true` for a whole-package ask
    /// whenever the `(group, name)` identity stands in the index — even when
    /// every version is refused — and for a `--version` ask exactly when that
    /// version was served. `false` names an identity (or version) the index
    /// does not hold or cannot act on; `unavailable` says why.
    pub found: bool,

    /// The reverse-FQDN group qualifier of the ask, echoed back verbatim.
    pub group: Group,

    /// The bare package name of the ask, echoed back verbatim.
    pub name: String,

    /// Every version of the package this build can act on (`usable_versions`,
    /// PROP-044 §4.5), narrowed to the ask when `--version` named one. Required
    /// member: a not-found ask writes `[]`, never an absent key — the writer
    /// has no `skip_serializing_if` here.
    pub versions: Vec<VersionEntry>,

    /// Versions of this package this build refused to serve — named, not
    /// hidden (PROP-044 §4.5), narrowed to the ask when `--version` named one.
    /// Absent when there are none (the writer carries `skip_serializing_if =
    /// "Vec::is_empty"`).
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub unavailable: Vec<Unavailable>,
}

pub use crate::generated::shared::BootSnippetEntry;

pub use crate::generated::shared::CompatibilityEntry;

pub use crate::generated::shared::ConflictsEntry;

pub use crate::generated::shared::DeliveryMode;

pub use crate::generated::shared::FeaturesEntry;

pub use crate::generated::shared::Group;

pub use crate::generated::shared::I18nEntry;

pub use crate::generated::shared::ObsoletesEntry;

pub use crate::generated::shared::PackageKind;

pub use crate::generated::shared::ProvidesEntry;

pub use crate::generated::shared::RequiresAnyEntry;

pub use crate::generated::shared::RequiresEntry;

pub use crate::generated::shared::SubskillEntry;

pub use crate::generated::shared::Timestamp;

/// One version this build refused to serve, and why — the row
/// every `vibe-index` answering surface carries (`crates/vibe-
/// index/src/index/quarantine.rs`, `Unavailable`). It names the full coordinate
/// even inside an envelope that already names the package: a row that
/// identifies itself survives being copied out of its envelope by a script.
/// Local to this schema family: `formats/vocabularies.json` does not (yet)
/// carry it, and each of the six report schemas that need it declare their
/// own copy.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct Unavailable {
    /// The refused version's group.
    pub group: Group,

    /// The reader capabilities of `must_understand` this build lacks, in the
    /// record's own order. Required member: a refusal always writes its list,
    /// `[]` never occurring in practice today (a row exists exactly because the
    /// list is non-empty) but lawful on the wire — the writer has no skip here.
    pub missing: Vec<String>,

    /// The refused version's bare package name.
    pub name: String,

    /// What a person or a script does about it — the fixed two-part text
    /// `recipe_for` builds (the explanation with the PROP-044 anchor, then the
    /// `fix:` surface).
    pub recipe: String,

    /// The refused version.
    pub version: Version,
}

pub use crate::generated::shared::Version;

pub use crate::generated::shared::VersionEntry;

pub use crate::generated::shared::WorkspaceOriginEntry;
