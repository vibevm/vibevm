// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Common JSON problem-details envelope for structured HTTP failures.
/// `ApiError` emits it for 400, 401, 403, 404 and 500; the rate-limit
/// middleware emits the same four required members for 429; quarantine refusals
/// add `unavailable`. Source of truth: `crates/vibe-index/src/server/error.rs`
/// (`ApiError`, private `Body`) and `crates/vibe-index/src/server/mod.rs`
/// (`rate_limited_response`); the generated reader lives in `crates/vibe-
/// wire/src/generated/index_http/e1/error_response/`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct ErrorResponse {
    /// Request-specific explanation and, where applicable, a fix surface.
    pub detail: String,

    /// HTTP status code repeated in the body (400, 401, 403, 404, 429 or 500
    /// today).
    pub status: u16,

    /// Short human-readable problem title selected by the server error
    /// constructor.
    pub title: String,

    /// Stable problem kind such as `vibe-index/error/not-found`,
    /// `.../unavailable` or `.../rate-limited`. A string rather than a closed
    /// enum: adding a new error kind must remain readable by older raw parsers.
    #[serde(rename = "type")]
    pub type_: String,

    /// The full refused-version row on `vibe-index/error/unavailable`; absent
    /// on every other problem kind.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unavailable: Option<Unavailable>,
}

pub use crate::generated::shared::Group;

/// One package version this build refuses and the recovery recipe, mirroring
/// `crates/vibe-index/src/index/quarantine.rs::Unavailable`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct Unavailable {
    /// The refused version's group qualifier.
    pub group: Group,

    /// Reader capabilities unknown to this build, in record order.
    pub missing: Vec<String>,

    /// The refused version's bare package name.
    pub name: String,

    /// The generated explanation and fix surface for the refusal.
    pub recipe: String,

    /// The refused semantic version.
    pub version: Version,
}

pub use crate::generated::shared::Version;
