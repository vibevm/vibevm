// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Wire format shared by the JSON liveness and readiness responses
/// (`GET /healthz`, `GET /readyz`). Source of truth for the writer:
/// `crates/vibe-index/src/server/routes/health.rs` (`Health`,
/// `healthz`, `readyz`); the generated reader lives in `crates/vibe-
/// wire/src/generated/index_http/e1/health_response/`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct HealthResponse {
    /// The registry name held by the in-memory index at probe time.
    pub registry: String,

    /// The probe verdict: `ok` from the liveness route or `ready` from the
    /// readiness route.
    pub status: HealthStatus,
}

/// The two probe states the server implements. Closed because each value names
/// one concrete handler, not an extensible external register.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub enum HealthStatus {
    #[serde(rename = "ok")]
    Ok,

    #[serde(rename = "ready")]
    Ready,
}
