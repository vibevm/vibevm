// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Wire format for `GET /v1/purls/{purl}`: usable versions describing the
/// PURL and refused versions that would also have matched. Read both by
/// `crates/vibe-registry/src/index_client/wire.rs` (`PurlLookupResults`,
/// `PurlLookupHit`, `BindingSite`) and raw `curl | jq` consumers. Source
/// of truth for the writer: `crates/vibe-index/src/server/routes/purls.rs`
/// (`Response`, `Hit`, `lookup`); the generated reader lives in `crates/vibe-
/// wire/src/generated/index_http/e1/purls_response/`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct PurlsResponse {
    /// Always `purls` for this response.
    pub command: String,

    /// How many usable hit rows the response carries; consumed by the client
    /// view. Bounded by construction and checked at the wire boundary.
    pub hit_count: u32,

    /// Every usable version describing the PURL, in lookup order. Required even
    /// when empty and consumed by the client view.
    pub hits: Vec<PurlHit>,

    /// The requested package URL as supplied by the caller; consumed by the
    /// vibe-registry client view.
    pub purl: String,

    /// Refused versions that would have matched the PURL, absent when none
    /// exist.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub unavailable: Vec<Unavailable>,
}

/// Where the PURL binding originated: the version's package-level `describes`
/// field or a subskill's `describes` field. Closed because both values name
/// concrete places in the writer model.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub enum BindingSite {
    #[serde(rename = "package")]
    Package,

    #[serde(rename = "subskill")]
    Subskill,
}

pub use crate::generated::shared::Group;

pub use crate::generated::shared::PackageKind;

/// One usable package version describing the requested PURL. Every client-view
/// field is present; `group` is a server addition tolerated by that view.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct PurlHit {
    /// Whether the package or one subskill declared the binding; consumed by
    /// the client view.
    pub binding_site: BindingSite,

    /// The package's reverse-FQDN group qualifier.
    pub group: Group,

    /// The version's installable kind, consumed by the client view.
    pub kind: PackageKind,

    /// The package's bare name, consumed by the client view.
    pub name: String,

    /// The semantic version describing the PURL, consumed by the client view.
    pub version: Version,
}

/// One package version this build refuses and the recovery recipe, mirroring
/// `crates/vibe-index/src/index/quarantine.rs::Unavailable`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct Unavailable {
    /// The refused version's group qualifier.
    pub group: Group,

    /// Reader capabilities unknown to this build, in record order.
    pub missing: Vec<String>,

    /// The refused version's bare package name.
    pub name: String,

    /// The generated explanation and fix surface for the refusal.
    pub recipe: String,

    /// The refused semantic version.
    pub version: Version,
}

pub use crate::generated::shared::Version;
