// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Wire format for search-mode `GET /v1/packages?q=...`: scored package
/// hits assembled by the live server. Read both by `crates/vibe-
/// registry/src/index_client/wire.rs` (`SearchResults`, `SearchHit`) and
/// raw `curl | jq` consumers. Source of truth for the writer: `crates/vibe-
/// index/src/server/routes/packages.rs` (`SearchResponse`, `SearchHit`,
/// `list_or_search`); the generated reader lives in `crates/vibe-
/// wire/src/generated/index_http/e1/package_search_response/`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct PackageSearchResponse {
    /// Always `search` for this response.
    pub command: String,

    /// How many hits survived offset and limit. Bounded by construction and
    /// checked at the wire boundary.
    pub hit_count: u32,

    /// The selected scored hit list. Required: a matchless query writes an
    /// empty array, never an absent key.
    pub hits: Vec<SearchHit>,

    /// The search query as supplied by the caller. This field is consumed by
    /// the vibe-registry client view.
    pub query: String,
}

pub use crate::generated::shared::Group;

pub use crate::generated::shared::PackageKind;

/// One scored package hit. Every field consumed by
/// `index_client/wire.rs::SearchHit` is present; `group` and `unavailable` are
/// server additions tolerated by that view.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SearchHit {
    /// The scored version's description, or null; consumed by the client view
    /// and always present.
    #[serde(deserialize_with = "crate::behaviour::required_nullable::deserialize")]
    pub description: Option<String>,

    /// The hit package's reverse-FQDN group qualifier.
    pub group: Group,

    /// The scored version's installable kind.
    pub kind: PackageKind,

    /// The version chosen for the hit, or null when absent. The key is always
    /// present and is consumed by the client view.
    #[serde(deserialize_with = "crate::behaviour::required_nullable::deserialize")]
    pub latest_stable: Option<Version>,

    /// Query tokens that matched this package, consumed by the client view.
    /// Required even when empty.
    pub matched_tokens: Vec<String>,

    /// The hit package's bare name.
    pub name: String,

    /// The number of distinct query tokens matched by the hit. Bounded by
    /// construction and checked at the wire boundary.
    pub score: u32,

    /// Refused versions of the hit package, absent when none exist.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub unavailable: Vec<Unavailable>,
}

/// One package version this build refuses and the recovery recipe, mirroring
/// `crates/vibe-index/src/index/quarantine.rs::Unavailable`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct Unavailable {
    /// The refused version's group qualifier.
    pub group: Group,

    /// Reader capabilities unknown to this build, in record order.
    pub missing: Vec<String>,

    /// The refused version's bare package name.
    pub name: String,

    /// The generated explanation and fix surface for the refusal.
    pub recipe: String,

    /// The refused semantic version.
    pub version: Version,
}

pub use crate::generated::shared::Version;
