// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Wire format for `GET /v1/capabilities/{capability}`: usable
/// versions advertising the capability and refused versions
/// that would also have matched. Source of truth for the writer:
/// `crates/vibe-index/src/server/routes/capabilities.rs` (`Response`,
/// `Hit`, `lookup`); the generated reader lives in `crates/vibe-
/// wire/src/generated/index_http/e1/capabilities_response/`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct CapabilitiesResponse {
    /// The requested capability as supplied by the caller.
    pub capability: String,

    /// Always `capabilities` for this response.
    pub command: String,

    /// How many usable hit rows the response carries. The writer uses usize;
    /// uint32 is JTD's widest exact unsigned integer.
    pub hit_count: u32,

    /// Every usable version advertising the capability, in lookup order.
    /// Required even when empty.
    pub hits: Vec<CapabilityHit>,

    /// Refused versions that would have matched the capability, absent when
    /// none exist.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub unavailable: Vec<Unavailable>,
}

/// One usable package version advertising the requested capability.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct CapabilityHit {
    /// The entry's own capability spelling selected by prefix overlap, or null
    /// when no concrete spelling was retained. The key is always present.
    #[serde(deserialize_with = "crate::behaviour::required_nullable::deserialize")]
    pub capability_advertised: Option<String>,

    /// The package's reverse-FQDN group qualifier.
    pub group: Group,

    /// The version's installable kind.
    pub kind: PackageKind,

    /// The package's bare name.
    pub name: String,

    /// The semantic version advertising the capability.
    pub version: Version,
}

pub use crate::generated::shared::Group;

pub use crate::generated::shared::PackageKind;

/// One package version this build refuses and the recovery recipe, mirroring
/// `crates/vibe-index/src/index/quarantine.rs::Unavailable`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct Unavailable {
    /// The refused version's group qualifier.
    pub group: Group,

    /// Reader capabilities unknown to this build, in record order.
    pub missing: Vec<String>,

    /// The refused version's bare package name.
    pub name: String,

    /// The generated explanation and fix surface for the refusal.
    pub recipe: String,

    /// The refused semantic version.
    pub version: Version,
}

pub use crate::generated::shared::Version;
