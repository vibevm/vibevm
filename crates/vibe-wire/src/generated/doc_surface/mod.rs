// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// The surface snapshot of the product on one declared version (PROP-057
/// `##OBS-SURFACE-SNAPSHOTS`, `##OBS-VERSION-CONTRACT`). It is a STRUCTURAL
/// description and never a fingerprint: the command tree with its flags, the
/// keys the manifest and the lock file accept, the members of every published
/// schema, the text of every specification fact marked `actionstage="doc"`,
/// and the format registry. There are no hashes, no build dates and no state
/// identifiers anywhere in it — the only identity it carries is the version
/// number the owner declared, because that number is the only event by which
/// this project computes a difference between versions. Written by `vibe doc
/// surface --record <version>` into `maintenance/surface/<version>.json` of a
/// documentation package, read by `vibe doc diff <old> <new>`, and rendered by
/// nothing: the directory is the documentation developers' kitchen and the site
/// does not serve it (`##OBS-NOTHING-LEAKS`). Source of truth for `crates/vibe-
/// wire/src/generated/doc_surface/`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct DocSurface {
    /// Every command of the binary, in the order a walk of `--help` reaches
    /// them, each with the flags it accepts. This is the surface a reader
    /// types, so it is taken from the help the reader would read and never from
    /// the parser's source.
    pub commands: Vec<SurfaceCommand>,

    /// The text of every specification fact marked `actionstage="doc"` with an
    /// audience — the promises the documentation answers to. The TEXT is here
    /// and not a hash of it: a diff that could only say «this changed» would
    /// send a writer to read the whole corpus again.
    pub facts: Vec<SurfaceFact>,

    /// The format registry as it stood, record by record.
    pub formats: Vec<SurfaceFormat>,

    /// The same for `vibe.lock`.
    pub lock_fields: Vec<String>,

    /// Every dotted key path `vibe.toml` accepts, sorted — `package`,
    /// `package.title`, `package.binary[].name`. The list is what the real
    /// parser answers when asked, so it states what the product accepts rather
    /// than what a struct is called.
    pub manifest_fields: Vec<String>,

    pub schema_version: u32,

    /// Every JTD schema the format registry names, with its members. A document
    /// a foreign parser reads is a contract, and its members are the part of
    /// the contract a page quotes.
    pub schemas: Vec<SurfaceSchema>,

    /// The version number the caller declared, verbatim. It is an INPUT and
    /// never a reading: the product cannot tell one amend of a version from
    /// another, and a snapshot that guessed its own number would be pretending
    /// to a history the project does not keep (`##OBS-VERSION-CONTRACT`).
    pub version: String,
}

/// One command of the binary and the flags it accepts.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SurfaceCommand {
    pub flags: Vec<SurfaceFlag>,

    /// The command line a reader types as far as this node — `vibe doc build`.
    pub path: String,

    /// The one-line description the parent's help gives it, empty at the root.
    pub summary: String,
}

/// One documentation obligation of the specifications.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SurfaceFact {
    /// `<observed path>#<anchor>` — the address a page cites it by.
    pub address: String,

    pub audiences: Vec<String>,

    pub text: String,
}

/// One option of one command, as its own help spells it.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SurfaceFlag {
    /// The long form when there is one, else the short — `--format`, `-h`.
    pub name: String,

    pub summary: String,

    /// The placeholder the flag takes, empty when it is a switch — `FORMAT`,
    /// `PATH`.
    pub value: String,
}

/// One record of `formats/REGISTRY.toml`, verbatim.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SurfaceFormat {
    pub corpus: String,

    pub epoch: u32,

    pub foreign_parsers: String,

    pub id: String,

    pub recoverable: bool,

    pub schema: String,

    pub sunset: String,
}

/// One published document format and the members its schema states.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SurfaceSchema {
    pub fields: Vec<SurfaceSchemaField>,

    /// The format id in the registry — the address that survives the file
    /// moving.
    pub id: String,

    pub path: String,
}

/// One member of one form of one schema.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SurfaceSchemaField {
    /// The definition the member belongs to, empty for the document's own root.
    pub form: String,

    pub name: String,

    /// Whether the member sits in `properties` rather than
    /// `optionalProperties`. Moving a member between the two is a contract
    /// change and this is what makes it visible.
    pub required: bool,

    /// The member's form in one phrase — `string`, `uint32`, `elements of
    /// doc_page`, `enum user|author`.
    pub shape: String,
}
