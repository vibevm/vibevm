// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// The five-platform release aggregate `DISTRIBUTIONS.json`, and with it the
/// whole distribution manifest family: the per-bundle `DISTRIBUTION.json`
/// and the per-build platform fragment are the same document's nested shapes,
/// published and read standalone as well as inside this root. Every object
/// preserves the pre-JTD member order, so the published bytes do not move.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct AggregateDistributionManifest {
    pub schema_version: u32,

    pub product: String,

    pub repository: String,

    pub version: String,

    pub tag: String,

    pub source_commit: String,

    pub platforms: Vec<PlatformDistributionFragment>,
}

/// The `DISTRIBUTION.json` every platform bundle carries, and the exact
/// manifest a platform fragment embeds.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct BundleDistributionManifest {
    pub schema_version: u32,

    pub product: String,

    pub repository: String,

    /// SemVer without the Git tag's `v` prefix.
    pub version: String,

    /// Exactly `v<version>`.
    pub tag: String,

    pub source_commit: String,

    pub target: String,

    pub components: Vec<DistributionComponent>,

    pub source_archive: DistributionSourceArchive,
}

/// An exact non-negative byte count. JTD has no uint64 form, so generated
/// Rust narrows the numeric form to u64 and refuses fractions, negatives and
/// overflow.
pub type ByteCount = u64;

/// One published release asset — the complete platform bundle, or the raw
/// bootstrap executable beside it.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct DistributionAsset {
    pub name: String,

    pub size: ByteCount,

    /// Lowercase `sha256:<64 hex digits>` digest of the complete bundle.
    pub digest: String,
}

/// One runtime executable carried inside a platform bundle.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct DistributionComponent {
    pub name: DistributionComponentName,

    /// Bundle-relative executable path (`vibe.exe` on Windows, `vibe`
    /// elsewhere).
    pub path: String,

    pub size: ByteCount,

    /// Lowercase `sha256:<64 hex digits>` digest of the component bytes.
    pub digest: String,
}

/// Which runtime executable a bundle component is. Closed vocabulary: the
/// installer resolves each component to a fixed destination path, so an
/// unfamiliar name has no safe behaviour.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub enum DistributionComponentName {
    #[serde(rename = "vibe")]
    Vibe,

    #[serde(rename = "vibe-index")]
    VibeIndex,
}

/// Non-executable source snapshot shipped alongside the two runtime binaries.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct DistributionSourceArchive {
    pub path: String,

    pub size: ByteCount,

    /// Lowercase `sha256:<64 hex digits>` digest of the archive bytes.
    pub digest: String,

    /// Lowercase full Git tree object ID represented by the archive.
    pub tree_oid: String,
}

/// What one platform build emits for the release aggregation job, and one
/// element of the aggregate's `platforms`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct PlatformDistributionFragment {
    pub schema_version: u32,

    pub product: String,

    pub repository: String,

    pub version: String,

    pub tag: String,

    pub source_commit: String,

    pub target: String,

    pub asset: DistributionAsset,

    /// Raw `vibe[.exe]` used by dependency-light hosted bootstraps before they
    /// have a ZIP reader or an installed VVM.
    pub bootstrap: DistributionAsset,

    /// The exact manifest embedded in this platform's bundle.
    pub bundle: BundleDistributionManifest,
}
