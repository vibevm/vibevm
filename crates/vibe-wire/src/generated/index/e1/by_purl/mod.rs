// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// `by-purl/<slug>.jsonl` — one line of the PURL inverted index, epoch 1 (PROP-
/// 005 §2.13): a package version whose package- or subskill-level `describes`
/// matches one given PURL. Root type `PurlRow`; every field required. Source of
/// truth for `crates/vibe-wire/src/generated/index/e1/by_purl/`.
#[derive(Serialize, Deserialize)]
pub struct ByPurl {
    #[serde(rename = "binding_site")]
    pub bindingSite: BindingSite,

    #[serde(rename = "group")]
    pub group: Group,

    #[serde(rename = "kind")]
    pub kind: PackageKind,

    #[serde(rename = "name")]
    pub name: String,

    /// The package URL this row indexes — the canonical spelling as it lives
    /// inside the entry, not the filesystem-safe slug that names the file.
    #[serde(rename = "purl")]
    pub purl: String,

    #[serde(rename = "version")]
    pub version: Version,
}

/// Where the PURL was declared on the entry — `package` for the top-level
/// `describes`, `subskill` for a subskill's. Closed vocabulary: paths are built
/// from these values, so an unfamiliar one has no safe behaviour. The wire
/// strings are the WRITER's form (`kebab-case` in `vibe-index`); the reader-
/// side copy in `vibe-registry` spells them `lowercase`, which coincides only
/// because both variants are single words — Phase 4.2 converges the two by
/// re-export.
#[derive(Serialize, Deserialize)]
pub enum BindingSite {
    #[serde(rename = "package")]
    Package,

    #[serde(rename = "subskill")]
    Subskill,
}

/// Reverse-FQDN namespace qualifier (PROP-008 §2.1) — the `vibe_core::Group`
/// newtype on the wire.
pub type Group = String;

/// Installable package kind (VIBEVM-SPEC §4.1). Open vocabulary: the register
/// grows by owner amendment, so a reader must not hard-fail on an unseen kind.
#[derive(Serialize, Deserialize)]
pub enum PackageKind {
    #[serde(rename = "feat")]
    Feat,

    #[serde(rename = "flow")]
    Flow,

    #[serde(rename = "lang")]
    Lang,

    #[serde(rename = "mcp")]
    Mcp,

    #[serde(rename = "stack")]
    Stack,

    #[serde(rename = "tool")]
    Tool,
}

/// Semantic version string — `semver::Version` in code.
pub type Version = String;
