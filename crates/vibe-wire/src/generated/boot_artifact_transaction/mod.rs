// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub enum BootArtifactTransactionMode {
    #[serde(rename = "commit")]
    Commit,

    #[serde(rename = "rollback")]
    Rollback,
}

/// Crash-recovery intent for publishing INDEX.md and its selected STATIC
/// carrier. The record is written durably before the first replacement.
/// A normal write retires it after redirects and stale cleanup; core-only
/// recovery may retire it after selected STATIC and INDEX are durable while
/// deliberately retaining the stale carrier until the next locked full write
/// atomically refreshes every redirect and converges to exactly one carrier.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct BootArtifactTransaction {
    pub index: Entry,

    pub mode: BootArtifactTransactionMode,

    pub schema: u32,

    pub selected: Entry,

    pub stale: Entry,

    pub transaction: String,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct Entry {
    pub after: State,

    pub before: State,

    pub target: String,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct State {
    pub present: bool,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub digest: Option<String>,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub staged: Option<String>,
}
