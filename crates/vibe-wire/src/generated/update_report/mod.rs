// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// `all` is the whole-workspace update; `scoped` names one or more packages.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub enum UpdateReportScope {
    #[serde(rename = "all")]
    All,

    #[serde(rename = "scoped")]
    Scoped,
}

/// Wire format for `vibe update --json` — the ONE document the command emits,
/// whatever it did. Update runs on the install substrate but is its OWN
/// command: it reports `update`, its own scope, targets and run identity, and
/// a handoff whose resume line is `vibe update`. It never impersonates install.
/// Every fact the pre-R7.3 document carried stays at the ROOT.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct UpdateReport {
    /// Always `"update"` for this report.
    pub command: String,

    /// True when this invocation ran to completion. False when it stopped part-
    /// way — the slot sets above are then a partial, boundary-measured record,
    /// never a final one.
    pub complete: bool,

    /// Dependency-slot paths materialised this run — a directory each, never
    /// counted as files. Excludes any slot a rollback removed.
    pub materialised: Vec<String>,

    /// Workspace-relative path of every node whose boot artifacts were
    /// regenerated.
    pub nodes_regenerated: Vec<String>,

    pub ok: bool,

    /// The package coordinates this invocation targeted. Empty for `all`.
    pub packages: Vec<String>,

    /// How many packages this update re-resolved.
    pub packages_resolved: u32,

    pub project: String,

    /// Slot paths removed — present before, absent from this resolution.
    pub pruned: Vec<String>,

    /// `all` is the whole-workspace update; `scoped` names one or more
    /// packages.
    pub scope: UpdateReportScope,

    /// Slot paths already present for the resolved version and source identity,
    /// trusted without materialising.
    pub skipped: Vec<String>,

    /// True on the PROP-011 §2.2 fast path: `vibe.lock` was already fresh, so
    /// no resolution ran and no slot moved.
    pub unchanged: bool,

    /// One line per package whose locked version moved.
    pub version_bumps: Vec<String>,

    /// Slot-scoped lifecycle rows this command itself ran. This command is the
    /// outermost one on its path and therefore the only one that reports them.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub contributions: Vec<UpdateContributionReport>,

    /// Present exactly when a hosted `agent` contribution parked this update.
    /// `ok` stays true; `resume` is the update command, never `vibe install`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub delegation: Option<UpdateDelegation>,

    /// Install-hook reports for this run. Emitted even when empty: the pre-R7.3
    /// document carried this member unconditionally and consumers key on it.
    #[serde(default)]
    pub hooks: Vec<UpdateHookReport>,

    /// The shared compile-trace member (PROP-054 `##OBS-TRACE`, R3.4): present
    /// exactly when this command ran with compile tracing enabled; disabled old
    /// JSON omits it and remains readable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trace: Option<CompileTraceReport>,
}

pub use crate::generated::shared::CompileTraceReport;

pub use crate::generated::shared::Duration;

pub use crate::generated::shared::TimingRow;

pub use crate::generated::shared::TraceReportStatus;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct UpdateContributionReport {
    pub handler: String,

    pub key: String,

    pub phase: String,

    pub point: String,

    pub provider: String,

    pub status: String,

    pub tier: String,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub flagged: Option<bool>,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reference: Option<String>,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stderr: Option<String>,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stdout: Option<String>,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct UpdateDelegation {
    pub resume: String,

    pub run_id: String,

    pub tasks: Vec<String>,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct UpdateHookReport {
    pub phase: String,

    pub status: String,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub note: Option<String>,
}
