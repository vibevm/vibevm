// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Epoch-1 compiler-native request. The root carries invocation identity plus
/// one canonical compiler-IR payload; only that payload is projected for a
/// foreign reader.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct CompileRequest {
    pub envelope: u32,

    pub execution: Execution,

    pub io: Io,

    /// The one canonical compiler-IR family, decoded through the generated
    /// duplicate-preserving permissive projection for foreign readers.
    #[serde(deserialize_with = "__reader_projection::deserialize_ir_0")]
    pub payload: Ir,

    /// Open compiler extension-point spelling. Structural decoding preserves
    /// unknown values; behavior admission decides whether a point is supported.
    pub point: String,

    pub project: Project,

    pub world: World,
}

pub use crate::generated::shared::AbsorptionOccurrence;

pub use crate::generated::shared::AbsorptionPlan;

pub use crate::generated::shared::AbsorptionState;

pub use crate::generated::shared::AbsorptionStateApplied;

pub use crate::generated::shared::AbsorptionStatePlanned;

pub use crate::generated::shared::AbsorptionStateUnplanned;

pub use crate::generated::shared::ArtifactContext;

pub use crate::generated::shared::ArtifactFrame;

pub use crate::generated::shared::ArtifactFrameCompatibilityFragment;

pub use crate::generated::shared::ArtifactFrameStaticLane;

pub use crate::generated::shared::ArtifactTarget;

pub use crate::generated::shared::Authority;

pub use crate::generated::shared::AuthorityHost;

pub use crate::generated::shared::AuthorityPackage;

pub use crate::generated::shared::CardinalityArtifact;

pub use crate::generated::shared::CardinalityDocument;

pub use crate::generated::shared::ClosureContribution;

pub use crate::generated::shared::ClosureContributionElided;

pub use crate::generated::shared::ClosureContributionHoisted;

pub use crate::generated::shared::ClosureContributionNormal;

pub use crate::generated::shared::ClosureContributionSimple;

pub use crate::generated::shared::ClosureDocument;

pub use crate::generated::shared::ClosureEdgeKind;

pub use crate::generated::shared::ClosureEdge;

pub use crate::generated::shared::ClosureIr;

pub use crate::generated::shared::ClosureOccurrence;

pub use crate::generated::shared::CompileMode;

pub use crate::generated::shared::ContributionAbsorption;

pub use crate::generated::shared::ContributionAbsorptionElided;

pub use crate::generated::shared::ContributionAbsorptionHoisted;

pub use crate::generated::shared::ContributionAbsorptionNormal;

pub use crate::generated::shared::ContributionAbsorptionSimple;

pub use crate::generated::shared::ContributionMeta;

pub use crate::generated::shared::DirectiveKind;

pub use crate::generated::shared::Directive;

pub use crate::generated::shared::DirectiveError;

pub use crate::generated::shared::Directives;

pub use crate::generated::shared::DocNodeKind;

pub use crate::generated::shared::DocNode;

pub use crate::generated::shared::DocTree;

pub use crate::generated::shared::DocumentAddress;

pub use crate::generated::shared::DocumentAddressSpec;

pub use crate::generated::shared::DocumentAddressStaticEntry;

pub use crate::generated::shared::DocumentIr;

pub use crate::generated::shared::DocumentObservation;

pub use crate::generated::shared::DocumentObservationFailed;

pub use crate::generated::shared::DocumentObservationResolved;

pub use crate::generated::shared::DocumentProvider;

pub use crate::generated::shared::DocumentProviderDependency;

pub use crate::generated::shared::DocumentProviderHostCoordinate;

pub use crate::generated::shared::DocumentProviderHostUngrouped;

pub use crate::generated::shared::DocumentProviderHostVirtualWorkspace;

pub use crate::generated::shared::DocumentProviderUnclaimed;

pub use crate::generated::shared::DocumentProviderUndetermined;

pub use crate::generated::shared::DocumentSubject;

pub use crate::generated::shared::EmbedResolutionSnapshot;

pub use crate::generated::shared::EmissionContributionWitness;

pub use crate::generated::shared::EmissionContributionWitnessElided;

pub use crate::generated::shared::EmissionContributionWitnessHoisted;

pub use crate::generated::shared::EmissionContributionWitnessNormal;

pub use crate::generated::shared::EmissionContributionWitnessSimple;

pub use crate::generated::shared::EmissionProvenance;

pub use crate::generated::shared::EmittedArtifact;

pub use crate::generated::shared::Execution;

pub use crate::generated::shared::ExpansionObservation;

pub use crate::generated::shared::ExpansionObservationFailed;

pub use crate::generated::shared::ExpansionObservationResolved;

pub use crate::generated::shared::FenceSnapshot;

pub use crate::generated::shared::FenceSnapshotClosed;

pub use crate::generated::shared::FenceSnapshotOpen;

pub use crate::generated::shared::InPlaceUse;

pub use crate::generated::shared::Io;

pub use crate::generated::shared::Ir;

pub use crate::generated::shared::IrClosureArtifact;

pub use crate::generated::shared::IrDocumentDocument;

pub use crate::generated::shared::IrDocumentsArtifact;

pub use crate::generated::shared::IrEmittedArtifact;

pub use crate::generated::shared::IrLaneArtifact;

pub use crate::generated::shared::IrSourceDocument;

pub use crate::generated::shared::LaneChunk;

pub use crate::generated::shared::LaneChunkForcedNewline;

pub use crate::generated::shared::LaneChunkNode;

pub use crate::generated::shared::LaneChunkNormalClose;

pub use crate::generated::shared::LaneChunkNormalOpen;

pub use crate::generated::shared::LaneContribution;

pub use crate::generated::shared::LaneContributionElided;

pub use crate::generated::shared::LaneContributionHoisted;

pub use crate::generated::shared::LaneContributionNormal;

pub use crate::generated::shared::LaneContributionSimple;

pub use crate::generated::shared::LaneFrame;

pub use crate::generated::shared::LaneIr;

pub use crate::generated::shared::LaneNode;

pub use crate::generated::shared::LaneNodeNormal;

pub use crate::generated::shared::LaneNodeSimple;

pub use crate::generated::shared::LevelClosure;

pub use crate::generated::shared::LevelDocument;

pub use crate::generated::shared::LevelEmitted;

pub use crate::generated::shared::LevelLane;

pub use crate::generated::shared::LevelSource;

pub use crate::generated::shared::LinkContributionWitness;

pub use crate::generated::shared::LinkContributionWitnessElided;

pub use crate::generated::shared::LinkContributionWitnessHoisted;

pub use crate::generated::shared::LinkContributionWitnessNormal;

pub use crate::generated::shared::LinkContributionWitnessSimple;

pub use crate::generated::shared::LinkOccurrence;

pub use crate::generated::shared::LinkOccurrenceNormal;

pub use crate::generated::shared::LinkOccurrenceSimple;

pub use crate::generated::shared::LinkResult;

pub use crate::generated::shared::LinkState;

pub use crate::generated::shared::LinkStateLinked;

pub use crate::generated::shared::LinkStateUnlinked;

pub use crate::generated::shared::OriginRename;

pub use crate::generated::shared::Project;

pub use crate::generated::shared::QualificationState;

pub use crate::generated::shared::QualificationStateApplied;

pub use crate::generated::shared::QualificationStatePending;

pub use crate::generated::shared::RenameEntry;

pub use crate::generated::shared::SourceDoc;

pub use crate::generated::shared::SourceResolutionSnapshot;

pub use crate::generated::shared::Span;

pub use crate::generated::shared::SpecAddress;

pub use crate::generated::shared::World;

pub use crate::generated::shared::WorldPackage;

#[doc(hidden)]
#[allow(clippy::collapsible_if, unused_variables)]
mod __reader_projection {
    #[derive(Clone, Copy)]
    struct NoDuplicateValue;

    impl<'de> serde::de::DeserializeSeed<'de> for NoDuplicateValue {
        type Value = serde_json::Value;

        fn deserialize<D>(self, deserializer: D) -> Result<Self::Value, D::Error>
        where
            D: serde::Deserializer<'de>,
        {
            deserializer.deserialize_any(NoDuplicateValueVisitor)
        }
    }

    struct NoDuplicateValueVisitor;

    impl<'de> serde::de::Visitor<'de> for NoDuplicateValueVisitor {
        type Value = serde_json::Value;

        fn expecting(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
            formatter.write_str("a JSON value without duplicate object members")
        }

        fn visit_bool<E>(self, value: bool) -> Result<Self::Value, E> {
            Ok(serde_json::Value::Bool(value))
        }

        fn visit_i64<E>(self, value: i64) -> Result<Self::Value, E> {
            Ok(serde_json::Value::Number(value.into()))
        }

        fn visit_u64<E>(self, value: u64) -> Result<Self::Value, E> {
            Ok(serde_json::Value::Number(value.into()))
        }

        fn visit_f64<E>(self, value: f64) -> Result<Self::Value, E>
        where
            E: serde::de::Error,
        {
            serde_json::Number::from_f64(value)
                .map(serde_json::Value::Number)
                .ok_or_else(|| E::custom("non-finite JSON number"))
        }

        fn visit_str<E>(self, value: &str) -> Result<Self::Value, E> {
            Ok(serde_json::Value::String(value.to_string()))
        }

        fn visit_string<E>(self, value: String) -> Result<Self::Value, E> {
            Ok(serde_json::Value::String(value))
        }

        fn visit_none<E>(self) -> Result<Self::Value, E> {
            Ok(serde_json::Value::Null)
        }

        fn visit_unit<E>(self) -> Result<Self::Value, E> {
            Ok(serde_json::Value::Null)
        }

        fn visit_some<D>(self, deserializer: D) -> Result<Self::Value, D::Error>
        where
            D: serde::Deserializer<'de>,
        {
            serde::de::DeserializeSeed::deserialize(NoDuplicateValue, deserializer)
        }

        fn visit_newtype_struct<D>(self, deserializer: D) -> Result<Self::Value, D::Error>
        where
            D: serde::Deserializer<'de>,
        {
            serde::de::DeserializeSeed::deserialize(NoDuplicateValue, deserializer)
        }

        fn visit_seq<A>(self, mut sequence: A) -> Result<Self::Value, A::Error>
        where
            A: serde::de::SeqAccess<'de>,
        {
            let mut items = Vec::new();
            while let Some(item) = sequence.next_element_seed(NoDuplicateValue)? {
                items.push(item);
            }
            Ok(serde_json::Value::Array(items))
        }

        fn visit_map<A>(self, mut map: A) -> Result<Self::Value, A::Error>
        where
            A: serde::de::MapAccess<'de>,
        {
            let mut object = serde_json::Map::new();
            while let Some(key) = map.next_key::<String>()? {
                if object.contains_key(&key) {
                    return Err(<A::Error as serde::de::Error>::custom(
                        "duplicate object member",
                    ));
                }
                let value = map.next_value_seed(NoDuplicateValue)?;
                object.insert(key, value);
            }
            Ok(serde_json::Value::Object(object))
        }
    }

    pub(super) fn deserialize_ir_0<'de, D>(
        deserializer: D,
    ) -> Result<crate::generated::shared::Ir, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let mut value = serde::de::DeserializeSeed::deserialize(NoDuplicateValue, deserializer)?;
        prune_ir(&mut value);
        serde_json::from_value(value).map_err(<D::Error as serde::de::Error>::custom)
    }

    fn prune_absorption_occurrence(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "absorbed" | "node" | "requested_address"));
        if let Some(child) = object.get_mut("requested_address") {
            prune_spec_address(child);
        }
    }

    fn prune_absorption_plan(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "contributions" | "mode"));
        if let Some(child) = object.get_mut("contributions") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_contribution_absorption(item);
                }
            }
        }
        if let Some(child) = object.get_mut("mode") {
            prune_compile_mode(child);
        }
    }

    fn prune_absorption_state(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("state")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "unplanned" => {
                object.retain(|key, _| matches!(key.as_str(), "state"));
            }
            "planned" => {
                object.retain(|key, _| matches!(key.as_str(), "plan" | "state"));
                if let Some(child) = object.get_mut("plan") {
                    prune_absorption_plan(child);
                }
            }
            "applied" => {
                object.retain(|key, _| matches!(key.as_str(), "plan" | "state"));
                if let Some(child) = object.get_mut("plan") {
                    prune_absorption_plan(child);
                }
            }
            _ => {}
        }
    }

    fn prune_artifact_context(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "artifact" | "frame" | "mode" | "target"));
        if let Some(child) = object.get_mut("frame") {
            prune_artifact_frame(child);
        }
        if let Some(child) = object.get_mut("mode") {
            prune_compile_mode(child);
        }
        if let Some(child) = object.get_mut("target") {
            prune_artifact_target(child);
        }
    }

    fn prune_artifact_frame(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "static-lane" => {
                object.retain(|key, _| {
                    matches!(key.as_str(), "generated_path" | "kind" | "source_root")
                });
            }
            "compatibility-fragment" => {
                object.retain(|key, _| matches!(key.as_str(), "kind"));
            }
            _ => {}
        }
    }

    fn prune_artifact_target(_value: &mut serde_json::Value) {}

    fn prune_authority(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "host" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "name"));
            }
            "package" => {
                object
                    .retain(|key, _| matches!(key.as_str(), "group" | "kind" | "name" | "version"));
            }
            _ => {}
        }
    }

    fn prune_cardinality_artifact(_value: &mut serde_json::Value) {}

    fn prune_cardinality_document(_value: &mut serde_json::Value) {}

    fn prune_closure_contribution(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "normal" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "emission_order" | "kind" | "meta" | "seed" | "seed_address"
                    )
                });
                if let Some(child) = object.get_mut("emission_order") {
                    if let Some(items) = child.as_array_mut() {
                        for item in items {
                            prune_closure_occurrence(item);
                        }
                    }
                }
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("seed_address") {
                    prune_spec_address(child);
                }
            }
            "simple" => {
                object.retain(|key, _| matches!(key.as_str(), "document" | "kind" | "meta"));
                if let Some(child) = object.get_mut("document") {
                    prune_closure_document(child);
                }
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "elided" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "hoisted" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta" | "target"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("target") {
                    prune_spec_address(child);
                }
            }
            _ => {}
        }
    }

    fn prune_closure_document(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "address" | "aliases" | "origin" | "tree"));
        if let Some(child) = object.get_mut("address") {
            prune_document_address(child);
        }
        if let Some(child) = object.get_mut("aliases") {
            if let Some(items) = child.as_object_mut() {
                for item in items.values_mut() {
                    prune_spec_address(item);
                }
            }
        }
        if let Some(child) = object.get_mut("tree") {
            prune_doc_tree(child);
        }
    }

    fn prune_closure_edge(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "from" | "kind" | "requested_target" | "to"));
        if let Some(child) = object.get_mut("requested_target") {
            prune_spec_address(child);
        }
    }

    fn prune_closure_ir(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "absorption"
                    | "context"
                    | "contributions"
                    | "edges"
                    | "link"
                    | "nodes"
                    | "pending_embeds"
                    | "pending_sources"
                    | "qualification"
                    | "renames"
            )
        });
        if let Some(child) = object.get_mut("absorption") {
            prune_absorption_state(child);
        }
        if let Some(child) = object.get_mut("context") {
            prune_artifact_context(child);
        }
        if let Some(child) = object.get_mut("contributions") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_closure_contribution(item);
                }
            }
        }
        if let Some(child) = object.get_mut("edges") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_closure_edge(item);
                }
            }
        }
        if let Some(child) = object.get_mut("link") {
            prune_link_state(child);
        }
        if let Some(child) = object.get_mut("nodes") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_closure_document(item);
                }
            }
        }
        if let Some(child) = object.get_mut("pending_embeds") {
            prune_embed_resolution_snapshot(child);
        }
        if let Some(child) = object.get_mut("pending_sources") {
            prune_source_resolution_snapshot(child);
        }
        if let Some(child) = object.get_mut("qualification") {
            prune_qualification_state(child);
        }
        if let Some(child) = object.get_mut("renames") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_origin_rename(item);
                }
            }
        }
    }

    fn prune_closure_occurrence(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "node" | "requested_address"));
        if let Some(child) = object.get_mut("requested_address") {
            prune_spec_address(child);
        }
    }

    fn prune_compile_mode(_value: &mut serde_json::Value) {}

    fn prune_contribution_absorption(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "normal" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "kind" | "meta" | "occurrences" | "seed" | "seed_address"
                    )
                });
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("occurrences") {
                    if let Some(items) = child.as_array_mut() {
                        for item in items {
                            prune_absorption_occurrence(item);
                        }
                    }
                }
                if let Some(child) = object.get_mut("seed_address") {
                    prune_spec_address(child);
                }
            }
            "simple" => {
                object.retain(|key, _| matches!(key.as_str(), "address" | "kind" | "meta"));
                if let Some(child) = object.get_mut("address") {
                    prune_document_address(child);
                }
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "elided" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "hoisted" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta" | "target"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("target") {
                    prune_spec_address(child);
                }
            }
            _ => {}
        }
    }

    fn prune_contribution_meta(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "origin" | "path"));
    }

    fn prune_directive(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "address" | "kind" | "line" | "options"));
        if let Some(child) = object.get_mut("address") {
            prune_spec_address(child);
        }
    }

    fn prune_directive_error(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "line" | "message"));
    }

    fn prune_directives(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "aliases" | "directives" | "errors" | "in_place_uses"
            )
        });
        if let Some(child) = object.get_mut("aliases") {
            if let Some(items) = child.as_object_mut() {
                for item in items.values_mut() {
                    prune_spec_address(item);
                }
            }
        }
        if let Some(child) = object.get_mut("directives") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_directive(item);
                }
            }
        }
        if let Some(child) = object.get_mut("errors") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_directive_error(item);
                }
            }
        }
        if let Some(child) = object.get_mut("in_place_uses") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_in_place_use(item);
                }
            }
        }
    }

    fn prune_doc_node(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "children"
                    | "heading"
                    | "heading_line"
                    | "id"
                    | "kind"
                    | "level"
                    | "parent"
                    | "span"
                    | "trailing"
            )
        });
        if let Some(child) = object.get_mut("children") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
        if let Some(child) = object.get_mut("span") {
            prune_span(child);
        }
    }

    fn prune_doc_tree(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "anchors" | "directives" | "duplicate_anchors" | "lines" | "nodes"
            )
        });
        if let Some(child) = object.get_mut("anchors") {
            if let Some(items) = child.as_object_mut() {
                for item in items.values_mut() {}
            }
        }
        if let Some(child) = object.get_mut("directives") {
            prune_directives(child);
        }
        if let Some(child) = object.get_mut("duplicate_anchors") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
        if let Some(child) = object.get_mut("lines") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
        if let Some(child) = object.get_mut("nodes") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_doc_node(item);
                }
            }
        }
    }

    fn prune_document_address(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "spec" => {
                object.retain(|key, _| matches!(key.as_str(), "address" | "kind"));
                if let Some(child) = object.get_mut("address") {
                    prune_spec_address(child);
                }
            }
            "static-entry" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "origin" | "path"));
            }
            _ => {}
        }
    }

    fn prune_document_ir(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "source" | "tree"));
        if let Some(child) = object.get_mut("source") {
            prune_source_doc(child);
        }
        if let Some(child) = object.get_mut("tree") {
            prune_doc_tree(child);
        }
    }

    fn prune_document_observation(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("state")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "resolved" => {
                object.retain(|key, _| matches!(key.as_str(), "document" | "state"));
                if let Some(child) = object.get_mut("document") {
                    prune_document_ir(child);
                }
            }
            "failed" => {
                object.retain(|key, _| matches!(key.as_str(), "reason" | "requested" | "state"));
                if let Some(child) = object.get_mut("requested") {
                    prune_spec_address(child);
                }
            }
            _ => {}
        }
    }

    fn prune_document_provider(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "unclaimed" => {
                object.retain(|key, _| matches!(key.as_str(), "kind"));
            }
            "undetermined" => {
                object.retain(|key, _| matches!(key.as_str(), "kind"));
            }
            "dependency" => {
                object.retain(|key, _| matches!(key.as_str(), "group" | "kind" | "name"));
            }
            "host-ungrouped" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "name"));
            }
            "host-coordinate" => {
                object.retain(|key, _| matches!(key.as_str(), "group" | "kind" | "name"));
            }
            "host-virtual-workspace" => {
                object.retain(|key, _| matches!(key.as_str(), "kind"));
            }
            _ => {}
        }
    }

    fn prune_document_subject(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "declared_path" | "provider"));
        if let Some(child) = object.get_mut("provider") {
            prune_document_provider(child);
        }
    }

    fn prune_embed_resolution_snapshot(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "discovery_order" | "documents" | "explicit_use_keys"
            )
        });
        if let Some(child) = object.get_mut("discovery_order") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
        if let Some(child) = object.get_mut("documents") {
            if let Some(items) = child.as_object_mut() {
                for item in items.values_mut() {
                    prune_document_observation(item);
                }
            }
        }
        if let Some(child) = object.get_mut("explicit_use_keys") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
    }

    fn prune_emission_contribution_witness(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "normal" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "chunk_digest" | "kind" | "meta" | "seed" | "seed_address"
                    )
                });
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("seed_address") {
                    prune_spec_address(child);
                }
            }
            "simple" => {
                object.retain(|key, _| {
                    matches!(key.as_str(), "address" | "chunk_digest" | "kind" | "meta")
                });
                if let Some(child) = object.get_mut("address") {
                    prune_document_address(child);
                }
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "elided" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "hoisted" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta" | "target"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("target") {
                    prune_spec_address(child);
                }
            }
            _ => {}
        }
    }

    fn prune_emission_provenance(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "backend"
                    | "bytes_digest"
                    | "context"
                    | "contributions"
                    | "emitted_transforms"
                    | "producer"
                    | "renames"
                    | "source_lane_digest"
            )
        });
        if let Some(child) = object.get_mut("context") {
            prune_artifact_context(child);
        }
        if let Some(child) = object.get_mut("contributions") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_emission_contribution_witness(item);
                }
            }
        }
        if let Some(child) = object.get_mut("emitted_transforms") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
        if let Some(child) = object.get_mut("renames") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_origin_rename(item);
                }
            }
        }
    }

    fn prune_emitted_artifact(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "bytes_b64" | "provenance"));
        if let Some(child) = object.get_mut("provenance") {
            prune_emission_provenance(child);
        }
    }

    fn prune_expansion_observation(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("state")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "resolved" => {
                object.retain(|key, _| matches!(key.as_str(), "requested" | "state" | "targets"));
                if let Some(child) = object.get_mut("requested") {
                    prune_spec_address(child);
                }
                if let Some(child) = object.get_mut("targets") {
                    if let Some(items) = child.as_array_mut() {
                        for item in items {
                            prune_spec_address(item);
                        }
                    }
                }
            }
            "failed" => {
                object.retain(|key, _| matches!(key.as_str(), "reason" | "requested" | "state"));
                if let Some(child) = object.get_mut("requested") {
                    prune_spec_address(child);
                }
            }
            _ => {}
        }
    }

    fn prune_fence_snapshot(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("state")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "closed" => {
                object.retain(|key, _| matches!(key.as_str(), "state"));
            }
            "open" => {
                object.retain(|key, _| matches!(key.as_str(), "delimiter" | "run" | "state"));
            }
            _ => {}
        }
    }

    fn prune_in_place_use(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "address" | "line"));
        if let Some(child) = object.get_mut("address") {
            prune_spec_address(child);
        }
    }

    fn prune_ir(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("shape")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "source-document" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "cardinality" | "doc" | "ir_schema" | "level" | "shape"
                    )
                });
                if let Some(child) = object.get_mut("cardinality") {
                    prune_cardinality_document(child);
                }
                if let Some(child) = object.get_mut("doc") {
                    prune_source_doc(child);
                }
                if let Some(child) = object.get_mut("level") {
                    prune_level_source(child);
                }
            }
            "document-document" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "cardinality" | "doc" | "ir_schema" | "level" | "shape"
                    )
                });
                if let Some(child) = object.get_mut("cardinality") {
                    prune_cardinality_document(child);
                }
                if let Some(child) = object.get_mut("doc") {
                    prune_document_ir(child);
                }
                if let Some(child) = object.get_mut("level") {
                    prune_level_document(child);
                }
            }
            "documents-artifact" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "cardinality" | "documents" | "ir_schema" | "level" | "shape"
                    )
                });
                if let Some(child) = object.get_mut("cardinality") {
                    prune_cardinality_artifact(child);
                }
                if let Some(child) = object.get_mut("documents") {
                    if let Some(items) = child.as_array_mut() {
                        for item in items {
                            prune_document_ir(item);
                        }
                    }
                }
                if let Some(child) = object.get_mut("level") {
                    prune_level_document(child);
                }
            }
            "closure-artifact" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "cardinality" | "closure" | "ir_schema" | "level" | "shape"
                    )
                });
                if let Some(child) = object.get_mut("cardinality") {
                    prune_cardinality_artifact(child);
                }
                if let Some(child) = object.get_mut("closure") {
                    prune_closure_ir(child);
                }
                if let Some(child) = object.get_mut("level") {
                    prune_level_closure(child);
                }
            }
            "lane-artifact" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "cardinality" | "ir_schema" | "lane" | "level" | "shape"
                    )
                });
                if let Some(child) = object.get_mut("cardinality") {
                    prune_cardinality_artifact(child);
                }
                if let Some(child) = object.get_mut("lane") {
                    prune_lane_ir(child);
                }
                if let Some(child) = object.get_mut("level") {
                    prune_level_lane(child);
                }
            }
            "emitted-artifact" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "cardinality" | "emitted" | "ir_schema" | "level" | "shape"
                    )
                });
                if let Some(child) = object.get_mut("cardinality") {
                    prune_cardinality_artifact(child);
                }
                if let Some(child) = object.get_mut("emitted") {
                    prune_emitted_artifact(child);
                }
                if let Some(child) = object.get_mut("level") {
                    prune_level_emitted(child);
                }
            }
            _ => {}
        }
    }

    fn prune_lane_chunk(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "normal-open" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "contribution" | "kind" | "marker" | "occurrence"
                    )
                });
            }
            "node" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "node"));
                if let Some(child) = object.get_mut("node") {
                    prune_lane_node(child);
                }
            }
            "forced-newline" => {
                object.retain(|key, _| {
                    matches!(key.as_str(), "contribution" | "kind" | "occurrence")
                });
            }
            "normal-close" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "contribution" | "kind" | "marker" | "occurrence"
                    )
                });
            }
            _ => {}
        }
    }

    fn prune_lane_contribution(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "normal" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "chunks" | "kind" | "meta" | "seed" | "seed_address"
                    )
                });
                if let Some(child) = object.get_mut("chunks") {
                    if let Some(items) = child.as_array_mut() {
                        for item in items {
                            prune_lane_chunk(item);
                        }
                    }
                }
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("seed_address") {
                    prune_spec_address(child);
                }
            }
            "simple" => {
                object.retain(|key, _| {
                    matches!(key.as_str(), "address" | "chunks" | "kind" | "meta")
                });
                if let Some(child) = object.get_mut("address") {
                    prune_document_address(child);
                }
                if let Some(child) = object.get_mut("chunks") {
                    if let Some(items) = child.as_array_mut() {
                        for item in items {
                            prune_lane_chunk(item);
                        }
                    }
                }
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "elided" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "hoisted" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta" | "target"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("target") {
                    prune_spec_address(child);
                }
            }
            _ => {}
        }
    }

    fn prune_lane_frame(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object
            .retain(|key, _| matches!(key.as_str(), "generated_path" | "renames" | "source_root"));
        if let Some(child) = object.get_mut("renames") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_origin_rename(item);
                }
            }
        }
    }

    fn prune_lane_ir(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "context" | "contributions" | "frame" | "source_link_digest" | "source_node_count"
            )
        });
        if let Some(child) = object.get_mut("context") {
            prune_artifact_context(child);
        }
        if let Some(child) = object.get_mut("contributions") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_lane_contribution(item);
                }
            }
        }
        if let Some(child) = object.get_mut("frame") {
            prune_lane_frame(child);
        }
    }

    fn prune_lane_node(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "normal" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "body"
                            | "contribution"
                            | "fence_after"
                            | "fence_before"
                            | "kind"
                            | "marker"
                            | "node"
                            | "occurrence"
                            | "origin"
                            | "requested_address"
                    )
                });
                if let Some(child) = object.get_mut("fence_after") {
                    prune_fence_snapshot(child);
                }
                if let Some(child) = object.get_mut("fence_before") {
                    prune_fence_snapshot(child);
                }
                if let Some(child) = object.get_mut("requested_address") {
                    prune_spec_address(child);
                }
            }
            "simple" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "address"
                            | "body"
                            | "contribution"
                            | "fence_after"
                            | "fence_before"
                            | "kind"
                            | "occurrence"
                            | "origin"
                    )
                });
                if let Some(child) = object.get_mut("address") {
                    prune_document_address(child);
                }
                if let Some(child) = object.get_mut("fence_after") {
                    prune_fence_snapshot(child);
                }
                if let Some(child) = object.get_mut("fence_before") {
                    prune_fence_snapshot(child);
                }
            }
            _ => {}
        }
    }

    fn prune_level_closure(_value: &mut serde_json::Value) {}

    fn prune_level_document(_value: &mut serde_json::Value) {}

    fn prune_level_emitted(_value: &mut serde_json::Value) {}

    fn prune_level_lane(_value: &mut serde_json::Value) {}

    fn prune_level_source(_value: &mut serde_json::Value) {}

    fn prune_link_contribution_witness(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "normal" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "kind" | "meta" | "occurrence_count" | "seed" | "seed_address"
                    )
                });
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("seed_address") {
                    prune_spec_address(child);
                }
            }
            "simple" => {
                object.retain(|key, _| matches!(key.as_str(), "address" | "kind" | "meta"));
                if let Some(child) = object.get_mut("address") {
                    prune_document_address(child);
                }
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "elided" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
            }
            "hoisted" => {
                object.retain(|key, _| matches!(key.as_str(), "kind" | "meta" | "target"));
                if let Some(child) = object.get_mut("meta") {
                    prune_contribution_meta(child);
                }
                if let Some(child) = object.get_mut("target") {
                    prune_spec_address(child);
                }
            }
            _ => {}
        }
    }

    fn prune_link_occurrence(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("kind")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "normal" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "address"
                            | "body"
                            | "contribution"
                            | "fence_after"
                            | "fence_before"
                            | "kind"
                            | "marker"
                            | "node"
                            | "occurrence"
                            | "trailing_newline_required"
                    )
                });
                if let Some(child) = object.get_mut("address") {
                    prune_spec_address(child);
                }
                if let Some(child) = object.get_mut("fence_after") {
                    prune_fence_snapshot(child);
                }
                if let Some(child) = object.get_mut("fence_before") {
                    prune_fence_snapshot(child);
                }
            }
            "simple" => {
                object.retain(|key, _| {
                    matches!(
                        key.as_str(),
                        "address"
                            | "body"
                            | "contribution"
                            | "fence_after"
                            | "fence_before"
                            | "kind"
                            | "occurrence"
                            | "trailing_newline_required"
                    )
                });
                if let Some(child) = object.get_mut("address") {
                    prune_document_address(child);
                }
                if let Some(child) = object.get_mut("fence_after") {
                    prune_fence_snapshot(child);
                }
                if let Some(child) = object.get_mut("fence_before") {
                    prune_fence_snapshot(child);
                }
            }
            _ => {}
        }
    }

    fn prune_link_result(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "contributions" | "input_digest" | "mode" | "occurrences"
            )
        });
        if let Some(child) = object.get_mut("contributions") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_link_contribution_witness(item);
                }
            }
        }
        if let Some(child) = object.get_mut("mode") {
            prune_compile_mode(child);
        }
        if let Some(child) = object.get_mut("occurrences") {
            if let Some(items) = child.as_array_mut() {
                for item in items {
                    prune_link_occurrence(item);
                }
            }
        }
    }

    fn prune_link_state(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("state")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "unlinked" => {
                object.retain(|key, _| matches!(key.as_str(), "state"));
            }
            "linked" => {
                object.retain(|key, _| matches!(key.as_str(), "result" | "state"));
                if let Some(child) = object.get_mut("result") {
                    prune_link_result(child);
                }
            }
            _ => {}
        }
    }

    fn prune_origin_rename(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "origin" | "rename"));
        if let Some(child) = object.get_mut("rename") {
            prune_rename_entry(child);
        }
    }

    fn prune_qualification_state(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        let Some(tag) = object
            .get("state")
            .and_then(serde_json::Value::as_str)
            .map(str::to_owned)
        else {
            return;
        };
        match tag.as_str() {
            "pending" => {
                object.retain(|key, _| matches!(key.as_str(), "mode" | "state"));
                if let Some(child) = object.get_mut("mode") {
                    prune_compile_mode(child);
                }
            }
            "applied" => {
                object.retain(|key, _| matches!(key.as_str(), "mode" | "state"));
                if let Some(child) = object.get_mut("mode") {
                    prune_compile_mode(child);
                }
            }
            _ => {}
        }
    }

    fn prune_rename_entry(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "original" | "qualified"));
    }

    fn prune_source_doc(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "address" | "format" | "subject" | "text"));
        if let Some(child) = object.get_mut("address") {
            prune_document_address(child);
        }
        if let Some(child) = object.get_mut("subject") {
            prune_document_subject(child);
        }
    }

    fn prune_source_resolution_snapshot(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "discovery_order" | "documents" | "expansions" | "explicit_use_keys"
            )
        });
        if let Some(child) = object.get_mut("discovery_order") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
        if let Some(child) = object.get_mut("documents") {
            if let Some(items) = child.as_object_mut() {
                for item in items.values_mut() {
                    prune_document_observation(item);
                }
            }
        }
        if let Some(child) = object.get_mut("expansions") {
            if let Some(items) = child.as_object_mut() {
                for item in items.values_mut() {
                    prune_expansion_observation(item);
                }
            }
        }
        if let Some(child) = object.get_mut("explicit_use_keys") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
    }

    fn prune_span(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| matches!(key.as_str(), "end" | "start"));
    }

    fn prune_spec_address(value: &mut serde_json::Value) {
        let Some(object) = value.as_object_mut() else {
            return;
        };
        object.retain(|key, _| {
            matches!(
                key.as_str(),
                "anchor" | "authority" | "doc_path" | "pinned_r" | "raw"
            )
        });
        if let Some(child) = object.get_mut("anchor") {
            if let Some(items) = child.as_array_mut() {
                for item in items {}
            }
        }
        if let Some(child) = object.get_mut("authority") {
            prune_authority(child);
        }
    }
}
