// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Epoch-1 engine-owned deploy lock sidecar. It durably retains committed and
/// pending generation bindings, including the exact package-native provider
/// image identity needed for read-only process-restart rehydration. Neither
/// slot is reconstructible after a crash.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct DeployLockResources {
    /// Record schema epoch; exactly 1 in epoch 1.
    pub schema: u32,

    /// The binding owned by the last finalised receipt.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub committed: Option<LockBinding>,

    /// The binding durably staged for the deployment in flight.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pending: Option<LockBinding>,
}

/// An exact non-negative byte count. Stock JTD has no uint64 form, so the
/// schema's numeric form is narrowed by generated Rust to u64; decimals,
/// negatives and overflow refuse at the generated reader.
pub type ImageByteCount = u64;

/// One generation's physical lock set joined to the exact plan and optional
/// native provider runtime that produced it.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct LockBinding {
    pub generation: u32,

    pub plan_hash: String,

    pub resources: Vec<String>,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub native: Option<NativeProviderBinding>,
}

/// Exact package-native deploy provider identity retained for process restart.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct NativeProviderBinding {
    pub target: String,

    pub mechanism: String,

    pub pin: String,

    pub descriptor_id: String,

    pub protocol: u32,

    pub provider_version: String,

    #[serde(deserialize_with = "crate::behaviour::required_nullable::deserialize")]
    pub provider_hash: Option<String>,

    pub provider_root: String,

    pub platform: String,

    pub record_root: String,

    pub origin: String,

    #[serde(deserialize_with = "crate::behaviour::required_nullable::deserialize")]
    pub record_id: Option<String>,

    #[serde(deserialize_with = "crate::behaviour::required_nullable::deserialize")]
    pub record_path: Option<String>,

    pub image: String,

    pub image_digest: String,

    pub image_bytes: ImageByteCount,
}
