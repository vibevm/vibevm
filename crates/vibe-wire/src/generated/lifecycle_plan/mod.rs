// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// Wire format for the complete pre-dispatch lifecycle ritual. `command` is
/// always `lifecycle:plan`. It carries selection only—never outcomes or handler
/// messages—and is emitted before execution begins.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct LifecyclePlan {
    pub chain: Vec<String>,

    pub command: String,

    pub contributions: Vec<PlannedContribution>,

    pub notices: Vec<String>,

    pub requested: String,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct PlannedContribution {
    pub handler: String,

    pub key: String,

    pub phase: String,

    pub point: String,

    pub provider: String,

    pub tier: String,

    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
}
