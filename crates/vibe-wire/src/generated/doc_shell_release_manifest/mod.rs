// Code generated by jtd-codegen for Rust v0.2.1

use serde::{Deserialize, Serialize};

/// `DOC-SHELL.json` — the release manifest beside one documentation-
/// shell archive (PROP-057 `##SHELL-RELEASE-ASSET`). It binds one vibe
/// release identity to the exact name, byte count and digest of the
/// separately downloadable shell archive. Source of truth for `crates/vibe-
/// wire/src/generated/doc_shell_release_manifest/`.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct DocShellReleaseManifest {
    pub schema_version: u32,

    pub product: String,

    pub repository: String,

    pub version: String,

    pub tag: String,

    pub source_commit: String,

    pub asset: DistributionAsset,
}

/// An exact non-negative byte count. JTD has no uint64 form, so generated
/// Rust narrows the numeric form to u64 and refuses fractions, negatives and
/// overflow.
pub type ByteCount = u64;

/// The separately published documentation-shell archive.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct DistributionAsset {
    pub name: String,

    pub size: ByteCount,

    /// Lowercase `sha256:<64 hex digits>` digest of the complete archive.
    pub digest: String,
}
