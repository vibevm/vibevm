<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">PROP-012: The managed redirect block — vibevm as a co-tenant of the agent instruction files</title>
  <status stage="impl" state="done" comment="B0 2026-07-24: IMPLEMENTED, M1.18 phase 7 block engine"/>
  <p><milestone-line fact="true" status="impl/done">**Milestone:** design proposal; it refines [PROP-009 §2.3](PROP-009-loading-model.xml) (M1.18, Phases 1–6 shipped) and **corrects a destructive defect already in the shipped Phase-4 code** — so it is a prerequisite for PROP-009 §4 / the M1.18 Phase-7 redirect rewrite, not a far-future milestone. Owner to place in [`ROADMAP.md`](../../../ROADMAP.md). Not implementation-locked.</milestone-line></p>
  <p><status-line fact="true" status="impl/done">**Status:** IMPLEMENTED — shipped with M1.18 Phase 7 (the block engine in `vibe-workspace::boot_artifacts`, plan-time validation, the `vibe check` `RedirectBlock` finding, and the self-migration that put the `&lt;vibevm&gt;` block into this repository's own instruction files). Requirements were captured 2026-05-22 (drafts 1–2); decision units typed at REQ grain 2026-06-12 (the depth program).</status-line></p>
  <p><related fact="true" status="spec/done">**Related:** [PROP-009](PROP-009-loading-model.xml) (the loading model — §2.3 the redirect, §4 migration; the Phase-4 implementation `vibe-workspace::boot_artifacts::write_boot_artifacts` / `render_redirect` / `REDIRECT_FILES` this PROP reworks); [PROP-007](PROP-007-workspace.xml) (workspaces — every entry-point node carries its own instruction files).</related></p>
  <p><discipline-line fact="true" status="spec/done">**Discipline:** the general co-tenant / managed-block pattern — the co-tenant law, the marker design, the well-formedness state machine that classifies a file before mutation, and the create / update / remove verbs — is the `managed-blocks` flow: `spec://org.vibevm.world/managed-blocks/flows/managed-blocks/MANAGED-BLOCKS-PROTOCOL#root`. This PROP is vibevm's instance of it — the `&lt;vibevm&gt;` redirect block in the agent instruction files, verified by `vibe-workspace::boot_artifacts` and the `vibe check` `RedirectBlock` finding.</discipline-line></p>
  <p><OWNER-SANCTION fact="true" status="impl/done">**Owner sanction:** PROP-012 amends PROP-009 §2.3 (a PROP document — editable) and reshapes the redirect-file wording in `VIBEVM-SPEC.md` §6.1 / §4.2. Those `VIBEVM-SPEC.md` edits fall inside the M1.18 Phase-7 spec-consistency sanction already granted; PROP-012 adds no new owner-frozen surface.</OWNER-SANCTION></p>
  <motivation title="1. Motivation">
    <facts ordered="false">
      <redirect-status-quo fact="true" status="impl/done">PROP-009 §2.3 has `vibe install` generate the `CLAUDE.md` / `AGENTS.md` / `GEMINI.md` **thin redirect** that points a session at the boot artifacts.</redirect-status-quo>
      <whole-file-write fact="true" status="impl/done">The Phase-4 implementation — `vibe-workspace::boot_artifacts::write_boot_artifacts` — does this with an unconditional whole-file `fs::write`: every `vibe install` / `vibe reinstall` / `vibe uninstall` / `vibe update` replaces the **entire file** with vibevm's generated content.</whole-file-write>
    </facts>
    <facts ordered="false">
      <wrongness-claim fact="true" status="impl/done">This is wrong, and it is wrong in the most damaging possible place.</wrongness-claim>
    </facts>
    <p><SHARED-SURFACE fact="true" status="impl/done">`CLAUDE.md` / `AGENTS.md` / `GEMINI.md` are not vibevm's files. They are the **shared contact surface every coding agent reads at session start**, and many parties legitimately write there:</SHARED-SURFACE></p>
    <facts ordered="false">
      <tenant-developer fact="true" status="impl/done">the developer's own hand-authored project instructions;</tenant-developer>
      <tenant-other-tools fact="true" status="impl/done">another tool, agent, framework, or convention the developer runs *alongside* vibevm — vibevm must assume it is never the only tenant of a repository;</tenant-other-tools>
      <tenant-vibevm-repo fact="true" status="impl/done">in the vibevm repository itself, `CLAUDE.md` is today ~200 hand-authored lines carrying the four non-negotiable rules and the session-end command.</tenant-vibevm-repo>
    </facts>
    <facts ordered="false">
      <silent-destruction fact="true" status="impl/done">A whole-file overwrite silently destroys every byte of that.</silent-destruction>
      <data-loss-event fact="true" status="impl/done">The first `vibe install` in any project with a non-trivial `CLAUDE.md` is a data-loss event — and it destroys precisely the file a developer is most likely to have invested in by hand.</data-loss-event>
    </facts>
    <facts ordered="false">
      <FOUNDING-RULE-CONTRADICTION fact="true" status="impl/done">It also contradicts vibevm's own founding rule. PROP-009 §2.1 — the C++-`#include` rule — is that *installing a dependency must never modify a node's authored content*.</FOUNDING-RULE-CONTRADICTION>
      <rule-violated fact="true" status="impl/done">The redirect write violates exactly that, for the most visible authored file in the project.</rule-violated>
    </facts>
    <facts ordered="false">
      <FIX-MANAGED-BLOCK fact="true" status="impl/done">The fix is the standard managed-block discipline (the general problem and the co-tenant law: the `managed-blocks` flow's `spec://org.vibevm.world/managed-blocks/flows/managed-blocks/MANAGED-BLOCKS-PROTOCOL#problem`) — the shape `ssh`, shell-rc installers, and countless config tools already use: vibevm owns one small, clearly-delimited, **machine-findable** region of each instruction file, and never touches a byte outside it.</FIX-MANAGED-BLOCK>
      <good-co-tenant fact="true" status="impl/done">vibevm becomes a **good co-tenant** — it writes its redirect into its own pen and leaves the rest of the file to whoever else shares it.</good-co-tenant>
    </facts>
  </motivation>
  <decisions title="2. Decisions">
    <co-tenant title="2.1 vibevm owns one delimited block, nothing else">
      <p><req-co-tenant fact="true" status="impl/done">`req r1`</req-co-tenant></p>
      <p><ONE-BLOCK-LAW fact="true" status="impl/done">**Decision.** `vibe` owns exactly one **managed block** inside each agent instruction file — a contiguous region bounded by an opening and a closing marker.</ONE-BLOCK-LAW></p>
      <facts ordered="false">
        <OUTSIDE-PRESERVED fact="true" status="impl/done">`vibe` reads and rewrites only the content *between* the markers; every byte outside the block is treated as another tenant's property and preserved verbatim across every `vibe` operation.</OUTSIDE-PRESERVED>
      </facts>
      <facts ordered="false">
        <SHARED-FILE-REFRAME fact="true" status="impl/done">The instruction file ceases to be "a vibevm-generated file." It is a **shared file with a vibevm-managed block in it**.</SHARED-FILE-REFRAME>
        <REDIRECT-FILE-SET fact="true" status="impl/done">The set of instruction files is PROP-009's `REDIRECT_FILES` — today `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`; the set is open.</REDIRECT-FILE-SET>
      </facts>
    </co-tenant>
    <markers title="2.2 The markers, and well-formedness">
      <p><req-markers fact="true" status="impl/done">`req r1`</req-markers></p>
      <p><BARE-TAGS fact="true" status="impl/done">**Decision.** The block is delimited by the literal **bare tags** `&lt;vibevm&gt;` and `&lt;/vibevm&gt;` — opening and closing — each alone on its own line.</BARE-TAGS></p>
      <facts ordered="false">
        <bare-tags-why fact="true" status="impl/done">Bare tags are chosen over HTML-comment delimiters (`&lt;!-- vibevm:begin --&gt;` …): they read unambiguously to an LLM, the file's primary consumer.</bare-tags-why>
        <cosmetic-cost fact="true" status="impl/done">A markdown renderer may display a bare non-standard tag oddly — an accepted cosmetic cost, addressed separately if it ever matters.</cosmetic-cost>
      </facts>
      <p><canonical-block-lead fact="true" status="impl/done">The canonical block vibevm writes:</canonical-block-lead></p>
      <fence>&lt;vibevm&gt;
&lt;!-- Generated by vibe — do not edit inside this block. It is rewritten on `vibe install`. --&gt;
... the boot redirect (§2.6) ...
&lt;/vibevm&gt;</fence>
      <facts ordered="false">
        <PLAIN-TEXT-SCAN fact="true" status="impl/done">The markers are located by a **plain line-anchored text scan** — no markdown parse, no model in the loop. "Programmatically findable without an LLM" is a hard requirement: the markers gate a mutating write, so detection must be deterministic and trivial.</PLAIN-TEXT-SCAN>
        <WELL-FORMED-DEF fact="true" status="impl/done">A **well-formed** instruction file contains either *zero* markers (the block is absent — §2.3) or *exactly one* `&lt;vibevm&gt;` line followed, later in the file, by *exactly one* `&lt;/vibevm&gt;` line.</WELL-FORMED-DEF>
        <MALFORMED-DEF fact="true" status="impl/done">Anything else is **malformed**: two or more of either marker, an opener with no closer, a closer with no opener, or a closer preceding its opener.</MALFORMED-DEF>
      </facts>
    </markers>
    <create title="2.3 Malformed → hard stop; absent → create; present → splice">
      <p><req-create fact="true" status="impl/done">`req r1`</req-create></p>
      <p><CLASSIFY-FIRST fact="true" status="impl/done">**Decision.** On every operation that would write the block, `vibe` first classifies each instruction file:</CLASSIFY-FIRST></p>
      <facts ordered="false">
        <CLASS-MALFORMED fact="true" status="impl/done">**Malformed** (§2.2) — `vibe` **aborts the whole operation** with an error naming the file and the exact defect, and changes nothing. It does not proceed until the user repairs the file by hand. vibevm **never guesses** which of two blocks is canonical and **never auto-deletes** a stray marker — a malformed managed block is always a human's call.</CLASS-MALFORMED>
        <CLASS-ABSENT fact="true" status="impl/done">**Absent** (zero markers) — `vibe` appends a fresh block at the end of the file, preceded by one blank line. If the file itself does not exist, `vibe` creates it containing only the block.</CLASS-ABSENT>
        <CLASS-PRESENT fact="true" status="impl/done">**Present** (one well-formed pair) — `vibe` replaces the content *between* the markers with freshly-generated content; the markers themselves and all text outside them are untouched. If the new inter-marker content is byte-identical to the old, `vibe` writes nothing — no git churn.</CLASS-PRESENT>
      </facts>
    </create>
    <placement title="2.4 Placement is the user&apos;s — vibevm never moves the block">
      <p><req-placement fact="true" status="impl/done">`req r1`</req-placement></p>
      <p><PLACEMENT-ONCE fact="true" status="impl/done">**Decision.** vibevm decides the block's position **exactly once** — when it first creates the block (§2.3, absent → create) it is appended at the **end of the file**.</PLACEMENT-ONCE></p>
      <p><POSITION-USERS fact="true" status="impl/done">From then on the position is the **user's**: `vibe` rewrites the content between the markers and **never relocates the markers**; whatever text precedes or follows the block stays exactly where it is, across every `vibe` operation.</POSITION-USERS></p>
      <p><humble-default fact="true" status="impl/done">The end-of-file default is the humble, co-tenant choice — vibevm does not claim the attention-priority opening of a shared file, so the developer's own instructions and any other tool's content load first.</humble-default></p>
      <p><POSITION-KNOB fact="true" status="impl/done">The position is then a deliberate, user-owned knob. Because vibevm honours wherever the block sits, the user controls how strongly its redirect weighs in a session:</POSITION-KNOB></p>
      <facts ordered="false">
        <position-top fact="true" status="impl/done">moved to the **top** of the file, vibevm's redirect reads as the **"First Prompt"** — a system-prompt-like instruction with maximum attention weight;</position-top>
        <position-end fact="true" status="impl/done">left at the **end**, vibevm behaves as a **sidecar**, secondary to the user's own content.</position-end>
      </facts>
      <p><manual-once fact="true" status="impl/done">It is a manual configuration the user makes by hand, once; vibevm supplies the polite default and never overrides the choice.</manual-once></p>
    </placement>
    <plan-time title="2.5 Validate before mutate">
      <p><req-plan-time fact="true" status="impl/done">`req r1`</req-plan-time></p>
      <p><PLAN-TIME-VALIDATION fact="true" status="impl/done">**Decision.** The well-formedness classification of §2.3 runs at **plan time** — before any `vibedeps/` materialisation or boot-artifact write.</PLAN-TIME-VALIDATION></p>
      <p><FAIL-FAST fact="true" status="impl/done">A malformed block fails the operation **fast and clean**: an install never gets half-applied, with a materialised `vibedeps/` tree and an unwritable redirect. This is the same plan-time-not-apply-time discipline vibevm already applies to its user-owned-path guards.</FAIL-FAST></p>
    </plan-time>
    <content title="2.6 The block content is unchanged — this PROP changes the envelope, not the payload">
      <p><design-content fact="true" status="impl/done">`design r1`</design-content></p>
      <p><PAYLOAD-UNCHANGED fact="true" status="impl/done">**Decision.** What goes *between* the markers is exactly the redirect content PROP-009 §2.3 already specifies — an instruction to read `spec/boot/STATIC.xml` (when present) and then `spec/boot/INDEX.md`.</PAYLOAD-UNCHANGED></p>
      <facts ordered="false">
        <ENVELOPE-NOT-PAYLOAD fact="true" status="impl/done">PROP-012 changes how that content is *delivered* — a fenced region of a possibly-shared file — not what it *says*.</ENVELOPE-NOT-PAYLOAD>
      </facts>
      <facts ordered="false">
        <SUPERSEDES-WORDING fact="true" status="impl/done">Consequently this PROP **supersedes the "thin generated file" wording of PROP-009 §2.3**: the instruction file is no longer wholly generated; only its `&lt;vibevm&gt;` block is.</SUPERSEDES-WORDING>
        <SESSION-ORDER-UNCHANGED fact="true" status="impl/done">PROP-009 §2.3's session-start order is otherwise unchanged — the agent reads the instruction file, then `STATIC.md`, then `INDEX.md`.</SESSION-ORDER-UNCHANGED>
        <WARNING-SCOPED fact="true" status="impl/done">The "generated — do not edit" warning moves *inside* the block and is reworded to scope it to the block.</WARNING-SCOPED>
        <CHECK-FINDING fact="true" status="impl/done">A malformed block is also a `vibe check` finding (§3), so the user meets the problem in the linter rather than mid-install.</CHECK-FINDING>
      </facts>
    </content>
  </decisions>
  <surface title="3. Command and crate surface">
    <p><design-surface fact="true" status="impl/done">`design r1`</design-surface></p>
    <facts ordered="false">
      <SURF-BOOT-ARTIFACTS fact="true" status="impl/done">**`vibe-workspace::boot_artifacts`** — the implementation site. `write_boot_artifacts`'s redirect loop changes from a whole-file `fs::write` to a locate-validate-splice. New helpers: locate the block, classify well-formedness, splice the body. `render_redirect` becomes the block-*body* renderer; `REDIRECT_BODY`'s "do not edit" framing moves inside the block.</SURF-BOOT-ARTIFACTS>
      <SURF-ORCHESTRATION fact="true" status="impl/done">**The install orchestration** (`apply_resolution` and the `vibe reinstall` path) — gains the §2.5 plan-time validation pass over every node's instruction files before any mutation.</SURF-ORCHESTRATION>
      <SURF-CLI fact="true" status="impl/done">**`vibe-cli`** — `vibe install` / `reinstall` / `uninstall` / `update` surface the malformed-block abort as a clear error; `vibe init` scaffolds each instruction file with a `&lt;vibevm&gt;` block instead of a one-line file.</SURF-CLI>
      <SURF-CHECK fact="true" status="impl/done">**`vibe-check`** — `CheckId::RedirectBlock` reports a malformed `&lt;vibevm&gt;` block (a file with anything other than zero markers or one ordered pair), composing with the boot-directory check.</SURF-CHECK>
      <SURF-EXIT-CODE fact="true" status="spec/work">**Exit code** — the malformed-block abort is conflict-shaped; reusing exit code `3` (package conflict) is the working assumption (§5).</SURF-EXIT-CODE>
    </facts>
  </surface>
  <migration title="4. Migration">
    <p><req-migration fact="true" status="impl/done">`req r1`</req-migration></p>
    <p><MIGRATION-CASE fact="true" status="impl/done">The one non-trivial case is an instruction file that is *wholly* the **old whole-file generated redirect** — written by the Phase-4 `fs::write`, recognisable by its generated header, and carrying no `&lt;vibevm&gt;` markers. For a file with no markers, `vibe`:</MIGRATION-CASE></p>
    <facts ordered="false">
      <MIG-RECLAIM fact="true" status="impl/done">if the **entire file** is recognisably the old whole-file generated redirect (it matches the known generated header) — replaces it with a clean file containing just the `&lt;vibevm&gt;` block. The old content *was* vibevm's; reclaiming it as a block loses nothing.</MIG-RECLAIM>
      <MIG-APPEND fact="true" status="impl/done">otherwise — takes the §2.3 **absent → create** path: appends a block, preserves everything else.</MIG-APPEND>
    </facts>
    <facts ordered="false">
      <self-migration-path fact="true" status="impl/done">This is also the gentle path for the **vibevm repository's own self-migration** (PROP-009 §4 / M1.18 Phase 7).</self-migration-path>
      <SELF-MIGRATION-APPEND fact="true" status="impl/done">vibevm's `CLAUDE.md` / `AGENTS.md` / `GEMINI.md` are hand-authored — not the generated form — so they take the append path: every hand-authored line, including the four non-negotiable rules, **stays**, and a `&lt;vibevm&gt;` block is appended.</SELF-MIGRATION-APPEND>
      <delicacy-removed fact="true" status="impl/done">The destructive whole-file write is what made the Phase-7 self-migration delicate; with PROP-012 it is not.</delicacy-removed>
    </facts>
    <p><PREREQUISITE-LAW fact="true" status="impl/done">Because the defect is in shipped code, PROP-012 **must land before any `vibe install` / `vibe reinstall` is run against a project with a real `CLAUDE.md`** — including the vibevm repository itself. It is therefore a prerequisite of the Phase-7 redirect rewrite, not a successor to it (§7).</PREREQUISITE-LAW></p>
  </migration>
  <open title="5. Open questions">
    <facts ordered="true">
      <OPEN-EXIT-CODE fact="true" status="spec/work">**Exit code.** Reuse `3` (package conflict — a malformed managed block is conflict-shaped) or mint a dedicated code? Leaning: reuse `3`.</OPEN-EXIT-CODE>
      <OPEN-OLD-REDIRECT-DETECTION fact="true" status="spec/work">**Old-redirect detection for migration (§4).** Match the exact Phase-4 generated-header string (precise; a false positive would wrongly destroy a hand-authored file) versus a looser heuristic. Leaning: exact string match.</OPEN-OLD-REDIRECT-DETECTION>
    </facts>
    <p><draft2-closed fact="true" status="spec/done">Closed in draft 2: the marker syntax — bare `&lt;vibevm&gt;` … `&lt;/vibevm&gt;` tags (§2.2), chosen for legibility to an LLM; the markdown-rendering cosmetics are an accepted, deferred cost.</draft2-closed></p>
  </open>
  <rejected title="6. Rejected / deferred alternatives">
    <facts ordered="false">
      <REJ-WHOLE-FILE fact="true" status="spec/done">**The whole-file overwrite** (the shipped Phase-4 status quo). Rejected — it destroys co-tenant content; undoing it is the reason this PROP exists.</REJ-WHOLE-FILE>
      <REJ-HEURISTIC-DETECTION fact="true" status="spec/done">**Heuristic or LLM-assisted block detection** ("find the part that looks like vibevm's"). Rejected — the region that gates a mutating write must be found by a deterministic byte scan, with no model and no guessing.</REJ-HEURISTIC-DETECTION>
      <REJ-AUTO-REPAIR fact="true" status="spec/done">**Auto-repairing a malformed file** (delete the surplus marker, keep the first block). Rejected — vibevm never decides which of two regions is canonical; a malformed file is a hard stop for the human (§2.3).</REJ-AUTO-REPAIR>
      <REJ-SIDECAR fact="true" status="spec/done">**A sidecar file** (`.vibe/redirect`, a `vibe`-owned file the agent is pointed at) instead of a block inside `CLAUDE.md`. Rejected — the entire value of `CLAUDE.md` / `AGENTS.md` / `GEMINI.md` is that every agent already reads them with zero configuration (PROP-009 §6.1's cross-agent property). A sidecar the agent does not natively read forfeits that.</REJ-SIDECAR>
    </facts>
  </rejected>
  <phases title="7. Phase plan">
    <facts ordered="true">
      <PHASE-1-BLOCK-ENGINE fact="true" status="impl/done">**The block engine** in `vibe-workspace::boot_artifacts` — locate / classify / splice; `write_boot_artifacts`'s redirect loop reworked; unit tests for every well-formedness class (absent, well-formed, each malformed shape, byte-identical no-op).</PHASE-1-BLOCK-ENGINE>
      <PHASE-2-PLAN-TIME fact="true" status="impl/done">**Plan-time validation** wired into `apply_resolution` and the `vibe reinstall` path; the malformed-block abort surfaced by `vibe install` / `reinstall` / `uninstall` / `update`; `vibe init` scaffolds the block.</PHASE-2-PLAN-TIME>
      <PHASE-3-MIGRATION fact="true" status="impl/done">**Migration** of the old whole-file generated redirect (§4).</PHASE-3-MIGRATION>
      <PHASE-4-CHECK-DOCS fact="true" status="impl/done">**`vibe check` finding**, the `docs/` note, and the `VIBEVM-SPEC.md` §6.1 / §4.2 wording (inside the M1.18 Phase-7 spec-consistency pass).</PHASE-4-CHECK-DOCS>
    </facts>
    <p><SEQUENCING fact="true" status="impl/done">**Sequencing.** PROP-012 corrects shipped Phase-1–6 behaviour and is a **prerequisite** for PROP-009 §4 / the M1.18 Phase-7 redirect rewrite — the vibevm self-migration must not be able to destroy `CLAUDE.md`. Recommended: implement PROP-012 **within M1.18 Phase 7**, ahead of the self-migration step, rather than as a later standalone milestone. Owner to confirm.</SEQUENCING></p>
  </phases>
  <history title="8. Version history">
    <facts ordered="false">
      <HISTORY-DRAFT-1 fact="true" status="spec/done">**2026-05-22 — draft 1.** Requirements captured in an owner discussion during M1.18 Phase-7 planning. The whole-file redirect overwrite shipped in PROP-009 Phase 4 is destructive to co-tenant content; vibevm must own only a `&lt;vibevm&gt;`-delimited block of each agent instruction file: exactly one block per file, a hard stop on a malformed file (the user repairs it by hand — vibevm never guesses), absent → create at end of file, present → splice, validated at plan time. The block's position is thereafter the user's — vibevm never relocates it, so the user can promote the block to a "First Prompt" or leave it a sidecar. Recorded as a separate PROP at the owner's request.</HISTORY-DRAFT-1>
      <HISTORY-DRAFT-2 fact="true" status="spec/done">**2026-05-22 — draft 2.** Owner review settled the one substantive §5 question — the marker syntax: bare `&lt;vibevm&gt;` … `&lt;/vibevm&gt;` tags (§2.2), chosen because they read unambiguously to an LLM, the file's primary consumer; the markdown-rendering cosmetics of a bare tag are an accepted, deferred cost. The two remaining §5 questions — the exit code and old-redirect detection — are implementation details with working answers. PROP-012 is ready for implementation within M1.18 Phase 7.</HISTORY-DRAFT-2>
      <HISTORY-UNIT-TYPING fact="true" status="spec/done">**2026-06-12 — unit typing (the depth program).** §2.1–2.5 and §4 typed `req r1`, §2.6 and §3 typed `design r1`; the Status line updated to reflect the shipped M1.18 Phase-7 implementation (the audit's finding 2026-06-12-04 recorded PROP-012 as implemented-with-zero-edges; the affirmation sweep tags the implementation against these units).</HISTORY-UNIT-TYPING>
    </facts>
  </history>
</spec>
