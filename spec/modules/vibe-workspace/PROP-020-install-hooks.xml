<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">PROP-020 — Install hooks</title>
  <status stage="impl" state="done" comment="C 2026-07-25: the hook subsystem ships (HooksDecl parse + hooks.rs runner + apply wiring); motivation, rejected and out-of-scope facts stay spec-stage; fact grain 2026-07-24"/>
  <p><status-line fact="true" status="impl/done">**Status: IMPLEMENTED** (specified 2026-06-24 in an owner-requested design
session; verified against the tree 2026-07-25 by the spec-actualization
campaign). `[hooks]` parses in `vibe-core` (`manifest/package/hooks.rs`),
`vibe-workspace/src/hooks.rs` runs the whole contract — the pre/post phases, the
Git-Bash-first Windows interpreter selection, the trust gate over
`DEFAULT_ALLOWED_GROUPS`, the ran / skipped / failed statuses and the
`VIBE_HOOK_*` environment — and `vibe-install`'s apply pipeline drives it. One of
four orthogonal specs carved from the bridge-packages design (the others:
[PROP-021](../vibe-registry/PROP-021-submodule-sources.xml) submodule sources,
[PROP-022](PROP-022-materialization-modes.xml) materialization modes,
[PROP-023](../vibe-registry/PROP-023-bridge-packages.xml) bridge packages). The
four compose to solve bridge packages but each stands alone — hooks exist for
*any* package, not only bridges.</status-line></p>
  <p><related fact="true" status="spec/done">**Related:** [PROP-009](PROP-009-loading-model.xml) (the install/materialise
pipeline hooks slot into), [PROP-007](PROP-007-workspace.xml) (workspace +
`vibedeps/`), [PROP-022](PROP-022-materialization-modes.xml) (a hook's
working tree is the materialised slot; how its edits are reset on update is a
materialization-mode property), [PROP-015 §2.6](../vibe-mcp/PROP-015-mcp-integration.xml#skill)
(skill projection reads the slot a hook prepared), [PROP-019 §2.13](../../common/PROP-019-version-manager.xml#security)
(the same "a build/script the user installs is code they chose to run" trust
posture), [PROP-000 §20](../../common/PROP-000.xml#token-secrecy) (the publish
token a hook never sees).</related></p>
  <motivation title="1. Motivation">
    <problem title="1.1 The problem — install is pure file I/O, with no preparation step">
      <facts ordered="false">
        <install-pure-io fact="true" status="impl/done">Today `vibe install` resolves, fetches, materialises a package's tree into
  `vibedeps/`, regenerates boot artefacts, and writes the lockfile — all pure
  file copying.</install-pure-io>
        <no-preparation-step fact="true" status="impl/done">A package that needs a *preparation* step after its content
  lands (normalise a vendored layout, generate a derived file, assemble a clean
  skill subtree out of an upstream repo's mess) has nowhere to put it.</no-preparation-step>
      </facts>
      <facts ordered="false">
        <forcing-case-bridges fact="true" status="spec/done">The forcing case is bridge packages ([PROP-023](../vibe-registry/PROP-023-bridge-packages.xml)):
  a maintainer wraps someone else's repository whose structure does not match
  vibevm conventions, and needs to *bring it into order* before vibevm's skill
  machinery reads it.</forcing-case-bridges>
        <hooks-universal fact="true" status="spec/done">But the need is general — any package may want a
  post-materialise step — so hooks are a **universal** mechanism, not a
  bridge-only feature.</hooks-universal>
      </facts>
    </problem>
    <what title="1.2 What this is — declared lifecycle scripts, run per package">
      <facts ordered="false">
        <WHAT-HOOKS-ARE fact="true" status="impl/done">A package may declare `pre-install` / `post-install` scripts in its manifest.
  vibevm runs them at fixed points in the install pipeline, in the package's own
  materialised slot, choosing the right interpreter for the host OS.</WHAT-HOOKS-ARE>
        <EFFECTS-EPHEMERAL fact="true" status="impl/done">Their
  effects are **ephemeral**: re-installing or updating the package resets the
  slot first (per [PROP-022](PROP-022-materialization-modes.xml)), then re-runs
  the hooks, so a hook is a pure function of the package content, never an
  accreting pile of edits.</EFFECTS-EPHEMERAL>
      </facts>
    </what>
  </motivation>
  <decisions title="2. Decisions">
    <phases title="2.1 Two phases, anchored to the materialise pipeline">
      <p><req-phases fact="true" status="impl/done">`req r1`</req-phases></p>
      <p><ONE-PER-PHASE fact="true" status="impl/done">A package declares at most one script per phase:</ONE-PER-PHASE></p>
      <facts ordered="false">
        <PHASE-PRE-INSTALL fact="true" status="impl/done">**`pre-install`** — runs immediately after the package's slot is fully
  populated (content materialised, submodules fetched per
  [PROP-021](../vibe-registry/PROP-021-submodule-sources.xml)) and **before**
  vibevm uses the slot (before boot regeneration, before any later
  `vibe skill` projection reads it). This is the "bring the tree into order"
  hook.</PHASE-PRE-INSTALL>
        <PHASE-POST-INSTALL fact="true" status="impl/done">**`post-install`** — runs after the install run is durable for that package
  (lockfile written, boot artefacts regenerated). For finalisation that needs
  the package already registered.</PHASE-POST-INSTALL>
      </facts>
      <p><CWD-IS-SLOT fact="true" status="impl/done">The hook's **working directory is the package's materialised slot**; it sees
exactly the tree vibevm will use.</CWD-IS-SLOT></p>
      <facts ordered="false">
        <RESET-THEN-RERUN fact="true" status="impl/done">**On update / reinstall, hook effects are reset, then hooks re-run.** The slot
  is first returned to its pristine materialised state — for `snapshot`/
  `hardlink` modes by re-materialising from cache, for `in-place` mode by
  `git clean -dfx` ([PROP-022 §2.4](PROP-022-materialization-modes.xml#in-place))
  — so a previous run's edits never compound.</RESET-THEN-RERUN>
        <RESET-IS-MODE-PROPERTY fact="true" status="impl/done">This reset is a
  materialization-mode property; hooks only define *when* the re-run happens.</RESET-IS-MODE-PROPERTY>
      </facts>
    </phases>
    <script-selection title="2.2 Interpreter selection is OS-derived">
      <p><req-interpreter fact="true" status="impl/done">`req r1`</req-interpreter></p>
      <p><SCRIPT-FORMS fact="true" status="impl/done">A package ships a phase script as `&lt;base&gt;.sh` (portable, POSIX shell) and/or
`&lt;base&gt;.ps1` (PowerShell). The runner picks per host:</SCRIPT-FORMS></p>
      <facts ordered="false">
        <UNIX-SELECTION fact="true" status="impl/done">**Unix (macOS / Linux):** run `&lt;base&gt;.sh` via `bash`. A `.ps1` is ignored.</UNIX-SELECTION>
        <WINDOWS-SELECTION fact="true" status="impl/done">**Windows:** prefer `&lt;base&gt;.sh` via **Git Bash** when a `bash` is found
  (one cross-platform script for `.sh` packages); else fall back to
  `&lt;base&gt;.ps1` via **PowerShell** when one is found. A phase that declares a
  script but finds no usable interpreter is a hard error with a remediation
  hint — never a silent skip.</WINDOWS-SELECTION>
      </facts>
      <facts ordered="false">
        <HOOK-ENV fact="true" status="impl/done">The runner passes a documented environment: `VIBE_PACKAGE_GROUP`,
  `VIBE_PACKAGE_NAME`, `VIBE_PACKAGE_VERSION`, `VIBE_PACKAGE_KIND`,
  `VIBE_PACKAGE_DIR` (the slot, also CWD), `VIBE_HOOK_PHASE`.
  ([PROP-024 §2.3](../../common/PROP-024-code-bearing-packages.xml#build) adds
  `VIBE_PROJECT_ROOT`, the workspace absolute root, so a build hook can target a
  gitignored build dir *outside* the slot; it lands with that work.)</HOOK-ENV>
        <TOKEN-NEVER-IN-ENV fact="true" status="impl/done">The publish token
  ([PROP-000 §20](../../common/PROP-000.xml#token-secrecy)) is **never** placed in
  a hook's environment.</TOKEN-NEVER-IN-ENV>
      </facts>
      <p><RUNNER-SEAM fact="true" status="impl/done">The process runner is an injectable seam (`HookRunner`) so tests assert the
selection logic and argument/env shape without spawning real processes.</RUNNER-SEAM></p>
    </script-selection>
    <trust-gate title="2.3 Trust gate — allow-list of groups, plus first-run consent">
      <p><req-trust fact="true" status="impl/done">`req r1`</req-trust></p>
      <p><TRUST-CHEAP fact="true" status="impl/done">Running a package's hook is running third-party code at install time. Until a
content-scanning gate exists (§4), trust is governed cheaply:</TRUST-CHEAP></p>
      <facts ordered="false">
        <ALLOW-LIST fact="true" status="impl/done">**Allow-listed groups run silently.** A config key (global
  `~/.vibe/config.toml` `[hooks].allowed_groups`, with a project-level
  override) lists trusted package groups. **`org.vibevm` is in the allow-list
  by default.** A package whose group is allow-listed runs its hooks with no
  prompt.</ALLOW-LIST>
        <FIRST-RUN-CONSENT fact="true" status="impl/done">**Other groups need consent.** On the first hook run of a non-allow-listed
  package, vibevm prints what will run (phase, script path, group) and asks
  `y/n`. Declining skips the hook and marks the package install as
  hooks-skipped (surfaced, not silent).</FIRST-RUN-CONSENT>
        <NON-INTERACTIVE-SAFETY fact="true" status="impl/done">**Non-interactive safety.** With `--assume-yes` / in CI, allow-listed
  packages still run; a non-allow-listed package's hook is **not** run
  silently — the install **aborts** with a hint to either allow-list the group
  or pass an explicit `--allow-hooks` opt-in. A script must never execute
  unseen third-party code by default.</NON-INTERACTIVE-SAFETY>
      </facts>
    </trust-gate>
    <manifest title="2.4 Hooks are declared in the manifest">
      <p><req-manifest-hooks fact="true" status="impl/done">`req r1`</req-manifest-hooks></p>
      <p><HOOKS-TABLE fact="true" status="impl/done">Hooks live in a package-role `[hooks]` table in `vibe.toml`:</HOOKS-TABLE></p>
      <fence lang="toml">[hooks]
pre-install  = "hooks/prepare"   # base path, relative to package root
post-install = "hooks/finalise"</fence>
      <facts ordered="false">
        <BASE-PATH-VALUE fact="true" status="impl/done">The value is a **base path without extension**; the runner resolves `.sh` /
  `.ps1` beside it per §2.2.</BASE-PATH-VALUE>
        <PACKAGE-ONLY-TABLE fact="true" status="impl/done">The table is package-only (its presence on a
  `[project]`-role manifest is a validation error, like the other package-only
  sections).</PACKAGE-ONLY-TABLE>
        <ABSENT-DEFAULT fact="true" status="impl/done">An empty/absent `[hooks]` means no hooks — the common case.</ABSENT-DEFAULT>
      </facts>
    </manifest>
    <failure title="2.5 Failure semantics are phase-specific">
      <p><req-failure fact="true" status="impl/done">`req r1`</req-failure></p>
      <p><STREAMS-AND-EXIT fact="true" status="impl/done">A hook's stdout/stderr stream to the user. A non-zero exit is handled by
phase:</STREAMS-AND-EXIT></p>
      <facts ordered="false">
        <FAIL-PRE-ABORTS fact="true" status="impl/done">**`pre-install` failure → the package install aborts.** The slot is rolled
  back (removed) and the install reports the failing package; vibevm never
  registers or projects from a package whose preparation failed.</FAIL-PRE-ABORTS>
        <FAIL-POST-FLAGS fact="true" status="impl/done">**`post-install` failure → the package is installed but flagged.** The
  package is already durable (lockfile written); the failure surfaces as a
  warning with the captured output, never a silent success.</FAIL-POST-FLAGS>
      </facts>
    </failure>
  </decisions>
  <rejected title="3. Rejected alternatives">
    <facts ordered="false">
      <REJ-INLINE-COMMANDS fact="true" status="spec/done">**Inline `command = "..."` strings in the manifest** instead of files —
  rejected: a versioned script file is auditable, diffable, and platform-split
  (`.sh`/`.ps1`); an inline string hides the code in TOML and resists review.</REJ-INLINE-COMMANDS>
      <REJ-BOTH-EXTENSIONS fact="true" status="spec/done">**Running every matching extension on Windows** (`.sh` *and* `.ps1`) —
  rejected: one logical hook per phase keeps behaviour predictable; the
  selection is a single deterministic choice (§2.2).</REJ-BOTH-EXTENSIONS>
      <REJ-GENERAL-LIFECYCLE fact="true" status="spec/done">**A general lifecycle (`pre-uninstall`, `pre-build`, …)** now — deferred:
  only the two phases the bridge case needs are specified; more can be added
  later behind their own anchors without disturbing these.</REJ-GENERAL-LIFECYCLE>
    </facts>
  </rejected>
  <out-of-scope title="4. Out of scope">
    <facts ordered="false">
      <OOS-CONTENT-SCANNING fact="true" status="spec/done">**Content scanning / the LLM "antivirus".** A future gate that inspects a
  package's hooks (and code) for malicious behaviour is far-backlog. Until it
  lands, hook execution is an **explicitly accepted risk** governed only by
  §2.3's allow-list + consent. This is the deliberate posture, not an
  oversight — recorded here as the project's stance.</OOS-CONTENT-SCANNING>
      <OOS-SANDBOXING fact="true" status="spec/done">**Sandboxing / capability-limiting** hooks (containers, seccomp). Hooks run
  with the user's privileges, like `cargo build` scripts or `npm postinstall`.</OOS-SANDBOXING>
      <OOS-NON-GIT-RESET fact="true" status="spec/done">**Non-git slot reset for `in-place`** — a hook over an `in-place`
  ([PROP-022 §2.4](PROP-022-materialization-modes.xml#in-place)) package whose
  source is not git has no cheap reset; `in-place` therefore requires a git
  source (PROP-022 §4).</OOS-NON-GIT-RESET>
    </facts>
  </out-of-scope>
  <acceptance title="5. Acceptance">
    <facts ordered="false">
      <ACC-PHASE-TIMING fact="true" status="impl/done">A package with `[hooks].pre-install` runs the script in its slot before boot
  regeneration; `post-install` runs after the lockfile is written.</ACC-PHASE-TIMING>
      <ACC-INTERPRETER fact="true" status="impl/done">Interpreter selection follows §2.2 on each OS; a declared hook with no usable
  interpreter errors rather than silently skipping.</ACC-INTERPRETER>
      <ACC-CONSENT fact="true" status="impl/done">An allow-listed group (incl. `org.vibevm` by default) runs hooks with no
  prompt; a non-allow-listed package prompts interactively and **aborts** in a
  non-interactive run without `--allow-hooks`.</ACC-CONSENT>
      <ACC-FAILURE-SEMANTICS fact="true" status="impl/done">A `pre-install` non-zero exit rolls back the slot and fails the package; a
  `post-install` failure installs-but-flags.</ACC-FAILURE-SEMANTICS>
      <ACC-RESET-RERUN fact="true" status="impl/done">Updating a package resets the prior hook's edits (per the materialization
  mode) before re-running the hooks.</ACC-RESET-RERUN>
      <ACC-RUNNER-SEAM fact="true" status="impl/done">The `HookRunner` seam lets tests assert selection/env/failure paths without
  spawning real processes.</ACC-RUNNER-SEAM>
      <ACC-FLOOR-GREEN fact="true" status="impl/done">Full `self-check.sh` green; conform 0/0/0; specmap clean.</ACC-FLOOR-GREEN>
    </facts>
  </acceptance>
</spec>
