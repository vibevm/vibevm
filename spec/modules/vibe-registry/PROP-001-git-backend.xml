<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">PROP-001: Git-backed registry for `vibe-registry`</title>
  <status stage="impl" state="done" comment="B0 2026-07-24: accepted and shipped 2026-04-22; partially superseded by PROP-002"/>
  <p><milestone-line fact="true" status="impl/done">**Milestone:** M1.1 ([`ROADMAP.md`](../../../ROADMAP.md#m11--git-backed-registry)).</milestone-line></p>
  <p><status-line fact="true" status="impl/done">**Status:** accepted 2026-04-22, shipped 2026-04-22. **Partially superseded by [PROP-002](PROP-002-decentralized-registry.xml) (2026-04-24).** See the "Superseded parts" block below.</status-line></p>
  <p><supersedes-line fact="true" status="spec/done">**Supersedes:** nothing.</supersedes-line></p>
  <p><related fact="true" status="spec/done">**Related:** [spec://org.vibevm.core/vibevm/common/PROP-000#registry](../../common/PROP-000.xml#registry), [`VIBEVM-SPEC.md` §8](../../../VIBEVM-SPEC.md), [PROP-002](PROP-002-decentralized-registry.xml).</related></p>
  <section title="Superseded parts (by PROP-002)">
    <p><superseded-lead fact="true" status="spec/done">The following decisions in this PROP were revised by [PROP-002](PROP-002-decentralized-registry.xml) when the registry model moved from monorepo-as-registry to decentralized per-package repos. Use PROP-002 as the authoritative source for these:</superseded-lead></p>
    <facts ordered="false">
      <SUP-REGISTRY-TRAIT fact="true" status="spec/done">**§2.3 `Registry` trait** — the single-registry trait is extended by a `MultiRegistryResolver` coordinating several `[[registry]]` entries, each wrapped as a `GitPackageRegistry`. The monorepo-era `GitRegistry` is retired.</SUP-REGISTRY-TRAIT>
      <SUP-CACHE-LAYOUT fact="true" status="spec/done">**§2.4 Cache layout** — `~/.vibe/registries/&lt;hash&gt;/clone/` (one clone per registry URL) is replaced by `~/.vibe/registries/&lt;canonical-url-hash&gt;/packages/&lt;kind&gt;-&lt;name&gt;/{clone,meta.toml}` (one clone per package; the package directory was later re-keyed by identity to `&lt;group&gt;.&lt;name&gt;` — PROP-002 §2.6).</SUP-CACHE-LAYOUT>
      <SUP-SOURCE-URI fact="true" status="spec/done">**§2.6 Lockfile `source_uri` format** — `git+&lt;transport&gt;://&lt;host&gt;/&lt;path&gt;.git#&lt;kind&gt;/&lt;name&gt;/v&lt;ver&gt;` (path-in-monorepo) is replaced by full lockfile fields: `registry`, `source_url`, `source_ref`, `resolved_commit`, `content_hash`; `#fragment` is no longer used.</SUP-SOURCE-URI>
    </facts>
    <p><NOT-SUPERSEDED fact="true" status="spec/done">What is **not** superseded (and remains authoritative here): §2.1 (shell-out-to-git backend choice), §2.2 (`GitBackend` trait), §2.5 (1-hour freshness TTL), §2.7 (Windows UX and stderr classification).</NOT-SUPERSEDED></p>
    <facts ordered="false">
      <ARG-PRUNED fact="true" status="spec/done">Additionally, the size-footprint argument in §2.1 is pruned by [PROP-000 §15](../../common/PROP-000.xml#dep-weight) (dependency weight is not a decision factor).</ARG-PRUNED>
      <remaining-args fact="true" status="spec/done">The remaining arguments against `git2` (Windows SSH-auth lottery, diagnostic clarity of shell-out error messages) still carry the decision for M1 — but the argument tree is narrower now.</remaining-args>
      <revisit-narrowed fact="true" status="spec/done">Revisit when a concrete reason arises, e.g. programmatic object reads that shell-out can't do cheaply.</revisit-narrowed>
    </facts>
  </section>
  <motivation title="1. Motivation">
    <p><m0-state fact="true" status="impl/done">M0 shipped with a local-directory registry only.</m0-state></p>
    <p><M1-GOAL fact="true" status="impl/done">M1 makes the registry a git
repository hosted on GitVerse (default
`git@gitverse.ru:anarchic/vibespecs.git`). The implementation must:</M1-GOAL></p>
    <facts ordered="false">
      <REQ-CLONE-PULL fact="true" status="impl/done">Clone the registry into `~/.vibe/registries/&lt;hash&gt;/` on first use and
  `git pull` on subsequent use (`VIBEVM-SPEC.md` §8.3).</REQ-CLONE-PULL>
      <REQ-PRESERVE-LOCAL fact="true" status="impl/done">Preserve the existing `LocalRegistry` code path so tests and the
  `--registry &lt;path&gt;` override keep working.</REQ-PRESERVE-LOCAL>
      <REQ-SSH-AUTH fact="true" status="impl/done">Authenticate against GitVerse using the SSH identity the user has
  already configured (see [`spec/boot/90-user.xml`](../../boot/90-user.xml)).</REQ-SSH-AUTH>
      <REQ-CROSS-PLATFORM fact="true" status="impl/done">Run on Windows, macOS, and Linux with no per-platform build hoops.</REQ-CROSS-PLATFORM>
      <REQ-FOOTPRINT fact="true" status="impl/done">Carry its operational weight on constrained dev machines without
  bloating the `vibe` binary or adding a C toolchain requirement.</REQ-FOOTPRINT>
    </facts>
    <p><mechanics-in-readme fact="true" status="impl/done">This PROP records the architectural decisions. The mechanics
(`Registry` trait surface, error variants, wire-level command lines)
live in the crate's module documentation — `lib.rs` doc comments and the error
strings that cite `spec://` anchors — not in a README; `crates/vibe-registry`
has none.</mechanics-in-readme></p>
  </motivation>
  <decisions title="2. Decisions">
    <backend title="2.1 Backend: shell out to `git`, not `git2`">
      <p><SHELL-OUT fact="true" status="impl/done">**Decision:** `vibe-registry` performs all git operations by spawning the
system `git` binary via `std::process::Command`. We do **not** link
against `libgit2` (via the `git2` crate) in v1.</SHELL-OUT></p>
      <p><backend-why-lead fact="true" status="impl/done">**Why:**</backend-why-lead></p>
      <facts ordered="true">
        <WHY-SSH-WINDOWS fact="true" status="impl/done">**SSH on Windows is the killer.** GitVerse authenticates via SSH.
   Git for Windows ships OpenSSH and a working `ssh-agent`; the user's
   identity (`olegchir@UNIT-2040`) is already loaded and the push to
   `gitverse.ru` is proven (see [`spec/boot/90-user.xml`](../../boot/90-user.xml)).
   `libgit2` uses `libssh2` for SSH, which talks to `ssh-agent`
   through a named-pipe protocol that is fragile on Windows and
   routinely requires `SSH_AUTH_SOCK` juggling or explicit key paths.
   Cargo itself falls back to the system `git` on auth failure for
   this exact reason. Shell-out makes that fallback the primary path
   and retires the class of bug.</WHY-SSH-WINDOWS>
      </facts>
      <facts ordered="true">
        <WHY-FOOTPRINT fact="true" status="impl/done">**Dependency footprint.** `git2` pulls `libgit2-sys`,
   `libssh2-sys`, `libz-sys`, and `openssl-sys` (or a vendored
   alternative). Non-vendored builds demand a working C toolchain on
   every developer and CI machine; vendored builds add 3–8 MB to the
   release binary. Shell-out adds zero bytes and zero build-time
   native dependencies.</WHY-FOOTPRINT>
      </facts>
      <facts ordered="true">
        <WHY-DEBUGGABILITY fact="true" status="impl/done">**Feature parity and debuggability.** The user's `git` is by
   definition current. Errors surface with the full native message;
   `tracing` logs the exact argv so a user can re-run the failing
   command by hand. `libgit2`'s error strings (`ERROR class=Net (12):
   unexpected http status code: 401`) are harder to diagnose.</WHY-DEBUGGABILITY>
      </facts>
      <facts ordered="true">
        <WHY-NO-PROGRAMMATIC fact="true" status="impl/done">**We do not need programmatic git.** The v1 operations are
   `git clone`, `git fetch`, `git pull --ff-only`, and `git
   --version` for preflight. No partial clone, no in-memory object
   reads, no custom refspecs, no progress UI. Shell-out handles this
   trivially.</WHY-NO-PROGRAMMATIC>
      </facts>
      <facts ordered="true">
        <WHY-LICENSING fact="true" status="impl/done">**Licensing.** `git` is GPL v2, but shell-out is `exec` not
   linkage — the GNU FAQ explicitly separates these. Our binary stays
   unambiguously permissive.
   `libgit2` is GPL v2 with a Linking Exception (permissive for our
   purposes), but shell-out leaves the entire conversation at the
   door.</WHY-LICENSING>
      </facts>
      <p><risks-lead fact="true" status="impl/done">**Risks accepted:**</risks-lead></p>
      <facts ordered="false">
        <RISK-GIT-IN-PATH fact="true" status="impl/done">**Runtime dependency on `git` in `PATH`.** Acceptable: our target
  audience is developers who already have git installed. We perform a
  preflight `git --version` check and emit an actionable error (with
  a pointer to `https://git-scm.com/downloads`) if it is missing.</RISK-GIT-IN-PATH>
        <RISK-STDERR-PARSING fact="true" status="impl/done">**stderr parsing for fine-grained error classification.** We
  mitigate by running git with `LC_ALL=C` and keying off exit
  code + substring markers (`fatal: ` prefix, `Permission denied
  (publickey)`, `Repository not found`). See §2.7.</RISK-STDERR-PARSING>
      </facts>
      <p><revisit-lead fact="true" status="spec/done">**When to revisit:** if and when we need one of:</revisit-lead></p>
      <facts ordered="false">
        <revisit-sparse fact="true" status="spec/done">partial/sparse clone with custom filters,</revisit-sparse>
        <revisit-object-reads fact="true" status="spec/done">programmatic object reads (e.g. to fetch a `latest` marker file
  without a working-tree checkout),</revisit-object-reads>
        <revisit-credential-store fact="true" status="spec/done">OS-credential-store integration that can't be delegated to `git`,</revisit-credential-store>
        <revisit-bundling fact="true" status="spec/done">running on a platform where bundling `git` is easier than requiring
  it.</revisit-bundling>
      </facts>
      <p><REVISIT-PATH fact="true" status="spec/done">At that point, add a `libgit2` feature behind the `GitBackend` trait
(§2.2). The trait is designed so the switch costs one `impl` block
and one line in the factory, and nothing else in the codebase moves.</REVISIT-PATH></p>
    </backend>
    <backend-trait title="2.2 `GitBackend` trait">
      <p><GITBACKEND-TRAIT fact="true" status="impl/done">**Decision:** `vibe-registry::git_backend::GitBackend` is the single
interface through which the registry layer touches git. It has exactly
the operations we use:</GITBACKEND-TRAIT></p>
      <fence lang="rust">pub trait GitBackend: Send + Sync {
    /// Clone `url` (checked out at `refname`) into `dest`.
    /// Caller guarantees `dest` is either empty or absent.
    fn bootstrap(&amp;self, url: &amp;str, refname: &amp;str, dest: &amp;Path) -&gt; Result&lt;(), GitError&gt;;

    /// Fast-forward `dest` to the tip of `refname` on origin.
    /// No-op if already up to date.
    fn update(&amp;self, dest: &amp;Path, refname: &amp;str) -&gt; Result&lt;(), GitError&gt;;
}</fence>
      <p><METHOD-NAME-NOTE fact="true" status="impl/done">**Method-name note.** The "make a fresh clone" operation is called
`bootstrap` rather than the obvious `clone` or `clone_into` because
the backend is held as `Arc&lt;dyn GitBackend&gt;` at its call sites and
both of those names collide with blanket-impl methods from the
standard library (`std::clone::Clone::clone`,
`std::borrow::ToOwned::clone_into`), forcing ugly `&lt;T as
GitBackend&gt;::…` disambiguations at every call. `bootstrap` is
semantically accurate — it's how we initialise the registry cache
from empty state — and has no std-library namesake.</METHOD-NAME-NOTE></p>
      <p><WHY-NARROW fact="true" status="impl/done">**Why narrow.** The narrower the trait, the cheaper the backend swap.
If we need `ls_remote` or `fetch_ref` later, we add a method — that
addition is a visible, deliberate change, not a quiet interface drift.</WHY-NARROW></p>
      <p><implementations-lead fact="true" status="impl/done">**Implementations:**</implementations-lead></p>
      <facts ordered="false">
        <IMPL-SHELLGIT fact="true" status="impl/done">`ShellGit` — default, built from `std::process::Command`. See §2.7.</IMPL-SHELLGIT>
        <IMPL-LIBGIT2-RESERVED fact="true" status="spec/done">`LibGit2` — reserved. Not implemented in M1; the trait is the entry
  point for a future feature-gated addition.</IMPL-LIBGIT2-RESERVED>
      </facts>
      <p><NO-MOCK fact="true" status="impl/done">The `vibe-registry` crate does not expose a mock implementation.
Tests use `ShellGit` against a bare git repository created in a
`tempdir` — exercising the production code path end-to-end.</NO-MOCK></p>
    </backend-trait>
    <registry-trait title="2.3 `Registry` trait">
      <p><REGISTRY-TRAIT fact="true" status="spec/done">**Decision:** introduce a `vibe-registry::Registry` trait that both
`LocalRegistry` and `GitRegistry` implement:</REGISTRY-TRAIT></p>
      <fence lang="rust">pub trait Registry {
    fn list_versions(&amp;self, kind: PackageKind, name: &amp;str)
        -&gt; Result&lt;Vec&lt;semver::Version&gt;, RegistryError&gt;;
    fn resolve(&amp;self, pkgref: &amp;PackageRef)
        -&gt; Result&lt;ResolvedPackage, RegistryError&gt;;
    fn fetch(&amp;self, resolved: &amp;ResolvedPackage, cache_root: &amp;Path)
        -&gt; Result&lt;CachedPackage, RegistryError&gt;;
}</fence>
      <p><CONSUMERS-UNCHANGED fact="true" status="spec/done">`vibe-install` and `vibe-cli` continue to consume `ResolvedPackage` /
`CachedPackage` exactly as in M0; the only change is that the concrete
type is chosen at CLI-arg-parse time.</CONSUMERS-UNCHANGED></p>
      <p><SELECTION-RULE fact="true" status="impl/done">**Selection rule.** CLI precedence stays as defined in `VIBEVM-SPEC.md`
§9.1: `--registry &lt;path&gt;` (explicit, always a local directory) wins
over the `[registry]` section in `vibe.toml` (a URL — git or
`file://`).</SELECTION-RULE></p>
    </registry-trait>
    <cache-layout title="2.4 Cache layout">
      <p><CACHE-LAYOUT fact="true" status="spec/done">**Decision:** the on-disk layout under `~/.vibe/registries/` is:</CACHE-LAYOUT></p>
      <fence>~/.vibe/registries/
└── &lt;hash&gt;/
    ├── clone/        ← the git working tree
    └── meta.toml     ← { url, ref, last_pulled_at }</fence>
      <facts ordered="false">
        <LAYOUT-HASH fact="true" status="spec/done">`&lt;hash&gt;` = lowercase hex of the first 16 bytes of
  `sha256(normalized_url)`. 16 hex chars is enough to avoid realistic
  collisions while keeping the directory name tab-completable (same
  trick Cargo uses for its git cache). The full hash lives in
  `meta.toml` for audit.</LAYOUT-HASH>
        <LAYOUT-NORMALIZED-URL fact="true" status="spec/done">`normalized_url` strips a trailing `.git` and lowercases the
  scheme + host so `git@gitverse.ru:anarchic/vibespecs.git` and
  `ssh://git@gitverse.ru/anarchic/vibespecs` hash to the same
  registry.</LAYOUT-NORMALIZED-URL>
        <LAYOUT-META fact="true" status="spec/done">`meta.toml` is written after each successful clone or update. It
  carries the url (for debugging), the ref, and the UTC RFC3339
  timestamp of the last successful fetch.</LAYOUT-META>
        <LAYOUT-CLONE-DELEGATE fact="true" status="spec/done">The `clone/` subdirectory is the registry working tree. `GitRegistry`
  internally wraps a `LocalRegistry::new(clone_dir)` and delegates
  `resolve` / `list_versions` / `fetch` to it — the packaged layout
  (`&lt;kind&gt;/&lt;name&gt;/v&lt;ver&gt;/…`) is identical in both worlds.</LAYOUT-CLONE-DELEGATE>
      </facts>
      <p><PER-PROJECT-CACHE-UNCHANGED fact="true" status="spec/done">Per-project package cache (`&lt;project&gt;/.vibe/cache/&lt;kind&gt;/&lt;name&gt;/&lt;ver&gt;/`)
is unchanged from M0.</PER-PROJECT-CACHE-UNCHANGED></p>
    </cache-layout>
    <freshness title="2.5 Freshness policy">
      <p><FRESHNESS-TTL fact="true" status="impl/done">**Decision:** the default freshness TTL is **1 hour**, checked against
`meta.toml.last_pulled_at`. An install whose registry cache is older
than the TTL triggers an implicit `update`. An install whose cache is
younger skips the pull. `vibe registry sync` forces an update
regardless of age.</FRESHNESS-TTL></p>
      <p><ttl-why fact="true" status="spec/done">**Why 1 hour:** short enough to pick up new package versions within
one working session, long enough to amortise network round-trips over
a burst of installs. Revisit once real usage arrives.</ttl-why></p>
      <p><NO-OFFLINE-YET fact="true" status="impl/done">**Superseded — `--offline` shipped.** This recorded the M1 state: a
network failure during an implicit update failed the install with a clear
message. Offline resolution landed with [PROP-002 §2.2.2.1](PROP-002-decentralized-registry.xml)
(`url_is_local`) and the PROP-030 flag, and is live in `vibe install --help`.</NO-OFFLINE-YET></p>
    </freshness>
    <source-uri title="2.6 Lockfile `source_uri` format">
      <p><SOURCE-URI-FORMAT fact="true" status="spec/done">**Decision:** when a package originates from a git registry, the
lockfile records its source as</SOURCE-URI-FORMAT></p>
      <fence>git+ssh://git@gitverse.ru/anarchic/vibespecs.git#&lt;kind&gt;/&lt;name&gt;/v&lt;ver&gt;</fence>
      <p><fragment-note fact="true" status="spec/done">The `#fragment` names the package directory inside the registry
relative to the registry root. The scheme prefix (`git+ssh` /
`git+https` / `git+file`) encodes the transport. Local-directory
registries continue to produce `file://…` URIs as in M0.</fragment-note></p>
      <p><scheme-prefix-why fact="true" status="spec/done">**Why a scheme prefix.** `pip` and Cargo both use `git+…` prefixes to
disambiguate a git source from a plain URL; it reads obviously in
the lockfile.</scheme-prefix-why></p>
    </source-uri>
    <windows-ux title="2.7 Windows UX and stderr parsing">
      <p><NO-WINDOW-FLAG fact="true" status="impl/done">**Decision:** on Windows, every `git` subprocess is spawned with the
`CREATE_NO_WINDOW` creation flag (`0x08000000`) via
`std::os::windows::process::CommandExt::creation_flags`.</NO-WINDOW-FLAG></p>
      <p><no-window-why fact="true" status="impl/done">**Why.** If `vibe` ever runs inside a process without a console of its
own (a GUI launcher, IDE plugin, Windows service), a child with
`CREATE_CONSOLE` semantics would flash a separate black window. The
flag costs nothing in the console-attached case (stdio still
inherits), and covers the hypothetical hostless case for free.</no-window-why></p>
      <p><LOCALE-STDERR fact="true" status="impl/done">**Decision:** every `git` invocation runs with `LC_ALL=C` and
`LANG=C` in the environment so error strings are stable across user
locales. We key error classification off:</LOCALE-STDERR></p>
      <facts ordered="false">
        <CLASS-EXIT-CODE fact="true" status="impl/done">exit code (zero vs non-zero),</CLASS-EXIT-CODE>
        <CLASS-STDERR-SUBSTRINGS fact="true" status="impl/done">stderr substrings: `fatal: repository … not found`,
  `Permission denied (publickey)`, `Could not resolve host`,
  `Repository .* is empty`, `unable to access`.</CLASS-STDERR-SUBSTRINGS>
      </facts>
      <p><CATCH-ALL fact="true" status="impl/done">Anything unmatched is reported as a generic "git command failed"
with the raw stderr attached. Stable classification covers the
diagnoses we hand-hold the user through; the catch-all covers the
rest without hiding information.</CATCH-ALL></p>
    </windows-ux>
  </decisions>
  <rejected title="3. Rejected alternatives">
    <section title="3.1 `git2` crate as the primary backend">
      <p><REJ-GIT2 fact="true" status="spec/done">Rejected for M1. See §2.1. The decision is reversible via
`GitBackend` (§2.2).</REJ-GIT2></p>
    </section>
    <section title="3.2 Hybrid `git2` + shell-out fallback">
      <p><REJ-HYBRID fact="true" status="spec/done">Cargo does this. Rejected for v1 because it doubles the surface area
(two backends under one implementation), makes error messages
conditional on which path fired, and provides zero benefit on our
target matrix. Revisit only if we ever take the `libgit2` branch and
need auth fallback to system `git`.</REJ-HYBRID></p>
    </section>
    <section title="3.3 Sparse / partial clone in M1">
      <p><REJ-SPARSE fact="true" status="spec/done">Rejected: `vibespecs` is tiny. Optimisation is M2. The `GitBackend`
trait is narrow enough that adding a `clone_sparse` method later is a
one-line extension.</REJ-SPARSE></p>
    </section>
    <section title="3.4 Hosting the registry cache under the project">
      <p><REJ-PROJECT-CACHE fact="true" status="spec/done">Rejected: cache-per-project duplicates the same git clone across every
project on the same machine. `VIBEVM-SPEC.md` §8.3 already pins the
cache at `~/.vibe/registries/&lt;hash&gt;/` for this reason.</REJ-PROJECT-CACHE></p>
    </section>
    <section title="3.5 Vendoring `git` with the `vibe` binary">
      <p><REJ-VENDORING fact="true" status="spec/done">Rejected: vendoring a full git is the antithesis of "single Rust
binary". If we ever want zero runtime dependencies, the answer is
`libgit2`, not a bundled git.</REJ-VENDORING></p>
    </section>
  </rejected>
  <out-of-scope title="4. Out of scope for M1.1">
    <facts ordered="false">
      <OOS-HTTPS-AUTH fact="true" status="spec/done">Authentication for HTTPS registries with token / PAT
  (M2, PROP later).</OOS-HTTPS-AUTH>
      <OOS-PUBLISH fact="true" status="spec/done">`vibe publish` (`VIBEVM-SPEC.md` §8.4 pins this to v2+).</OOS-PUBLISH>
      <OOS-LLM-REVIEW fact="true" status="spec/done">LLM-based install review (`VIBEVM-SPEC.md` §8.5, M2).</OOS-LLM-REVIEW>
      <OOS-PROGRESS-UI fact="true" status="spec/done">Progress UI for long clones.</OOS-PROGRESS-UI>
      <OOS-MULTI-REGISTRY fact="true" status="spec/done">Multiple registries per project.</OOS-MULTI-REGISTRY>
      <OOS-OFFLINE fact="true" status="spec/done">`--offline` flag.</OOS-OFFLINE>
    </facts>
  </out-of-scope>
  <acceptance title="5. Acceptance (for M1.1 implementation)">
    <p><acceptance-lead fact="true" status="impl/done">Code-complete and live on 2026-04-22. Every box below ticks; the
milestone is shippable.</acceptance-lead></p>
    <facts ordered="false">
      <ACC-REGISTRY-TRAIT fact="true" status="impl/done">[x] `vibe-registry` exposes a `Registry` trait and two
  implementations (`LocalRegistry`, `GitRegistry`).</ACC-REGISTRY-TRAIT>
      <ACC-GITBACKEND fact="true" status="impl/done">[x] `GitBackend` trait + `ShellGit` implementation land in
  `vibe-registry::git_backend`.</ACC-GITBACKEND>
      <ACC-PREFLIGHT fact="true" status="impl/done">[x] `ShellGit` preflight (`git --version`) runs once per instance
  (cached via `OnceLock`) and emits `GitError::NotInstalled` with
  an actionable message if absent.</ACC-PREFLIGHT>
      <ACC-BOOTSTRAP-UPDATE fact="true" status="impl/done">[x] `ShellGit::bootstrap` and `ShellGit::update` succeed against
  a bare fixture repo in an integration test.</ACC-BOOTSTRAP-UPDATE>
      <ACC-CACHE-PATH fact="true" status="impl/done">[x] Cache lives at `~/.vibe/registries/&lt;hash&gt;/{clone,meta.toml}`.</ACC-CACHE-PATH>
      <ACC-META-TIMESTAMP fact="true" status="impl/done">[x] `meta.toml` gains a well-formed `last_pulled_at` after each
  fetch.</ACC-META-TIMESTAMP>
      <ACC-FRESHNESS fact="true" status="impl/done">[x] Freshness policy: ≤1h skips pull; &gt;1h pulls; `vibe registry
  sync` always pulls (TTL=0 uses `&gt;=` so same-second wallclock
  still triggers).</ACC-FRESHNESS>
      <ACC-E2E-INSTALL fact="true" status="impl/done">[x] End-to-end install against a `git+file://…` registry seeded
  with the canonical `flow:wal@0.1.0` fixture succeeds; the
  lockfile records a `git+…#flow/wal/v0.1.0` source URI.</ACC-E2E-INSTALL>
      <ACC-MANUAL-SMOKE fact="true" status="impl/done">[x] Manual smoke-test against the real
  `git@gitverse.ru:anarchic/vibespecs.git` (commit `98e51fc`)
  ran 2026-04-22 on Windows / Git Bash; every step matched the
  expected output, including the
  `git+ssh://git@gitverse.ru/anarchic/vibespecs.git#flow/wal/v0.1.0`
  lockfile source URI. Procedure and last-pass metadata live in
  [`manual-tests/M1.1-git-registry-smoke.md`](../../../manual-tests/M1.1-git-registry-smoke.md).</ACC-MANUAL-SMOKE>
      <ACC-SYNC-FORCE fact="true" status="impl/done">[x] `vibe registry sync` (no args) force-pulls the configured
  registry.</ACC-SYNC-FORCE>
      <ACC-NO-WINDOW fact="true" status="impl/done">[x] Windows: every spawned git carries `CREATE_NO_WINDOW`; no
  stray console windows from a hostless parent.</ACC-NO-WINDOW>
      <ACC-TESTS-GREEN fact="true" status="impl/done">[x] `cargo test --workspace` green (77 tests).</ACC-TESTS-GREEN>
      <ACC-CLIPPY fact="true" status="impl/done">[x] `cargo clippy --workspace --all-targets -- -D warnings` clean.</ACC-CLIPPY>
    </facts>
  </acceptance>
  <open-questions title="6. Open questions">
    <p><parking-lot-lead fact="true" status="spec/work">None blocking. Parking lot:</parking-lot-lead></p>
    <facts ordered="false">
      <OPEN-GIT-BINARY-PATH fact="true" status="impl/done">**Resolved — shipped as proposed.** The `VIBE_GIT_BINARY`
  PATH override lives in `git_backend/shell.rs` and its comment cites §6 of this
  PROP; the env-var form was chosen over a CLI flag exactly to keep the CLI
  surface stable.</OPEN-GIT-BINARY-PATH>
      <OPEN-CACHE-LOCK fact="true" status="spec/work">Does the registry cache need a lock file against concurrent `vibe`
  invocations? Probably yes for M2; a crash mid-clone leaves a
  half-populated `clone/`. For M1, document the behaviour ("if a
  clone fails, delete the cache dir and retry") rather than
  mechanising it.</OPEN-CACHE-LOCK>
    </facts>
  </open-questions>
</spec>
