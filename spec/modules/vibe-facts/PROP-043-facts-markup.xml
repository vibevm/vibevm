<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">PROP-043 — The facts markup: the inline status grammar and its IR</title>
  <status stage="impl" state="done" action="continue" actionstage="impl" comment="contract ratified 2026-07-24; SPLIT 2026-08-22 by the owner&apos;s facts/progress boundary ruling — this document is now the universal facts layer (grammar, placement, parsing, genres); the campaign toolchain moved verbatim to PROP-047 (modules/vibe-progress)"/>
  <p><self-uri fact="true" status="spec/done">`spec://org.vibevm.core/vibevm/modules/vibe-facts/PROP-043`</self-uri></p>
  <p><status-line fact="true" status="impl/done">**Status:** RATIFIED 2026-07-24 (owner, in session — «ратифицирую PROP-043»)
and **IMPLEMENTED**: the markup language below is binding. The tool surface,
the data contracts and the campaign consumer moved verbatim to PROP-047 at the
2026-08-22 boundary split (`##BOUNDARY-SPLIT`); their statuses live there.</status-line></p>
  <p><related fact="true" status="spec/done">**Depends on / relates to** (all as *external companions* — see the
separability law, §2): the `addressable-specs` flow (anchors, `spec://` URIs),
[PROP-035](../vibe-workspace/PROP-035-spec-compiler.xml) (the document IR whose
Markdown/XML dual-frontend model this markup targets),
[PROP-014 specmap](../../../vibedeps/org.vibevm.ai-native.core-ai-native/0.8.0/spec/mechanisms/PROP-014-specmap-bidirectional-traceability.md)
(spec↔code traceability — consumed by the progress layer through the
evidence-provider seam, PROP-047 §4),
[PROP-029](../../common/PROP-029-fully-qualified-addresses.xml) (`spec://`
grammar), and [PROP-047](../vibe-progress/PROP-047-progress-campaigns.xml) —
the campaign toolchain built on this grammar. The first consumer was the
spec-actualization campaign
(`spec/terraforms/SPEC-ACTUALIZATION-CAMPAIGN-v0.1.xml`).</related></p>
  <motivation title="1. Motivation">
    <facts ordered="false">
      <memory-accumulated fact="true" status="spec/done">The spec tree is the project's memory, and it has accumulated every stage of
  thought — shipped contracts, half-executed plans, parked ideas, and prose that
  no longer matches the code.</memory-accumulated>
      <status-lines-freeform fact="true" status="spec/done">Document-level `**Status:**` lines exist (~55 of
  them) but are free-form: fifteen vocabularies, unparseable, and silent below
  document granularity.</status-lines-freeform>
    </facts>
    <p><needs-lead fact="true" status="spec/done">The project needs (the tool and campaign-substrate needs live
in PROP-047 §1 since the boundary split):</needs-lead></p>
    <facts ordered="true">
      <NEED-MARKUP fact="true" status="impl/done">a **machine-readable markup** that records, inline in the sources, where
   every document / section / paragraph / text fragment stands;</NEED-MARKUP>
    </facts>
    <p><KNOWLEDGE-IN-SOURCES fact="true" status="impl/done">The durable knowledge lives **in the markup, in the sources**. Every other
artifact this PROP defines (cache, state projections, baselines) is derived
acceleration, erasable without loss of any fact (PROP-047 §5.5, the erasure law).</KNOWLEDGE-IN-SOURCES></p>
  </motivation>
  <separability title="2. The separability law">
    <p><SEPARABILITY-LAW fact="true" status="impl/done">The facts machinery is designed as a **standalone layer hosted inside
vibevm**, extractable at any moment:</SEPARABILITY-LAW></p>
    <facts ordered="false">
      <SEP-CORE fact="true" status="impl/done">The **core** (parser, model, rollup, renderers, cache) is its own crate with
  no dependency on vibe-core, vibe-spec, specmap, or any vibevm subsystem. Its
  input is a file tree plus a config; its output is the parsed model this
  document defines (the campaign data contracts consuming it are PROP-047 §5).</SEP-CORE>
      <SEP-NON-COLLISION fact="true" status="impl/done">Non-collision with neighbouring inline grammars (`@spec://`, `#use`,
  `#embed`, `#source`) is a **convention held by tests**, not shared code: the
  core ships fixtures containing those foreign directives and asserts zero
  false matches.</SEP-NON-COLLISION>
      <SEP-SELF-CONTAINED-SPECS fact="true" status="impl/done">The specs of this layer are self-contained: they cite neighbouring vibevm
  systems as external companions and never fold their content in (the campaign
  templates and the owner guide belong to the progress layer beside PROP-047).</SEP-SELF-CONTAINED-SPECS>
    </facts>
    <p><SEP-NEVER fact="true" status="impl/done">**Never** couple the core to a vibevm crate; never let a foreign subsystem
reach into the core's parsing; never split ONE layer's normative text across
documents. *Owner-revised 2026-08-22 (`##BOUNDARY-SPLIT`): the original
one-document form of this clause predates the boundary — the system is now TWO
layers with exactly one home each (the facts grammar here, the campaign
toolchain in PROP-047), and the no-split law binds within each home.*</SEP-NEVER></p>
    <p><BOUNDARY-SPLIT fact="true" status="spec/done">**The facts/progress boundary (owner ruling 2026-08-22, chat,
near-verbatim): «факты — то, поверх чего можно построить совершенно разные
процессы рефакторингов; синтаксис, IR и операции над фактами — в модуль facts,
и это можно показывать сразу; в progress остаются наши инструменты для
рефакторингов vibevm, доделываемые до зрелости».** The law: this document owns
the universal lower layer — grammar (§3), parsing (§4), genre semantics (§5) —
plus the adoption registry beside it (PROP-046); PROP-047 owns the upper layer
— tool, config, evidence, campaign data contracts, maintenance discipline. The
dependency points strictly upward (progress knows facts; facts never knows
progress), and every unit of both documents kept its anchor through the split —
only the doc-paths changed, with the old path carrying a tombstone pointer.</BOUNDARY-SPLIT></p>
    <p><BOUNDARY-CLI fact="true" status="impl/done">**The CLI follows the boundary.** The markup lint is a facts
operation: `vibe facts check [--exhaustive]` becomes its durable home, with
`vibe progress check` kept as a transitional alias (printing the new spelling);
the gate panel switches to the facts spelling. Campaign verbs
(`scan`/`mirror`/`seal`/`gate`/`baseline`/`rescan`/`resume`/`weave`/`report`)
stay under `vibe progress`. The same wave repairs B-100: a bare `--campaign
&lt;id&gt;` resolves against `campaigns/&lt;id&gt;` instead of silently minting a
cwd-relative state zone. Landed: `vibe facts check` carries the lint
byte-identically (one implementation, two entries), the alias prints its
stderr note (suppressed under --json), the shared campaign resolver fails
loud on an unknown bare id with the existing zones listed and writes
nothing, and the gate panel's markup line runs the facts spelling —
proven live on the original B-100 scenario.</BOUNDARY-CLI></p>
    <p><BOUNDARY-CRATES fact="true" status="spec/done">**Crate names lag the boundary deliberately.** `progress-core`
remains the crate name of the facts core for now — renaming it is a mechanical
ripple (engine copies, sync-engines) with no consumer until the progress
layer's own maturation wave, which is when it happens. The spec boundary and
the CLI boundary are the ones the public sees.</BOUNDARY-CRATES></p>
  </separability>
  <markup title="3. The markup language">
    <element title="3.1 The `&lt;status&gt;` element">
      <p><STATUS-ELEMENT fact="true" status="impl/done">One XML-shaped element, embedded in Markdown (and, later, native in XML
documents — the frontend duality of PROP-035 §5):</STATUS-ELEMENT></p>
      <fence>&lt;status stage="impl" state="work"/&gt;                       — point marker
&lt;status stage="test" state="plan"&gt;wrapped text&lt;/status&gt;   — fragment wrapper</fence>
      <facts ordered="false">
        <POINT-SELF-CLOSING fact="true" status="impl/done">A **point marker** MUST be self-closing (`/&gt;`). An unclosed `&lt;status …&gt;`
  point form is not well-formed XML and is a `check` error.</POINT-SELF-CLOSING>
        <FRAGMENT-WRAPPER fact="true" status="impl/done">A **fragment wrapper** is the paired form around text inside a paragraph.</FRAGMENT-WRAPPER>
        <FENCE-AWARE fact="true" status="impl/done">Inside fenced code blocks, inline code spans, and URLs the element and the
  shorthand (§3.7) are **not recognized** — the scanner is fence-aware.</FENCE-AWARE>
      </facts>
      <p><DECISION-ELEMENT-NAME fact="true" status="impl/done">**Decision — element name `status`, not `progress`.**</DECISION-ELEMENT-NAME></p>
      <facts ordered="false">
        <element-name-why fact="true" status="spec/done">**Why:** `&lt;progress&gt;` is an HTML5 element: GitHub-class sanitizers strip it,
  and `html:true` renderers (VS Code preview) draw a literal progress-bar widget
  mid-spec. `status` is not an HTML element and renders inert.</element-name-why>
        <element-name-rejected fact="true" status="spec/done">**Considered and rejected:** `progress` (HTML collision), `vp`/`prg`
  (unreadable), HTML comments (invisible in raw reading, defeating the point).</element-name-rejected>
        <element-name-revisit fact="true" status="spec/done">**Revisit when:** the XML storage frontend lands and element naming is
  re-grounded in a schema.</element-name-revisit>
      </facts>
    </element>
    <attributes title="3.2 Attributes">
      <table>
        <tr>
          <td>Attribute</td>
          <td>Required</td>
          <td>Values</td>
        </tr>
        <tr>
          <td><ROW-ATTR-STAGE fact="true" status="impl/done">`stage`</ROW-ATTR-STAGE></td>
          <td><ROW-ATTR-STAGE-REQUIRED fact="true" status="impl/done">yes</ROW-ATTR-STAGE-REQUIRED></td>
          <td><ROW-ATTR-STAGE-VALUES fact="true" status="impl/done">`idea` · `spec` · `impl` · `test` · `doc` · `freeze` · `unknown`</ROW-ATTR-STAGE-VALUES></td>
        </tr>
        <tr>
          <td><ROW-ATTR-STATE fact="true" status="impl/done">`state`</ROW-ATTR-STATE></td>
          <td><ROW-ATTR-STATE-REQUIRED fact="true" status="impl/done">yes</ROW-ATTR-STATE-REQUIRED></td>
          <td><ROW-ATTR-STATE-VALUES fact="true" status="impl/done">`plan` · `work` · `done` · `hold` · `void`</ROW-ATTR-STATE-VALUES></td>
        </tr>
        <tr>
          <td><ROW-ATTR-ACTION fact="true" status="impl/done">`action`</ROW-ATTR-ACTION></td>
          <td><ROW-ATTR-ACTION-REQUIRED fact="true" status="impl/done">no</ROW-ATTR-ACTION-REQUIRED></td>
          <td><ROW-ATTR-ACTION-VALUES fact="true" status="impl/done">`continue` · `drift` · `rework` · `remove`</ROW-ATTR-ACTION-VALUES></td>
        </tr>
        <tr>
          <td><ROW-ATTR-ACTIONSTAGE fact="true" status="impl/done">`actionstage`</ROW-ATTR-ACTIONSTAGE></td>
          <td><ROW-ATTR-ACTIONSTAGE-REQUIRED fact="true" status="impl/done">no</ROW-ATTR-ACTIONSTAGE-REQUIRED></td>
          <td><ROW-ATTR-ACTIONSTAGE-VALUES fact="true" status="impl/done">any `stage` value; absent ⇒ the action targets `stage`</ROW-ATTR-ACTIONSTAGE-VALUES></td>
        </tr>
        <tr>
          <td><ROW-ATTR-AUDIENCE fact="true" status="impl/done">`audience`</ROW-ATTR-AUDIENCE></td>
          <td><ROW-ATTR-AUDIENCE-REQUIRED fact="true" status="impl/done">no</ROW-ATTR-AUDIENCE-REQUIRED></td>
          <td><ROW-ATTR-AUDIENCE-VALUES fact="true" status="impl/done">CSV of `user` · `author` · `dev`; absent ⇒ `dev`</ROW-ATTR-AUDIENCE-VALUES></td>
        </tr>
        <tr>
          <td><ROW-ATTR-COMMENT fact="true" status="impl/done">`comment`</ROW-ATTR-COMMENT></td>
          <td><ROW-ATTR-COMMENT-REQUIRED fact="true" status="impl/done">no</ROW-ATTR-COMMENT-REQUIRED></td>
          <td><ROW-ATTR-COMMENT-VALUES fact="true" status="impl/done">free text</ROW-ATTR-COMMENT-VALUES></td>
        </tr>
        <tr>
          <td><ROW-ATTR-REF fact="true" status="impl/done">`ref`</ROW-ATTR-REF></td>
          <td><ROW-ATTR-REF-REQUIRED fact="true" status="impl/done">no</ROW-ATTR-REF-REQUIRED></td>
          <td><ROW-ATTR-REF-VALUES fact="true" status="impl/done">`spec://…` URI, path, or task id (e.g. `DRIFT-012`)</ROW-ATTR-REF-VALUES></td>
        </tr>
      </table>
      <facts ordered="false">
        <VOCAB-CLOSED fact="true" status="impl/done">Vocabularies are **closed**. Any value outside the tables is a `check` error
  with a nearest-legal-value hint (typos like `rewrok` die in CI, not in
  review).</VOCAB-CLOSED>
        <VOCAB-AMENDMENT-ONLY fact="true" status="impl/done">New values enter only by amendment to this section.</VOCAB-AMENDMENT-ONLY>
      </facts>
      <p><MULTI-MARKERS fact="true" status="impl/done">Multiple markers on one node: at most **one** status marker (stage/state), any
number of **action** markers — a unit may legitimately need
`remove`+`actionstage="doc"` and `continue`+`actionstage="test"` at once.</MULTI-MARKERS></p>
    </attributes>
    <stages title="3.3 Stages">
      <facts ordered="false">
        <STAGE-IDEA fact="true" status="impl/done">`idea` — a thought worth keeping, not yet specified.</STAGE-IDEA>
        <STAGE-SPEC fact="true" status="impl/done">`spec` — being specified / specified.</STAGE-SPEC>
        <STAGE-IMPL fact="true" status="impl/done">`impl` — being implemented / implemented.</STAGE-IMPL>
        <STAGE-TEST fact="true" status="impl/done">`test` — being tested / tested.</STAGE-TEST>
        <STAGE-DOC fact="true" status="impl/done">`doc` — being documented / documented.</STAGE-DOC>
        <STAGE-FREEZE fact="true" status="impl/done">`freeze` — freezing as a deliberate process: `freeze/plan` (we intend to
  freeze), `freeze/work` (final checks), `freeze/done` (frozen).</STAGE-FREEZE>
        <STAGE-UNKNOWN fact="true" status="impl/done">`unknown` — looked at, not understood; explicit triage demand. Distinct from
  *no marker* ("nobody looked yet").</STAGE-UNKNOWN>
      </facts>
      <p><DECISION-FREEZE-STAGE fact="true" status="impl/done">**Decision — `freeze`, not a terminal `done` stage.**</DECISION-FREEZE-STAGE></p>
      <facts ordered="false">
        <freeze-why fact="true" status="spec/done">**Why:** freezing is a process (planned, executed, and later reversed —
  unfreeze is an ordinary marker change back to `spec`/`idea`/`impl`; history
  lives in git). A `done` stage collided semantically with the `done` state.</freeze-why>
        <freeze-rejected fact="true" status="spec/done">**Considered and rejected:** `stage="done"` (ambiguous against
  `state="done"`); a boolean `frozen` flag (hides the process).</freeze-rejected>
        <freeze-revisit fact="true" status="spec/done">**Revisit when:** never expected; the cycle-of-improvement premise is core.</freeze-revisit>
      </facts>
      <facts ordered="false">
        <STAGE-ORDER fact="true" status="impl/done">**The stage order is fixed for aggregation:**
  `idea &lt; spec &lt; impl &lt; test &lt; doc &lt; freeze`; `unknown` sits outside the order
  and compares below everything.</STAGE-ORDER>
        <ORDER-NOT-PROCESS fact="true" status="impl/done">The order is a **sort key for rollup**, not a
  process law: test and doc interleave in reality, and falling back (impl →
  spec, spec → idea) is legal and expressed by simply changing the marker.</ORDER-NOT-PROCESS>
      </facts>
    </stages>
    <states title="3.4 States">
      <facts ordered="false">
        <STATE-PLAN fact="true" status="impl/done">`plan` — intended, not started.</STATE-PLAN>
        <STATE-WORK fact="true" status="impl/done">`work` — in progress.</STATE-WORK>
        <STATE-DONE fact="true" status="impl/done">`done` — done for that
  stage.</STATE-DONE>
        <STATE-HOLD fact="true" status="impl/done">`hold` — deliberately parked (neither worked nor discarded).</STATE-HOLD>
        <STATE-VOID fact="true" status="impl/done">`void` — the unit no longer asserts anything. Named for a **void
  contract** — without effect — deliberately not the programming sense of
  "still works, discouraged". The unit was either split into heirs and left as
  a pointer to them, or cancelled with no replacement; its text survives only so
  its name is not reused and inbound links do not break. It is neither work
  outstanding nor work completed but **no claim at all**, and §3.10 sorts it
  accordingly. Marking one is the author's judgment about their own corpus —
  nothing derives it.</STATE-VOID>
      </facts>
    </states>
    <actions title="3.5 Actions">
      <facts ordered="false">
        <ACTION-CONTINUE fact="true" status="impl/done">`continue` — unfinished; carry on (spawns tasks in the campaign).</ACTION-CONTINUE>
        <ACTION-DRIFT fact="true" status="impl/done">`drift` — diverged from reality; reconcile. Operationally bound to the
  **sync-from-code** flow when the code is right and the spec is stale: the
  marker names the problem, that flow is the procedure.</ACTION-DRIFT>
        <ACTION-REWORK fact="true" status="impl/done">`rework` — exists but bad; redo (pairs with the feature-flag disable path).</ACTION-REWORK>
        <ACTION-REMOVE fact="true" status="impl/done">`remove` — bad or abandoned; delete (or demote to `idea`/`hold` archive).</ACTION-REMOVE>
      </facts>
      <p><ACTIONSTAGE-NARROWS fact="true" status="impl/done">`actionstage` narrows the target: `action="remove" actionstage="doc"` = "the
documentation of this is to be removed", while `stage` keeps describing the
unit itself.</ACTIONSTAGE-NARROWS></p>
    </actions>
    <audience title="3.6 Audience">
      <facts ordered="false">
        <AUDIENCE-VALUES fact="true" status="impl/done">For whom this promise must eventually be told: `user` (writes specs in their
  own project, installs dependencies; never opens the package internals),
  `author` (builds packages; wants depth), `dev` (vibevm's own developers — the
  spec tree itself serves them; the default).</AUDIENCE-VALUES>
        <AUDIENCE-DOC-USE fact="true" status="spec/done">Primary use: `actionstage="doc"`
  markers feed the two guides' tables of contents
  (`vibe progress report --view doc --audience user|author`).</AUDIENCE-DOC-USE>
      </facts>
    </audience>
    <shorthand title="3.7 Shorthand">
      <p><SHORTHAND-FORMS fact="true" status="impl/done">`@status:&lt;stage&gt;/&lt;state&gt;` and `@status:&lt;stage&gt;` are macro-equivalents of a
point marker. The **legacy** spellings `@&lt;stage&gt;/&lt;state&gt;` and `@&lt;stage&gt;` mean
exactly the same and are still read, so a document written before the
qualified form keeps parsing.</SHORTHAND-FORMS></p>
      <facts ordered="false">
        <SHORTHAND-FULL fact="true" status="impl/done">`@status:test/plan` ⇒ `&lt;status stage="test" state="plan"/&gt;`</SHORTHAND-FULL>
        <SHORTHAND-BARE fact="true" status="impl/done">`@status:impl` ⇒ `&lt;status stage="impl" state="work"/&gt;` — bare shorthand defaults to
  `state="work"`, with exactly one exception: `@status:unknown` ⇒ `state="hold"`.
  (`@status:freeze` ⇒ `freeze/work`: "freezing now".)</SHORTHAND-BARE>
      </facts>
      <facts ordered="false">
        <SHORTHAND-NAMES-ITS-KEY fact="true" status="impl/done">**Why the key is named.** These documents share the `@` space with
  foreign annotation grammars — JSDoc, TypeScript directives, npm scopes,
  Java annotations, and plain `x@y` addresses. Without a key the reader must
  tell its own tokens from those by a vocabulary of stages and states, which
  answers "is this mine?" with a dictionary lookup instead of with the token's
  own shape.</SHORTHAND-NAMES-ITS-KEY>
        <SHORTHAND-DISAMBIGUATION fact="true" status="impl/done">**Disambiguation against the `@spec://` directive grammar** (in-place spec
  citations) applies to the **legacy** form only: after `@spec` the scanner
  looks ahead — `://` follows ⇒ foreign directive, not ours; `/&lt;state&gt;`,
  whitespace, or end-of-token follows ⇒ shorthand. The qualified form needs no
  lookahead: `@status:spec/done` cannot be confused with `@spec://…`, because
  the key is stated before the value.</SHORTHAND-DISAMBIGUATION>
        <SHORTHAND-STANDALONE fact="true" status="impl/done">A shorthand is recognized only as a standalone token at the start
or end of a paragraph's text, never mid-sentence, never inside code or links.</SHORTHAND-STANDALONE>
      </facts>
    </shorthand>
    <placement title="3.8 Placement">
      <p><placement-lead fact="true" status="impl/done">Six granularities, one rule each — and no ambiguous positions:</placement-lead></p>
      <facts ordered="true">
        <PLACE-DOCUMENT fact="true" status="impl/done">**Document** — marker in the preamble, before the first heading.
   *Pilot amendment (2026-07-24):* a document that **opens with its
   heading** (the standard shape of this repo's specs) has no preamble;
   there, the standalone marker immediately after that first heading is
   the **document** marker, not a section marker.</PLACE-DOCUMENT>
        <PLACE-SECTION fact="true" status="impl/done">**Section** — marker on its own line **immediately after the heading
   line** (for any heading other than a preamble-less file's first one,
   per the amendment above). This is the only legal standalone position
   inside a body.</PLACE-SECTION>
        <PLACE-PARAGRAPH fact="true" status="impl/done">**Paragraph** — marker **inside the paragraph's own text**: the first
   token (right after the newlines, or right after the paragraph's `@fact:&lt;ID&gt;`
   anchor) or the last token (right before them).</PLACE-PARAGRAPH>
        <PLACE-LIST-ITEM fact="true" status="impl/done">**List item** *(fact amendment, 2026-07-24 — owner-directed)* — every
   item of a bulleted or numbered list is a **unit of its own**, at every
   nesting level. Its marker — shorthand or XML form alike — sits **inside
   the item's own text**: the first or last token of the item (before any
   nested sub-items, which carry their own markers).</PLACE-LIST-ITEM>
      </facts>
      <facts ordered="false">
        <FACT-ANCHOR-SYNTAX fact="true" status="impl/done">**Fact anchors — the anchored-when-marked law** *(owner, 2026-07-24;
     spelling amended 2026-08-06)*. A stable fact address is written
     `@fact:&lt;ID&gt;` as the **first token** of a paragraph or list item. The
     **legacy** spelling `##&lt;ID&gt;` means the same and is still read.</FACT-ANCHOR-SYNTAX>
        <FACT-ANCHOR-NAMES-ITS-KEY fact="true" status="impl/done">**Why the key is named.** `##&lt;ID&gt;` was never a heading — an ATX
     heading requires a space after the hashes, and this markup is written
     closed up — but it reads like one to a markdown linter, to a parser
     outside CommonMark, and to the eye. `@fact:` states what the token is
     instead of relying on a reader knowing what it is not.</FACT-ANCHOR-NAMES-ITS-KEY>
        <FENCE-IS-AN-EXAMPLE-UNTIL-MARKED fact="true" status="impl/done">**A fenced block is an example, not an assertion** *(owner,
     2026-08-06)*. By default nobody is asked to believe what a fence says
     and no agent is asked to run it. Marking the fact `@fact/code:&lt;ID&gt;`
     makes the fence **part of that fact's body**: the fact then has an
     address, a verdict, and it comes due for re-judgement when the block's
     text moves.</FENCE-IS-AN-EXAMPLE-UNTIL-MARKED>
        <WHY-A-FENCE-NEEDED-THIS fact="true" status="impl/done">**Why the type exists.** A fence carries no anchor of its own and
     cannot be given one — it is a payload, copied out and pasted elsewhere,
     and an anchor written inside would travel with the copy. Measured over
     this corpus: **372 fenced blocks carry zero facts** while all 7255 text
     blocks carry theirs, so a claim inside a fence belonged to nobody, could
     not be judged, and could not be made stale. Two false statements
     survived exactly that way in one week.</WHY-A-FENCE-NEEDED-THIS>
        <ONE-OBJECT-TYPE-IS-IMPLEMENTED fact="true" status="impl/done">**The known type set is `code`, and that is a measurement.**
     Fences are the only block kind falling outside fact bodies: the corpus
     holds no images at all, and 891 of 908 table rows and 84 of 96 block
     quotes already sit inside a fact. A type naming a block kind that is
     already covered would address nothing.</ONE-OBJECT-TYPE-IS-IMPLEMENTED>
        <UNKNOWN-OBJECT-TYPE-IS-AN-ERROR fact="true" status="impl/done">**An unknown type is a `check` error**, as are a typed anchor
     that is not its block's last fact and one with no matching block below
     it. Ignoring an unimplemented type would let the grammar promise what it
     cannot do, and the author would learn years later that nothing read it.</UNKNOWN-OBJECT-TYPE-IS-AN-ERROR>
        <FACT-ID-GRAMMAR fact="true" status="impl/done">`&lt;ID&gt;` is
     `[A-Za-z][A-Za-z0-9_-]*`; the unit is then addressable as
     `spec://…/&lt;doc&gt;#&lt;ID&gt;`, sharing one address space with the heading
     `{#anchor}`s — a duplicate across both forms is a `check` error. The
     **address is unchanged by the spelling**: it names the id, never the
     opener.</FACT-ID-GRAMMAR>
        <ANCHORED-WHEN-MARKED fact="true" status="impl/done">**Every unit that carries a status marker — paragraph or list item —
     MUST also carry a `@fact:&lt;ID&gt;` anchor**; a marked, anchor-less unit is a
     `check` error.</ANCHORED-WHEN-MARKED>
        <ANCHOR-MARKER-POSITIONS fact="true" status="impl/done">The marker may stand immediately after the anchor
     (`1. ##RULE-001 rule text @freeze/done` — the owner's canonical
     example shape) or as the unit's last token.</ANCHOR-MARKER-POSITIONS>
      </facts>
      <facts ordered="false">
        <DECISION-TWO-REGISTERS fact="true" status="impl/done">**Decision — two anchor-id registers (owner ruling, 2026-07-24).**
     `@fact:UPPER-SLUG` names a **normative fact** (a law, rule, carrier,
     changelog entry — content with binding weight); `@fact:kebab-case`
     names a **service unit** (status lines, lead-ins, connective
     prose).</DECISION-TWO-REGISTERS>
        <registers-why fact="true" status="spec/done">**Why:** the register itself carries the normativity
     signal at zero syntax cost; ratified from the PROP-029 re-pilot
     mix reviewed in session.</registers-why>
        <registers-rejected fact="true" status="spec/done">**Considered and rejected:** single UPPER
     register (service units become shouty; the signal is lost); single
     kebab register (normative facts stop standing out; the re-pilot
     would need re-anchoring).</registers-rejected>
        <registers-revisit fact="true" status="spec/done">**Revisit when:** the post-campaign fold
     (§3.9) shows the mixed registers confusing report consumers or
     check tooling.</registers-revisit>
      </facts>
      <facts ordered="false">
        <TABLE-ADDRESSING fact="true" status="impl/done">**Table addressing** *(proposed 2026-07-24, this session — same
     syntax, no new grammar)*. A `@fact:&lt;ID&gt;` as the first token of the
     **first cell of a body row** addresses that **row**
     (`| ##ROW-PKGREF pkgref | … |`); in **any other cell** it addresses
     that **cell**; the **whole table** is addressed by the anchor of its
     lead paragraph (`the target set: ##TBL-MIRRORS`) or its section.</TABLE-ADDRESSING>
        <table-positional-rejected fact="true" status="spec/done">Positional schemes (`r2c3`) are rejected — a row shuffle breaks
     them; a minted id travels with its row.</table-positional-rejected>
        <CELLS-ANCHOR-EXEMPT fact="true" status="impl/done">Cells stay **exempt from the
     anchored-when-marked obligation** (mint ids only where something
     cites them); a table that is really a list of facts is deconstructed
     into an anchored list instead.</CELLS-ANCHOR-EXEMPT>
        <PLACE-TABLE-CELL fact="true" status="impl/done">**Table cell** *(fact amendment, 2026-07-24)* — every non-empty body
   cell of a table is a **unit of its own**; its marker sits inside the
   cell's text, first or last token. Header rows and the delimiter row are
   structure, not units. A table whose rows are really a list of facts is
   better deconstructed into a list (§3.9); cell markup is for tables whose
   tabular shape is essential.</PLACE-TABLE-CELL>
        <PLACE-FRAGMENT fact="true" status="impl/done">**Fragment** — paired `&lt;status&gt;…&lt;/status&gt;` around text within a
   paragraph, list item, or cell — the form for an **inline fact** that
   cannot be pulled out of its sentence.</PLACE-FRAGMENT>
      </facts>
      <facts ordered="false">
        <NO-ORPHAN-MARKER fact="true" status="impl/done">A standalone marker between two paragraphs is a **`check` error** — there is
  no "nearest paragraph" heuristic.</NO-ORPHAN-MARKER>
        <SECTION-SPAN fact="true" status="impl/done">A section's span follows the owner-fixed
  IR rule (PROP-035 §5): from its heading to the next heading of the same or
  higher level.</SECTION-SPAN>
      </facts>
    </placement>
    <granularity title="3.9 The granularity doctrine — facts are the campaign grain">
      <facts ordered="false">
        <MAINTENANCE-GRAIN fact="true" status="impl/done">**Anchored units are the maintenance granularity.** Between campaigns,
  markup lives on `{#anchor}`-ed units; reports cite `spec://…#anchor`; this
  is the stable, refactor-proof form.</MAINTENANCE-GRAIN>
        <CAMPAIGN-GRAIN fact="true" status="impl/done">**Facts are the campaign granularity** *(fact amendment, 2026-07-24 —
  owner-directed; supersedes the paragraph grain of the original text)*. An
  actualization campaign demands verbatim exhaustiveness at the grain of
  individual **facts**, because a paragraph routinely carries several and an
  LLM pass silently skips the inner ones. Operationally:</CAMPAIGN-GRAIN>
        <COUNTABLE-UNITS fact="true" status="impl/done">every paragraph, list item, and non-empty table body cell carries its
    own marker — these are the **countable units** the exhaustive counter
    enforces;</COUNTABLE-UNITS>
        <DECONSTRUCTION-LAW fact="true" status="impl/done">a paragraph that carries **more than one fact is deconstructed** —
    rewritten, sense-preserving and wording-preserving, into a bulleted or
    numbered list with one fact per item, each item marked. Most prose is
    expected to become lists; a paragraph stays prose only when it truly
    carries one fact (or none — connective tissue);</DECONSTRUCTION-LAW>
        <INLINE-FRAGMENT fact="true" status="impl/done">a fact that cannot leave its sentence (an inline clause, an enumeration
    inside one sentence that resists splitting) is wrapped as a
    **fragment**: `&lt;status …&gt;the fact&lt;/status&gt;`;</INLINE-FRAGMENT>
        <FORM-ONLY fact="true" status="impl/done">campaign passes still add missing `{#anchor}`s; deconstruction changes
    *form only* — semantic edits belong to the drift-correction stage,
    never to markup passes.</FORM-ONLY>
        <POST-CAMPAIGN-FOLD fact="true" status="spec/done">After a campaign, density is folded back: a section whose units agree
  collapses to one unit marker (`check` verifies the fold is lossless);
  mixed sections stay fact-marked.</POST-CAMPAIGN-FOLD>
      </facts>
    </granularity>
    <rollup title="3.10 Inheritance and rollup">
      <facts ordered="false">
        <ROLLUP-DOWNWARD fact="true" status="impl/done">**Downward (defaulting):** a node's marker covers unmarked descendants.</ROLLUP-DOWNWARD>
        <ROLLUP-UPWARD fact="true" status="impl/done">**Upward (aggregation):** an unmarked node's computed status is the
  worst-of its children per the §3.3 order (`unknown` wins the bottom).</ROLLUP-UPWARD>
        <ROLLUP-VOID-OUTSIDE fact="true" status="impl/done">`void` is the one value outside the `(stage, state)` order:
  it sorts **above every other pair regardless of stage**, so worst-of never
  returns it while any live unit remains. `worst-of {spec/void, impl/plan}` is
  `impl/plan` — the live part governs and the tombstone's stage does not drag
  the document back to `spec`; `worst-of {done, void}` is `done`. A document
  whose every unit is `void` **is** `void`, which falls out of the same rule
  rather than being special-cased. This is a property of the **pair**: giving
  `void` the top state slot *within* its stage would leave `@spec/void`
  governing by stage anyway, which is why two of the three options originally
  proposed for it could not work.</ROLLUP-VOID-OUTSIDE>
        <EXPLICIT-BEATS fact="true" status="impl/done">An explicit marker always beats both directions. Reports show *explicit*
  and *computed* separately — a divergence is information, not noise.</EXPLICIT-BEATS>
      </facts>
    </rollup>
  </markup>
  <parsing title="4. Parsing rules">
    <facts ordered="false">
      <PARSE-MARKDOWN-FIRST fact="true" status="impl/done">Markdown frontend first: the scanner operates on the document tree
  (headings → units per the PROP-035 §5 body-span rule), then recognizes
  `&lt;status&gt;` elements and shorthand in text nodes only — never inside fenced
  code, inline code, or link targets.</PARSE-MARKDOWN-FIRST>
      <PARSE-COUNTABLE fact="true" status="impl/done">**Countable units** *(fact amendment, 2026-07-24)*: inside a text block
  the scanner recognizes list items (`-` / `*` / `+` and `N.` / `N)`
  lines, with their indented continuation lines, at every nesting level)
  and table rows (`|`-delimited); the units the exhaustive counter walks
  are: plain paragraphs, the lead lines of a block before its first list
  item, each list item, and each non-empty body cell of a table. Header
  and delimiter rows of a table are structure. A marker counts for the
  unit whose text carries it (first/last token of that unit, or a
  fragment wrapper inside it).</PARSE-COUNTABLE>
      <PARSE-FACT-ANCHORS fact="true" status="impl/done">**Fact anchors** *(fact amendment, 2026-07-24)*: a `@fact:&lt;ID&gt;` first token
  of a paragraph or list item is that unit's anchor, recorded alongside
  the heading anchors; the scanner enforces the anchored-when-marked law
  (§3.8) — a marked unit with no anchor, and a duplicate id, are `check`
  errors. An opener inside code spans/fences is opaque, as all markup is.</PARSE-FACT-ANCHORS>
      <PARSE-TYPED-FACT-CODE fact="true" status="impl/done">**Typed fact — the fence joins the body** *(owner ruling variant D
  2026-08-06; built 2026-08-20, B-068)*: `@fact/code:&lt;ID&gt;` is a fact
  definition in the same position and id namespace as `@fact:&lt;ID&gt;`, whose
  body is its own unit **plus the first fenced block after it** (blank
  lines between are fine; any other block breaks the adjacency and is a
  parse error, as is a typed fact that is not the last fact of its text
  block). The attached fence enters the fact's content hash — editing the
  fence stales the fact — while staying opaque to marker/anchor scans, as
  all fences are. `code` is the one implemented type: an unknown type
  (`@fact/&lt;t&gt;:`) is a parse error naming `&lt;t&gt;`, never a silent skip — a
  grammar must not promise what it cannot check. By default a fence
  remains an example belonging to no fact; the typed form is the opt-in
  that turns it into a judgeable assertion.</PARSE-TYPED-FACT-CODE>
      <PARSE-XML-GRAMMAR fact="true" status="impl/done">The element grammar is XML: attributes quoted, point markers self-closed.
  A future XML storage frontend consumes the same attribute schema natively;
  the markup language does not change.</PARSE-XML-GRAMMAR>
      <PARSE-FOREIGN-OPAQUE fact="true" status="impl/done">Foreign inline grammars (`@spec://`, `#use`, `#embed`, `#source`,
  `&lt;!-- REVIEW: … --&gt;`) are opaque text to this scanner (§2).</PARSE-FOREIGN-OPAQUE>
    </facts>
  </parsing>
  <genres title="5. Genre semantics">
    <facts ordered="false">
      <EVERY-GENRE-IN-SCOPE fact="true" status="impl/done">**Every genre is in scope** — contracts, design docs, research, plans, manual
  tests: a design decision the code ignores is first-class drift.</EVERY-GENRE-IN-SCOPE>
      <GENRE-TERMINALS fact="true" status="impl/done">Genre only
  changes what the terminal looks like: a contract unit ends at `freeze/done`
  with evidence; a research doc's "implementation" is its downstream deltas; a
  campaign plan's own status line converts to a document marker mechanically.</GENRE-TERMINALS>
    </facts>
  </genres>
  <config title="6. Scope configuration — `facts.toml`">
    <facts ordered="false">
      <CONFIG-FILE fact="true" status="impl/done">Optional dev-mode mechanics, configured by a `facts.toml` at the package
  root (the `clippy.toml` pattern — tool config, not manifest pollution);
  `progress.toml` is read as a silent legacy fallback for the transition
  (owner correction 2026-08-22: the observed tree is a facts-layer concern).</CONFIG-FILE>
      <INCLUDE-STYLE fact="true" status="impl/done">Include-style globs name what **is** observed (not gitignore-style excludes):</INCLUDE-STYLE>
    </facts>
    <fence lang="toml">schema = 1
include = ["spec/**/*.md", "packages/**/*.md"]   # the default when absent</fence>
    <facts ordered="false">
      <DEFAULT-EXCLUDES fact="true" status="impl/done">**Default excludes** (applied always, even under explicit includes),
  in two kinds. **By directory**, matched against any path component:
  `vibedeps/`, `.vibe/`, `refs/`, `fixtures/`, `campaigns/`, `target/`,
  `node_modules/`, `**/vendor/`. **By file name**, matched against the
  basename wherever it sits: `LICENSE.md`.</DEFAULT-EXCLUDES>
      <excludes-rationale fact="true" status="spec/done">Rationale: regenerated dependency copies must
  never carry authored markup (PROP-009's install-never-edits-authored-spec
  law), third-party and test-asserted content is off-limits, and the campaign
  zone (PROP-047 §5.4) is not itself corpus. A licence is the same case one granularity
  down — verbatim text the observing project neither authored nor is the
  source of truth for, replaced wholesale from upstream — so it needed the
  file-name kind rather than a directory.</excludes-rationale>
      <CONFIG-EXCLUDE fact="true" status="impl/done">**Project-side `exclude`** — an optional list of globs in
  `progress.toml`, matched against the `/`-separated repo-relative path and
  applied **after** the includes and after both default kinds. It exists for
  what an include glob cannot say: *everything under this subtree except these
  named files* — a derived index, a generated projection, anything whose own
  words make a hand edit a defect. this section is include-style so that nothing is
  observed by accident, and an **enumerated** exclude list serves that purpose
  exactly as well as an enumerated include list; both are explicit and both
  are reviewable. It must not become a wildcard escape hatch, so: a pattern
  matching **no** observed file is reported by name on every subcommand, the
  count of files it removes is printed by `scan`, an invalid glob is a clean
  error naming the pattern, and an absent key behaves as a config that never
  had one.</CONFIG-EXCLUDE>
      <NESTED-OWNS-SUBTREE fact="true" status="spec/done">A nested package with its own
  `progress.toml` owns its subtree (the host aggregates; it does not reach in) —
  this is how a specspace keeps its own cadence.</NESTED-OWNS-SUBTREE>
    </facts>
  </config>
  <future title="7. Out of scope / future">
    <facts ordered="false">
      <FUT-XML-STORAGE fact="true" status="spec/done">XML document storage (arrives with the PROP-035 XML frontend — this markup is
  already native to it);</FUT-XML-STORAGE>
      <FUT-EXTRACTION fact="true" status="spec/done">extraction into a
  standalone distributable product (the separability law keeps it cheap);</FUT-EXTRACTION>
    </facts>
  </future>
</spec>
