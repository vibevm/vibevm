<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title id="root">PROP-027 — `mcp` packages: the agent-server kind and its delivery</title>
  <status stage="impl" state="done" comment="B0 2026-07-24: IMPLEMENTED with the MCP-SOVEREIGNTY waves"/>
  <p><milestone-line fact="true" status="impl/done">**Milestone:** M1.26 candidate («MCP sovereignty» —
[MCP-SOVEREIGNTY-PLAN-v0.1](../../../legacy-spec/terraforms/MCP-SOVEREIGNTY-PLAN-v0.1.md)).</milestone-line></p>
  <p><status-line fact="true" status="impl/done">**Status:** IMPLEMENTED — the kind and the manifest laws (§2.1–§2.3)
shipped with the plan's Wave 1; the servers themselves (Waves 3–4:
`mcp:org.vibevm.ai-native/rust-ai-native-mcp`, `…/typescript-ai-native-mcp`, both
live-chained vibe-free); the registration lifecycle (§2.4–§2.5) with
Wave 5 (`vibe mcp install/uninstall/status` speak package servers; the
pin-server fixture e2e pins the walk). §2.7's composition rows inherit
the kind-agnostic feature suites — no feature branches on `kind`, and
the mcp e2e exercises code-bearing + binaries + the pin end to end. Units typed at REQ grain; the code carries the matching
`scope!` / `#[spec(implements)]` / `#[verifies]` edges.</status-line></p>
  <p><related fact="true" status="spec/done">**Related:** [PROP-015](PROP-015-mcp-integration.xml) (the product MCP
server + the agent-integration command family this PROP extends),
[PROP-025](../vibe-workspace/PROP-025-binary-delivery.xml) (the binary
delivery an `[[mcp_server]]` rides), [PROP-024](../../common/PROP-024-code-bearing-packages.xml)
(code-bearing packages; §2.4 is why mcp packages vendor),
[`VIBEVM-SPEC.md` §4.1](../../../VIBEVM-SPEC.md) (the kind register,
amended under owner sanction 2026-07-07).</related></p>
  <motivation title="1. Motivation">
    <facts ordered="false">
      <motivation-gap fact="true" status="impl/done">The discipline stacks ship engines, gates, CLIs, and type oracles —
  everything an agent-facing toolchain needs EXCEPT the transport.</motivation-gap>
      <prototype-cycle fact="true" status="impl/done">The
  prototype topology served them through vibevm's own MCP
  (`vibe mcp serve` + the `tcg_*` adapters), which put the whole vibevm
  product into every consumer's runtime path and closed an operational
  cycle over vibevm itself (the tool served its own development).</prototype-cycle>
      <OWNER-RESOLUTION fact="true" status="impl/done">The
  owner's resolution (2026-07-07): agent-server delivery is a first-class
  package concern — a KIND, not a bolt-on surface — and vibe's job there
  is install-time wiring, never serving.</OWNER-RESOLUTION>
    </facts>
  </motivation>
  <decisions title="2. Decisions">
    <kind title="2.1 The `mcp` kind">
      <p><req-kind fact="true" status="impl/done">`req r1`</req-kind></p>
      <p><MCP-KIND-DEF fact="true" status="impl/done">An **`mcp` package** is one whose primary deliverable is one or more
Model Context Protocol servers.</MCP-KIND-DEF></p>
      <facts ordered="false">
        <KIND-REGISTER fact="true" status="impl/done">`kind = "mcp"` joins the installable
  register (VIBEVM-SPEC §4.1); slots materialise under
  `vibedeps/&lt;group&gt;.&lt;name&gt;/&lt;version&gt;/` like every other kind (the identity slot — PROP-022 §2.1, owner ruling 2026-08-13; the path carried a kind prefix before that ruling).</KIND-REGISTER>
        <TABLE-ONLY-IN-KIND fact="true" status="impl/done">The
  `[[mcp_server]]` table (§2.2) is **legal only in this kind** — the kind
  IS the taxonomy, enforced by `Manifest::validate`, not advisory.</TABLE-ONLY-IN-KIND>
        <KIND-PROMISES-SERVER fact="true" status="impl/done">An
  `mcp`-kind manifest that declares NO `[[mcp_server]]` is refused: the
  kind promises a server.</KIND-PROMISES-SERVER>
      </facts>
    </kind>
    <manifest title="2.2 The `[[mcp_server]]` declaration">
      <p><req-manifest fact="true" status="impl/done">`req r1`</req-manifest></p>
      <fence lang="toml">[[mcp_server]]
name = "rust-ai-native"           # agent-visible server name = the family (PROP-028 §2.4)
binary = "rust-ai-native-mcp"     # must match a [[binary]] in this manifest
description = "AI-Native Rust discipline + type oracle over MCP"
args = ["--path", "{project_root}"]</fence>
      <facts ordered="false">
        <SERVER-IS-BINARY fact="true" status="impl/done">The server IS a [PROP-025](../vibe-workspace/PROP-025-binary-delivery.xml)
  binary: delivery, consent, staleness, and slot residence come from that
  machinery wholesale — `binary` must resolve to a `[[binary]]` declared
  in the same manifest.</SERVER-IS-BINARY>
        <SERVER-NAME fact="true" status="impl/done">`name` is what an MCP host shows as the tool
  namespace; names are unique within the package.</SERVER-NAME>
        <ARGS-CLOSED-SET fact="true" status="impl/done">`args` may carry
  substitution tokens ONLY from the closed set `{project_root}` (the
  absolute, verbatim-free root of the consuming project, resolved at
  registration time); unknown `{…}` tokens are refused at validation.</ARGS-CLOSED-SET>
      </facts>
    </manifest>
    <exact-pin title="2.3 The exact-pin law">
      <p><req-exact-pin fact="true" status="impl/done">`req r1`</req-exact-pin></p>
      <facts ordered="false">
        <VENDORING-WHY fact="true" status="impl/done">Cargo path-deps cannot cross package slots (PROP-024 §2.4), so an mcp
  package VENDORS the crates of the toolchain it serves.</VENDORING-WHY>
        <SKEW-REOPENED fact="true" status="impl/done">Vendoring
  re-opens the version skew the in-slot prototype excluded by
  construction: a server built from engine copy X enriching against a
  consumer whose gates run engine copy Y.</SKEW-REOPENED>
        <EXACT-PIN-LAW fact="true" status="impl/done">The pin closes it: **every
  `[requires.packages]` entry of an `mcp`-kind package MUST be an exact
  `=X.Y.Z` requirement** — the resolver holds the served engines and the
  consumer's gates to ONE version set; no runtime handshake exists or is
  needed.</EXACT-PIN-LAW>
        <PIN-VALIDATION fact="true" status="impl/done">`Manifest::validate` refuses any other requirement shape
  (caret, bare, partial `=X.Y`, compound ranges).</PIN-VALIDATION>
        <PIN-SOURCES fact="true" status="impl/done">Git-source deps pin by
  rev inherently; path-source deps are local-dev surfaces outside this
  law.</PIN-SOURCES>
        <PIN-LOCKSTEP fact="true" status="impl/done">The operational consequence is accepted and priced by the plan:
  the mcp package bumps in lockstep with the package it serves.</PIN-LOCKSTEP>
      </facts>
    </exact-pin>
    <registration title="2.4 Registration: `vibe mcp install` learns packages">
      <p><req-registration fact="true" status="impl/done">`req r1`</req-registration></p>
      <p><REG-TODAY fact="true" status="impl/done">`vibe mcp install` today writes vibevm's own server into agent configs
(PROP-015).</REG-TODAY></p>
      <p><REG-PACKAGE-DISCOVERY fact="true" status="impl/done">It grows package discovery: every installed package of kind
`mcp` contributes its `[[mcp_server]]` entries, written into the target
agents' configs with</REG-PACKAGE-DISCOVERY></p>
      <facts ordered="false">
        <REG-COMMAND-PATH fact="true" status="impl/done">`command` = the absolute, **verbatim-free** path to the slot-resident
  built artifact (a real executable — no shim, no `cmd /c` wrapper
  class), `args` with the closed-set substitutions resolved;</REG-COMMAND-PATH>
        <REG-MANAGED-SIDECAR fact="true" status="impl/done">a **managed sidecar**: a top-level `"vibevm": { "managed": [...] }`
  object in the JSON config names the entries vibevm owns (never a key
  INSIDE a server entry — hosts validate entry shapes), so re-installs
  rewrite ONLY vibevm-managed entries and operator-owned servers are
  never touched — the `&lt;vibevm&gt;` block convention of the boot files,
  applied to agent configs.</REG-MANAGED-SIDECAR>
        <REG-PROJECT-SCOPE fact="true" status="impl/done">Registration is PROJECT-scope only (the
  `{project_root}` substitution demands a project, and a project's
  servers belong in its committed config), and every project-scope
  agent config is JSON — so no TOML sidecar form exists;</REG-PROJECT-SCOPE>
        <REG-REFRESH fact="true" status="impl/done">lifecycle: `vibe mcp install` re-run refreshes paths after a slot
  move;</REG-REFRESH>
        <REG-STATUS fact="true" status="impl/done">`vibe mcp status` reports each declared server's artifact state
  (an unbuilt artifact registers fine and fails at agent launch — the
  recipe names `vibe bin build &lt;name&gt;`);</REG-STATUS>
        <REG-UNINSTALL fact="true" status="impl/done">`vibe mcp uninstall` removes
  managed entries plus the emptied sidecar, and nothing else.</REG-UNINSTALL>
      </facts>
    </registration>
    <consent title="2.5 Consent: registration is the same trust act as building">
      <p><req-consent fact="true" status="impl/done">`req r1`</req-consent></p>
      <facts ordered="false">
        <CONSENT-TRUST-ACT fact="true" status="impl/done">Registering a server schedules package code execution at agent-session
  start; building its binary compiles package code.</CONSENT-TRUST-ACT>
        <CONSENT-GATE-INHERITED fact="true" status="impl/done">One trust model, two
  verbs: registration inherits PROP-025's consent gate verbatim —
  `org.vibevm` packages are allow-listed; any other origin requires the
  explicit `--assume-yes` (or is refused with the recipe naming that
  exact flag).</CONSENT-GATE-INHERITED>
        <CONSENT-WRITE-SCOPE fact="true" status="impl/done">Registration writes touch ONLY the target agent's config
  files and only managed entries; server processes receive the project
  root as cwd and NO secrets from vibe.</CONSENT-WRITE-SCOPE>
      </facts>
    </consent>
    <vibe-free title="2.6 Serving is vibe-free">
      <p><req-vibe-free fact="true" status="impl/done">`req r1`</req-vibe-free></p>
      <facts ordered="false">
        <VIBE-FREE-SERVING fact="true" status="impl/done">An mcp package's servers run without vibe: the artifact is launched by
  the agent host directly from the slot path, links its vendored engines,
  and speaks stdio MCP.</VIBE-FREE-SERVING>
        <VIBE-ROLE fact="true" status="impl/done">`vibe` is required only to install, build, and
  register.</VIBE-ROLE>
        <VIBE-FREE-ACCEPTANCE fact="true" status="impl/done">The acceptance form of this requirement: the server's live
  chain passes with `vibe` absent from `PATH` and no vibevm process
  running.</VIBE-FREE-ACCEPTANCE>
      </facts>
    </vibe-free>
    <composition title="2.7 Composition: an mcp package is a full package">
      <p><req-composition fact="true" status="impl/done">`req r1`</req-composition></p>
      <p><COMPOSITION-LAW fact="true" status="impl/done">Every package-role feature applies to `mcp` packages exactly as to the
other kinds; no feature branches on `kind`, so each row inherits its
feature's own kind-agnostic suite (the mcp-kind e2e adds the
code-bearing + binaries + exact-pin composition end to end):</COMPOSITION-LAW></p>
      <table>
        <tr>
          <td>Feature</td>
          <td>Spec</td>
          <td>Composition rule</td>
        </tr>
        <tr>
          <td><ROW-CODE-BEARING fact="true" status="impl/done">Code-bearing layout</ROW-CODE-BEARING></td>
          <td><fact status="impl/done">PROP-024</fact></td>
          <td><fact status="impl/done">the package IS code-bearing by definition; `spec/` for prompt content, crates at root</fact></td>
        </tr>
        <tr>
          <td><ROW-BINARIES fact="true" status="impl/done">Binaries</ROW-BINARIES></td>
          <td><fact status="impl/done">PROP-025</fact></td>
          <td><fact status="impl/done">`[[mcp_server]].binary` references them; `vibe bin list/build/exec` see them like any other</fact></td>
        </tr>
        <tr>
          <td><ROW-SKILLS fact="true" status="impl/done">Skills</ROW-SKILLS></td>
          <td><fact status="impl/done">PROP-015 §2.8, PROP-018 §2.4</fact></td>
          <td><fact status="impl/done">`[[skill]]` legal; a server may ship its teaching skill</fact></td>
        </tr>
        <tr>
          <td><ROW-BOOT-SNIPPET fact="true" status="impl/done">Boot snippet</ROW-BOOT-SNIPPET></td>
          <td><fact status="impl/done">PROP-009</fact></td>
          <td><fact status="impl/done">legal but not required; agents learn servers via registration, not boot text</fact></td>
        </tr>
        <tr>
          <td><ROW-HOOKS fact="true" status="impl/done">Hooks</ROW-HOOKS></td>
          <td><fact status="impl/done">PROP-020</fact></td>
          <td><fact status="impl/done">pre/post-install hooks run in the slot as usual</fact></td>
        </tr>
        <tr>
          <td><ROW-MATERIALIZATION fact="true" status="impl/done">Materialization modes</ROW-MATERIALIZATION></td>
          <td><fact status="impl/done">PROP-022</fact></td>
          <td><fact status="impl/done">snapshot/in-place per the standard rules</fact></td>
        </tr>
        <tr>
          <td><ROW-BRIDGES fact="true" status="impl/done">Bridges / submodules</ROW-BRIDGES></td>
          <td><fact status="impl/done">PROP-023 / PROP-021</fact></td>
          <td><fact status="impl/done">no special-casing</fact></td>
        </tr>
        <tr>
          <td><ROW-PUBLISH fact="true" status="impl/done">Publish</ROW-PUBLISH></td>
          <td><fact status="impl/done">PROP-002 §2.10</fact></td>
          <td><fact status="impl/done">standard registry publish; the exact-pin law travels in the manifest</fact></td>
        </tr>
        <tr>
          <td><ROW-MUTABILITY fact="true" status="impl/done">In-workspace mutability</ROW-MUTABILITY></td>
          <td><fact status="impl/done">PROP-011 §2.6</fact></td>
          <td><fact status="impl/done">dev-loop re-materialisation applies</fact></td>
        </tr>
      </table>
    </composition>
  </decisions>
  <rejected title="3. Rejected alternatives">
    <facts ordered="false">
      <REJ-ANY-KIND fact="true" status="spec/done">**`[[mcp_server]]` as an any-kind surface** (the plan's original
  draft): rejected by the owner — the taxonomy should say what a
  package IS; embedded servers would blur the register and hide the
  vendoring/pinning obligations §2.3 makes explicit.</REJ-ANY-KIND>
      <REJ-CROSS-SLOT fact="true" status="spec/done">**Cross-slot path-deps or manifest rewriting** instead of vendoring:
  PROP-025 v2 territory, specified-only; the reproducible-hash model
  (PROP-024 §2.2) forbids post-materialise rewriting today.</REJ-CROSS-SLOT>
      <REJ-HANDSHAKE fact="true" status="spec/done">**A runtime version handshake** instead of the exact pin: weaker (it
  detects skew instead of preventing it) and needs a wire surface;
  the resolver already enforces equality for free.</REJ-HANDSHAKE>
      <REJ-VIBE-LAUNCHER fact="true" status="spec/done">**vibe as the server launcher** (`vibe mcp exec &lt;name&gt;` in agent
  configs): would keep vibe in the runtime path — the exact property
  this PROP removes.</REJ-VIBE-LAUNCHER>
    </facts>
  </rejected>
  <open title="4. Open questions">
    <facts ordered="true">
      <OPEN-MULTI-SERVER fact="true" status="spec/work">Multi-server packages (legal today) — does `vibe mcp install` offer
   per-server opt-out? v1: all-or-nothing per package.</OPEN-MULTI-SERVER>
      <OPEN-APP-KIND fact="true" status="spec/work">The `app` kind (anticipated, VIBEVM-SPEC §4.1) — whether it reuses
   §2.4's managed-entry machinery for desktop-integration surfaces.</OPEN-APP-KIND>
      <OPEN-STABLE-PATHS fact="true" status="spec/work">Stable artifact paths (PROP-025 v2 shims) would make managed entries
   survive version bumps without a re-install; deferred with it.</OPEN-STABLE-PATHS>
    </facts>
  </open>
</spec>
