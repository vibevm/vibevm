<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>vibe prefs — the settings TUI (PROP-041) visual sign-off</title>
  <p><purpose fact="true" status="impl/done">**Purpose.** `vibe prefs ui` is the TUI surface over `vibe-settings` (PROP-040):
a page tree, per-type edit forms, provenance, validation feedback, lint, and
search — built on the `vibe tree` TUI's component library + theme (Шаг 3) and
driven by `vibe.prefs` actions. The automated suite (347 vibe-cli tests,
`self-check` all green) proves the model + the rendering fns; it cannot drive a
real terminal and confirm the surface *reads right* — the page tree aligns, the
form fields edit, the provenance shows the winning layer, the validation
warnings land inline. That is what a human signs off here.</purpose></p>
  <p><settings-mutation-note fact="true" status="impl/done">This TUI writes user settings (`~/.vibe/`, the vibe-settings system) — palette/
tier/mode/sort/shape/static-first persist. Back them up or accept the delta.</settings-mutation-note></p>
  <section title="Preconditions">
    <facts ordered="false">
      <PRE-TTY fact="true" status="impl/done">A real interactive terminal (tty), ideally truecolor.</PRE-TTY>
      <PRE-BINARY fact="true" status="impl/done">`cargo build -p vibe-cli` (invoke `./target/debug/vibe`).</PRE-BINARY>
      <PRE-CONTEXT fact="true" status="impl/done">Run from the vibevm repo root (a project context → L1+L2+L3; elsewhere L1 only).</PRE-CONTEXT>
    </facts>
  </section>
  <section title="Steps">
    <facts ordered="true">
      <STEP-1-LAUNCH fact="true" status="impl/done">**Launch + the page tree.**</STEP-1-LAUNCH>
    </facts>
    <fence>   ./target/debug/vibe prefs ui</fence>
    <p><S1-PAGE-TREE fact="true" status="impl/done">Left pane: the page hierarchy (Appearance / Tree groups → the palette/tier/
   mode/sort/shape/static-first pages) through the same visual language as the
   tree TUI (`│├└─`, `▾`/`▸` fold, theme colours). `↑`/`↓` move, `←`/`→` fold.</S1-PAGE-TREE></p>
    <facts ordered="true">
      <STEP-2-EDIT-FORM fact="true" status="impl/done">**Open a page + the edit form (§4).** `Enter` on a page → the right pane
   shows the form: per-type fields (bool→toggle, enum/closed-set→selection,
   int/string→text), each with an `applies` badge + a write-layer selector at
   the bottom. `↑`/`↓` field focus, `Space`/`Enter` toggle/select, typing edits a
   text field, `Tab` cycles the write-layer (L3 project / L1 no-project).</STEP-2-EDIT-FORM>
    </facts>
    <facts ordered="true">
      <STEP-3-APPLY-RESET fact="true" status="impl/done">**Apply / reset (§4 Configurable lifecycle).** Edit a field → the form is
   modified; `a` applies (writes through `vibe-settings` to the chosen layer;
   a scope-forbidden layer is refused with the reason), `r` resets. Quit +
   relaunch — the change persisted.</STEP-3-APPLY-RESET>
    </facts>
    <facts ordered="true">
      <STEP-4-PROVENANCE fact="true" status="impl/done">**Provenance (§5).** `?` on a focused field → an inline block: the resolved
   value + each layer's contribution (default/L1/L2/L3/cli/env), the winning
   layer marked, shadowed layers dimmed. `x` clears the focused write-layer for
   that key → the value falls back to the layer beneath.</STEP-4-PROVENANCE>
    </facts>
    <facts ordered="true">
      <STEP-5-VALIDATION-LINT fact="true" status="impl/done">**Validation + lint (§6).** Type a wrong-type value → an inline warning line
   under the field (gold, rule cited); `apply` is blocked. `c` opens the lint
   modal — `schema::validate` across L1/L2/L3 as a flat warning list; selecting
   a warning jumps to the owning page focused on that field.</STEP-5-VALIDATION-LINT>
    </facts>
    <facts ordered="true">
      <STEP-6-SEARCH fact="true" status="impl/done">**Search (§7).** `/` or `F1` → the Search Everywhere window (the same engine
   the tree TUI uses): match by key / display name / description / synonyms;
   selecting opens the owning page focused on the field. Deprecated keys surface
   their `replaced_by`.</STEP-6-SEARCH>
    </facts>
    <facts ordered="true">
      <STEP-7-ACTIONS-FOOTER fact="true" status="impl/done">**Actions + footer (§8).** The footer lists the enabled `vibe.prefs` actions
   for the current context (open/apply/reset/search/lint/layer/provenance/quit),
   keymap-bound. `Esc` pops the modal stack (provenance → page).</STEP-7-ACTIONS-FOOTER>
    </facts>
    <facts ordered="true">
      <STEP-8-THEME-SWITCH fact="true" status="impl/done">**Theme switch (the live Theme, §1 `#built-on-tree-tui`).** Change
   `vibe.tree.palette` in the form (or `~/.vibe/settings.toml`) → the whole
   prefs UI re-skins to match the tree TUI.</STEP-8-THEME-SWITCH>
    </facts>
  </section>
  <section title="Pass">
    <p><pass-criteria fact="true" status="impl/done">Every step reads as described: the page tree aligns, the form edits + applies +
persists, the provenance shows the winning layer, validation lands inline, the
lint + search work, and the footer lists the live actions. The owner signs the
date below.</pass-criteria></p>
  </section>
  <section title="Sign-off">
    <facts ordered="false">
      <SIGNOFF-OWNER fact="true" status="impl/work">[ ] Owner visual sign-off (date / initials): ______</SIGNOFF-OWNER>
    </facts>
  </section>
</spec>
