<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>Design rationale: Loading &amp; boot composition model</title>
  <status stage="doc" state="done" comment="B0 2026-07-24: non-normative design record, captured 2026-05-21 in an owner session"/>
  <p><companion-line fact="true" status="doc/done">**Companion to:** [PROP-009](../modules/vibe-workspace/PROP-009-loading-model.xml) (the loading model — **shipped**, M1.18 phases 1–7, 2026-05-22).</companion-line></p>
  <p><rename-note fact="true" status="doc/done">**Terminology (2026-07-16 rename — read this before the body).** This record captures the 2026-05-21 session and keeps that session's three names; the shipped grammar has two. Read `inline` as **`link = "static"`** (the verbatim `STATIC.md` lane), the design's `static` as **`link = "dynamic"`** (the default, a by-reference `INDEX.md` read), and the design's `dynamic` as a **`dynamic` entry carrying a `when`**. The default moved with the names: design-`static` was the default and its shipped counterpart `dynamic` is. See [PROP-009 §2.4](../modules/vibe-workspace/PROP-009-loading-model.xml#inclusion-types), which is normative.</rename-note></p>
  <p><status-line fact="true" status="doc/done">**Status:** non-normative design record. Captured 2026-05-21 in an owner design session.</status-line></p>
  <p><authority-line fact="true" status="doc/done">**Authority:** the PROP is the contract. If this document and PROP-009 disagree, the PROP wins.</authority-line></p>
  <section title="1. What this document is">
    <p><origin-question fact="true" status="doc/done">PROP-007 shipped the workspace data model (M1.17) but deliberately left one question
open — [PROP-007 §6 question 3](../modules/vibe-workspace/PROP-007-workspace.xml#open),
the *per-member materialisation target*: when a dependency is resolved for member M,
into which member's `spec/` does its content land?</origin-question></p>
    <p><scope-of-answer fact="true" status="doc/done">This session answered it. The answer turned out to be far larger than picking a
directory: it is a redesign of vibevm's entire **loading model** — how a dependency's
content is materialised, how the boot sequence is composed across a workspace
hierarchy, and how an AI agent consumes it at session start. This document records
the *why* and the fork-by-fork reasoning; **PROP-009** is the contract, and it shipped.</scope-of-answer></p>
    <p><workspace-arc-continuity fact="true" status="doc/done">It belongs to the workspace arc and continues
[`workspace-and-qualified-naming.xml`](workspace-and-qualified-naming.xml).</workspace-arc-continuity></p>
  </section>
  <section title="2. The problem the deferred question was hiding">
    <p><narrow-question-illusion fact="true" status="doc/done">PROP-007 §6 q3 reads as a narrow placement question. It is not.</narrow-question-illusion></p>
    <p><flat-model-shape fact="true" status="doc/done">vibevm's boot model ([`VIBEVM-SPEC.md` §6](../../VIBEVM-SPEC.md)) is a **flat, single,
shared, mutable namespace** — `spec/boot/NN-*.md`, read in filename order, one
sequence, one entry point. It is correct for exactly one project shape: one project →
one boot sequence → one entry point.</flat-model-shape></p>
    <p><workspace-breaks-lead fact="true" status="doc/done">A workspace breaks every one of those assumptions at once:</workspace-breaks-lead></p>
    <facts ordered="false">
      <break-n-nodes fact="true" status="doc/done">**N nodes** — the root plus every member.</break-n-nodes>
      <break-n-entry-points fact="true" status="doc/done">**N entry points** — a developer `cd`s into any member and opens an agent there.
  This is PROP-007's load-bearing principle: "the user works in a sub-project and
  doesn't notice it is part of something bigger."</break-n-entry-points>
      <break-n-boot-sequences fact="true" status="doc/done">**N boot sequences** — each entry point needs a sequence coherent *for that node*.</break-n-boot-sequences>
      <break-shared-deps fact="true" status="doc/done">**One shared dependency set** — unified resolution (PROP-007 §2.4) means one version
  of each external dependency across the whole workspace.</break-shared-deps>
    </facts>
    <p><flat-model-verdict fact="true" status="doc/done">The flat model cannot be stretched over this. It must be replaced.</flat-model-verdict></p>
    <p><OWNER-CONSTRAINT-NO-REWRITE fact="true" status="doc/done">The owner's hard constraint, stated at the outset: **installing a dependency must
never modify anyone's authored spec.** The owner's analogy is C++ — you do not rewrite
your `#include` directives into the literal text of the dependency's headers. Merging
a dependency's spec into the consuming spec is the broken option, and it is what a
naïve "bubble everything into the root `spec/boot/`" would amount to.</OWNER-CONSTRAINT-NO-REWRITE></p>
  </section>
  <section title="3. The owner&apos;s mental model — boot as linking">
    <p><linking-frame fact="true" status="doc/done">The owner framed the loading mechanism directly as **static vs dynamic linking**, and
that framing became the spine of the design.</linking-frame></p>
    <table>
      <tr>
        <td>C++ / the linker</td>
        <td>vibevm</td>
      </tr>
      <tr>
        <td><ROW-LINK-COMPILE fact="true" status="doc/done">Compile + link time</ROW-LINK-COMPILE></td>
        <td><fact status="doc/done">`vibe install`</fact></td>
      </tr>
      <tr>
        <td><ROW-LINK-LOAD fact="true" status="doc/done">Load + run time</ROW-LINK-LOAD></td>
        <td><fact status="doc/done">start of an AI session</fact></td>
      </tr>
      <tr>
        <td><ROW-LINK-OBJECT fact="true" status="doc/done">Object file / library</ROW-LINK-OBJECT></td>
        <td><fact status="doc/done">a package (flow / feat / stack / tool)</fact></td>
      </tr>
      <tr>
        <td><ROW-LINK-EXECUTABLE fact="true" status="doc/done">The executable</ROW-LINK-EXECUTABLE></td>
        <td><fact status="doc/done">a node's effective boot sequence</fact></td>
      </tr>
      <tr>
        <td><ROW-LINK-LOADER fact="true" status="doc/done">The dynamic loader (`ld.so`)</ROW-LINK-LOADER></td>
        <td><fact status="doc/done">the AI agent following INCLUDE pointers</fact></td>
      </tr>
      <tr>
        <td><ROW-LINK-DT-NEEDED fact="true" status="doc/done">`DT_NEEDED` entry</ROW-LINK-DT-NEEDED></td>
        <td><fact status="doc/done">an INCLUDE pointer (a `spec://` reference)</fact></td>
      </tr>
      <tr>
        <td><ROW-LINK-STATIC fact="true" status="doc/done">Static linking (`.a` copied into the binary)</ROW-LINK-STATIC></td>
        <td><fact status="doc/done">`vibe install` inlines a dependency's boot</fact></td>
      </tr>
      <tr>
        <td><ROW-LINK-DYNAMIC fact="true" status="doc/done">Dynamic linking (`.so` referenced, resolved at load)</ROW-LINK-DYNAMIC></td>
        <td><fact status="doc/done">the INCLUDE pointer stays; the agent resolves it</fact></td>
      </tr>
      <tr>
        <td><ROW-LINK-VENDOR fact="true" status="doc/done">`vendor/` / `~/.cargo/registry`</ROW-LINK-VENDOR></td>
        <td><fact status="doc/done">the materialised-dependency tree</fact></td>
      </tr>
    </table>
    <p><AGENT-IS-THE-LOADER fact="true" status="doc/done">The load-bearing consequence: **at session start, the AI agent is the loader.** Every
reference the agent resolves itself costs tool calls, and tool calls are — the owner's
words — *безумно дорого*. Static linking moves resolution to `vibe install` time (done
once, by the machine); dynamic linking leaves it to the agent (every session, by the
LLM). Exactly the C++ tradeoff: a statically-linked binary loads with no help; a
dynamically-linked one needs the loader to find and map every `.so`.</AGENT-IS-THE-LOADER></p>
  </section>
  <section title="4. The model — four principles">
    <p><P1-TWO-TREES fact="true" status="doc/done">**P1 — Two trees.** A node's authored `spec/` (only the node's author writes it) and
the materialised dependencies (a separate tree, only `vibe` writes it) are physically
separate and never intermixed. Installing a dependency never touches authored `spec/`.
This is "your code vs `vendor/`". Under unified resolution a dependency is materialised
**once** for the whole workspace.</P1-TWO-TREES></p>
    <p><P2-COMPUTED-SEQUENCE fact="true" status="doc/done">**P2 — The boot sequence is computed, not assembled by hand.** Each node has an
*effective boot sequence* = inherited foundation (from ancestors, flowing down) + the
node's own authored boot + the boot of its transitive dependencies (flowing up) + user
overrides. `vibe` computes it from the unified resolution. This is the owner's
"matryoshka" — but computed directly per level from the resolution graph, not
physically copied leaf-to-root (copying drifts; computation does not). Every level is
self-contained: a session opened at any node gets a sequence coherent for that node's
subtree. The root's sequence is the union of everything; a small member's is small —
the hierarchy gives cost-scoping for free.</P2-COMPUTED-SEQUENCE></p>
    <p><P3-INDEX-PER-ENTRY-POINT fact="true" status="doc/done">**P3 — One generated index per entry point; the agent never walks the graph.**
`vibe install` generates, for each entry-point node, the boot artifacts (§6). The
agent reads them in a flat, predictable loop — reads parallelise, collapsing latency.
No recursion, no discovery, no cycle-detection on the agent's side: `vibe` did that
once. Boot stays **pure file-reading** — the `CLAUDE.md` / `AGENTS.md` / `GEMINI.md`
redirect points at generated files; it does *not* become "run `vibe`". This preserves
the zero-dependency cross-agent property that is the whole point of VIBEVM-SPEC §6.1.</P3-INDEX-PER-ENTRY-POINT></p>
    <p><P4-THREE-INCLUSION-TYPES fact="true" status="doc/done">**P4 — Three inclusion types: `inline`, `static`, `dynamic`.** Declared per dependency
in the consumer's `vibe.toml`; default `static`. They are the points on the linker
spectrum (§6). *(As shipped after the 2026-07-16 rename: two types, `link = "static" | "dynamic"`,
default `dynamic`, with the third case expressed as a `dynamic` entry carrying a `when` — see the
rename note above.)*</P4-THREE-INCLUSION-TYPES></p>
  </section>
  <section title="5. The fork-by-fork record">
    <p><forks-lead fact="true" status="doc/done">Four forks were put to the owner. The options, the resolution, the reasoning.</forks-lead></p>
    <section title="Fork 1 — the form of static boot delivery">
      <p><fork1-options fact="true" status="doc/done">Options offered: a single inlined aggregate (cheapest at boot, but duplication and
the lost-in-the-middle risk §6.4 already rejected); a resolved path index (clean, but
1 + N reads); a hybrid.</fork1-options></p>
      <p><FORK1-REFINED-HYBRID fact="true" status="doc/done">**Resolution — a refined hybrid.** Not one of the three: a model where `inline`,
`static`, and `dynamic` *all coexist* as per-dependency inclusion types (§6), plus a
dedicated generated `STATIC.md`. The owner's reasoning: `STATIC.md` is an *emergency
priority lane*. For the highest-importance content — top-level skills, critical
disciplines — the boot text is concatenated verbatim into one file read first, so its
priority is guaranteed by *position* and does not depend on the agent performing
resolution correctly. This is the honest answer to lost-in-the-middle (§6.5): you do
not fix attention degradation by being clever about file counts; you fix it by putting
the must-not-be-missed content physically first.</FORK1-REFINED-HYBRID></p>
    </section>
    <section title="Fork 2 — where materialised dependencies live">
      <p><FORK2-COMMITTED-TREE fact="true" status="doc/done">**Resolution — a committed dependency tree.** A fresh clone is bootable with no
`vibe install`; the dependencies are visible and diffable; it is consistent with the
spec-driven ethos — the spec corpus *is* the product. Plus: a `vibe` command to
regenerate a subtree of the materialised state on demand — for when dependencies are
believed stale or a previous index pass was wrong.</FORK2-COMMITTED-TREE></p>
    </section>
    <section title="Fork 3 — uniform vs workspace-only">
      <p><FORK3-UNIFORM fact="true" status="doc/done">**Resolution — uniform.** A single-package project is a degenerate (zero-member)
workspace; one loading model everywhere. Consistent with `Workspace::discover`, which
already degenerates cleanly to "just this one node". The cost — every existing
project's layout migrates — is acceptable: vibevm is pre-release, and M1.17's
no-legacy hard break already set the precedent.</FORK3-UNIFORM></p>
    </section>
    <section title="Fork 4 — milestone scope">
      <p><FORK4-UNIFIED-VIEWS fact="true" status="doc/done">**Resolution — boot + effective spec, unified.** The boot index and the `vibe build`
effective spec (VIBEVM-SPEC §4.6) are the same idea: a computed, layered, materialised
view of the workspace. PROP-009 specifies one *computed-view engine*; the boot index
("what to read at session start") and the effective spec ("the full merged corpus for
build") are two views it emits.</FORK4-UNIFIED-VIEWS></p>
    </section>
  </section>
  <section title="6. The three inclusion types — the refined Fork-1 answer in detail">
    <p><inclusion-types-lead fact="true" status="doc/done">Each dependency in a consumer's `vibe.toml` carries an inclusion type (working syntax
of this session: `link = "inline" | "static" | "dynamic"`); the default was `static`. At `vibe install`
time, for each entry-point node `vibe` generates:</inclusion-types-lead></p>
    <facts ordered="false">
      <GEN-STATIC-MD fact="true" status="doc/done">**`STATIC.md`** — the verbatim concatenation of every `static`-typed contribution in
  the node's effective boot, in priority order. Read first; one read; maximum attention
  weight. The emergency lane — used sparingly, for top-level skills and critical
  disciplines. Generated only when the node has inline contributions (shipped name:
  `static` contributions, carried by the `vibe:static` markers inside `STATIC.md`).</GEN-STATIC-MD>
      <GEN-INDEX-MD fact="true" status="doc/done">**`INDEX.md`** — the ordered, resolved manifest of the rest of the boot sequence.
  `static` entries appear as resolved file paths the agent reads directly (a flat,
  parallelisable loop). `dynamic` entries appear as INCLUDE pointers the agent resolves
  at boot.</GEN-INDEX-MD>
      <SESSION-START-ORDER fact="true" status="doc/done">Session-start order: `CLAUDE.md` → `STATIC.md` → `INDEX.md` and the files it names.</SESSION-START-ORDER>
    </facts>
    <p><cost-profile-lead fact="true" status="doc/done">Cost profile:</cost-profile-lead></p>
    <table>
      <tr>
        <td>Type</td>
        <td>Reads at boot</td>
        <td>Content on disk</td>
        <td>Use</td>
      </tr>
      <tr>
        <td><ROW-COST-INLINE fact="true" status="doc/done">`inline` (ships as `link = "static"`)</ROW-COST-INLINE></td>
        <td><fact status="doc/done">~1 (already in `STATIC.md`)</fact></td>
        <td><fact status="doc/done">duplicated (bounded — few items)</fact></td>
        <td><fact status="doc/done">critical disciplines, top-level skills</fact></td>
      </tr>
      <tr>
        <td><ROW-COST-STATIC fact="true" status="doc/done">`static` (ships as `link = "dynamic"`)</ROW-COST-STATIC></td>
        <td><fact status="doc/done">1 + N (N parallelisable)</fact></td>
        <td><fact status="doc/done">lives once</fact></td>
        <td><fact status="doc/done">the default — ordinary dependencies</fact></td>
      </tr>
      <tr>
        <td><ROW-COST-DYNAMIC fact="true" status="doc/done">`dynamic`</ROW-COST-DYNAMIC></td>
        <td><fact status="doc/done">1 + N + graph-walk</fact></td>
        <td><fact status="doc/done">lives once</fact></td>
        <td><fact status="doc/done">conditional / context-gated boot</fact></td>
      </tr>
    </table>
    <p><dynamic-is-lazy-pull fact="true" status="doc/done">`dynamic` is, mechanically, the subskill `lazy-pull` delivery mode (PROP-003 §2.5) —
the loading model generalises subskill delivery rather than inventing a parallel axis.</dynamic-is-lazy-pull></p>
  </section>
  <section title="7. Consequences and findings">
    <facts ordered="false">
      <FINDING-NUMBERING fact="true" status="doc/done">**Numbering.** The `NN-` prefix namespace (10–89, author-chosen) cannot survive a
  workspace and is already admitted provisional (§6.5). In the computed model `vibe`
  owns the order in the generated artifacts; a package author declares only a
  *category* (the existing range bands — foundation / flow / stack / user-override —
  become categories) and optionally a coarse early/late hint. Prefix collisions become
  impossible by construction.</FINDING-NUMBERING>
      <FINDING-MIRROR-BREAKAGE fact="true" status="doc/done">**Mirror-layout breakage.** VIBEVM-SPEC §13.1's mirror layout — source path = target
  path — works today only because a dependency always lands at the same path in every
  project. Moving dependency content into a separate tree breaks that; a package's
  internal cross-references (a boot snippet pointing at its own protocol document) must
  become package-relative or `spec://` URIs, rewritten by `vibe` at materialisation.</FINDING-MIRROR-BREAKAGE>
      <FINDING-PUBLISH-REGEN fact="true" status="doc/done">**Published-copy regeneration.** A package published by `vibe workspace publish` is
  consumed standalone — its boot index must be regenerated for the published shape,
  where dependencies are registry-resolved and version-pinned rather than path-sourced.
  This is exactly PROP-007 §2.5's dual-form `{ path, version }`; publish staging gains
  an index-regeneration step.</FINDING-PUBLISH-REGEN>
      <FINDING-DOGFOOD fact="true" status="doc/done">**vibevm dogfoods itself.** The vibevm repository is itself a vibevm project;
  PROP-009 changes how *this* repository boots. `spec/boot/00-core.xml` and
  `spec/boot/90-user.xml` stay user-owned authored boot; the generated `STATIC.md` /
  `INDEX.md` join them. The migration is part of the milestone.</FINDING-DOGFOOD>
    </facts>
  </section>
  <section title="8. What this supersedes and parks">
    <facts ordered="false">
      <SUBSUMES-INSTALL-BUILD fact="true" status="doc/done">**Workspace-aware `vibe install` / `vibe build`** (PROP-007 §9.3, §6 q3) is no longer
  a separate deferred item — it is *subsumed*: it becomes the install/build half of
  PROP-009.</SUBSUMES-INSTALL-BUILD>
      <parks-version-workspace fact="true" status="doc/done">**`version = { workspace = true }`** (PROP-007 §6 q4) and the **publish-signalling
  polish** (`--archive` etc., PROP-007 §9.3) are parked behind PROP-009 — recorded, not
  dropped.</parks-version-workspace>
      <prop-008-unaffected fact="true" status="doc/done">**PROP-008** (qualified naming) is unaffected; it still follows PROP-005 (index).</prop-008-unaffected>
    </facts>
  </section>
  <section title="9. Session log">
    <facts ordered="false">
      <session-log-2026-05-21 fact="true" status="doc/done">**2026-05-21.** Session restored from `CONTINUE.md` + `spec/WAL.xml`. The owner
  reopened PROP-007 §6 q3 (the materialisation target). The discussion established
  that the question is a loading-model redesign, not a directory choice; produced the
  linker-model spine (§3), the four principles (§4), and the four-fork resolution
  recorded in §5. PROP-009 and its implementation milestone (M1.18) deferred to the
  contract-writing step. Out of band, a standing test-environment misdiagnosis was
  corrected — `os error 740` on `cargo test -p vibe-install` is Windows UAC installer
  detection (the test binary's name contains "install"), not Windows Defender; see the
  WAL.</session-log-2026-05-21>
    </facts>
  </section>
  <section title="10. Pointers">
    <facts ordered="false">
      <ptr-prop-009 fact="true" status="doc/done">[PROP-009](../modules/vibe-workspace/PROP-009-loading-model.xml) — the contract; shipped M1.18.</ptr-prop-009>
      <ptr-prop-007 fact="true" status="doc/done">[PROP-007](../modules/vibe-workspace/PROP-007-workspace.xml) — the workspace data model; §6 q3 is the question this answers.</ptr-prop-007>
      <ptr-workspace-naming fact="true" status="doc/done">[`workspace-and-qualified-naming.xml`](workspace-and-qualified-naming.xml) — the preceding design session.</ptr-workspace-naming>
      <ptr-vibevm-spec fact="true" status="doc/done">[`VIBEVM-SPEC.md`](../../VIBEVM-SPEC.md) — §6 (boot directory model), §4.2 (layout), §4.6 (effective spec), §13.1 (mirror layout).</ptr-vibevm-spec>
      <ptr-prop-003 fact="true" status="doc/done">[PROP-003 §2.5](../modules/vibe-resolver/PROP-003-dep-evolution.xml) — subskills and delivery modes.</ptr-prop-003>
    </facts>
  </section>
</spec>
