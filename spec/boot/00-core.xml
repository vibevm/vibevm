<?xml version="1.0" encoding="UTF-8"?>
<spec xmlns="https://vibevm.org/spec/1">
  <title>vibevm — boot snippet: project foundation</title>
  <p><project-identity fact="true" status="impl/done">**Project:** vibevm — a CLI software project manager for spec-driven AI-assisted development.
**Binary:** `vibe`.
**Source of truth:** [`VIBEVM-SPEC.md`](../../VIBEVM-SPEC.md) (project root). This is the entire implementation specification.</project-identity></p>
  <section title="Session boot sequence">
    <p><boot-sequence-lead fact="true" status="impl/done">Every session starts here. In order:</boot-sequence-lead></p>
    <facts ordered="true">
      <BOOT-STEP-BOOT-DIR fact="true" status="impl/done">Read this file and the rest of `spec/boot/` end to end — including the generated `INDEX.md` (the computed boot manifest) and `STATIC.xml` (the priority lane). `vibe` owns the boot order; see `VIBEVM-SPEC.md` §6.</BOOT-STEP-BOOT-DIR>
      <BOOT-STEP-WAL fact="true" status="impl/done">Read `spec/WAL.xml` — current project state (checkpoint, not log).</BOOT-STEP-WAL>
      <BOOT-STEP-PROPS fact="true" status="impl/done">Read the relevant PROP/FEAT under `spec/common/` and `spec/modules/` for the task at hand.</BOOT-STEP-PROPS>
      <BOOT-STEP-START-WORK fact="true" status="impl/done">Only then start work.</BOOT-STEP-START-WORK>
    </facts>
    <p><WAL-STALENESS-CHECK fact="true" status="impl/done">If `spec/WAL.xml` is older than 24 hours, verify the state with the user before doing destructive work.</WAL-STALENESS-CHECK></p>
  </section>
  <section title="The four non-negotiable rules">
    <p><four-rules-lead fact="true" status="impl/done">See [`CLAUDE.md`](../../CLAUDE.md) (and its identical copies `AGENTS.md` / `GEMINI.md`) for the full text. Authoritative reference: [spec://org.vibevm.core/vibevm/common/PROP-000#commits](../common/PROP-000.xml#commits). Summary only:</four-rules-lead></p>
    <facts ordered="true">
      <RULE-ATTRIBUTION fact="true" status="impl/done">**Attribution — keep this repository human-authored.** Never mark commits, branches, comments, or any artefact as machine-authored. The rule itself (and its copy in PROP-000 §12.1) is the only place in the project where that topic is discussed.</RULE-ATTRIBUTION>
      <RULE-CONVENTIONAL-COMMITS fact="true" status="impl/done">**Conventional Commits** — short subject, long explanatory body answering *why*.</RULE-CONVENTIONAL-COMMITS>
      <RULE-ATOMIC-GROUPING fact="true" status="impl/done">**Group commits by meaning** — one logical unit per commit, split mixed working trees.</RULE-ATOMIC-GROUPING>
      <RULE-AUTONOMY fact="true" status="impl/done">**Autonomy on routine changes only** — commit and push routine work without asking; stop and ask for history rewrites, force-push, large blobs, CI/signing changes, and anything whose reversal costs work.</RULE-AUTONOMY>
    </facts>
  </section>
  <section title="Files you MUST NOT touch without explicit instruction">
    <facts ordered="false">
      <NOTOUCH-REFS-BOOK fact="true" status="impl/done">`refs/book/` — the user's book, read-only reference material.</NOTOUCH-REFS-BOOK>
    </facts>
  </section>
  <section title="Units marked `void` — do not implement">
    <facts ordered="false">
      <VOID-NOT-WORK fact="true" status="impl/done">A unit whose state is `void` (`@spec/void`, or `state="void"` in a `&lt;status&gt;` element) **asserts nothing** and is not work. It was either split into heirs and left as a pointer to them, or cancelled with no replacement; its text survives only so its name is not reused and inbound links do not break. Do not implement it, do not plan from it, do not count it as outstanding.</VOID-NOT-WORK>
      <VOID-TEXT-STILL-READS fact="true" status="impl/done">The trap, and the only reason this rule is needed: the prose is unchanged. A tombstone still reads like the requirement it used to be, because it was one. The marker is the only thing saying otherwise, so read it before acting on the paragraph.</VOID-TEXT-STILL-READS>
      <VOID-FOLLOW-HEIRS fact="true" status="impl/done">If the unit names successors, they carry the claim and the work is theirs. If it names none, the claim was withdrawn and there is nothing to do.</VOID-FOLLOW-HEIRS>
    </facts>
  </section>
  <section title="Reading layers (per book, `refs/book/`)">
    <p><reading-layers-lead fact="true" status="impl/done">vibevm's instance of the **two-process-model** flow (`spec://org.vibevm.world/two-process-model/flows/two-process-model/TWO-PROCESS-MODEL#root`) — human and agent as two processes sharing one repository; these are its reading layers, information flowing top-down, the human winning conflicts:</reading-layers-lead></p>
    <facts ordered="false">
      <LAYER-HEAD fact="true" status="impl/done">**Head** (human's memory) — not your concern, but respect that it exists. Human wins conflicts with the spec.</LAYER-HEAD>
      <LAYER-WAL fact="true" status="impl/done">**WAL** (`spec/WAL.xml`) — volatile, rewritten each session, describes *current* state.</LAYER-WAL>
      <LAYER-SPEC fact="true" status="impl/done">**Spec** (other files under `spec/`) — stable decisions, addressable via `spec://…` URIs.</LAYER-SPEC>
      <LAYER-CODE fact="true" status="impl/done">**Code** (everything under `crates/`, including each crate's own `tests/`) — artefacts. Losing them is inconvenient; losing the spec is a catastrophe.</LAYER-CODE>
    </facts>
    <p><SYNC-FROM-CODE-PATH fact="true" status="impl/done">Information flows top-down. If code changes first, reconcile up via the **sync-from-code** flow (`spec://org.vibevm.world/sync-from-code/flows/sync-from-code/SYNC-PROTOCOL#root`; also `refs/book/` chapter 3) — propose a spec update, do not rewrite code back.</SYNC-FROM-CODE-PATH></p>
  </section>
  <section title="Hard conventions">
    <facts ordered="false">
      <CONV-LANGUAGE fact="true" status="impl/done">**Language:** Rust. See [spec://org.vibevm.core/vibevm/common/PROP-000#language](../common/PROP-000.xml#language).</CONV-LANGUAGE>
      <CONV-MANIFESTS fact="true" status="impl/done">**Manifests:** TOML. One `vibe.toml` per node — the role is set by section (`[project]` ⊕ `[package]`, optionally `[workspace]`); lockfile = `vibe.lock`.</CONV-MANIFESTS>
      <CONV-TERMINOLOGY fact="true" status="impl/done">**Terminology:** only six installable kinds — `flow`, `feat`, `stack`, `tool`, `mcp`, `lang` (the register grows only by owner amendment to `VIBEVM-SPEC.md` §4.1; `app` is anticipated). Never say "lifecycle", "phase", "goal", "plugin" (except that "plugin" == "package" in passing context). See `VIBEVM-SPEC.md` §4.</CONV-TERMINOLOGY>
      <CONV-REPO-URLS fact="true" status="impl/done">**Repository URLs:** vibevm source = `git@gitverse.ru:vibevm/vibevm.git` / `https://gitverse.ru/vibevm/vibevm`. Package registry = the GitHub organization `https://github.com/vibespecs` (deliberate split-host posture — see `spec/boot/90-user.xml` and [PROP-000 §7](../common/PROP-000.xml#registry)). The legacy GitVerse monorepo `git@gitverse.ru:anarchic/vibespecs.git` is read-only transition state.</CONV-REPO-URLS>
    </facts>
  </section>
  <section title="Uncertainty protocol">
    <p><uncertainty-lead fact="true" status="impl/done">When the spec is silent on a question:</uncertainty-lead></p>
    <facts ordered="true">
      <UNC-STEP-SPEC fact="true" status="impl/done">Re-read the relevant section of `VIBEVM-SPEC.md`.</UNC-STEP-SPEC>
      <UNC-STEP-BOOK fact="true" status="impl/done">Re-read the relevant chapter in `refs/book/`.</UNC-STEP-BOOK>
      <UNC-STEP-ANALOGS fact="true" status="impl/done">Look at the closest analog under `refs/src/` (cargo, uv, spec-kit).</UNC-STEP-ANALOGS>
      <UNC-STEP-REVIEW-MARKER fact="true" status="impl/done">If still unclear: mark the decision with `&lt;!-- REVIEW: … --&gt;`, pick the conservative interpretation, proceed, flag in the end-of-session report. Never silently invent semantic behavior.</UNC-STEP-REVIEW-MARKER>
    </facts>
  </section>
  <section title="End of session">
    <facts ordered="false">
      <EOS-WAL-REWRITE fact="true" status="impl/done">Update `spec/WAL.xml` to reflect the *current* state (rewrite, not append — it is a checkpoint).</EOS-WAL-REWRITE>
      <EOS-MILESTONE-COMMIT fact="true" status="impl/done">Propose a milestone commit if work is a logical unit. For routine work, follow rule 4 above: commit and push using rules 2–3. For non-routine operations, stop and ask the user first.</EOS-MILESTONE-COMMIT>
    </facts>
  </section>
</spec>
