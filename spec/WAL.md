# WAL — Project Continuation State {#root}

_Updated: 2026-08-17 (**Ф4.2b: шесть шагов из семи**. Слой преобразований
построен на девять десятых — открытие словаря, snake_case, `BTreeMap`,
политика пустого, формы опционального, строгость по реестру; седьмой шаг
(доменные типы) **замерен и нарезан, но не начат**. Семнадцать коммитов, шесть
посадок, панель зелёная на каждой. Шесть делегированных пакетов — шесть приняты
первым проходом, ноль циклов доработки.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета целиком → короткий доклад → **сразу исполнение**.
Промт есть слово владельца, отдельного «да» ждать не нужно. @status:impl/done

@fact:WAL-THE-LAW **PROP-044 — ратифицированный стоячий закон каждого формата**
(2026-08-13). Терминология §2b обязательна: snapshot ↔ frozen — антонимы
**одной булевой оси без третьего состояния**; «канал» — авторский указатель
версий (PROP-005 §2.18); capture/named ref — провайдерское; режим
материализации — `copy`. @status:impl/done

@fact:WAL-RULE-OLDER-THAN-NUMBER **Правило старше собственного числа — и это
доказано дважды за одну сессию.** Пакет говорил «снять все 309 rename'ов» —
дерево дало 308, потому что свойство схемы зовётся `ref` (ключевое слово Rust,
эскейп `ref_`), и снятие уронило бы провод формата без оракула. Пакет
перечислял два класса опционального поля — дерево дало три. Оба раза выиграло
ПРАВИЛО, записанное в пакете, а не число рядом с ним. Отсюда стоячая клауза
§0.9 всякого пакета: **число расходится с деревом — верь дереву, исполняй
правило, назови расхождение**. @status:impl/done

@fact:WAL-EMPTY-OUTPUT-IS-A-CLAIM **Пустой вывод измерителя есть УТВЕРЖДЕНИЕ и
меряется как всякое другое.** Скрипт находки `f4-transform-radius` §11 не мог
сработать ни на одном поле (якорь `$` сразу после двоеточия), и его молчание
пять сессий читалось как «исключений ноль». Прежде чем читать пустой вывод
доказательством, скорми инструменту случай, который он ОБЯЗАН пометить. Три
ложных утверждения находки помечены на месте. @status:impl/done

@fact:WAL-CLAIM-ABOUT-A-LANGUAGE-LIMIT **Утверждение о пределе языка тоже есть
утверждение.** Схема журнала записала, что у JTD нет `nullable` и нет формы
«present-but-null», и на этом основании расхождение `removed.version` было
подшито как «recorded, not resolved». Ложь: `nullable` — штатный флаг RFC 8927,
и `schemas/list_report.jtd.json` его уже применяет. Исправлено на месте,
разрешимая правка записана как `BACKLOG.md` **B-078**. Соседнее утверждение
того же рода (B-056: у JTD нет 64-битного целого) проверено и подтвердилось —
проверять надо каждое. @status:impl/done

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста
cargo xtask mirror --check
```

## Current phase {#current-phase}

@fact:WAL-PHASE **Change-native стройка, ТЗ №1: Ф0–Ф3 закрыты; в фазе Ф4
закрыты Ф4.0, Ф4.1, Ф4.2a и шаги Ф4.2b-0 … Ф4.2b-6. Следующий шаг —
Ф4.2b-7 (доменные типы по `x-rust-type`), он ЗАМЕРЕН и НАРЕЗАН.** Порядок
внутри фазы жёсткий, каждый шаг — отдельным коммитом; граница ФАЗЫ — полная
панель + правка карты + раскатка. @status:impl/done

@fact:WAL-LAYER-STATE **Слой преобразований — девять из десяти.** В
`xtask/src/codegen/` живут восемь модулей пасса плюс раскладка, словник и
драйвер: `postproc` (драйвер + боксирование), `snake_case`, `ordered_maps`,
`empty_policy`, `optional_shapes`, `strictness`, `open_vocabulary`. Порядок —
ПРАВИЛО, записанное в шапке `postproc.rs`: пасс, привязанный к форме эмиссии,
работает пока файл ЕЩЁ ЕСТЬ выход генератора; открытие словарей вписывает
рукописный Rust и потому идёт последним. Слой применяется только к НАШЕМУ дому
схем (`FormatOwner::{Ours,Foreign}`) — дом движка не получает политику вовсе. @status:impl/done

@fact:WAL-TREE-NUMBERS **Состояние сгенерированного дерева (перемеряется
одной командой каждое):** `Option<Box<` — **0**; `Box<` — 15, и все они плечи
двух дискриминаторных объединений; `Unknown(String)` — **11** открытых
словарей при 3 закрытых; camelCase-полей — **0**; `serde(rename)` — **24**
(23 вариантных + один полевой, носитель провода `"ref"`); `HashMap` — **0**.
Аннотации: `x-vocabulary` 5, `x-empty` 31, `x-default` 21 — долга нет. @status:impl/done

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист,
панель `self-check: all green` с реальным кодом выхода **0** на посадке
`0fd7ce2d`; корпус 281 файл, 13788 маркеров, 0 неразмеченных; долг **0
неосуждённых, 0 осиротевших**, 34 stale; `vibe check` — 0 errors, 1 warning,
44 info; карта — 6061 спек-юнит / 1038 tagged / 976 рёбер / 0 подозрений /
0 сирот. **17 коммитов впереди зеркал — раскатка сделана этим сворачиванием.** @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты — не
чинить.** *(i)* `vibe check` даёт **44 info** `manifest_epoch` — до-эпоховые
манифесты под `packages/`. *(ii)* Одно **warning** `local_source_freshness` по
`org.vibevm.world/addressable-specs`. **За тридцать три посадки подряд оба не
сдвинулись ни разу. Рост любого из них — находка, а не шум.** @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-F42B7 **Ф4.2b-7 — доменные типы по `x-rust-type`, последний
   шаг блока.** Замер целиком в §7 плана (решение **Р22**); нарезанный пакет —
   `cache/agents/sorted/F42B7-DOMAIN-TYPES/2026-08-17-cut-not-run-packet.md`.
   Два плеча (правая часть псевдонима против имени типа), один дефект данных
   (`x-rust-type: "Group"` называет собственный псевдоним), радиус ВПЕРВЫЕ
   выходит за `generated/**`: импорт оракула плюс три зависимости `vibe-wire`. @status:spec/plan
2. @fact:WAL-NEXT-F42C **Ф4.2c — реэкспорт рукописных типов** и
   сгенерированные ридеры трёх поверхностей без ридера (G11). Меняет граф
   типов продукта: `vibe-index` получает РАНТАЙМНОЕ ребро на `vibe-wire`. Его
   же триггер — именованный дефер имени варианта `KindName0`. @status:spec/plan
3. @fact:WAL-NEXT-INDEPENDENT **Независимая полоса — и её нельзя вести
   параллельно фазе** (измерено): S6 заводит запись в `formats/REGISTRY.toml`,
   а она перегенерирует `crates/vibe-wire/src/generated/format_id/mod.rs` —
   тот самый файл, который держит дерево Ф4.2b. S1 и S6 режутся холодными
   МЕЖДУ шагами; S7 — судейская, в любой момент, и она же закрывает долг
   печати. @status:impl/done
4. @fact:WAL-NEXT-OWNER **За владельцем три вопроса, ни один не блокирует:**
   S2 (org-права), **Ж8** (`--full` и исчезнувшая запись) и **B-056** (язык
   схем не выражает `u64`). @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-PROP044-IS-THE-LAW **Вся идеология форматов — ратифицированный
  PROP-044**; здесь не пересказывается. Терминология §2b обязательна. @status:impl/done
- @fact:WAL-C-SCHEMA-DESCRIBES-THE-WIRE **Схема описывает ПРОВОД, и это
  разделяющий критерий.** Аннотации политики (`x-vocabulary`, `x-empty`,
  `x-default`, `x-rust-type`, `x-vocabularies`) говорят что-то о ФОРМАТЕ.
  Боксирование, канонический порядок ключей и snake_case — вопросы с ровно
  одним законным ответом, и потому они НЕ аннотации, а безусловные
  преобразования. Всякое будущее «а не вписать ли это в схему» проверяется
  этим вопросом. @status:impl/done
- @fact:WAL-C-LAYER-IS-SCOPED-BY-HOME **Слой ключится ДОМОМ схемы** (Р10/Р19),
  и это связывает ВЕСЬ слой, а не отдельный пасс: дом движка не проходит
  пост-обработку вовсе. Дома называют своего владельца там же, где названы их
  каталоги, — третий дом обязан назвать его, а не получить умолчанием. @status:impl/done
- @fact:WAL-C-PASS-ORDER **Порядок пассов — закон:** привязанный к форме
  эмиссии работает, пока файл ЕЩЁ ЕСТЬ выход генератора; открытие словарей
  пишет рукописный Rust и идёт последним. @status:impl/done
- @fact:WAL-C-SITE-COUNT-TRIPWIRE **У каждого пасса есть сторож счёта:** число
  сайтов схемы обязано сойтись с числом найденных в Rust. Это он делает
  пропуск объединений НАЗВАННЫМ правилом, а не совпадением. @status:impl/done
- @fact:WAL-C-VOCABULARY-ONE-HOME **Словарь живёт в ОДНОМ доме
  (`formats/vocabularies.json`), подстановку делает наш слой, и она
  ТРАНЗИТИВНА**; средствами JTD это неисправимо. @status:impl/done
- @fact:WAL-C-ROUTE-BY-PATH **Маршрут выхода ключится ДОМОМ схемы и её путём,
  а не стемом** — каталог схемы несёт эпоху (PROP-044 §4.6). @status:impl/done
- @fact:WAL-C-CODEGEN-STAGING **`check-codegen` — это `git diff --exit-code`,
  рабочее дерево против ИНДЕКСА, и untracked он НЕ ВИДИТ.** Слайс, трогающий
  `generated/**`, садится в порядке **стейдж → панель → коммит**. @status:impl/done
- @fact:WAL-C-JOURNAL-IS-THE-TRUTH **Каталог — проекция журнала, и починка
  идёт в одну сторону.** Если `rebuild --check` разошёлся, неправ КАТАЛОГ. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки:** применить дифф →
  `cargo fmt --all` → **`cargo xtask specmap`** → стейдж → панель → суд →
  коммит. Карта, пересобранная ДО того как лёг код, — карта дерева, которого
  уже нет. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель ОБРЫВАЕТСЯ на первом красном
  шаге** — зелёный хвост единственное доказательство, что прогнались все. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ** — красное на два таргета требует `--no-fail-fast`. И
  **`cargo test -p <крейт>` печатает НЕСКОЛЬКО строк `test result`** — цитируй
  все, одна прочитывается как «тесты не добавлены». @status:impl/done
- @fact:WAL-C-FMT-CHECK-IS-THE-GATE **`cargo fmt` без `--check` выходит с нулём
  и когда результат НЕ чист.** Файл, вырезанный текстом, начинать шапкой `//!`. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён
  тем же заходом**: scan → mirror → batch JSON → `merge-verdicts.py` → долг 0.
  **`campaigns/**`, `BACKLOG.md` и `spec/WAL.md` в судимый корпус НЕ входят.** @status:impl/done
- @fact:WAL-C-EVIDENCE-CITES-SYMBOLS **Evidence цитирует ИМЯ СИМВОЛА, а не
  номер строки**, и вердикт, заменяющий прежний, объясняет замену. @status:impl/done
- @fact:WAL-C-HONEST-SEAL **Печать — только за проверенное**; сейчас она
  сознательно не ставится (см. `##WAL-KI-SEAL-DEBT`). @status:impl/done
- @fact:WAL-C-SPECMAP **Правка текста спеки и новый код двигают карту**:
  `cargo xtask specmap` в той же посадке. В `xtask/**` нет ни одного
  `specmark::scope!` — новые файлы там его НЕ несут (измерено). @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ;
  режим — `SUBAGENT-MODE.toml` (`claudez`); пакеты режет босс; воркеры не
  запускают git; диффы читает, правит и коммитит босс; ревью не делегируется
  никогда. §8 несёт **50 размеченных фактов**, §9 — разбор чистого фан-аута. @status:impl/done
- @fact:WAL-C-PACKET-TRAVELS-AS-A-FILE **Пакет едет ФАЙЛОМ, а не аргументом.**
  `claudez -p "$(cat …)"` роняется на `Argument list too long` уже при 34 КБ, и
  отказ приходит от ОС строкой запускалки. Форма: копия в корень worktree
  (`PACKET-<id>.md`) плюс короткий указатель в `-p` С КОПИЕЙ закрывающих клауз. @status:impl/done
- @fact:WAL-C-PACKET-PERIMETER **Закрытый периметр обязан называть и файл,
  который предписанный им раскол СОЗДАСТ, и файл, который предписанная им
  правка СЛОМАЕТ.** Второе оплачено: схлопывание коллекций уронило тест,
  посаженный двумя шагами раньше, и воркеру пришлось выбирать между двумя
  инструкциями одного пакета. Команда замера разлёта
  (`cargo check --workspace --all-targets`) входит в пакет. @status:impl/done
- @fact:WAL-C-PACKET-GIVES-RULE-AND-COORDINATE **Пакет даёт ПРАВИЛО, а
  координату — как его иллюстрацию**, и несёт клаузу §0.9 «число расходится с
  деревом — верь дереву». Дважды за сессию это спасло провод. @status:impl/done
- @fact:WAL-C-PACKET-NAMES-THE-BOOT-IT-NEEDS **Worktree несёт `CLAUDE.md`, и
  воркер грузится по нему раньше, чем прочтёт пакет.** Пакет, компилирующий
  контекст внутрь, обязан сказать, какое чтение ему нужно, а какое он сделал
  избыточным. Клауза работает: за шесть запусков ни один воркер не читал
  статическую полосу. @status:impl/done
- @fact:WAL-C-SELF-VERIFY-FOLLOWS-THE-TOUCH **Self-verify пишется от того, что
  правка ТРОГАЕТ, а не от того, что добавляет.** Правка общих ДАННЫХ обязана
  назвать крейты, чьи тесты на этих данных стоят. @status:impl/done
- @fact:WAL-C-REPORT-BEFORE-GATES-BUT-PENDING **Отчёт пишется РАНЬШЕ финальных
  гейтов, НО каждая строка приёмки начинается словом PENDING.** @status:impl/done
- @fact:WAL-C-RED-PROOF **Красное доказательство — пункт приёмки, и босс
  перемеряет его СВОЕЙ целью.** Где красного честно НЕ БЫВАЕТ (порядок ключей
  против `HashMap` падал бы вероятностно) — это говорится вслух, а красное
  берётся у компилятора: отказ типов сильнее отказа значения. @status:impl/done
- @fact:WAL-C-KILLED-TASK-LEAVES-A-LIVE-WORKER **Убитая harness'ом задача не
  убивает воркера.** Фоновый сторож до стабилизации лога, никогда не
  прибираться вокруг живого процесса, читать worktree. Терминальный сигнал —
  событие `"type":"result"` в stream-json. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: никогда голый `cd` — и опасен не сам
  `cd`, а КАЖДАЯ команда после него (оплачено: `git apply` диффа воркера ушёл
  в worktree; спасла атомарность `git apply`, не дисциплина). Ремонт —
  `cd <абсолютный корень>` с `pwd` и `git rev-parse --abbrev-ref HEAD` в той же
  команде. `cmd | tail; echo $?` печатает код ПАЙПА; правки только
  editor-инструментами; python — с `PYTHONIOENCODING=utf-8`; бюджет файла 600
  строк ПОСЛЕ `cargo fmt`, и он покрывает `tests/**`. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты heredoc
  `-F -`; раскатка только `cargo xtask mirror`; Rules 1–4 связывают каждый
  коммит. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки
  дисциплины не трогаются — и именно этот запрет породил Р10. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-17, 17 коммитов (`42052926`..`57ef3c1b`):** шесть
шагов Ф4.2b (`50b0aa35`, `21b0ac94`, `17193dd8`, `ab9edff6`, `e4b46885`,
`0fd7ce2d`), исправление ложного предела языка (`351a9594`), шесть записей
плана и четыре закона запускалок. Панель зелёная на каждой посадке; `vibe
check` не сдвинулся ни разу. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, `.wt/` пуст, ветки `wt/*`
удалены, дерево чисто. Один worktree под `~/.fractality/runs/**` — чужой, к
этой кампании отношения не имеет и не трогается. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-B078 **B-078 — «у JTD нет nullable» оказалось ложью (P2, open).**
  Ложное утверждение исправлено на месте; разрешимая правка (`removed.version`
  переезжает в `properties` с `"nullable": true`) записана и ждёт своего шага:
  она меняет провод журнала и задевает ветку required-nullable слоя. @status:spec/plan
- @fact:WAL-KI-HELLO-SCHEMA-MISSING **Реестр называет несуществующую схему.**
  `formats/REGISTRY.toml` указывает на `schemas/hello/e1/hello.jtd.json`,
  которой на диске нет; строит её Ф6.1. Пасс строгости пропускает её ПО ИМЕНИ
  с явной строкой в выводе — механизм построен и проверен. @status:impl/done
- @fact:WAL-KI-VARIANT-NAME-NOT-COVERED **Имя ВАРИАНТА генератор выбирает сам,
  и `x-rust-type` его не покрывает.** `KindName0` — коллизионный суффикс для
  wire-строки `"kind/name"`. Дефер с триггером «Ф4.2c». @status:spec/plan
- @fact:WAL-KI-B056 **B-056 — язык схем не выражает `u64` (P2, open).** Три
  выхода измерены; решение владельца. **Класс, а не случай.** @status:spec/plan
- @fact:WAL-KI-TUPLE-GAP **Второй пол JTD: кортежа нет.** `Event::Renamed`
  несёт `(Group, String)`; схема описывает `elements`, теряя арность. @status:impl/done
- @fact:WAL-KI-VOCAB-OUTSIDE-THE-MAP **Единственный дом словаря вне периметра
  карты** — специмап индексирует JTD-схемы под `schemas/`. Дефер по имени. @status:spec/plan
- @fact:WAL-KI-GENERATOR-PIN-UNRECONCILED **Пин генератора не сведён с
  артефактами.** Заявлено `jtd-codegen 0.4.1`, артефакты печатают `v0.2.1`, и
  ни один гейт версию не проверяет — а к форме эмиссии теперь привязаны СЕМЬ
  пассов. Свести до Ф4.2c. @status:spec/plan
- @fact:WAL-KI-J8-OPEN **Ж8 — открытый вопрос владельца.** Что означает
  `--full` для записи, которую скан больше не видит. @status:spec/plan
- @fact:WAL-KI-SEAL-DEBT **Печать суда сознательно не поставлена.**
  `spec/modules/vibe-index/PROP-005-package-index.md` несёт двинувшиеся факты;
  прецедент отката есть (`498e8c8b`). Закрывается S7. @status:impl/done
- @fact:WAL-KI-F43-PERIMETER **Периметр запрета Ф4.3 — 133 файла.** Гейт
  садится рехетом (К4); целевое правило К2 — дефер с триггером «посадка
  Ф4.2». @status:spec/plan
- @fact:WAL-KI-FIVE-SICK-DICTIONARIES **Раздвоенных словарей пять**, и у
  `PackageKind` паритет-теста НЕТ вопреки записанному. Четыре сводит Ф4.2c
  реэкспортом; `SourceKind` — именованный дефер. @status:spec/plan
- @fact:WAL-KI-STALE **34 файла stale** (стоячий долг сдвигов байтов) —
  адресовано кампанией S7. @status:impl/done
- @fact:WAL-KI-B074 **Второй якорь факта в абзаце глотается молча** (B-074). @status:impl/done
- @fact:WAL-KI-B075 **Панель может мигнуть на чистом дереве** (B-075) — читать
  отказ, не перезапускать вслепую. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие
  выходного гейта Phase E, ждёт рулинга. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → документы предмета (PROP-044 + три ТЗ) →
`SUBAGENT-LAUNCHERS.md` целиком перед фан-аутом** — и берёт каждое число из
команд сверху, после rescan. Этот файл главнее `CONTINUE.md`; ратифицированный
PROP-044 главнее обоих. @status:impl/done
