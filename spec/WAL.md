# WAL — Project Continuation State {#root}

_Updated: 2026-08-17 (**блок Ф4.2b закрыт целиком — семь шагов из семи**; фаза
Ф4.2c замерена, отсужена, нарезана, и два её шага из четырёх сели. Слой
преобразований — девять пассов. Двенадцать коммитов, три посадки кода, панель
зелёная на каждой. Шлюз GLM лежал весь день: один делегированный пакет, два
прогона убиты HTTP 529, хвост забран боссом по калькуляции, а не по нетерпению.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета целиком → короткий доклад → **сразу исполнение**.
Промт есть слово владельца, отдельного «да» ждать не нужно. @status:impl/done

@fact:WAL-THE-LAW **PROP-044 — ратифицированный стоячий закон каждого формата**
(2026-08-13). Терминология §2b обязательна: snapshot ↔ frozen — антонимы
**одной булевой оси без третьего состояния**; «канал» — авторский указатель
версий (PROP-005 §2.18); capture/named ref — провайдерское; режим
материализации — `copy`. @status:impl/done

@fact:WAL-INSTRUMENT-MEASURES-THE-OTHER-THING **Класс ошибки, пойманный дважды
за две сессии: инструмент мерит НЕ ТУ величину, а его вывод читают как ответ.**
Прошлая сессия: скрипт находки не мог сработать ни на одном поле, и его молчание
пять сессий читалось как «исключений ноль». Эта сессия: пин генератора «не сходился
с артефактами» — бинарь отвечает `jtd-codegen 0.4.1`, и ОН ЖЕ пишет в шапку
`for Rust v0.2.1`, потому что это версия Rust-**таргета**; две величины разных
вещей сравнивались как одна, тоже пять сессий. **Лечение одно: прежде чем читать
вывод доказательством, скорми инструменту случай, который он ОБЯЗАН различить.**
@status:impl/done

@fact:WAL-RULE-OLDER-THAN-NUMBER **Правило старше собственного числа.** Пакет
даёт ПРАВИЛО, а координату — как его иллюстрацию, и несёт стоячую клаузу §0.9:
**число расходится с деревом — верь дереву, исполняй правило, назови
расхождение**. @status:impl/done

@fact:WAL-A-PASS-REMOVES-EXACTLY-WHAT-IT-MUST **Закон, найденный этой сессией и
уже сработавший дважды: пасс снимает РОВНО то, что обязан.** Доменные типы
снимают импортные items, осиротевшие ИХ подстановкой, — не «подчищают импорты».
Открытие словаря снимает серде-пару, а не всю строку derive, — иначе открытый
словарь оказался бы единственным типом, который нельзя ни напечатать, ни
сравнить. Один закон, два слоя. @status:impl/done

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста
cargo xtask mirror --check
```

## Current phase {#current-phase}

@fact:WAL-PHASE **Change-native стройка, ТЗ №1: Ф0–Ф3 закрыты; в фазе Ф4
закрыты Ф4.0, Ф4.1, Ф4.2a и ВЕСЬ блок Ф4.2b (семь шагов из семи). Идёт Ф4.2c:
`-1` и `-2` сели, входом следующей сессии служит `-3`.** Порядок внутри фазы
жёсткий, каждый шаг — отдельным коммитом; граница ФАЗЫ — полная панель + правка
карты + раскатка. @status:impl/done

@fact:WAL-LAYER-STATE **Слой преобразований — ДЕВЯТЬ пассов.** В
`xtask/src/codegen/`: `postproc` (драйвер + боксирование), `snake_case`,
`ordered_maps`, `empty_policy`, `optional_shapes`, `strictness`, `domain_types`
(с подмодулями `rulings`, `variants`), `derive_floor`, `open_vocabulary`.
Порядок — ПРАВИЛО, записанное в шапке `postproc.rs`: пасс, привязанный к форме
эмиссии, работает пока файл ЕЩЁ ЕСТЬ выход генератора; открытие словарей
вписывает рукописный Rust и потому идёт последним. Слой применяется только к
НАШЕМУ дому схем (`FormatOwner::{Ours,Foreign}`) — дом движка не получает
политику вовсе, и это проверяется побайтовым `diff -r` на каждой посадке. @status:impl/done

@fact:WAL-SLOT-IS-FORCED-NOT-CHOSEN **Слот пасса бывает ВЫНУЖДЕННЫМ, и это
сильнее «показалось уместным».** Трейт-этаж: `strictness` якорится на
НЕТРОНУТУЮ строку derive ⇒ этаж после него; `open_vocabulary` обязан УВИДЕТЬ
расширенную строку ⇒ этаж до него. Между ними ровно один слот. Всякий новый
пасс выводит своё место так же, а не выбирает. @status:impl/done

@fact:WAL-TREE-NUMBERS **Состояние сгенерированного дерева (каждое число одной
командой):** `Option<Box<` — **0**; `Box<` — 15, все плечи двух дискриминаторных
объединений; `Unknown(String)` — **11**; camelCase-полей — **0**;
`serde(rename)` — 24; `HashMap` — **0**; `KindName0` — **0**.
Derive: 74 типа несут `Debug, Clone, PartialEq, Eq, Serialize, Deserialize`,
11 открытых словарей — бессерде-этаж `Debug, Clone, PartialEq, Eq`, 2 реестровых
типа (`FormatId`, `ForeignParsers`) свой собственный набор.
Аннотации: `x-vocabulary` 5, `x-empty` 31, `x-default` 21, `x-rust-type` 8,
`x-rust-variants` 1 — долга нет ни на одной оси. @status:impl/done

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист,
панель `self-check: all green` с реальным кодом выхода **0** на посадке
`dca804db`; корпус 281 файл, 13788 маркеров, 0 неразмеченных; долг **0
неосуждённых, 0 осиротевших**, 34 stale; `vibe check` — 0 errors, 1 warning,
44 info; карта — 6061 спек-юнит / 1038 tagged / 976 рёбер / 0 подозрений /
0 сирот. **Зеркала на 12 коммитов позади — раскатка сделана этим сворачиванием.** @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты — не
чинить.** *(i)* `vibe check` даёт **44 info** `manifest_epoch` — до-эпоховые
манифесты под `packages/`. *(ii)* Одно **warning** `local_source_freshness` по
`org.vibevm.world/addressable-specs`. **За тридцать шесть посадок подряд оба не
сдвинулись ни разу. Рост любого из них — находка, а не шум.** @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-F42C3 **Ф4.2c-3 — реэкспорт, слой поведения, ребро и
   `--kind`. ОДНИМ коммитом, и это не укрупнение:** перенос метода в
   `vibe-wire` без реэкспорта оставил бы поведение в двух определениях,
   реэкспорт без переноса не собрался бы вовсе — орфанное правило. Решения
   Р24–Р27 записаны; замер — `harvest/f42c-reexport-radius.md`. @status:spec/plan
2. @fact:WAL-NEXT-F42C4 **Ф4.2c-4 — три поверхности без ридера (G11):**
   `by-cap`, `by-purl` и `primary.jsonl.gz`. Отдельный шаг: его предмет не
   типы, а поверхности, и приёмка другая — не «байты не двинулись», а
   «опубликованное читается обратно». @status:spec/plan
3. @fact:WAL-NEXT-F43 **Ф4.3 — запрет рукописного провода**, рехетом по крейтам
   (К4); периметр — 133 файла; целевое К2 — дефер с триггером «посадка Ф4.2». @status:spec/plan
4. @fact:WAL-NEXT-INDEPENDENT **Независимая полоса — и её нельзя вести
   параллельно фазе** (измерено): S6 заводит запись в `formats/REGISTRY.toml`,
   а она перегенерирует `crates/vibe-wire/src/generated/format_id/mod.rs`.
   S1 и S6 режутся холодными МЕЖДУ шагами; S7 — судейская, в любой момент, и
   она же закрывает долг печати. @status:impl/done
5. @fact:WAL-NEXT-OWNER **За владельцем три вопроса, ни один не блокирует:**
   S2 (org-права), **Ж8** (`--full` и исчезнувшая запись) и **B-056** (язык
   схем не выражает `u64`); рядом — **B-078**, разрешимая правка провода
   журнала. @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-PROP044-IS-THE-LAW **Вся идеология форматов — ратифицированный
  PROP-044**; здесь не пересказывается. Терминология §2b обязательна. @status:impl/done
- @fact:WAL-C-SCHEMA-DESCRIBES-THE-WIRE **Схема описывает ПРОВОД, и это
  разделяющий критерий.** Аннотации политики (`x-vocabulary`, `x-empty`,
  `x-default`, `x-rust-type`, `x-rust-variants`, `x-vocabularies`) говорят
  что-то о ФОРМАТЕ. Боксирование, канонический порядок ключей, snake_case и
  **трейт-этаж** — вопросы с ровно одним законным ответом, и потому они НЕ
  аннотации, а безусловные преобразования. Всякое будущее «а не вписать ли это
  в схему» проверяется этим вопросом. @status:impl/done
- @fact:WAL-C-GENERATOR-EMITS-FORM-NOT-BEHAVIOUR **Генератор эмитит ФОРМУ и
  никогда ПОВЕДЕНИЕ**, и эта линия только что провела границу трейт-этажа:
  `Debug`/`Clone`/`PartialEq`/`Eq` — форма, а `Default` — суждение о типе
  («есть ли у него осмысленное пустое значение»), поэтому `Default` живёт
  рукописным impl'ом рядом с `generated/`. Тот же довод раньше не пустил в
  генератор предикаты `is_empty`. @status:impl/done
- @fact:WAL-C-DECLARED-NOT-DERIVED **Имя, которое выбирает человек,
  ОБЪЯВЛЯЮТ, а не выводят.** `x-rust-type` объявляет имя типа, `x-rust-variants`
  — имя варианта; ни для одного нет законного правила вывода, а выдуманное
  правило регистра внутри пасса переизобретает правило именования генератора,
  от которого пасс как раз защищается (Р16). Ключ карты — **wire-значение**,
  никогда минтованное имя. @status:impl/done
- @fact:WAL-C-SCOPE-OF-A-RENAME **Охват переименования решает область
  видимости имени.** Имя ТИПА уникально в файле по построению ⇒ переименование
  файловое. Имя ВАРИАНТА принадлежит своему enum'у ⇒ переименование
  ограничено телом объявления; тот же токен снаружи — другая вещь. @status:impl/done
- @fact:WAL-C-ANNOTATION-RESOLVES-ALONE **Аннотация называет САМОДОСТАТОЧНЫЙ
  путь** (Р23): `vibe_core::Group`, `semver::Version`,
  `chrono::DateTime<chrono::Utc>`. Аннотация, чья разрешимость держится
  импортом ГЕНЕРАТОРА, ломается при сдвиге формы эмиссии — ровно та связь,
  ради разрыва которой слой построен. @status:impl/done
- @fact:WAL-C-DESCRIPTION-BECOMES-A-DOC-COMMENT **Описание схемы едет в
  док-комментарий эмитированного файла и проходит через ТЕ ЖЕ переименования,
  что и код.** Процитировать в описании идентификатор, который пасс переименует,
  значит выпустить док, называющий несуществующее. @status:impl/done
- @fact:WAL-C-LAYER-IS-SCOPED-BY-HOME **Слой ключится ДОМОМ схемы** (Р10/Р19),
  и это связывает ВЕСЬ слой, а не отдельный пасс: дом движка не проходит
  пост-обработку вовсе. Проверяется `diff -r` против снимка ДО на каждой
  посадке. @status:impl/done
- @fact:WAL-C-SITE-COUNT-TRIPWIRE **У каждого пасса есть сторож.** У словарного
  — счёт сайтов; у трейт-этажа — постусловие «нетронутой строки derive не
  осталось»; у доменных типов — отказ на аннотации без объявления. Сторож
  делает пропуск НАЗВАННЫМ правилом, а не совпадением. @status:impl/done
- @fact:WAL-C-VOCABULARY-ONE-HOME **Словарь живёт в ОДНОМ доме
  (`formats/vocabularies.json`), подстановку делает наш слой, и она
  ТРАНЗИТИВНА**; средствами JTD это неисправимо. **Следствие для сообщений:**
  отказ, называющий только авторскую схему, отправляет автора в файл, где
  аннотации нет, — называть надо оба дома. @status:impl/done
- @fact:WAL-C-ROUTE-BY-PATH **Маршрут выхода ключится ДОМОМ схемы и её путём,
  а не стемом** — каталог схемы несёт эпоху (PROP-044 §4.6). @status:impl/done
- @fact:WAL-C-CODEGEN-STAGING **`check-codegen` — это `git diff --exit-code`,
  рабочее дерево против ИНДЕКСА, и untracked он НЕ ВИДИТ.** Слайс, трогающий
  `generated/**`, садится в порядке **стейдж → панель → коммит**. Его отказ
  теперь называет ОБЕ причины (схемы двинулись против «генератор не тот») —
  прежний единственный рецепт у контрибьютора с другой сборкой на PATH вписал
  бы чужую эмиссию в дерево под видом дрейфа. @status:impl/done
- @fact:WAL-C-GENERATOR-PIN **Пин генератора СВЕДЁН, гейта на версию нет
  НАМЕРЕННО.** Дом пина
  (`packages/org.vibevm.ai-native/jtd-codegen/v0.1.0/README.md`
  `##PINNED-VERSION`) запрещает пересказывать число в дереве потребителя и
  прямо объявляет, что CI не принуждает сборку генератора. Настоящее сведение
  сильнее сравнения строк и стоит в панели: зелёный `check-codegen` утверждает
  побайтовое равенство артефактов сегодняшней эмиссии. @status:impl/done
- @fact:WAL-C-JOURNAL-IS-THE-TRUTH **Каталог — проекция журнала, и починка
  идёт в одну сторону.** Если `rebuild --check` разошёлся, неправ КАТАЛОГ. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки:** применить дифф →
  `cargo fmt --all` → **`cargo xtask specmap`** → стейдж → панель → суд →
  коммит. Карта, пересобранная ДО того как лёг код, — карта дерева, которого
  уже нет. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель ОБРЫВАЕТСЯ на первом красном
  шаге** — зелёный хвост единственное доказательство, что прогнались все. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ** — красное на два таргета требует `--no-fail-fast`. И
  **`cargo test -p <крейт>` печатает НЕСКОЛЬКО строк `test result`** — цитируй
  все. @status:impl/done
- @fact:WAL-C-FMT-CHECK-IS-THE-GATE **`cargo fmt` без `--check` выходит с нулём
  и когда результат НЕ чист.** Файл, вырезанный текстом, начинать шапкой `//!`. @status:impl/done
- @fact:WAL-C-FIXTURES-ARE-REAIMED-NOT-SATISFIED **Тест, покрасневший на
  законном изменении, ПЕРЕНАЦЕЛИВАЮТ, а не удовлетворяют** — и проверяют, что
  перенацеленный ещё падает на настоящем нарушении. Дважды за сессию это дало
  правку КОНСТРУКЦИИ, а не фикстуры. @status:impl/done
- @fact:WAL-C-EXPECTATIONS-DO-NOT-RESTATE-A-NEIGHBOUR **Ожидание одного пасса
  не пересказывает вывод другого.** Иначе каждый будущий пасс, трогающий все
  типы, правил бы ожидания всех остальных, и связанность растёт как квадрат их
  числа: нормализуй чужой этаж в ОДНОМ месте упряжи. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён
  тем же заходом**: scan → mirror → batch JSON → `merge-verdicts.py` → долг 0.
  **`campaigns/**`, `BACKLOG.md` и `spec/WAL.md` в судимый корпус НЕ входят.** @status:impl/done
- @fact:WAL-C-EVIDENCE-CITES-SYMBOLS **Evidence цитирует ИМЯ СИМВОЛА, а не
  номер строки**, и вердикт, заменяющий прежний, объясняет замену. @status:impl/done
- @fact:WAL-C-HONEST-SEAL **Печать — только за проверенное**; сейчас она
  сознательно не ставится (см. `##WAL-KI-SEAL-DEBT`). @status:impl/done
- @fact:WAL-C-SPECMAP **Правка текста спеки и новый код двигают карту**:
  `cargo xtask specmap` в той же посадке. В `xtask/**` и в `crates/vibe-wire/**`
  нет ни одного `specmark::scope!` — новые файлы там его НЕ несут (измерено). @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ;
  режим — `SUBAGENT-MODE.toml` (`claudez`); пакеты режет босс; воркеры не
  запускают git; диффы читает, правит и коммитит босс; ревью не делегируется
  никогда. §8 несёт **53** размеченных факта, §9 — разбор чистого фан-аута. @status:impl/done
- @fact:WAL-C-GATEWAY-529-IS-A-THIRD-CLASS **Отказ шлюза — ТРЕТИЙ способ
  завершения прогона**, не завершение и не kill: `result`-событие приходит
  (значит процесс кончился и `-c` безопасен немедленно), но несёт
  `terminal_reason: api_error` и `api_error_status: 529`, а harness печатает
  голый exit 1. Забирает он ровно ХВОСТ — то есть верификацию, которую пакет
  кладёт последней. @status:impl/done
- @fact:WAL-C-DEAD-TRANSPORT-REOPENS-THE-CALCULUS **Умерший транспорт
  переоткрывает КАЛЬКУЛЯЦИЮ, а не переотправляет пакет.** Провалившийся прогон
  меняет ОСТАВШУЮСЯ задачу, поэтому вопрос «делегировать или взять» задаётся
  заново про остаток, а не наследуется от исходного пакета. И то, чем он решён,
  говорится вслух. @status:impl/done
- @fact:WAL-C-PACKET-TRAVELS-AS-A-FILE **Пакет едет ФАЙЛОМ, а не аргументом.**
  `claudez -p "$(cat …)"` роняется на `Argument list too long` уже при 34 КБ, и
  отказ приходит от ОС строкой запускалки. Форма: копия в корень worktree
  (`PACKET-<id>.md`) плюс короткий указатель в `-p` С КОПИЕЙ закрывающих клауз. @status:impl/done
- @fact:WAL-C-PACKET-PERIMETER **Закрытый периметр обязан называть и файл,
  который предписанный им раскол СОЗДАСТ, и файл, который предписанная им
  правка СЛОМАЕТ.** Команда замера разлёта
  (`cargo check --workspace --all-targets`) входит в пакет и окупается дважды:
  её молчание про остальные крейты доказывает то, что пакет утверждал прозой. @status:impl/done
- @fact:WAL-C-REVIEW-IS-A-RUN **Дифф, прочитанный БЕЗ прогона, есть
  свидетельство о намерении, а не о поведении.** Воркер может отдать тесты,
  которым его же реализация не удовлетворяет: «воркер ничего не проверил» и
  «артефакты воркера неверны» — разные утверждения, и различает их только
  прогон. @status:impl/done
- @fact:WAL-C-SELF-VERIFY-FOLLOWS-THE-TOUCH **Self-verify пишется от того, что
  правка ТРОГАЕТ, а не от того, что добавляет.** @status:impl/done
- @fact:WAL-C-REPORT-BEFORE-GATES-BUT-PENDING **Отчёт пишется РАНЬШЕ финальных
  гейтов, НО каждая строка приёмки начинается словом PENDING.** @status:impl/done
- @fact:WAL-C-RED-PROOF **Красное доказательство — пункт приёмки, и босс
  перемеряет его СВОЕЙ целью.** Где красного честно НЕ БЫВАЕТ — это говорится
  вслух, а красное берётся у компилятора: отказ типов сильнее отказа значения
  (`E0369` на смене трейт-этажа, `E0308` на смене представления карты). @status:impl/done
- @fact:WAL-C-KILLED-TASK-LEAVES-A-LIVE-WORKER **Убитая harness'ом задача не
  убивает воркера.** Фоновый сторож до стабилизации лога, никогда не
  прибираться вокруг живого процесса, читать worktree. Терминальный сигнал —
  событие `"type":"result"` в stream-json. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: никогда голый `cd` — и опасен не сам
  `cd`, а КАЖДАЯ команда после него. Ремонт — `cd <абсолютный корень>` с `pwd`
  и `git rev-parse --abbrev-ref HEAD` в той же команде. `cmd | tail; echo $?`
  печатает код ПАЙПА; правки только editor-инструментами; python — с
  `PYTHONIOENCODING=utf-8`; бюджет файла 600 строк ПОСЛЕ `cargo fmt`, и он
  покрывает `tests/**`. **Windows иногда держит сгенерированный файл замапленным
  (`os error 1224`) — это не логический отказ, повторить прогон.** @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты heredoc
  `-F -`; раскатка только `cargo xtask mirror`; Rules 1–4 связывают каждый
  коммит. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки
  дисциплины не трогаются — и именно этот запрет породил Р10. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-17, 12 коммитов (`c2c240b3`..`7352052f`):** Ф4.2b-7
(`c2c240b3`), Ф4.2c-1 (`95feb37f`), Ф4.2c-2 (`dca804db`), исправление
одностороннего отказа `check-codegen` (`e8d8238f`), находка радиуса реэкспорта
(`d20ad5f2`), решения Р23–Р27 и нарезка Ф4.2c, три новых факта запускалок
(`a01b39e8`). Панель зелёная на каждой посадке кода; `vibe check` не сдвинулся
ни разу. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, `.wt/` пуст, ветки `wt/*`
удалены, дерево чисто. Два worktree под `~/.fractality/runs/**` — чужие, к этой
кампании отношения не имеют и не трогаются. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-GATEWAY-DOWN **Шлюз z.ai отдавал HTTP 529 весь день** — три
  пробы подряд, включая пустую. Все GLM-маршруты (claudez, fractality,
  opencode) идут через него, значит делегирование было недоступно целиком.
  Проверять пустой пробой перед нарезкой пакета. @status:impl/done
- @fact:WAL-KI-B078 **B-078 — «у JTD нет nullable» оказалось ложью (P2, open).**
  Разрешимая правка (`removed.version` переезжает в `properties` с
  `"nullable": true`) записана и ждёт своего шага: она меняет провод журнала. @status:spec/plan
- @fact:WAL-KI-HELLO-SCHEMA-MISSING **Реестр называет несуществующую схему.**
  `formats/REGISTRY.toml` указывает на `schemas/hello/e1/hello.jtd.json`,
  которой на диске нет; строит её Ф6.1. Пасс строгости пропускает её ПО ИМЕНИ
  с явной строкой в выводе. @status:impl/done
- @fact:WAL-KI-B056 **B-056 — язык схем не выражает `u64` (P2, open).** Три
  выхода измерены; решение владельца. **Класс, а не случай.** @status:spec/plan
- @fact:WAL-KI-TUPLE-GAP **Второй пол JTD: кортежа нет.** `Event::Renamed`
  несёт `(Group, String)`; схема описывает `elements`, теряя арность. @status:impl/done
- @fact:WAL-KI-VOCAB-OUTSIDE-THE-MAP **Единственный дом словаря вне периметра
  карты** — специмап индексирует JTD-схемы под `schemas/`. Дефер по имени. @status:spec/plan
- @fact:WAL-KI-J8-OPEN **Ж8 — открытый вопрос владельца.** Что означает
  `--full` для записи, которую скан больше не видит. @status:spec/plan
- @fact:WAL-KI-SEAL-DEBT **Печать суда сознательно не поставлена.**
  `spec/modules/vibe-index/PROP-005-package-index.md` несёт двинувшиеся факты;
  прецедент отката есть (`498e8c8b`). Закрывается S7. @status:impl/done
- @fact:WAL-KI-F43-PERIMETER **Периметр запрета Ф4.3 — 133 файла.** Гейт
  садится рехетом (К4); целевое правило К2 — дефер с триггером «посадка
  Ф4.2». @status:spec/plan
- @fact:WAL-KI-SOURCEKIND **Из пяти раздвоенных словарей остался ОДИН.**
  `BindingSite`, `PackageKind`, `NamingConvention` и `DeliveryMode` сводит
  Ф4.2c-3 реэкспортом; `SourceKind` — именованный дефер: обе его копии
  принадлежат форматам, которые это ТЗ описывать не берётся. @status:spec/plan
- @fact:WAL-KI-VALUEENUM-COLLISION **`--kind` теряет `clap`-completion.** У
  `vibe-index` флаг выводился через `ValueEnum`, а тот не выводится по варианту
  с нагрузкой; открытый `PackageKind` его несёт. Замена (строка, разбираемая в
  открытый тип) — ратифицированное Б.1 поведение, цена названа заранее и падает
  на Ф4.2c-3. @status:spec/plan
- @fact:WAL-KI-STALE **34 файла stale** (стоячий долг сдвигов байтов) —
  адресовано кампанией S7. @status:impl/done
- @fact:WAL-KI-B074 **Второй якорь факта в абзаце глотается молча** (B-074). @status:impl/done
- @fact:WAL-KI-B075 **Панель может мигнуть на чистом дереве** (B-075) — читать
  отказ, не перезапускать вслепую. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие
  выходного гейта Phase E, ждёт рулинга. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → документы предмета (PROP-044 + три ТЗ) →
`SUBAGENT-LAUNCHERS.md` целиком перед фан-аутом** — и берёт каждое число из
команд сверху, после rescan. Этот файл главнее `CONTINUE.md`; ратифицированный
PROP-044 главнее обоих. @status:impl/done
