# WAL — Project Continuation State {#root}

_Updated: 2026-08-18, вечер (**спека PROP-005 перестала лгать о коде, и это
измерено, а не заявлено**. Мандат называл шесть мест; полный делегированный
проход с контролями вернул **тридцать** в трёх классах. Предусловие §11
закрыто: 70 рулингов плана картированы, семь бездомных получили дом, три
деферала спасены в реестр. 13 коммитов, четыре зелёные панели, 46 вердиктов,
зеркала раскатаны дважды. Осталась одна механическая работа — сама свёртка
плана, и её рецепт лежит в `TASKS.md`.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета целиком → короткий доклад → **сразу исполнение**.
Промт есть слово владельца, отдельного «да» ждать не нужно. @status:impl/done

@fact:WAL-THE-LAW **PROP-044 — ратифицированный стоячий закон каждого формата**
(2026-08-13). Терминология §2b обязательна: snapshot ↔ frozen — антонимы
**одной булевой оси без третьего состояния**; «канал» — авторский указатель
версий (PROP-005 §2.18); режим материализации — `copy`. @status:impl/done

## Current phase {#current-phase}

@fact:WAL-PHASE **Change-native стройка, ТЗ №1: все фазы Ф0–Ф6 закрыты, приёмка
прогнана, предсказания сверены, СПЕК-ДИФФЫ Ф4 и Ф6 закрыты. Осталась одна
работа: §11 — свёртка плана в могильники. Всё, что делало её опасной,
обезврежено и закоммичено; остаток механический.** @status:impl/done

@fact:WAL-THE-SPEC-NO-LONGER-LIES **PROP-005 выправлена против дерева, и
находок оказалось тридцать, а не шесть.** Три класса, и третий точечным грепом
ненаходим по построению: @status:impl/done

1. @fact:WAL-CLASS-BEHIND **Спека ОТСТАЛА от кода** — схема записи стала
   файлом (`schemas/index/e1/entry.jtd.json`), типы генерируются и
   реэкспортируются, лестница обнаружения идёт через хэндшейк, `hello.json`
   живёт в корне каталога и в таблице маршрутов, `PkgKey = (Group, String)`,
   поиск не хранит индекс. Плюс **новая §2.19** — ответ `unavailable`: форма
   строки, какие поверхности его должны, почему сырой файл не должен, почему
   CLI и сервер согласны ПО ПОСТРОЕНИЮ. @status:impl/done
2. @fact:WAL-CLASS-AHEAD **Спека ВПЕРЕДИ кода при `impl/done`** — самый дорогой
   класс, потому что читатель планирует вокруг такого утверждения. Шесть
   якорей переведены в `spec/plan` с названным сегодняшним состоянием:
   `index_url` (секции строгие — пример спеки давал ОТКАЗ РАЗБОРА),
   потребительская сверка по `repomd.json`, HTTP-триггер переиндексации,
   четырёхуровневый резолвер конфигов, fsync каталога. @status:impl/done
3. @fact:WAL-CLASS-SELF-CONTRADICTION **Спека противоречит СЕБЕ** — §2.5
   «вне воркспейса» против §6 «член воркспейса» и против `Cargo.toml:15`.
   Пережило де-рот 2026-05-22, который правил §6 и §9 и пропустил §2.5. @status:impl/done

@fact:WAL-SEVENTY-RULINGS-MAPPED **Предусловие §11 закрыто: 70 рулингов плана
картированы с цитатами** (`spec` 5 · `both` 22 · `code` 36 · `none` 7).
Семь бездомных получили дом в тот же заход; три деферала Приложения Б.6
перенесены в `campaigns/packages-2026-09/deferrals.md`, которого они не
достигали, — свёртка стёрла бы их вместе с триггерами, а мандат следующей
кампании осушается именно из реестра. Карты:
`harvest/plan-mortality-{c,d,section1}.md`. @status:impl/done

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш. И `judging-debt.py` читает ЗЕРКАЛО —
до `mirror` он показывает ноль над реальным долгом:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan   --campaign campaigns/packages-2026-09
cargo run -q -p vibe-cli --bin vibe -- progress mirror --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста, 53 шага
cargo xtask mirror --check
```

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист,
HEAD `9c663b09`, панель `self-check: all green` — **четыре зелёных прогона за
сессию**, каждый 53 шага с реальным кодом выхода 0; корпус 281 файл,
13836 маркеров, 0 неразмеченных; долг **0 неосуждённых, 0 осиротевших**,
34 stale; `vibe check` — 0 errors, 1 warning, 44 info; карта — 6108 спек-юнитов
/ 1042 tagged / 975 рёбер / 0 подозрений / 0 сирот. Зеркала синхронны
(gitverse + github @ `9c663b09`). `.wt/` пуст — все четыре рабочих каталога
сессии сняты чисто. @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты — не
чинить.** `vibe check` даёт **44 info** `manifest_epoch` и **1 warning**
`local_source_freshness`. **За сорок с лишним посадок подряд оба не сдвинулись
ни разу. Рост любого из них — находка, а не шум.** @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-COLLAPSE **§11 — свёртка плана. Единственный незакрытый пункт
   мандата, и рецепт уже написан:** `TASKS.md`, пункт «§11 — смертность плана:
   сама свёртка». Ключевое из него: план читается **ЦЕЛИКОМ** прежде правки
   (свёртка есть переписывание набело); дословно сохраняются §0, §10 и §11;
   могильники берут коммиты из таблиц «Коммиты посадки» трёх карт и дома из
   «Карт домов»; **ссылок на план в `spec/**` ЧЕТЫРЕ, а не две**, как говорит
   §11.2. @status:spec/plan
2. @fact:WAL-NEXT-BACKLOG **Семь развилок владельца, ни одна не блокирует:**
   `BACKLOG` **B-083** … **B-089**. Их суть — в `##WAL-KI-*` ниже. @status:impl/done
3. @fact:WAL-NEXT-INDEPENDENT **Независимая полоса — S1/S6/S7 режутся
   холодными**; **S7** судейская, она же закрывает долг печати и P1-находку
   аудита `2026-08-06-01`. @status:impl/done
4. @fact:WAL-NEXT-OWNER **Живых СТОП-ВЛАДЕЛЕЦ два:** **S2** (org-права) и
   **Ж8** (`--full` и запись, которую скан больше не видит). @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-PROP044-IS-THE-LAW **Вся идеология форматов — ратифицированный
  PROP-044**; здесь не пересказывается. Терминология §2b обязательна. @status:impl/done
- @fact:WAL-C-A-PERIMETER-IS-COUNTED-NOT-NAMED **Когда работа разрушительна,
  периметр считают по ФАЙЛУ, а не по понятию.** Оплачено этой сессией: замер
  смертности плана был нарезан по понятию «секция фазы», покрыл 2762 строки из
  3055, и **оба бездомных куска оказались ровно в непокрытых 293** — там жили
  архитектурные решения, правила исполнения и отложенное. Периметр, заданный
  смыслом, всегда уже периметра, заданного пересчётом; сумма сегментов сверяется
  с `wc -l` ДО фан-аута. Полная запись — §8 запускалок. @status:impl/done
- @fact:WAL-C-A-SPEC-LIES-IN-THREE-DIRECTIONS **Спека лжёт тремя способами, и
  греп находит только один.** «Отстала от кода» ищется по имени новой сущности.
  «Впереди кода при `impl/done`» не ищется ничем: искать нечего — надо читать
  утверждение и спрашивать дерево, есть ли оно. «Противоречит себе» находится
  только сплошным чтением. Поэтому шесть точечных находок стали тридцатью при
  полном проходе, и второй класс — самый дорогой: читатель вокруг него ПЛАНИРУЕТ. @status:impl/done
- @fact:WAL-C-A-PROMISE-IS-RE-MARKED-NOT-DELETED **Спека, обещающая
  непостроенное, чинится СТАТУСОМ, а не удалением требования.** Код обязан
  соответствовать спеке, а не наоборот, поэтому якорь остаётся, `@status:`
  становится `spec/plan`, рядом появляется факт «что верно сегодня», а выбор
  «строить или отозвать» уходит владельцу в `BACKLOG`. @status:impl/done
- @fact:WAL-C-ANCHORS-ARE-NEVER-DELETED **Якорь не удаляют — ему ставят
  надгробие с наследником.** Поймано механической сверкой в этой сессии: правка
  §3.2 удалила `##dep-prometheus`, и это увидел не глаз, а диф множеств якорей
  (`comm` старого и нового списка). Такую сверку гоняют ПЕРЕД посадкой всякой
  правки спеки: «удалённых — 0» есть отдельное утверждение. @status:impl/done
- @fact:WAL-C-A-RULING-HAS-TWO-LAWFUL-HOMES **Дом рулинга бывает двух видов, и
  свёртка обязана сказать какого.** Якорь спеки — для политики, связывающей за
  пределами одного файла. Докблок кода — для формы именно этого кода; это
  законный дом по закону дисциплины «explanation capital must be runnable
  capital». Что домом НЕ является: сам план, `spec/WAL.md` и `CONTINUE.md`
  (летучие), находка в `harvest/` (замер, не контракт), коммит-сообщение. @status:impl/done
- @fact:WAL-C-JUDGING-DEBT-READS-THE-MIRROR **`judging-debt.py` читает зеркало,
  а не живой скан.** Прогнанный до `progress mirror`, он показывает **ноль над
  реальным долгом** — поймано этой сессией: 0 неосуждённых при одном
  неосуждённом. Порядок scan → mirror → batch → merge → debt обязателен целиком,
  и последний шаг — не формальность, а измерение. @status:impl/done
- @fact:WAL-C-A-COUNT-CAN-BE-A-PROPERTY-OF-THE-REGEX **Счёт бывает свойством
  инструмента, а не предмета.** Три шаблона дали 68, 69 и 70 рулингов на одном
  файле: один заголовок с отступом, другой с тире вместо точки. Верное число
  нашлось потому, что воркер прогнал ДВА шаблона вместо одного. Всякое число,
  на котором стоит решение, меряется двумя разными способами. @status:impl/done
- @fact:WAL-C-QUARANTINE-IS-A-READER-JUDGEMENT **Карантин есть суждение
  ЧИТАТЕЛЯ о паре «запись × сборка», а не свойство записи.** Выводится из
  `must_understand` в точке применения и НИКОГДА не хранится на проводе. Отсюда
  же: CLI и сервер отвечают одинаково по построению — предикат читает ЗАПИСЬ, а
  не носитель, который у сервера вечно пуст. Контракт — PROP-005 §2.19. @status:impl/done
- @fact:WAL-C-DEFAULT-MUST-BE-SAFE **Умолчание обязано быть безопасным, и это
  свойство КОНСТРУКЦИИ.** Отвечающий путь спрашивает именованные аксессоры
  (`quarantine::usable_*`), путь ПИСАТЕЛЯ, мутации и операционные счётчики —
  нет: каталог есть проекция журнала, и возможности читателя не вправе
  сокращать то, что ЗАПИСАНО. Асимметрия сказана в док-комментариях обеих
  сторон — единственная защита от того, что следующий автор возьмёт не тот
  аксессор. @status:impl/done
- @fact:WAL-C-EMPTY-OUTPUT-IS-A-CLAIM **Пустой или нулевой вывод есть
  УТВЕРЖДЕНИЕ, и меряется тем же мерилом.** Прежде чем читать пустоту
  доказательством — скорми инструменту случай, который он ОБЯЗАН различить.
  Правило работало всю сессию: «ноль хитов `sha256` у клиента индекса» стало
  находкой только рядом с контролями 43 и 39. @status:impl/done
- @fact:WAL-C-A-BAN-MUST-CARVE-OUT-THE-REQUIREMENT **Запрет пакета обязан
  вырезать требование, которое лишь делит с ним написание** — иначе он его
  проглатывает. Три случая записаны в §8 запускалок. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель ОБРЫВАЕТСЯ на первом красном
  шаге** и о последующих не говорит ничего. Зелёный хвост — единственное
  доказательство, что прогнались все; после починки панель гоняется ЗАНОВО,
  насквозь. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ** — красное на два таргета требует `--no-fail-fast`; и
  `cargo test -p <крейт>` печатает НЕСКОЛЬКО строк `test result` — цитируй все. @status:impl/done
- @fact:WAL-C-FMT-CHECK-IS-THE-GATE **`cargo fmt` без `--check` выходит с нулём
  и когда результат НЕ чист.** Файл, вырезанный текстом, начинать шапкой `//!`. @status:impl/done
- @fact:WAL-C-FIXTURES-ARE-REAIMED-NOT-SATISFIED **Тест, покрасневший на
  законном изменении, ПЕРЕНАЦЕЛИВАЮТ, а не удовлетворяют** — и говорят, что он
  охраняет ТЕПЕРЬ. И обратное: **красный СУЩЕСТВУЮЩИЙ тест при аддитивной
  правке есть дефект правки, а не повод править тест** — поэтому новый сторож
  строит СВОЮ фикстуру, а общую не трогает. @status:impl/done
- @fact:WAL-C-NEVER-DUPLICATE-A-NORMATIVE-VALUE **Нормативное значение не
  копируют.** Порядок пассов, поверхность писателя каталога, эпоха, текст
  рецепта карантина, предикаты совпадения и проход отказа — каждый в одном
  доме. Числа-замеры в прозе спеки не пишутся вовсе: они живут в датированной
  находке, потому что число в прозе гниёт, а замер — нет. @status:impl/done
- @fact:WAL-C-JOURNAL-IS-THE-TRUTH **Каталог — проекция журнала, и починка идёт
  в одну сторону.** Если `rebuild --check` разошёлся, неправ КАТАЛОГ. Пути
  «прочитал → записал» не существует с Ф3. И проектор, встретив событие, чей
  носитель не построен, **отказывает целиком поимённо**, а не пропускает его. @status:impl/done
- @fact:WAL-C-CODEGEN-CANNOT-HEAL-ITSELF **Красный сгенерированный выход
  блокирует инструмент, который должен его перезаписать.** `xtask` зависит от
  `vibe-index` → `vibe-wire`, поэтому `cargo xtask codegen` падает на сборке
  САМОГО xtask. Лечится готовым `target/debug/xtask.exe` либо возвратом выхода
  из снимка. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки:** применить дифф →
  `cargo fmt --all` → **`cargo xtask specmap`** → стейдж → панель → суд →
  коммит. Карта, пересобранная ДО того как лёг код, — карта дерева, которого
  уже нет. Слайс, трогающий `generated/**`, садится в порядке стейдж → панель
  → коммит: `check-codegen` — это `git diff` против ИНДЕКСА. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён
  тем же заходом**: scan → mirror → batch JSON → `merge-verdicts.py` → долг 0.
  **`campaigns/**`, `BACKLOG.md` и `TASKS.md` в судимый корпус НЕ входят** —
  перепроверено этой сессией: десять новых якорей `BACKLOG` не сдвинули счёт. @status:impl/done
- @fact:WAL-C-HONEST-SEAL **Печать — только за проверенное**; сейчас она
  сознательно не ставится (см. `##WAL-KI-SEAL-DEBT`). @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ до
  первого фан-аута; режим — `SUBAGENT-MODE.toml` (`claudez`); пакеты режет
  босс; воркеры не запускают git; диффы читает, правит и коммитит босс;
  **ревью, спеки, планы и вердикты не делегируются никогда**. @status:impl/done
- @fact:WAL-C-A-MEASUREMENT-PACKET-IS-THE-CHEAPEST-DELEGATION **Замер —
  идеальная форма делегирования, и эта сессия показала почему.** Четыре
  read-only пакета, четыре приёмки с первого прохода, ноль `-c`-циклов, ноль
  git- и cargo-вызовов. Форма: закрытый список записи из ДВУХ файлов (находка и
  отчёт), `find -newer` вместо запрещённого git для доказательства периметра,
  **контрольный список якорей, где ложные вперемешку с заведомо верными** — им
  ловится и пасс, нашедший всё, и пасс, не нашедший ничего. @status:impl/done
- @fact:WAL-C-PROBE-GENRE **Жанр ПРОБЫ: два периметра записи.** Навсегда —
  находка и отчёт; временно — всё, что правит проба, и КАЖДАЯ правка
  возвращается, что доказывается копией в temp и `diff`-ом. И пакет говорит
  прямым текстом: «твоя работа не починить, а измерить». @status:impl/done
- @fact:WAL-C-CARGO-BAN-WHEN-A-LANE-IS-HOT **Когда одна лана держит cargo,
  вторая получает пакет с ПОЛНЫМ запретом cargo**, а не с советом. @status:impl/done
- @fact:WAL-C-FOUR-TERMINAL-CLASSES **Способов завершения прогона ЧЕТЫРЕ:**
  завершение · kill · отказ шлюза (529, лечится повтором) · **исчерпание квоты
  аккаунта (429, повтором НЕ лечится)**. У ланов разные токены и разные квоты;
  пустая проба стоит секунд (`claudez2 -p "Reply with exactly: PONG2"`).
  За эту сессию все четыре прогона завершились классом `completed`. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: никогда голый `cd` — и опасен не сам
  `cd`, а КАЖДАЯ команда после него. `cmd | tail; echo $?` печатает код ПАЙПА;
  правки только editor-инструментами; python — с `PYTHONIOENCODING=utf-8` и
  ВИНДОВЫМ путём (`C:\...`), потому что `/c/...` из Git Bash он не видит;
  бюджет файла 600 строк ПОСЛЕ `cargo fmt`. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты heredoc
  `-F -`; раскатка только `cargo xtask mirror`; Rules 1–4 связывают каждый
  коммит. `git worktree remove` снимать по одному; если сорвалось — оставить
  каталог И регистрацию, никогда не прунить поверх выжившего каталога. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки
  дисциплины не трогаются. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-18, 13 коммитов (`5f6be1e4`..`9c663b09`):** выправка
PROP-005 (`414581d4`), пять развилок в `BACKLOG` (`35bf5985`), суд 41 вердикта
(`3772ca1a`), дом рулинга логирования (`0088c0cf`), спасение дефералов
(`f5f857a4`), два пробела (`e7ac5a49`), урок нарезки (`a1654ca8`), карта 70
рулингов (`d77fa168`), три дома (`8aea1247`), два правила пакетов (`9a05f57b`),
рецепт свёртки (`9c663b09`), плюс две находки (`5f6be1e4`) и чек-лист
(`d444a4ec`). Четыре зелёные панели по 53 шага; `vibe check` не сдвинулся ни
разу; 46 вердиктов, долг 0 после каждого захода. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, `.wt/` пуст, дерево
чисто, зеркала синхронны. Два worktree под `~/.fractality/runs/**` — чужие, к
этой кампании отношения не имеют и не трогаются. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-COLLAPSE-REMAINS **§11 — свёртка плана не выполнена, и это
  решение, а не забывчивость.** Editor-инструментами файл на 3055 строк
  сворачивается только переписыванием набело; сессия прочитала около сорока
  процентов и отказалась переписывать документ, зная меньше половины его
  текста. Рецепт — в `TASKS.md`. @status:spec/plan
- @fact:WAL-KI-B083 **`[[registry]].index_url` описан и не существует (B-083,
  P2).** `RegistrySection` несёт `deny_unknown_fields`, поэтому TOML-пример
  спеки — не пример, а ОТКАЗ РАЗБОРА. Единственный источник сегодня —
  `VIBEVM_INDEX_URL_<REGISTRY>`. @status:spec/plan
- @fact:WAL-KI-B084 **`repomd.json` объявлен единственной точкой доверия, и ни
  один потребитель по нему ничего не сверяет (B-084, P2).** Ноль вхождений
  `sha256`/`etag` в клиенте при контролях 43 и 39. Обнажены метаданные в пути,
  не содержимое. @status:spec/plan
- @fact:WAL-KI-B085 **HTTP-триггер переиндексации обещан тремя якорями и не
  построен (B-085, P2)** — и на него указывает документированный хук §11:
  оператор, исполнивший рецепт, вешает хук на 404. @status:spec/plan
- @fact:WAL-KI-B086 **Машины приоритетов конфигурации нет (B-086, P2).**
  Ни `state/config.toml`, ни семейства `VIBE_INDEX_*`; две несвязанные
  переменные есть и спекой не названы. @status:spec/plan
- @fact:WAL-KI-B087 **Атомарная запись не синхронизирует каталог (B-087, P2).**
  Все четыре `sync_all` крейта — файловые; шага 4 протокола нет. Задевает и
  журнал, восстановлением из которого чинится всё остальное. @status:spec/plan
- @fact:WAL-KI-B088 **Чекера «спека не ссылается на ЗАКРЫТЫЙ план» нет (B-088,
  P3),** и норма уже промахнулась на единственном применении: §11.2 говорит
  «обе ссылки», их четыре. Гейт обязан различать ТРИ жанра — активный план,
  закрытый, датированное упоминание провенанса. @status:spec/plan
- @fact:WAL-KI-B089 **Документы индекса не знают ни хэндшейка, ни отказа
  (B-089, P2).** `consumer-protocol.md`, объявленный «HTTP API reference», даёт
  ноль вхождений `hello.json` и `unavailable` при контроле 4 на `repomd`.
  Наружная копия той же лжи хуже внутренней. @status:spec/plan
- @fact:WAL-KI-B081 **Словарь видов живёт не в одном месте (B-081, P2).**
  `known()` — тихая копия: седьмой вид молча выпал бы из справки. @status:spec/plan
- @fact:WAL-KI-B082 **Гейт окна перелома обещает больше, чем меряет (B-082,
  P2).** Сегодня тихо (`public = false`), в день переключения — ложная зелень. @status:spec/plan
- @fact:WAL-KI-B079 **Семь конвертов ответа `vibe-index` не описаны схемой и не
  стоят в реестре форматов (B-079, P2).** Чеканка формата — владельческий акт. @status:spec/plan
- @fact:WAL-KI-B080 **Резолвер клиента способен выбрать версию, которой сборка
  пользоваться не может (B-080, P2).** Клиент не читает `must_understand`
  нигде; речь сервера этого не лечит. @status:spec/plan
- @fact:WAL-KI-B056-NOW-BLOCKS **B-056 держит одну вещь.** Язык схем не
  выражает `u64` — поэтому `Repomd`/`RepomdFileEntry` вне периметра реэкспорта
  именованным дефером. Триггер — рулинг владельца. @status:spec/plan
- @fact:WAL-KI-J8-OPEN **Ж8 — открытый вопрос владельца:** что означает
  `--full` для записи, которую скан больше не видит. @status:spec/plan
- @fact:WAL-KI-CORPUS-TOMBSTONE-GAP **Корпус не несёт надгробия, и это
  измерено:** ни одно проецируемое событие журнала его не порождает. @status:spec/plan
- @fact:WAL-KI-MAP-EDGES **Три ребра прослеживаемости к PROP-005 ушли** с
  определениями в крейт провода, который в периметр карты не входит. @status:spec/plan
- @fact:WAL-KI-B078 **B-078 — «у JTD нет nullable» оказалось ложью (P2, open).** @status:spec/plan
- @fact:WAL-KI-VOCAB-OUTSIDE-THE-MAP **Единственный дом словаря вне периметра
  карты** — специмап индексирует JTD-схемы под `schemas/`. Дефер по имени. @status:spec/plan
- @fact:WAL-KI-SEAL-DEBT **Печать суда сознательно не поставлена.** PROP-005
  переписана в тридцати местах за один день; печать закрывает S7 либо сессия,
  готовая перечитать файл целиком. @status:impl/done
- @fact:WAL-KI-STALE **34 файла stale** (стоячий долг сдвигов байтов) —
  адресовано кампанией S7. @status:impl/done
- @fact:WAL-KI-AUDIT-P1 **Аудит `2026-08-06-01` (P1)** — «ruled — re-judgement
  campaign pending», её исполняет кампания S7. @status:impl/done
- @fact:WAL-KI-B074 **Второй якорь факта в абзаце глотается молча** (B-074). @status:impl/done
- @fact:WAL-KI-B075 **Панель может мигнуть на чистом дереве** (B-075) — читать
  отказ, не перезапускать вслепую. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие
  выходного гейта Phase E, ждёт рулинга. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → документы предмета (PROP-044 + ТЗ) →
`SUBAGENT-LAUNCHERS.md` целиком перед фан-аутом** — и берёт каждое число из
команд сверху, после rescan И mirror. Этот файл главнее `CONTINUE.md`;
ратифицированный PROP-044 главнее обоих. @status:impl/done
