# WAL — Project Continuation State {#root}

_Updated: 2026-08-17 (**Ф6.1 ЗАКРЫТА ЦЕЛИКОМ — пять шагов из пяти**; 13
коммитов, четыре посадки кода, панель зелёная на каждой, зеркала раскатаны
трижды. Вечный файл `hello.json` существует по всей цепи: схема → тип →
писатель → маршрут → клиент → корпус. Ф6.2 спроектирована и нарезана на четыре
шага, все развилки закрыты замером. Питание на машине пропадало один раз
посреди сессии; дерево восстановлено и доказано прогоном, потерь нет.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета целиком → короткий доклад → **сразу исполнение**.
Промт есть слово владельца, отдельного «да» ждать не нужно. @status:impl/done

@fact:WAL-THE-LAW **PROP-044 — ратифицированный стоячий закон каждого формата**
(2026-08-13). Терминология §2b обязательна: snapshot ↔ frozen — антонимы
**одной булевой оси без третьего состояния**; «канал» — авторский указатель
версий (PROP-005 §2.18); режим материализации — `copy`. @status:impl/done

@fact:WAL-RULE-OLDER-THAN-NUMBER **Правило старше собственного числа — и за эту
сессию оно сработало ПЯТЬ раз, все пять против автора пакета.** Маршрутов
сервера **15**, а не «около 14»; карантин наполняется на 315–364, а не «около
319–361»; `specmark::scope!` в `crates/vibe-index/tests/` не несёт **ни один**
из 21 файла, вопреки прямому указанию пакета; четыре теста `xtask/src/rebuild.rs`
НЕ краснеют от нового файла писателя (они строят свои каталоги и видят его с
обеих сторон сравнения); в `cli_search.rs` **15** тестов на индекс-моке, а не 8.
Каждый раз исполнитель верил дереву, исполнял правило и называл расхождение.
@status:impl/done

@fact:WAL-INSTRUMENT-MEASURES-THE-OTHER-THING **Инструмент мерит НЕ ТУ величину
— пойман в ПЯТЫЙ раз, и на этой сессии дважды у босса.** *(i)* Опрос воркера
шаблоном `"command":"[^"]*cargo[^"]*` показал НОЛЬ cargo-вызовов у воркера,
сделавшего их семь: класс `[^"]*` обрывается на экранированной кавычке внутри
`echo \"PROGRESS…\"`. Лечение — структурный обход JSONL, а не фрагмент строки.
*(ii)* WAL записал 1035 tagged в том самом коммите, где карта уже несла 1036.
**Правило одно: прежде чем читать вывод доказательством, скорми инструменту
случай, который он ОБЯЗАН различить.** @status:impl/done

@fact:WAL-FORCED-SLOT **Место шага ВЫВОДИТСЯ из ограничений — за кампанию
шесть раз, дважды на этой сессии.** Единый дом поверхности писателя обязан лечь
ПЕРЕД её ростом (иначе правка делается дважды и одна из копий однажды
отстанет); пасс обязан лечь ПЕРЕД схемой (иначе схема в истинной форме не
генерируется вовсе). **И впервые вынужденный порядок окупился НАБЛЮДАЕМО:**
шаг Ф6.1c тронул одно место вместо двух — `golden_corpus.rs` правки не
потребовал. @status:impl/done

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста
cargo xtask mirror --check
```

## Current phase {#current-phase}

@fact:WAL-PHASE **Change-native стройка, ТЗ №1: Ф0–Ф5 закрыты; Ф6.1 закрыта
ЦЕЛИКОМ (Ф6.1a, Ф6.1b-0, Ф6.1b, Ф6.1c, Ф6.1d). Вход следующей сессии —
Ф6.2a.** Порядок внутри фазы жёсткий, каждый шаг — отдельным коммитом; граница
ФАЗЫ — полная панель + правка карты ПОСЛЕ того как лёг код + раскатка.
@status:impl/done

@fact:WAL-HANDSHAKE-EXISTS **Вечный файл существует по всей цепи.**
`schemas/hello/e1/hello.jtd.json` (90 строк) → сгенерированный `Handshake` в
`crates/vibe-wire/src/generated/hello/e1/hello/` → `Index::write_to` пишет
`hello.json` в корень данных → маршрут `/v1/index/hello.json` → `IndexClient`
спрашивает его ПЕРВЫМ у обоих кандидатов → золотой корпус несёт его как
проекцию своего журнала. Форма вырожденная и полная одновременно: пишется
`{"vibe":"hello/1","worlds":[{"epoch":1,"path":"."}]}`, а СХЕМА описывает весь
набор ключей вечного файла с первого дня (Р43). @status:impl/done

@fact:WAL-NO-INVENTED-NUMBERS **Ни одного числа хэндшейка не выдумано.** `vibe`
= `FormatId::Handshake.epoch()`, эпоха мира = `FormatId::IndexRepomd.epoch()`,
эпоха клиента — оттуда же; часов в хэндшейке нет, поэтому две сборки одного
состояния дают одни байты. Сторож `catalog_format_family_agrees_on_its_epoch`
держит шесть каталожных форматов на одной эпохе и сверяет их **и с ENUM** —
то есть ловит вдобавок реестр, отредактированный без перегенерации.
@status:impl/done

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист,
HEAD `ea3bc3e0`, панель `self-check: all green` с реальным кодом выхода **0**,
**53 шага**; корпус 281 файл, 13788 маркеров, 0 неразмеченных; долг **0
неосуждённых, 0 осиротевших**, 34 stale; `vibe check` — 0 errors, 1 warning,
44 info; карта — 6061 спек-юнит / **1041** tagged / **974** ребра / 0
подозрений / 0 сирот. Зеркала синхронны (gitverse + github). `.wt/` пуст —
пять рабочих каталогов сессии сняты чисто. @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты — не
чинить.** `vibe check` даёт **44 info** `manifest_epoch` и **1 warning**
`local_source_freshness`. **За пятьдесят с лишним посадок подряд оба не
сдвинулись ни разу. Рост любого из них — находка, а не шум.** @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-F62 **Ф6.2 — карантин по-настоящему, ЧЕТЫРЕ шага, все
   развилки закрыты замером (Р49–Р51).** Ф6.2a — загрузчик перестаёт
   выбрасывать карантинную запись, заводится именованный предикат, умолчание
   обязано быть безопасным. Ф6.2b — семь глаголов CLI отвечают `unavailable
   {name, version, missing, recipe}` вместо молчания, обе ветки (текст и
   `--json`). Ф6.2c — шесть отвечающих по имени поверхностей HTTP, и здесь же
   закрывается расхождение двух поверхностей. Ф6.2d — `--log-level` по Р51.
   @status:spec/plan
2. @fact:WAL-NEXT-ACCEPTANCE **После Ф6.2 — приёмка всего ТЗ (§10)**, восемь
   шагов скриптом, плюс сверка предсказаний P1–P6 в отчёте кампании.
   @status:spec/plan
3. @fact:WAL-NEXT-INDEPENDENT **Независимая полоса — S1/S6 режутся холодными
   МЕЖДУ шагами** (S6 заводит запись в `formats/REGISTRY.toml`, а она
   перегенерирует `format_id/mod.rs`); **S7** — судейская, в любой момент, и
   она же закрывает долг печати. @status:impl/done
4. @fact:WAL-NEXT-OWNER **За владельцем четыре вопроса, ни один не блокирует:**
   S2 (org-права), **Ж8** (`--full` и исчезнувшая запись), **B-056** (язык схем
   не выражает `u64`; держит реэкспорт манифеста каталога) и **B-078**
   (разрешимая правка провода журнала). @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-PROP044-IS-THE-LAW **Вся идеология форматов — ратифицированный
  PROP-044**; здесь не пересказывается. Терминология §2b обязательна. @status:impl/done
- @fact:WAL-C-ETERNAL-FILE-IS-ABOVE-WORLDS **Хэндшейк стоит НАД мирами и вне
  манифеста.** `repomd.json` — манифест ОДНОГО мира; положить надмировой файл в
  пер-мировой манифест значит сделать одноразовый артефакт органом целостности
  для единственного неодноразового, и вопрос «чей repomd его хэширует»
  перестаёт иметь ответ, как только миров два. Прецедент корневого файла вне
  карты в дереве есть — `.gitignore` и `README.md` от `init`. **Цена названа:**
  `verify` ходит только по ключам карты, поэтому хэндшейк вне машинной проверки
  целостности; содержимое пакетов это не задевает — его сторожит `content_hash`
  на выборке. @status:impl/done
- @fact:WAL-C-HANDSHAKE-IS-ASKED-FIRST **Хэндшейк спрашивается ПЕРВЫМ, и этого
  требует ключ `successor`.** Он имеет смысл ровно тогда, когда по старому
  адресу больше ничего нет; ищи мы хэндшейк только там, где нашёлся каталог, —
  переехавший индекс провалил бы пробу `repomd` раньше, чем кто-либо прочёл
  указатель. По `successor` НЕ ходят: авто-переход требует сторожа циклов и
  правила доверия, ни то ни другое не решено. @status:impl/done
- @fact:WAL-C-SCHEMA-DESCRIBES-THE-WIRE **Схема описывает ПРОВОД.** Аннотации
  политики говорят что-то о ФОРМАТЕ; боксирование, канонический порядок ключей,
  snake_case и трейт-этаж — вопросы с ровно одним законным ответом, и потому
  они НЕ аннотации, а безусловные преобразования. **Терпимость записи реестра
  (`foreign_parsers = "many"`) относится к незнакомым КЛЮЧАМ, а не к праву
  соврать о типе знакомого** — понижение доменного типа до `String` «на всякий
  случай» запрещено. @status:impl/done
- @fact:WAL-C-ONE-THING-ONE-SPELLING **Одна вещь пишется одним способом.**
  Интерим-форма, дающая верный рустовый тип и верные байты ценой второй
  законной записи в схемах, ОТВЕРГАЕТСЯ — даже когда она работает. Так снята
  форма `"type": "string"` для опциональной даты: она восстанавливала
  состояние, которое Р11 уже устраняло, и хуже всего в вечном файле, где
  следующий автор увидел бы прецедент. @status:impl/done
- @fact:WAL-C-NEVER-DUPLICATE-A-NORMATIVE-VALUE **Нормативное значение не
  копируют.** Порядок пассов живёт в одном месте; **поверхность писателя
  каталога — тоже** (`crates/vibe-index/src/index/mod.rs`, `WRITER_FILES` /
  `WRITER_DIRS`), и её дом выбран не по вкусу: `specmark::scope!` этого модуля
  называет `PROP-005#layout`, тогда как `memory.rs` размечен
  `PROP-008#identity`. Эпоха тоже не копируется — клиент читает её из
  сгенерированного `FormatId`. @status:impl/done
- @fact:WAL-C-ONE-LOADER-ONE-TRUTH **Один загрузчик — одна правда.** Реестр
  форматов читают трое через ОДИН `load_format_registry`; флаги эпох — один
  `Epochs::load`. Второй парсер разошёлся бы с первым. @status:impl/done
- @fact:WAL-C-NEVER-OVERWRITE-A-FILE-IN-USE **Сгенерированный файл ЗАМЕНЯЮТ, а
  не перезаписывают** (`xtask/src/codegen/write.rs`). **Красный `check-codegen`
  на `os error 1224` оставляет ДЫРУ, а не прежнее состояние**; дерево
  восстанавливается из индекса. **И то же верно для красной СХЕМЫ:** кодоген
  вытирает выход ДО генерации, а `xtask` зависит от `vibe-index` → `vibe-wire`,
  то есть инструмент восстановления падает вместе с деревом — спасает готовый
  `target/debug/xtask.exe`. @status:impl/done
- @fact:WAL-C-CORPUS-IS-REGENERATED-NOT-PATCHED **Эталон, который обязан БЫТЬ
  выходом функции, регенерируют через эту функцию** — даже когда разница в
  одном поле: правка руками даёт верные байты по неверной причине. Форма:
  временный `#[ignore]`-регенератор рядом с тестом, прогон, удаление. @status:impl/done
- @fact:WAL-C-JOURNAL-IS-THE-TRUTH **Каталог — проекция журнала, и починка идёт
  в одну сторону.** Если `rebuild --check` разошёлся, неправ КАТАЛОГ. **После
  Ф3 пути «прочитал → записал» не существует** — мутации строят состояние из
  журнала, `load_from` остался только у семи читающих глаголов CLI. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки:** применить дифф →
  `cargo fmt --all` → **`cargo xtask specmap`** → стейдж → панель → суд →
  коммит. Карта, пересобранная ДО того как лёг код, — карта дерева, которого
  уже нет. @status:impl/done
- @fact:WAL-C-CODEGEN-STAGING **`check-codegen` — это `git diff --exit-code`
  против ИНДЕКСА, и untracked он НЕ ВИДИТ.** Слайс, трогающий `generated/**`,
  садится в порядке **стейдж → панель → коммит**. У ВОРКЕРА этот гейт законно
  краснеет. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель ОБРЫВАЕТСЯ на первом красном
  шаге** — зелёный хвост единственное доказательство, что прогнались все; и она
  гоняется ДВАЖДЫ, если посадка добавила шаг и первый прогон покраснел. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ** — красное на два таргета требует `--no-fail-fast`; и
  `cargo test -p <крейт>` печатает НЕСКОЛЬКО строк `test result` — цитируй все. @status:impl/done
- @fact:WAL-C-FMT-CHECK-IS-THE-GATE **`cargo fmt` без `--check` выходит с нулём
  и когда результат НЕ чист.** Файл, вырезанный текстом, начинать шапкой `//!`. @status:impl/done
- @fact:WAL-C-FIXTURES-ARE-REAIMED-NOT-SATISFIED **Тест, покрасневший на
  законном изменении, ПЕРЕНАЦЕЛИВАЮТ, а не удовлетворяют** — и говорят, что он
  охраняет ТЕПЕРЬ. @status:impl/done
- @fact:WAL-C-SCOPE-MARKER-FOLLOWS-THE-DIRECTORY **`specmark::scope!` на новом
  `.rs` — если соседи ПО КАТАЛОГУ его несут.** Правило оплачено сиротами в
  карте у `src/` продуктовых и движковых крейтов; интеграционные тесты
  `vibe-index` живут без него единообразно (1 из 21 — и этот один снят).
  Проверять грепом, а не допущением. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён
  тем же заходом**: scan → mirror → batch JSON → `merge-verdicts.py` → долг 0.
  **`campaigns/**`, `BACKLOG.md` и `spec/WAL.md` в судимый корпус НЕ входят.** @status:impl/done
- @fact:WAL-C-HONEST-SEAL **Печать — только за проверенное**; сейчас она
  сознательно не ставится (см. `##WAL-KI-SEAL-DEBT`). @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ;
  режим — `SUBAGENT-MODE.toml` (`claudez`); пакеты режет босс; воркеры не
  запускают git; диффы читает, правит и коммитит босс; ревью не делегируется
  никогда. @status:impl/done
- @fact:WAL-C-PACKET-CLAUSES **Клаузы пакета, оплаченные этой сессией:** пакет
  едет ФАЙЛОМ в корень worktree, в `-p` — указатель плюс копия закрывающих
  клауз; периметр — закрытый список, называющий и создаваемое, и **ломаемое**;
  замена git — `find . -newer <пакет>` с прунами; клауза §0.9 «верь дереву
  против числа пакета» обязательна; **прогон кодогена обновляет mtime ВСЕГО
  сгенерированного корпуса и чужого дома движка при тех же байтах — пакет
  обязан это предсказать, иначе воркер поднимет ложную тревогу**; gitignored
  инструменты (`tools/jtd-codegen/jtd-codegen.exe`) провизионируются ДО спавна;
  отчёт пишется РАНЬШЕ финальных гейтов, но каждая строка начинается словом
  PENDING. @status:impl/done
- @fact:WAL-C-FOUR-TERMINAL-CLASSES **Способов завершения прогона ЧЕТЫРЕ:**
  завершение · kill · отказ шлюза (529, лечится повтором) · **исчерпание квоты
  аккаунта (429, повтором НЕ лечится)**. Последние два приходят с
  `terminal_reason: api_error` и различаются только статусом. **У ланов разные
  токены и разные квоты**; пустая проба стоит секунд. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: никогда голый `cd` — и опасен не сам
  `cd`, а КАЖДАЯ команда после него. `cmd | tail; echo $?` печатает код ПАЙПА;
  правки только editor-инструментами; python — с `PYTHONIOENCODING=utf-8`;
  бюджет файла 600 строк ПОСЛЕ `cargo fmt`. Сборка `jq` здесь пишет CRLF даже
  в пайп. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты heredoc
  `-F -`; раскатка только `cargo xtask mirror`; Rules 1–4 связывают каждый
  коммит. **`git worktree remove` снимать по одному; если сорвалось — оставить
  каталог И регистрацию, никогда не прунить поверх выжившего каталога.** @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки
  дисциплины не трогаются. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-17, 13 коммитов (`0f73c39b`..`ea3bc3e0`):** два
замера Ф6 (`0f73c39b`, `24d4cc27`), четыре посадки кода — Ф6.1a (`c6a984e9`),
Ф6.1b-0 (`e1d77d5b`), Ф6.1b (`2668fa55`), Ф6.1c (`69bdad89`), Ф6.1d
(`5c023848`), — и решения Р38–Р51 в четырёх docs-коммитах плюс правка
устаревшего числа карты (`4b021a01`). Панель зелёная на каждой посадке;
`vibe check` не сдвинулся ни разу. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, `.wt/` пуст, дерево
чисто, зеркала синхронны. Два worktree под `~/.fractality/runs/**` — чужие, к
этой кампании отношения не имеют и не трогаются. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-QUARANTINE-TWO-SURFACES **Две поверхности расходятся об одной
  записи, и это ЖИВОЙ дефект до Ф6.2.** CLI грузит каталог с диска и
  карантинит; сервер поднимается из журнала, проекция не карантинит (Ж7),
  поэтому сервер отдал бы карантинную версию как обычную, а сырой маршрут
  `by-name/{name}` — запись дословно. Решение принято (Р49): проверка
  переезжает в путь ОТВЕТА. @status:spec/plan
- @fact:WAL-KI-LATEST-STABLE-INCONSISTENT **`latest_stable` может указывать на
  версию, которой сборка пользоваться не может.** Он берётся ПОЛЕМ из файла
  каталога, который писала проекция, а проекция не карантинит; пакет, чья
  единственная версия карантинна, уже сегодня отдаёт агрегат при пустом списке
  версий. Найдено замером Ф6.2, лечится там же. @status:spec/plan
- @fact:WAL-KI-CODEGEN-WIPES-BEFORE-GENERATING **Красная схема оставляет
  дерево вычищенным И роняет инструмент восстановления.** `codegen` вытирает
  `generated/**` до генерации; `xtask` зависит от `vibe-index` → `vibe-wire`,
  поэтому `cargo xtask` перестаёт собираться вместе с деревом. Обход —
  готовый `target/debug/xtask.exe`. Кандидат в `BACKLOG`/E-BUG, решение за
  владельцем. @status:spec/plan
- @fact:WAL-KI-B056-NOW-BLOCKS **B-056 держит одну вещь.** Язык схем не
  выражает `u64`, а `RepomdFileEntryFile.size` рукописно `u64` против
  сгенерированного `u32` — поэтому `Repomd`/`RepomdFileEntry` вышли из периметра
  реэкспорта именованным дефером. Триггер — рулинг владельца. @status:spec/plan
- @fact:WAL-KI-CORPUS-TOMBSTONE-GAP **Корпус не несёт надгробия, и это
  измерено, а не пропущено:** ни одно проецируемое событие журнала его не
  порождает. Триггер — шаг, заводящий такое событие (ср. Ж8). @status:spec/plan
- @fact:WAL-KI-MAP-EDGES **Три ребра прослеживаемости к PROP-005 ушли** с
  определениями в крейт провода, который в периметр карты не входит. Ratchet
  зелёный — потому долг назван прозой. Дом — либо расширение периметра карты,
  либо `#[spec]` на слое поведения. @status:spec/plan
- @fact:WAL-KI-B078 **B-078 — «у JTD нет nullable» оказалось ложью (P2, open).**
  Разрешимая правка ждёт своего шага: она меняет провод журнала. @status:spec/plan
- @fact:WAL-KI-TUPLE-GAP **Второй пол JTD: кортежа нет.** `Event::Renamed`
  несёт `(Group, String)`; схема описывает `elements`, теряя арность. @status:impl/done
- @fact:WAL-KI-VOCAB-OUTSIDE-THE-MAP **Единственный дом словаря вне периметра
  карты** — специмап индексирует JTD-схемы под `schemas/`. Дефер по имени. @status:spec/plan
- @fact:WAL-KI-J8-OPEN **Ж8 — открытый вопрос владельца.** Что означает
  `--full` для записи, которую скан больше не видит. @status:spec/plan
- @fact:WAL-KI-SEAL-DEBT **Печать суда сознательно не поставлена.**
  `spec/modules/vibe-index/PROP-005-package-index.md` несёт двинувшиеся факты;
  прецедент отката есть (`498e8c8b`). Закрывается S7. @status:impl/done
- @fact:WAL-KI-STALE **34 файла stale** (стоячий долг сдвигов байтов) —
  адресовано кампанией S7. @status:impl/done
- @fact:WAL-KI-B074 **Второй якорь факта в абзаце глотается молча** (B-074). @status:impl/done
- @fact:WAL-KI-B075 **Панель может мигнуть на чистом дереве** (B-075) — читать
  отказ, не перезапускать вслепую. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие
  выходного гейта Phase E, ждёт рулинга. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → документы предмета (PROP-044 + ТЗ) →
`SUBAGENT-LAUNCHERS.md` целиком перед фан-аутом** — и берёт каждое число из
команд сверху, после rescan. Этот файл главнее `CONTINUE.md`; ратифицированный
PROP-044 главнее обоих. @status:impl/done
