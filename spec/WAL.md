# WAL — Project Continuation State {#root}

_Updated: 2026-08-15 (**ФАЗА 3 ЗАКРЫТА ЦЕЛИКОМ**: сели Ф3.1, Ф3.2a–d, Ф3.3 —
восемь шагов, 29 коммитов, панель зелёная на каждой посадке. Тринадцать
делегированных пакетов, все приняты без циклов доработки; один остановился сам
и был прав. Сворачивание №23: `CONTINUE.md` и `NEXT-SESSION-PROMPT.md`
переписаны на вход **Ф4**.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета целиком → короткий доклад → **сразу исполнение**.
Промт есть слово владельца, отдельного «да» ждать не нужно. @status:impl/done

@fact:WAL-THE-LAW **PROP-044 — ратифицированный стоячий закон каждого формата**
(2026-08-13). Терминология §2b обязательна: snapshot ↔ frozen — антонимы **одной
булевой оси без третьего состояния**; «канал» — авторский указатель версий
(PROP-005 §2.18); capture/named ref — провайдерское; режим материализации —
`copy`. @status:impl/done

@fact:WAL-THE-PLANS **Три плана строек, взаимно сцеплены:**
[`TZ-CHANGE-NATIVE-FORMATS-v0.1.md`](../campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md)
(главная стройка Ф0–Ф6; **Ф0, Ф1, Ф2, Ф3 закрыты, следующий шаг — Ф4, §7 плана**),
[`TZ-IDENTITY-REGISTRY-BUILDS-v0.1.md`](../campaigns/packages-2026-09/TZ-IDENTITY-REGISTRY-BUILDS-v0.1.md)
(**S3 ЗАКРЫТ**; S1 и S6 измерены И несут записанное боссово решение — режутся
холодными; S2 — СТОП-ВЛАДЕЛЕЦ; **S4/S5 разлочены закрытием Ф3**; S7 — судейская),
[`TZ-CHANGE-NATIVE-WAVE2-v0.1.md`](../campaigns/packages-2026-09/TZ-CHANGE-NATIVE-WAVE2-v0.1.md)
(W1–W4 заперты до посадки Ф1–Ф5). @status:impl/done

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста
cargo xtask mirror --check
```

## Current phase {#current-phase}

@fact:WAL-PHASE **Change-native стройка, ТЗ №1: Ф0, Ф1, Ф2 и Ф3 ЗАКРЫТЫ.
Следующий шаг — Ф4 (схема и генератор; D4).** Порядок внутри фазы жёсткий,
каждый шаг — отдельным коммитом; граница ФАЗЫ — полная панель + правка карты +
раскатка. @status:impl/done

@fact:WAL-F3-IS-A-COMMAND-NOW **Закон Ф3 перестал быть прозой и стал двумя
командами.** «Писатель не принимает на вход то, что сам опубликовал»
проверяется так: `grep -rnw load_from` по шести путям мутации и по подъёму
сервера возвращает пусто, а `cargo xtask rebuild --check` сворачивает журнал в
черновой каталог и сравнивает с настоящим побайтово. Первая команда стережёт
конструкцию, вторая — результат; ни одна не требует читать код. @status:impl/done

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист, HEAD
`672bd938`; зеркала синхронны; панель `self-check: all green`, реальный код
выхода **0** (полный прогон на сворачивании); корпус 281 файл, 13788 маркеров,
0 неразмеченных; долг **0 неосуждённых, 0 осиротевших**, 34 stale; `vibe check`
— 0 errors, 1 warning, 44 info; карта — 6061 спек-юнит / 1029 tagged / 976 рёбер
/ 0 подозрений / **0 сирот под трещоткой**. @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты — не
чинить, не удивляться.** *(i)* `vibe check` даёт **44 info** `manifest_epoch` —
до-эпоховые манифесты под `packages/`, помечаемые намеренно (Ф1.2, решение D5).
*(ii)* Одно **warning** `local_source_freshness` по
`org.vibevm.world/addressable-specs` — следствие правки исходника пакета в S3
при намеренно НЕ запущенном `vibe install`. **За семнадцать посадок подряд оба
не сдвинулись ни разу. Рост любого из них — находка, а не шум:** регрессию
позапрошлой сессии поймал именно этот счётчик, а не golden. @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-F4 **Ф4 — схема и генератор (D4), §7 плана.** Ф4.1 — схемы
   `schemas/index/e1/` (включая `journal.jtd.json` под словарь событий из
   одиннадцати вариантов, выросший в Ф3, и `by_cap`/`by_purl`, добавленные
   замером Ф0.3); Ф4.2 — `vibe-wire-gen` в `xtask/src/wiregen/` с открытыми
   словарями `Unknown(String)`, замена рукописных типов реэкспортом
   сгенерированных, `BindingSite` становится ОДНИМ определением (B7 закрывается
   структурно); Ф4.3 — запрет рукописного провода шагом панели. Форма генератора
   задана находкой
   [`harvest/f0-gen-poc.md`](../campaigns/packages-2026-09/harvest/f0-gen-poc.md)
   §7 — исполнитель её не переизобретает. @status:spec/plan
2. @fact:WAL-NEXT-MEASURE-FIRST **Перед нарезкой Ф4 — замер, как перед каждой
   фазой.** За три сессии замеры опровергли пятнадцать записанных утверждений, и
   минимум семь меняли конструкцию, а не объём; последним пал счётчик в
   замороженной базовой линии, состаренный нашей же посадкой. Минимум под Ф4:
   что делает `cargo xtask codegen` и что покрывает `check-codegen`; где стоят
   **69 файлов** с рукописным `derive(Deserialize)` вне `generated/**` (значит
   вопрос Ф4.3 — правило белого списка, а не механизм запрета); сколько ещё
   словарей больны тем же, чем `BindingSite`; есть ли теперь ридер у
   `by-cap`/`by-purl`. @status:spec/plan
3. @fact:WAL-NEXT-INDEPENDENT **Независимая полоса режется холодной:** S1 и S6
   измерены, и боссово решение по каждому УЖЕ записано в
   `TZ-IDENTITY-REGISTRY-BUILDS-v0.1.md` (S1 — `RepoNotVisible` и «already
   exists» как позднее свидетельство; S6 — свежесть сайдкаром). S7 — судейская,
   в любой момент, и она же закрывает долг печати. @status:spec/plan
4. @fact:WAL-NEXT-OWNER **За владельцем два вопроса, оба не блокируют главную
   полосу:** S2 (переименовать `_`-репо в vibespecs — нужны org-права) и **Ж8**
   (что значит `--full` для записи, которую скан больше не видит). @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-PROP044-IS-THE-LAW **Вся идеология форматов — ратифицированный
  PROP-044**; здесь не пересказывается. Терминология §2b обязательна в каждом
  новом тексте и коде. @status:impl/done
- @fact:WAL-C-JOURNAL-IS-THE-TRUTH **Каталог — проекция журнала, и починка идёт
  в одну сторону.** Если `rebuild --check` разошёлся, неправ КАТАЛОГ; правка
  журнала «чтобы сошлось» отмывает секретную правду в слой правды, и сообщение
  гейта запрещает это прямым текстом. Мутация = дозапись события + перепроекция;
  `write_to` вызывается ТОЛЬКО над результатом проекции. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки, оплаченный красной панелью:**
  применить дифф → `cargo fmt --all` → **`cargo xtask specmap`** → панель → суд
  → коммит. Карта, пересобранная ДО того как лёг код, — карта дерева, которого
  уже нет: новый `.rs` с `specmark::scope!` добавляет ребро. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель ОБРЫВАЕТСЯ на первом красном
  шаге** — лог кончается ровно на нём, последующие шаги не выполняются. «Упала
  на X» ничего не говорит о шагах после X; зелёный хвост — единственное
  доказательство, что прогнались все. Посадка, трогающая саму панель, гоняет её
  дважды. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ**, а не на первом упавшем тесте. Красное доказательство,
  растянутое на два таргета, покажет одно падение и умолчит о втором — под
  красное доказательство берётся `--no-fail-fast`. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён тем
  же заходом**: scan → **mirror** → batch JSON → `merge-verdicts.py` → долг 0.
  Мержу предшествует `progress mirror`, иначе долг не проявится.
  **`campaigns/**` и `spec/WAL.md` в судимый корпус НЕ входят.** @status:impl/done
- @fact:WAL-C-EVIDENCE-CITES-SYMBOLS **Evidence цитирует ИМЯ СИМВОЛА, а не номер
  строки**, и вердикт, заменяющий прежний, объясняет замену — почему прежний был
  верен тогда и неверен теперь. Проверено дорого: при ручной перепроверке трёх
  фактов Ф1.3 четыре координаты из шести протухли за одну фазу, а сами факты
  оказались верны. @status:impl/done
- @fact:WAL-C-HONEST-SEAL **Печать — только за проверенное**: seal файла ручается
  за ВСЕ его вердикты. См. `##WAL-KI-SEAL-DEBT` — сейчас она сознательно не
  ставится. @status:impl/done
- @fact:WAL-C-SPECMAP **Правка текста спеки и новый код двигают карту**:
  `cargo xtask specmap` в той же посадке; `--check` зелёный. **Новый `.rs`-файл
  обязан нести `specmark::scope!`** — иначе трещотка ловит его pub-функции как
  сирот; якорь берётся у РОДИТЕЛЬСКОГО модуля, а не у соседей по каталогу. @status:impl/done
- @fact:WAL-C-CODEGEN-STAGING **`check-codegen` — это `git diff --exit-code` по
  generated-каталогам**, то есть рабочее дерево против ИНДЕКСА, и untracked-файлов
  он не видит. Слайс, трогающий `generated/**`, садится в порядке
  **стейдж → панель → коммит**. Для Ф4 это не деталь, а главный риск фазы. @status:impl/done
- @fact:WAL-C-PHASE-ORDER **Порядок шагов внутри фазы жёсткий**, каждый шаг —
  отдельный коммит; граница ФАЗЫ (не шага) = полная панель + правка карты +
  раскатка `cargo xtask mirror`. @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ; режим —
  `SUBAGENT-MODE.toml` (`claudez`); пакеты режет босс; воркеры не запускают git;
  диффы читает, правит и коммитит босс; ревью не делегируется никогда. §8 несёт
  **40 размеченных фактов**, §9 — разбор чистого фан-аута. @status:impl/done
- @fact:WAL-C-DONT-HAND-OVER-A-COUNT **Число, которое воркер может пересчитать
  сам, ему не отдают.** Пакет Ф3.3 велел найти все пятнадцать самому и
  остановиться, если число другое; воркер нашёл шестнадцать и остановился, не
  написав ни строки. Шестнадцатый добавила посадка этой же кампании часами позже
  замера. Скажи пакет «убери пятнадцать» — ушли бы пятнадцать, а панель была бы
  зелёной поверх дыры. @status:impl/done
- @fact:WAL-C-RED-PROOF **Тест, сторожащий СОХРАНЯЕМОЕ поведение, требуется
  красным как пункт приёмки** — он обязан быть прогнан против непра́вленого кода
  и показан упавшим; зелёный прогон доказывает только новое. То же для гейта:
  гейт, которого не видели красным, — не гейт. @status:impl/done
- @fact:WAL-C-KILLED-TASK-LEAVES-A-LIVE-WORKER **Убитая harness'ом задача не
  убивает воркера** — семь случаев за сессию, все семь дописали работу; логи
  росли через 5 и через 19 минут после «убийства». Судить по росту
  `worker-stdout.jsonl`, ставить фоновый сторож, **никогда не прибираться вокруг
  живого процесса**. И `git worktree prune` + `git -C <вычищенный каталог>` бьёт
  по ХОЗЯЙСКОМУ дереву. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: cwd Bash-инструмента персистентен —
  **никогда голый `cd`**, только `( cd … && … )` / `git -C` / абсолютный путь;
  `cmd | tail; echo $?` печатает код ПАЙПА — редирект в файл и `$?` сразу;
  **правки только editor-инструментами** (`sed -i` — нарушение, даже на одной
  строке); python — в файл, `PYTHONIOENCODING=utf-8`; бюджет файла 600 строк
  ПОСЛЕ `cargo fmt`, **и он покрывает `tests/**`, где гейта нет**. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` по всему дереву — явные пути;
  коммиты heredoc `-F -`; раскатка только `cargo xtask mirror` (fast-forward,
  никогда `--force`); Rules 1–4 связывают каждый коммит. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки дисциплины
  не трогаются. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-14/15, 29 коммитов (`392ef4b3`..`672bd938`):**
перепроверка трёх фактов Ф1.3 вручную → четыре замера под Ф3
(`f3-index-state-and-projection`, `f3-journal-physics`, `f3-rmw-break-and-reset`,
`f32-projector-surface`) → рулинги Ж1–Ж8 и разбиение Ф3.2 на четыре шага →
**Ф3.1** (модуль журнала: NDJSON, только дозапись, шардирование по месяцу,
одиннадцать вариантов события) → **Ф3.2a** (`init` пишет правду до каталога) →
**Ф3.2b** (чистая `project(events) -> Index`) → **Ф3.2c1** (`add`/`remove`
перестают читать каталог) → **Ф3.2c2** (сервер поднимается из проекции) →
**Ф3.2c3** (полный скан отличается от инкрементального ОДНИМ событием) →
**Ф3.2d** (`xtask rebuild --check`) → **Ф3.3** (снятие строгости с шестнадцати
агрегатов) → три правки PROP-005 и семь уроков в §8 запускалок. Панель зелёная
на каждой посадке, раскатано на оба зеркала. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, `.wt/`-worktree'ов нет,
дерево чисто. Два worktree под `~/.fractality/runs/**` — чужие, из фрактальности,
к этой кампании отношения не имеют и не трогаются. Следующий шаг начинает свежая
нарезка замера под Ф4. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-F3-MINES-CURED **Обе мины Ф3 сняты по построению, а не патчем.**
  *(1)* Карантин на чтении больше не может стереть отфильтрованное: проекция
  собирается из событий, и выброшенного читателем в ней не существует.
  *(2)* Надгробия переживают `reindex` в ОБОИХ режимах — водораздел
  `EntrySetReplaced` чистит только набор записей, а факты реестра применяются по
  всему журналу. Это ШИРЕ, чем записанная мина: она называла только полный
  режим, а инкрементальный стирал их так же. @status:impl/done
- @fact:WAL-KI-J8-OPEN **Ж8 — открытый вопрос владельца, Ф3 закрылась без него
  намеренно.** Что означает `--full` для записи, которую скан больше не видит:
  похоронить надгробием (и убить живой пакет при сетевом сбое) или удержать как
  ненаблюдавшуюся (и вечно держать удалённый). Законы PROP-044 их не разводят —
  оба ответа честны, цена разная. Сегодняшнее поведение стоит. Рядом того же
  рода: `rescan-org` объявлен обновлением кэша организации, а идёт с
  `mode = "full"` — теперь со всеми эффектами водораздела, записанными явно. @status:spec/plan
- @fact:WAL-KI-SEAL-DEBT **Печать суда сознательно не поставлена.**
  `spec/modules/vibe-index/PROP-005-package-index.md` несёт двинувшиеся факты
  трёх заходов. Три из них — `HASH-JOIN-KEY`,
  `HASH-LABELS-DIFFER-BY-DESIGN-TODAY`, `TWO-INTEGRITY-LAYERS` — перепроверены
  вручную 2026-08-14 (`392ef4b3`), но Ф3 двинула в том же файле другие якоря, и
  печать снова ручалась бы за то, чего никто не смотрел целиком. Асимметрия цен:
  не напечатать стоит одной проверки в будущем, напечатать ошибочно — ложного
  «проверено», и прецедент отката есть (`498e8c8b`). Закрывается S7 или сессией,
  готовой перечитать файл целиком. @status:impl/done
- @fact:WAL-KI-F4-DEBTS **Что Ф4 наследует известным, а не находит.**
  *(i)* `BindingSite` определён ДВАЖДЫ и совпадает случайно: писатель
  `index/inverted.rs:88` — `kebab-case`, клиент `index_client/wire.rs:94` —
  `lowercase`, значения однословные, паритет-теста нет (живой дефект B7).
  *(ii)* `by-cap/<slug>.jsonl` и `by-purl/<slug>.jsonl` — публикуемые поверхности
  БЕЗ ридера файла, тогда как у соседей он есть: прямое нарушение G11.
  *(iii)* Рукописный `derive(Deserialize)` стоит в **69 файлах** вне
  `generated/**` — значит вопрос Ф4.3 не «как запретить», а по какому ПРАВИЛУ
  строится белый список. *(iv)* Значения словарей остались закрытыми осознанно
  (`package_kind_rejects_unknown` зелёный) — их открывает Ф4.2 и никто раньше. @status:spec/plan
- @fact:WAL-KI-CFG-TEST-UNAVAILABLE **`#[cfg(test)]` недоступен для элемента,
  который зовут из `tests/**`** — интеграционные тесты собираются отдельной
  единицей компиляции, где `#[cfg(test)]`-элементов библиотеки не существует.
  Это снимает «очевидную» альтернативу всякий раз, когда обсуждается фикстурный
  конструктор. @status:impl/done
- @fact:WAL-KI-DETERMINISM-IS-NOT-IDEMPOTENCE **Детерминизма не хватает для
  идемпотентности.** Часы приходят на СОБЫТИЕ мутации, поэтому у повтора они
  другие и `generated_at` сдвигается: пустой diff не нужно получать — нужно не
  создавать работу. @status:impl/done
- @fact:WAL-KI-STALE **34 файла stale** (стоячий долг сдвигов байтов) —
  адресовано кампанией S7; до неё живут видимыми. @status:impl/done
- @fact:WAL-KI-B074 **Второй якорь факта в абзаце глотается молча** (B-074) —
  механизм чека не построен. @status:impl/done
- @fact:WAL-KI-B075 **Панель может мигнуть на чистом дереве** (B-075) — читать
  отказ, не перезапускать вслепую. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие выходного
  гейта Phase E, ждёт рулинга. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → документы предмета (PROP-044 + три ТЗ) →
`SUBAGENT-LAUNCHERS.md` целиком перед фан-аутом** — и берёт каждое число из
команд сверху, после rescan. Этот файл главнее `CONTINUE.md`; ратифицированный
PROP-044 главнее обоих. @status:impl/done
