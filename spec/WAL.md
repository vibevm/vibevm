# WAL — Project Continuation State {#root}

_Updated: 2026-08-05, wind-down №13 (**БЭКЛОГ ОСУШАЕТСЯ; ГРУППА №1 «ДИСЦИПЛИНА
НЕ НАВЕДЕНА НА СЕБЯ» ЗАКРЫТА ЦЕЛИКОМ.** 13 строк закрыто, из них 6 —
измерением: они просили построить построенное. Панель была красной структурно —
wind-down переписывает `spec/WAL.md`, а тот лежал в наблюдаемом корне карты, так
что последний коммит каждой сессии делал карту протухшей. Починено ключом
изъятия.)_

##WAL-NUMBERS-COME-FROM-COMMANDS **Every number below is reproduced by
commands; run them rather than quoting this file.** @impl/done

```bash
python campaigns/packages-2026-09/tasks/drift-registry.py
python campaigns/packages-2026-09/tasks/summary.py
python campaigns/packages-2026-09/tasks/text-stability.py
```

## Current phase {#current-phase}

##WAL-PHASE **Progress Control (PROP-043) — wave 2, `packages-2026-09`:
Phase E RUNNING under the EXTENDED mandate (2026-08-04, §7 LOG). Все четыре
волны карты закрыты; работа идёт по рулингу владельца 2026-08-05 — **сперва
осушить бэклог, к тестам не подходить**. Фаза T НЕ начата. @impl/done

##WAL-STATE **State at checkpoint** (2026-08-05; the commands supersede):
registry **11 641 confirmed / 177 drift**, 0 owed, 0 needing re-judgement.
Corpus 274 files; `progress check` clean. Panel GREEN (48 steps, bare, tail
read). `main` @ `ff2079e1`, 49 commits this session; `.wt/` empty.
**`gitverse` synced through `586e7c7`; `github` UNREACHABLE.** Backlog 50 rows,
of which at most 13 are live. @impl/done

## Next {#next}

1. ##WAL-NEXT-B045 **B-045 — грамматика имён, пакет уже написан.** Три
   доводки, все измерены: сверка вида пакета (тип `KindMismatch` не
   существует, а докблок `package_ref.rs:437` утверждает, что сверка идёт;
   код возврата `TYPE_MISMATCH = 4` мёртв); короткие имена `uninstall` и
   `update` (резолвер lockfile-first уже отвечает без сети) при сохранении
   требования у трёх redirect-глаголов; шесть мис-цитат `PROP-008 §2.4`,
   чья честная опора — §2.6 `##INDEX-DEPENDENCY`. @spec/done
2. ##WAL-NEXT-AUDIT-01 **AUDIT `-01`** — дыра покрытия: e2e пути
   `vibe init` (без `--registry`) → `vibe install` не существует; это старейшая
   открытая находка и та самая дыра, что пропустила `-02` через восемь фаз. @spec/done
3. ##WAL-NEXT-BACKLOG-REST **Остаток бэклога** — измерять КАЖДУЮ строку по
   дереву перед работой: за два дня семнадцать строк просили построить
   построенное. @spec/done
4. ##WAL-NEXT-OWNER-OPEN **Ждёт владельца, не блокирует:** `github`
   (перехват ssh — единственное, что требует его рук); B-050 (nightly-пин
   против рулинга о паритете); AUDIT `-14` (схема контракта индекса), `-04`
   (рулинг о TUI-подсистеме); миграция бэклога; гейт выхода фазы E; B-007,
   B-015, B-017, B-020, B-024. @spec/done

## Constraints — do not violate {#constraints}

- ##WAL-C-A-ROW-TAKES-ITS-CITATIONS-WITH-IT **Строка уносит с собой свои
  цитаты.** Рулинг «строка умирает вместе с коммитом» не имел спутника, и на
  десятой удалённой строке это выстрелило: B-016/B-021/B-037 оставили висячие
  адреса в трёх живых дизайн-документах. Закрытие есть переезд содержимого
  **и ссылок**: каждая цитата указывает туда, куда уехало содержимое, либо
  становится текстом без адреса. **Рулинг закрытой строки живёт в коммите,
  который её закрыл, а не по адресу** — коммит неудаляем, якорь нет. Проверка:
  греп `](#b-0NN)` по файлу и `BACKLOG.md#b-0NN` по дереву, тем же заходом. @impl/done
- ##WAL-C-SYNC-COPIES-CRATES-NOT-MANIFESTS **`sync-engines` копирует крейты,
  но НЕ корневые манифесты.** Крейт, наследующий `workspace = true`, переносит
  наследование через копирование и не находит ничего на той стороне: три
  mcp-пакета перестали загружать собственные манифесты, и проявилось это как
  падение `cargo fmt --check` (fmt сперва разрешает workspace). Новая
  `workspace`-зависимость объявляется в авторском стеке И в каждом
  пространстве, которое вендорит крейт. @impl/done
- ##WAL-C-THE-PANEL-OWNS-THE-USER-HOME **Под бегущей панелью нельзя ни один
  `vibe`-глагол, пишущий домашний каталог.** Растяжка снимает слепок `~/.vibe`
  в начале и сверяет после тестов; `vibe progress mirror`, запущенный рядом,
  пишет туда кэш — гейт сработал и обвинил несуществующий тест, подробно и
  корректно по своим меркам. Правило шире, чем «не гонять cargo параллельно».
  Первый ход при срабатывании на пути `progress-cache` — перезапуск панели в
  одиночку, а не охота за тестом. @impl/done
- ##WAL-C-IMPLEMENTS-IS-A-CLAIM-ABOUT-RUNNING-CODE **`implements` —
  утверждение о коде, который работает** (записано в PROP-014). Голое
  объявление ребра не несёт, и градиент ядовит: чем аккуратнее проект объявляет
  формы заранее, тем больше ложного покрытия он накапливает. Математики
  покрытия в индексе нет вовсе — вред доставляется читателю `vibe explain`.
  Снятие ребра ничего не глушит: модульный `scope!` покрывает элемент. @impl/done
- ##WAL-C-A-MESSAGE-MAY-NAME-THE-WRONG-CAUSE **Сообщение может быть о
  правильном событии и называть неправильную причину.** Дважды за сессию:
  отсутствующее поле манифеста рапортовалось синтаксической ошибкой TOML (а
  настоящий диагноз с кареткой лежал в `#[source]`, который `Display` не
  печатает), и недостижимый хост объявлялся разошедшимся зеркалом. Второе
  дороже: лекарство от расхождения есть ручное примирение историй. Цитата REQ в
  сообщении этого класса не ловит. @impl/done
- ##WAL-C-MEASURE-WHAT-IS-BUILT-FIRST **Перед реализацией плана измеряют, что
  из него уже реализовано.** За два дня **семнадцать** строк просили построить
  построенное. Замер по АВТОРСКОМУ дереву: находка в `vibedeps/`, `.vibe/` или
  `legacy-spec/` ничего не доказывает. И греп — тоже измерение, которое может
  соврать: `impl .*Trait for` не совпал с `impl<P: …> Trait for` и дал бы
  ложный вывод о целом классе. @impl/done
- ##WAL-C-PLAN-IS-NOT-TRUTH **План — не источник истины (владелец,
  2026-08-05).** Статусы с плановых документов сняты; строка умирает вместе с
  коммитом, который делает её неправдой. **Следствие, ещё не разрешённое:**
  строки, закрытые ДО рулинга, остались в файле, и живое от истории сейчас не
  отличить — регекс для этого ненадёжен. Решение об удалении — владельца. @impl/done
- ##WAL-C-VENDORED-INCLUDES-STACK-CRATES **Вендорятся не только движки:**
  mcp-пакеты зеркалят раскладку крейтов своего стека. `cargo xtask sync-engines`
  идёт ОТДЕЛЬНЫМ шагом после любой правки авторского крейта. 51 пара. @impl/done
- ##WAL-C-ENGINE-FIELD-AND-ITS-FRONTEND-LAND-TOGETHER **Движковое поле и его
  фронтенд приземляются вместе.** Вендоринг нового поля факта оставил растовый
  фронтенд без него в пяти конструкциях, и дерево было красным между двумя
  коммитами. @impl/done
- ##WAL-C-WORKERS-DO-NOT-FMT **Воркеры не форматируют, и `cargo fmt --all`
  после применения — шаг закона слияния, а не вежливость.** Пропуск разнёсся
  `sync-engines` по пяти вендор-деревьям и был найден хостовым `fmt --check`
  через path-зависимость. @impl/done
- ##WAL-C-A-TRUNCATED-PIPE-READS-GREEN **Усечённый ВИД вывода — тот же дефект,
  что проглоченный код возврата.** Гейт слияния — ПАНЕЛЬ; предпанельная
  проверка, расходящаяся с ней, не стоит ничего. @impl/done
- ##WAL-C-RESULT-EVENT-IS-TERMINAL **Терминальный сигнал воркера — событие
  `{"type":"result"}` в stream-json**, а не `TASK-DONE` и не греп маркера
  (совпадает с текстом самого пакета). Форма запуска — БЕЗ хвостового `&`. @impl/done
- ##WAL-C-MTIME-IS-NOT-LIVENESS **Правило «mtime старше 5 минут = зависание»
  не может сработать:** телеметрия размышления держит mtime свежим. Живость —
  метка последнего НЕ-телеметрийного события и число вызовов `Edit`. @impl/done
- ##WAL-C-VERDICT-STANDARD **The verdict standard.** PRESCRIBES → confirmed
  when coherent and every referent resolves; DESCRIBES → checked against the
  tree; unexercisable → unverifiable; `world` adds source 2. @impl/done
- ##WAL-C-A-COMPOUND-FACT-IS-JUDGED-CLAUSE-BY-CLAUSE **Предложение с двумя
  независимыми утверждениями требует ссылки НА КАЖДОЕ.** @impl/done
- ##WAL-C-A-REFUTED-SENTENCE-HAS-MANY-HOMES **Опровергнутая фраза живёт во
  всех местах, где её пересказали.** Охота за домами — шаг посадки. @impl/done
- ##WAL-C-ONE-LAW-ONE-IMPLEMENTATION **One law, one implementation — and the
  divergence of two is SILENT by nature.** Проверено и в обратную сторону:
  два `compute_content_hash` расходиться не могут — у дублирования есть
  парный тест. @impl/done
- ##WAL-C-A-DOCBLOCK-CAN-LIE **Код, обещающий больше, чем делает, — тот же
  дефект, что тихая потеря.** Заплачено снова дважды: докблок `Watcher` и
  докблок `PackageRef.kind`, утверждающий сверку, которой нет. @impl/done
- ##WAL-C-FILE-BUDGET-IS-NEUTRAL **Бюджет 600 строк — нейтральный ключ
  конформа;** меряет босс и только после `cargo fmt`. @impl/done
- ##WAL-C-MARK-NOT-SUPPRESS **Помечать, а не гасить (владелец, 2026-08-01).** @impl/done
- ##WAL-C-SIGNAL-NOT-WALL **Проверка свежести — СИГНАЛ, а не гейт.** @impl/done
- ##WAL-C-SILENCE-IS-THE-DISEASE **Когда законное проходит незамеченным, лечи
  молчание, а не состояние.** @impl/done
- ##WAL-C-MEASURE-BEFORE-ASSERTING **Правдоподобная модель системы — не
  измерение.** @impl/done
- ##WAL-C-EVIDENCE-IS-NOT-A-VERDICT **Воркеры собирают доказательства, вердикт
  пишет босс.** @impl/done
- ##WAL-C-WORKER-JUDGMENT **Воркеры эскалируют настоящее суждение — чтите
  его.** Эта сессия: пять опровержений босса, включая два его самых сильных
  утверждения в собственном скелете спеки. @impl/done
- ##WAL-C-BUILD-FIRST **BUILD-FIRST (владелец, 2026-08-02).** @impl/done
- ##WAL-C-PARITY-IS-THE-INVARIANT-NOT-THE-CODE **Паритет — тот же инвариант,
  никогда тот же код.** @impl/done
- ##WAL-C-STRUCT-FIELD-RIPPLE **Добавление поля ломает каждую литеральную
  конструкцию и каждый исчерпывающий match**; находит только `--all-targets`. @impl/done
- ##WAL-C-REAL-MIRROR **Настоящее зеркало — `vibe progress mirror --campaign
  <zone>`**; **merge и seal НИКОГДА не цеплять одной командой.** @impl/done
- ##WAL-C-CAMPAIGN-FRAME **Рамка кампании.** **T/F/G остаются вне добра**;
  публикация — отдельная операция; версии НЕ бампаются до предпубликационной
  границы. Версия фронтенда конформа — ключ кэша, не релиз; её бампать можно. @impl/done
- ##WAL-C-NO-PAUSE **Мандат исполняется автономно** — останов только на
  настоящей развилке владельца, по одной. @impl/done
- ##WAL-C-SELF-COORDINATE **Хост — это пакет (B-031).** `spec://vibevm/…`
  парсится и НИКОГДА не разрешается. @impl/done
- ##WAL-C-PERIMETER **Закон периметра.** SPEC в `core-ai-native`, ENGINE в его
  крейтах, DRIVER в CLI стека, DEPLOYMENT у потребителя. @impl/done
- ##WAL-C-OWN-CORPUS **Кампания внутри собственного корпуса:** `campaigns/**`
  исключён структурно, корневые документы не совпадают ни с одним
  include-глобом. @impl/done
- ##WAL-C-NO-MEASUREMENTS-ANSWER **«Замеров нет и нескоро будет»** — стоячий
  ответ; не поднимается заново. @impl/done
- ##WAL-C-PRESENTATION-FORMAT **Формат подачи (обязателен).** Суть
  по-человечески БЕЗ чтения спек → дерево для развилок → точные имена
  приложением; спеки не цитировать. Развилки — по одной. **И когда владелец
  говорит «я ничего не понял» — объяснять с нуля.** @impl/done
- ##WAL-C-DELEGATION **Транспорт воркеров E/T** (механика
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` — читать ЦЕЛИКОМ;
  `SUBAGENT-MODE.toml` перечитывать перед КАЖДЫМ fan-out'ом, сейчас `claudez`):
  запускаторы `C:\Users\olegc\opt\bin\{claudez,claudez2}`, свои state-каталоги,
  `-c` ключуется парой (state dir, cwd). Логи — в
  `C:\Users\olegc\git\v\cache\agents\sorted\<task-id>\`; WORKER-REPORT
  обязателен; полные пути логов — в чат при каждом запуске. @impl/done
- ##WAL-C-ARCHIVE-BEFORE-CLEANUP **Отчёт воркера архивируется СВОИМ шагом, до
  любой уборки.** Проверено 2026-08-05: все отчёты сессии в архиве, и два
  остатка прошлых сессий сняты — их отчёты там уже лежали. @impl/done
- ##WAL-C-REAL-EXITS **Коды возврата читаются НАСТОЯЩИМИ, никогда через
  pipe/grep** (`${PIPESTATUS[0]}`), и **вид вывода не усекается**. Панель
  фоном — bare-формой. @impl/done
- ##WAL-C-COMMIT-MESSAGE-VIA-FILE **Сообщение коммита идёт через heredoc или
  `-F`.** @impl/done
- ##WAL-C-SHELL-TRAPS **Ловушки оболочки:** cwd Bash-инструмента ПЕРСИСТЕНТЕН —
  абсолютные пути или `git -C` (за сессию едва не прочитан worktree вместо
  хоста); **CRLF против `str.replace` — только редакторские инструменты**,
  питон-переписывание открывать с `newline=''` с обеих сторон; **в heredoc
  питона путь Windows с `\v`/`\f` рвётся — собирать через `os.path.join`**. @impl/done
- ##WAL-C-STAGE-EXPLICIT **Никогда `git add -A`;** стейджить явные пути. @impl/done
- ##WAL-C-DURABLE-CITATIONS **Брифы цитируют только durable-файлы.** @impl/done
- ##WAL-C-ATTRIBUTION **Правила 1–4 связывают каждый коммит** (человеческая
  атрибуция — без AI-трейлеров; Conventional Commits; атомарность; автономия).
  Воркер — инструмент, никогда не соавтор. @impl/done

## Done (collapsed — see `git log` and the §7 LOG) {#done}

##WAL-DONE **2026-08-05, this run: 49 коммитов, 13 строк бэклога закрыто,
группа №1 закрыта целиком.** Панель починена структурно (ключ изъятия карты,
643 единицы с нулём рёбер ушли). Построены: спека поверхности конформа Rust;
дом корневых ключей конфига; no-zombie-проба в трёх стеках; текст причины
отступления (движок + фронтенд); сканер JTD-схем; диагноз манифеста; диагноз
зеркал. Аудит: `-03` принята с измеренным замещением, `-05` починена, `-09`
наполовину, `-15` заведена и закрыта в тот же день, `-16` заведена и закрыта;
`-04`/`-10`/`-11`/`-12` пересужены. Ранее: волна Г и все четыре волны карты
2026-08-05; фаза D закрыта 2026-08-03. @impl/done

## In progress {#in-progress}

##WAL-INFLIGHT **Ничего не в полёте.** Воркеров нет; `.wt/` пуст; дерево
чистое. Пакет для B-045 написан и готов к запуску. @impl/done

## Known issues {#known-issues}

- ##WAL-KI-GITHUB-UNREACHABLE **Зеркало `github` НЕДОСТИЖИМО** — ssh на
  `git@github.com` заворачивается на `127.92.0.49` (петлевой ⇒ перехват порта
  22 на машине). **Не расхождение**; форсить нельзя. Требует рук владельца:
  `ssh -vT git@github.com` и `git config --get-regexp 'url\..*\.insteadof'`. @impl/done
- ##WAL-KI-GITVERSE-BEHIND-TWO **`gitverse` синхронен до `586e7c7`** — два
  последних коммита не разосланы; прогнать `cargo xtask mirror`. @impl/done
- ##WAL-KI-BACKLOG-MID-MIGRATION **Бэклог в середине миграции:** 50 строк, из
  которых живых не более 13, и различить их сейчас нечем — рулинг снял статусы,
  а строки, закрытые до него, остались. Решение об удалении — владельца. @impl/done
- ##WAL-KI-AUDIT-OPEN **`AUDIT.md` несёт открытые находки** — `-01`, `-04`,
  `-06`, `-07`, `-10`, `-11`, `-13`, `-14`, райдер `2026-06-12-01`. Durable
  home находок аудита; в бэклоге не дублируются. @impl/done
- ##WAL-KI-PHASE-E-SIX **Шесть файлов корпуса стоят `work`** — третье условие
  гейта выхода фазы E, ждёт рулинга. @impl/done
- ##WAL-KI-UNIX-HALF-UNCHECKED **Половина `#[cfg(unix)]` ячейки перехвата
  stderr не проверяется компилятором на этой машине.** @impl/done
- ##WAL-KI-VACUITY-AND-SCHEMA-ROOTS **Проверка пустоты сканирования ослабла
  сканером JTD:** она срабатывает на нуле элементов кода, а схемы теперь дают
  элементы — проект с заполненным `schema_roots` и сломанным `scan_roots` её
  не тронет. Названо при постройке, не починено. @impl/done

## Session context {#session-context}

##WAL-CTX-BOOT **Холодная сессия начинается с quick-start кампании**, читает
`CONTINUE.md`, закон транспорта
`campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` ЦЕЛИКОМ, живые строки
`BACKLOG.md` и открытые находки `AUDIT.md` — и берёт каждое число из команд
наверху. `CONTINUE.md` — снапшот холодного возврата; этот файл его суперсидит. @impl/done
