# WAL — Project Continuation State {#root}

_Updated: 2026-08-05, wind-down №12 (**ВОЛНА Г ЗАКРЫТА, И С НЕЙ ВСЕ ЧЕТЫРЕ
ВОЛНЫ КАРТЫ.** Затем владелец повернул сессию к бэклогу: сперва измерить, что в
нём уже построено, и только потом строить. Замер дал 10 строк из 39, просивших
построить построенное. Три строки построены. Живых 25 из 62. Рулинг владельца:
план — не источник истины, статусы с плановых документов сняты.)_

##WAL-NUMBERS-COME-FROM-COMMANDS **Every number below is reproduced by
commands; run them rather than quoting this file.** @impl/done

```bash
python campaigns/packages-2026-09/tasks/drift-registry.py
python campaigns/packages-2026-09/tasks/summary.py
python campaigns/packages-2026-09/tasks/text-stability.py
```

## Current phase {#current-phase}

##WAL-PHASE **Progress Control (PROP-043) — wave 2, `packages-2026-09`:
Phase E RUNNING under the EXTENDED mandate (2026-08-04, §7 LOG: «Хочу все
остальные волны сделать»). **ВОЛНЫ А, Б, В, Г ЗАКРЫТЫ** — §4 карты не имеет
открытой волны; остаток там `##WAVE-PARKED`, вынесенный за волны по
построению. Работа перешла на **осушение плана** по рулингу владельца
2026-08-05. @impl/done

##WAL-STATE **State at checkpoint** (2026-08-05; the commands supersede):
registry **11 575 confirmed / 182 drift**, 0 stale, 0 owed. Corpus 273 files;
`progress check` clean. Panel GREEN (bare, tail read) with a **new step —
`cargo xtask specmap --check`**. `main` @ `010d7104`, 38 commits this session;
`gitverse` synced, **`github` BEHIND** (ssh intercepted at `127.92.0.49`, not a
divergence). Backlog: **25 live rows of 62**. @impl/done

## Next {#next}

1. ##WAL-NEXT-BACKLOG **Осушать план дальше — и он в ТРЁХ домах, не в двух.**
   `BACKLOG.md` 25 живых из 62; `AUDIT.md` **10 открытых находок** из 25
   (последний прогон 2026-08-03, durable home, в бэклоге не дублируются);
   `campaigns/packages-2026-09/deferrals.md` 5 хвостов. `TOOLING-MAP.md` —
   не бэклог, а порядок волн и 11 развилок владельца. Итого 40 пунктов. @spec/done
2. ##WAL-NEXT-GROUP-ONE **Первой — группа «дисциплина не наведена на себя»:**
   B-002, B-004 (не начаты), B-060 и B-061 (записаны как «Specified, not
   built» с маршрутом). Она дешёвая и ядовитая: каждая строка есть причина,
   по которой гейт молчит там, где должен кричать, а тесты фазы T зафиксируют
   текущее поведение — включая неверное. @spec/done
3. ##WAL-NEXT-THEN-TESTS **Тесты — ПОСЛЕ бэклога** (решение владельца
   2026-08-05). Фаза T стоит между E и F, её периметр 6 963 факта из 10 825, её
   спека не ратифицирована. Первой её половиной идёт триажа — она дешёвая и её
   побочный продукт есть инвентарь, который нужен любому решению о форме. @spec/done
4. ##WAL-NEXT-PHASE-E-RULING **Гейт выхода фазы E измерен и ждёт одного
   рулинга:** 273 файла, 267 `done`, 6 `work` (три дизайна закрытых волн, два
   ручных теста, черновик PROP-010). Рулинг нужен потому, что волны Б и В
   несут на карте `@doc/work`, а WAL называет их закрытыми. @spec/done
5. ##WAL-NEXT-OWNER-OPEN **Ждёт владельца, не блокирует:** B-007 (жанр ADR —
   рулинг, не стройка), B-024 (`disputed`), B-017 (содержание тира
   приватности), B-020 (креды), B-015 (безопасность канала — последней). @spec/done

## Constraints — do not violate {#constraints}

- ##WAL-C-PLAN-IS-NOT-TRUTH **План — не источник истины (владелец,
  2026-08-05).** Дословно: «бэклог — это ПЛАН… Произведённые в ходе этого
  плана спецификации — уже являются источниками истины. Когда всё в плане
  реализовано, план больше не нужен». Отсюда: статусы с `BACKLOG.md`,
  `TOOLING-MAP.md` и закона транспорта **сняты** (118 маркеров); якоря
  оставлены — на них ~160 ссылок. **Строка умирает вместе с коммитом, который
  делает её неправдой**, и закрытие есть переезд содержимого в спеку, а не
  флип поля. @impl/done
- ##WAL-C-PLAN-ROTS-MEASURED **Плану, которому верят, это стоит четырнадцати
  протухших утверждений за день.** Три фразы в строке B-056, две `disposition`
  (B-010 `open` и B-011 `planned` на построенном), затем сплошной замер: **10
  из 39 живых строк просили построить построенное**. Самая дорогая — B-041:
  её `planned` заставила босса рекомендовать строить карту развития, лежавшую
  в корне третьи сутки. @impl/done
- ##WAL-C-INFERENCE-IS-ONE-FRONTIER **Граница инференса — одна веха, а не нить
  через дерево.** Сегодня дерево не запускает моделей: обе реализации
  `InferenceBackend` перекладывают рассуждение на вызывающего агента, а
  `vibe-llm` — девятистрочная заглушка до v1.5. Требуют инференса ровно две
  строки (B-020, B-042); безопасность канала (B-015) — последней и не раньше
  канала. @impl/done
- ##WAL-C-MEASURE-WHAT-IS-BUILT-FIRST **Перед тем как реализовывать план,
  измеряют, что из него уже реализовано.** Иначе часть работы — возврат к
  тому, что есть. Замер делается по АВТОРСКОМУ дереву: находка в `vibedeps/`,
  `.vibe/` или `legacy-spec/` ничего не доказывает про оригинал. @impl/done
- ##WAL-C-TWO-GATES-TWO-LISTS **Два гейта держат два списка изъятий, и один из
  них никогда не гонялся.** `vibe-spec` изъят в `conform.toml` с записанной
  причиной; `specmap.toml` её не отзеркалил, хотя сам говорит, что списки
  держатся в шаге. Нашлось только при заведении хостового гейта. Изъятие в
  одном гейте — повод проверить второй. @impl/done
- ##WAL-C-A-COMPOUND-FACT-IS-JUDGED-CLAUSE-BY-CLAUSE **Предложение с двумя
  независимыми утверждениями требует ссылки НА КАЖДОЕ.** PROP-014 §2.3 стоял
  `confirmed` по двум адресам, и оба про первую половину; вторая клауза не
  имела ссылки вовсе и была ложной. Два проверенных адреса делают всё
  предложение проверенным на вид. @impl/done
- ##WAL-C-A-REFUTED-SENTENCE-HAS-MANY-HOMES **Опровергнутая фраза живёт во
  всех местах, где её пересказали.** У B-056 их было четыре — дизайн,
  PROP-035, комментарии `merge.rs`, строка бэклога; посадка исправила два.
  Реестр не может быть инструментом поиска остальных: `campaigns/` —
  структурное исключение движка, корневые документы не совпадают ни с одним
  include-глобом. Охота за домами — шаг посадки. @impl/done
- ##WAL-C-A-TRUNCATED-PIPE-READS-GREEN **Усечённый ВИД вывода — тот же дефект,
  что проглоченный код возврата.** `cargo test --workspace | grep … | head -40`
  спрятал строку `FAILED` за границей `head`, и прогон был объявлен зелёным;
  панель через десять минут была красной на 12 тестах. Гейт слияния — ПАНЕЛЬ;
  предпанельная проверка, расходящаяся с ней, не стоит ничего. @impl/done
- ##WAL-C-VENDORED-INCLUDES-STACK-CRATES **Вендорятся не только движки:**
  mcp-пакеты зеркалят раскладку крейтов своего стека, поэтому
  `typescript-ai-native-cli` имеет копию под `typescript-ai-native-mcp`.
  `cargo xtask sync-engines` идёт ОТДЕЛЬНЫМ шагом после любой правки
  авторского крейта, а не после того, как панель поймает. 51 пара. @impl/done
- ##WAL-C-THE-TAIL-IS-THE-CRATES-THE-PACKET-DID-NOT-NAME **Хвост посадки
  приземляется в крейтах, которых пакет не называл.** Правка, ужесточающая
  общий тип, называет потребителей, которых воркер МОЖЕТ проверить; на
  остальное босс закладывает рабочим-пространственный прогон. @impl/done
- ##WAL-C-RESULT-EVENT-IS-TERMINAL **Терминальный сигнал воркера — событие
  `{"type":"result"}` в stream-json**, а не `TASK-DONE` (воркер может не
  сказать его вовсе) и не греп маркера (совпадает с текстом самого пакета).
  Форма запуска — БЕЗ хвостового `&`, иначе нотификация приходит от обёртки. @impl/done
- ##WAL-C-MTIME-IS-NOT-LIVENESS **Правило «mtime старше 5 минут = зависание»
  не может сработать:** телеметрия размышления пишет строку на токен и держит
  mtime вечно свежим. Живость — метка последнего НЕ-телеметрийного события;
  для кодового пакета — число вызовов `Edit`. @impl/done
- ##WAL-C-VERDICT-STANDARD **The verdict standard.** PRESCRIBES → confirmed
  when coherent and every referent resolves; DESCRIBES → checked against the
  tree; unexercisable → unverifiable; `world` adds source 2, §3.8 bounds it. @impl/done
- ##WAL-C-DESIGN-FACTS-ARE-JUDGED-AGAINST-THE-BUILD **Факты дизайна судят
  ПОСЛЕ его построек, не в сессии авторства.** Дизайн B-056 судился самим
  собой, и эта сессия правила один из тех вердиктов. Дизайн B-040 отложили —
  и суд против построенного дал 21 SUPPORTS / 11 PARTIAL / 3 NO-CODE, из них
  десять фактов держали `@spec/plan` на работе, приземлённой часами раньше. @impl/done
- ##WAL-C-STALENESS-OVER-REPORTS **A document-level staleness signal
  over-reports by orders of magnitude.** `tasks/text-stability.py` сравнивает
  абзац каждого судимого факта побайтно. Чистый результат лицензирует ПЕЧАТЬ и
  никогда не утверждение о свежести. @impl/done
- ##WAL-C-ONE-LAW-ONE-IMPLEMENTATION **One law, one implementation — and the
  divergence of two is SILENT by nature.** Из трёх сканеров конформа разошёлся
  один: TS и Go сравнивали с тем же путём, который хранят, Rust — нет. @impl/done
- ##WAL-C-JUDGE-WHERE-PROVENANCE-LIVES **Catch it where the provenance still
  exists.** @impl/done
- ##WAL-C-A-DOCBLOCK-CAN-LIE **Код, обещающий больше, чем делает, — тот же
  дефект, что тихая потеря.** Заплачено снова: докблок `Watcher` утверждал,
  что реализация живёт в хосте (её нет), а `implements`-ребро заставляло карту
  считать REQ покрытым. @impl/done
- ##WAL-C-DEGENERATE-CASE-IS-THE-REGRESSION-GATE **Сохранённое старое имя как
  вырожденный случай — регрессионный гейт, а не вежливость.** @impl/done
- ##WAL-C-FILE-BUDGET-IS-NEUTRAL **Бюджет 600 строк — нейтральный ключ
  конформа, считает любой файл, тестовые в том числе;** меряет босс и только
  после `cargo fmt`. @impl/done
- ##WAL-C-MARK-NOT-SUPPRESS **Помечать, а не гасить (владелец, 2026-08-01).** @impl/done
- ##WAL-C-SIGNAL-NOT-WALL **Проверка свежести — СИГНАЛ, а не гейт.** @impl/done
- ##WAL-C-SILENCE-IS-THE-DISEASE **Когда законное проходит незамеченным, лечи
  молчание, а не состояние.** @impl/done
- ##WAL-C-MEASURE-BEFORE-ASSERTING **Правдоподобная модель системы — не
  измерение.** Дизайн B-040 опроверг сам себя ЧЕТЫРЕЖДЫ, и три раза до того,
  как был написан код. @impl/done
- ##WAL-C-EVIDENCE-IS-NOT-A-VERDICT **Воркеры собирают доказательства, вердикт
  пишет босс,** и раздел механический: собранная строка приходит `PENDING`,
  который `merge-verdicts.py` отвергает. @impl/done
- ##WAL-C-WORKER-JUDGMENT **GLM-воркеры эскалируют настоящее суждение — чтите
  его.** Эта сессия: воркер поправил замер самого пакета (15 цепочек внутри
  крейта, 2 снаружи) ДО правки; другой измерил совместимость и доказал, что
  правка не меняет ни одного файла. @impl/done
- ##WAL-C-BUILD-FIRST **BUILD-FIRST (владелец, 2026-08-02).** @impl/done
- ##WAL-C-PARITY-IS-THE-INVARIANT-NOT-THE-CODE **Паритет — тот же инвариант,
  никогда тот же код.** @impl/done
- ##WAL-C-STRUCT-FIELD-RIPPLE **Добавление поля или варианта ломает каждую
  литеральную конструкцию и каждый исчерпывающий match**; находит только
  `--all-targets`. @impl/done
- ##WAL-C-REAL-MIRROR **Настоящее зеркало — `vibe progress mirror --campaign
  <zone>`**; любое изменение набора якорей требует его до `merge-verdicts.py`;
  **merge и seal НИКОГДА не цеплять одной командой.** @impl/done
- ##WAL-C-CAMPAIGN-FRAME **Рамка кампании.** Волны карты исполняются фазами
  кампании; **T/F/G остаются вне добра**; публикация — отдельная операция
  после рефакторинга; версии НЕ бампаются до предпубликационной границы. @impl/done
- ##WAL-C-NO-PAUSE **Мандат исполняется автономно** — останов только на
  настоящей развилке владельца, по одной. @impl/done
- ##WAL-C-SELF-COORDINATE **Хост — это пакет (B-031).** `spec://vibevm/…`
  парсится и НИКОГДА не разрешается. @impl/done
- ##WAL-C-PERIMETER **Закон периметра.** SPEC в `core-ai-native`, ENGINE в его
  крейтах, DRIVER в CLI стека, DEPLOYMENT у потребителя. @impl/done
- ##WAL-C-OWN-CORPUS **Кампания внутри собственного корпуса:** корпус —
  `spec/**` и два пакетных пространства; `campaigns/**` исключён структурно в
  движке, корневые документы не совпадают ни с одним include-глобом. @impl/done
- ##WAL-C-NO-MEASUREMENTS-ANSWER **«Замеров нет и нескоро будет»** — стоячий
  ответ; не поднимается заново. @impl/done
- ##WAL-C-PRESENTATION-FORMAT **Формат подачи (обязателен).** Суть
  по-человечески БЕЗ чтения спек → дерево для развилок (`AskUserQuestion` с
  рекомендацией) → точные имена приложением; спеки не цитировать. Развилки —
  по одной. **И когда владелец говорит «я ничего не понял» — объяснять с
  нуля.** @impl/done
- ##WAL-C-DELEGATION **Транспорт воркеров E/T** (механика
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` — читать ЦЕЛИКОМ;
  переключатель `SUBAGENT-MODE.toml` перечитывать перед КАЖДЫМ fan-out'ом,
  сейчас `claudez`): запускаторы `C:\Users\olegc\opt\bin\{claudez,claudez2}`,
  свои state-каталоги, `-c` ключуется парой (state dir, cwd) — **поэтому
  доработка `-c` идёт ИЗ worktree воркера и ТОЛЬКО после конца его прогона.**
  Логи — в `C:\Users\olegc\git\v\cache\agents\sorted\<task-id>\`;
  WORKER-REPORT обязателен; полные пути логов — в чат при каждом запуске. @impl/done
- ##WAL-C-ARCHIVE-BEFORE-CLEANUP **Отчёт воркера архивируется СВОИМ шагом, до
  любой уборки** — снятие worktree на Windows часто невозможно, и отчёт
  уезжает вместе с ним. Измерено: десять остатков `.wt/` против двух
  worktree, которые знал git, и два неархивированных отчёта. @impl/done
- ##WAL-C-REAL-EXITS **Коды возврата читаются НАСТОЯЩИМИ, никогда через
  pipe/grep** (`${PIPESTATUS[0]}`, если хвост неизбежен), и **вид вывода не
  усекается**. Панель фоном — bare-формой. @impl/done
- ##WAL-C-COMMIT-MESSAGE-VIA-FILE **Сообщение коммита идёт через heredoc или
  `-F`, никогда через `-m "…"` с обратными кавычками.** @impl/done
- ##WAL-C-SHELL-TRAPS **Ловушки оболочки:** cwd Bash-инструмента ПЕРСИСТЕНТЕН —
  абсолютные пути или `git -C`; **CRLF против `str.replace` — только
  редакторские инструменты**, питон-переписывание открывать с `newline=''` с
  обеих сторон; снятие worktree с артефактами сборки упирается в MAX_PATH
  (`-c core.longpaths=true`). @impl/done
- ##WAL-C-STAGE-EXPLICIT **Никогда `git add -A`;** стейджить явные пути. @impl/done
- ##WAL-C-DURABLE-CITATIONS **Брифы цитируют только durable-файлы;**
  wind-down обесценивает доказательство, ссылающееся на `CONTINUE.md`/`WAL.md`. @impl/done
- ##WAL-C-ATTRIBUTION **Правила 1–4 связывают каждый коммит** (человеческая
  атрибуция — без AI-трейлеров; Conventional Commits; атомарность;
  автономия). Воркер — инструмент, никогда не соавтор. @impl/done

## Done (collapsed — see `git log` and the §7 LOG) {#done}

##WAL-DONE **2026-08-05, this run: волна Г закрыта целиком, план осушается, 38
коммитов.** Реестровый долг B-056 (19 вердиктов, два файла запечатаны); F-132
закрыт правкой ложной клаузулы; B-040 — дизайн плюс четыре посадки при одной
отклонённой измерением и sealed traits как сознательном отказе; дизайн B-040
просужен целиком против построенного (35 вердиктов); замер бэклога по дереву
(10 из 39 уже построены); B-062 закрыт рулингом владельца (118 маркеров сняты);
построены B-048, B-059, B-014. Ранее: обе дыры гейта и реестровая гигиена
2026-08-05; волна В 2026-08-04/05; волны Б и А целиком; фаза D закрыта
2026-08-03. @impl/done

## In progress {#in-progress}

##WAL-INFLIGHT **Ничего не в полёте.** Воркеров нет; все worktree сессии
сняты; дерево чистое; `main` синхронен с `gitverse`. @impl/done

## Known issues {#known-issues}

- ##WAL-KI-GITHUB-BEHIND **Зеркало `github` ОТСТАЁТ** — `git@github.com` ssh
  заворачивается на `127.92.0.49` (loopback ⇒ перехват на машине). **Не
  расхождение**; следующий fan-out догонит fast-forward. Никогда `--force`. @impl/done
- ##WAL-KI-AUDIT-OPEN **`AUDIT.md` несёт 10 открытых находок** из 25;
  последний прогон 2026-08-03. Это durable home находок аудита, в бэклоге они
  не дублируются — и при «сделать бэклог» их надо считать отдельно. @impl/done
- ##WAL-KI-PHASE-E-SIX **Шесть файлов корпуса стоят `work`** — три дизайна
  закрытых волн, два ручных теста, черновик PROP-010. Это третье условие
  гейта выхода фазы E, и оно ждёт рулинга. @impl/done
- ##WAL-KI-B060-B061 **B-060 и B-061 записаны как «Specified, not built»** с
  маршрутом и ценой: схемы JTD не размечены и прочитаны быть не могут;
  `implements`-ребро от объявления заявляет покрытие, которого нет. @impl/done
- ##WAL-KI-UNIX-HALF-UNCHECKED **Половина `#[cfg(unix)]` ячейки перехвата
  stderr не проверяется компилятором на этой машине.** @impl/done
- ##WAL-KI-DESIGN-DRIFTS **Шесть drift-вердиктов стоят против документов,
  переживших свой предмет.** @impl/done
- ##WAL-KI-WT-LEFTOVERS **`.wt/` держит два handle-locked остатка**
  (`E14-L4-PARITY-LOOP`, `P-GOFLAG-RULE`) — снять, когда отпустят хэндлы.
  Восемь других остатков сняты этой сессией. @impl/done

## Session context {#session-context}

##WAL-CTX-BOOT **Холодная сессия начинается с quick-start кампании**, читает
`CONTINUE.md`, закон транспорта
`campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` ЦЕЛИКОМ, `TOOLING-MAP.md`
§4–§5, живые строки `BACKLOG.md` и открытые находки `AUDIT.md` — и берёт
каждое число из команд наверху. `CONTINUE.md` — снапшот холодного возврата;
этот файл его суперсидит. @impl/done
