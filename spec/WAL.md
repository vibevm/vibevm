# WAL — Project Continuation State {#root}

_Updated: 2026-08-20 (**слайс снятия закрыт целиком** — все три операции снятия
построены: `remove`, `yank`, `bury`. Пункт 5, склад пакетов, **измерен и
нарезан**, и по нему приняты все решения владельца. 13 коммитов, шесть зелёных
панелей, три принятых делегированных пакета, долг 0 после каждого захода,
зеркала раскатаны шесть раз.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета → короткий доклад → исполнение. Промт есть слово
владельца. @status:impl/done

@fact:WAL-HOW-TO-TALK-TO-THE-OWNER **Форма доклада владельцу (правило
2026-08-20, стоило непонятого доклада).** Сперва **простыми словами**: в чём
суть проблемы, решения, рекомендации. Технические подробности — отдельным
блоком после. **Номера разделов и якорей спек не приводить** — владелец их не
откроет. Точность при этом не теряется: не «есть настройка в файле», а «файл
такой-то, делает то-то». Есть внутренняя структура — показать деревом. @status:impl/done

## Current phase {#current-phase}

@fact:WAL-PHASE **Пункты 1–4 слайса закрыты.** Координата `B-056` починена
(живая развилка стала `B-091`); факт снятия `buried` заменил `renamed` и дал
проектору **первое в его истории порождающее плечо**; построены глаголы `yank`
и `bury`. Контракт получил не случай, а **правило**: оба отказа записаны как
свойство всякого глагола снятия. @status:impl/done

@fact:WAL-STORE-IS-MEASURED-AND-CUT **Пункт 5 — склад пакетов — измерен, а не
начат.** `PROP-010` несёт 107 фактов и **ни одного `impl/done`**, то есть
утверждает, что не построено ничего; замер даёт **17 построенных целиком и 26
частично**. Нарезка со статусов документа была бы планом про несуществующее
дерево. Измеренная база — `campaigns/packages-2026-09/harvest/prop010-current-state.md`,
вердикт каждому из 107 со сходимостью суммы. @status:impl/done

@fact:WAL-SEVEN-RULINGS **Семь решений владельца 2026-08-20, все записаны в
спеки в тот же день:** @status:impl/done

1. @fact:WAL-R-STORE-NAME **Склад — `~/.vibe/cache/`.** Имя не конфликтует с
   проектной папкой, потому что проектная `.vibe/cache/` **удаляется тем же
   коммитом**: склад её заменяет, а не сосуществует с ней. @status:spec/work
2. @fact:WAL-R-PROMOTION **Это подъём, а не стройка.** Распакованный слой по
   идентичности уже есть, проектный; меняются уровень, режим записи и роль. @status:spec/work
3. @fact:WAL-R-WRITTEN-ONCE **«Пишется один раз» связывает НАШ код, а не диск.**
   Владелец: это его жёсткий диск. Обязательств три — не переписывать запись,
   держать проверку **командой**, а не налогом на каждую установку, и называть
   расхождение вместо проглатывания. @status:spec/work
4. @fact:WAL-R-CHECK-REPAIR **`vibe cache check` / `--repair`** — единственное
   место полного пересчёта. Починка лестницей от дешёвого: гитовое ли дерево →
   снять повреждения и сбросить на закреплённый коммит → пересчитать → и лишь
   потом качать заново. **Обновления вперёд в лестнице нет** (довод мой,
   владелец оставил вопрос открытым): восстанавливаемый хэш принадлежит одной
   версии. @status:spec/work
5. @fact:WAL-R-REFRESH-VS-SWITCH **Обновление копии и смена источника — разные
   операции.** Обновление — на месте; только смена качает с нуля, во временный
   каталог рядом, с заменой при успехе. Сегодня стирание срабатывает при ЛЮБОЙ
   неудаче. Мера владельца: зависимость может весить десять гигабайт. @status:spec/work
6. @fact:WAL-R-CONFIG-SPLIT **Реестры остаются отдельным файлом**
   `~/.vibe/registry.toml`, дом настроек — `~/.vibe`, не XDG. И то и другое
   **уже так в коде**; ошибался документ. Довод — разделяемость: файлом реестров
   делятся с коллегами, не отдавая личные настройки. @status:impl/done
7. @fact:WAL-R-WALK-IS-SILENT **Полный обход индексатора не сообщает об
   исчезнувших пакетах.** Отклонена реальная и дешёвая возможность: обход держит
   оба состава и не сравнивает их. У пропажи слишком много невинных причин.
   Закону о молчании не противоречит — тот про снятие, действие человека. @status:spec/plan

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
scan, потом mirror, и только затем долг — иначе он отвечает про вчера. После
`merge-verdicts.py` нужен ЕЩЁ один scan, иначе проекция уедет коммитом позже:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan   --campaign campaigns/packages-2026-09
cargo run -q -p vibe-cli --bin vibe -- progress mirror --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста, 53 шага
cargo xtask mirror --check        # читать СТРОКИ, не только хвост
```

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист,
HEAD — коммит рулингов; панель `self-check: all green` **шесть раз за сессию**,
каждый 53 шага с реальным кодом выхода 0; корпус 281 файл, 13929 маркеров,
0 неразмеченных; долг **0 неосуждённых, 0 осиротевших**, 37 stale; карта —
6173 спек-юнита / 1047 tagged / 978 рёбер / 0 подозрений / 0 сирот. Воркеров
нет, `.wt/` пуст. @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты.**
`vibe check` даёт **44 info** `manifest_epoch` и **1 warning**
`local_source_freshness`. За сорок с лишним посадок подряд оба не сдвинулись
ни разу. **Рост любого — находка, а не шум.** @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-STORE **Склад в `~/.vibe/cache/`** — подъём уровня, режим
   записи один раз, чтение как источника, удаление проектной папки тем же
   коммитом. @status:spec/work
2. @fact:WAL-NEXT-REFRESH **Развести обновление и смену источника**, затем дать
   смене временный каталог с заменой при успехе. @status:spec/work
3. @fact:WAL-NEXT-CACHE-WINS **Попадание в склад важнее молчащего реестра** — у
   резолвера сегодня офлайн-режима нет вовсе, сужение идёт выше него. @status:spec/work
4. @fact:WAL-NEXT-CACHE-CMDS **Семья `vibe cache …`** — `check`, `--repair`,
   `path`, `list`, `add`, `clean`. Не построено ничего. @status:spec/work
5. @fact:WAL-NEXT-OFFLINE **Глобальный `--offline`** — флаг на корне,
   `VIBE_OFFLINE`, ключ конфигурации. **Не спутать с существующим**: тот живёт
   на подкоманде `install` и принадлежит другой спеке. @status:spec/work
6. @fact:WAL-NEXT-OWNER **Живых СТОП-ВЛАДЕЛЕЦ два:** **S2** (org-права) и
   **умолчание установки в `README.md`** — он документирует `~/opt/bin`, что для
   `vibe` на этой машине уже неверно; менять ли умолчание в продукте — вопрос
   владельцу. @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-ANCHOR-WITH-A-VERDICT-IS-NEVER-REPLACED **Якорь, у которого есть
  вердикт, не заменяют — ему ставят надгробие с наследником.** Сверка гоняется
  дифом множеств якорей против HEAD ПЕРЕД посадкой всякой правки спеки. @status:impl/done
- @fact:WAL-C-EMPTY-OUTPUT-IS-A-CLAIM **Пустой ИЛИ ОБРЕЗАННЫЙ вывод есть
  утверждение.** Скорми инструменту случай, который он ОБЯЗАН различить. Новое
  за эту сессию: `ls | grep | head -5` был прочитан как «файла в корпусе нет», а
  он там был — четыре факта уехали неосуждёнными и всплыли на проверке долга.
  Контроль был, но обрезка съела ответ. @status:impl/done
- @fact:WAL-C-EVERY-LANDING-FALSIFIES-THE-PREVIOUS-STATUS **Каждая посадка
  делает ложными утверждения предыдущей о сегодняшнем состоянии.** За сессию их
  нашлось: «не построено» в трёх местах, «шесть вариантов» в докблоке,
  «одно устаревающее предложение» там, где их четыре. **Счёт в прозе подстрока
  с именем предмета не находит** — искать надо отдельно. @status:impl/done
- @fact:WAL-C-RECORDING-A-DECISION-FINDS-DEFECTS **Запись решения находит
  дефекты, которых не находит поиск.** Сверка написанной спеки с пакетом,
  который её исполняет, показала перекрытие двух правил отказа — до того, как
  появился код. @status:impl/done
- @fact:WAL-C-A-COUNT-CAN-BE-A-PROPERTY-OF-THE-TOOL **Счёт бывает свойством
  инструмента.** Всякое число, на котором стоит решение, меряют двумя способами. @status:impl/done
- @fact:WAL-C-THE-PANEL-MUST-COVER-WHAT-LANDS **Панель, запущенная до правки, не
  про то дерево.** Дешевле снять и перезапустить, чем ждать оба cargo на одном
  `target/`. @status:impl/done
- @fact:WAL-C-CODEGEN-DIES-ON-A-PIPE **`cargo xtask codegen` нельзя пропускать
  через `head`**: обрыв по SIGPIPE уничтожает сгенерированное дерево (22 файла),
  после чего `codegen` сам не запускается — он зависит от того, что стёр.
  Лечится `git restore crates/vibe-wire/src/generated/`. Заведено B-093. @status:impl/done
- @fact:WAL-C-MIRROR-CHECK-SAYS-IN-SYNC-OVER-A-BEHIND-HOST **`mirror --check`
  печатает `BEHIND <хост>` и следом «all targets in sync», код выхода 0.**
  До рулинга (B-090) сверять строки, а не только хвост. @status:spec/plan
- @fact:WAL-C-GITVERSE-IS-FLAKY **GitVerse отвечает через раз** — лечится
  повтором. Отказ push'а на один хост — НЕ расхождение. @status:impl/done
- @fact:WAL-C-A-PROMISE-IS-RE-MARKED-NOT-DELETED **Спека, обещающая
  непостроенное, чинится СТАТУСОМ, а не удалением требования.** @status:impl/done
- @fact:WAL-C-JUDGING-DEBT-READS-THE-MIRROR **`judging-debt.py` читает зеркало.**
  Порядок scan → mirror → batch → merge → **scan снова** → debt обязателен
  целиком: без второго скана проекция кампании уедет коммитом позже. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён тем
  же заходом.** `campaigns/**`, `BACKLOG.md`, `TASKS.md` и `spec/WAL.md` в
  судимый корпус НЕ входят; **`spec/boot/90-user.md` — ВХОДИТ**. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель обрывается на первом красном
  шаге.** Зелёный хвост — единственное доказательство, что прогнались все. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ** (`--no-fail-fast`); **`cargo fmt` без `--check` выходит с
  нулём и когда результат НЕ чист**; **красный СУЩЕСТВУЮЩИЙ тест при аддитивной
  правке есть дефект правки**. @status:impl/done
- @fact:WAL-C-RED-BEFORE-GREEN **Страж доказывается КРАСНЫМ прогоном.** За
  сессию доказаны красным все восемь новых стражей: обезвреживание охраняемого
  роняет ровно свой тест и ни один другой. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки:** применить дифф →
  `cargo fmt --all` → **`cargo xtask specmap`** → стейдж → панель → суд →
  коммит → `cargo xtask mirror`. @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ до
  первого фан-аута; режим `claudez` — это Claude Code на GLM-5.2, дешёвый слот.
  **Закрытый список записи обязан называть файл, который правка ЛОМАЕТ**, а не
  только создаёт. **Чекпойнт между пакетами**: перевести worktree на посаженную
  базу — и диф вернёт только второй пакет; `-c` сохраняет контекст и тёплый
  `target/`. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: никогда голый `cd`; `cmd | tail; echo $?`
  печатает код ПАЙПА; правки только editor-инструментами; python — с
  `PYTHONIOENCODING=utf-8` и виндовым путём. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты heredoc
  `-F -`; раскатка только `cargo xtask mirror`; Rules 1–4 связывают каждый
  коммит. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки дисциплины
  не трогаются. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-19…20, 13 коммитов (`89295cbd`..`8097105d`):** починка
координаты и находка B-092; замер периметра `renamed`; догон проекции кампании;
факт снятия `buried` с первым порождающим плечом проектора; находка B-093;
выправка счёта в докблоке; B-094 и уточнение B-093; глагол `yank` с выносом
сторожа блокировки; сведение чек-листа; глагол `bury`; замер `PROP-010` и
нарезка пункта 5; семь рулингов владельца. Шесть зелёных панелей по 53 шага;
три делегированных пакета, все приняты с первого раза. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, дерево чисто. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-B090 **`mirror --check` объявляет «all targets in sync» над
  отставшим хостом (B-090, P2).** @status:spec/plan
- @fact:WAL-KI-B093 **`xtask codegen` уничтожает своё дерево при обрыве (B-093,
  P2)** — и воркеру, которому git запрещён, восстановиться нечем. @status:spec/plan
- @fact:WAL-KI-B094 **Дымовой список подкоманд `vibe-index` односторонний
  (B-094, P3)** — новая подкоманда гейту невидима. @status:spec/plan
- @fact:WAL-KI-B092 **Цитата факта в форме определения заводит вторую координату
  (B-092, P3)**; три места в `BACKLOG.md`, ни одним гейтом не покрыты. @status:spec/plan
- @fact:WAL-KI-README-INSTALL-PATH **`README.md` документирует `~/opt/bin` как
  место установки**, что для `vibe` на этой машине уже неверно после переезда
  хранилища в `~/.vibe/opt`. Менять ли умолчание в продукте — владельцу. @status:spec/plan
- @fact:WAL-KI-OLDER **Прежние строки без изменений:** B-078…B-088 (обещания без
  кода, конверты без схем, атомарная запись без синхронизации каталога),
  три ребра прослеживаемости к PROP-005, печать суда сознательно не поставлена,
  37 файлов stale, аудит `2026-08-06-01` (P1) ждёт кампании S7, шесть файлов
  корпуса на `work` ждут рулинга Phase E. @status:spec/plan

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → `TASKS.md` (слайс) → измеренную базу
`harvest/prop010-current-state.md`** — и берёт каждое число из команд сверху.
Этот файл главнее `CONTINUE.md`; ратифицированный PROP-044 главнее обоих. @status:impl/done
