# WAL — Project Continuation State {#root}

_Updated: 2026-08-06, wind-down №14 (**БЭКЛОГ ОСУШАЕТСЯ ДАЛЬШЕ; ЗАВЕДЁН P1.**
Закрыты B-008, B-045 и старейшая находка аудита `-01` — она была открыта с
2026-05-23. Заведена `2026-08-06-01`, **P1**: треть вердиктов кампании не имеет
собственных доказательств, и один из них был ложным, пока сама же кампания на
пакетной стороне судила тот же смысл `drift`. Четыре измерения соврали за
сессию, и все четыре — греповые.)_

##WAL-NUMBERS-COME-FROM-COMMANDS **Every number below is reproduced by
commands; run them rather than quoting this file.** @impl/done

```bash
python campaigns/packages-2026-09/tasks/drift-registry.py
python campaigns/packages-2026-09/tasks/summary.py
python campaigns/packages-2026-09/tasks/text-stability.py
```

##WAL-RESCAN-BEFORE-QUOTING **Первое дело новой сессии — пересканировать
корпус.** Эта сессия правила `PROP-008` — документ с **92 вердиктами**, — и
кэш кампании об этом не знает: все три команды выше до `vibe progress scan` /
`mirror` отвечают про КЭШ, а не про дерево. `text-stability.py` теперь сам об
этом предупреждает (находка этой сессии), но скан всё равно нужен, и за ним —
пере-суд §2.6: одна изменённая формулировка и один новый факт. @impl/done

## Current phase {#current-phase}

##WAL-PHASE **Progress Control (PROP-043) — wave 2, `packages-2026-09`:
Phase E RUNNING под EXTENDED-мандатом (2026-08-04, §7 LOG). Все четыре волны
карты закрыты; работа идёт по рулингу владельца 2026-08-05 — **сперва осушить
бэклог, к тестам не подходить**. Фаза T НЕ начата. @impl/done

##WAL-STATE **State at checkpoint** (2026-08-06; команды суперсидят): registry
**11 641 confirmed / 177 drift** — цифра ДО пересканирования, см.
`##WAL-RESCAN-BEFORE-QUOTING`. Корпус 274 файла, из них один расходится с
диском. `main` @ `c38725d8` + чекпойнт-коммиты, **11 коммитов работы за
сессию**; `.wt/` пуст, дерево чистое. **Панель прогнана последним действием —
хвост читается ДО раскатки.** `gitverse` синхронен до `586e7c7`; `github`
НЕДОСТИЖИМ. Бэклог **48 строк**, живых не более 11. @impl/done

## Next {#next}

1. ##WAL-NEXT-RESCAN **Пересканировать корпус и пере-судить §2.6 PROP-008** —
   `##WAL-RESCAN-BEFORE-QUOTING`. Дешёвый, механический, обязателен раньше
   любого разговора о числах. @spec/done
2. ##WAL-NEXT-BACKLOG-LIVE **Живые строки бэклога, не запаркованные
   владельцем:** B-019 (б — узел «команда» в карте; в — узел «вариант ошибки»,
   чью границу систем владелец просил решить ДО реализации), B-032 (протокол
   гранулярности планирования), B-046 (мультиязычная композиция — три варианта,
   выбор владельца), остаток B-047 (норма поверхностей — цензус построен,
   осталась доводка дыр и подъём нормы в спеку). Каждую — измерять по дереву
   ПЕРЕД работой. @spec/done
3. ##WAL-NEXT-AUDIT-REST **Аудит:** `-04` (одно решение про TUI-подсистему),
   `-14` (схема контракта индекса) — оба owner-court; `-10` переформулирована
   и ждёт редакторского рулинга. @spec/done
4. ##WAL-NEXT-OWNER-OPEN **Ждёт владельца, не блокирует:** **P1
   `2026-08-06-01`** (доказательства вердиктов — три вопроса); `github`
   (перехват ssh); B-050; миграция бэклога; гейт выхода фазы E; B-007, B-015,
   B-017, B-020, B-024. @spec/done

## Constraints — do not violate {#constraints}

- ##WAL-C-A-GREP-LIES-IN-BOTH-DIRECTIONS **Греп — измерение, которое врёт в обе
  стороны, и «не нашёл» — тоже не факт.** Четыре раза за одну сессию:
  *(i)* `"command":"cargo` не совпал, потому что воркер пишет
  `cd … && cargo …`, и босс едва не отправил отказ верной работе;
  *(ii)* шаблон без проверки квалификации посчитал `flow:org.vibevm.world/wal`
  неквалифицированным (169 против 173); *(iii)* **`grep -P` в этой локали
  отказывается работать**, пишет об этом в поток, который конвейер выбрасывает,
  и возвращает чистый ноль; *(iv)* шаблон `**Decision:**` нашёл 24 секции там,
  где их 146 — в этом дереве метка пишется `**Decision.**`, с точкой. Правило:
  мерить по структурному полю, а не по префиксу свободной строки; отсутствие
  сигнала держать к тому же стандарту доказательности, что и присутствие. @impl/done
- ##WAL-C-FILE-BUDGET-DOES-NOT-CHOOSE-A-TYPE **Бюджет длины файла не выбирает
  форму публичного типа.** Воркер схлопнул структурный вариант ошибки в строку,
  чтобы уложить `lib.rs` в 600; замер был ВЕРЕН (со структурой файл остаётся
  611), вывод — нет. Настоящий шов лежал в самом типе: `SolveError` уехал в свой
  модуль, `lib.rs` 599 → 523 при базовых **590** — файл стоял в десяти строках
  от бюджета ещё до правки, и это ровно класс `##B054-THE-CLASS`. Платить
  данными за строки — не та валюта. @impl/done
- ##WAL-C-A-VERDICT-NO-FACT-CAN-FALSIFY **Вердикт, который не может опровергнуть
  ни один отдельный факт, — не вердикт этого факта.** 4 151 из 11 862 вердиктов
  кампании имеют единственным доказательством абзац, общий с другими; у
  `PROP-008` это 90 якорей на один абзац, и в нём стояла ложная фраза. Заведено
  P1 `2026-08-06-01`; **решение — владельца, агент только поднимает строгость**. @impl/done
- ##WAL-C-A-TOOL-MAY-COMPARE-ITSELF **Инструмент может сравнивать два поля
  внутри собственного кэша и печатать ноль про дерево.** `text-stability.py`
  ответил «0 stale, 0 к пересуду» на правку документа с 92 вердиктами: оба поля
  живут в кэше и оба одинаково протухли. Починено предупреждением, не сменой
  сравнения; exit остался 0 — это измеритель, а не гейт. @impl/done
- ##WAL-C-A-ROW-TAKES-ITS-CITATIONS-WITH-IT **Строка уносит с собой свои
  цитаты.** Закрытие есть переезд содержимого **и ссылок**. Рулинг закрытой
  строки живёт в коммите, который её закрыл, а не по адресу. Проверка: греп
  `](#b-0NN)` по файлу и `BACKLOG.md#b-0NN` по дереву, тем же заходом. @impl/done
- ##WAL-C-A-PROMISE-HAS-MANY-HOMES **Обещание, которого код не выполняет, живёт
  во всех местах, где его пересказали — и их бывает четыре.** У сверки вида:
  спека PROP-008, докблок `PackageRef`, `docs/commands/uninstall.md` и
  `update.md`. Причём доки описывали форму, которую код ОТБИВАЛ. Одна стройка
  сделала все четыре правдой; охота за домами — шаг посадки, а не вежливость. @impl/done
- ##WAL-C-SYNC-COPIES-CRATES-NOT-MANIFESTS **`sync-engines` копирует крейты, но
  НЕ корневые манифесты.** Новая `workspace`-зависимость объявляется в авторском
  стеке И в каждом пространстве, которое вендорит крейт. @impl/done
- ##WAL-C-THE-PANEL-OWNS-THE-USER-HOME **Под бегущей панелью нельзя ни один
  `vibe`-глагол, пишущий домашний каталог.** Правило шире, чем «не гонять cargo
  параллельно»; первый ход при срабатывании на пути `progress-cache` —
  перезапуск панели в одиночку, а не охота за тестом. @impl/done
- ##WAL-C-IMPLEMENTS-IS-A-CLAIM-ABOUT-RUNNING-CODE **`implements` — утверждение
  о коде, который работает** (PROP-014). Голое объявление ребра не несёт. @impl/done
- ##WAL-C-A-MESSAGE-MAY-NAME-THE-WRONG-CAUSE **Сообщение может быть о правильном
  событии и называть неправильную причину.** Цитата REQ этого класса не ловит. @impl/done
- ##WAL-C-MEASURE-WHAT-IS-BUILT-FIRST **Перед реализацией плана измеряют, что
  из него уже реализовано.** За три дня это остановило девятнадцать строек
  построенного. Замер по АВТОРСКОМУ дереву: находка в `vibedeps/`, `.vibe/` или
  `legacy-spec/` ничего не доказывает. @impl/done
- ##WAL-C-PLAN-IS-NOT-TRUTH **План — не источник истины (владелец, 2026-08-05).**
  Строка умирает вместе с коммитом, который делает её неправдой. **Следствие,
  ещё не разрешённое:** строки, закрытые ДО рулинга, остались в файле; решение
  об удалении — владельца. @impl/done
- ##WAL-C-VENDORED-INCLUDES-STACK-CRATES **Вендорятся не только движки:**
  mcp-пакеты зеркалят раскладку крейтов своего стека. `cargo xtask sync-engines`
  идёт ОТДЕЛЬНЫМ шагом после любой правки авторского крейта. 51 пара. @impl/done
- ##WAL-C-ENGINE-FIELD-AND-ITS-FRONTEND-LAND-TOGETHER **Движковое поле и его
  фронтенд приземляются вместе**, иначе дерево красное между коммитами. @impl/done
- ##WAL-C-WORKERS-DO-NOT-FMT **Воркеры не форматируют, и `cargo fmt --all`
  после применения — шаг закона слияния, а не вежливость.** @impl/done
- ##WAL-C-A-NEW-FILE-CHANGES-THE-MAP **Новый файл с `scope!` и новое ребро
  `#[verifies]` двигают коммитнутую карту** — `cargo xtask specmap` идёт в ту
  же посадку, иначе гейт свежести краснеет следующим прогоном. @impl/done
- ##WAL-C-A-TRUNCATED-PIPE-READS-GREEN **Усечённый ВИД вывода — тот же дефект,
  что проглоченный код возврата.** Гейт слияния — ПАНЕЛЬ. @impl/done
- ##WAL-C-RESULT-EVENT-IS-TERMINAL **Терминальный сигнал воркера — событие
  `{"type":"result"}` в stream-json**, а не `TASK-DONE` и не греп маркера.
  Форма запуска — БЕЗ хвостового `&`. @impl/done
- ##WAL-C-MTIME-IS-NOT-LIVENESS **Живость — метка последнего НЕ-телеметрийного
  события и число вызовов `Edit`**, а не mtime. @impl/done
- ##WAL-C-ONE-THREAD-ONE-WRITER **`-c` отправляется только после того, как
  правимый прогон закончился** — иначе два писателя на одном worktree. @impl/done
- ##WAL-C-REJECTION-NAMES-WHAT-IS-ACCEPTED **Отказ называет принятое первым,
  потом ровно один неверный пункт, потом дословный текст для затронутых
  разделов отчёта.** Проверено: доработка вернула всё дословно, включая
  признание, что довод первого прогона был верен, а вывод нет. @impl/done
- ##WAL-C-VERDICT-STANDARD **The verdict standard.** PRESCRIBES → confirmed
  when coherent and every referent resolves; DESCRIBES → checked against the
  tree; unexercisable → unverifiable; `world` adds source 2. @impl/done
- ##WAL-C-A-COMPOUND-FACT-IS-JUDGED-CLAUSE-BY-CLAUSE **Предложение с двумя
  независимыми утверждениями требует ссылки НА КАЖДОЕ.** @impl/done
- ##WAL-C-ONE-LAW-ONE-IMPLEMENTATION **One law, one implementation — and the
  divergence of two is SILENT by nature.** Один закон при ДВУХ точках вызова
  законен, если точки на шве ячеек и разница между ними названа. @impl/done
- ##WAL-C-A-DOCBLOCK-CAN-LIE **Код, обещающий больше, чем делает, — тот же
  дефект, что тихая потеря.** @impl/done
- ##WAL-C-FILE-BUDGET-IS-NEUTRAL **Бюджет 600 строк — нейтральный ключ;**
  меряет босс и только после `cargo fmt`. @impl/done
- ##WAL-C-MARK-NOT-SUPPRESS **Помечать, а не гасить (владелец, 2026-08-01).** @impl/done
- ##WAL-C-SIGNAL-NOT-WALL **Проверка свежести — СИГНАЛ, а не гейт.** @impl/done
- ##WAL-C-SILENCE-IS-THE-DISEASE **Когда законное проходит незамеченным, лечи
  молчание, а не состояние.** @impl/done
- ##WAL-C-A-NORM-WITHOUT-A-CHECKER-DRIFTS **Норма без чекера дрейфует молча.**
  Лицензионная норма стояла в PROP-000 §3 и в леджере владельца, не
  проверялась ничем, и один крейт из двадцати выпал из неё на месяцы. Чекер
  читает список участников из манифеста воркспейса, а не из своей копии. @impl/done
- ##WAL-C-EVIDENCE-IS-NOT-A-VERDICT **Воркеры собирают доказательства, вердикт
  пишет босс.** @impl/done
- ##WAL-C-WORKER-JUDGMENT **Воркеры эскалируют настоящее суждение — чтите его.**
  Эта сессия: одно опровержение (шестая «мис-цитата» законна). @impl/done
- ##WAL-C-BUILD-FIRST **BUILD-FIRST (владелец, 2026-08-02).** @impl/done
- ##WAL-C-PARITY-IS-THE-INVARIANT-NOT-THE-CODE **Паритет — тот же инвариант,
  никогда тот же код.** @impl/done
- ##WAL-C-STRUCT-FIELD-RIPPLE **Добавление поля ломает каждую литеральную
  конструкцию и каждый исчерпывающий match**; находит только `--all-targets`.
  Добавление ВАРИАНТА в enum ошибок безопаснее: потребители ходят через
  `#[from]` и `downcast_ref`, но это надо ИЗМЕРИТЬ, а не предположить. @impl/done
- ##WAL-C-REAL-MIRROR **Настоящее зеркало — `vibe progress mirror --campaign
  <zone>`**; **merge и seal НИКОГДА не цеплять одной командой.** @impl/done
- ##WAL-C-CAMPAIGN-FRAME **Рамка кампании.** **T/F/G остаются вне добра**;
  публикация — отдельная операция; версии НЕ бампаются до предпубликационной
  границы. Версия фронтенда конформа — ключ кэша, её бампать можно. @impl/done
- ##WAL-C-NO-PAUSE **Мандат исполняется автономно** — останов только на
  настоящей развилке владельца, по одной. @impl/done
- ##WAL-C-SELF-COORDINATE **Хост — это пакет (B-031).** `spec://vibevm/…`
  парсится и НИКОГДА не разрешается. @impl/done
- ##WAL-C-PERIMETER **Закон периметра.** SPEC в `core-ai-native`, ENGINE в его
  крейтах, DRIVER в CLI стека, DEPLOYMENT у потребителя. @impl/done
- ##WAL-C-OWN-CORPUS **Кампания внутри собственного корпуса:** `campaigns/**`
  исключён структурно, корневые документы не совпадают ни с одним
  include-глобом. @impl/done
- ##WAL-C-NO-MEASUREMENTS-ANSWER **«Замеров нет и нескоро будет»** — стоячий
  ответ; не поднимается заново. @impl/done
- ##WAL-C-PRESENTATION-FORMAT **Формат подачи (обязателен).** Суть
  по-человечески БЕЗ чтения спек → дерево для развилок → точные имена
  приложением; спеки не цитировать. Развилки — по одной. **И когда владелец
  говорит «я ничего не понял» — объяснять с нуля.** @impl/done
- ##WAL-C-DELEGATION **Транспорт воркеров E/T** (механика
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` — читать ЦЕЛИКОМ;
  `SUBAGENT-MODE.toml` перечитывать перед КАЖДЫМ fan-out'ом, сейчас `claudez`):
  запускаторы `C:\Users\olegc\opt\bin\{claudez,claudez2}`, свои state-каталоги,
  `-c` ключуется парой (state dir, cwd). Логи — в
  `C:\Users\olegc\git\v\cache\agents\sorted\<task-id>\`; WORKER-REPORT
  обязателен; полные пути логов — в чат при каждом запуске. @impl/done
- ##WAL-C-ARCHIVE-BEFORE-CLEANUP **Отчёт воркера архивируется СВОИМ шагом, до
  любой уборки.** @impl/done
- ##WAL-C-REAL-EXITS **Коды возврата читаются НАСТОЯЩИМИ, никогда через
  pipe/grep** (`${PIPESTATUS[0]}`), и **вид вывода не усекается**. Панель
  фоном — bare-формой. @impl/done
- ##WAL-C-COMMIT-MESSAGE-VIA-FILE **Сообщение коммита идёт через heredoc или
  `-F`.** @impl/done
- ##WAL-C-SHELL-TRAPS **Ловушки оболочки:** cwd Bash-инструмента ПЕРСИСТЕНТЕН —
  абсолютные пути или `git -C`; **CRLF против `str.replace` — только
  редакторские инструменты**; **питон-скрипт в heredoc рвётся на экранировании
  — писать скрипт файлом редакторским инструментом и запускать его**;
  `PYTHONIOENCODING=utf-8` обязателен для вывода с кириллицей. @impl/done
- ##WAL-C-STAGE-EXPLICIT **Никогда `git add -A`;** стейджить явные пути. @impl/done
- ##WAL-C-DURABLE-CITATIONS **Брифы цитируют только durable-файлы.** @impl/done
- ##WAL-C-ATTRIBUTION **Правила 1–4 связывают каждый коммит** (человеческая
  атрибуция — без AI-трейлеров; Conventional Commits; атомарность; автономия).
  Воркер — инструмент, никогда не соавтор. @impl/done

## Done (collapsed — see `git log` and the §7 LOG) {#done}

##WAL-DONE **2026-08-06, this run: 11 коммитов работы, 2 строки бэклога и
старейшая находка аудита закрыты, 1 P1 заведён.** Закрыты B-008 (+ чекер
нормы), B-045 (сверка вида, короткие имена, цитаты, вынос `SolveError`),
AUDIT `-01` (e2e пути по умолчанию). Построены: цензус поверхностей хоста
(B-047 п.1), предупреждение в `text-stability.py`, шаг панели про лицензии.
Пересчитаны AUDIT `-10` (27 файлов / 169 вхождений) и B-007 (полные ADR 4 → 12).
Ранее: 2026-08-05 — группа №1 закрыта целиком, 13 строк; волна Г и все четыре
волны карты; фаза D закрыта 2026-08-03. @impl/done

## In progress {#in-progress}

##WAL-INFLIGHT **Ничего не в полёте.** Воркеров нет; `.wt/` пуст; дерево
чистое; все отчёты и `meta.md` в архиве. @impl/done

## Known issues {#known-issues}

- ##WAL-KI-CACHE-BEHIND-THE-TREE **Кэш кампании отстал от дерева на один
  файл** — `PROP-008` (92 вердикта), правленный этой сессией. Пересканировать
  первым делом; `text-stability.py` теперь предупреждает сам. @impl/done
- ##WAL-KI-VERDICT-EVIDENCE-P1 **P1 `2026-08-06-01`:** 4 151 из 11 862
  вердиктов кампании не имеют собственных доказательств; один был ложным.
  Три вопроса владельцу в `AUDIT.md`; агент строгость поднял, выбор не делает. @impl/done
- ##WAL-KI-GITHUB-UNREACHABLE **Зеркало `github` НЕДОСТИЖИМО** — ssh на
  `git@github.com` заворачивается на `127.92.0.49`. **Не расхождение**; форсить
  нельзя. Требует рук владельца. @impl/done
- ##WAL-KI-GITVERSE-BEHIND **`gitverse` синхронен до `586e7c7`** — прогнать
  `cargo xtask mirror`. @impl/done
- ##WAL-KI-BACKLOG-MID-MIGRATION **Бэклог в середине миграции:** 48 строк, из
  которых живых не более 11, и различить их сейчас нечем. Решение об удалении
  строк, закрытых до рулинга, — владельца. @impl/done
- ##WAL-KI-AUDIT-OPEN **`AUDIT.md` несёт открытые находки** — `2026-08-06-01`
  (P1), `-04`, `-06`, `-07`, `-10` (переформулирована), `-11`, `-13`, `-14`,
  райдер `2026-06-12-01`. Durable home находок аудита; в бэклоге не дублируются. @impl/done
- ##WAL-KI-PHASE-E-SIX **Шесть файлов корпуса стоят `work`** — третье условие
  гейта выхода фазы E, ждёт рулинга. @impl/done
- ##WAL-KI-UNIX-HALF-UNCHECKED **Половина `#[cfg(unix)]` ячейки перехвата
  stderr не проверяется компилятором на этой машине.** @impl/done
- ##WAL-KI-VACUITY-AND-SCHEMA-ROOTS **Проверка пустоты сканирования ослабла
  сканером JTD:** она срабатывает на нуле элементов кода, а схемы теперь дают
  элементы. Названо при постройке, не починено. @impl/done

## Session context {#session-context}

##WAL-CTX-BOOT **Холодная сессия начинается с quick-start кампании**, читает
`CONTINUE.md`, закон транспорта
`campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` ЦЕЛИКОМ, живые строки
`BACKLOG.md` и открытые находки `AUDIT.md` — и берёт каждое число из команд
наверху, **после пересканирования**. `CONTINUE.md` — снапшот холодного
возврата; этот файл его суперсидит. @impl/done
