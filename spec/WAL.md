# WAL — Project Continuation State {#root}

_Updated: 2026-08-04, wind-down №6 (**PHASE E — БАТЧ 1 ВОЛНЫ Б ЗАКРЫТ
ЦЕЛИКОМ, БАТЧ 2 ОТЦЕНЗУСОВАН, ПАУЗА ВЛАДЕЛЬЦА. One session landed the
whole parity opener: B-029 + B-034 (the symmetric per-language config
surface — root = shared budget, one uniform section shape, neutral
`gated`, unified exempt `{unit, reason}`, nine loud tombstones; the
gated-or-exempt invariant + vacuous/scope guards + count summaries on
ALL three drivers from one generic core), B-039 TS-shaped (the
`ts-flag-sites` rule on the new `TsEnvRead` fact, `composition_root`,
the demo tier instantiated), B-003 rides (the Go floor stops gating
fixtures), the doc carriers speak v2, nine re-sealed, the B-035 loop's
first pass cut. Four owner rulings taken verbatim (map forks №2 and №9,
B-049 «строить», the pause). Batch 2's census pair landed ready. The
owner PAUSED further launches; the continuation prompt is the word.**)_

##WAL-NUMBERS-COME-FROM-COMMANDS **Every number below is reproduced by two
commands; run them rather than quoting this file.** @impl/done

```bash
python campaigns/packages-2026-09/tasks/drift-registry.py
python campaigns/packages-2026-09/tasks/summary.py
```

## Current phase {#current-phase}

##WAL-PHASE **Progress Control (PROP-043) — wave 2, `packages-2026-09`:
Phase E RUNNING under the EXTENDED mandate (2026-08-04, §7 LOG: «Хочу все
остальные волны сделать») — волны Б/В/Г целиком, карта задаёт порядок,
развилки владельца — по одной. Волна Б: батч 1 из 4 ЗАКРЫТ; батч 2
(B-033 + B-030) отцензусован, стройка стоит на ПАУЗЕ владельца
(«ничего не запускай пока я не скажу») — снимается промтом
продолжения.** Live zone `campaigns/packages-2026-09/`. @impl/done

##WAL-STATE **State at wind-down** (2026-08-04; the commands supersede):
registry **88 obligations / 179 drift verdicts — owed 6** (no judging
batch this checkpoint; re-judgements drain with their builds). The
conform config surface is per-language everywhere that runs the v0.8.0
engine; the coverage invariant speaks three nouns; the fractality
package's policy deliberately stays flat on its frozen 0.7.0 slot.
Panel green (tail read, fifth run — the three reds en route were the
panel catching real misses). Mirrors synced at `7ffcdb91`+. Specmap
orphan ratchet honestly at **42** (host side untouched). Five observed
files stand unsealed pending their new anchors' own judging pass:
PROP-035, PROP-029, `spec/design/lane-composition-dedup.md`,
`spec/design/host-as-package.md`, `spec/design/gate-parity-config.md`. @impl/done

## Next — the Б/В/Г mandate {#next}

1. ##WAL-NEXT-BATCH-2 **Волна Б, батч 2: B-033 + B-030** — on the
   owner's word (the pause lifts with the continuation prompt). The
   census pair is LANDED and ready
   (`harvest/e11-r1-seam-errors-census.md`,
   `harvest/e11-r2-assertions-census.md`); next step is the boss design
   sketch: the Go seam-error rule with its own id (structure half
   promoted + the message half needing new go-extract input), the TS
   twin built NOW by the parity principle, B-030's Go assertion scan +
   the Rust/TS survey verdicts, the parity principle LIFT into the
   discipline core (boss contract diff — ruling №9), and B-049 riding
   (`[rust] floor_disable` mirroring the Go/TS mechanics). Then claudez
   slices per the transport law. @spec/done
2. ##WAL-NEXT-BATCH-2-TAIL **After batch 2 lands:** the B-035 loop pass
   №2 and the **F-185 family re-judgement** (B-033 completes the
   family): `vibe progress mirror --campaign` → `merge-verdicts.py` →
   seal, never chained. Then батч 3 (B-036 + B-037 + B-038 — fork №1
   computed-names approaches with B-038) → батч 4 (B-025 → F-146's last
   anchor; B-026 → F-206). Exit: M-PARITY. @spec/done
3. ##WAL-NEXT-WAVE-V **Волна В after Б** (or interleaved where disjoint):
   B-019а + B-016.1 + B-017 (B-024 decided alongside) → B-018.1/.2 →
   B-018.4 + B-016.2 → B-020 + B-021 (B-014 decided there). B-020
   unlocks the four LEDGER-INTENT interims. Exit: M-ASK + M-DRIFT. @spec/done
4. ##WAL-NEXT-WAVE-G **Волна Г parallel-opportunistic:** B-040 (its
   seams census is CUT — `harvest/g1-b040-seams-census.md`; the point
   refactor is a boss design over it), B-005, F-132 schemas, B-010's
   check-verb fix. @spec/done
5. ##WAL-NEXT-OWNER **On the owner, none blocking:** nine remaining map
   forks (№1 arrives with B-038; №3–№8, №11 with волна В); audit's open
   rows; DBT-0023; MT-02/MT-03; the pre-publication boundary call. @spec/done

## Constraints — do not violate {#constraints}

- ##WAL-C-VERDICT-STANDARD **The verdict standard.** PRESCRIBES →
  confirmed when coherent and every referent resolves; DESCRIBES →
  checked against the tree; unexercisable → unverifiable; `world` adds
  source 2, §3.8 bounds it. @impl/done
- ##WAL-C-BUILD-FIRST **BUILD-FIRST (owner, 2026-08-02).** A discipline
  rule is never weakened for being unused; an annotation is legitimate
  only as an interim naming a recorded build. @impl/done
- ##WAL-C-CAMPAIGN-FRAME **The campaign frame.** The map's waves execute
  through the campaign's phases. The 2026-08-04 mandate covers волны
  Б/В/Г whole; **T/F/G остаются вне добра**; publication is a separate
  operation after the refactor ends; versions are NOT bumped until the
  pre-publication boundary (mint + publication = one operation). @impl/done
- ##WAL-C-PAUSE **The owner's pause (2026-08-04) is in force until his
  word:** no new worker launches, no batch-2 design start — the
  continuation prompt he holds IS the word that lifts it. @impl/done
- ##WAL-C-SELF-COORDINATE **The host is a package (B-031, live).** Root
  identity `org.vibevm.core/vibevm`; `spec://vibevm/…` parses and NEVER
  resolves (`LegacyHostAuthority` hint); never reintroduce the old form
  in living text; the one legacy fixture is `concat!` on purpose. @impl/done
- ##WAL-C-FRESHEST **Versions in addresses (B-028, owner-ruled).**
  `@version` optional; absent → freshest INSTALLED (semver-newest slot).
  The flow publishes the FULL grammar; redbook cites, never restates. @impl/done
- ##WAL-C-CONFIG-V2 **The config surface is per-language (B-029+B-034,
  live).** Root `conform.toml` = `max_file_lines` + `[rust]`/`[go]`/
  `[typescript]` of ONE uniform shape (`roots`, `exclude_substrings`,
  neutral `gated`, `[[<lang>.exempt]] {unit, reason}`, `floor_disable`
  where the floor has it, + language extras); units = each language's
  native one (crate / package / cell — Python inherits the norm on
  arrival); the retired flat root keys are LOUD TOMBSTONES with per-key
  move hints — never reintroduce them, never add a compat alias; the
  fractality package's flat policy is DELIBERATE (frozen 0.7.0 slot,
  migrates on its own stack upgrade). @impl/done
- ##WAL-C-LANE-LAW **The compiled lane's law (B-011 + B-006, live):**
  labels origin-qualified; once-each emission; lane-citing addresses
  scanner-rejected AND panel-linted; regeneration = `cargo run -q -p
  vibe-cli --bin vibe -- install --assume-yes`. @impl/done
- ##WAL-C-DELEGATION **The E/T worker transport** (mechanics
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` — read WHOLE;
  switch `SUBAGENT-MODE.toml` re-read before EVERY fan-out, now
  `claudez`): launchers `C:\Users\olegc\opt\bin\{claudez,claudez2}`,
  per-launcher state dirs, `-c` continues its own thread; disjoint
  perimeters → parallel (≤5/launcher, ≤10 total; cargo-heavy 2–3),
  intersection → ONE thread; stream-json logs DIRECTLY into
  `C:\Users\olegc\git\v\cache\agents\sorted\<task-id>\`; packet-mandated
  heartbeats; ~30 s polls (log growth primary; completion by
  `"type":"result"`, never the marker text); WORKER-REPORT mandatory,
  its EXISTENCE part of the set-compare; boss scaffolds carry named
  refinement points; ПРИНЯТО · НЕ ПРИНЯТО→`-c` (rework section text
  DICTATED verbatim) · re-commission (ceiling 2) · discard; every cycle
  in meta.md; acceptance by artifacts; workers get no git verbs; briefs
  cite durable files only. **§8 now carries SIX paid facts** — report
  existence · clippy -D warnings · bare panel + read tail · wc -l ≤ 600
  · new engine files carry `specmark::scope!` · an engine enum change is
  a cross-package ripple (grep the WHOLE tree for consumers; budget
  exhaustive-match arms in every frontend; `cargo clean -p` kills the
  stale-fingerprint ghost). **The boss states worker log paths in chat
  at every fan-out (owner ask, 2026-08-04).** @impl/done
- ##WAL-C-CENSUS-NOT-PROOF **A census/reader-table is evidence, never a
  completeness proof.** The merge plan for any changed shared surface
  greps the whole tree for consumers (vendor copies excluded); the
  panel's package-workspace sweep is the real perimeter check — it
  caught three real misses this landing. @impl/done
- ##WAL-C-REAL-EXITS **Exit codes are read REAL, never through a
  pipe/grep.** Bare `bash tools/self-check.sh` in background, exit =
  the task's; **the mirror fan-out waits for the READ TAIL, never a
  notification.** The panel runs on an UNTOUCHED tree — applying diffs
  mid-panel invalidates the run. @impl/done
- ##WAL-C-PACKAGE-FMT **The fmt/vendor reach after package-crate
  edits:** host `cargo fmt --all` does NOT cover the eight package
  workspaces — fmt per package manifest; `cargo xtask sync-engines`
  from the HOST root the same pass (it also syncs the lang↔mcp CLI
  twins — 9 sync sets); rematerialise (`vibe install`) after canonical
  package edits. @impl/done
- ##WAL-C-NO-MEASUREMENTS-ANSWER **«Замеров нет и нескоро будет»** —
  the standing answer; never re-raised. @impl/done
- ##WAL-C-DEFERRED-IS-OWNER-RULED **`deferred` in the registry = an
  owner-ruled row.** The gate reads owed + rulings. @impl/done
- ##WAL-C-REAL-MIRROR **The real mirror is `vibe progress mirror
  --campaign <zone>`**; any anchor-set change requires it before
  `merge-verdicts.py`; never chain merge and seal. Seal REFUSES files
  carrying unjudged markers — honest state; `vibe progress seal` takes
  explicit PATHs. @impl/done
- ##WAL-C-VERDICT-FIRST **A false `confirmed` is repaired verdict-first.** @impl/done
- ##WAL-C-STRIKE-PER-ANCHOR **A strike-by-ruling checks each anchor's own
  recorded reason.** @impl/done
- ##WAL-C-QUEUE-FROM-REGISTRY **The owner's queue derives from the
  registry, never a harvest snapshot.** @impl/done
- ##WAL-C-PERIMETER **The perimeter law.** SPEC in `core-ai-native`,
  ENGINE in its five crates (vendored ×6), DRIVER per stack CLI,
  DEPLOYMENT in the consumer; `legacy-spec/**` excluded; a `not-found`
  is a fact about the perimeter until checked. @impl/done
- ##WAL-C-READ-FURTHER **Read the document further before searching
  wider.** @impl/done
- ##WAL-C-OWN-CORPUS **The campaign is inside its own corpus:** exclude
  `campaigns/*/run/**` from evidence; historical evidence JSON keeps
  pre-migration spellings BY DESIGN — never "fix" them. @impl/done
- ##WAL-C-CACHE-MERGE-ONLY **`run/cache.json` is load-and-merge only;
  never hand-write `verified_at`/`processed_hash`;** WinError 5 → retry.
  Print via `PYTHONIOENCODING=utf-8`. @impl/done
- ##WAL-C-PROGRESS-WRITES **Every parsing `vibe progress` subcommand
  writes zone state; always `--campaign`; never point at
  `campaigns/progress-2026-08`.** @impl/done
- ##WAL-C-SELF-CHECK-EXCLUSION **No real `vibe` command while
  `tools/self-check.sh` runs.** @impl/done
- ##WAL-C-STAGE-EXPLICIT **Never `git add -A` while a worker is out;**
  stage explicit paths. **`git add -u` stages tracked-only — NEW files
  (a worker's created file, fresh vibedeps modules) need explicit adds
  or they silently miss the commit; `git diff` omits untracked files —
  a worker's NEW file is hand-carried at merge (check `status --short`
  for `??`).** @impl/done
- ##WAL-C-DURABLE-CITATIONS **Briefs cite durable files only; a wind-down
  invalidates evidence citing `CONTINUE.md`/`spec/WAL.md`.** @impl/done
- ##WAL-C-PRESENTATION-FORMAT **Presentation format (binding).** Суть
  по-человечески БЕЗ чтения спек → дерево для развилок → точные имена
  приложением; жаргон приложением; спеки не цитировать. Развилки карты
  — по одной; AskUserQuestion-форма с рекомендацией работает. @impl/done
- ##WAL-C-SHELL-TRAPS **Shell traps that already fired:** Bash-tool cwd
  PERSISTS — absolute paths; python `open('/tmp/…')` on Windows writes
  to the drive root — scratchpad; a worker watcher greps the PACKET echo
  — watch `"type":"result"`; `grep -v '\.vibe'` deletes our packages;
  PowerShell `-match` case-insensitive; **CRLF vs `str.replace` PAID
  AGAIN 2026-08-04 — byte-replace scripts silently miss on CRLF files,
  editor tools only**; Git Bash heredocs eat `\\`; `git commit -q`
  глотает вывод; Windows holds worktree handles — and a worktree with
  `node_modules` blows MAX_PATH on removal: `rm -rf` → `cmd //c "rd /s
  /q …"` → `git worktree prune`; a structural Edit can EAT an adjacent
  section heading — after BACKLOG surgery verify `grep -c "^### B-0"`;
  a stale cargo fingerprint compiles fixed code against a dead rmeta —
  `cargo clean -p <crate>` before deeper diagnosis. @impl/done
- ##WAL-C-BOOT-PAIR **Boot pair marking:** `00-core.md`/`90-user.md`
  additively; `refs/book/` NOTOUCH. @impl/done
- ##WAL-C-MISC **Small standing facts:** the deviates escape hatch
  speaks `spec://` URIs only; a helper without `#[test]` in a split-out
  tests file needs the deviates hatch; MT-02/MT-03 await manual
  sign-off; `cargo outdated` unrunnable here; W3-of-B011's named
  follow-ups stand (directive-errors wiring, `#[verifies]` exhibits);
  the nested boot-bearing-umbrella fixpoint is parked in
  `desubstitute_covered_units`'s doc comment; the engine rule-struct
  field names (`gated_crates:` etc.) and health's JSON report keys keep
  the old spelling as internal API/schema — cosmetic follow-ups, not
  config reads. @impl/done

## Done (collapsed — see `git log` and the §7 LOG) {#done}

##WAL-DONE **2026-08-04, this session (wind-down №6):** волна Б батч 1
целиком — censuses E8×3 → fork №2 presented on measured fact → RULED
(native units; full symmetry «хорошо и надолго», Python coming) → B-003
built & measured live while the fork stood → the staged W1→W2a∥W2b
chain landed green (engine Config v2 + three frontends + host/rust-demo
migration; the panel caught the TCG census gap, the flat test tail and
the Fact-ripple — all fixed, lessons → §8 now SIX paid facts) → W3 doc
sweep + W4 ts-flag-sites landed → nine carriers re-sealed → B-035 loop
pass №1 (infrastructure parity achieved; content debt = batch 2; two
fresh finds → B-049 ruled «строить», Go residual measured latent on
B-048) → fork №9 RULED («Ядро дисциплины») → batch-2 census pair landed
→ owner PAUSED. 13 claudez cycles, ALL ПРИНЯТО first pass, 0 reworks.
Волна Г rode: G1 seams census (B-040). Earlier sessions: волна А whole
(B-011, B-006, B-031, B-028), Phase D closed 2026-08-03. @impl/done

## In progress {#in-progress}

##WAL-INFLIGHT **Nothing is in flight.** No workers out, `.wt/` empty,
tree clean, mirrors synced. **The owner's pause holds** — the next
session opens батч 2 design ON THE CONTINUATION PROMPT (which is the
word that lifts the pause); no re-asking beyond that. @impl/done

## Known issues {#known-issues}

- ##WAL-KI-OPEN **Open on the owner, none blocking:** nine remaining map
  forks; F-129; F-122; F-126; F-127; F-128; F-120; the H-roster; F-069;
  F-125; the 2026-06-12-01 rider. @impl/done
- ##WAL-KI-RATCHET **Specmap orphan ratchet at 42** (host side untouched
  this session; the engine's new files carry scope! and self-trace
  reads 0 gated orphans). @impl/done
- ##WAL-KI-AUDIT **Audit's active subset (`AUDIT.md` §2026-08-03):**
  cargo-outdated (-03), dead_code shadow (-04); DBT-0023 filed. @impl/done
- ##WAL-KI-UNSEALED **Five observed files await their anchors' judging
  pass** (honest seal refusals): PROP-035, PROP-029, the three design
  docs (lane-composition-dedup, host-as-package, gate-parity-config). @impl/done
- ##WAL-KI-STALE-DOC **`package-tree.schema.v1.json`'s description
  string still says «e.g. "vibevm"»** — doc-pass candidate. @impl/done

## Session context {#session-context}

##WAL-CTX-BOOT **A cold session starts at the campaign quick-start**,
reads `CONTINUE.md` (the batch-2 recipe), **the transport law
`campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` WHOLE (§8 now SIX
paid facts)**, the two E11 censuses in `harvest/`, the BACKLOG rows of
batch 2 (B-033, B-030, B-049, B-035's norm/loop lines), plan §5E + §7
LOG tail — and takes every number from the two commands at the top.
`CONTINUE.md` is the cold-resume snapshot; this file supersedes it. @impl/done
