# WAL — Project Continuation State {#root}

_Updated: 2026-08-16 (**Ф4.2b-0 ЗАКРЫТ**: `xtask/src/codegen/mod.rs` расколот по
шву ответственности, место для слоя преобразований освобождено. Главное этой
сессии — **замер перед первой правкой слоя: семь опровержений, шесть меняют
конструкцию**, список преобразований вырос с восьми до десяти. Три коммита,
панель зелёная на посадке. Два делегированных пакета, оба приняты без циклов
доработки. Сворачивание №25: `CONTINUE.md` и `NEXT-SESSION-PROMPT.md`
переписаны на вход **Ф4.2b-1**.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета целиком → короткий доклад → **сразу исполнение**.
Промт есть слово владельца, отдельного «да» ждать не нужно. @status:impl/done

@fact:WAL-THE-LAW **PROP-044 — ратифицированный стоячий закон каждого формата**
(2026-08-13). Терминология §2b обязательна: snapshot ↔ frozen — антонимы
**одной булевой оси без третьего состояния**; «канал» — авторский указатель
версий (PROP-005 §2.18); capture/named ref — провайдерское; режим
материализации — `copy`. @status:impl/done

@fact:WAL-CONTRACT-LISTS-MORE-THAN-THE-PLAN **§4.2 контракта перечисляет
БОЛЬШЕ, чем список преобразований плана, и разница уже стоила открытия.**
`##M-SCHEMA-DESCRIBES-FORM` называет среди эмитируемого нашим слоем
`canonical ordering`; список плана его не выписал, и это дало девятое
преобразование (`##WAL-F42-NINE-AND-TEN`). Всякий раз, когда список кажется
полным, он сверяется с §4.2, а не сам с собой. @status:impl/done

@fact:WAL-THE-PLANS **Три плана строек, взаимно сцеплены:**
[`TZ-CHANGE-NATIVE-FORMATS-v0.1.md`](../campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md)
(главная стройка Ф0–Ф6; **Ф0–Ф3 закрыты, Ф4.0/Ф4.1/Ф4.2a/Ф4.2b-0 закрыты,
следующий шаг — Ф4.2b-1, §7 плана, блок «Ф4.2b»**),
[`TZ-IDENTITY-REGISTRY-BUILDS-v0.1.md`](../campaigns/packages-2026-09/TZ-IDENTITY-REGISTRY-BUILDS-v0.1.md)
(**S3 ЗАКРЫТ**; S1 и S6 измерены и несут записанное боссово решение — режутся
холодными; S2 — СТОП-ВЛАДЕЛЕЦ; S7 — судейская),
[`TZ-CHANGE-NATIVE-WAVE2-v0.1.md`](../campaigns/packages-2026-09/TZ-CHANGE-NATIVE-WAVE2-v0.1.md)
(W1–W4 заперты до посадки Ф1–Ф5). @status:impl/done

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста
cargo xtask mirror --check
```

## Current phase {#current-phase}

@fact:WAL-PHASE **Change-native стройка, ТЗ №1: Ф0–Ф3 закрыты; в фазе Ф4
закрыты Ф4.0 (генератор видит подкаталоги), Ф4.1 (шесть схем каталога), Ф4.2a
(боксирование) и Ф4.2b-0 (раскол драйвера). Следующий шаг — Ф4.2b-1: открытие
словаря по `x-vocabulary`.** Порядок внутри фазы жёсткий, каждый шаг —
отдельным коммитом; граница ФАЗЫ — полная панель + правка карты + раскатка. @status:impl/done

@fact:WAL-F42B0-IS-DONE **Раскол выбран по ОТВЕТСТВЕННОСТИ, и это доказало
дерево, а не вкус.** В `mod.rs` жили раскладка (дома схем, деревья выхода,
законность имени модуля, дерево `mod.rs`), драйвер и поиск бинаря; **все пять
тестов файла упражняют только раскладку**, поэтому она уехала целиком и с
тестами. 586 → `mod.rs` 282 / `layout.rs` 205 / `layout/tests.rs` 140.
Коммит `440c45fc`. @status:impl/done

@fact:WAL-F42-NINE-AND-TEN **Преобразований ДЕСЯТЬ, а не восемь, и два из
записанных сайтов не имеют.** Добавлены: **№9 `HashMap` → `BTreeMap`**
(безусловное; 7 сайтов, включая `Repomd.files`; порядок обхода рандомизирован
пер-процесс ⇒ реэкспорт сделал бы `rebuild --check` мерцающим) и **№10 снятие
`Box` у семи вложенных подсекций** (ответ ратифицирован Приложением Б.2:
пустое нормализует ПРОЕКТОР, `Option` остаётся). Без сайтов: №6 (время —
сворачивается в №8) и №4 (`deny_unknown_fields`: 69 осматриваемых структур,
ноль получателей при сегодняшнем реестре). @status:impl/done

@fact:WAL-ORACLES-HAVE-A-MEASURED-BLIND-SPOT **Оракулы провода — страховочная
сеть Ф4.2, и у неё измерена слепая зона.** Они сравнивают `serde_json::Value`,
чьё равенство **порядок ключей не учитывает** (сказано в их собственной
шапке), а фикстуры полны по построению (`FULLY_POPULATED_KEY_COUNT = 33`).
Значит они не поймают ни смену порядка ключей (отсюда №9), ни состояние
«присутствующий пустой объект» (отсюда №10). Сеть переживёт все
преобразования — но она не всё, и остальное ловит двойная сборка (G5). @status:impl/done

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист,
панель `self-check: all green` с реальным кодом выхода **0** на посадке
`440c45fc`; корпус 281 файл, 13788 маркеров, 0 неразмеченных; долг **0
неосуждённых, 0 осиротевших**, 34 stale; `vibe check` — 0 errors, 1 warning,
44 info; карта — 6061 спек-юнит / **1038** tagged / 976 рёбер / 0 подозрений /
0 сирот. @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты — не
чинить.** *(i)* `vibe check` даёт **44 info** `manifest_epoch` — до-эпоховые
манифесты под `packages/` (Ф1.2, решение D5). *(ii)* Одно **warning**
`local_source_freshness` по `org.vibevm.world/addressable-specs`. **За
двадцать семь посадок подряд оба не сдвинулись ни разу. Рост любого из них —
находка, а не шум.** @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-F42B1 **Ф4.2b-1 — открытие словаря по `x-vocabulary`**: 14
   копий enum'ов (10 открыть `Unknown(String)`, 3 оставить закрытыми, 1
   аннотировать — `init_report.outcome_action`). Самое дорогое из десяти;
   форма доказана спайком Ф0.2 и лежит ЦЕЛИКОМ в
   [`harvest/f0-gen-poc.md`](../campaigns/packages-2026-09/harvest/f0-gen-poc.md)
   §3–§4, потому что каталог того спайка был одноразовым. @status:spec/plan
2. @fact:WAL-NEXT-SEAM-IS-OPEN **Новых входов заводить не нужно — шов открыт.**
   `generate_into` уже держит `resolved` (путь к схеме ПОСЛЕ подстановки
   словника — именно её обязан читать слой, аннотации живут на подставленных
   определениях); реестр уже разбирается в структуру (`format_id.rs`);
   `serde_json` уже в зависимостях xtask. @status:impl/done
3. @fact:WAL-NEXT-ANNOTATIONS-RIDE-WITH-THE-PASS **Каждый шаг приносит СВОИ
   аннотации тем же коммитом.** Семь плоских CLI-схем не несут сегодня ни
   одной; долг хостового дома — 30 сайтов (1 `x-vocabulary`, 10 `x-empty`,
   19 `x-default`), поимённо в
   [`harvest/f4-transform-radius.md`](../campaigns/packages-2026-09/harvest/f4-transform-radius.md)
   §3. Код читателя, легший раньше данных, краснеет на первом прогоне. @status:spec/plan
4. @fact:WAL-NEXT-INDEPENDENT **Независимая полоса режется холодной:** S1 и S6
   измерены, боссовы решения по каждому записаны в ТЗ. S7 — судейская, в любой
   момент, и она же закрывает долг печати. @status:spec/plan
5. @fact:WAL-NEXT-OWNER **За владельцем три вопроса, ни один не блокирует:**
   S2 (org-права), **Ж8** (`--full` и исчезнувшая запись) и **B-056** (язык
   схем не выражает `u64`). @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-PROP044-IS-THE-LAW **Вся идеология форматов — ратифицированный
  PROP-044**; здесь не пересказывается. Терминология §2b обязательна. @status:impl/done
- @fact:WAL-C-SCHEMA-DESCRIBES-THE-WIRE **Схема описывает ПРОВОД, и это
  разделяющий критерий.** Аннотации политики (`x-vocabulary`, `x-empty`,
  `x-default`, `x-rust-type`, `x-vocabularies`) говорят что-то о ФОРМАТЕ и
  потому законны. Боксирование варианта и канонический порядок ключей —
  вопросы, у которых ровно один законный ответ, и потому они НЕ аннотации, а
  безусловные преобразования генератора. Всякое будущее «а не вписать ли это в
  схему» проверяется этим вопросом. @status:impl/done
- @fact:WAL-C-LAYER-IS-SCOPED-BY-HOME **Слой преобразований ключится ДОМОМ
  схемы** (решение Р10): хостовый `schemas/` получает политику целиком, дом
  движка не получает её вовсе. Довод не бюджетный: политика нашего провода не
  вправе связывать публичный Rust-API чужого пакета с собственным поездом
  релизов — ровно это и запрещает §2 ТЗ. @status:impl/done
- @fact:WAL-C-ANNOTATIONS-ARE-INERT **Аннотации политики написаны, но сегодня
  их НИКТО не читает** — потребитель строится шагами Ф4.2b. Не принимать их за
  работающий механизм и не переизобретать: это данные, ждущие читателя. @status:impl/done
- @fact:WAL-C-VOCABULARY-ONE-HOME **Словарь живёт в ОДНОМ доме
  (`formats/vocabularies.json`, 18 фрагментов), подстановку делает наш слой, и
  она ТРАНЗИТИВНА**: фрагмент объявляет свои зависимости, резолвер замыкает,
  отказывает на цикле и называет цепочку. Средствами JTD это неисправимо —
  `ref` разрешается только внутри документа, а висячая ссылка роняет
  jtd-codegen ПАНИКОЙ без диагностики. @status:impl/done
- @fact:WAL-C-ROUTE-BY-PATH **Маршрут выхода ключится ДОМОМ схемы и её путём,
  а не стемом.** Каталог схемы несёт эпоху (PROP-044 §4.6), поэтому
  `index/e1/entry` и будущий `index/e2/entry` обязаны остаться разными
  модулями. @status:impl/done
- @fact:WAL-C-CODEGEN-STAGING **`check-codegen` — это `git diff --exit-code`,
  то есть рабочее дерево против ИНДЕКСА, и untracked он НЕ ВИДИТ.** Слайс,
  трогающий `generated/**`, садится в порядке **стейдж → панель → коммит**. @status:impl/done
- @fact:WAL-C-JOURNAL-IS-THE-TRUTH **Каталог — проекция журнала, и починка
  идёт в одну сторону.** Если `rebuild --check` разошёлся, неправ КАТАЛОГ. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки:** применить дифф →
  `cargo fmt --all` → **`cargo xtask specmap`** → панель → суд → коммит. Карта,
  пересобранная ДО того как лёг код, — карта дерева, которого уже нет. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель ОБРЫВАЕТСЯ на первом красном
  шаге** — «упала на X» ничего не говорит о шагах после X; зелёный хвост —
  единственное доказательство, что прогнались все. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ** — красное доказательство, растянутое на два таргета,
  показывает одно падение без `--no-fail-fast`. @status:impl/done
- @fact:WAL-C-FMT-CHECK-IS-THE-GATE **`cargo fmt` без `--check` выходит с нулём
  и когда результат НЕ чист.** Проверять той формой, которую гоняет панель;
  файл, вырезанный текстом, начинать шапкой `//!`. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён
  тем же заходом**: scan → **mirror** → batch JSON → `merge-verdicts.py` →
  долг 0. **`campaigns/**`, `BACKLOG.md` и `spec/WAL.md` в судимый корпус НЕ
  входят.** @status:impl/done
- @fact:WAL-C-EVIDENCE-CITES-SYMBOLS **Evidence цитирует ИМЯ СИМВОЛА, а не
  номер строки**, и вердикт, заменяющий прежний, объясняет замену. @status:impl/done
- @fact:WAL-C-HONEST-SEAL **Печать — только за проверенное**; сейчас она
  сознательно не ставится (см. `##WAL-KI-SEAL-DEBT`). @status:impl/done
- @fact:WAL-C-SPECMAP **Правка текста спеки и новый код двигают карту**:
  `cargo xtask specmap` в той же посадке. **Специмап индексирует JTD-схемы под
  `schemas/` РЕКУРСИВНО**. В `xtask/**` нет ни одного `specmark::scope!` —
  новые файлы там его НЕ несут (измерено). @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ; режим
  — `SUBAGENT-MODE.toml` (`claudez`); пакеты режет босс; воркеры не запускают
  git; диффы читает, правит и коммитит босс; ревью не делегируется никогда.
  §8 несёт **46 размеченных фактов**, §9 — разбор чистого фан-аута. @status:impl/done
- @fact:WAL-C-PACKET-PERIMETER **Закрытый периметр записи обязан называть файл,
  который предписанный им же раскол создаст** — иначе два пункта пакета
  сталкиваются, и слабый писатель разрешает столкновение ужатием. @status:impl/done
- @fact:WAL-C-PACKET-GIVES-RULE-AND-COORDINATE **Пакет даёт ПРАВИЛО, а
  координату — как его иллюстрацию.** Проверено ценой боссовой ошибки: проза
  сказала «все четыре теста», список перечислил пять имён, воркер поверил
  ДЕРЕВУ, перенёс пять и назвал расхождение. Правило спасло шаг; одна
  координата без правила дала бы защитимую неверную правку. @status:impl/done
- @fact:WAL-C-PACKET-NAMES-THE-BOOT-IT-NEEDS **Worktree несёт `CLAUDE.md`, и
  воркер грузится по нему раньше, чем прочтёт пакет** — оба воркера открылись
  чтением статической полосы. Пакет, компилирующий контекст внутрь, обязан
  сказать, какое чтение ему нужно, а какое он только что сделал избыточным.
  Сам `CLAUDE.md` воркеру не отменяют: он несёт правила, связывающие коммит. @status:impl/done
- @fact:WAL-C-SELF-VERIFY-FOLLOWS-THE-TOUCH **Self-verify пишется от того, что
  правка ТРОГАЕТ, а не от того, что добавляет.** Правка общих ДАННЫХ обязана
  назвать крейты, чьи тесты на этих данных стоят. @status:impl/done
- @fact:WAL-C-REPORT-BEFORE-GATES-BUT-PENDING **Отчёт пишется РАНЬШЕ финальных
  гейтов, НО каждая строка приёмки начинается словом PENDING.** @status:impl/done
- @fact:WAL-C-RED-PROOF **Красное доказательство — пункт приёмки, и босс
  перемеряет его СВОЕЙ целью. Чистый рефакторинг не исключение:** сторож,
  которого не показали падающим в НОВОМ доме, неотличим от удалённого. @status:impl/done
- @fact:WAL-C-TEST-RE-AIMED-NOT-SATISFIED **Тест, покрасневший на ЗАКОННОМ
  изменении, перенацеливают на его инвариант — и обязаны доказать, что
  суженный сторож всё ещё падает на настоящем нарушении.** @status:impl/done
- @fact:WAL-C-KILLED-TASK-LEAVES-A-LIVE-WORKER **Убитая harness'ом задача не
  убивает воркера.** Фоновый сторож до стабилизации лога, **никогда не
  прибираться вокруг живого процесса**, читать worktree, добирать хвост.
  Терминальный сигнал — событие `"type":"result"` в stream-json, а не grep по
  `TASK-DONE`. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: никогда голый `cd` — только
  `( cd … && … )` / `git -C` / абсолютный путь; `cmd | tail; echo $?` печатает
  код ПАЙПА; **правки только editor-инструментами**; python — с
  `PYTHONIOENCODING=utf-8`; бюджет файла 600 строк ПОСЛЕ `cargo fmt`, **и он
  покрывает `tests/**`**. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты heredoc
  `-F -`; раскатка только `cargo xtask mirror`; Rules 1–4 связывают каждый
  коммит. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки
  дисциплины не трогаются — **и именно этот запрет породил Р10**. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-16, 3 коммита (`440c45fc`..`d592f622`):** боссов замер
слоя (чтение `generated/index/e1/entry/mod.rs` и `journal/…` целиком + словник
+ реестр) → раскол `codegen/mod.rs` по шву ответственности (`440c45fc`) →
read-only пакет радиуса, 677 строк находки (`6d399715`) → семь опровержений и
решения Р10–Р15 в §7 плана (`d592f622`) → новый факт в §8 запускалок. Панель
зелёная на кодовой посадке, оба воркера приняты первым проходом. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, `.wt/` пуст, ветки `wt/*`
удалены, дерево чисто. Два worktree под `~/.fractality/runs/**` — чужие, к этой
кампании отношения не имеют и не трогаются. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-HELLO-SCHEMA-MISSING **Реестр называет несуществующую схему.**
  `formats/REGISTRY.toml:195` указывает на `schemas/hello/e1/hello.jtd.json`,
  которой на диске нет (13 файлов схем), при живом `FormatId::Handshake`.
  Строит её Ф6.1; до тех пор преобразование №4 обязано пропускать такую запись
  ПО ИМЕНИ с явной строкой в выводе, а не падать и не считать «нет схемы ⇒
  политика не нужна». @status:spec/plan
- @fact:WAL-KI-VARIANT-NAME-NOT-COVERED **Имя ВАРИАНТА генератор выбирает сам,
  и `x-rust-type` его не покрывает.** `KindName0` — коллизионный суффикс для
  wire-строки `"kind/name"` (`repomd/mod.rs:64`); сведение с рукописным
  `NamingConvention` требует переименования варианта. Дефер с триггером
  «Ф4.2c». @status:spec/plan
- @fact:WAL-KI-B056 **B-056 — язык схем не выражает тип, которым пишет наш
  писатель (P2, open).** У JTD нет 64-битного целого вовсе, а `Repomd.File.size`
  объявлен `u64`. Три выхода измерены; каждый трогает либо провод, либо
  продуктовый тип ⇒ решение владельца. **Класс, а не случай.** @status:spec/plan
- @fact:WAL-KI-TUPLE-GAP **Второй пол JTD: кортежа нет.** `Event::Renamed`
  несёт `(Group, String)`; схема описывает `elements` строк, теряя арность.
  Оракул пинит длину пары. Тот же класс, что B-056. @status:impl/done
- @fact:WAL-KI-VOCAB-OUTSIDE-THE-MAP **Единственный дом словаря вне периметра
  карты.** Специмап индексирует JTD-схемы под `schemas/`;
  `formats/vocabularies.json` туда не попадает. Переименованием не лечится;
  починка требует тронуть движок дисциплины ⇒ **дефер по имени**. @status:spec/plan
- @fact:WAL-KI-GENERATOR-PIN-UNRECONCILED **Пин генератора не сведён с
  артефактами.** Заявлено `jtd-codegen 0.4.1`, все артефакты печатают в шапке
  `v0.2.1`, и ни один гейт версию не проверяет. Постпроцессор привязан к форме
  эмиссии и сам это говорит (`postproc.rs:32-36`) — свести ДО следующих
  преобразований. @status:spec/plan
- @fact:WAL-KI-J8-OPEN **Ж8 — открытый вопрос владельца.** Что означает
  `--full` для записи, которую скан больше не видит. Сегодняшнее поведение
  стоит. @status:spec/plan
- @fact:WAL-KI-SEAL-DEBT **Печать суда сознательно не поставлена.**
  `spec/modules/vibe-index/PROP-005-package-index.md` несёт двинувшиеся факты
  нескольких заходов; прецедент отката есть (`498e8c8b`). Закрывается S7. @status:impl/done
- @fact:WAL-KI-F43-PERIMETER **Периметр запрета Ф4.3 — 133 файла** (объединение
  `Serialize`∪`Deserialize` по `crates/**`+`xtask/**` вне `generated/**`).
  Гейт садится рехетом (К4); целевое правило К2 — дефер с триггером «посадка
  Ф4.2». @status:spec/plan
- @fact:WAL-KI-FIVE-SICK-DICTIONARIES **Раздвоенных словарей пять, а не один**
  (`BindingSite`, `PackageKind`, `NamingConvention`, `DeliveryMode`,
  `SourceKind`), и **у `PackageKind` паритет-теста НЕТ** вопреки записанному.
  Четыре сводит Ф4.2c реэкспортом; `SourceKind` — именованный дефер. @status:spec/plan
- @fact:WAL-KI-UNTESTED-FRAGMENT-REFUSAL **Названный пробел покрытия:** отказ
  на сломанной аннотации ВНУТРИ фрагмента реализован, но тестом не закрыт. @status:spec/plan
- @fact:WAL-KI-STALE **34 файла stale** (стоячий долг сдвигов байтов) —
  адресовано кампанией S7. @status:impl/done
- @fact:WAL-KI-B074 **Второй якорь факта в абзаце глотается молча** (B-074) —
  механизм чека не построен. @status:impl/done
- @fact:WAL-KI-B075 **Панель может мигнуть на чистом дереве** (B-075) — читать
  отказ, не перезапускать вслепую. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие
  выходного гейта Phase E, ждёт рулинга. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → документы предмета (PROP-044 + три ТЗ) →
`SUBAGENT-LAUNCHERS.md` целиком перед фан-аутом** — и берёт каждое число из
команд сверху, после rescan. Этот файл главнее `CONTINUE.md`; ратифицированный
PROP-044 главнее обоих. @status:impl/done
