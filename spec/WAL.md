# WAL — Project Continuation State {#root}

_Updated: 2026-08-16 (**Ф4.0, Ф4.1 и Ф4.2a ЗАКРЫТЫ**: генератор ходит по
подкаталогам, словарь имеет один дом с транзитивной подстановкой, **все шесть
схем каталога описаны, у каждой оракул провода**, и механизм впервые правит
содержимое своего выхода. 25 коммитов, панель зелёная на каждой посадке.
Тринадцать делегированных пакетов, все приняты без циклов доработки; два были
срезаны harness'ом и оба приняты после боссова добора хвоста. Сворачивание
№24: `CONTINUE.md` и `NEXT-SESSION-PROMPT.md` переписаны на вход **остатка
Ф4.2**.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета целиком → короткий доклад → **сразу исполнение**.
Промт есть слово владельца, отдельного «да» ждать не нужно. @status:impl/done

@fact:WAL-THE-LAW **PROP-044 — ратифицированный стоячий закон каждого формата**
(2026-08-13). Терминология §2b обязательна: snapshot ↔ frozen — антонимы
**одной булевой оси без третьего состояния**; «канал» — авторский указатель
версий (PROP-005 §2.18); capture/named ref — провайдерское; режим
материализации — `copy`. @status:impl/done

@fact:WAL-THE-PLANS **Три плана строек, взаимно сцеплены:**
[`TZ-CHANGE-NATIVE-FORMATS-v0.1.md`](../campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md)
(главная стройка Ф0–Ф6; **Ф0–Ф3 закрыты, Ф4.0/Ф4.1/Ф4.2a закрыты, следующий
шаг — остаток Ф4.2, §7 плана**),
[`TZ-IDENTITY-REGISTRY-BUILDS-v0.1.md`](../campaigns/packages-2026-09/TZ-IDENTITY-REGISTRY-BUILDS-v0.1.md)
(**S3 ЗАКРЫТ**; S1 и S6 измерены и несут записанное боссово решение — режутся
холодными; S2 — СТОП-ВЛАДЕЛЕЦ; S7 — судейская),
[`TZ-CHANGE-NATIVE-WAVE2-v0.1.md`](../campaigns/packages-2026-09/TZ-CHANGE-NATIVE-WAVE2-v0.1.md)
(W1–W4 заперты до посадки Ф1–Ф5). @status:impl/done

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста
cargo xtask mirror --check
```

## Current phase {#current-phase}

@fact:WAL-PHASE **Change-native стройка, ТЗ №1: Ф0–Ф3 закрыты; в фазе Ф4
закрыты Ф4.0 (генератор видит подкаталоги), Ф4.1 (шесть схем каталога) и
Ф4.2a (боксирование). Следующий шаг — остаток Ф4.2: семь преобразований из
восьми.** Порядок внутри фазы жёсткий, каждый шаг — отдельным коммитом;
граница ФАЗЫ — полная панель + правка карты + раскатка. @status:impl/done

@fact:WAL-F41-IS-DONE **Шесть схем из шести, и у каждой ОРАКУЛ ПРОВОДА.**
`schemas/index/e1/{entry,by_name,by_cap,by_purl,repomd}.jtd.json` и
`schemas/journal/e1/journal.jtd.json`; оракулы —
`crates/vibe-index/tests/wire_parity_*.rs`. Оракул строит полностью
заполненный рукописный тип, гоняет его через JSON в сгенерированный и
обратно и сравнивает ЗНАЧЕНИЯ. Он ловит четыре класса, невидимых в диффе:
пропущенное поле (терпимый читатель роняет молча), пропущенное плечо
объединения (отказ на `from_value`), потерю на ТРЕТЬЕМ уровне вложенности
через общий фрагмент, и сдвиг значений словаря. @status:impl/done

@fact:WAL-ORACLES-SURVIVE-F42 **Оракулы — страховочная сеть остатка Ф4.2, и
это свойство их конструкции.** Преобразования генератора меняют форму типов в
Rust (`String`→`Group`, `Option<Box<bool>>`→`bool`, camelCase→snake_case,
`FixedOffset`→`Utc`) и обязаны НЕ трогать байты. Оракул сравнивает провод, а
не типы, поэтому переживает все преобразования и краснеет ровно тогда, когда
преобразование сдвинуло формат. @status:impl/done

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист,
HEAD `3f564059`, origin ahead/behind 0/0; зеркала синхронны; панель
`self-check: all green`, реальный код выхода **0**, 51 шаг; корпус 281 файл,
13788 маркеров, 0 неразмеченных; долг **0 неосуждённых, 0 осиротевших**, 34
stale; `vibe check` — 0 errors, 1 warning, 44 info; карта — 6061 спек-юнит /
**1038** tagged / 976 рёбер / 0 подозрений / 0 сирот. @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты — не
чинить.** *(i)* `vibe check` даёт **44 info** `manifest_epoch` — до-эпоховые
манифесты под `packages/` (Ф1.2, решение D5). *(ii)* Одно **warning**
`local_source_freshness` по `org.vibevm.world/addressable-specs`. **За
двадцать шесть посадок подряд оба не сдвинулись ни разу. Рост любого из них —
находка, а не шум.** @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-F42 **Остаток Ф4.2 — семь преобразований из восьми.**
   Список полный и решённый (план Р5 + находка
   [`harvest/f4-jtd-tagged-union-probe.md`](../campaigns/packages-2026-09/harvest/f4-jtd-tagged-union-probe.md)
   §5): открытие словаря **по аннотации, а не по факту закрытости**;
   snake_case; `x-empty`; `deny_unknown_fields` по реестру;
   `Option<Box<скаляр>>` → скаляр с умолчанием; `DateTime<Utc>`; доменные типы
   по `x-rust-type`. Восьмое — боксирование — построено (`06875323`).
   @status:spec/plan
2. @fact:WAL-NEXT-SPLIT-FIRST **Первое дело следующего шага — РАСКОЛ.**
   `xtask/src/codegen/mod.rs` — **586 из 600**, а каждое преобразование
   требует проводки в `generate_into`. Прецедент раскола в этом же каталоге
   есть дважды (`format_id.rs`, `postproc.rs`). Ужимать вместо раскола
   запрещено: бюджет — сигнал структуры, а не редактор. @status:spec/plan
3. @fact:WAL-NEXT-MEASURE-FIRST **Перед нарезкой — замер, как перед каждым
   шагом.** За сессию замеры опровергли шесть записанных утверждений, и три
   меняли ПОРЯДОК шагов. Минимум под остаток Ф4.2: что сгенерированные типы
   несут сегодня (прочесть `generated/index/e1/entry/mod.rs` целиком); кто их
   потребляет (`vibe-index` НЕ зависит от `vibe-wire`, только dev-зависимость
   ради оракулов; `vibe-cli` зависит); каков радиус смены формы типа до того,
   как он станет диффом. @status:spec/plan
4. @fact:WAL-NEXT-INDEPENDENT **Независимая полоса режется холодной:** S1 и S6
   измерены, боссовы решения по каждому записаны в ТЗ. S7 — судейская, в
   любой момент, и она же закрывает долг печати. @status:spec/plan
5. @fact:WAL-NEXT-OWNER **За владельцем три вопроса, ни один не блокирует:**
   S2 (org-права), **Ж8** (`--full` и исчезнувшая запись) и **B-056** (язык
   схем не выражает `u64`). @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-PROP044-IS-THE-LAW **Вся идеология форматов — ратифицированный
  PROP-044**; здесь не пересказывается. Терминология §2b обязательна. @status:impl/done
- @fact:WAL-C-SCHEMA-DESCRIBES-THE-WIRE **Схема описывает ПРОВОД, и это
  разделяющий критерий.** Аннотации политики (`x-vocabulary`, `x-empty`,
  `x-default`, `x-rust-type`, `x-vocabularies`) говорят что-то о ФОРМАТЕ и
  потому законны. Боксирование варианта — деталь представления в Rust, и
  потому оно НЕ аннотация, а безусловное преобразование генератора. Всякое
  будущее «а не вписать ли это в схему» проверяется этим вопросом. @status:impl/done
- @fact:WAL-C-ANNOTATIONS-ARE-INERT **Аннотации политики написаны, но сегодня
  их НИКТО не читает** — потребитель строится остатком Ф4.2. Не принимать их
  за работающий механизм и не переизобретать: это данные, ждущие читателя. @status:impl/done
- @fact:WAL-C-VOCABULARY-ONE-HOME **Словарь живёт в ОДНОМ доме
  (`formats/vocabularies.json`, 18 фрагментов), подстановку делает наш слой, и
  она ТРАНЗИТИВНА**: фрагмент объявляет свои зависимости, резолвер замыкает,
  отказывает на цикле и называет цепочку. Средствами JTD это неисправимо —
  `ref` разрешается только внутри документа, а висячая ссылка роняет
  jtd-codegen ПАНИКОЙ без диагностики, поэтому валидацию делает наш слой. @status:impl/done
- @fact:WAL-C-ROUTE-BY-PATH **Маршрут выхода ключится ДОМОМ схемы и её путём,
  а не стемом.** Каталог схемы несёт эпоху (PROP-044 §4.6), поэтому
  `index/e1/entry` и будущий `index/e2/entry` обязаны остаться разными
  модулями; а маршрут по стему уводил бы вторую engine-схему в `vibe-wire`. @status:impl/done
- @fact:WAL-C-CODEGEN-STAGING **`check-codegen` — это `git diff --exit-code`,
  то есть рабочее дерево против ИНДЕКСА, и untracked он НЕ ВИДИТ.** Слайс,
  трогающий `generated/**`, садится в порядке **стейдж → панель → коммит**. За
  сессию правило применялось трижды и трижды было нужно. @status:impl/done
- @fact:WAL-C-JOURNAL-IS-THE-TRUTH **Каталог — проекция журнала, и починка
  идёт в одну сторону.** Если `rebuild --check` разошёлся, неправ КАТАЛОГ. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки:** применить дифф →
  `cargo fmt --all` → **`cargo xtask specmap`** → панель → суд → коммит. Карта,
  пересобранная ДО того как лёг код, — карта дерева, которого уже нет. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель ОБРЫВАЕТСЯ на первом красном
  шаге** — «упала на X» ничего не говорит о шагах после X; зелёный хвост —
  единственное доказательство, что прогнались все. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ** — красное доказательство, растянутое на два таргета,
  показывает одно падение без `--no-fail-fast`. Сработало на глазах в этой
  сессии. @status:impl/done
- @fact:WAL-C-FMT-CHECK-IS-THE-GATE **`cargo fmt` без `--check` выходит с нулём
  и когда результат НЕ чист.** Файл, вырезанный текстом, начался с пустой
  строки; `cargo fmt --all` до него дошёл, пустоту не убрал, `--check` на ней
  упал — панель умерла на третьем шаге из 51. Проверять той формой, которую
  гоняет панель; вырезанный файл начинать шапкой `//!`. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён
  тем же заходом**: scan → **mirror** → batch JSON → `merge-verdicts.py` →
  долг 0. **`campaigns/**`, `BACKLOG.md` и `spec/WAL.md` в судимый корпус НЕ
  входят** (проверено: 281 файл, BACKLOG среди них нет). @status:impl/done
- @fact:WAL-C-EVIDENCE-CITES-SYMBOLS **Evidence цитирует ИМЯ СИМВОЛА, а не
  номер строки**, и вердикт, заменяющий прежний, объясняет замену. @status:impl/done
- @fact:WAL-C-HONEST-SEAL **Печать — только за проверенное**; сейчас она
  сознательно не ставится (см. `##WAL-KI-SEAL-DEBT`). @status:impl/done
- @fact:WAL-C-SPECMAP **Правка текста спеки и новый код двигают карту**:
  `cargo xtask specmap` в той же посадке. **Специмап индексирует JTD-схемы под
  `schemas/` РЕКУРСИВНО** — новая схема даёт запись, и определения внутри неё
  тоже. @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ; режим
  — `SUBAGENT-MODE.toml` (`claudez`); пакеты режет босс; воркеры не запускают
  git; диффы читает, правит и коммитит босс; ревью не делегируется никогда.
  §8 несёт **45 размеченных фактов**, §9 — разбор чистого фан-аута. @status:impl/done
- @fact:WAL-C-PACKET-PERIMETER **Закрытый периметр записи обязан называть файл,
  который предписанный им же раскол создаст** — иначе два пункта пакета
  сталкиваются, и слабый писатель разрешает столкновение ужатием. @status:impl/done
- @fact:WAL-C-SELF-VERIFY-FOLLOWS-THE-TOUCH **Self-verify пишется от того, что
  правка ТРОГАЕТ, а не от того, что добавляет.** Правка общих ДАННЫХ обязана
  назвать крейты, чьи тесты на этих данных стоят — тот же закон, что для
  общего ТИПА, только на другом субстрате. @status:impl/done
- @fact:WAL-C-REPORT-BEFORE-GATES-BUT-PENDING **Отчёт пишется РАНЬШЕ финальных
  гейтов, НО каждая строка приёмки начинается словом PENDING.** Первая
  половина спасает отчёт от среза; без второй срез сохраняет отчёт вместе с
  неизмеренным утверждением — случилось, поймано перемером
  (`clippy exit 0` при реальном 101). @status:impl/done
- @fact:WAL-C-RED-PROOF **Красное доказательство — пункт приёмки, и босс
  перемеряет его СВОЕЙ целью.** Для объединения красных ДВА: поле у плеча и
  целое плечо — плечо, которого не упражняли, не доказывает ничего. @status:impl/done
- @fact:WAL-C-TEST-RE-AIMED-NOT-SATISFIED **Тест, покрасневший на ЗАКОННОМ
  изменении, перенацеливают на его инвариант — и обязаны доказать, что
  суженный сторож всё ещё падает на настоящем нарушении.** Без этого шага
  сужение неотличимо от удаления в три приёма. @status:impl/done
- @fact:WAL-C-KILLED-TASK-LEAVES-A-LIVE-WORKER **Убитая harness'ом задача не
  убивает воркера** — два случая за сессию, оба дописали работу (логи росли на
  62 и 5 КБ ПОСЛЕ «убийства»). Фоновый сторож до стабилизации лога, **никогда
  не прибираться вокруг живого процесса**, читать worktree, добирать хвост.
  «Killed» — не вердикт. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: никогда голый `cd` — только
  `( cd … && … )` / `git -C` / абсолютный путь; `cmd | tail; echo $?` печатает
  код ПАЙПА; **правки только editor-инструментами**; python — с
  `PYTHONIOENCODING=utf-8`; бюджет файла 600 строк ПОСЛЕ `cargo fmt`, **и он
  покрывает `tests/**`**. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты heredoc
  `-F -`; раскатка только `cargo xtask mirror`; Rules 1–4 связывают каждый
  коммит. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки
  дисциплины не трогаются. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-15/16, 25 коммитов (`d25f954a`..`3f564059`):** замер
под Ф4 четырьмя заходами (три read-only пакета + боссов спайк генератора) →
переписан §7 плана под шесть опровержений и девять решений → **Ф4.0** (раскол
`codegen.rs`; рекурсивный сбор схем и маршрут по дому; отказ на сегменте пути,
который не может быть модулем) → **Ф4.1a** (словник + подстановка) →
**Ф4.1a-2** (транзитивное замыкание) → **Ф4.1b-1..3** (шесть схем, шесть
оракулов) → **Ф4.2a** (боксирование плеч объединения) → пять новых фактов в §8
запускалок и запись B-056 в `BACKLOG.md`. Панель зелёная на каждой посадке,
раскатано на оба зеркала. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, `.wt/`-worktree'ов нет,
дерево чисто. Два worktree под `~/.fractality/runs/**` — чужие, к этой
кампании отношения не имеют и не трогаются. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-B056 **B-056 — язык схем не выражает тип, которым пишет наш
  писатель (P2, open).** У JTD нет 64-битного целого вовсе (генератор
  отвергает `uint64` и `int64`), а `Repomd.File.size` объявлен `u64`. Схема
  говорит `uint32`: истина ниже 2³², ложь на нём и выше — то есть наш писатель
  способен породить документ, который наша же схема объявляет невалидным.
  Режим отказа при этом громкий (закон 1 соблюдён). Три выхода измерены и
  записаны; каждый трогает либо провод, либо продуктовый тип ⇒ решение
  владельца. **Класс, а не случай:** всякое поле шире 32 бит придёт к той же
  развилке. @status:spec/plan
- @fact:WAL-KI-TUPLE-GAP **Второй пол JTD: кортежа нет.** `Event::Renamed`
  несёт `(Group, String)`; схема описывает `elements` строк, теряя арность и
  позиционный смысл. Записано в описании самого поля; оракул пинит длину пары.
  Тот же класс, что B-056. @status:impl/done
- @fact:WAL-KI-VOCAB-OUTSIDE-THE-MAP **Единственный дом словаря вне периметра
  карты.** Специмап индексирует JTD-схемы под `schemas/`; `formats/vocabularies.json`
  туда не попадает, и карта потеряла две записи при миграции инлайновых копий.
  Переименованием не лечится (проверено: карта смотрит только в `schemas/`, а
  `check-codegen` от переименования падает — имя словника несущее). Починка
  требует тронуть движок дисциплины, что ТЗ запрещает ⇒ **дефер по имени**. @status:spec/plan
- @fact:WAL-KI-GENERATOR-PIN-UNRECONCILED **Пин генератора не сведён с
  артефактами.** Заявлено `jtd-codegen 0.4.1`, все артефакты печатают в шапке
  `v0.2.1`, и ни один гейт версию не проверяет. Постпроцессор привязан к форме
  эмиссии, поэтому свести надо ДО следующих преобразований, а не после. @status:spec/plan
- @fact:WAL-KI-CODEGEN-BUDGET **`xtask/src/codegen/mod.rs` — 586 из 600.**
  Следующее преобразование обязано начаться с раскола. @status:impl/done
- @fact:WAL-KI-J8-OPEN **Ж8 — открытый вопрос владельца.** Что означает
  `--full` для записи, которую скан больше не видит: похоронить надгробием или
  удержать как ненаблюдавшуюся. Сегодняшнее поведение стоит. @status:spec/plan
- @fact:WAL-KI-SEAL-DEBT **Печать суда сознательно не поставлена.**
  `spec/modules/vibe-index/PROP-005-package-index.md` несёт двинувшиеся факты
  нескольких заходов. Асимметрия цен: не напечатать стоит одной проверки,
  напечатать ошибочно — ложного «проверено», и прецедент отката есть
  (`498e8c8b`). Закрывается S7. @status:impl/done
- @fact:WAL-KI-F43-PERIMETER **Периметр запрета Ф4.3 — 133 файла** (объединение
  `Serialize`∪`Deserialize` по `crates/**`+`xtask/**` вне `generated/**`), а не
  69: записанное число было срезом только по `Deserialize` и только по
  `crates/`. Гейт садится рехетом (К4); целевое правило К2 «реестр + маркер» —
  дефер с триггером «посадка Ф4.2». @status:spec/plan
- @fact:WAL-KI-FIVE-SICK-DICTIONARIES **Раздвоенных словарей пять, а не один**
  (`BindingSite`, `PackageKind`, `NamingConvention`, `DeliveryMode`,
  `SourceKind`), и **у `PackageKind` паритет-теста НЕТ** вопреки записанному.
  Четыре сводит Ф4.2 реэкспортом; `SourceKind` живёт в форматах, которые это
  ТЗ не описывает, — именованный дефер. @status:spec/plan
- @fact:WAL-KI-UNTESTED-FRAGMENT-REFUSAL **Названный пробел покрытия:** отказ
  на сломанной аннотации ВНУТРИ фрагмента реализован, но тестом не закрыт
  (воркер сказал прямо). Поведение зеркалит покрытый путь на стороне схемы. @status:spec/plan
- @fact:WAL-KI-STALE **34 файла stale** (стоячий долг сдвигов байтов) —
  адресовано кампанией S7. @status:impl/done
- @fact:WAL-KI-B074 **Второй якорь факта в абзаце глотается молча** (B-074) —
  механизм чека не построен. @status:impl/done
- @fact:WAL-KI-B075 **Панель может мигнуть на чистом дереве** (B-075) — читать
  отказ, не перезапускать вслепую. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие
  выходного гейта Phase E, ждёт рулинга. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → документы предмета (PROP-044 + три ТЗ) →
`SUBAGENT-LAUNCHERS.md` целиком перед фан-аутом** — и берёт каждое число из
команд сверху, после rescan. Этот файл главнее `CONTINUE.md`; ратифицированный
PROP-044 главнее обоих. @status:impl/done
