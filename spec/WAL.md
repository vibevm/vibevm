# WAL — Project Continuation State {#root}

_Updated: 2026-08-10, wind-down №18 (**вопрос №1 владельца получил проектное
предложение**: рамка перевёрнута на «среду перманентного перелома», созван
отдельный Fable-совет, написаны PROP-044 + вердикт-лоре + ТЗ, приняты четыре
рулинга владельца — допубликационный переключатель, нейтральность к
провайдеру, модель заморозки, смертность плана. Исследование импортировано в
репозиторий. Долг 0/0, панель зелёная, ~16 коммитов раскатаны этим
сворачиванием.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Работа следующей сессии и её порядок — в
`NEXT-SESSION-PROMPT.md`** (переписан 2026-08-10): три документа предложения
целиком → доклад → стоп. Ратификация PROP-044 — гейт всего дальнейшего. @status:impl/done

@fact:WAL-THE-PROPOSAL **Центр состояния — три документа вопроса №1:**
контракт [`spec/common/PROP-044-change-native-formats.md`](common/PROP-044-change-native-formats.md)
(идеология change-native форматов + четыре рулинга владельца 2026-08-10),
лоре [`spec/design/change-native-formats-verdict.md`](design/change-native-formats-verdict.md)
(вердикт совета: «одноразовые миры», отвергнутые альтернативы), план
[`campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md`](../campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md)
(B1–B15, D1–D14, Ф0–Ф6, приложения А/Б, §11 смертность). **Не
ратифицировано; фазы не начаты.** Исследование, на котором всё стоит, —
[`spec/research/schema-evolution-2026-08/`](research/schema-evolution-2026-08/)
(импортировано 2026-08-10; вне размечаемого корпуса; справочное). @status:impl/done

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/summary.py
python campaigns/packages-2026-09/tasks/judging-debt.py
python campaigns/packages-2026-09/tasks/text-stability.py
```

## Current phase {#current-phase}

@fact:WAL-PHASE **Progress Control (PROP-043) — wave 2, `packages-2026-09`,
Phase E. Внутри осушения бэклога вопрос №1 владельца доведён до предложения
(2026-08-09/10); остальные четыре вопроса — в `BACKLOG.md #owner-decisions`.** @status:impl/done

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): корпус **281
файл, 0 неразмеченных фактов**; вердиктов **12 223 при 98.2 %**, per-fact
63.8 %; долг **0 неосуждённых, 0 осиротевших**, 35 stale. `main` чист,
панель зелёная (прогон этого сворачивания), карта чиста. Раскатка — этим
сворачиванием (`cargo xtask mirror`); проверка — `mirror --check`. @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-RATIFY **Ратификация PROP-044 владельцем** (или правки) —
   гейт фазы 0 ТЗ. @status:spec/plan
2. @fact:WAL-NEXT-KNOBS **Открытые ручки владельца:** политика резолвера для
   публичного потребления каналов (ТЗ D14 СТОП); уровень лога по умолчанию
   (D11); **идентичность — четыре пункта разбора 2026-08-10, в ТЗ НЕ
   заведены**: модель доверия группы (записать «заявка, не удостоверение»),
   конвенция для авторов без доменов, грамматические сужения (группа ≥2
   сегментов; `_` в группе; запрет `+build` в публикуемых версиях), слот
   `vibedeps/<kind>-<name>` → слот от идентичности (сейчас в пути нет группы
   и есть вид — коллизия одноимённых пакетов разных групп, переезд при смене
   вида). @status:spec/plan
3. @fact:WAL-NEXT-PHASE0 **После ратификации — фаза 0 ТЗ** (спайки, без
   коммитов): M-RMW (объём переделки мутаций), PoC генератора открытых
   словарей, инвентарь форматов. @status:spec/plan
4. @fact:WAL-NEXT-OWNER-Q **Вопросы №4 и №5 владельца** — не тронуты
   (`AUDIT.md 2026-08-06-01`; `BACKLOG.md #b-019` часть (в)). @status:spec/plan

## Constraints — do not violate {#constraints}

- @fact:WAL-C-PROP044-IS-THE-LAW **Вся идеология форматов — PROP-044**; здесь
  не пересказывается. Четыре рулинга владельца 2026-08-10 записаны там как
  spec/done: `##THE-PUBLIC-SWITCH` (до объявления владельцем первого показа —
  никаких миграций; факт публикации не выводится из технических событий),
  `##PROVIDER-NEUTRALITY` (git — представление; акт — событие журнала;
  проверяльщик — наш хэш; локаторы непрозрачны), `##THE-FREEZE-MODEL`
  (frozen — флаг манифеста внутри хэшируемого содержимого, односторонний;
  каждая поверхность показывает статус), суженный `##FORBID-BYTE-MUTATION`
  (адрес = замороженная координата). @status:impl/done
- @fact:WAL-C-NEW-SPEC-JUDGED-SAME-PASS **Новый/правленый спек-файл размечен
  и осуждён тем же заходом**: scan → mirror → batch JSON →
  `merge-verdicts.py` (правка существующего факта — `--force` с evidence
  «что заменяет и почему прежний был верен») → `seal` → долг 0. Имена
  `@fact:` не совпадают с якорями заголовков `{#…}` — DuplicateId. @status:impl/done
- @fact:WAL-C-EDITING-A-SPEC-MOVES-THE-MAP **Правка текста спеки двигает
  карту** — `cargo xtask specmap` в той же посадке; `--check` зелёный. @status:impl/done
- @fact:WAL-C-PLAN-IS-TEMPORARY **План временный**: посадка фазы = спек-дифф +
  могильник секции в том же коммите; при закрытии плана ссылки на него
  уходят из спек (ТЗ §11). Содержимое не живёт в двух местах. @status:impl/done
- @fact:WAL-C-INSTRUMENTS **Прочитай отказ, никогда не обходи**: инструменты
  ловят то, что не ловит внимательность (мигающий тест панели — B-075:
  не перезапускать вслепую, читать). @status:impl/done
- @fact:WAL-C-MEASURE-FIRST **Мерить построенное до постройки, периметр
  называть с числом**; grep лжёт в обе стороны — мерить по структурному полю. @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читать ЦЕЛИКОМ;
  `SUBAGENT-MODE.toml` перед КАЖДЫМ фан-аутом; пакет собирать копированием
  закрывающих секций ДО тела; воркеры не запускают git; диффы читает и
  коммитит босс; `meta.md` — отдельный шаг. Native-агенты Claude — только по
  тесту проверяемости (архитектура/вердикты — да, как этот созыв совета). @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: cwd Bash-инструмента персистентен —
  абсолютные пути/`git -C`; правки только editor-инструментами; python — в
  файл, `PYTHONIOENCODING=utf-8`; реальные коды выхода, вердикт из хвоста;
  бюджет длины файла 600 строк ПОСЛЕ `cargo fmt`. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты через
  heredoc `-F -`; раскатка только `cargo xtask mirror` (fast-forward, никогда
  `--force`); Rules 1–4 связывают каждый коммит; воркер — инструмент, не
  соавтор. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; правка
  авторского движка ⇒ `cargo xtask sync-engines` отдельным шагом (51 пара). @status:impl/done
- @fact:WAL-C-PRESENTATION **Подача владельцу**: сначала простой смысл без
  чтения спек, дерево для развилок, точные имена приложением; ясность
  первой, точность сразу за ней. @status:impl/done
- @fact:WAL-C-NO-PAUSE **Мандат исполняется автономно** — остановка только на
  настоящей развилке владельца, по одной за раз. Resume = доклад, потом
  ждать. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-09/10, ~16 коммитов:** доклад по пяти вопросам после
полного чтения исследования (~12 400 строк) → рамка владельца перевёрнута →
созыв Fable-совета → PROP-044 (47 фактов, судимы тем же заходом) → ТЗ
(D1–D14, Ф0–Ф6, приложения) → вердикт-лоре (35 фактов) → импорт исследования
(41 файл) → четыре рулинга владельца в контракт → спайк jtd-codegen
(тегированные объединения работают; строгость/открытые словари — наш слой) →
разбор идентичности (четыре пункта, ждут рулинга) → строка вопроса №1 в
бэклоге обновлена. Панель зелёная на каждой границе. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет; `.wt/` пуст; дерево
чисто. (Два worktree старых fractality-прогонов в глобальном доме — не этой
работы.) @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-P1-BLOBS **Document-level вердиктов 4 347** (после конверсий);
  крупнейший блоб (276) содержит ложную клаузу; намеренные партии —
  `PROP-008` 90 и `PROP-005` 276; вопрос №4 владельца (P1 в `AUDIT.md`). @status:impl/done
- @fact:WAL-KI-STALE **35 файлов stale** — сдвиги байтов после суждения;
  per-fact ответ — `text-stability.py`. @status:impl/done
- @fact:WAL-KI-B074 **Второй якорь факта в абзаце глотается молча** (B-074) —
  механизм чека не построен. @status:impl/done
- @fact:WAL-KI-B070-DISSOLVING **B-070 (словарь в 14 местах) закрывается
  структурно фазой Ф4 ТЗ (G9), B-072 — фазами Ф2–Ф3, B-071 — D11, B-067 —
  растворён D14** — при ратификации; до неё все живы. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие
  выходного гейта Phase E, ждёт рулинга. @status:impl/done
- @fact:WAL-KI-B075 **Панель может мигнуть на чистом дереве** (B-075,
  git clone в общем прогоне) — читать отказ, не перезапускать вслепую. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` → 
`NEXT-SESSION-PROMPT.md` → три документа предложения → транспортный закон
воркеров → живые строки `BACKLOG.md` и открытые находки `AUDIT.md`** — и
берёт каждое число из команд сверху, после rescan. Этот файл главнее
`CONTINUE.md`; рулинги владельца главнее обоих. @status:impl/done
