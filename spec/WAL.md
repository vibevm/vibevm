# WAL — Project Continuation State {#root}

_Updated: 2026-08-19 (**мандат change-native закрыт целиком** — §11 свёрнут,
план из 3406 строк стал 630 могильников. Следом закрыта B-089: три документа
индекса перестали лгать наружу. Затем — разговор с владельцем, давший **пять
рулингов**, все записаны в спеки тем же днём, и **новый слайс из пяти
коммитов** с одной открытой развилкой. 8 коммитов, четыре зелёные панели,
31 вердикт, долг 0 после каждого захода, зеркала раскатаны четырежды.)_

@fact:WAL-READ-THE-PROMPT-FIRST **Порядок работы — `NEXT-SESSION-PROMPT.md`**:
boot → документы предмета → короткий доклад → исполнение. Промт есть слово
владельца. @status:impl/done

@fact:WAL-THE-LAW **PROP-044 — ратифицированный стоячий закон каждого формата.**
Терминология §2b обязательна: snapshot ↔ frozen — антонимы **одной булевой оси
без третьего состояния**; «канал» — авторский указатель версий; режим
материализации — `copy`. @status:impl/done

## Current phase {#current-phase}

@fact:WAL-PHASE **Change-native стройка ЗАКРЫТА ПОЛНОСТЬЮ.** Все фазы Ф0–Ф6,
приёмка, сверка предсказаний и §11 (свёртка плана) исполнены. План
`campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md` — теперь
могильники плюс дословные §0/§10/§11; **он удаляем, и решение об удалении
владельческое**. Ссылок на него в `spec/**` нет ни одной живой. @status:impl/done

@fact:WAL-NEW-SLICE **Слайс в полёте — новый: снятие пакетов, локальный склад и
одна починка адреса.** Пять коммитов и одна развилка перечислены в `TASKS.md`,
раздел «Current slice». Рассуждение — не там, а по якорям спек, которые он
называет. @status:impl/done

@fact:WAL-FIVE-RULINGS **Пять рулингов владельца 2026-08-19, все записаны в
спеки в тот же день:** @status:impl/done

1. @fact:WAL-R-STORE **Склад пакетов — распакованные каталоги по идентичности**
   (`PROP-010` §2.7), проверяемые хэшем, который локфайл уже пиннит. Клоны
   отвергнуты: клон привязан к живости источника ПО ПОСТРОЕНИЮ — его обновление
   и есть обращение к источнику. @status:spec/work
2. @fact:WAL-R-CACHE-WINS **Попадание в кэш важнее молчащего реестра**
   (`PROP-010` §2.6): пакет из склада используется, даже если не существует ни
   в одном реестре. Это НЕ режим `--offline`. @status:spec/work
3. @fact:WAL-R-THREE-ABSENCES **Отсутствие бывает трёх видов, ответов два:**
   есть в кэше → молча ставится; есть надгробие → говорится причина и преемник;
   нет ничего → «такого пакета не существует». Третий и второй **намеренно
   неразличимы** при полном удалении. @status:spec/work
4. @fact:WAL-R-THREE-OPERATIONS **Снятие — три операции над двумя сущностями**
   (`PROP-005` §2.11): полное удаление (`remove`, построено), отзыв версии
   (`yank`, не хватает только команды), закрытие имени (`bury`, не построено).
   Переименование — **не отдельный носитель**: это снятие, называющее
   преемника, и арм `renamed` уходит из словаря журнала. @status:spec/plan
5. @fact:WAL-R-JOURNAL **Журнал остаётся только-дописываемым** (`PROP-044` §3),
   и цена названа: имя, опубликованное однажды, названо там навсегда. Удаление,
   дотягивающееся до журнала, — отдельный механизм, сознательно не построенный
   и ни в одном плане не стоящий. @status:spec/plan

@fact:WAL-NUMBERS-COME-FROM-COMMANDS **Числа воспроизводятся командами; сперва
rescan, иначе они отвечают про кэш. И `judging-debt.py` читает ЗЕРКАЛО —
до `mirror` он показывает ноль над реальным долгом:** @status:impl/done

```bash
cargo run -q -p vibe-cli --bin vibe -- progress scan   --campaign campaigns/packages-2026-09
cargo run -q -p vibe-cli --bin vibe -- progress mirror --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
bash tools/self-check.sh          # реальный код выхода, вердикт из хвоста, 53 шага
cargo xtask mirror --check
```

@fact:WAL-STATE **Состояние на чекпойнте** (команды главнее): `main` чист,
HEAD `6dff5f05`, панель `self-check: all green` — **четыре зелёных прогона за
сессию**, каждый 53 шага с реальным кодом выхода 0; корпус 281 файл,
13884 маркера, 0 неразмеченных; долг **0 неосуждённых, 0 осиротевших**,
36 stale; карта — 6131 спек-юнит / 1042 tagged / 975 рёбер / 0 подозрений /
0 сирот. Зеркала синхронны (gitverse + github @ `6dff5f05`). Воркеров нет,
`.wt/` пуст. @status:impl/done

@fact:WAL-EXPECTED-DIAGNOSTICS **Два ожидаемых числа, которые НЕ дефекты — не
чинить.** `vibe check` даёт **44 info** `manifest_epoch` и **1 warning**
`local_source_freshness`. **За сорок с лишним посадок подряд оба не сдвинулись
ни разу. Рост любого из них — находка, а не шум.** @status:impl/done

## Next {#next}

1. @fact:WAL-NEXT-B056 **Починка координаты `B-056` — самый мелкий пункт и ни
   от чего не зависит.** Номер занят дважды: закрытая строка про наследование
   документов и живая про невыразимый тип. Ссылка ведёт на закрытую, а на
   живую указывает ратифицированная спека. @status:spec/plan
2. @fact:WAL-NEXT-RETIREMENT-FACT **Факт снятия заменяет `renamed`** — схема
   журнала, событие, новое плечо проектора, оракул, записка о переломе, плюс
   фраза `PROP-005` §2.18, которая от этого станет ложной. @status:spec/plan
3. @fact:WAL-NEXT-VERBS **`yank`, затем `bury`** — первая одна команда, вторая
   зависит от пункта 2. @status:spec/plan
4. @fact:WAL-NEXT-STORE **Склад пакетов.** Трудная половина — не склад, а то,
   что он заменяет: стирание клона обслуживает переключение на зеркало, и
   отменять его нельзя, не дав зеркалам другой механизм. @status:spec/plan
5. @fact:WAL-NEXT-OWNER **Живых СТОП-ВЛАДЕЛЕЦ два:** **S2** (org-права) и
   **обход против пропавшего пакета** (бывший Ж8, сужен рулингами). @status:impl/done

## Constraints — do not violate {#constraints}

- @fact:WAL-C-ANCHOR-WITH-A-VERDICT-IS-NEVER-REPLACED **Якорь, у которого есть
  вердикт, не заменяют — ему ставят надгробие с наследником.** Поймано ТРИЖДЫ
  за сессию дифом множеств якорей против HEAD, и один раз — на якоре,
  заведённом этой же сессией часом раньше. Механика: удаление якоря делает его
  вердикт ОСИРОТЕВШИМ, а осиротевших обязано быть ноль. Сверка гоняется ПЕРЕД
  посадкой всякой правки спеки, с контролем, что диф видит подсаженное удаление. @status:impl/done
- @fact:WAL-C-THE-PANEL-MUST-COVER-WHAT-LANDS **Панель, запущенная до правки,
  не про то дерево.** Дважды за сессию пришлось снимать идущий прогон, потому
  что после его старта менялись файлы. Два cargo на одном `target/` всё равно
  сериализуются на локе, так что дешевле снять и перезапустить, чем ждать оба. @status:impl/done
- @fact:WAL-C-RECORDING-A-DECISION-FINDS-DEFECTS **Запись решения находит
  дефекты, которых поиск не находит.** За один заход письма нашлись: мина
  стирания надгробий, исключение из уже записанного закона о молчании, и ДВА
  моих собственных выдуманных утверждения — про `rebuild`, у которого есть
  только `--check`, и про `verify`, который обходит записи манифеста, а не все
  файлы. Оба поймал замер ПЕРЕД посадкой, а не после. @status:impl/done
- @fact:WAL-C-THE-TOMBSTONE-ERASURE-MINE **Надгробие, поставленное не фактом
  журнала, стирается следующей же посторонней публикацией.** Носитель
  наполняется только при ЧТЕНИИ каталога с диска; проекция из журнала его не
  создаёт, а мутация строит состояние проекцией и записывает его. Сегодня
  недостижимо (производителя нет вовсе) — и потому записано сейчас, пока
  производитель проектируется. @status:spec/plan
- @fact:WAL-C-EMPTY-OUTPUT-IS-A-CLAIM **Пустой или нулевой вывод есть
  УТВЕРЖДЕНИЕ.** Прежде чем читать ноль доказательством — скорми инструменту
  случай, который он ОБЯЗАН различить. Сработало трижды: греп триггеров искал
  русское написание там, где реестр пишет английское; проверяльщик статусов
  дал два ложных срабатывания узким окном; «ноль ссылок на якорь» подтвердилось
  только рядом с контролем на заведомо цитируемом. @status:impl/done
- @fact:WAL-C-A-COUNT-CAN-BE-A-PROPERTY-OF-THE-TOOL **Счёт бывает свойством
  инструмента, а не предмета.** План был 3406 строк, а не 3055 — число хором
  повторяли промт, WAL, `CONTINUE.md` и `TASKS.md`. Всякое число, на котором
  стоит решение, меряют двумя способами. @status:impl/done
- @fact:WAL-C-A-PERIMETER-IS-COUNTED-NOT-NAMED **Когда работа разрушительна,
  периметр считают по ФАЙЛУ, а не по понятию.** Карта смертности плана была
  нарезана по понятию «рулинг» и не поймала операционные уроки вне рулингов;
  один из них («панель обрывается на первом красном») жил только в летучем WAL. @status:impl/done
- @fact:WAL-C-MIRROR-CHECK-SAYS-IN-SYNC-OVER-A-BEHIND-HOST **`mirror --check`
  печатает `BEHIND <хост>` и следующей строкой `all targets in sync`, код
  выхода 0.** `Behind` не попадает в вердикт по построению. Быстрый старт и
  рецепт завершения оба читают этот хвост — значит чекпойнт может записать
  «зеркала синхронны» над отставшим хостом. Развилка `BACKLOG` **B-090**;
  до рулинга сверять строки, а не только хвост. @status:spec/plan
- @fact:WAL-C-GITVERSE-IS-FLAKY **GitVerse отвечает через раз** (`Connection
  closed by 127.250.0.143`) — три отказа из шести прогонов за сессию, лечится
  повтором. Гейт при этом говорит верно: «ничего не разошлось, почини связь и
  повтори». Отказ push'а на один хост — НЕ расхождение. @status:impl/done
- @fact:WAL-C-A-PROMISE-IS-RE-MARKED-NOT-DELETED **Спека, обещающая
  непостроенное, чинится СТАТУСОМ, а не удалением требования.** @status:impl/done
- @fact:WAL-C-A-RULING-HAS-TWO-LAWFUL-HOMES **Дом рулинга бывает двух видов:**
  якорь спеки — для политики, связывающей за пределами одного файла; докблок
  кода — для формы именно этого кода. Домом НЕ являются: план, `spec/WAL.md` и
  `CONTINUE.md` (летучие), находка в `harvest/`, коммит-сообщение. @status:impl/done
- @fact:WAL-C-JUDGING-DEBT-READS-THE-MIRROR **`judging-debt.py` читает зеркало,
  а не живой скан.** Порядок scan → mirror → batch → merge → debt обязателен
  целиком. Батч вердиктов — временный вход (пишется в scratchpad); долговременный
  выход — `run/cache.json`. @status:impl/done
- @fact:WAL-C-JUDGE-SAME-PASS **Новый/правленый спек-файл размечен и осуждён
  тем же заходом.** **`campaigns/**`, `BACKLOG.md`, `TASKS.md` и `spec/WAL.md`
  в судимый корпус НЕ входят** — перепроверено: удаление восьми якорей
  `BACKLOG` не сдвинуло счётчик маркеров. @status:impl/done
- @fact:WAL-C-PANEL-STOPS-AT-FIRST-RED **Панель ОБРЫВАЕТСЯ на первом красном
  шаге** и о последующих не говорит ничего. Зелёный хвост — единственное
  доказательство, что прогнались все. Записано в шапку `tools/self-check.sh`. @status:impl/done
- @fact:WAL-C-CARGO-TEST-FAIL-FAST **`cargo test` останавливается на первом
  упавшем ТАРГЕТЕ** (`--no-fail-fast`); **`cargo fmt` без `--check` выходит с
  нулём и когда результат НЕ чист**; **красный СУЩЕСТВУЮЩИЙ тест при аддитивной
  правке есть дефект правки, а не повод править тест**. @status:impl/done
- @fact:WAL-C-NEVER-DUPLICATE-A-NORMATIVE-VALUE **Нормативное значение не
  копируют. Числа-замеры в прозе спеки не пишутся вовсе** — их дом датированная
  находка. @status:impl/done
- @fact:WAL-C-JOURNAL-IS-THE-TRUTH **Каталог — проекция журнала, и починка идёт
  в одну сторону.** Если `rebuild --check` разошёлся, неправ КАТАЛОГ. И у
  `rebuild` есть **только** `--check`: доказательство поставляется, починка нет. @status:impl/done
- @fact:WAL-C-LANDING-ORDER **Порядок посадки:** применить дифф →
  `cargo fmt --all` → **`cargo xtask specmap`** → стейдж → панель → суд →
  коммит → `cargo xtask mirror`. Карта, пересобранная ДО того как лёг текст, —
  карта дерева, которого уже нет. @status:impl/done
- @fact:WAL-C-HONEST-SEAL **Печать — только за проверенное**; сейчас она
  сознательно не ставится (см. `##WAL-KI-SEAL-DEBT`). @status:impl/done
- @fact:WAL-C-DELEGATION **Транспорт воркеров**:
  `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС ЦЕЛИКОМ до
  первого фан-аута; режим — `SUBAGENT-MODE.toml` (`claudez`); пакеты режет
  босс; воркеры не запускают git; **ревью, спеки, планы и вердикты не
  делегируются никогда**. Замер — идеальная форма пакета; авторство спеки — нет. @status:impl/done
- @fact:WAL-C-SHELL **Shell-ловушки**: никогда голый `cd` — и опасен не сам
  `cd`, а КАЖДАЯ команда после него (сработало этой сессией: `cd` внутри
  проверки ссылок сдвинул каталог, следующая команда не нашла файл; ремонт —
  абсолютный `cd` с `pwd` и веткой в ТОЙ ЖЕ команде, дальше `( cd … && … )`).
  `cmd | tail; echo $?` печатает код ПАЙПА; правки только editor-инструментами;
  python — с `PYTHONIOENCODING=utf-8` и ВИНДОВЫМ путём. @status:impl/done
- @fact:WAL-C-GIT **Git**: никогда `git add -A` — явные пути; коммиты heredoc
  `-F -`; раскатка только `cargo xtask mirror`; Rules 1–4 связывают каждый
  коммит. @status:impl/done
- @fact:WAL-C-VENDORED **Вендоренные копии не редактируются**; движки
  дисциплины не трогаются. @status:impl/done
- @fact:WAL-C-AUTONOMY **Промт авторизует исполнение** — остановки только на
  СТОП-ВЛАДЕЛЕЦ, по одной за раз; доклады статусов, не вопросы. @status:impl/done

## Done (collapsed — see `git log`) {#done}

@fact:WAL-DONE **2026-08-18…19, 8 коммитов (`01856e25`..`6dff5f05`):** два дома
бездомным рулингам (`01856e25`, `ba3aea4a`), свёртка плана 3406→630 плюс снятие
двух указателей в PROP-044 (`a2e06bd7`), чек-лист (`becbcd9a`), выправка трёх
документов индекса и смерть строки B-089 (`02c4902b`), находка про гейт
раскатки (`7333800a`), пять рулингов владельца (`4882df53`), схлопывание
переименования и два имени (`04e6fe1d`), новый слайс (`6dff5f05`). Четыре
зелёные панели по 53 шага; 31 вердикт; долг 0 после каждого захода. @status:impl/done

## In progress {#in-progress}

@fact:WAL-INFLIGHT **Ничего в полёте.** Воркеров нет, дерево чисто, зеркала
синхронны. @status:impl/done

## Known issues {#known-issues}

- @fact:WAL-KI-B090 **`mirror --check` объявляет «all targets in sync» над
  отставшим хостом (B-090, P2).** Развилка: сузить вердикт до «ни один не
  РАЗОШЁЛСЯ» либо краснеть на отставании. @status:spec/plan
- @fact:WAL-KI-B056-DOUBLE **Номер `B-056` занят дважды**, и ссылка ведёт на
  закрытую строку, тогда как на живую указывает ратифицированная спека.
  Починка адреса — первый пункт слайса; вопрос разрядности (`u64` против
  `u32`) остаётся владельческой развилкой. @status:spec/plan
- @fact:WAL-KI-SCAN-DROPS-SILENTLY **Полный обход держит прежний и новый состав
  в памяти одновременно и НИКОГДА их не сравнивает** — прежний расходуется на
  три поля настроек, остальное выбрасывается, — после чего записывает
  утверждение, что найденное и есть весь опубликованный состав. Посчитать и
  сказать разницу — недостающая проверка; хоронить ли необъяснённую пропажу —
  развилка владельца. @status:spec/plan
- @fact:WAL-KI-B083 **`[[registry]].index_url` описан и не существует (B-083,
  P2)**; секция строгая, поэтому TOML-пример спеки — отказ разбора. @status:spec/plan
- @fact:WAL-KI-B084 **`repomd.json` объявлен точкой доверия, и ни один
  потребитель по нему ничего не сверяет (B-084, P2).** @status:spec/plan
- @fact:WAL-KI-B085 **HTTP-триггер переиндексации обещан тремя якорями и не
  построен (B-085, P2).** @status:spec/plan
- @fact:WAL-KI-B086 **Машины приоритетов конфигурации нет (B-086, P2).** @status:spec/plan
- @fact:WAL-KI-B087 **Атомарная запись не синхронизирует каталог (B-087, P2)** —
  задевает и журнал, восстановлением из которого чинится всё остальное. @status:spec/plan
- @fact:WAL-KI-B088 **Чекера «спека не ссылается на ЗАКРЫТЫЙ план» нет (B-088,
  P3)**, и норма промахнулась на своём единственном применении: §11.2 говорил
  «обе ссылки», их было четыре. @status:spec/plan
- @fact:WAL-KI-B080 **Резолвер клиента способен выбрать версию, которой сборка
  пользоваться не может (B-080, P2).** @status:spec/plan
- @fact:WAL-KI-B079 **Семь конвертов ответа `vibe-index` не описаны схемой и не
  стоят в реестре форматов (B-079, P2).** @status:spec/plan
- @fact:WAL-KI-B081-B082 **Словарь видов живёт не в одном месте (B-081)**;
  **гейт окна перелома обещает больше, чем меряет (B-082)**. Оба — результат
  прогона предсказаний, а не чтения. @status:spec/plan
- @fact:WAL-KI-CORPUS-TOMBSTONE-GAP **Корпус не несёт надгробия** — ни одно
  проецируемое событие журнала его не порождает. Закрывается пунктом 2 слайса. @status:spec/plan
- @fact:WAL-KI-MAP-EDGES **Три ребра прослеживаемости к PROP-005 ушли** с
  определениями в крейт провода, который в периметр карты не входит. @status:spec/plan
- @fact:WAL-KI-B078 **B-078 — «у JTD нет nullable» оказалось ложью (P2, open).** @status:spec/plan
- @fact:WAL-KI-SEAL-DEBT **Печать суда сознательно не поставлена.** PROP-005
  правится третью сессию подряд; печать закрывает либо кампания S7, либо
  сессия, готовая перечитать файл целиком. @status:impl/done
- @fact:WAL-KI-STALE **36 файлов stale** (стоячий долг сдвигов байтов) —
  адресовано кампанией S7. @status:impl/done
- @fact:WAL-KI-AUDIT-P1 **Аудит `2026-08-06-01` (P1)** — «ruled — re-judgement
  campaign pending», её исполняет кампания S7. @status:impl/done
- @fact:WAL-KI-PHASE-E-SIX **Шесть файлов корпуса на `work`** — условие
  выходного гейта Phase E, ждёт рулинга. @status:impl/done

## Session context {#session-context}

@fact:WAL-CTX-BOOT **Холодная сессия читает `CONTINUE.md` →
`NEXT-SESSION-PROMPT.md` → `TASKS.md` (слайс) → якоря спек, которые он
называет** — и берёт каждое число из команд сверху, после rescan И mirror.
Этот файл главнее `CONTINUE.md`; ратифицированный PROP-044 главнее обоих. @status:impl/done
