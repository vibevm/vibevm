# pin-stack v0.1.0 (fixture)

The stack `mcp:org.vibevm/pin-server` exact-pins (`=0.1.0`). The sibling
v0.2.0 exists precisely so a resolver that ignores the pin would pick it
— the PROP-027 §2.3 fixture pair.
