# Integration Beta — boot snippet

Companion fixture to `flow:integration-alpha`. Provides
`interface:trace-discipline` so any subskill probing for that
interface fires when this package is in the graph.
