# integration-beta protocol

Pulled into the install graph by integration-alpha's
conditional dep `[target."context(stack:integration-rust)"]`.
Provides the `interface:trace-discipline` interface tag that
other packages can require.
