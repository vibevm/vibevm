[package]
name = "integration-rust"
group = "org.vibevm"
kind = "stack"
version = "1.0.0"
authors = ["vibevm test fixtures"]
license = "EULA"
description = "Stack fixture activating integration-alpha's Rust-specific subskill and conditional dep"
keywords = ["test", "fixture", "integration", "rust"]

[boot_snippet]
source = "boot/30-stack-integration-rust.md"
category = "stack"

[provides]
capabilities = [
    "interface:build-system",
]
