# integration-rust stack

Rust-targeting stack fixture. Provides the `interface:build-system`
interface tag and the `stack:integration-rust` pkgref key that
integration-alpha probes for via `if_present`.
