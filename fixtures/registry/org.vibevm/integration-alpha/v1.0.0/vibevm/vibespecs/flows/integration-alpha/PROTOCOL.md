# integration-alpha protocol (canonical English)

The integration-alpha fixture exercises every PROP-003 r2 surface in
combination: features, subskills, language preference, conditional
dependencies, and PURL `describes` binding.

This is the canonical English content. A Russian sidecar
(`PROTOCOL.ru.md`) carries the localised version; `vibe install
--language ru` picks it up via the i18n fallback chain.
