# Integration Alpha — boot snippet

When working with `flow:integration-alpha`, follow the protocol in
`spec/flows/integration-alpha/PROTOCOL.md` for the canonical
discipline.

This snippet is the entry-point the agent reads at session start.
