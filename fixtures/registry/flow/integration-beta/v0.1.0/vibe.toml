[package]
name = "integration-beta"
kind = "flow"
version = "0.1.0"
authors = ["vibevm test fixtures"]
license = "EULA"
description = "Companion fixture pulled by integration-alpha's conditional dep when stack:integration-rust is active"
keywords = ["test", "fixture", "integration"]

[boot_snippet]
source = "boot/41-flow-integration-beta.md"
category = "flow"

[provides]
capabilities = [
    "interface:trace-discipline",
    "capability:beta-conventions",
]
