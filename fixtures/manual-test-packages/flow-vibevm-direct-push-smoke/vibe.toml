[package]
name = "vibevm-direct-push-smoke"
group = "org.vibevm"
kind = "flow"
version = "0.0.1"
authors = ["Oleg Chirukhin"]
license = "EULA"
description = "Throwaway test fixture for `vibe registry publish --repo-url`. Not a real package — just exercises the no-API direct git push path against a freshly provisioned host repo."
keywords = ["test", "smoke", "direct-push"]

[compatibility]
min_vibe_version = "0.1.0"
requires_kinds = []

[boot_snippet]
source = "boot/10-flow-vibevm-direct-push-smoke.md"
category = "flow"
