{
  "metadata": {
    "description": "Wire format for `vibe list --json`. Source of truth for `crates/vibe-wire/src/generated/list_report.rs`.",
    "spec": {
      "implements": "spec://org.vibevm.core/vibevm/common/PROP-000#JTD-SSOT"
    },
    "rustOptions": {
      "package": "vibe_wire::list_report"
    },
    "x-vocabularies": ["package_kind"]
  },
  "properties": {
    "ok": {
      "type": "boolean"
    },
    "command": {
      "type": "string",
      "metadata": {
        "description": "Always `\"list\"` for this report."
      }
    },
    "project": {
      "type": "string"
    },
    "count": {
      "type": "uint32"
    },
    "packages": {
      "elements": {
        "ref": "list_entry"
      },
      "metadata": {
        "x-empty": "emit",
        "description": "Required member: an empty project lists `[]` packages, never an absent key — omitting a required collection would produce a document invalid by this same schema (rule R21)."
      }
    }
  },
  "definitions": {
    "list_entry": {
      "properties": {
        "kind": {
          "ref": "package_kind"
        },
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "source_url": {
          "type": "string"
        },
        "content_hash": {
          "type": "string"
        },
        "boot_snippet": {
          "type": "string",
          "nullable": true,
          "metadata": {
            "description": "Filename of the package's boot snippet under `spec/boot/`, or null if absent."
          }
        },
        "files_written": {
          "elements": {
            "type": "string"
          },
          "metadata": {
            "x-empty": "emit",
            "description": "Required member: an entry always writes `files_written`, `[]` when the install wrote no files — omitting a required collection would produce a document invalid by this same schema (rule R21)."
          }
        }
      },
      "optionalProperties": {
        "registry": {
          "type": "string",
          "metadata": {
            "description": "`[[registry]].name` from `vibe.toml` that served this package. Absent for `--registry <path>` and legacy installs.",
            "x-default": null
          }
        },
        "source_ref": {
          "type": "string",
          "metadata": {
            "description": "Git ref the content was fetched at — typically `v<version>`. Absent for non-git sources.",
            "x-default": null
          }
        },
        "resolved_commit": {
          "type": "string",
          "metadata": {
            "description": "Commit hash the ref resolved to. Reserved for resolver-aware installs.",
            "x-default": null
          }
        },
        "overridden": {
          "type": "boolean",
          "metadata": {
            "description": "True iff this package was resolved through a `[[override]]` entry in `vibe.toml`. PROP-044 §2b rules the boolean axis two-part with no third state, so an absent key means `false` — exactly how the hand-written twin already serialises it.",
            "x-default": false
          }
        }
      }
    }
  }
}
