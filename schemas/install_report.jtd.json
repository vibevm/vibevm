{
  "metadata": {
    "description": "Wire format for `vibe install --json` (the final apply report). Source of truth for `crates/vibe-wire/src/generated/install_report.rs`.",
    "spec": {
      "implements": "spec://org.vibevm.core/vibevm/common/PROP-000#JTD-SSOT"
    },
    "rustOptions": {
      "package": "vibe_wire::install_report"
    }
  },
  "properties": {
    "ok": {
      "type": "boolean"
    },
    "command": {
      "type": "string",
      "metadata": {
        "description": "Always `\"install\"` for this report."
      }
    },
    "project": {
      "type": "string"
    },
    "installed": {
      "elements": {
        "ref": "applied_report"
      },
      "metadata": {
        "x-empty": "emit",
        "description": "Required member: an apply that installed nothing writes `[]`, never an absent key — omitting a required collection would produce a document invalid by this same schema (rule R21)."
      }
    }
  },
  "definitions": {
    "applied_report": {
      "properties": {
        "package": {
          "type": "string",
          "metadata": {
            "description": "Package label in `<kind>:<name>@<version>` form."
          }
        },
        "files_written": {
          "type": "uint32"
        },
        "paths": {
          "elements": {
            "type": "string"
          },
          "metadata": {
            "description": "Forward-slash-normalised paths of every file the install wrote. Required member: an entry that wrote nothing still writes `paths` as `[]` — omitting a required collection would produce a document invalid by this same schema (rule R21).",
            "x-empty": "emit"
          }
        }
      }
    }
  }
}
