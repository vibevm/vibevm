{
  "metadata": {
    "description": "Wire format for `vibe registry publish --json`. Source of truth for `crates/vibe-wire/src/generated/registry_publish_report.rs`.",
    "spec": {
      "implements": "spec://org.vibevm.core/vibevm/common/PROP-000#JTD-SSOT"
    },
    "rustOptions": {
      "package": "vibe_wire::registry_publish_report"
    }
  },
  "properties": {
    "ok": {
      "type": "boolean"
    },
    "command": {
      "type": "string",
      "metadata": {
        "description": "Always `\"registry:publish\"` for this report."
      }
    },
    "host": {
      "type": "string",
      "metadata": {
        "description": "Human-readable host name from the `RepoCreator` adapter — e.g. `\"gitverse.ru\"`."
      }
    },
    "org_url": {
      "type": "string",
      "metadata": {
        "description": "Organization-root URL the package was published under, as recorded in `vibe.toml`'s `[[registry]].url`."
      }
    },
    "repo_name": {
      "type": "string",
      "metadata": {
        "description": "Repository name under the org, derived from the registry's `naming` convention. For default `kind-name` naming this is `<kind>-<name>`."
      }
    },
    "repo_url": {
      "type": "string",
      "metadata": {
        "description": "Clone URL of the per-package repository — what consumers' lockfile `source_url` would record."
      }
    },
    "tag": {
      "type": "string",
      "metadata": {
        "description": "The tag pushed for this release — typically `v<semver>`."
      }
    },
    "created_repo": {
      "type": "boolean",
      "metadata": {
        "description": "True iff this run created the repository (it didn't exist before). False iff the repo was reused — typical when re-publishing additional versions."
      }
    },
    "dry_run": {
      "type": "boolean",
      "metadata": {
        "description": "True iff `--dry-run` was passed. When true, no API calls or pushes were made; the report describes the planned action."
      }
    }
  }
}
