# Промт для следующей сессии

Скопируйте всё, что ниже разделителя, в первое сообщение новой сессии.

---

Продолжай работу в этом репозитории.

ЧИТАЙ СНАЧАЛА, В ЭТОМ ПОРЯДКЕ: `CONTINUE.md` → `spec/WAL.md` раздел
`#constraints` ЦЕЛИКОМ → **`spec/terraforms/OWNER-PROGRAMME-2026-08-06-CAMPAIGN-v0.1.md`
ЦЕЛИКОМ — это план, и он главный** → `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md`
ЦЕЛИКОМ → живые строки `BACKLOG.md` → открытые находки `AUDIT.md`.

КУРС ИЗМЕНИЛСЯ. Прежний был «осушаем бэклог» — он **отменён**. Разговор с
владельцем 2026-08-06 авторизовал восемнадцать работ. Порядок владельца:
**Б (гигиена) → В (таксономия) → А (индекс)**. Всё — в файле программы; здесь
не пересказывается.

ПЕРВОЕ ДЕЛО, ДО ЛЮБЫХ ЧИСЕЛ: пересканировать корпус —
`cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09`,
затем `mirror`. До скана все измерители отвечают про кэш, а не про дерево.

ПОДГОТОВКА: `python campaigns/packages-2026-09/tasks/{summary,judging-debt,text-stability,drift-registry}.py`
+ `bash tools/self-check.sh` фоном BARE-формой → «all green» ХВОСТОМ. Под
бегущей панелью НЕ запускать никаких vibe/cargo команд.

РАБОТА: группа Б целиком, и **внутри неё Б1 первым** (правило закрытия плана в
два дома) — остальное в Б есть его применение. Потом Б2 (перевод 22 цитат с
плановых строк на элементы спек + починка 8 уже висящих + надгробия 35 закрытым
строкам), Б3 (шаг панели на валидацию разметки — СНАЧАЛА измерить опасность
ловушки домашнего каталога), Б4 (две колонки в отчёте вердиктов + пересуд 90
якорей), Б5 (политика примеров в документации), Б6 (нормализация путей),
Б7 (записать три факта, не требующих решения).

ПЕРЕД КАЖДОЙ работой измеряй по АВТОРСКОМУ дереву, не построена ли она уже: за
три дня таких нашлось девятнадцать. И греп — измерение, которое врёт в обе
стороны.

ОДИН ДОЛГ БОССА ПЕРЕД ВЛАДЕЛЬЦЕМ: прийти с вариантами границы «что считать
доделанным» для подсистемы текстового интерфейса. Владелец решил её доделывать;
границы у неё нет, и без неё работа не имеет конца.

ЖДЁТ ВЛАДЕЛЬЦА, НЕ БЛОКИРУЕТ: имя флага для кэша организации (ось согласована,
слово нет); умеет ли клиент читать приватный индекс (НЕ ИЗМЕРЕНО — мерить первым
шагом пункта А2); что делать с утверждениями внутри код-блоков (названо, не
решено); гейт выхода фазы E; B-050; B-007; B-017/B-020/B-024; AUDIT -06/-07,
-11, -13, райдер 2026-06-12. **github недостижим — единственное, что требует его
рук.**

БОСС — судейство/дизайн/ревью/вердикты/коммиты; ИСПОЛНЕНИЕ — ТОЛЬКО
claudez-воркеры (`SUBAGENT-MODE.toml` перечитывать перед КАЖДЫМ fan-out);
boss-скелеты с именованными точками уточнения И ПРАВОМ ВОРКЕРА ОТКАЗАТЬСЯ,
ПРИЛОЖИВ ИЗМЕРЕННОЕ ЧИСЛО; ревью по WORKER-REPORT; ПОЛНЫЕ ПУТИ логов — в чат.
Отчёт архивировать СВОИМ шагом до уборки. `-c` слать ТОЛЬКО после конца
правимого прогона И ТОЛЬКО из каталога воркера — `cd` живёт ВНУТРИ скобок
`( cd .wt/<id> && claudez -c … )`, иначе коррекция уходит в корень репозитория с
правом писать в настоящее дерево. Отказ называет ПРИНЯТОЕ первым, потом ровно
один неверный пункт, потом дословный текст для затронутых разделов отчёта.

ПОСЛЕ применения работы воркера — `cargo fmt --all`; появился файл со `scope!`
или ребро `verifies` — ещё и `cargo xtask specmap` ТОЙ ЖЕ посадкой; **правка
ТЕКСТА любого документа под `spec/` тоже двигает карту** (оплачено красной
панелью трижды за сессию). ПОСЛЕ любой правки движка или крейта стека —
`cargo xtask sync-engines` отдельным шагом.

ЗАКРЫТИЕ РАБОТЫ = переезд содержимого В СПЕКУ, переезд ЕЁ ЦИТАТ на элементы
спек, **суд перевезённого тем же заходом**, и удаление хвостов — ОДНИМ коммитом.

ГРАНИЦЫ: фазу T не начинать; T/F/G вне добра; версии пакетов не бампать;
публикацию не трогать; вендор-копии руками не править; коммитнутую карту руками
не менять.

ПРАВИЛА: `spec/WAL.md` `#constraints` — полный список; суть по-человечески,
дерево для развилок, точные имена приложением, спеки не цитировать и номера
секций не приводить; **если ясность и точность конфликтуют — сперва ясно, сразу
за этим точно**; «замеров нет» — стоячий ответ; роллаут — ТОЛЬКО
`cargo xtask mirror`; exit-коды НАСТОЯЩИЕ и вид вывода НЕ усекать; длину файла
мерить ПОСЛЕ `cargo fmt`; коммиты через heredoc/`-F`; питон-скрипты писать
ФАЙЛОМ, `PYTHONIOENCODING=utf-8` для кириллицы; абсолютные пути в Bash; правки —
редакторскими инструментами; коммиты — человеческая атрибуция, Conventional
Commits, атомарность, явные пути стейджинга (`add -A` запрещён).

АВТОНОМИЯ: работай сам, останавливайся только на настоящем смысловом решении
владельца. НЕ ОСТАНАВЛИВАЙСЯ НА ГРАНИЦАХ РАБОТЫ. ТОКЕНЫ НЕ ЭКОНОМЬ.
