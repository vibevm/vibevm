# Промт для следующей сессии

Скопируйте всё, что ниже разделителя, в первое сообщение новой сессии.

_Переписан 2026-08-10. Предыдущий промт (чтение исследования эволюции схем +
доклад по пяти вопросам) **исполнен** 2026-08-09/10: исследование прочитано,
рамка владельцем перевёрнута («среда перманентного перелома»), созван
отдельный Fable-совет, написаны идеология и ТЗ, приняты четыре рулинга.
История — в `git log` и в `CONTINUE.md`._

---

Продолжаем работу в репозитории `C:\Users\olegc\git\v\vibevm`.

# СНАЧАЛА ЧИТАЙ. ПОТОМ ДОКЛАД. ПОТОМ ЖДИ.

Никаких правок, коммитов, воркеров до моего слова.

## Шаг 1. Загрузка проекта

По `CLAUDE.md`: статическая полоса целиком → манифест загрузки →
`spec/WAL.md` → `CONTINUE.md`. Директиву об усилиях (в пользовательском
загрузочном файле) принять всерьёз: объём работ не является доводом; решение,
чьё обоснование рассыпается при удалении слова «дёшево», — не решение.

## Шаг 2. Предмет — три документа ЦЕЛИКОМ, в этом порядке

1. **`spec/common/PROP-044-change-native-formats.md`** — идеология
   change-native форматов. Контракт. Там же четыре свежих рулинга владельца:
   допубликационный переключатель (`##THE-PUBLIC-SWITCH`), нейтральность к
   провайдеру хранения (`##PROVIDER-NEUTRALITY`), модель заморозки
   (`##THE-FREEZE-MODEL`), суженный закон неизменности байтов.
2. **`spec/design/change-native-formats-verdict.md`** — вердикт созванного
   совета: почему выбраны «одноразовые миры», почему отвергнуты три другие
   архитектуры, бюджет собственной неправоты. Лоре к контракту.
3. **`campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md`** — ТЗ:
   базовая линия B1–B15, решения D1–D14, фазы Ф0–Ф6, приложения А
   (формы кода) и Б (пофактные решения), раздел 11 (смертность плана).

Полное исследование (12 документов, ~12 400 строк) теперь ЖИВЁТ В РЕПОЗИТОРИИ:
`spec/research/schema-evolution-2026-08/` (порядок чтения — его
`12-HANDOFF.md §2`). Оно — справочная база, НЕ обязательное чтение: выжимка
уже в трёх документах выше. Открывать при конкретной надобности.

Долговечный якорь пяти вопросов владельца — `BACKLOG.md`, раздел
`#owner-decisions`.

## Шаг 3. Доклад

Коротко, человеческим языком, без цитирования номеров секций:

1. Пять вопросов владельца — строка на каждый, что изменилось.
2. Состояние предложения по вопросу 1: что ратификации ждёт, что уже
   рулинги (четыре), что осталось открытыми ручками.
3. Открытые ручки владельца поимённо (см. «Что дальше» ниже) — каждая одной
   фразой с рекомендацией.
4. Состояние репозитория: ветка, чистота, расхождение с зеркалами,
   судейский долг (`python campaigns/packages-2026-09/tasks/judging-debt.py`
   после `vibe progress scan --campaign campaigns/packages-2026-09`),
   когда панель была зелёной.
5. Что закрывается по времени.

Потом — стоп.

## Что дальше (кандидаты, НЕ авторизация)

Работа начинается только со слова владельца. Очередь, как она видится:

1. **Ратификация PROP-044** владельцем (или правки). Это гейт фазы 0 ТЗ.
2. **Открытые ручки** (все — СТОП-ВЛАДЕЛЕЦ в ТЗ):
   - политика резолвера по умолчанию для публичного потребления
     незамороженных версий (D14; рекомендация: снаружи frozen-only,
     каналы — явным объявлением; внутри `packages/` — наоборот);
   - уровень лога сервера по умолчанию (D11; рекомендация: warn всегда);
   - идентичность — четыре пункта разбора 2026-08-10, НЕ заведены в ТЗ,
     ждут рулинга: модель доверия группы (заявка, не удостоверение —
     записать); конвенция для авторов без доменов; три грамматических
     сужения (группа ≥2 сегментов; `_` в группе; запрет `+build` в
     публикуемых версиях); слот `vibedeps/<kind>-<name>` противоречит
     идентичности (нет группы, есть вид) — сменить на слот от идентичности
     до публики.
3. После ратификации — **фаза 0 ТЗ** (спайки, без коммитов): замер объёма
   переделки мутаций (M-RMW), PoC генератора открытых словарей, инвентарь
   форматов.
4. Вопросы владельца №4 (планка доказательства) и №5 (соединение движков) —
   не тронуты, его.

## Чего НЕ делать до слова

- не начинать фазы ТЗ; не «улучшать» PROP-044; не раскатывать зеркала без
  нужды; не трогать движки дисциплины и вендоренные копии; не бампать версии
  пакетов (B-067 растворён D14, но бампы — события релиза).
