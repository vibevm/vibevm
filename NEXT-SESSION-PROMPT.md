# Промт для следующей сессии

Скопируйте всё, что ниже разделителя, в первое сообщение новой сессии.

_Переписан 2026-08-13. Промт 2026-08-10 (три документа → полный доклад по
пяти вопросам → стоп) **исполнен** 2026-08-13: сессия восстановила контекст,
прочитала предмет целиком и выдала доклад; владелец его принял. Состояние с
тех пор не сдвинулось: PROP-044 не ратифицирован, ручки открыты, фазы не
начаты. История — в `git log` и в `CONTINUE.md`._

---

Продолжаем работу в репозитории `C:\Users\olegc\git\v\vibevm`.

# СНАЧАЛА ЧИТАЙ. ПОТОМ КОРОТКИЙ ДОКЛАД. ПОТОМ ЖДИ.

Никаких правок, коммитов, воркеров до моего слова.

## Шаг 1. Загрузка проекта

По `CLAUDE.md`: статическая полоса целиком → манифест загрузки →
`spec/WAL.md` → `CONTINUE.md`. Директиву об усилиях (в пользовательском
загрузочном файле) принять всерьёз: объём работ не является доводом; решение,
чьё обоснование рассыпается при удалении слова «дёшево», — не решение.

## Шаг 2. Предмет — три документа ЦЕЛИКОМ, в этом порядке

1. **`spec/common/PROP-044-change-native-formats.md`** — идеология
   change-native форматов. Контракт. Там же четыре рулинга владельца
   2026-08-10: допубликационный переключатель (`##THE-PUBLIC-SWITCH`),
   нейтральность к провайдеру хранения (`##PROVIDER-NEUTRALITY`), модель
   заморозки (`##THE-FREEZE-MODEL`), суженный закон неизменности байтов.
2. **`spec/design/change-native-formats-verdict.md`** — вердикт созванного
   совета: почему «одноразовые миры», почему отвергнуты три другие
   архитектуры, бюджет собственной неправоты. Лоре к контракту.
3. **`campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md`** — ТЗ:
   базовая линия B1–B15, решения D1–D14, фазы Ф0–Ф6, приложения А
   (формы кода) и Б (пофактные решения), раздел 11 (смертность плана).

Полное исследование (`spec/research/schema-evolution-2026-08/`, порядок —
его `12-HANDOFF.md §2`) — справочная база, НЕ обязательное чтение.
Долговечный якорь пяти вопросов владельца — `BACKLOG.md #owner-decisions`.

## Шаг 3. Доклад — сжатый

Полный пятипунктный доклад уже сделан и принят 2026-08-13 — не повторять
его. Доложить коротко, человеческим языком:

1. Подтверждение готовности: предмет прочитан, что именно ждёт моего слова.
2. Открытые ручки поимённо, по строке с рекомендацией (список — в «Что
   дальше» ниже).
3. Состояние репозитория, каждое число из команд, не из памяти:
   `vibe progress scan --campaign campaigns/packages-2026-09` →
   `python campaigns/packages-2026-09/tasks/judging-debt.py` (долг),
   `cargo xtask mirror --check` (зеркала), ветка и чистота дерева,
   когда панель была зелёной (по WAL).

Потом — стоп.

## Что дальше (кандидаты, НЕ авторизация)

Работа начинается только со слова владельца. Очередь, как она видится:

1. **Ратификация PROP-044** владельцем (или правки). Это гейт фазы 0 ТЗ.
2. **Открытые ручки** (все — СТОП-ВЛАДЕЛЕЦ в ТЗ):
   - политика резолвера по умолчанию для публичного потребления
     незамороженных версий (D14; рекомендация: снаружи frozen-only,
     каналы — явным объявлением; внутри `packages/` — наоборот);
   - уровень лога сервера по умолчанию (D11; рекомендация: warn всегда);
   - идентичность — четыре пункта разбора 2026-08-10, НЕ заведены в ТЗ,
     ждут рулинга: модель доверия группы (заявка, не удостоверение —
     записать); конвенция для авторов без доменов (`io.github.<user>`-класс);
     три грамматических сужения (группа ≥2 сегментов; `_` в группе; запрет
     `+build` в публикуемых версиях); слот `vibedeps/<kind>-<name>`
     противоречит идентичности (нет группы, есть вид) — сменить на слот от
     идентичности до публики.
3. После ратификации — **фаза 0 ТЗ** (спайки, без коммитов): Ф0.1 замер
   объёма переделки мутаций (M-RMW), Ф0.2 PoC генератора открытых словарей,
   Ф0.3 инвентарь форматов. Выход — три находки в
   `campaigns/packages-2026-09/harvest/`.
4. Вопросы владельца №4 (планка доказательства) и №5 (соединение движков) —
   не тронуты, его.

## Чего НЕ делать до слова

- не начинать фазы ТЗ; не «улучшать» PROP-044; не раскатывать зеркала без
  нужды; не трогать движки дисциплины и вендоренные копии; не бампать версии
  пакетов (B-067 растворён D14, но бампы — события релиза).
