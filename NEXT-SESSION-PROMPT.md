# Промт для следующей сессии

Скопируйте всё, что ниже разделителя, в первое сообщение новой сессии.

_Написан 2026-08-09 по прямому указанию владельца. Предыдущий промт (от
2026-08-06, программа А5b → аудит TUI → хвост P1 → дешёвые строки) **исполнен**;
его содержимое в `git log` и в `spec/WAL.md`._

---

Продолжаем работу в репозитории `C:\Users\olegc\git\v\vibevm`.

# СНАЧАЛА ПРОЧИТАЙ, ПОТОМ МОЛЧИ И ЖДИ

Эта сессия начинается **не с работы**. Она начинается с чтения, потом с
короткого доклада, потом ты **останавливаешься и ждёшь моих инструкций**.

Никаких правок кода, никаких коммитов, никакого запуска воркеров, никаких
предложений «давай я начну с…». Я прочитаю доклад и скажу, что делать.

## Что прочитать

**1. Загрузочная последовательность проекта**, как предписано в `CLAUDE.md`:
статическая полоса целиком, затем всё, что перечисляет манифест загрузки, затем
`spec/WAL.md`, затем `CONTINUE.md`.

**2. Хэндофф исследования — ЦЕЛИКОМ:**

```
C:\Users\olegc\git\v\discovery\vibevm-schema-evolution-discovery\12-HANDOFF.md
```

Этот каталог **вне репозитория**, загрузочная последовательность его не
находит. В хэндоффе есть раздел 2 — полный список того, что читать дальше, в
указанном порядке. **Прочитай всё, что он перечисляет как обязательное.**
Двенадцать документов, около 12 400 строк. Не выборочно и не по диагонали.

Материал стоил трёх кругов исследования — четырёх веб-исследователей, пяти
GLM-воркеров и финального ревью отдельной моделью. Его прочтение стоит меньше,
чем его повторение.

**3. Директиву об усилиях.** Она записана в пользовательском загрузочном файле
проекта и прочтётся сама, но отнесись всерьёз: **объём работ не является
доводом**. Ни «дёшево», ни «одна строка», ни «сокращает объём» не могут быть
аргументом за решение. Мы три месяца делаем то, что «просто» делается за вечер,
и это правильно.

## Что доложить, прочитав

Коротко и человеческим языком, без жаргона, без цитирования номеров разделов
спецификаций:

1. **Пять моих вопросов** — все пять одной строкой каждый, с пометкой: какой
   разобран, какой всплыл заново, какие не тронуты.
2. **Где стои́т вопрос 1** — что установлено твёрдо, что оспорено, что осталось
   нерешённым.
3. **Пять мест, где прежний разбор экономил усилия**, и что в каждом меняется
   под директивой.
4. **Три вещи, которых не предложили вовсе**, а надо.
5. **Состояние репозитория** — ветка, чистота, нераскатанные коммиты,
   неубранные рабочие копии в `.wt/`.
6. **Что закрывается по времени** — если есть решение, окно которого закроется
   само собой, назвать отдельно.

Потом — **стоп**. Жди инструкций.

## Что мы будем делать

**Решать вопрос 1** — формат каталога пакетов. До дизайна в прошлый раз не
дошли: есть исследование, разбор и вердикт ревьюера, решения нет.

**Остальные четыре вопроса не забывать.** Они перечислены в хэндоффе с текущим
состоянием каждого. Один из них — недетерминированная запись каталога — по ходу
работы оказался частью вопроса 1; учти это, когда дойдёт до дизайна.

## Чего НЕ делать до моего слова

- не начинать проектирование;
- не запускать воркеров и исследования;
- не трогать код;
- не предлагать «быстрый вариант» ни в каком виде.
