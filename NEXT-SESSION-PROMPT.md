# Промт для следующей сессии

Скопируйте всё, что ниже разделителя, в первое сообщение новой сессии.

---

Продолжай работу в этом репозитории.

ЧИТАЙ СНАЧАЛА, В ЭТОМ ПОРЯДКЕ: `CONTINUE.md` → `spec/WAL.md` раздел
`#constraints` ЦЕЛИКОМ → **`spec/terraforms/OWNER-PROGRAMME-2026-08-06-CAMPAIGN-v0.1.md`
ЦЕЛИКОМ — это план, и он главный** → `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md`
ЦЕЛИКОМ → живые строки `BACKLOG.md` → открытые находки `AUDIT.md`.

ГДЕ МЫ. Программа выполнена наполовину. Группа Б закрыта ЦЕЛИКОМ. Из В
закрыты В1 (пакет omnichannel), В2 (вид пакета `lang`), В3 (`vibe tools`);
В4 ждёт А5 по построению. **Группа А не начата — с неё и начинай.** Ничего
не блокирует, рук владельца не ждёт ничего.

ПЕРВОЕ ДЕЛО, ДО ЛЮБЫХ ЧИСЕЛ: пересканировать корпус —
`cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09`,
затем `progress mirror`. До скана все измерители отвечают про кэш, а не про
дерево.

ПОДГОТОВКА: `python campaigns/packages-2026-09/tasks/{summary,judging-debt,text-stability,drift-registry}.py`
+ `bash tools/self-check.sh` фоном BARE-формой → «all green» ХВОСТОМ. Под
бегущей панелью НЕ запускать никаких vibe/cargo команд.

РАБОТА: группа А по порядку программы. А1 автопубликация индекса (владелец
назвал блокером; флаг объявлен и выбрасывается одной строкой). А2 приватный
индекс — **первым шагом ИЗМЕРИТЬ, умеет ли клиент авторизоваться вообще**:
от ответа зависит, это «добавить пуш» или «добавить пуш и аутентифицированное
чтение». А3 кэш организации: имя флага решено — `--cache-org`, включён по
умолчанию, и дешёвая проверка свежести — не улучшение, а условие честности
этого умолчания. А4 вебхуки, гайд ЖИВЁТ В СПЕКАХ. А5 оба уровня поиска —
простые фильтры это ПОСТОЯННЫЙ уровень, не черновик языка запросов. А6
генерировать типы индекса, мягкость становится осознанным выбором.

ПЕРЕД КАЖДОЙ работой измеряй по АВТОРСКОМУ дереву, не построена ли она уже.
И НАЗЫВАЙ ПЕРИМЕТР ВМЕСТЕ С ЧИСЛОМ — прошлая сессия ошиблась периметром
шесть раз, и ни разу из-за плохого образца.

ПОТОМ, В ЛЮБОМ ПОРЯДКЕ: В4 (после А5); аудит тонкости TUI — граница принята
владельцем, доделан = ТОНКАЯ ПОВЕРХНОСТЬ, проверка удалением на бумаге, 63
файла и 18 426 строк; хвост Б4 — 88 якорей на общем доказательстве
(конвертируются сами, ложных среди них нет); судейский долг ~142 факта в 12
файлах, почти все написаны прошлой сессией — гасить по файлу, начиная с
самого дешёвого, НИКОГДА не судить вслепую ради числа.

ЧТО ЗНАТЬ ПРО РАЗМЕТКУ. Она мигрировала: `@fact:ИМЯ` и
`@status:стадия/состояние`. Старое написание читается по-прежнему.
Канонической для хеширования осталась СТАРАЯ форма — именно это позволило
27 407 замен не потревожить ни одного вердикта. Забор — пример, пока не
помечен `@fact/code:`; тогда он становится телом факта. Неизвестный тип
объекта — ОШИБКА, а не пожатие плечами.

ИНСТРУМЕНТЫ, КОТОРЫЕ ОТКАЗЫВАЮТ, ЦЕННЕЕ ПОСЛУШНЫХ. Прошлая сессия шесть раз
ошиблась и почти каждый раз её ловил не человек: панель нашла три схемы,
конформ потребовал оракул поведения, тест шаблона навыка не дал отгрузить
инструмент, которому никто не научит агента, `merge-verdicts` отказался
перезаписывать вердикт без `--force`, `seal` отказался печатать файл с
несуждёнными фактами. Не обходи отказ — читай его.

ЖДЁТ ВЛАДЕЛЬЦА, НЕ БЛОКИРУЕТ: гейт выхода фазы E (шесть файлов в `work`);
B-050; B-007; B-017/B-020/B-024; AUDIT -04, -06/-07, -11, -13, -14 и райдер
2026-06-12; B-067 (версии 38 пакетов — законно, пока потребление локальное,
блокирует в момент публикации).

БОСС — судейство/дизайн/ревью/вердикты/коммиты; ИСПОЛНЕНИЕ — ТОЛЬКО
claudez-воркеры (`SUBAGENT-MODE.toml` перечитывать перед КАЖДЫМ fan-out);
boss-скелеты с именованными точками уточнения И ПРАВОМ ВОРКЕРА ОТКАЗАТЬСЯ,
ПРИЛОЖИВ ИЗМЕРЕННОЕ ЧИСЛО; ревью по WORKER-REPORT; ПОЛНЫЕ ПУТИ логов — в чат.
`meta.md` писать СВОИМ шагом сразу после прогона, не при уборке — шесть
отчётов пролежали без вердикта босса именно поэтому. `-c` слать ТОЛЬКО после
конца правимого прогона И ТОЛЬКО из каталога воркера — `cd` живёт ВНУТРИ
скобок `( cd .wt/<id> && claudez -c … )`.

ПОСЛЕ применения работы воркера — `cargo fmt --all`; появился файл со `scope!`
или ребро `verifies` — ещё и `cargo xtask specmap` ТОЙ ЖЕ посадкой; **правка
ТЕКСТА любого документа под `spec/` тоже двигает карту**. ПОСЛЕ любой правки
движка или крейта стека — `cargo xtask sync-engines` отдельным шагом.
Вендор-копии НЕ править руками — перегенерировать.

ЗАКРЫТИЕ РАБОТЫ = переезд содержимого В СПЕКУ, переезд ЕЁ ЦИТАТ на элементы
спек, **суд перевезённого тем же заходом**, и удаление хвостов — ОДНИМ
коммитом. Правило записано в двух домах и работает: печать отказывает файлу,
чьи новые факты не осуждены.

ГРАНИЦЫ: фазу T не начинать; T/F/G вне добра; версии пакетов не бампать;
публикацию не трогать; коммитнутую карту руками не менять.

ПРАВИЛА: `spec/WAL.md` `#constraints` — полный список; суть по-человечески,
дерево для развилок, точные имена приложением, спеки не цитировать и номера
секций не приводить; **если ясность и точность конфликтуют — сперва ясно,
сразу за этим точно**; «замеров нет» — стоячий ответ; роллаут — ТОЛЬКО
`cargo xtask mirror`; exit-коды НАСТОЯЩИЕ и вид вывода НЕ усекать; длину
файла мерить ПОСЛЕ `cargo fmt`, и бюджет длины НИКОГДА не выбирает форму
публичного типа; коммиты через heredoc/`-F`; питон-скрипты писать ФАЙЛОМ,
`PYTHONIOENCODING=utf-8` для кириллицы; абсолютные пути в Bash; правки —
редакторскими инструментами; коммиты — человеческая атрибуция, Conventional
Commits, атомарность, явные пути стейджинга (`add -A` запрещён).

АВТОНОМИЯ: работай сам, останавливайся только на настоящем смысловом решении
владельца. НЕ ОСТАНАВЛИВАЙСЯ НА ГРАНИЦАХ РАБОТЫ. ТОКЕНЫ НЕ ЭКОНОМЬ.
