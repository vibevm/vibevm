# Промт для следующей сессии — вставить первым сообщением

_Переписан 2026-08-14, HEAD `d480f6c7`. Ф0 закрыта, Ф1.1 села; вход — Ф1.2._

---

Продолжаем работу в репозитории C:\Users\olegc\git\v\vibevm.

СНАЧАЛА ЧИТАЙ. ПОТОМ КОРОТКИЙ ДОКЛАД. ПОТОМ — СРАЗУ ИСПОЛНЯЙ: этот промт и
есть слово владельца, отдельного «да» не жди.

## Шаг 1. Загрузка проекта

По CLAUDE.md: статическая полоса целиком → манифест загрузки → spec/WAL.md →
CONTINUE.md. Директиву об усилиях (пользовательский загрузочный файл) принять
всерьёз: объём работ не является доводом; решение, чьё обоснование
рассыпается при удалении слова «дёшево», — не решение.

## Шаг 2. Предмет

**Обязательно целиком, в этом порядке:**

1. `spec/common/PROP-044-change-native-formats.md` — контракт, РАТИФИЦИРОВАН
   владельцем 2026-08-13. Вся идеология форматов; законы связывают каждый шаг.
2. `campaigns/packages-2026-09/TZ-CHANGE-NATIVE-FORMATS-v0.1.md` — главная
   стройка. Ф0 исполнена, Ф1.1 села; **твой вход — Ф1.2**. Секция Ф1.1 несёт
   четыре решения «РЕШЕНО», принятые прошлой сессией, — не переоткрывай.
3. `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` — транспортный закон
   воркеров, БОСС читает ЦЕЛИКОМ до первого фан-аута. **§9 — разбор чистого
   фан-аута** (4/4 без доработок): что именно сделало его чистым.

**Обязательно, но можно по разделам — три находки Ф0.** Это не архив, а
текущая правда о дереве; они дороже, чем кажется, потому что каждая
переоткрывается только новым спайком:

- `campaigns/packages-2026-09/harvest/f0-rmw-volume.md` — анатомия шести
  RMW-путей, вердикт бюджета Ф3 (ДА), что усыхает и что нет.
- `campaigns/packages-2026-09/harvest/f0-format-inventory.md` — 21 формат с
  evidence; §4 — черновик реестра (уже воплощён в Ф1.1).
- `campaigns/packages-2026-09/harvest/f0-gen-poc.md` — §7 читать обязательно:
  форма `vibe-wire-gen` для Ф4.2 задана этой находкой.

**По надобности:** `TZ-IDENTITY-REGISTRY-BUILDS-v0.1.md` (независимая полоса
S1/S3/S6/S7), `TZ-CHANGE-NATIVE-WAVE2-v0.1.md` (W1–W4, заперты до Ф1–Ф5),
`BACKLOG.md` #owner-decisions (вопросы владельца — не трогать).

## Шаг 3. Доклад — короткий, числа из команд

```
cargo run -q -p vibe-cli --bin vibe -- progress scan --campaign campaigns/packages-2026-09
python campaigns/packages-2026-09/tasks/judging-debt.py
cargo xtask mirror --check
git status --short
```

Подтверди, что предмет прочитан, и назови шаг, которым начинаешь. Без паузы
после доклада.

## Шаг 4. Исполнение

**Главная полоса: Ф1.2 → Ф1.3 → Ф1.4 → Ф1.5 → Ф2 → Ф3 → Ф4 → Ф5 → Ф6.**
Порядок ВНУТРИ фазы жёсткий, каждый шаг — отдельный коммит. Граница ФАЗЫ (не
шага) = полная панель `bash tools/self-check.sh` (реальный код выхода, вердикт
из хвоста) + правка карты `cargo xtask specmap` в той же посадке + раскатка
`cargo xtask mirror`. Любая граница — безопасная остановка.

**Ф1.3 несёт две ловушки, уже найденные, — не переоткрывай их спайком, а
построй по ним** (полностью в WAL `##WAL-KI-HASH-RECIPE-TRAPS`):

1. Реализаций хэша ровно две: `vibe-index/src/content_hash.rs:40` и
   **`vibe-registry/src/shippable.rs:77`** (план называет её иначе — план
   неправ). Третий участник, которого план не назвал вовсе:
   `vibe-core/src/content_hash.rs` — newtype `ContentHash` с
   `PREFIX = "sha256:"` и проверкой НА ГРАНИЦЕ ДЕСЕРИАЛИЗАЦИИ
   (`try_from = "String"`). Как только индекс начнёт эмитить `sha256-tree/1:`,
   всякий путь через этот тип упадёт. `parse` обязан выучить ОБЕ формы; локфайл
   продолжает ПИСАТЬ рецепт 0 — иначе срабатывает СТОП-ВЛАДЕЛЕЦ.
2. Golden-фикстура `golden-flow-wal-0.1.0` НЕ ловит расхождение порядка, ради
   которого нужна: сортировка идёт по `PathBuf` ДО нормализации `\`→`/`, а
   разница проявляется только когда сосед каталога несёт символ между 0x2F и
   0x5C (каталог `spec/` рядом с файлом `specX.md`). Такой пары в фикстуре нет
   — добавь, иначе рецепт 1 замораживается, ничего не доказывая.

**Ф2.1** упрётся в фикстурный конструктор `VersionEntry::minimal`
(`types/entry/mod.rs:167`) — реши в пакете, не оставляй воркеру. Пять
clock-сайтов перечислены в WAL `##WAL-KI-F21-CLOCK-SITES`.

**Независимая полоса** (когда главная упирается или сессия предпочитает начать
с малого): S1 → S3 → S6 identity-ТЗ; S7 — судейская, в любой момент. S2 — НЕ
трогать (нужен владелец с org-правами).

**Делегирование — по умолчанию.** `SUBAGENT-MODE.toml` = `claudez`
(подтверждено владельцем 2026-08-14). Пакеты режет босс; воркеры не запускают
git; диффы читает, правит и коммитит босс; ревью не делегируется никогда.
Что доказано работающим: периметр записи как ЗАКРЫТЫЙ СПИСОК файлов (читать
можно пересекаясь — конфликтуют только записи); заголовки результата вшиты в
пакет дословно; на каждое утверждение базовой линии — confirm-or-refute с
цитатой; при cargo-тяжёлом пакете — своя линия.

**Слайс, трогающий `crates/vibe-wire/src/generated/**`, садится в порядке
стейдж → панель → коммит:** `check-codegen` это `git diff --exit-code`, то есть
рабочее дерево против ИНДЕКСА, и untracked-файлов он не видит.

**Shell** (оплачено ошибками, не повторяй): никогда голый `cd` — только
`( cd … && … )` / `git -C` / абсолютный путь, иначе следующие команды молча
читают чужое дерево; `cmd | tail; echo $?` печатает код ПАЙПА — редирект в файл
и `$?` сразу; правки только editor-инструментами; python — в файл с
`PYTHONIOENCODING=utf-8`.

Остановки — только на помеченных СТОП-ВЛАДЕЛЕЦ, по одной за раз; в остальном
— доклады статусов, не вопросы. Живые СТОПы: смена вида `content_hash` в
локфайле (Ф1.3 — обходится по конструкции плана) и S2 (креды org).

## Чего НЕ делать

Не начинать волну 3 (триггеры — в W-ТЗ §0: первый поезд эпох, `public = true`);
не трогать движки дисциплины и вендоренные копии; не публиковать наружу и не
бампать версии пакетов; не вводить flatten-захват и не писать мигратор каталога
(запреты §10 первого ТЗ); слово snapshot употреблять только в смысле PROP-044
§2b — в смысле режима материализации оно вычищено из дерева, не возвращай его.
