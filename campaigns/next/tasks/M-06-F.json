{
  "id": "M-06-F",
  "tasks": [
    {
      "id": "M-06-F.1",
      "title": "Integrate derivatives and full debugger surface parity",
      "goal": "Close source-to-local-derivative-to-boot provenance across all interfaces.",
      "read_paths": [
        "crates/vibe-workspace/src/vibedeps/derived.rs",
        "crates/vibe-spec/src/compiler/ir.rs",
        "crates/vibe-cli/src/commands/tree/model.rs",
        "crates/vibe-mcp/src/tools.rs",
        "crates/vibe-cli/src/cli.rs",
        "crates/vibe-cli/src/main.rs",
        "crates/vibe-cli/src/commands/mod.rs",
        "crates/vibe-cli/Cargo.toml",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs",
        "crates/vibe-cli/src/commands/tree/mod.rs",
        "crates/vibe-mcp/Cargo.toml",
        "crates/vibe-mcp/src/lib.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/common/PROP-072-boot-observability.xml",
        "crates/vibe-workspace/src/boot_debugger/",
        "crates/vibe-cli/src/commands/tree/",
        "crates/vibe-cli/src/commands/boot/",
        "crates/vibe-mcp/src/tools/boot_debugger.rs",
        "crates/vibe-mcp/src/tools.rs",
        "crates/vibe-cli/src/cli.rs",
        "crates/vibe-cli/src/main.rs",
        "crates/vibe-cli/src/commands/mod.rs",
        "crates/vibe-cli/Cargo.toml",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs",
        "crates/vibe-cli/src/commands/tree/mod.rs",
        "crates/vibe-mcp/Cargo.toml",
        "crates/vibe-mcp/src/lib.rs"
      ],
      "steps": [
        "Consume accepted M-10-D derivative bindings and preserve base/local/result identity in current and hypothetical graphs.",
        "Run inspect/source/why/cost/compare across viewer, library, CLI, MCP and canonical API.",
        "Test retained captured schemas/data after provider removal and explicit unmappable native transforms."
      ],
      "positive_cases": [
        "A derivative contribution can be traced to its exact upstream and local sources."
      ],
      "negative_cases": [
        "Equal bytes cannot merge distinct ownership/provenance; missing mapping stays unknown.",
        "Revoked provider cannot execute during retained inspection."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-workspace target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-workspace <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-cli target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-cli <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-mcp target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-mcp <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "Equivalent requests agree on structured nodes/metrics/diagnostics across surfaces.",
        "Whole authorized structure is reachable without human-only hidden fields."
      ],
      "safe_stop": "Leave failed parity/unknown mapping explicit; no weaker substitute for a supported interface.",
      "commit_subject": "test(boot): close debugger provenance and surface parity",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/common/PROP-072-boot-observability.xml (PROPOSED file until its source-contract task creates it). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_06_f_1_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    },
    {
      "id": "M-06-F.2",
      "title": "Prove scale, snapshot integrity and unchanged boot law",
      "goal": "Validate the complete debugger under large captures and concurrent changes.",
      "read_paths": [
        "crates/vibe-workspace/src/compile_trace.rs",
        "crates/vibe-spec/src/compiler/trace.rs",
        "crates/vibe-cli/src/commands/tree/build.rs",
        "vibevm/vibespecs/common/PROP-048-tokenomics.xml",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs",
        "crates/vibe-mcp/Cargo.toml",
        "crates/vibe-mcp/src/lib.rs",
        "crates/vibe-spec/Cargo.toml",
        "crates/vibe-spec/src/lib.rs",
        "crates/vibe-spec/src/compiler/mod.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/common/PROP-072-boot-observability.xml",
        "crates/vibe-workspace/src/boot_debugger/",
        "crates/vibe-cli/tests/",
        "crates/vibe-mcp/src/",
        "crates/vibe-spec/src/compiler/",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs",
        "crates/vibe-mcp/Cargo.toml",
        "crates/vibe-mcp/src/lib.rs",
        "crates/vibe-spec/Cargo.toml",
        "crates/vibe-spec/src/lib.rs",
        "crates/vibe-spec/src/compiler/mod.rs"
      ],
      "steps": [
        "Create a million-token-scale synthetic boot fixture with repeated/conditional/ordinary resources and streamed results.",
        "Exercise cancellation, stale pages, trace retention/eviction and changed source/catalog during inspection and preview.",
        "Compare unchanged-declaration canonical output before/after; no compaction, status stripping, externalization or boot-budget gate is permitted."
      ],
      "positive_cases": [
        "All authorized structure remains traversable with bounded transport buffering.",
        "Same capture gives reproducible byte/scalar/tokenizer measurements."
      ],
      "negative_cases": [
        "Mixed snapshots, silent eviction/capture omission and high-size admission refusal fail.",
        "Observer failure cannot alter compiler output or claim successful complete capture."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-workspace target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-workspace <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-cli target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-cli <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-mcp target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-mcp <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-spec target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-spec <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "Scale evidence separates result size from transport chunk limits and records memory/cancellation outcomes.",
        "M-06 exit includes interactive UI, structured input/output and dependency-aware preview."
      ],
      "safe_stop": "Preserve canonical bytes and captured baseline; mark incomplete observability honestly.",
      "commit_subject": "test(boot): prove scalable snapshot inspection without quotas",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/common/PROP-072-boot-observability.xml (PROPOSED file until its source-contract task creates it). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_06_f_2_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    }
  ]
}
