{
  "id": "M-01-A",
  "tasks": [
    {
      "id": "M-01-A.1",
      "title": "Freeze address grammar and compatibility corpus",
      "goal": "Make accepted spellings and ambiguous lookup behavior executable before public reads.",
      "read_paths": [
        "crates/vibe-spec/src/address.rs",
        "crates/vibe-spec/src/resolver/lookup.rs",
        "crates/vibe-spec/src/doctree.rs",
        "crates/vibe-spec/Cargo.toml",
        "crates/vibe-spec/src/lib.rs",
        "crates/vibe-spec/src/resolver.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/common/PROP-057-resource-resolution.xml",
        "crates/vibe-spec/src/address.rs",
        "crates/vibe-spec/src/address/",
        "crates/vibe-spec/src/resolver/",
        "crates/vibe-spec/Cargo.toml",
        "crates/vibe-spec/src/lib.rs",
        "crates/vibe-spec/src/resolver.rs"
      ],
      "steps": [
        "Inventory current URI/stem/anchor spellings and capture examples before editing.",
        "Define permanent exact scheme, authority, version, Vibe path, resource-export and unsupported-revision rules.",
        "Implement validated construction and explicit legacy conversion; add accepted/refused corpus entries."
      ],
      "positive_cases": [
        "Qualified selected dependency and unique abbreviated stem resolve canonically."
      ],
      "negative_cases": [
        "Percent encoding, empty segments, wrong version and duplicate stems refuse with distinct reasons."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-spec target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-spec <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "Corpus maps every supported/legacy spelling to a stable result or typed refusal.",
        "No guessed first-match or unsafe compatibility fallback remains."
      ],
      "safe_stop": "Parser and characterization may land before the public query.",
      "commit_subject": "feat(spec): define validated resource address grammar",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/common/PROP-057-resource-resolution.xml (PROPOSED file until its source-contract task creates it). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_01_a_1_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    },
    {
      "id": "M-01-A.2",
      "title": "Unify portable component containment",
      "goal": "Make address lookup use the existing filesystem identity boundary.",
      "read_paths": [
        "crates/vibe-spec/src/address.rs",
        "crates/vibe-spec/src/resolver/lookup.rs",
        "crates/vibe-safefs/src/component.rs",
        "crates/vibe-spec/Cargo.toml",
        "crates/vibe-spec/src/lib.rs",
        "crates/vibe-spec/src/resolver.rs",
        "crates/vibe-safefs/Cargo.toml",
        "crates/vibe-safefs/src/lib.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/common/PROP-057-resource-resolution.xml",
        "crates/vibe-spec/src/resolver/",
        "crates/vibe-safefs/src/component.rs",
        "crates/vibe-safefs/src/component/",
        "crates/vibe-spec/Cargo.toml",
        "crates/vibe-spec/src/lib.rs",
        "crates/vibe-spec/src/resolver.rs",
        "crates/vibe-safefs/Cargo.toml",
        "crates/vibe-safefs/src/lib.rs"
      ],
      "steps": [
        "Compare URI validation with shared safefs portable/NFC/case rules.",
        "Route physical lookup through shared validated components without normalizing distinct logical names.",
        "Add native and portable cases for Windows special paths and Unicode aliases."
      ],
      "positive_cases": [
        "Nested portable path stays under its admitted root."
      ],
      "negative_cases": [
        "Drive/UNC/ADS/device roots, trailing dots, control bytes and NFC/case aliases cannot escape or silently alias."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-spec target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-spec <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-safefs target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-safefs <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "Rejected components cause no out-of-root observation.",
        "One shared component authority replaces local blacklists."
      ],
      "safe_stop": "Unsupported native observation remains a typed refusal.",
      "commit_subject": "fix(spec): enforce portable source containment",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/common/PROP-057-resource-resolution.xml (PROPOSED file until its source-contract task creates it). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_01_a_2_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    },
    {
      "id": "M-01-A.3",
      "title": "Preserve failures across compiler and prompt callers",
      "goal": "Finish shared lookup semantics at existing integration points.",
      "read_paths": [
        "crates/vibe-spec/src/resolver.rs",
        "crates/vibe-spec/src/embed.rs",
        "crates/vibe-lifecycle/src/agent/resolver.rs",
        "crates/vibe-spec/Cargo.toml",
        "crates/vibe-spec/src/lib.rs",
        "crates/vibe-lifecycle/Cargo.toml",
        "crates/vibe-lifecycle/src/lib.rs",
        "crates/vibe-lifecycle/src/agent/mod.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/common/PROP-057-resource-resolution.xml",
        "crates/vibe-spec/src/resolver.rs",
        "crates/vibe-spec/src/resolver/",
        "crates/vibe-spec/src/embed.rs",
        "crates/vibe-lifecycle/src/agent/resolver.rs",
        "crates/vibe-spec/Cargo.toml",
        "crates/vibe-spec/src/lib.rs",
        "crates/vibe-lifecycle/Cargo.toml",
        "crates/vibe-lifecycle/src/lib.rs",
        "crates/vibe-lifecycle/src/agent/mod.rs"
      ],
      "steps": [
        "Trace the compiler/embed/prompt call sites cited by the baseline.",
        "Replace first-match and guessed not-found handling with typed lookup results.",
        "Characterize unchanged valid content and distinguish absent, unreadable, ambiguous and unstable outcomes per caller."
      ],
      "positive_cases": [
        "Existing valid embed and provider prompt retain their content."
      ],
      "negative_cases": [
        "Unreadable source never becomes not-found; duplicate anchor never succeeds."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-spec target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-spec <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-lifecycle target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-lifecycle <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "Every inventoried caller has a result/refusal fixture.",
        "No alternate lookup path bypasses containment."
      ],
      "safe_stop": "Internal callers complete; public exposure waits M-01-D.",
      "commit_subject": "fix(spec): propagate exact lookup failure causes",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/common/PROP-057-resource-resolution.xml (PROPOSED file until its source-contract task creates it). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_01_a_3_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    }
  ]
}
