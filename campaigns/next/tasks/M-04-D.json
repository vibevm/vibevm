{
  "id": "M-04-D",
  "tasks": [
    {
      "id": "M-04-D.1",
      "title": "Persist durable closure with retained evidence",
      "goal": "Record an explicit disposition whose evidence survives disposable cache loss.",
      "read_paths": [
        "crates/vibe-requirements/src/query.rs",
        "crates/progress-core/src/evidence.rs",
        "crates/vibe-lifecycle/src/state/store.rs",
        "crates/vibe-lifecycle/src/mechanism/record.rs",
        "xtask/src/main.rs",
        ".cargo/config.toml",
        "crates/vibe-requirements/Cargo.toml",
        "crates/vibe-requirements/src/lib.rs",
        "crates/vibe-orchestrator/Cargo.toml",
        "crates/vibe-orchestrator/src/lib.rs",
        "crates/vibe-wire/Cargo.toml",
        "crates/vibe-wire/src/lib.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/common/PROP-058-change-bindings-and-closure.xml",
        "crates/vibe-requirements/src/change/",
        "crates/vibe-orchestrator/src/change/",
        "schemas/",
        "formats/",
        "crates/vibe-wire/src/",
        "crates/vibe-requirements/Cargo.toml",
        "crates/vibe-requirements/src/lib.rs",
        "crates/vibe-orchestrator/Cargo.toml",
        "crates/vibe-orchestrator/src/lib.rs",
        "crates/vibe-wire/Cargo.toml",
        "crates/vibe-wire/src/lib.rs"
      ],
      "steps": [
        "Define accepted/abandoned/superseded closure with exact intent/world/policy, authority and unresolved dispositions.",
        "Retain evidence values or independently verifiable immutable references outside disposable lifecycle caches.",
        "Apply closure through saved-state preconditions; exclude closure bytes from its own intent subject."
      ],
      "positive_cases": [
        "An explicit accepted closure retains enough evidence to inspect after cache deletion."
      ],
      "negative_cases": [
        "Cache-only IDs, truncated fact universe or stale subject cannot close.",
        "Writing closure cannot mutate its own subject hash."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-requirements target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-requirements <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-orchestrator target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-orchestrator <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-wire target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-wire <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check",
        "CONDITIONAL ORACLE: cargo xtask check-codegen only when the actual diff affects schemas, generators, generated-output registration or a dependent contract. Bind a narrower existing oracle if available; the global command needs that concrete impact and no adequate narrower check.",
        "CONDITIONAL ORACLE: cargo xtask wire-diff only when the actual diff affects wire/format behavior, its corpus or epoch semantics. Bind a narrower existing oracle if available; the global command needs that concrete impact and no adequate narrower check."
      ],
      "acceptance": [
        "Retained closure resolves each required proof independently of run cache.",
        "Closure write is an explicit action with exact authored changes."
      ],
      "safe_stop": "Prepared closure is inspectable before committed disposition.",
      "commit_subject": "feat(change): retain durable closure evidence",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/common/PROP-058-change-bindings-and-closure.xml (PROPOSED file until its source-contract task creates it). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_04_d_1_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    },
    {
      "id": "M-04-D.2",
      "title": "Recompute closure applicability without erasing history",
      "goal": "Distinguish past acceptance from present readiness after drift.",
      "read_paths": [
        "crates/vibe-requirements/src/query.rs",
        "crates/progress-core/src/terminal.rs",
        "crates/vibe-lifecycle/src/mechanism/record.rs",
        "crates/vibe-workspace/src/vibedeps/slot_record.rs",
        "crates/vibe-requirements/Cargo.toml",
        "crates/vibe-requirements/src/lib.rs",
        "crates/vibe-orchestrator/Cargo.toml",
        "crates/vibe-orchestrator/src/lib.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/common/PROP-058-change-bindings-and-closure.xml",
        "crates/vibe-requirements/src/change/",
        "crates/vibe-orchestrator/src/change/",
        "crates/vibe-requirements/Cargo.toml",
        "crates/vibe-requirements/src/lib.rs",
        "crates/vibe-orchestrator/Cargo.toml",
        "crates/vibe-orchestrator/src/lib.rs"
      ],
      "steps": [
        "Revalidate retained intent/source/world/policy/output witnesses on closure inspection.",
        "Mark applicability stale or proof unavailable while preserving historical disposition.",
        "Exercise source updates, policy changes, output tamper and erased disposable caches."
      ],
      "positive_cases": [
        "Unchanged retained proof preserves applicability after cache cleanup."
      ],
      "negative_cases": [
        "Changed source/definition/policy/output cannot retain a fresh acceptance claim.",
        "Missing retained immutable source is unavailable, not silently refreshed."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-requirements target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-requirements <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-orchestrator target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-orchestrator <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "Report separates historical decision, current applicability and missing proof.",
        "No revalidation query mutates closure or reinstalls sources."
      ],
      "safe_stop": "Keep historical closure and report exact drift instead of deleting it.",
      "commit_subject": "test(change): revalidate closure applicability",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/common/PROP-058-change-bindings-and-closure.xml (PROPOSED file until its source-contract task creates it). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_04_d_2_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    }
  ]
}
