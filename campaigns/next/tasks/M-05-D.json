{
  "id": "M-05-D",
  "tasks": [
    {
      "id": "M-05-D.1",
      "title": "Persist requested intent separately from resolved configuration",
      "goal": "Make lock replay explain exact choices, captures and selected technical edges.",
      "read_paths": [
        "crates/vibe-install/src/record.rs",
        "crates/vibe-install/src/plan.rs",
        "crates/vibe-core/src/manifest/lockfile.rs",
        "vibevm/vibespecs/common/PROP-044-change-native-formats.xml",
        "xtask/src/main.rs",
        ".cargo/config.toml",
        "crates/vibe-install/Cargo.toml",
        "crates/vibe-install/src/lib.rs",
        "crates/vibe-core/Cargo.toml",
        "crates/vibe-core/src/lib.rs",
        "crates/vibe-core/src/manifest/mod.rs",
        "crates/vibe-wire/Cargo.toml",
        "crates/vibe-wire/src/lib.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/modules/vibe-resolver/PROP-003-dep-evolution.xml",
        "crates/vibe-install/src/record.rs",
        "crates/vibe-core/src/manifest/lockfile.rs",
        "schemas/",
        "formats/",
        "crates/vibe-wire/src/",
        "vibevm/vibespecs/common/PROP-044-change-native-formats.xml",
        "crates/vibe-install/Cargo.toml",
        "crates/vibe-install/src/lib.rs",
        "crates/vibe-core/Cargo.toml",
        "crates/vibe-core/src/lib.rs",
        "crates/vibe-core/src/manifest/mod.rs",
        "crates/vibe-wire/Cargo.toml",
        "crates/vibe-wire/src/lib.rs"
      ],
      "steps": [
        "Serialize authored-request digest separately from active/dormant resolved selections and feature closure.",
        "Record exact owner capture/definition, resolution domain/target, induced dependencies and selected capability providers.",
        "Add reader migration/refusal and canonical ordering through existing generated format machinery."
      ],
      "positive_cases": [
        "Lock preserves explicit choices and actual provider edges for offline replay."
      ],
      "negative_cases": [
        "Same-version changed definition or lost request digest cannot be accepted as exact replay.",
        "Derived selections cannot become the only authored authority."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-install target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-install <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-core target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-core <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-wire target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-wire <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check",
        "CONDITIONAL ORACLE: cargo xtask check-codegen only when the actual diff affects schemas, generators, generated-output registration or a dependent contract. Bind a narrower existing oracle if available; the global command needs that concrete impact and no adequate narrower check.",
        "CONDITIONAL ORACLE: cargo xtask wire-diff only when the actual diff affects wire/format behavior, its corpus or epoch semantics. Bind a narrower existing oracle if available; the global command needs that concrete impact and no adequate narrower check."
      ],
      "acceptance": [
        "Lock round-trip reconstructs requested versus resolved semantics distinctly.",
        "Every induced edge explains its source declaration and consumer intent."
      ],
      "safe_stop": "Keep an old lock recognizable; do not silently reinterpret unknown epochs.",
      "commit_subject": "feat(install): record exact requested and resolved configuration",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/modules/vibe-resolver/PROP-003-dep-evolution.xml (existing source). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_05_d_1_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    },
    {
      "id": "M-05-D.2",
      "title": "Connect feature and subskill activation to real consumers",
      "goal": "Make selected contributions reach visibility, loading and projection while preserving source bytes.",
      "read_paths": [
        "crates/vibe-resolver/src/features.rs",
        "crates/vibe-install/src/record.rs",
        "crates/vibe-agent-projection/src/pkgskill/projection/binding.rs",
        "crates/vibe-workspace/src/extension_world.rs",
        "crates/vibe-install/Cargo.toml",
        "crates/vibe-install/src/lib.rs",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs",
        "crates/vibe-agent-projection/Cargo.toml",
        "crates/vibe-agent-projection/src/lib.rs",
        "crates/vibe-agent-projection/src/pkgskill.rs",
        "crates/vibe-resolver/Cargo.toml",
        "crates/vibe-resolver/src/lib.rs"
      ],
      "write_paths": [
        "vibevm/vibespecs/modules/vibe-resolver/PROP-003-dep-evolution.xml",
        "crates/vibe-install/src/",
        "crates/vibe-workspace/src/extension_world.rs",
        "crates/vibe-agent-projection/src/pkgskill/",
        "crates/vibe-resolver/src/features.rs",
        "crates/vibe-install/Cargo.toml",
        "crates/vibe-install/src/lib.rs",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs",
        "crates/vibe-agent-projection/Cargo.toml",
        "crates/vibe-agent-projection/src/lib.rs",
        "crates/vibe-agent-projection/src/pkgskill.rs",
        "crates/vibe-resolver/Cargo.toml",
        "crates/vibe-resolver/src/lib.rs"
      ],
      "steps": [
        "Carry active feature/subskill exports into effective contribution selection and owning projection consumers.",
        "Preserve full package materialization; activation changes effective contributions only.",
        "Retain dormant branch selections as authored intent while disabling their effects and diagnose removed dormant options."
      ],
      "positive_cases": [
        "Activated subskill appears in the real loading/projection result and lock provenance."
      ],
      "negative_cases": [
        "Inactive branch contributes no effects; dormant removed option reports drift.",
        "Unselected package files are not physically filtered from captured source."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-install target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-install <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-workspace target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-workspace <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-agent-projection target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-agent-projection <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-resolver target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-resolver <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "End-to-end fixture observes contribution activation, not only a populated field.",
        "Changing activation leaves captured package bytes unchanged."
      ],
      "safe_stop": "Do not admit subskill semantics before every required consumer works.",
      "commit_subject": "feat(install): activate selected feature contributions",
      "notes": [
        "Internal children execute sequentially; cross-parent prerequisites remain exactly the root campaign graph.",
        "Permanent destination: vibevm/vibespecs/modules/vibe-resolver/PROP-003-dep-evolution.xml (existing source). Create/update the governing permanent contract before implementation assertions cite its real anchors; campaign retirement/promotion is root-owned.",
        "TO CREATE: m_05_d_2_cases test family for the cases below. This is not an existing test. Bind the owning target and intended regression cases before dispatch; confirm created names and nonzero exact selection before running. Do not compile unrelated targets for discovery.",
        "Only listed file/subtree perimeters may change; new implementation/test/schema paths within them are PROPOSED, not claims of existing files. Reuse prerequisite-owned modules where present and stop for a packet correction if an essential edit falls outside the perimeter.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    }
  ]
}
