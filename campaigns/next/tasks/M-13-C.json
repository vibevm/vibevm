{
  "id": "M-13-C",
  "tasks": [
    {
      "id": "M-13-C.1",
      "title": "Implement pure versioned migration planning",
      "goal": "Parse and plan deterministic authored-format migrations while preserving owner syntax and values.",
      "read_paths": [
        "crates/vibe-core/src/manifest/package.rs",
        "vibevm/vibespecs/common/PROP-044-change-native-formats.xml",
        "vibevm/vibespecs/common/PROP-045-xml-spec-sources.xml",
        "vibevm/vibespecs/common/PROP-032-project-model-ide-substrate.xml",
        "vibevm/vibespecs/common/PROP-031-algorithmic-refactoring.xml",
        "xtask/src/main.rs",
        ".cargo/config.toml",
        "crates/vibe-core/Cargo.toml",
        "crates/vibe-core/src/lib.rs",
        "crates/vibe-core/src/manifest/mod.rs",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs"
      ],
      "write_paths": [
        "crates/vibe-core/src/manifest/",
        "crates/vibe-workspace/src/",
        "schemas/",
        "formats/",
        "crates/vibe-wire/",
        "crates/vibe-core/Cargo.toml",
        "crates/vibe-core/src/lib.rs",
        "crates/vibe-core/src/manifest/mod.rs",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs"
      ],
      "steps": [
        "Define a source-version registry with a frozen recognized epoch-less grammar and deterministic transform chain; unknown input is a typed refusal.",
        "Retain a syntax-preserving representation for authored TOML/text so comments, ordering and owner selections survive planned edits.",
        "Produce validated in-memory before/after diffs and typed NeedsInput for ambiguity; do not implement SourceDescriptor or feature-specific converters before their own reader atoms."
      ],
      "positive_cases": [
        "A recognized legacy fixture yields deterministic planned changes with comments/values preserved.",
        "A current-format input yields an explicit no-op plan."
      ],
      "negative_cases": [
        "Unknown version, ambiguous selection and malformed syntax refuse before filesystem effects.",
        "No LLM or heuristic default chooses missing authored intent."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-core target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-core <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-workspace target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-workspace <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-wire target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-wire <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "CONDITIONAL ORACLE: cargo xtask check-codegen only when the actual diff affects schemas, generators, generated-output registration or a dependent contract. Bind a narrower existing oracle if available; the global command needs that concrete impact and no adequate narrower check.",
        "CONDITIONAL ORACLE: cargo xtask wire-diff only when the actual diff affects wire/format behavior, its corpus or epoch semantics. Bind a narrower existing oracle if available; the global command needs that concrete impact and no adequate narrower check.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "Repeated planning over exact input gives identical output/diff identity.",
        "Fixture coverage demonstrates genuine transforms and no-op/ambiguity cases."
      ],
      "safe_stop": "Pure executable migration planner is complete; no authored file changed by plan.",
      "commit_subject": "feat(migrate): plan deterministic syntax-preserving format changes",
      "notes": [
        "Exact module/type/epoch names are settled inside this task's approved perimeter. SourceDescriptor converter belongs to M-06-A, feature converter to M-05-B.",
        "Schema changes: inspect the existing codegen entrypoint before regeneration; never hand-edit generated types or invent a new codegen command. The listed check-codegen and wire-diff oracles are conditional on the actual affected inputs, never on the allowed write perimeter alone.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    },
    {
      "id": "M-13-C.2",
      "title": "Expose migration plans and input requirements as machine values",
      "goal": "Connect the migration planner to strict request/report contracts and structured one-shot surfaces.",
      "read_paths": [
        "vibevm/vibespecs/common/PROP-044-change-native-formats.xml",
        "vibevm/vibespecs/common/PROP-031-algorithmic-refactoring.xml",
        "vibevm/vibespecs/common/PROP-032-project-model-ide-substrate.xml",
        "crates/vibe-mcp/src/tools/output.rs",
        "crates/vibe-cli/src/commands/refactor/mod.rs",
        "xtask/src/main.rs",
        ".cargo/config.toml",
        "crates/vibe-core/Cargo.toml",
        "crates/vibe-core/src/lib.rs",
        "crates/vibe-core/src/manifest/mod.rs",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs",
        "crates/vibe-cli/Cargo.toml",
        "crates/vibe-cli/src/commands/mod.rs",
        "crates/vibe-mcp/Cargo.toml",
        "crates/vibe-mcp/src/lib.rs"
      ],
      "write_paths": [
        "crates/vibe-core/src/manifest/",
        "crates/vibe-workspace/src/",
        "crates/vibe-cli/src/commands/refactor/",
        "crates/vibe-mcp/src/",
        "schemas/",
        "formats/",
        "crates/vibe-wire/",
        "crates/vibe-core/Cargo.toml",
        "crates/vibe-core/src/lib.rs",
        "crates/vibe-core/src/manifest/mod.rs",
        "crates/vibe-workspace/Cargo.toml",
        "crates/vibe-workspace/src/lib.rs",
        "crates/vibe-cli/Cargo.toml",
        "crates/vibe-cli/src/commands/mod.rs",
        "crates/vibe-cli/src/commands/refactor/mod.rs",
        "crates/vibe-mcp/Cargo.toml",
        "crates/vibe-mcp/src/lib.rs"
      ],
      "steps": [
        "Register migration request/report/error schemas, exact source/target epochs, diff/preconditions and typed input questions.",
        "Add pure structured entrypoints over .1; keep CLI/MCP readers thin and do not infer arbitrary paths or write authority from request fields.",
        "Validate requests/results against owning schemas, preserve preflight/partial-error distinctions for later apply and align identifiers with M-16-A's planned facade."
      ],
      "positive_cases": [
        "CLI and machine planning return equivalent diffs and NeedsInput values.",
        "A consumer can inspect exact unchanged source preconditions without parsing prose."
      ],
      "negative_cases": [
        "Wrong types/unknown fields cannot silently default.",
        "Opening a plan cannot create state/cache/lock or execute a converter's external provider."
      ],
      "checks": [
        "SELECT BEFORE DISPATCH: vibe-cli target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-cli <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-mcp target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-mcp <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-wire target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-wire <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "SELECT BEFORE DISPATCH: vibe-workspace target/cases are TO BIND from source/metadata to affected positive/negative obligations; choose --lib, --test <name>, --bin <name>, or affected --doc before compiling.",
        "RUN BOUND CHECKS: after binding, use cargo test -p vibe-workspace <target> <case> -- --exact for lib/test/bin, or a reviewed small family without --exact; bind supported rustdoc selection separately. Verify intended nonzero cases in output; use target-specific --list only when needed to confirm selection, and reuse its valid inventory.",
        "CONDITIONAL ORACLE: cargo xtask check-codegen only when the actual diff affects schemas, generators, generated-output registration or a dependent contract. Bind a narrower existing oracle if available; the global command needs that concrete impact and no adequate narrower check.",
        "CONDITIONAL ORACLE: cargo xtask wire-diff only when the actual diff affects wire/format behavior, its corpus or epoch semantics. Bind a narrower existing oracle if available; the global command needs that concrete impact and no adequate narrower check.",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "Plan/report contract has generated validation and usable examples.",
        "No dependency on completed M-16-B is introduced; later facade adapts these owned domain contracts."
      ],
      "safe_stop": "Strict machine migration planning is complete; mutation remains separate.",
      "commit_subject": "feat(migrate): expose typed plans and ambiguity reports",
      "notes": [
        "Schema changes: inspect the existing codegen entrypoint before regeneration; never hand-edit generated types or invent a new codegen command. The listed check-codegen and wire-diff oracles are conditional on the actual affected inputs, never on the allowed write perimeter alone.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    }
  ]
}
