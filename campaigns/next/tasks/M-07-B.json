{
  "id": "M-07-B",
  "tasks": [
    {
      "id": "M-07-B.1",
      "title": "Run installed bundle smokes in isolated native homes",
      "goal": "Build a reusable smoke harness that tests the exact prepared artifact on each supported native target.",
      "read_paths": [
        "xtask/src/dist/build.rs",
        "xtask/src/dist/release.rs",
        "xtask/src/dist/release/tests.rs",
        "crates/vibe-publish/src/release_manifest.rs",
        "rust-toolchain.toml",
        "tools/self-check.sh",
        "xtask/src/main.rs",
        ".cargo/config.toml",
        "crates/vibe-publish/Cargo.toml",
        "crates/vibe-publish/src/lib.rs"
      ],
      "write_paths": [
        "xtask/src/dist/",
        "crates/vibe-publish/src/",
        "schemas/",
        "formats/",
        "crates/vibe-wire/",
        "crates/vibe-publish/Cargo.toml",
        "crates/vibe-publish/src/lib.rs"
      ],
      "steps": [
        "Extract the current native target list and archive/install route from existing distribution code; do not invent a target list or CLI flags.",
        "For each target, unpack the subject-bound bundle into a fresh isolated home and invoke the installed executable through a harness port; retain target/runtime/bundle identity and bounded output.",
        "Add native harness fixtures and a machine report for pass/fail/unavailable; foreign-host jobs remain unavailable until actually executed on that target."
      ],
      "positive_cases": [
        "Each available native target launches the exact unpacked binary.",
        "Changing the installed artifact digest invalidates the smoke receipt."
      ],
      "negative_cases": [
        "Cross-compilation or a fake process cannot count as native execution evidence.",
        "Unpack failure, wrong binary or nonzero execution is a failed/unavailable smoke, not pass."
      ],
      "checks": [
        "cargo test -p xtask -- --list",
        "cargo test -p xtask",
        "cargo test -p vibe-publish -- --list",
        "cargo test -p vibe-publish",
        "cargo xtask check-codegen",
        "cargo xtask wire-diff",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "All four native targets have distinct evidence rows; missing hosts remain outstanding obligations.",
        "Harness proves actual installed artifact identity and leaves the real home unchanged."
      ],
      "safe_stop": "Harness and runnable local smoke are ready; unavailable target evidence is explicitly pending.",
      "commit_subject": "feat(dist): smoke exact bundles in isolated native homes",
      "notes": [
        "Native executors are supplied by the root task. Do not create CI infrastructure or acquire machines inside this task.",
        "Schema changes: inspect the existing codegen entrypoint before regeneration; never hand-edit generated types or invent a new codegen command. The listed check-codegen and wire-diff commands are existing self-check gates.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    },
    {
      "id": "M-07-B.2",
      "title": "Make smoke default-on with explicit scoped skip",
      "goal": "Wire smoke policy into standalone preparation and its service reports without running full source tests implicitly.",
      "read_paths": [
        "xtask/src/dist/release.rs",
        "xtask/src/dist/build.rs",
        "crates/vibe-publish/src/release_manifest.rs",
        ".github/workflows/release-distributions.yml",
        "xtask/src/main.rs",
        ".cargo/config.toml",
        "crates/vibe-publish/Cargo.toml",
        "crates/vibe-publish/src/lib.rs"
      ],
      "write_paths": [
        "xtask/src/dist/",
        "crates/vibe-publish/src/",
        "schemas/",
        "formats/",
        "crates/vibe-wire/",
        "vibevm/vibespecs/common/PROP-060-publication-authority.xml",
        "crates/vibe-publish/Cargo.toml",
        "crates/vibe-publish/src/lib.rs"
      ],
      "steps": [
        "Add the explicit skip input to the existing preparation surface using the actual CLI grammar; route it through the shared service.",
        "Bind requested target scope and skip reason/policy to a new prepared plan; keep pass/fail/skipped/unavailable distinct.",
        "Ensure default failed smoke blocks readiness; explicit skip creates new evidence rather than editing a failed row; source tests remain separately requested."
      ],
      "positive_cases": [
        "Default preparation executes artifact smoke.",
        "An explicit skip produces a new plan with skipped status while still validating archive/subject identity."
      ],
      "negative_cases": [
        "Failed smoke never becomes passed by changing a presentation flag.",
        "Skip does not omit identity checks, expand destinations or trigger source tests."
      ],
      "checks": [
        "cargo test -p xtask -- --list",
        "cargo test -p xtask",
        "cargo test -p vibe-publish -- --list",
        "cargo test -p vibe-publish",
        "cargo test -p vibe-wire -- --list",
        "cargo test -p vibe-wire",
        "cargo xtask check-codegen",
        "cargo xtask wire-diff",
        "cargo run --quiet -p vibe-cli -- check --path . --quiet",
        "cargo run --quiet -p vibe-cli -- facts check --exhaustive",
        "cargo fmt --all -- --check",
        "git diff --check"
      ],
      "acceptance": [
        "CLI and machine preparation return equivalent smoke policy/status.",
        "Existing accepted skipped path is represented without a false execution claim."
      ],
      "safe_stop": "Local preparation reports include exact smoke policy; remote apply remains untouched.",
      "commit_subject": "feat(dist): expose default smoke and explicit skip policy",
      "notes": [
        "Schema changes: inspect the existing codegen entrypoint before regeneration; never hand-edit generated types or invent a new codegen command. The listed check-codegen and wire-diff commands are existing self-check gates.",
        "Registration perimeter includes existing crate manifests and module owners only for required exports/dependencies; do not change unrelated declarations."
      ]
    }
  ]
}
