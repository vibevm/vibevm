# Леджер отложенного — кампания документации {#root}

<status stage="spec" state="work" comment="ведётся с 2026-09-11; в зоне кампании с фазы 1 (A1.10); строки с пометкой BACKLOG перенесены в корневой BACKLOG.md отдельным коммитом фазы 1"/>

Правило (`campaign-plans`): отложенное живёт здесь, а не в чате; строка
называет находку, откуда она, куда уходит (BACKLOG продукта или следующая
кампания) и триггер.

| Id | Дата | Что | Откуда | Куда | Триггер |
|---|---|---|---|---|---|
| X-001 | 2026-09-11 | `vibe cache add` печатает «already present — bytes untouched (write-once)», хотя запись store заменена по хэшу (`crates/vibe-cli/src/commands/cache/add.rs:70` считает `was_present` до выборки; `store/refresh.rs:24` заменяет запись) | A0.8, A0.20 (пробы B2, B4, B5, C2) | BACKLOG продукта (дефект вывода CLI) | любая работа над `cache add`; сайт на строку отчёта не опирается |
| X-002 | 2026-09-11 | Докстринг `campaigns/packages-2026-09/tasks/judging-debt.py` цитирует `PROP-043 §10.1`, `##DEBT-IS-A-LIST-NOT-A-RATIO`, `##DEBT-MUST-BE-ASKABLE` — якоря живут в PROP-047 §6.2 после tombstone 2026-08-22 | A0.19 | BACKLOG продукта (стале-ссылка в чужой зоне кампании) | правка скрипта при внедрении `[judging] exempt` (фаза 2) |
| X-003 | 2026-09-11 | Doc-комментарий `crates/vibe-core/src/package_ref/kind.rs:16` говорит «four installable package kinds» при шести вариантах enum | A0.19 | правится попутно в A2.1 (тот же файл получает `Doc` и `App`) | A2.1 |
| X-004 | 2026-09-11 | Адресуемость детей `prompt` (`assert`, `needs`) якорем: `pNN` нумерует блоки, дети адресуются позицией при цитировании человеком | A0.4, A0.25 | следующая волна документации | запрос автора страницы сослаться на конкретный ассерт |
| X-005 | 2026-09-11 | `vibe` (`vibe bin build`, `cache add` из корня) пишет в дерево untracked `vibevm/vibedeps/.gitignore` («Managed by vibe»); вопрос, должен ли файл быть в истории, и почему он исчез позже в той же сессии | J-025 | BACKLOG продукта (вопрос владельцу о файле в истории) | рендерер сайта запускает `vibe` в выкладке — `git pull` не должен спотыкаться |
| X-006 | 2026-09-11 | `docdebt.json` (PROP-047 §5.2) объявлен, но пуст и никем не наполняется; кандидат на выход гейта покрытия `vibe doc check --coverage` | A0.19, A0.5 | фаза 2 (A2.16) решает, пишет ли гейт этот файл | A2.16 |
| X-008 | 2026-09-11 | `schemas/install_plan.jtd.json` требует поле `plans`, а `vibe install --json` печатает `packages` (`crates/vibe-cli/src/commands/install/report.rs:150`); сгенерированный тип `install_plan` в `vibe-cli` не используется, поэтому `check-codegen` расхождения не ловит | A0.12 | BACKLOG продукта (дефект контракта `--json`) | раннер примеров помечает документ «не проверен» до исправления |
| X-009 | 2026-09-11 | Ключи конверта `invoked_by` и `unattended` (`crates/vibe-cli/src/output.rs:471`, `:487`) не объявлены ни в одной JTD-схеме: `--invoked-by` делает `--json`-документ невалидным по собственной схеме | A0.12 | BACKLOG продукта | раннер вычищает `VIBE_INVOKED_BY` из окружения |
| X-010 | 2026-09-11 | Документ `install:closure-diff` собирается вручную (`closure_diff.rs:195`, `:208`) и схемы не имеет — часть машинного вывода вне контракта JTD (PROP-000 §16) | A0.12 | BACKLOG продукта | — |
| X-011 | 2026-09-11 | Взаимный порядок stdout и stderr при раздельном захвате не сохраняется; пример, документирующий чередование, нуждается в режиме общего потока | A0.12 | следующая волна документации | первый пример, где чередование — суть |
| X-012 | 2026-09-11 | Раннер всегда не-TTY, а продукт ветвится по `stdin().is_terminal()` минимум в пяти местах; примеры документируют не-TTY ветку, интерактивное поведение описывается прозой; PTY-режим — позже | A0.12 | следующая волна документации | запрос показать интерактивную подсказку выводом |
| X-013 | 2026-09-11 | `research/ts-demo` на `b1291b06` не зелёный: floor красный на `prettier` (`src/main.ts:11`, 81 символ), `eslint` (плагин `@org.vibevm/eslint-plugin-ai-native` через `file:`-симлинк не резолвит `@typescript-eslint/utils`), `specmap` (orphan `compose`); `vibe.lock` схемы 6 при требуемой 7; `tsconfig.json` ниже пола GUIDE §1 на семь флагов без `deviates`; boot-сниппет стека называет полом четвёрку, GUIDE — двенадцать флагов | A0.14 | BACKLOG продукта (долг стека TypeScript, не кампании) | до открытия web-пакета решить, чем чинить резолв плагина |
| X-014 | 2026-09-11 | Резолв eslint-плагина `ai-native` в пакете вне раскладки репозитория: в пробе помог `npm install` внутри каталога плагина (не коммитится); без плагина теряется канал REQ-цитирующих диагностик (Scaffold F) | A0.14 | атом A4.1 (инициализация web-пакета) решает: чинить резолв или временно без плагина с `deviates` | A4.1 |
| X-015 | 2026-09-11 | Лендинг `vibevm-org` ссылается на `og.png` в четырёх мета-тегах, но файла нет ни в `public/`, ни в сборке; при переносе у сайта появляется реальное превью (генерируемое по D-20) — паритет по этому тегу объявляется исключением | A0.26 | атом A4.16 (перенос) — завести файл; вопрос владельцу, нужно ли фирменное изображение вместо генерируемого | A4.16 |
| X-016 | 2026-09-11 | JSON-LD `description` в `BaseLayout.astro:25` жёстко английский на обеих локалях, в отличие от `og:description`; при переносе локализуется с записью в леджер | A0.26 | атом A4.16 | A4.16 |
| X-017 | 2026-09-11 | **P1, безопасность.** Маршруты `vibe-index` `/v1/index/by-name/{name}`, `by-cap/{slug}`, `by-purl/{slug}` (`crates/vibe-index/src/server/routes/index_files.rs:80-126`) джойнят percent-декодированный захват с `data_dir` без проверки; на идентичном маршруте с той же axum 0.8.9 `..%2Fsecret.txt` вернул файл вне корня (200). На живом бинарнике не подтверждено. Лечение — лексический `safe_join` или `ServeDir`; тест `oneshot(GET /v1/index/by-name/..%2F..%2Fsecret.json)` | A0.7 | BACKLOG продукта P1 — сообщить владельцу на гейте фазы 0 | до публикации индекс-сервера наружу |
| X-018 | 2026-09-11 | Qwik SSG молча недогенерирует страницы при коде выхода 0 (`trailingSlash: false` → 1 из 7; неверный `base` → 0 и пустой sitemap): причина в `traverseRouteTree` от жёсткого `/` при фильтре от base | A0.10 | гейт сборки сайта по числу страниц (A4.x); вопрос в апстрим Qwik | A4.1 |
| X-019 | 2026-09-11 | Внутри pnpm-workspace `pnpm --version` даёт 11.24.0 при 10.33.2 снаружи; источник не найден (`packageManager`, `.npmrc`, `engines` пусты) — воспроизводимость сборки | A0.10 | `packageManager` в `package.json` web-пакета (A4.1) | A4.1 |
| X-020 | 2026-09-11 | Ядро Qwik 2.0.0-beta.43 несёт в тексте ошибок адрес превью-развёртки на Cloudflare Pages; попадёт в консоль пользователя при ошибке рантайма — не запрос, но чужой адрес в бандле | A0.10 | принять как факт беты; пересмотреть при выходе стабильной 2.0 | триггер D-12 |
| X-021 | 2026-09-11 | Политика `vibe doc serve`, когда пин экземпляра указывает на хэш оболочки, которого нет в `doc-shell/`, а сети нет: предложено падать в запасную оболочку с предупреждением; в D-12 и PROP-019 не записано | A0.11 | PROP-057 `local` (A1.3) фиксирует: запасная оболочка + предупреждение, никаких обращений в сеть без согласия | A1.3 |
| X-022 | 2026-09-11 | Проверка релиза `xtask/src/dist/release.rs` «требует и удалённо проверяет все двенадцать активов»; тринадцатый актив оболочки ломает счёт — расширить проверку | A0.11 | атом фазы 5 (публикация оболочки) | A5.x |
| X-007 | 2026-09-11 | Гарантия свежести git-источника: `DEFAULT_FRESHNESS_SECS = 3600` расходуется только монорепозиторным реестром; per-package путь обновляет клон на месте — есть ли путь, где TTL задержит новый HEAD, не установлено | A0.8 | не нужно кампании (сайт читает выкладку, не git-источник) | если git-источник понадобится для чужого хоста |
| X-023 | 2026-09-11 | `derived.json` как артефакт сборки продукта (сайт без тулчейна Rust): манифест derived-команд, производитель на стороне продукта, транспорт до сайта, правило на случай отсутствия артефакта, вопрос о его пометке | A0.28, вариант (б) | только по слову владельца; рендерер собирает `vibe` сам (dev-профиль) | владелец захочет рендер без тулчейна Rust на сервере |
| X-024 | 2026-09-12 | Закон `--exhaustive` (`facts.toml`) и страницы doc-пакета: страницы по замыслу не размечены фактами (`[judging] exempt`, PROP-057 §observability), но `vibe facts check --exhaustive` пока не разбирает `vibepacks/org.vibevm.core/vibevm-docs`; решить, освобождает ли `[judging] exempt` и от `--exhaustive`, до того как проверка начнёт видеть пакет | P.3 | A2.5 (ячейка гейта doc-пакетов), PROP-057 §observability | первый прогон `facts check` по doc-пакету — **решено 2026-09-12 (P2-O5, `b9be9f3b`): да, `[judging] exempt` освобождает и от `--exhaustive`; выбор словаря по виду пакета в читателе — хвост P2-O3** |
| X-025 | 2026-09-12 | Герметичность примеров установки без `--offline` (`vibe install org.vibevm.world/wal --path hello-vibe --assume-yes` на `hello-vibe-empty`): в снятии фазы P команда идёт в сеть; раннер должен выбрать — реестр-каталог внутри фикстуры с подстановкой `${REGISTRY}` в её файлы, `--offline` в команде страницы, или сеть в панели | P.3, `EXAMPLES-TODO.md` | A2.9 | проектирование `example.toml` фикстур |
| X-026 | 2026-09-12 | Примеры, чей вывод снимается с релизного дистрибутива, а не с отладочной сборки (`start/install-vibe#windows-install`): нужен статус «снимается на релизе» и правило раннера (пропуск с причиной, не пустой `expect`), иначе `INV-EXAMPLES-RUN` красный по замыслу | P.3 | A2.9, фаза 5 | первый релиз с оболочкой |
| X-027 | 2026-09-12 | Карточка предмета документации (`title`, `abstract` для `org.vibevm.core/vibevm`, D-20, P.4): корень хоста — `[project]`, не `[package]` (A0.20), полям карточки нет места; решить, откуда сайт берёт карточку предмета — из `[project]`, из doc-пакета или из индекса | P.4 | A2.3 (индекс и карточка), PROP-057 `CARD-*`, `REL-FIELD-PLACEMENT` | проектирование карточки в A2.3 |
| X-028 | 2026-09-12 | Порог длины фразы для линтера: при лимите 25 слов корпус даёт 195 предупреждений на 44 страницах (162 в коридорах, 19 в промптах, 14 в `outcome`); закон различает ошибки (процедуры, предупреждения) и предупреждения (коридоры) — калибровать лимиты по виду блока: 25 для шагов, 35 для коридоров, промпты без лимита | PP-C1 | A2.25 | первый прогон `vibe doc check --style` |
| X-029 | 2026-09-12 | Правила проверки цитат для `vibe doc check --citations`: адрес с многоточием или `&lt;…&gt;` в прозе — плейсхолдер, не цитата; `org.acme` — зарезервированная учебная организация, не разрешается; `spec://org.vibevm.core/vibevm-docs/…` — самоадресация страниц, разрешается по дереву пакета | PP-C1 §1.2 | A2.11 | реализация проверки цитат |
| X-030 | 2026-09-12 | Грамматика `ref` у `derived kind="manifest-field"`: страница `reference/manifest` ставит `ref="org.vibevm.core/vibevm-docs"` в смысле «весь манифест этого пакета», карточка (D-20) — в смысле одного поля (`abstract`); предложение: `<координата>` — весь манифест, `<координата>#<поле.в.точечной.записи>` — одно поле, без координаты — текущий пакет | P.3, PROP-045 §7 `ROW-DOCVOCAB-DERIVED` | A2.13 (генератор `derived`) | реализация генератора |
| X-031 | 2026-09-12 | Вывод примеров зависит от платформы: `vibe deploy --plan` печатает «profile `local` on `windows`», и снятый на Windows `expect` покраснеет на Linux/macOS; нужна либо нормализация имени платформы в фикстуре, либо `when="os:…"` на примере с отдельными снятиями | A2.9, первый прогон | A2.9 хвост (нормализация `platform`) или A3.x при первом прогоне панели на другой ОС | панель на не-Windows машине |
| X-032 | 2026-09-12 | Статусный барьер разметки обязательств: факты, которые руководство уже цитирует и которые проходят критерий A3.13, носят не-`done` статус и потому не помечены — PROP-050 (`FRIENDS-LIST`, `FRIEND-CLOSURE`, `EFFECTIVE-SET`, `ACCESS-IS-SEMVER-SURFACE`, `PER-NODE-PERSPECTIVE`, `ROOT-DRIVES-MATERIALISATION`, `FRIENDSHIP-IS-CONSUMER-DECLARED`, `CLOSURE-DRIFT-CONTROL`), PROP-053 (`vibe clean` целиком, `impl/plan`), PROP-010 (`USER-LEVEL-REGISTRIES`, `HASH-INTEGRITY-GATE`), семь фактов у M3; статус документа PROP-046 `spec/work` при одиннадцати фактах `impl/done`; PROP-033 описывает второе, непоставленное наполнение глагола `vibe refactor` | A3.13, отчёты M1–M3, M5 | атом актуализации статусов в кампании фактов (по свидетельству кода и тестов), затем второй проход разметки скриптами `P3-M<n>-mark.py` | слово владельца о статусах спек |
| X-033 | 2026-09-12 | `vibe check --path <пакет>` требует `vibevm/vibespecs/boot/` от любого пакета, кроме вида `doc`: отказывает `app` (web-пакет), `stack` `typescript-ai-native`, `tool` `jtd-codegen` — гейт считает всякий пакет проектом | P4-O1, А-3 | BACKLOG B-145 (P2): освободить `app` по той же норме, что `doc` (`KIND-APP-VS-TOOL`), и пересмотреть правило для пакетов без boot-сниппета | вне кампании |
| X-034 | 2026-09-12 | Qwik 2.0.0-beta.43: `<RouterOutlet />` внутри `layout.tsx` уводит SSG в синхронный бесконечный цикл без ошибки и без вывода («SSG render timed out»); лечение — `<Slot />` в layout, outlet только в `root.tsx`; предупреждение адаптера о «прямой сборке ssr» печатается на каждой зелёной сборке и ведёт в стену при следовании совету | P4-O1, А-1, А-2 | записано в `site/README.md`; кандидат в апстримный тикет при следующем пересмотре пина | пересмотр пина Qwik |
| X-035 | 2026-09-12 | Инлайн-скрипты в `<head>` (тема — без неё FOUC; после P4-O2 второй — отключение восстановления прокрутки роутером) требуют `sha256` каждого в CSP публичного сайта и локального читателя; иначе — блокирующий `<script src>` | P4-O1, P4-O2 | атомы A4.4 (CSP сайта) и A4.7 (CSP сервера): хэш считается сборкой и подставляется в заголовок | A4.4 / A4.7 |
| X-036 | 2026-09-12 | Doc-сканер не инъецирован в `xtask specmap`: четыре живые цитаты руководства на табличные факты (`PROP-008#ROW-*`, `PROP-002#ROW-GS-BRANCH-MEANING`) стали бы блокерами resolve-гейта под `--check`, потому что движок карты не чеканит юнит для факта в ячейке (B-144); `explain`/`query`/`select` читают документацию через свежую карту без гейта | P2-O8 | после починки B-144 в авторском движке — инъекция одним атомом и `specmap.json` с рёбрами `documents` | B-144 |
| X-037 | 2026-09-12 | Скилл руководства виден `vibe skill list` только по in-tree пути (скобка пакета); из машинного store — нет: нужно объявление «эта документация консультируется», которое `KIND-DOC-KNOWN-LIMIT` выводит за волну | P2-O8 | норма в PROP-057 (объявление в проекте) и атом в `vibe skill` — следующая волна | слово владельца о форме объявления |
| X-038 | 2026-09-12 | Примеры карты `architecture/traceability#select` и `authoring/specs-agents-can-cite#explain` спрашивают `PROP-000#KIND-SET` и перечисляют цитирующие страницы — каждая новая цитата этого факта красит их снова (уже дважды: `7fbe4345`) | P2-O8, J-088 | перевести примеры на юнит с устойчивым списком рёбер или на фиксацию «первые N строк» в фикстуре; до того — переблагословлять после проходов цитат | A3 хвост |
| X-039 | 2026-09-12 | Трипвайр раннера примеров считает `~/.vibe/steward/contexts/*/plan.toml` наблюдаемым: центральная сессия правит план стюарда — `--examples` падает сообщением не по делу, повтор зелёный | P2-O8 | исключить `~/.vibe/steward/**` из периметра трипвайра (`VIBE_SETTINGS` раннера и так приватный — проверить, что именно наблюдается) | P2-O10 или хвост A2.9 |
| X-040 | 2026-09-12 | Правило годового immutable-кеша в `nginx.conf` лендинга указывает на `/_astro/`, а сборка Qwik кладёт ассеты в `/assets/` и `/build/`; без правки правила новые ассеты идут без кеша, старое правило — мёртвое | P4-O5 | атом развёртывания A5.7 (контейнерный nginx сайта): правило на `/assets/` и `/build/` плюс 404 на `q-manifest.json`; серверный шаг — только с владельцем (R-24) | A5.7 |
| X-041 | 2026-09-12 | D-22 п. 7 ждёт сносок с обратными ссылками «из пивота», а словарь пивота (PROP-045 §7) сноски не знает: секция `#footnotes` в голдене острова — обычная проза; стили и компонент оболочки готовы, цепляться не к чему | P4-O2 | либо элемент `footnote` в словаре документации (поправка PROP-045 §7 — решение владельца), либо вычеркнуть сноски из D-22 с датой | слово владельца |
| X-042 | 2026-09-12 | Манифест страниц не несёт адресов картинок карточки, поэтому оболочка не может показать плейсхолдер, различающийся по координате (D-20-8); второй генератор хэша на TypeScript положил бы одно правило в два языка | P4-O2 | хвост P4-O4: поле `media` (`icon`, `banner`, `preview`) в `doc_manifest.jtd.json`, codegen обоих языков, заполнение в `build`/`manifest`; чтение в оболочке — правка после | P4-O4 |
| X-043 | 2026-09-12 | Qwik 2.0.0-beta.43 недетерминированно решает цепочку layout'ов между сборками: лендинг дважды получил документационную шапку поверх своей (два `<header class="docs-header">` в `dist/index.html`), в том числе на дереве до правок P4-O3, а следующая сборка тех же файлов дала одну; лечение X-034 (`layout-landing!.tsx` top-формой) уже применено | P4-O5 А-1, P4-O3 А-2 | BACKLOG B-146; пересмотр пина Qwik вместе с X-034 | паритет и линтер гоняются на той сборке, что уходит в выкладку (P5-O2: образ прогоняет их над своим `dist/`); третье воспроизведение — смена пина; дешёвый признак двойной шапки — строка `link(s) followed` линтера: 738 при одной, 750 при двойной (воспроизведено ещё дважды на одном коммите в хвосте P4-O3) |
| X-044 | 2026-09-12 | CSP на хэшах растёт линейно со страницами: 47 уникальных хэшей на 119 вхождений при 19 страницах (свои — 2, остальное — `q:func`, preload-списки и загрузчики фреймворка), на руководстве в 48 страниц ~150 хэшей и заголовок в килобайтах; nonce на статическом хостинге невозможен | P4-O3 А-1 | A5.7 (P5-O2): хэши остаются, `csp.conf` генерируется из `csp.txt` при сборке образа и вешается только на HTML-ответы; размер заголовка — владельцу в отчёте | заголовок больше ~16 КБ или нарушение CSP в проде — пересмотр (nonce сервером или `'strict-dynamic'`) |
| X-045 | 2026-09-12 | Голден острова цитирует `media/diagram.svg` относительно страницы, а конвейер кладёт медиа в корень дерева под хэшированными именами (`media/<хэш>.<ext>`) — адрес не сойдётся и у настоящего пакета с картинкой; SVG не разрешён в этой волне (D-20-6); в линтере ссылок — единственная запись `EXCEPTIONS` (`L-01`, 8 попаданий) | P4-O2 А-6, P4-O3 А-6 | BACKLOG B-147 | первый пакет с картинкой в странице (F-32): голден и форма адреса чинятся вместе, `L-01` снимается |
| X-046 | 2026-09-12 | Руководство не выводит `--help` команд третьего уровня (`vibe doc build --help` и подобные): все 54 блока `cli-help` — верхнего уровня, и смена флага подкоманды попадает в очередь как «нужна новая страница», потому что родительская помощь перечисляет только имена подкоманд | P2-O10, аномалия 1 | первая недельная петля A6.2: либо блоки `cli-help` на подкоманды в `reference/commands`, либо решение, что третий уровень документируется прозой, записанное в `AUTHORING.md` | первый прогон `vibe doc todo` после смены поверхности `vibe` |
| X-047 | 2026-09-12 | Первый `float` в JTD-схемах снимет `Eq` со всех генерируемых типов (`xtask/src/codegen/derive_floor.rs` говорит это прямо); метрика «тики на тысячу слов» поэтому записана как `tics_per_100k_words` (`uint32`), отчёт делит обратно | P2-O10, аномалия 2 | BACKLOG продукта при первой настоящей дробной метрике: решить пол derive для типов с `f64` (`PartialEq` без `Eq` или обёртка) | первая схема, которой нужна дробь без потери смысла |
| X-048 | 2026-09-12 | `crates/vibe-cli/tests/cli_spec_format.rs::redbook_polygon_*` запускает `vibe install` по сети, 10–15 минут на прогон; полный `cargo test -p vibe-cli` не заканчивается за час, панель `tools/self-check.sh` наследует эту стоимость | P2-O11, аномалия 3 | BACKLOG продукта: офлайновая фикстура реестра или флаг окружения для сетевых тестов | прогон панели на гейте фазы 2 — замерить длительность шага; дольше часа — завести строку BACKLOG |
| X-049 | 2026-09-12 | `vibe-index/tests/golden_corpus.rs` читает сгенерированные wire-типы из рабочего дерева, поэтому чужой кодген в соседнем периметре красит его на любой ревизии (1879 против 1891 байт) | P2-O11, аномалия 1 | правило параллельной работы (J-101); кандидат BACKLOG: голден сравнивает закоммиченные байты, а не рабочее дерево | второй ложный красный этого теста в параллельной волне |
