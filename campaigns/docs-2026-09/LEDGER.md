# LEDGER — документация 2026-09 {#root}

<status stage="spec" state="work" comment="леджер исполнения кампании documentation по форме campaign-plans; ведётся с 2026-09-11; фаза 0 принята, фаза 1 села, фаза P в работе"/>

Статус: **фаза 0 принята 2026-09-11** (26 спайков, коммитов ноль, внешние
каталоги не тронуты); **фаза 1 села 2026-09-12** — 16 коммитов по карте ниже;
A1.1/A1.2 закоммичены `ed75ce00` под целью сессии владельца («документация
сделана и задеплоена») и ждут его ратификации при слиянии ветки; **фаза P
в работе** — 44 страницы написаны и закоммичены по разделам (карта ниже);
снятие `expect` (PP-C2), инвентарь legacy (PP-C3) и проверки прозы (PP-C1)
у воркеров; следующее: вставка `expect`, прогон промптов P.7b, стиль-ревью
P.8, пакет передачи P.9.

Ветка `research-preview-1-docs` от `main@b1291b06`, тег
`research-preview-1-implementation`; worktree `vibevm-docs`.

## Фаза 0 — находки {#phase-0}

Полные вердикты — `PHASE-0-FINDINGS.md`; сырые находки — `findings/`.

| Спайк | Результат | Решение затронуто | Дата |
|---|---|---|---|
| A0.1 перепись kind | 5 исчерпывающих матчей в продукте, 1 в тесте; `BootCategory::App` уже есть | D-01, D-05 подтверждены | 2026-09-11 |
| A0.2 wire-словарь | ядро и wire — два пути; `known()` и три статических теста растут руками; break-заметки необязательны до `public = true` | D-01 | 2026-09-11 |
| A0.3 манифест | `ManifestWire` + `Manifest` + два `TryFrom`; `min_vibe_version` нигде не проверяется | F-05 переформулирован (владельцу) | 2026-09-11 |
| A0.4 пивот | словарь — параметр читателя; `when` — слот; дискриминатор `title=`; MD-проекция необратима | D-10 уточнено | 2026-09-11 |
| A0.5 аудитория agent | 12 мест правки; `--view doc` — фильтр, не гейт | D-11 подтверждено; PROP-043/047 поправлены | 2026-09-11 |
| A0.6 рёбра | хостовый сканер через шов `CodeScanner`; движок не трогать | D-14, D-17 | 2026-09-11 |
| A0.7 HTTP-сервер | отдельный крейт, CSP строкой, `--frame-ancestor`, без CORS; **P1-кандидат в `vibe-index`** (X-017, владельцу) | D-09 уточнено | 2026-09-11 |
| A0.8 store | API store/lock/реестра; `cache add` лжёт про «bytes untouched» (X-001) | D-01 | 2026-09-11 |
| A0.9 скиллы | `[[skill]]` по образцу; строка во встроенный шаблон безусловная | §7.4 | 2026-09-11 |
| A0.10 Qwik | пины 2.0.0-beta.43 / Vite 8.2.1 / Node 24.18.0 / pnpm 10.33.2; `ssg`; `base: "/"`; слэш обязателен; SSG молчит при недогенерации | D-06, D-12 уточнены | 2026-09-11 |
| A0.11 встраивание | `include_dir` + `build.rs`; оболочка — отдельный релизный актив с `DOC-SHELL.json`; `vibe doc shell install` | D-12 уточнено | 2026-09-11 |
| A0.12 раннер | макет на семи примерах; `VIBE_SETTINGS` + tripwire; без `match`; валидатор JTD в `vibe-doc` | D-10 уточнено | 2026-09-11 |
| A0.13 legacy | 1 184 утверждений, 90.6 % живо — предсказание PHASE-G не подтвердилось | D-15 | 2026-09-11 |
| A0.14 TS floor | floor хостовым бинарником; все семь шагов; `ts-demo` красный (X-013) | F-18 закрыт | 2026-09-11 |
| A0.15 план стюарда | контекст `8bc32a15-…`, план `vibevm-docs-2026-09` r1 | — | 2026-09-11 |
| A0.16 ячейка гейта | одна ячейка на правило, по образцу `snippet_presupposition.rs` | A2.5 | 2026-09-11 |
| A0.17 in-tree → store | `cache add --offline` из корня через project-local реестр | D-01, D-16 | 2026-09-11 |
| A0.18 i18n | `I18nDecl` и цепочка переиспользуются; sidecar не подходит | D-18; PROP-003 §2.7.6 | 2026-09-11 |
| A0.19 вне судейства | `[judging] exempt` + ячейка проверки | D-14 (F-34) | 2026-09-11 |
| A0.20 хост из исходников | корень хоста — `[project]`; сайт читает выкладку | D-07, D-16 уточнены | 2026-09-11 |
| A0.21 индекс и карточка | обратные запросы на стороне сайта; `[translations]` не хранится; язык — `[i18n].canonical` | D-18, D-20 уточнены | 2026-09-11 |
| A0.22 ридер | девять констант совпали; различия записаны (три темы, localStorage) | D-22 | 2026-09-11 |
| A0.23 токены и APCA | предсказание 10 подтверждено (Lc 57.15); 23 пары ниже порога; `apca-w3` — AGPL | D-21 уточнено | 2026-09-11 |
| A0.25 нумерация | `number_blocks` в `vibe-doc`, до фильтрации `when` | D-22 уточнено (F-43 закрыт) | 2026-09-11 |
| A0.26 лендинг | эталон 18 файлов; исключения паритета: путь ассетов, `og.png`, JSON-LD description | D-28 | 2026-09-11 |
| A0.28 цена рендера | dev-профиль: 22 с инкрементально при том же `--help`; дебаунс 5 мин | D-16 подтверждено; A5.1 | 2026-09-11 |

A0.24 снят девятой редакцией плана (лендинг переезжает в тот же сайт);
A0.27 в плане отсутствует.

## Фаза 1 — карта коммитов {#phase-1}

Заполняется по мере посадки; хэши — ветки `research-preview-1-docs`.

| Атом | Коммит | Что подтвердил или опроверг |
|---|---|---|
| — | `edd7e3ae` `chore(agents): run the coder-tier executor at high effort` | J-010: агент `opus5` без `effort: high` работал не в предписанном режиме |
| A1.3 | `c049789c` `docs(spec): record the documentation packages and site contract` | PROP-057: 263 факта, 17 именованных секций; статусы `spec/work` только у предложенного механизма оболочки, размещения полей и интервала опроса |
| A1.4 | `4df9ced4` `docs(spec): admit the -docs satellite role outside family unison` | PROP-028 §2.1 `ROLE-DOCS`, §2.2 `COMPANION-OUTSIDE-UNISON`, §3 `REJ-DOCS-IN-UNISON`; `req r2` в `roles` и `versioning` |
| A1.5 | `0d31a877` `docs(spec): open the dialect to a documentation vocabulary` | PROP-045 §7 — единственное записанное переоткрытие закона MD-подмножества с названным триггером |
| A1.6 | `390c7b1d` `docs(spec): admit the agent audience and name the coverage gate` | PROP-043 `ROW-ATTR-AUDIENCE-VALUES`, `AUDIENCE-VALUES`, `AUDIENCE-DOC-USE`; PROP-047 `CMD-REPORT` |
| A1.6b | `f46195bb` `docs(spec): let documentation translate by package, not sidecar` | PROP-003 §2.7.6 |
| A1.7 | `ee94b72a` `docs(design): add the documentation genre row` | строка `ROW-DOCS` в таблице жанров |
| A1.8 | `b93af0b3` `docs(campaign): mark phase G superseded by PROP-057` | статус `void` + причина; текст не тронут |
| A1.9 | `82620654` `docs(design): capture the documentation vision behind PROP-057` | конвертер `vibe refactor convert-source`; разметка `--exhaustive` скриптом воркера P1-O1 |
| A1.10 | `a6dbe253` `docs(campaign): open the documentation campaign zone` | PLAN, LEDGER, DEFERRALS, PHASE-0-FINDINGS, LEGACY-INVENTORY, JOURNAL, MAINTENANCE, findings/ — без провенанса и scratch-путей (J-046) |
| A1.13 | `8f956eeb` `docs(spec): adopt the documentation style law` | `STYLE.md` в зоне; `style/banned.en.txt`, `style/banned.ru.txt`; норма — PROP-057 §16 |
| A1.12 | `18da96a4` `docs(dev-guide): point developers at the documentation campaign` | DEV-GUIDE §8; Node не добавлен (R-11) |
| — | `eb803e29` `docs(backlog): file the phase-0 product findings` | B-123…B-130 из X-001, X-002, X-005, X-008–X-010, X-013 и F-05; P1 (X-017) не заводится — у владельца |
| — | `ab7731b6` `chore(specmap): tag the bridge code and regenerate the map` | пять сирот bridge-кампании получили `specmark::scope!` (J-049); 8 унаследованных `unbumped-hash` записаны как данные |
| — | `69053551` `chore(wire): raise the derive baseline for non-wire types` | J-048: ratchet держал не-wire типы; `release_manifest.rs` — настоящий wire, схема JTD у воркера P1-O3 |
| — | `bb6320b1` `refactor(wire): generate the distribution manifests from a JTD schema` | P1-O3: одна схема с `x-wire-order`, поведение в `vibe-wire::behaviour` (правило сироты), байты голденов не сдвинулись; шаг 3 панели зелёный |
| — | `03f01007` `chore(panel): clear the inherited test fixture and clippy reds` | P1-O4: фикстура `upstream_authors`, clippy по существу (замыкание в `fetch.rs`, `PinArgs` в `bridge.rs`); третий красный — перелом провода корпуса `index/e1` (J-069, review-маркер); ещё 11 фикстур `schema_version = 6` (J-070) — пакет P1-O5 |
| — | `b73195fd` `test: take the lock schema version from the constant in every fixture` | P1-O5: 10 фикстур на константе, каскад зелёный; красные — `golden_corpus` (J-069), `cli_search` ×2, ещё 16 литералов `7` (J-075) — пакет P1-O6 после фазы 2 |
| A1.1 + A1.2 | `ed75ce00` `docs(spec): admit doc and app kinds so documentation ships as packages` | под целью сессии владельца, с датированной пометкой «pending ratification» в тексте поправки; `VIBEVM-SPEC.md` §4.1 и PROP-000 `KIND-SET`/`INV-VOCABULARY` одним коммитом; ратификация — при слиянии (review-маркер) |
| A1.11 | локальный файл, не коммитится | план стюарда r2: DOCS-SPIKES принят, current_node DOCS-CONTRACT; `GOAL.md` перерисован |

## Фаза P — карта коммитов {#phase-p}

Проза ядра на английском, 44 страницы в 11 разделах, ~13 100 слов, 375
ссылок `rule`, 59 примеров, 19 промптов, 72 `derived`, 0 `figure`; индекс
страниц и якорей — `PAGE-INDEX.md` (`tasks/page-index.py`).

| Атом | Коммит | Что подтвердил или опроверг |
|---|---|---|
| P.1 | `42457142` `docs(campaign): map the pages of the core manual` | PAGE-MAP (44 страницы, лестница), EXAMPLES-TODO, INTERFACE-COPY, пакет PP-C1; порог «не меньше 13» перекрыт втрое |
| P.2 | `0df597b3` `docs(vibevm-docs): open the core documentation package skeleton` | `vibe.toml` вида `doc` с `[[documents]]`, `[i18n]`, `[[skill]]`; README как @fact-страница; LICENSE UPL-1.0; `AUTHORING.md` и `style/` пришли с импортом вижена (`82620654`) |
| P.3 | `7faef288` `docs(vibevm-docs): write the start section` | 5 страниц; промпт-сначала на `install-vibe`, `first-project` |
| P.3 | `4fcc71b2` `docs(vibevm-docs): write the model section` | 6 страниц |
| P.3 | `d2a71051` `docs(vibevm-docs): write the how-to section` | 8 страниц, 9 промптов |
| P.3 | `73f24039` `docs(vibevm-docs): write the agent section` | 3 страницы, 1 промпт |
| P.3 | `c604e671` `docs(vibevm-docs): write the lifecycle section` | 4 страницы, 2 промпта |
| P.3 | `8ca8d5ba` `docs(vibevm-docs): write the reference section` | 5 страниц; `reference/commands` целиком из `derived` |
| P.3 | `cc36532d` `docs(vibevm-docs): write the authoring section` | 7 страниц, 6 промптов |
| P.3 | `8487c6a0` `docs(vibevm-docs): write the architecture section` | 3 страницы |
| P.3 | `818a474d` `docs(vibevm-docs): write the glossary, questions and diagnostics` | глоссарий 50 терминов — единственное место определений; после PP-C1 `fingerprint` и `freshness fingerprint` разведены (одно слово — одно значение) |
| P.4 | в `0df597b3` и `42457142` | `title`/`abstract` doc-пакета, тело скилла `vibevm-docs`, `INTERFACE-COPY.md`; карточка предмета (`org.vibevm.core/vibevm`) — некуда положить, корень хоста `[project]` → X-027 |
| P.3 хвост | `700db4b8` `docs(vibevm-docs): capture the example outputs` | PP-C2b: 54 из 54 сняты (J-067); 39 вставлены; 15 примеров, чьи команды или фикстуры изменились после прогона промптов, ждут раннера A2.9 по слову владельца («минимизируй снятия»); 5 «не сейчас» (релиз, фаза 2, B-133) |
| P.3 правка | `c79325e7` `docs(vibevm-docs): describe the in-tree package layout` | семь страниц под реальную раскладку `vibe init package` (J-062); B-131…B-134 в BACKLOG |
| P.5 | коммит этого атома | 1184 строки закрыты: 957 + 93 перенесены, 80 + 24 сняты с причиной, 30 перенесены решением автора (J-064); редиректы реестра получили секцию на странице частного реестра |
| P.6 + P.7 | `435d3aef` `docs(vibevm-docs): act on the first prose check`; `2f48c888` `docs(vibevm-docs): shorten the longest sentences` | PP-C1: 385 цитат (3 якоря исправлены, 9 плейсхолдеров/самоадресаций — правила X-029), 11 запрещённых слов (4 исправлены, `capabilities` снят со списка), 195 длинных фраз (35 переписаны, порог — X-028), 3 абзаца-процедуры разбиты, 308 терминов до введения → 212 ссылок на глоссарий; повторная проверка: 0 нарушений (J-059, J-060) |
| P.7b | PP-O1 | 5 зелёных, 1 условный, 1 вне песочницы, 12 красных (J-065): баги продукта B-133, B-135…B-138; страницы и ассерты поправлены (`50439d94`); повторный прогон семи исправленных промптов отложен до гарнитуры A2.29 по слову владельца (J-067); 3 промпта ждут фазы 2 |
| P.8 | OPEN у владельца | три страницы вслух — предложение в review-маркерах |
| P.9 | после P.5–P.8 | пакет передачи §12.1 |

## Фаза 2 — карта коммитов {#phase-2}

Механика в `vibe`. Центральная сессия остаётся оркестратором (директива
владельца: код пишет `opus5` в режиме High пакетами); план писался под
центральную сессию Opus — разница только в том, кто читает отчёты. Атом —
коммит воркера по subject'у из плана, ревью диффа — центральной сессией
после посадки, push — только её рукой.

| Атом | Пакет | Коммит | Что подтвердил или опроверг |
|---|---|---|---|
| A2.1, A2.2, A2.4 | `findings/PACKET-P2-O1.md` | `b18453f2` `feat(core): teach PackageKind the doc and app kinds`; A2.2 — внутри `005ee978` (J-072); `29351a68` `feat(core): carry documentation relations, locales and the card in the manifest` | `is_read_only()` — одна точка «читают, а не ставят»; `vibe install` doc-пакета отказывает с подсказкой `cache add`; манифест руководства проходит `vibe check`; паритет прозы — хвост центральной сессии (J-073); корпуса `doc`/`app` — A2.3 |
| A2.8 | `findings/PACKET-P2-O2.md` | `a7bff252` `feat(specdoc): give the documentation genre its seven elements`; отчёт `6422ec88` | словарь — параметр читателя, `when` в слоте (J-071); корпусный тест прибивает 43 читаемые страницы; `audience="agent"` ждёт A2.12 |
| A2.0, A2.9, A2.10 | `findings/PACKET-P2-O4.md` | `f8220dd1` `feat(doc): open the vibe-doc crate`; `5c472381` `feat(doc): run documented examples so the docs cannot lie`; `e2a05ce9` `feat(doc): derive command, schema and manifest references at build time`; отчёт `b02703e5` | 52 из 59 примеров совпадают/сняты, 7 отложены в `examples/deferred.toml` (релиз, B-133, фаза 2 store/serve, scrape B-143); `derived` 72/72; правка страницы деплоя и снятие трёх примеров — коммит центральной сессии (J-074) |
| A2.11, A2.13, A2.22 | `findings/PACKET-P2-O6.md` | `51695bdd` `feat(doc): resolve rule citations against the current specs`; `fcc32b96` `feat(doc): render islands from the pivot so web and local share one content path`; `6aa578df` `feat(doc): number blocks at build time across every projection`; `cbd3b8e9` lockfile; отчёт `6e822cd4` | 381/381 цитат; остров; `pNN`; PROP-057 сведена (J-076); B-144 |
| A2.14, A2.24, A2.15, A2.23 | `findings/PACKET-P2-O7.md` | `55dfd646` `feat(doc): derive the page manifest, statuses and llms indexes`; `27de558e` `feat(doc): carry reading time and provenance in the page manifest`; `ff71092a` `feat(doc): check translations page by page`; `5ad8c7a8` `feat(doc): check translations block by block`; отчёт `15763604` | J-079; диск — J-078 |
| A2.25, A2.29 | `7675efc9` `feat(doc): lint documentation prose against the style law`; `316d2570` `feat(doc): run documented prompts through an agent and assert the outcome`; отчёт `facde833` | линтер по руководству при первом прогоне: 15 страниц чистых из 48, 85 ошибок (76 `term-before-introduction`, 7 `sentence-length` в процедурах, 2 `banned-word`), 124 предупреждения; пять правил дали честный ноль; прогон промптов с фальшивым раннером показал три состояния (сдержан, пропущен, провален) без настоящего агента; точка F1 отработана центральной сессией — `0d561a05`: 48 из 48 чистых, 0 ошибок, 119 предупреждений в очереди недельной петли |
| A2.26, A2.27 | `907948a2` `feat(doc): record version surfaces and diff them into pages to update`; `df0cc343` `feat(doc): build the maintenance queue`; отчёт `26bc0206`, `ec999b56` | снимок поверхности руководства: 166 команд (из `--help`), 221 поле `vibe.toml` и 55 полей `vibe.lock` (вопросом к настоящему парсеру, четыре формы `deny_unknown_fields`), 86 схем, 516 обязательств, 91 формат; два снимка одного бинарника байт в байт равны; снимок ключуется только номером версии — ни хэша, ни даты, ни состояния (тест по собственным ключам); `vibe doc diff` — «нужна новая страница» / «страницы, которых достигает смена»; `vibe doc todo`: восемь секций, каждая с `measured`, `null` вместо `0` там, где не мерили, примеры по флагу `--examples`; восемь чисел руководства: gaps 0, возраст страниц null (нет `reviews.toml` — A6.1), расхождения адаптаций null (нечего адаптировать), покрытие 100, тиков 431 на 100k слов (4,31 на тысячу; `uint32`, потому что первый `float` снял бы `Eq` со всех генерируемых типов — X-047), долг P1 0, записей без решения 0 при `--journal` на зону, дней с сверки null; **настоящий пробел**: 54 блока `cli-help` руководства — все верхнего уровня, смена флага `vibe doc build` попадает в «нужна новая страница» (X-046); гейты: fmt, build, 434 теста `vibe-doc`, 19 `commands::doc`, clippy `-D warnings`, `check-codegen` clean, specmap 0 suspects, facts clean; `conform check` — 27 чужих предсуществующих находок, по своим путям ноль |
| гейт фазы 2 (открыт) | `6d95657a` `chore(specmap): regenerate the map after the phase-2 wave` (94 ребра добавлены, 0 suspects, 0 сирот, 0 нерешённых хостовых рёбер); `cargo xtask conform check` 2026-09-12 | панель ещё не гонялась целиком (дерево не чистое: P4-O4 в работе); шаг 5 панели красен **27 новыми находками против пустой базовой линии**: по `git show b1291b06:<файл>` **десять файлов кампания вырастила за бюджет 600 строк сама** (specdoc `xml_doc`, `xml_doc_tests`, `xml_in`, `xml_out`; index `memory/tests`, `scanner/manifest`; progress-core `model`, `scope`; тесты `commands/progress` и `codegen/optional_shapes`) — разложены P2-O11 без изменения поведения (`c349d2e7` progress, `f8ee39ac` cli, `140ca684` index, `e34fa5ae` specdoc, `32c4a55c` xtask: 25 новых ячеек, самая большая 424 строки, числа тестов до/после равны по каждому крейту и по мультимножеству имён элементов; `conform check` после — ровно 17 унаследованных плюс 3 в новом крейте P4-O4 на момент прогона; отчёт в `findings/`); **семнадцать унаследованы с `main`** (bridge, vvm, orchestrator, `pkgskill` 958 строк, `manifest/package.rs` 602 на базе): 7 `file-length`, 5 `pub-doctest`, 3 `error-*-cites-req`, 2 `no-unwrap-in-domain` — **стоп-точка владельца**: чинить в кампании (код `main`, конфликт слияния с активной линией bridge) или заморозить базовую линию `conform freeze` с причиной «унаследовано с main@b1291b06»; рекомендация центральной сессии — заморозить с причиной и записать в BACKLOG; предсказания 1–3 отмечены (`b501eae6`); **слово владельца 2026-09-12: на границах фаз — точечный гейт вместо полной панели** (сквозные проверки на тёплом общем `target/` + свидетельства по затронутым крейтам из отчётов; сетевые тесты `vibe-cli` через `--skip`; полная панель — один раз перед слиянием в `main`); точечный гейт после P4-O4 (`8822f3bd`): `fmt` чист после `4ae339f8`, `check-codegen` clean, `specmap` 0 suspects, `conform` ровно 17 унаследованных, `facts` clean; `wire-diff` **зелёный после P2-O12** (`c6d8c662` `chore(corpora): reproject the index catalog on a pinned DEFLATE backend`): каталог корпуса `e1` расходился с проекцией журнала потому, что `flate2` выбирал бэкенд по фичам всего графа сборки (`zlib-rs` через `zip` в графе `vibe-cli`, `miniz_oxide` в графе `xtask`) — одно содержимое, разные сжатые байты, гейт красен с самого блессинга `cb041f5d`; бэкенд закреплён в `crates/vibe-index/Cargo.toml`, корпус перепроецирован, журнал не тронут — это вариант C из J-069, который ждал слова владельца: **исполнено под ратификацию**; `rebuild --check` — 16 файлов byte-identical; доктест `lock_fields` красный с P2-O10 — хвост 0 пакета P5-O1 |
| A2.16, A2.17, A2.18, A2.19, A2.20, хвост A2.5 | `075a5d6b` `feat(doc): gate documentation on spec obligations`; `6923c7ac` `feat(doc): derive placeholders and link previews from the coordinate`; `c3983882` `feat(cli): expose the documentation pipeline as vibe doc`; `7f919413` `feat(mcp): serve documentation pages to agents`; `41c02fe0` `feat(skill): point agents from errors to rules and docs`; `d40225e1` `feat(check): close the two translation rules the gate left open`; отчёт `811b17df` | первый прогон гейта показал 13 % (8 из 68 обязательств) — предсказание 8 подтверждено второй раз; на сдаче 100 %; крейт `vibe-doc-server` по чеклисту §2.8; `explain`/`query`/`select` видят документацию через свежую карту — инъекция сканера в `xtask specmap` отложена (X-036, B-144); скилл руководства — только in-tree (X-037); трипвайр раннера ловит `plan.toml` стюарда (X-039) |
| — | `findings/PACKET-P1-O6.md` | `8df28576` `test(cli): the search fixtures name the no-index rung instead of assuming it`; `aa8d196b` `test: take the lock schema version from the constant in the remaining fixtures`; отчёт `1c143bda` | причина красных `cli_search` — третья ступень лестницы индекса (`764cf517`), тест устарел; панель мертва на шаге 3 (рэтчет wire-derive, `vibe-core` 35 против 34 — `29351a68` без подъёма базы) — J-085 |
| A2.12, A2.21 | `findings/PACKET-P2-O5.md` | `d608a797` `feat(progress): admit the agent audience`; `b9be9f3b` `chore(facts): keep documentation observed but unjudged`; отчёт `98a91500` | X-024 решён: `[judging] exempt` освобождает и от `--exhaustive`; читатель `vibe facts check` пока не выбирает словарь по виду пакета — хвост в P2-O3 |
| A2.3, A2.5, A2.6, A2.7 | `findings/PACKET-P2-O3.md` | `cb041f5d` `feat(index): index doc packages with their relations and cards`; `9993148f` `feat(check): gate doc packages, translations and media on their contract`; `09afc33a` `feat(init): scaffold doc packages and their translations`; `93b0d7ff` `feat(cache): warm a doc package's subjects and source so citations resolve offline`; `153e68d9` `chore(cli): let the pivot read documentation and observe the manual` | J-077; отчёт `296a0259`; два кандидата гейта — хвост в P2-O8; попутно починен `rename_wire` кодгена (обе копии искали `"]` вместо `")]`) |

## Фаза 3 — ранние атомы {#phase-3-early}

Атомы фазы 3, не зависящие от механики, посажены до её конца.

| Атом | Коммит | Что подтвердил или опроверг |
|---|---|---|
| A3.3 | `62c6cd1b` `chore(docs): archive the 1.0.0 alpha layer under docs-legacy` | один коммит без иных изменений; ни один гейт и ни один тест не зависел от пути `docs/` |
| A3.3 хвост | `7f51b229` `docs(readme): point the root guides at the archived tree and the manual` | 7 ссылок README и 1 DEV-GUIDE переключены; раздел Documentation открывается руководством; полная переработка корневых гайдов — A3.12 |
| A3.4 | `bf5eff6a` | регрессионный список — колонка «решение» инвентаря (R-17 закрыт в P.5, как и требовал план) |
| A3.2 | `82620654` (импорт вижена), `0df597b3` (P.2) | шаблон страницы — `AUTHORING.md` §7 пакета (два скелета, prompt-first) и README пакета; отдельного раздела не потребовалось |
| A3.6 | страницы фазы P; `6bd8272d` | `audience` стоит в `<status>` всех 48 страниц, двенадцать несут несколько аудиторий; `vibe doc manifest --json` — 48 страниц; `llms.txt` перечисляет все 48 (тир «For agents» — 8 страниц, «Pages» — 40) в порядке сигнала стабильности P2-O7, не по именам разделов — принято |
| A3.12 | `85de0edc` `docs(guides): point the root guides at the manual and the vibe doc surface` | README: восемь видов, разделы руководства, команды локального чтения, архив одной строкой; RUNTIME-GUIDE: локальный читатель и диагностика вместо пустого troubleshooting; DEV-GUIDE §8 → §2.6 |
| A3.16 | три страницы `.md` отправлены владельцу 2026-09-12 (`start/what-vibevm-is`, `reference/manifest`, `architecture/how-vibe-is-built`, сборка `vibe doc build --format md`) | **стоп-точка**: ждёт чтения вслух; замечания — атомами правки страниц или правкой `STYLE.md` с датой; локальный читатель с оболочкой — после фазы 4 |
| гейт фазы 3 | `0d561a05` стиль; полный прогон 2026-09-12 | `vibe doc check --examples --citations --derived --coverage --media --style --min 100` — **exit 0**: примеры 55 совпали, 7 отложены по `deferred.toml`; цитаты 759, 0 неразрешённых; `derived` 72 без изменений; покрытие 516 из 516, 100 % по 571 паре; стиль 48 из 48 чистых, 119 предупреждений; медиа — 3 плейсхолдера; `--translations` над фикстурой адаптации — проверено тестами P2-O7 (`ff71092a`, `5ad8c7a8`), боевой перевод — волна C; `facts check --exhaustive` clean (376 файлов); регрессионный список закрыт в P.5; очередь «нужен автор» отработана в точке F1. **Открыто только у владельца**: стиль-ревью A3.16 и картинки F-32; панель целиком — после посадки фазы 2 (P2-O10) на чистом дереве |
| A3.13 | `6607378c` M1 (138), `ad95d7f8` M2 (82), `d93f8b3f`+`cb98cf93` M3 (95), `49faa6b2` M4 (131), `647b1ab7` M5 (50) | 496 пометок `actionstage="doc"` к 20 прежним — 516 обязательств; предсказание 8 подтверждено (было 20, из них 0 вне PROP-057); статусный барьер — X-032; ложные срабатывания зоны — `9db1cc33`, `7b102c59` |
| A3.5/A3.9 (частично) | `f81a6b80` | новая страница `model/dependency-visibility` под 24 обязательства PROP-050, которые не цитировала ни одна страница; глоссарий — `effective set`, `friend closure`; регистр корпуса 45/60/408 |
| A3.5, A3.7, A3.9, A3.10, A3.11 («подключить и проверить») | `6bd8272d` `docs(vibevm-docs): cite every obligation the specifications mark` | 404 непокрытых обязательства закрыты за один проход центральной сессии: аудитории двенадцати страниц расширены до всех читателей, которым они служат (справочники, глоссарий, boot-лейн, lifecycle, воркспейс, публикация, страницы агентов); секции получили правила, которые уже пересказывали, с одной-двумя фразами там, где пересказа не было; три новые страницы — `authoring/facts-and-status-markers` (PROP-043), `reference/tree` (PROP-036/037/042/039), `authoring/bridge-a-repository` (PROP-023/021); два снятия раннером; регистр корпуса 48/62/757/20 |

## Фаза 4 — карта коммитов {#phase-4}

| Атом | Коммит | Что подтвердил или опроверг |
|---|---|---|
| A4.1 | `4ccc9438` `feat(web): open the site package under the TypeScript discipline` | группа `org.vibevm.doc`, вид `app`, pnpm-workspace `design/` + `site/`, пины точные и совпадают с lock-файлом; eslint-плагин дисциплины не подключён (решение 1 отчёта); `vibe check --path <пакет>` красный — гейт `boot_directory` требует boot-каталог от всякого пакета, кроме `doc` (X-033, B-145) |
| A4.10 | `3e0efc08` `feat(web): lay down the semantic tokens and both themes`; `90f21883` `feat(web): build the shell components on the tokens` | палитра с источниками построчно, тринадцать семантических имён в трёх блоках, шрифты 476 КБ под OFL; аудит APCA — собственная реализация, 40 гейтуемых пар, 0 ниже порога после минта; минт схлопнул `--text-2`/`--text-3` в один тон и сдвинул тёмный `--accent` с `#D97757` на `#DC8264` — вопросы дизайн-ревью A4.14; тринадцать компонентов без поведения |
| A4.2, A4.9 | `8ed08eca` `feat(web): fix the island contract against the doc manifest` | TypeScript-типы из JTD целью `xtask codegen` (`check-codegen` диффит и их); один код — две конфигурации (`base` `/` и `/doc/`, `routesDir`), остров через `define`; `href()` единственный источник адресов, тест ловит литералы; страницы считаются по манифесту против вывода генератора (3 и 1); шаг панели 50; баг беты Qwik: `<RouterOutlet />` в layout — вечный цикл (X-034) |
| — | отчёт `findings/WORKER-REPORT-P4-O1.md` | инлайн-скрипт темы требует `sha256` в CSP сайта (X-035, атом развёртывания) |
| A4.16 | `2780ba06` `feat(web): port the landing onto the shared design system` | лендинг `/` и `/ru/` на компонентах `design/`, тексты `i18n.ts` дословно, мета-теги один к одному, `og.png` рисует сборка, корневые файлы генерирует `tools/root-files.mjs`; website-id Umami и ключ IndexNow — из окружения сборки, в репозитории пустые умолчания; статическая сборка 10 из 10 страниц; правило годового кеша nginx всё ещё на `/_astro/`, а ассеты теперь в `/assets/` и `/build/` (X-040) |
| A4.17 | `131bdd1d` `test(web): pin landing parity against the Astro build` | 17 общих адресов, 35 осознанных отличий по 14 правилам, 0 необъяснённых; тест поймал два настоящих дефекта до сдачи (`/ru/` с `lang="en"`, двойная шапка) — предсказание 13 подтверждено в обе стороны; скриншоты 1440/390 обеих тем — `findings/P4-O5-shots/` |
| — | отчёт `findings/WORKER-REPORT-P4-O5.md` | два общих файла (`design/src/index.ts`, `tools/build.mjs`) закоммичены со строками соседнего воркера — без них не собирались (J-090) |
| A4.3 | `1325a726` `feat(web): shelve documentation the way the edges rank it`; `a2528cc1` `feat(web): offer the other language and the other version as addresses`; `87bf0dfa` `feat(web): say whose the page is, what cites it and what it quotes`; `040e625b` `feat(web): read the library of editions out of the page manifests`; `68497e29` `feat(web): assemble the documentation shell over the island` | полки и карточки по рёбрам D-19, селектор языка и версий как адреса, мета-блок страницы, библиотека изданий из манифестов, оболочка над островом; полного списка версий нет — его даёт индекс реестра, не манифест (A5.2) |
| A4.11 | `8fe694b0` `feat(web): port the reader behaviours`; `cadc26f1` `fix(web): keep a block's number beside a block that scrolls` | якоря, настройки, режим чтения, возврат к месту, «для агента», `postMessage`; Qwik Router восстанавливает прокрутку сам — закрыто инлайн-скриптом (второй хэш CSP, X-035); `hidden` проигрывал `display` компонента — починено в `base.css`; 29 тестов Playwright зелёные |
| A4.12 | `c8a91c9d` `feat(web): give the page its contents, its tables and its paper` | оглавление, таблицы, лайтбокс, код, печать (живым предпросмотром не проверена); сноски — пивот их не эмитит (X-041) |
| A4.13 | `bc9de5c8` `feat(web): fall back to the source language without losing the reader` | фоллбэк-страницы, плашка, cookie `lang`; фильтр `noindex` из sitemap переехал в `root-files.mjs` (P4-O3, `f8240086`) |
| A4.5 | `8482e33e` `feat(web): publish the agent surfaces beside the pages` | `.md`, `.xml`, четыре тира `llms`, картинки карточки копируются из выхода `vibe doc build` под координату источника с языковым сегментом впереди (D-06), в обоих написаниях версии; страница без адаптации берёт поверхности источника; **все** деревья одной координаты читаются по очереди — `vibe doc build` пишет одну проекцию за прогон (первая версия шага молча теряла две трети поверхности; найдено на настоящем выходе руководства, J-097); `/doc/llms.txt` и `/doc/manifest.json` — каталоги сайта в форме arXiv («N adapted of M» у адаптации), `/doc/resolve.json` и рукописный `/doc/resolve/` с адресом только из таблицы; 164 файла из 7 деревьев на руководстве |
| A4.4 | `f8240086` `feat(web): prerender the site with the SEO contract` | голова страницы по контракту лендинга: canonical **относительный** на `latest` (те же маршруты собираются для локального читателя), `hreflang` со взаимными парами и `x-default`, Open Graph и Twitter Card с превью пакета через `VITE_DOC_MEDIA` (X-042), JSON-LD `TechArticle` + `BreadcrumbList`, тег Umami тем же кодом, что у лендинга; адрес `latest` материализован — на него ссылались переключатель версий, цитата без версии (D-06) и каждый canonical, а страницы не было (J-095): 12 → 18 статических, 8 → 14 встраиваемых; `/doc/sitemap.xml` — индекс по (пакет, язык), только `latest` и никогда фоллбэк, корневой sitemap несёт запись о нём; линтер `tools/lint-links.mjs` — шаг сборки: 740 ссылок, 0 битых, 18 пар `hreflang` взаимны, чужой хост в загружаемом адресе — красный, в цитате — счёт (R-09); `dist/csp.txt` — **47 хэшей на 119 вхождений** при 19 страницах, а не два (X-044); паритет 31 отличие, 0 необъяснённых; e2e 29/29, пол 26 тестов. **Решение центральной сессии:** внутренние ссылки (навигация, полки, `llms.txt` конвейера) остаются на нумерованных адресах — издание читает само себя, `latest` служит canonical, sitemap и цитате без версии. Открыто: `theme-color` парой с `media` (вопрос A4.14), голова во встраиваемой сборке — снята хвостом `39c9c850` `feat(web): keep the public head out of the local reader` (`seo/local.ts`: только `title`, description и три относительных `alternate`; семь публичных тегов отсутствуют структурно, признак — сравнение острова с плейсхолдером в `seo/mode.ts`; node-тест + шаг сборки над байтами `dist-embedded`: 14 из 14 без публичных тегов; пол 29 тестов) и `a3580a72` (опечатка `root-files.mjs`), IndexNow — действие выкладки (A5.6) |
| — | отчёт `findings/WORKER-REPORT-P4-O2.md` | плейсхолдер по координате не показан — манифест не несёт `media` (X-042, хвост P4-O4); скриншоты 1440/390 обеих тем страницы и страницы пакета — `findings/P4-O2-shots/` |
| A4.6 | `0cae55ca` `feat(xtask): embed the docs shell into vibe at release time` | `cargo xtask embed-doc-shell` собирает web-пакет встраиваемым адаптером и кладёт выборку выхода (шаблон, `assets/**`, `build/**`, `favicon.svg`: 129 файлов, 802 900 Б из 144 и 1,6 МБ) в `crates/vibe-doc-shell/shell/` (в `.gitignore`) с индексом `shell.json`; крейт `vibe-doc-shell` включает каталог за фичей `embedded-shell`, `build.rs` экспортирует `VIBE_DOC_SHELL_SHA256`, запасная оболочка без скриптов; пин в `doc-shell.lock`; `vibe doc shell install [--assume-yes]` — читающая половина релизного актива (форма `DOC-SHELL.json`, размер и дайджест, распаковка в `~/.vibe/opt/vibevm/doc-shell/<sha256>/`), пишущая половина — X-022, фаза 5; один тип `Shell` с тремя источниками и `provenance()`; вклад в отладочный `vibe.exe` +827 904 Б (112 801 280 → 113 629 184); `b1ea7057` `fix(doc-shell): refuse a drive prefix in a shell asset name` — дыра, найденная перечитыванием собственного кода (J-102) |
| A4.7 | `dd555de5` `feat(doc): serve the documentation locally with the embedded shell` | сервер вклеивает остров в шаблон на каждый запрос и правит ровно три вещи страницы (`<title>`, три `<link rel="alternate">`, остров); CSP — `sha256` каждого из **семи** инлайн-скриптов оболочки, посчитанных при старте из её байтов, ни одного внешнего источника; только `127.0.0.1` (`netstat`), обход путей 404/400/404; `--base` (по умолчанию `/doc/`), `--lang` с семантикой P2-O8, `--frame-ancestor`; резолвер `<base>resolve?uri=spec://…` → `302` (F-15); `<base>` и `<base><координата>/<версия>/` → `302` на первую страницу; хвост: поле `media` (`icon`, `banner`, `preview`) в `schemas/doc_manifest.jtd.json` + кодген Rust/TypeScript, **необязательное** (Р-2); `/doc/manifest.json` и тиры `llms` отдаются. **Не сделано (P4-O6):** оболочка показывает навигацию и мета-строку фикстуры, с которой собрана, — библиотека страниц вкомпилирована в сборку web-пакета |
| A4.8 | `e9c51b98` `test(doc): pin island parity across shell adapters` | остров статической сборки и остров из `serve` сравниваются байт в байт вычитанием шаблона из отданной страницы; предсказание 6: инструмент стоит, расхождений на момент сдачи 0 — проверится первым изменением оболочки |
| A4.15 | `408b4b97` `test(doc): pin the local reader to loopback resources` | Playwright `site/tests/local-reader.spec.ts` над собранным `vibe` (`VIBE_BIN`): 3 теста, 90 запросов на странице и 178 на двух — единственный хост `127.0.0.1`; статический вывод по `file://` — 1 запрос, сетевых 0; тега аналитики нет; шаг панели после сборки оболочки |
| A4.3, A4.4, A4.7 хвост (P4-O6) | `957e578a` `feat(web): render every edition from the documentation trees`; `630f9d09` `feat(web): read the served manifest in the local reader` | библиотека страниц — из деревьев `VIBE_DOC_OUT` (манифест + остров каждой страницы из `<страница>/index.html`), без деревьев — фикстура как прежде; над руководством: **103 страницы** (2 × (48 + 1) + `/doc/` + 4 лендинга), 96 островов из деревьев, **13 187 ссылок, 0 битых**, 54 пары `hreflang`, `dist` 20 МБ / 497 файлов; линтер получил третий ответ — ссылка, которую написал конвейер: **660 ссылок островов в форме, которой сайт не несёт** (`index.xml` 548, `.xml` спек) → P4-O7; локальный ридер читает `<base>manifest.json` при старте (навигация 48 страниц руководства, Playwright 34/34), ссылки — обычные переходы (X-051 закрыт), `media` разбирается, картинки и страница пакета ждут серверных маршрутов (P4-O7); оболочка перепинована (`doc-shell.lock`, 139 файлов), паритет A4.8 зелёный после первого изменения оболочки; **А-1**: вставка элемента в `<head>` отданной страницы останавливала резюмирование Qwik — локальный ридер до этого атома был страницей без единого поведения, и тест A4.15 этого не видел (J-106) |
| A4.3, A4.4, A4.7 хвост (P4-O7) | `9da13611` `feat(doc): write island links as site addresses`; `b95b07d3` `feat(doc): serve the edition's media and the package page`; `1ce25334` `fix(doc): rewrite the template head in place` | остров пишет адреса сайта: ссылки на страницы — форма каталога с якорем (правило «на один уровень глубже», верное и под языковым сегментом, адрес страницы конвейеру не нужен), цитаты `spec://` — относительный адрес резолвера `resolve?uri=…`, картинки — `media/` издания; над руководством **660 → 0** ссылок «в форме, которой сайт не несёт» и **51 → 0** «цитат за пределы библиотеки» (13 181 ссылка, 0 битых); читатель отдаёт `<base>media/<имя>` (200, svg/png, чужое имя 404, обход 400 — `curl --path-as-is`) и страницу пакета (200 в обоих написаниях, дверь 302 на неё); `retitle`/`relink` переписывают текст и атрибуты на месте — байтовый тест держит последовательность тегов `<head>` (J-109); `vibe doc check --examples --citations --coverage --media --style --min 100` — exit 0; `--derived` — 1 moved (`vibe doc --help` после `build-site`), принято центральной сессией; бинарник дерева возвращён без фичи; хвост `985e2a56` `feat(doc): serve latest as an alias of the served version`: `routes::as_numbered` подменяет `latest` на версию один раз после снятия базы — 200 (не 302) на всех восьми формах, байты совпали с нумерованными, 37 тестов сервера; хвост 2 `3c96b78c` `feat(doc): serve the agent files under the edition too`: пять машинных файлов (`manifest.json`, четыре тира `llms`) отдаются одинаково в корне, под версией и под `latest` (байты сверены), чужое имя — 404 без прыжка, `308` только когда пакет несёт `<адрес>.xml`; 39 тестов сервера, `routes/tests.rs` разрезан по предмету (449 + 185) |
| A4.3, A4.4, A5.3 хвост (P4-O8) | `b1e07385` `feat(web): build one library per source documentation`; `4206e9ff` `feat(web): take the link card from the manifest that names it` (X-055); `c1917fd3` `test(web): refresh the fixture islands from the pipeline` | библиотек столько, сколько исходных документаций: деревья группируются по координате источника, адаптация — к своему источнику; дверь `/doc/` — полка библиотек; каталоги сайта и sitemap-индекс — по записи на издание всех библиотек; **живой прогон над всем реестром: 48 библиотек, 48 изданий, 1237 страниц из 1237, 1136 островов из деревьев, 127 568 ссылок, 0 битых, 621 пара `hreflang`, 49 частей sitemap, выдача 212 МБ / 4511 файлов, повтор — те же числа**; над одним руководством 107 страниц (дерево несёт 50 страниц уровня 0); две библиотеки в фикстуре — 24 страницы, 796 ссылок; e2e 38 + 2 пропуска (запасная оболочка), пол 48 тестов; 490 ссылок страниц уровня 0 в форме, которой сайт не несёт (`descriptor.md`, `VIBEVM-SPEC.md` — X-059); `L-01` остаётся (X-060); введена переменная `VIBE_SITE_DIST` (второй набор библиотек рядом с `dist`) — не просили, разбор в отчёте |
| A4.14 (предварительно) | десять скриншотов (`P4-O2-shots/`, `P4-O5-shots/`) отправлены владельцу 2026-09-12 | **стоп-точка**: страница документации, страница пакета и лендинг в обеих темах, 1440 и 390 px; каталог документации — с фазой 5; вопросы по токенам в review-маркере; итог ревью правит D-21 с датой, смена токенов — один атом после |
| A5.1–A5.5 (P5-O1) | `bc9d99e6` хвост 0 (доктест спрашивал `package` вместо `package[]`, J-107); `a6c71db7` `feat(doc): configure the site from two sources and one table`; `e282ce83` `feat(doc): read the index feed and queue what changed`; `fc488f7d` `feat(doc): render every published version from its own bytes`; `628febc0` `feat(doc): compute shelves, languages and relations from the edges`; `97c9a57e` `feat(doc): recompute latest, canonical, hreflang and the sitemap on every build`; правки по живому прогону `f3f1740a`, `0a0b6fbd`, `4b86c464`; гейт-доктест `74ec32d0` | `vibe doc build-site --config site.toml --out <out>`: два источника конфигурации и одна таблица; лента индекса через `repomd.json` + `primary.jsonl` (`.gz` не читается — X-057), `<out>/.vibe-site/state.json` и деревья `.vibe-site/trees/<координата>/{html,md,xml}` остаются между прогонами; **живой прогон над `github.com/vibespecs` и чекаутом хоста: 48 изданий → 48 отрендерено, 0 отказов, 3476 файлов, 104 МБ, 1 м 10 с; повтор без изменений — 0 перерендеров, 2,5 с**; уровень 0 хоста 126 страниц, руководства 50; полки, языки и связи — по рёбрам при каждом рендере (R-23), `latest`/canonical/`hreflang`/sitemap пересчитываются; сайт над деревьями билдера — 107 страниц, 100 островов из конвейера, 13 725 ссылок, 0 битых; `VIBE_DOC_OUT` ставится из Rust (`join_paths`); **не сделано и почему**: сайт принимает одну исходную документацию (`editionsOf` — «2 source manifests in one library») → P4-O8; полки реестра пусты — опубликованный индекс не несёт рёбер связи и ни одного `doc`-пакета (X-056); `host_url`/`default_theme` — A5.9 (X-058); **требование к P5-O2**: выдача отказывает `/.vibe-site/` |
| A5.7 (P5-O2) | `65cd71ba` `feat(web): ship the serving nginx config for the whole domain` | nginx выдачи на весь домен по образцу лендинга (только чтение, R-28): годовой immutable-кеш — только `/assets/`, `/build/`, `/fonts/` (правило лендинга указывало на `/_astro/`, которого сборка Qwik не пишет — X-040); правило по расширениям снято (год кеша на `og.png` и `favicon.svg` — год старой карточки); `absolute_redirect off` / `port_in_redirect off` **добавлены впервые** — пакет п. 5 ошибочно называл их унаследованными, в `vibevm-org/nginx.conf` их нет; отказ `/.vibe-site`, `q-manifest.json`, `/csp.txt`; CSP из файла сборки и только на HTML; `.md` — `text/markdown`; `add_header` в `location` заменяет унаследованный набор — три заголовка безопасности выписаны в каждом блоке (у лендинга ассеты шли без `nosniff`); гейт по тексту конфигурации `site/src/seo/serving.test.ts` (10 утверждений) |
| A5.9 (P5-O2) | `11ba90f0` `feat(web): reuse the landing's first-party analytics tag` | тег уже был один на обе половины домена (`seo/analytics.ts`); не хватало X-058: `host_url` и `default_theme` теперь едут в сборку окружением (`ENV_NAMES` — пять имён), тест поперёк шва из `web.rs` читает `site/src/config.ts`; `data-host-url` из конфигурации, при пустом — origin; тема по умолчанию подставляется в инлайновый `theme-init.js` с утверждением, а не пробой; при пустом website-id тега нет (живой рендер: 0 страниц с тегом из 815), в embedded-сборке — гейт `PUBLIC_ONLY`; X-058 закрыт |
| A5.6 п. 1–2 (P5-O2) | `885324e9` `feat(web): build and serve the whole site from two containers`; `4d61eeb5` `fix(web): refuse a content policy the server cannot parse`; `1d1cd1a7` `fix(web): leave the builder's own directory out of what the gates measure` | один `Dockerfile` в пять стадий (Node-зависимости, дымовая сборка над фикстурой, Rust без Node, рендерер, стоковый nginx без содержимого), контекст — корень репозитория с allow-list `Dockerfile.dockerignore` (45,2 МБ; `target/` дерева — 248 ГБ), `compose.yaml` с плейсхолдерами имён и порта, том между контейнерами и `service_completed_successfully`, `site.toml` по умолчанию без живых значений; DEV-GUIDE §2.6 и §8.1; **локальная проба всего стека в Docker** (Desktop 4.60.1, движок 29.2.0): 48 версий → 815 страниц, гейт счёта сошёлся; `lint-links` 96 244 / 0 broken; `parity` 31 deliberate / 0 unexplained; `pnpm floor` 77 / 0; заголовки по таблице отчёта (`/en/` → `301 /`, `/doc` → `/doc/`, charset на всех текстовых типах, `/.vibe-site/`, `q-manifest.json`, `/csp.txt` — 404, ассеты — immutable + год, `Location: http://` — 0); холодная сборка 11 мин 04 с (cargo release 392 с), образы 102 МБ выдача / 922 МБ рендерер; **X-044 сработал**: политика всего реестра — 1641 хэш, 87 КБ, nginx не читает параметр от 4096 байт — генератор пишет пустую политику с причиной, домен без CSP, как лендинг до него; форма политики — владельцу; паритет A4.17 и линтер ссылок краснели об кухню `/.vibe-site/` (2636 и 220 ложных) — починено; CSP-фрагмент пишет сборка сайта в `<out>/.vibe-site/csp.conf`, контейнер выдачи берёт его хуком `/docker-entrypoint.d/20-csp-from-render.sh` (отступление О-1 от буквы пакета — следствие тома, принято); резолвер машины не даёт контейнерам маршрутизируемых адресов — временный `buildx`-билдер, снят; кеш-маунт `cargo` судит о свежести по метке времени (`--no-cache-filter vibe-build`, DEV-GUIDE §8.1); уборка: образы, тома, билдер сняты, общий кеш билдера 2,79 ГБ оставлен владельцу; проверка центральной сессией на тёплом `target/`: `cargo fmt --check` чисто, `cargo test -p vibe-cli site::` — 6 passed, 0 failed; `cargo xtask specmap --check` — карта корня отстала от `web.rs`/`tests.rs` (воркер перегенерировал карту web-пакета, не корня), перегенерирована одним коммитом волны; п. 3–7 и A5.8 — только с владельцем (R-24) |
| гейт фазы 5 (локальная часть) | P5-O1 (`findings/WORKER-REPORT-P5-O1.md`), P5-O2 (`findings/WORKER-REPORT-P5-O2.md`) | сайт собирает весь живой реестр (46 пакетов, 48 версий с хостом) без ручных шагов — в контейнере, из `docker/site.toml`; полки официальная/community — механика доказана над подготовленным чекаутом, в опубликованном индексе рёбер связи нет (X-056); повторная сборка без изменений индекса — `queue: 0 to render, 48 unchanged, 0 gone`, 2,5 с (P5-O1); `/doc/` отвечает `200` через полный стек локально (`127.0.0.1:8080`, P5-O2); `https://vibevm.org/doc/` — после выкладки, с владельцем (A5.6 п. 3–7, R-24); A5.10 — X-063 |
| A5.6 п. 3 (стоп-точка) | предложение владельцу 2026-09-12 — файл в scratch сессии, не в репозитории (R-25: имена сервисов, порт и пути сервера — владельца) | по runbook §7 приватного документа инфраструктуры и деплою соседа: compose-фрагмент на место сервиса лендинга (то же имя сервиса и контейнера, тот же порт), рендерер вторым сервисом с двумя томами, приватные `site.toml` с website-id и env с ключом IndexNow рядом с compose, деплой-скрипт по образцу нынешнего, новый чекаут репозитория vibevm рядом со старым (старый — до A5.8); хостовый nginx, порты, сертификаты, соседи — без правок (предсказание 11: ожидание 0); откат — бэкап compose и старый чекаут; вопросы: сам или агент под присмотром, когда, с `main` после слияния или с ветки как превью, тот же website-id, CSP (X-044) до или после; **ничего на сервере до ответа** (R-24, п. 4–7 стоят) |
| A5.6 п. 4–7 (выкладка) | 2026-09-12 21:34 UTC, `1b082751` `fix(web): let the link check pass the analytics script the domain answers`, `docs(web): say how to redeploy the site without downtime`; сервер: бэкапы compose `20260912-210651`, `-213334` | ответы владельца 2026-09-12: я выкладываю по SSH, сразу, ветку `research-preview-1-docs` как превью до его визуальной проверки (слияние в `main` — его словом после неё), тот же website-id, без CSP. Сделано на сервере: чекаут ветки рядом со старым, три приватных файла (конфигурация рендерера с website-id и веткой, ключ IndexNow, деплой-скрипт), в общем compose заменён только блок `vibevm-org` (то же имя, тот же порт) плюс рендерер и два тома; **хостовый nginx, порты, сертификаты, соседи — 0 правок**. Первая сборка на сервере 13 мин 16 с (`cargo build --release`), образы 622 МБ рендерер / 62 МБ выдача. Попытка 1: `up -d vibevm-org` пересоздал контейнер выдачи до ожидания рендерера — домен `502` на время рендера (по бэкапу лендинг вернулся через ~3 мин), а рендер упал: линтер ссылок счёл `src="/u/s.js"` (тег аналитики, впервые с непустым id) битой ссылкой — 814 FAIL, по одной на страницу. Починка `1b082751`: таблица адресов, за которые отвечает домен; локальная сборка с id — `ok 17x /u/s.js`, green. Попытка 2 по деплой-скрипту в порядке «сборка → рендер одноразовым сервисом с `--exit-code-from` → пересоздание выдачи»: сборка из кеша, `queue: 0 to render, 48 unchanged`, 815 страниц, `links: green — 97076 checked, 0 broken`, простой — секунды. Проверка через полный стек (п. 5): `/` и `/doc/` 200 `text/html; charset=utf-8`, `/ru/` 200, `/en/` → `Location: /`, `/doc` → `/doc/`, `llms.txt` корня и `/doc/` `text/plain; charset=utf-8`, оба sitemap 200, ключ-файл 200, `/u/s.js` 200, тег на `/` и `/doc/` с тем же id, `.md` страницы руководства `text/markdown; charset=utf-8`, `/.vibe-site/`, `q-manifest.json`, `/csp.txt` — 404, `Location: http://` — 0; HTTP/2 — с сервера (`curl --http2`), локальный curl без h2. IndexNow (п. 6): 409 адресов из двух sitemap → HTTP 200. A5.8 и слияние — после визуальной проверки владельца |
| гейт фазы 5 (живая часть) | 2026-09-12 | `https://vibevm.org/doc/` отвечает 200 через полный стек, `Location` без `http://`; правок вне контейнеров сайта — 0 (предсказание 11) |
| после выкладки: установщик Windows | `190d4163` `fix(install): read the Windows architecture from the environment`; ассет `install.ps1` релиза v1.0.0 заменён 2026-09-13 07:44 UTC по слову владельца («не бампай версию») | владелец сообщил: `irm https://vibevm.org/install.ps1 \| iex` падает с «не удается найти свойство OSArchitecture». Сайт отдаёт `/install.ps1` редиректом на `releases/latest/download/install.ps1`, ассет байт в байт равен `distribution/install/install.ps1`; скрипт под `Set-StrictMode` читал `[RuntimeInformation]::OSArchitecture`, которого нет у типа, что Windows PowerShell 5.1 находит на .NET Framework старше 4.7.1 (на машине с 4.8 не воспроизводится). Починка: архитектура из `PROCESSOR_ARCHITEW6432`/`PROCESSOR_ARCHITECTURE`; `test-install.ps1` получил реальную пробу цели; офлайн-тесты зелёные в 5.1 и 7.6. Доставка: `cargo xtask dist finalize` отказывает опубликованному релизу, а `prepare` удалил бы v1.0.0 целиком (четыре платформенных бандла не собрать на этой машине), поэтому ассет заменён напрямую через API релизов: загрузка под временным именем → удаление старого → переименование; DISTRIBUTIONS.json установщики не хэширует. Проверка: живой `/install.ps1` идентичен исходнику (12 022 Б), под 5.1 в тестовом режиме `Get-VibeTarget` → `x86_64-pc-windows-msvc`. Тот же вызов в `distribution/release/build-windows-x86_64.ps1` оставлен: скрипт сборки на машине владельца |
| гейт фазы 6 (приёмка §9 без панели) | прогон 2026-09-12 (логи в scratch сессии) | каждая строка §9, кроме последней, вернула 0: `vibe --help` несёт `doc`, `init --help` — восемь видов, словарь — `doc` и `app`, `check-codegen` clean, `wire-diff` green; `vibe doc check` руководства — examples 55 matched / 7 skipped, citations 781 / 0 unresolved, style 49 of 49, media 3 generated, coverage 530 of 530, derived 76 unchanged; фикстура адаптации — источника в store нет и она расходится в одном блоке по замыслу (F-44), гарантию держат тесты крейта `vibe-doc` (фильтр `translation`: 17 passed, в том числе `the_fixture_pair_parts_at_one_block_and_nowhere_else`), строка §9 поправлена (двенадцатая редакция плана); `doc manifest --llms full` и `--json` с `primary`; `progress report --view doc` для user и agent; `facts check exhaustive`; `specmap --check` 0 orphans; `sync-engines --check` 0; `docs-legacy` есть, `docs` нет; README и RUNTIME-GUIDE ссылаются; `cargo xtask mirror --check` — all targets in sync (gitverse, github, `main` @ b1291b0); последняя строка `tools/self-check.sh` — один раз перед слиянием по слову владельца; предсказания §4 — все 13 отвечены (см. таблицу), леджер отложенного закрыт (63 строки), BACKLOG B-153…B-158; открыто у владельца: инспекция, публикация пакета, выкладка |
| после выкладки: POST-O1 (зависание `vibe install` на «Resolving 1 root package…») | `b058098f` `perf(registry): read manifests from GitHub over HTTPS instead of cloning`; `818247cd` `perf(registry): keep one HTTPS client for raw reads and scrub credentials from git errors`; отчёт `findings/WORKER-REPORT-POST-O1.md` | быстрый путь чтения одного файла по HTTPS (таблица хостов: GitHub через raw, GitVerse через contents API — проверено 2026-09-14), промах авторитетен только для тега или SHA (ветка — нет, по слову владельца о git-источниках), токены только заголовком; один клиент на бэкенд — все 243 чтения прогона redbook идут по пулу соединений, фаза разрешения 116 → 90 с на холодном кэше; вычистка userinfo из каждого `GitError` и рендера argv. Находки: один 429 от raw-хоста стоит целого клона (≈20 с, B-161); оставшиеся рукопожатия — `index_client` (B-162). Ревью: код прочитан, 295 тестов зелёные, specmap регенерирован `4dbbbd8c` |
| после выкладки: POST-O2 (`vibe self update` вперёд, `self reinstall`) | `02d586e3` `feat(self): update moves a binary install forward, reinstall refreshes the current one`; `553086de` `docs(spec): bump the PROP-019 surface to r4 for the reinstall verb`; `f13e0929` `docs(vibevm-docs): describe self update and reinstall as two acts`; отчёт `findings/WORKER-REPORT-POST-O2.md` | по слову владельца 2026-09-14: `update` идёт к новейшему выпуску (тот же номер — не no-op: дайджест бандла из манифеста решает, переустанавливать ли, и неизменённый узнаётся до скачивания), `reinstall` перекачивает текущую версию, `install stable` — тот же путь. Живая проба на изолированном корне: `update` — только манифест, `reinstall` — свежий `#3`. Ревью: `##CMD-UPDATE` переписан, `##CMD-REINSTALL` добавлен, `#surface` r3 → r4 с единственным ребром; `--offline` не доходит до vvm — B-160; два теста `tests/vvm.rs` красны только вне дерева (B-157). Руководство: `model/versions`, `start/install-vibe`, `derived` `vibe self --help`, снимок поверхности 1.0.0 |
| волна C (адаптация `vibevm-docs-ru`) | `8a98f5d1`, `df55e9e9`, `6308c231` — 49 страниц; линтер `9c755627` `fix(doc): teach the style linter the grammar of an adaptation` | зеркало 49/49, 0 проблем; стиль 49 из 49 чисто (32 предупреждения); цитаты 782, 0 неразрешённых; `derived` 76 записаны для пакета. Линтер выучил грамматику адаптации: ORDINARY по якорю, «Вы» как написано кроме начала предложения, тире не пайковое в `ru`, ссылка на статью глоссария вводит термин любым текстом, кодовый спан между термином и глоссой. Адреса-примеры в прозе называют пакет адаптации; блок манифеста — собственный манифест пакета (генератор чужих не разрешает). Публикация на сайте — одной пересборкой вместе с W1-O1 |

## Обновления плана стюарда {#steward}

| Ревизия | Что добавлено |
|---|---|
| r1 (2026-09-11) | контекст `8bc32a15-5e0e-4511-ab6d-e125722c4375` для worktree; узлы фаз 0, 1, P, 2–6, слияние, волна C; мандаты M-001…M-013 |
| r2 (2026-09-11) | DOCS-SPIKES принят со свидетельством `campaigns/docs-2026-09/PHASE-0-FINDINGS.md`; current_node → DOCS-CONTRACT |
| r3 (2026-09-12) | DOCS-CONTRACT принят (16 коммитов; A1.1/A1.2 — ратификация при слиянии как открытый пункт свидетельства); DOCS-PROSE активен; current_node → DOCS-PROSE |
| r4 (2026-09-12) | подузлы фазы P: DOCS-ARCHITECTURE, DOCS-FOUNDATIONS, DOCS-ARCHITECTURE-REFERENCE, DOCS-PIPELINE приняты со свидетельствами коммитов разделов; DOCS-WORKFLOWS активен до снятия `expect`; DOCS-PROSE-CHECKS активен (PP-C1…C3, PP-O1) |
| r5 (2026-09-12) | DOCS-PROSE-CHECKS: свидетельства PP-C1 (`435d3aef`, `2f48c888`), PP-C3 (`bf5eff6a`), PP-O1 (`50439d94`); открыто: PP-C2b, PP-O1b, три промпта фазы 2; DOCS-WORKFLOWS: страницы поправлены под продукт (`c79325e7`, `50439d94`) |
| r6 (2026-09-12) | DOCS-PROSE, DOCS-WORKFLOWS, DOCS-PROSE-CHECKS, DOCS-HANDOVER-B приняты (снятия и повторный прогон промптов — раннеру и гарнитуре фазы 2 по слову владельца, J-067; стиль-ревью P.8 — review-маркер); DOCS-MACHINERY активен; current_node → DOCS-MACHINERY |

## Обновления пина Qwik {#qwik-pin}

| Дата | Было → стало | Причина |
|---|---|---|
| 2026-09-11 | — → `@qwik.dev/core`, `@qwik.dev/router` 2.0.0-beta.43; Vite 8.2.1; Node 24.18.0; pnpm 10.33.2 | первый пин по пробе A0.10 |

## Имена краулеров в robots.txt {#crawlers}

| Дата проверки | Провайдер | Источник | Имена |
|---|---|---|---|
| 2026-09-12 | OpenAI | `developers.openai.com/api/docs/bots` | `GPTBot`, `ChatGPT-User`, `OAI-SearchBot` (`OAI-AdsBot` документирован, не включён — рекламы на сайте нет) |
| 2026-09-12 | Anthropic | `support.claude.com` (статья 8896518) | `ClaudeBot`, `Claude-User`, `Claude-SearchBot`; `anthropic-ai` и `Claude-Web` документацией не названы — сняты (попадают под `User-agent: *`) |
| 2026-09-12 | Google | `developers.google.com/search/docs/crawling-indexing/google-common-crawlers` | `Google-Extended` и набор Googlebot |
| 2026-09-12 | Perplexity | `docs.perplexity.ai/guides/bots` | `PerplexityBot`, `Perplexity-User` |
| 2026-09-12 | Common Crawl | `commoncrawl.org/ccbot` | `CCBot` |
| 2026-09-12 | Apple | `support.apple.com/en-us/119829` (по цитатам в поиске: прямая выборка трижды оборвана) | `Applebot`, `Applebot-Extended` |
| 2026-09-12 | Amazon | `developer.amazon.com/amazonbot` | `Amazonbot`, `Amzn-SearchBot`, `Amzn-User` |
| 2026-09-12 | Meta | `developers.facebook.com/docs/sharing/webmasters/web-crawlers/` | `meta-externalagent`, `meta-externalfetcher` |
| 2026-09-12 | Yandex | `yandex.com/support/webmaster/robot-workings/check-yandex-robots.html` | `YandexBot` |

## Покрытие обязательств {#coverage}

| Дата | Аудитория | Обязательств | Покрыто |
|---|---|---|---|
| 2026-09-12 (разметка A3.13, до цитат) | user / author / dev / agent | 307 / 217 / 5 / 40 (514 фактов) | 107 / 55 / 0 / 3 — предпросмотр скриптом `coverage_preview` по тем же правилам, что A2.16 |
| 2026-09-12 (после `6bd8272d`) | user / author / dev / agent | 307 / 217 / 5 / 40 | 307 / 217 / 5 / 40 — 100 % по предпросмотру; гейт `vibe doc check --coverage --min 100` подтверждает после посадки P2-O8 |

## Отставание переводов {#translations}

Не измеряется по замыслу (D-18, D-27): проверяется только структура.

## Фаза 6 — карта коммитов {#phase-6}

| Атом | Коммит | Что сделано |
|---|---|---|
| A6.1 | `401fa6c9` `docs(vibevm-docs): consolidate the campaign journal` | 110 записей журнала кампании сведены в `JOURNAL.md` пакета руководства (сортировка, десять перекрёстных ссылок, четыре записи сокращены для публикации, четыре графы «→ регламент» заполнены, таблица находок фазы 0 приложена); зона заморожена на J-110 с указателем; `vibe doc todo` без флага — `findings_without_decision` 0; `vibe check` пакета clean |
| A6.2 (репетиция без владельца) | `8fa5902a`, `4925415c`, `c494683c`, `063741dd`, `a1e45597` (пять мелких правок); страница недели и `reviews.toml` — два коммита после них | очередь снята (`vibe doc todo --format md`): пробелы 0, стиль 119 предупреждений, 48 страниц без чтения; пять самых длинных фраз коридоров разрезаны (119 → 114), по коммиту на правку; страница недели `start/what-vibevm-is` прочитана вслух центральной сессией, два спотыкания исправлены; `maintenance/reviews.toml` заведён; 25 минут (J-112); чтение владельцем — стоп-точка A3.16 |
| A6.3 (репетиция без владельца) | аудит P6-C1 (`findings/P6-C1-corpus-audit.md`), правки и долг — коммиты 2026-09-12 | восемь метрик; аудит корпуса: команды верхнего уровня 0 без страницы, 25 полей без строки (B-151), 45 подкоманд без прозы (B-152), навигация по закону слоёв прячет маршрут новичка (B-150, X-061), синонимы `manual`/`documentation` закреплены статьёй глоссария, 35 страниц без прямой ссылки — «After the route» получил ссылки; бюджеты `llms` без числа (X-062); долг `docs:` — три строки P2/P3; чтение трёх страниц владельцем и релиз — стоп-точки; 40 минут |
| A6.5 (первая сверка, механическая половина + перечитывание) | коммиты 2026-09-12 `docs(vibevm-docs): …` (13 правок), `reviews.toml` 49 из 49 | все 49 страниц перечитаны против текущего продукта центральной сессией (J-115); примеры и промпты — см. строку гейта ниже; снимок поверхности — после прогона примеров; чтение владельцем и публикация — стоп-точки |
| гейт фазы 6 (сверка) | коммиты закрытия сверки 2026-09-12 | гейт документации против `target/release/vibe.exe` — exit 0: примеры 55 совпали, 7 отложены; цитаты 781, 0 неразрешённых; покрытие 530 из 530; стиль 49 из 49; медиа 3 плейсхолдера; derived 76 без изменений; снимок поверхности 1.0.0; `reviews.toml` 49 из 49 + `reconciled`; `maintenance/reconcile.md`; открыто у владельца: три страницы вслух (A3.16/A6.3), публикация пакета (A6.5 п. 5), выкладка (A5.6 п. 3–7) |
| A6.4 | `4d660290` `docs(spec): adopt the documentation maintenance protocol` (PROP-058); `92949344` `docs(vibevm-docs): explain how the manual is maintained`; `2dd71954` карта; импорт `MAINTENANCE.md` как design-документа — P6-C2 | PROP-058 «Documentation maintenance»: 13 секций, каждое правило цитирует `J-NNN`, гипотезы помечены и держат `spec/work` (месячная петля, полная сверка, правило пяти правок, сигналы сайта и агентов, квартальная чистка); 13 обязательств `dev` процитированы новой страницей `architecture/how-this-manual-is-maintained` (аудитория `dev,author`, `derived` `vibe doc todo --help`); чеклисты `maintenance/weekly.md`, `monthly.md`, `release.md`; `CHANGELOG.md` с записью 0.1.0; корпус 49 страниц, 781 цитата, 73 `derived`; `facts check --exhaustive` clean (383 файла), `vibe check` clean, стиль 49 из 49, покрытие 530 из 530 (dev 19 из 19), цитаты 781 без неразрешённых; открытые вопросы владельцу — §12 PROP-058 |

## Предсказания {#predictions}

| № | Статус | Число |
|---|---|---|
| 10 | подтверждено 2026-09-11 (A0.23) | `--faint #8E8D85` на `#FAF9F5` — Lc 57.15 при пороге 60 |
| 1 | опровергнуто 2026-09-12 (A2.1) | красных исчерпывающих матчей после добавления видов — **три**, не пять крейтов: `kind.rs`, `scanner/manifest.rs`, два матча в `extensions.rs`; wire-словарь матчит по открытому типу и не покраснел (отчёт P2-O1) |
| 2 | подтверждено 2026-09-12 (A2.2) | break-заметки нет и не требуется: `formats/EPOCHS.toml` `public = false`, `break_window_open = true` → вердикт `wire-diff` green/REPORTING; корпуса пополнены отдельно (`005ee978`, J-072) |
| 3 | опровергнуто 2026-09-12 (A2.9) | красных только из-за нормализации — **0**: 38 из 39 снятых `expect` совпали с первого прогона, цепочка `crlf → ansi → paths → slashes` построена в раннере заранее (отчёт P2-O4); дорогими оказались фикстуры и трипвайр раннера (X-039), а не нормализация |
| 6 | не подтвердилось при первом изменении оболочки 2026-09-12 (P4-O6) | тест A4.8 сравнивает остров вычитанием шаблона из отданной страницы, а не подстрокой; после перепиновки оболочки (139 файлов вместо 129) паритет остался зелёным — 0 расхождений |
| 8 | подтверждено 2026-09-12 (A3.13) | до разметки — 20 обязательств, все внутри PROP-057 и 0 в остальном корпусе; после разметки — 516 |
| 4 | опровергнуто 2026-09-12 (PP-C3, A3.4) | мера — по утверждениям, не по страницам: из **1184** утверждений legacy устарело (поведения нет) **80**, то есть 6,8 %; ещё 54 «нужен автор», 1050 перенесены в руководство или справочник; трети не набралось и близко |
| 5 | не наступило на 2026-09-12 (PP-C1, A2.11, сверка A6.5) | копий текста нормы вместо цитаты — **0**: проверка 385 цитат (PP-C1), проверка якорей (781 правило, 0 неразрешённых) и перечитывание 49 страниц не зафиксировали ни одной; таблицы страниц несут `rule` рядом с собой; предсказание остаётся триггером первой недельной петли |
| 7 | опровергнуто на конец кампании 2026-09-12 | пин `@qwik.dev/core` и `@qwik.dev/router` 2.0.0-beta.43 от 2026-09-11 не менялся: переименований пакетов и API — **0** (журнал пина — одна строка); кампания шла четыре дня, так что это ответ на короткий срок — триггер D-12 остаётся при следующем пересмотре пина |
| 9 | перенесено в волну C (D-29) | проверяется по датам чтения в `reviews.toml`, не по ревизиям (D-27); на 2026-09-12 — 49 из 49 страниц с датой чтения, адаптаций в реестре 0 |
| 11 | подтверждено 2026-09-12 (A5.6 п. 4–7) | правок хостового nginx, портов и сертификатов — **0**: сервис выдачи встал на место сервиса лендинга с тем же именем и портом, `HTTP/2 200` на `/` и `/doc/` через полный стек; изменения на сервере — только блок нашего сервиса в compose, чекаут, три приватных файла |
| 12 | ждёт волны C | в волне B — только фикстура `fixtures/doc/adaptation-ru`: расхождений структуры **0** (`--translations`, скрипт приёмки §9); первая настоящая адаптация ответит по блокам |
| 13 | подтверждено 2026-09-12 (A4.17, `131bdd1d`) | первый отчёт теста паритета поймал два настоящих дефекта: `/ru/` с `lang="en"` — невидимое, и двойную шапку — видимое; 35 осознанных отличий по 14 правилам, 0 необъяснённых; при первом направлении на выход деплоя (P5-O2) — 2636 ложных из-за кухни `/.vibe-site/`, ни одного в словах |
| остальные | открыты | — |

## Review-маркеры, открытые для владельца {#review}

| Где | Вопрос | Статус OPEN/RESOLVED, рулинг дословно |
|---|---|---|
| `VIBEVM-SPEC.md` §4.1 (A1.1) | Принять поправку реестра видов (текст ниже) | OPEN — закоммичено `ed75ce00` под целью сессии владельца с пометкой «pending ratification»; ратифицировать или откатить при слиянии |
| PROP-000 `KIND-SET`, `INV-VOCABULARY` (A1.2) | Расширить набор до восьми видов тем же коммитом | OPEN — в `ed75ce00`, вместе с A1.1 |
| P.8 стиль-ревью | Три страницы вслух до передачи в волну B; предлагаются `start/what-vibevm-is` (концепт), `howto/work-offline` (сценарий с промптом), `authoring/write-a-flow` (авторская) | OPEN — под целью сессии волна B не ждёт; замечания владельца правятся в точке F1 |
| F-05 | Поднять `vibe` до 1.1.0 в A2.1 и писать `min_vibe_version = "1.1.0"` в doc-пакетах (рекомендация) | OPEN |
| F-35 | Адреса: `https://vibevm.org/doc/`, хост `github.com/vibevm/vibevm` (`main`), реестр `github.com/vibespecs` | OPEN |
| D-23 / F-41 | Кто выполняет серверные шаги переключения: владелец по чеклисту или агент по OpenSSH с подтверждением каждого шага | OPEN |
| D-16 | Частота опроса хоста: раз в час, дебаунс 5 мин, dev-профиль рендерера (рекомендация) | OPEN |
| X-017 | P1 в `vibe-index` — уведомление; чинить вне кампании или внутри | OPEN |
| корпус `index/e1` (J-069) | Байты gzip зависят от унификации фич Cargo: A — сменить корневую фичу `zip` на `deflate-flate2` (miniz_oxide), C — прибить реализацию deflate внутри `vibe-index` (настоящая починка «same input, same bytes»); B (перемонтировать голден) не работает. Рекомендация — C; до решения шаг 6d панели красный | OPEN |
| PROP-057 `REL-FIELD-PLACEMENT` | Таблицы связи и `[media]` — верхний уровень манифеста; `title`, `abstract` — в `[package]` (рекомендация, вижен §10 п. 4) | OPEN |
| PROP-057 §12 | Механизм встраивания оболочки (восемь пунктов) — подтвердить; реализовано P4-O4 как предложено, четыре расхождения (отчёт, раздел «Расхождения»): **Р-1** `style-src 'self' 'unsafe-inline'` под настоящей оболочкой (каркас инлайнит стили компонентов, настройки чтения пишут `style`-атрибут, который хэш не покрывает; публичная сборка пришла к тому же) — правка `##LOCAL-CSP` с причиной или снятие инлайн-стилей в адаптере; **Р-2** поле `media` необязательное (`foreign_parsers = "many"`, PROP-044 §4.4; обязательное уронило бы `tsc` web-пакета); **Р-3** оболочка кладётся выборкой выхода, не целиком (исполнение, не пересмотр); **Р-4** релизный актив не производится (R-24, X-022) | OPEN |
| X-005, X-015 | Мелкое: `vibevm/vibedeps/.gitignore` в истории; фирменное `og.png` | OPEN |
| A4.14 / D-21 | Дизайн-ревью: минт по порогу APCA схлопнул `--text-2` и `--text-3` в один тон (светлая `#4F4E49`, тёмная `#BEB9B1`/`#BDB9B1`) и сдвинул тёмный `--accent` с `#D97757` на `#DC8264`; варианты: оставить как есть, или задать `--text-3` порог 45 (подписи, декоративный уровень) и вернуть акцент, гейтуя его только на `--bg` (рекомендация: второе — один акцент на обе темы был решением D-21) | OPEN; **третий вопрос (из P4-O3):** `theme-color` на страницах документации — одно значение тёмной краски бренда на обе темы, как у лендинга по эталону, или пара тегов с `media="(prefers-color-scheme: …)"`, следующая теме читателя; рекомендация центральной сессии — пара на страницах документации, лендинг без изменений (паритет) |
| F-32 / A3.15 | Картинки ядра: заказать `icon.png`, `banner.jpg`, `preview.png` в лимитах D-20 или сознательно оставить сгенерированные плейсхолдеры A2.17 (рекомендация: плейсхолдеры до дизайн-ревью A4.14, картинки — одним атомом после него) | OPEN |
| X-032 | Статусный барьер: восемь узловых фактов PROP-050, весь `vibe clean` (PROP-053), два факта PROP-010 и семь у M3 носят не-`done` статус при поставленном коде; актуализация статусов — атом кампании фактов по свидетельству кода, после него второй проход разметки | OPEN |

### Предлагаемый текст поправки `VIBEVM-SPEC.md` §4.1 (A1.1) {#a1-1-text}

Заменить первую фразу раздела «vibevm packages come in six kinds» на «eight
kinds», снять фразу «`app` — installable, runnable graphical applications — is
anticipated as a future kind and deliberately not yet specified», после
абзаца об амendment 2026-08-06 добавить:

> *Amendment, owner ruling of 2026-09-11 (recorded on his instruction): `doc`
> and `app` are added as the seventh and eighth kinds. The register was six
> until that date. Normative spec: PROP-057.*

и после определения **`mcp`** добавить два определения:

> **`doc`** — Documentation as a package. A `doc` package documents one or
> more other packages (its *subjects*, named in `[[documents]]`), carries a
> human-readable `title` and `abstract`, and is read, never executed: it may
> not declare a boot snippet, an MCP server or a binary, `vibe install`
> refuses it, and `vibe cache add` warms it into the machine store for the
> local reader, the site and the agent skill. Its translations are separate
> `doc` packages, one per language. Examples: `org.vibevm.core/vibevm-docs`,
> `org.vibevm.world/multi-user-planning-docs`. Normative spec: PROP-057.
>
> **`app`** — A standalone product with its own deployment profile in the
> build, package and deploy planes of PROP-054. The boundary with `tool` is
> mechanical: a `tool` lives in a project and runs through `vibe bin exec`
> by the lock file; an `app` runs in no consumer project and is built and
> deployed on its own. Example: `org.vibevm.doc/web`, the documentation
> site. Normative spec: PROP-057.

Той же правкой в `PROP-000` §6 `KIND-SET`: «`kind ∈ {flow, feat, stack,
tool, mcp, lang, doc, app}` — eight kinds; `mcp` shipped with PROP-027, `doc`
and `app` admitted by PROP-057 (owner amendment 2026-09-11)»; в
`INV-VOCABULARY`: «The installable kinds are `flow`, `feat`, `stack`, `tool`,
`mcp`, `lang`, `doc`, `app`; this set is exact and grows only by owner
amendment to `VIBEVM-SPEC.md` §4.1 (`doc` and `app` admitted 2026-09-11)».
