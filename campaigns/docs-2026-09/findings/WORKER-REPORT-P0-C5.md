# WORKER-REPORT P0-C5 (спайк A0.22 — инвентарь ридера oleg.guru)
Дата: 2026-09-11
Дерево: b1291b06

## Решения
- Прочитан `findings/PACKET-COMMON.md` первым и целиком (клауза субагента, запреты, форма находки/отчёта).
- Далее — только файлы, названные в пакете (`article.html.tpl`, `podcast-episode.html.tpl:1219-1375`, `build-articles.mjs:377-421`, `theme.js`, `lang-fallback.js`, первые 60 строк `lightbox.js`, разделы `CLAUDE.md`, design-system `ui.js`). Boot-лейн репозитория (`vibevm/vibespecs/boot/**`, `CLAUDE.md`/`AGENTS.md`/`GEMINI.md` хоста) не читался — по явному запрету пакета пакет считается всей инструкцией.
- Диапазоны строк, заданные пакетом, проверены по содержимому перед чтением (`grep -n`): оба (`podcast-episode.html.tpl:1219`, `build-articles.mjs:377`) совпали без сдвига.
- Находка `findings/A0.22-reader-inventory.md` оформлена как таблица из 10 строк (по числу фич) + таблица констант + раздел внешних зависимостей + REVIEW + открытые вопросы, по форме из PACKET-COMMON.md.
- Во всех командах самопроверки использованы абсолютные пути (не относительные), чтобы не зависеть от того, какой каталог считается текущим (рабочее дерево пакета — `vibevm-docs`, а результаты пишутся в `vibe-docs-vision`).

## Отклонения (с причинами)
- Сверх названных в пакете файлов сделано **четыре точечных `grep`** внутри того же read-only каталога `oleg-guru` (R-28 запрещает только запись, не чтение), чтобы проверить факты, которые задание прямо просит сверить «по коду», но источник которых пакет не назвал:
  1. константа времени чтения 200/250 слов/мин — найдена в `build-articles.mjs:198-208` (вне диапазона 377-421);
  2. состав полей мета-блока (`metaRows`) — `build-articles.mjs:509-528`, чтобы отличить «есть в источнике» от «проектируется заново» для фичи 8;
  3. наличие `@media print` — `grep -rl` по `scripts/templates/` и `src/`, пусто;
  4. наличие плавающей кнопки «для агента» — `grep -rln` по `scripts/templates/`, пусто (плюс сопутствующий `llms.txt`-контекст сайта).
  Без этого фичи 8/9/10 и константа времени чтения остались бы непроверенными, хотя задание явно требует их сверки. Дальше по дереву `oleg-guru`, `vibevm-org` и design-system каталогу целенаправленно не ходил.
- Ничего не дописывал сверх формы находки (например, не стал читать CSS `.meta-group`/`.timestamps` для ширины оглавления или остаток `lightbox.js` за 60-й строкой) — это осталось в «Открытых вопросах» находки, а не превратилось в самостоятельное расширение списка файлов.

## Вывод самопроверки (дословно)

Команда: `git -C C:/Users/olegc/git/oleg-guru status --short | head -5` — **до** записи находки:
```
 M public/llms-full.txt
 M scripts/build-llms-full.mjs
```
Та же команда **после**:
```
 M public/llms-full.txt
 M scripts/build-llms-full.mjs
```
Идентично, exit 0. Каталог `oleg-guru` не тронут (это чужие незакоммиченные файлы, не мои — не трогал).

Команда: `ls -la C:/Users/olegc/git/v/vibe-docs-vision/findings/A0.22-reader-inventory.md`:
```
-rw-r--r-- 1 olegc 197121 20986 Sep 11 23:11 /c/Users/olegc/git/v/vibe-docs-vision/findings/A0.22-reader-inventory.md
```
exit 0, файл существует. Подсчёт строк таблицы фич (`grep -cE '^\| ?[0-9]+ \|'`): **10**.

Команда: `git -C C:/Users/olegc/git/v/vibevm-docs status --short`:
```
 M .claude/agents/opus5.md
```
exit 0 — пусто кроме `.claude/agents/opus5.md`, как и предполагалось (не моё). Отмечу для полноты: при самой первой (базовой, до записи находки) проверке в этом дереве также встречался нетронутый файл `?? vibevm/vibedeps/.gitignore`, которого в финальной проверке уже нет — это дерево общее для сессий-соседей (см. окружение: git worktree, стек стэша общий), само задание никаких команд `git add/commit/stash/checkout/restore` не выполняло ни разу (только читающие `status`/`rev-parse`), так что это не результат этой задачи.

## Что не сделано и почему
Четыре пункта остались в «Открытых вопросах» находки, потому что их источник не входит в список файлов пакета, и задание не просило точку в них ставить дополнительным поиском по всему репозиторию:
- сохранение URL-фрагмента при переключении языка (фича 2) — не найдено ни в `buildLangSwitch`, ни в двух прочитанных инлайн-скриптах;
- ширина/sticky-позиционирование оглавления на десктопе (фича 6) — CSS `.meta-group`/`.timestamps` не входил в список;
- кнопка копирования кода с подписью языка (часть фичи 7) — не встречена в первых 60 строках `lightbox.js`;
- «прокручиваемый контейнер» таблиц (часть фичи 7) — подтверждено только текстом докблока, не CSS.

Дефектов пакета (отсутствующих правил) не встречено — во всех случаях выше речь о конкретных файлах вне списка, а не об отсутствующем правиле игры.
