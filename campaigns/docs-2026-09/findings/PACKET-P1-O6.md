# PACKET-P1-O6 — хвост панели после фазы 2: `cli_search`, литералы `7`, полный прогон

##subagent-quiet-clause

Ты — воркер кампании документации, исполнитель уровня `opus5`. Boot-лейн
репозитория не читать. Твоя инструкция — этот файл и ровно те файлы,
которые он называет. Предусловие: в дереве нет других работающих воркеров
(центральная сессия запускает этот пакет последним) — проверь `git status
--short`: только твои файлы.

## Читать сначала

1. Этот пакет целиком.
2. `campaigns/docs-2026-09/findings/WORKER-REPORT-P1-O5.md` §7 (16 фикстур с
   литералом `7`) и раздел про красные цели; `WORKER-REPORT-P1-O4.md` §3
   (корпус `index/e1` — перелом провода, **не чинить**, владелец решает).
3. `crates/vibe-cli/tests/cli_search.rs` — тесты
   `search_without_full_scan_keeps_unconfigured_status` и
   `search_full_scan_finds_matching_packages_in_github_org`: выяснить, что
   изменилось (поля конверта `search`, реестр `local` в фикстуре, сеть) и
   починить по существу — тест, если он устарел относительно контракта
   (назови контракт по `spec://`), продукт — если контракт нарушен.
4. `tools/self-check.sh` — все 47 шагов.
5. Правила репозитория: Conventional Commits с телом «почему»; атом —
   коммит; **коммитить только явной формой `git commit -m … -- <свои пути>`**;
   **никаких трейлеров и упоминаний моделей**; `cargo fmt --all`; ничего
   вне названных мест не менять; секреты и `infra/` не читать; `git push`
   не делать.

## Цель — до трёх коммитов

1. `cli_search` ×2 — зелёные по существу. Коммит:
   `fix(cli): …` или `test(cli): …` по тому, что оказалось причиной.
2. 16 фикстур с литералом `7` → `CURRENT_SCHEMA_VERSION` (кроме тех, что
   тестируют чужую схему намеренно — назвать). Коммит:
   `test: take the lock schema version from the constant in the remaining fixtures`.
3. `bash tools/self-check.sh` целиком в общем дереве: ожидание — красным
   остаётся только `index/e1` (`golden_corpus` на шаге 5 и `wire-diff` на
   шаге 6d). Если панель падает раньше или на другом — починить, если это
   в периметре тестов и clippy; иначе записать шаг и вывод и остановиться.
   Если шаги 5 и 6d — единственные красные, панель до конца не дойдёт:
   тогда прогони каждый шаг после 5 отдельно его же командой из скрипта и
   приложи вывод. Коммитов на этом пункте нет.

## Результат

`campaigns/docs-2026-09/findings/WORKER-REPORT-P1-O6.md`: причина каждого
красного и что сделано; хвост панели дословно; список шагов, прогнанных
отдельно, с кодами выхода; `git status --short` в конце.
