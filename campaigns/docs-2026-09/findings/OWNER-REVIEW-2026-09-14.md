# Замечания владельца к сайту — 2026-09-14, разбор и распределение

Шестнадцать пунктов после первого просмотра выложенного сайта. Здесь — что решено, кем делается и в каком
пакете; решения по дизайну и данным — центральной сессии, код — воркерам `opus5`, проза — центральной сессии.

| № | Замечание (кратко) | Решение | Где |
|---|---|---|---|
| 0 | Перевести весь официальный гайд vibevm на хороший русский | волна C: адаптация как отдельный doc-пакет `org.vibevm.core/vibevm-docs-ru` по PROP-057 `LOC-*`, блок в блок, проверка `--translations`; проза — центральная сессия | прозa, после раздачи кодовых пакетов |
| 1 | Версия всех пакетов — 1.0.0; не бампается, а перезаписывается (до отдельного решения) | `vibevm-docs` и `org.vibevm.doc/web` → `v1.0.0` (каталоги, манифесты, пути в Dockerfile/compose/nginx/DEV-GUIDE/тестах, `CHANGELOG`); политика «перезаписывать, не бампать» — фактами в PROP-057/058 (центральная сессия); серверный compose — при ближайшей выкладке | W1-O2 (Rust+web) |
| 2 | Кнопка копирования в буфер у строк установки на главной | пиктограмма рядом с каждой командой, без внешних ресурсов, с i18n | W1-O1 |
| 3 | Селектор темы в правом верхнем углу главной; тема по умолчанию тёмная | тот же переключатель, что на страницах документации; умолчание `dark` в конфигурации сайта и в web-пакете (выбор читателя сильнее) | W1-O1 (+ серверный `site.toml` — центральная сессия) |
| 4 | На `/doc` три вкладки-фильтра: featured, documents, projections | featured — из конфигурации сайта (п. 6); projections — уровень 0 (проекции спецификаций из байтов пакетов); documents — doc-пакеты, featured входят | W1-O4 |
| 5 | Бейдж GENERATED у сгенерированных документов; выпадашка all / human-authored / ai-generated; все существующие — ai-generated | два разных признака: **проекция** (сделана конвейером из исходников) — бейдж `GENERATED`; **авторство прозы** — поле `authorship` в манифесте doc-пакета (`human` \| `ai` \| `mixed`), фильтр в выпадашке; `vibevm-docs` получает `authorship = "ai"` по слову владельца. Напряжение с законом атрибуции репозитория (коммиты и артефакты — человеческие) названо владельцу; поле — метаданные документа, не атрибуция коммитов | W1-O3 (схема + web) |
| 6 | Где хранить projection/generated и featured | projection — свойство издания, которое знает сам билдер (уровень 0 против doc-пакета), в манифест не пишется; `authorship` — в манифесте doc-пакета; featured — в `site.toml`: `[site] featured = ["org.vibevm.core/vibevm", "org.vibevm.core/vibevm-docs"]` (координаты без версии) | W1-O3 |
| 7 | На странице каждого руководства — выбор версии | селектор версий всегда виден (и при одной версии), рядом с языком документации | W1-O4 |
| 8 | Припинить «What is VibeVM» и «Newcomer's Path» к верху руководства | свойство документации, не сайта: `[navigation] pinned = ["start/what-vibevm-is", "start/index"]` в манифесте doc-пакета (пути документов); сайт и локальный читатель показывают их первыми, порядок остальных — прежний (закон слоёв) | W1-O3 |
| 9 | Поиск в правом верхнем углу не работает | разобрать и починить: клиентский поиск по манифестам библиотеки (заголовки, аннотации, страницы) без внешних ресурсов | W1-O1 |
| 10 | Иконки на плашках — на уровне названия (десктоп) | правка карточки в дизайн-системе; на узких экранах — как было | W1-O1 |
| 11 | Иконки отражают суть плашки (flow → flow и т. д.) | набор глифов по видам пакетов рисует центральная сессия (SVG-символы), воркер вешает по `kind` | W1-O5 (после глифов) |
| 12 | У бридж-пакетов: Bridge Maintainer (из `author` бриджа) и Destination Author | из полей бриджевого происхождения манифеста (`[bridge]` — identity пакета против upstream); в карточке и шапке страницы пакета | W1-O3 |
| 13 | «What it covers» на страницах внутри проекта показывает аннотацию руководства, а не страницы | ошибка в карточках полки «Pages»: передаётся `view.abstract`; должна быть аннотация страницы из манифеста, без блока, если её нет | W1-O1 |
| 14 | Элементы управления чтением внизу страницы перекрывают поиск и выбор языка | ошибка слоёв/позиционирования; исправить, снять скриншоты до/после | W1-O1 |
| 15 | Язык сайта ≠ язык документации | в правом верхнем углу на всех страницах — язык интерфейса сайта; на страницах документации (индексы и документы) — отдельный фильтр English / Russian / Everything | W1-O4 |

Порядок: W1-O1 (web, сразу) → W1-O4 (web, после W1-O1: те же файлы) → W1-O2 и W1-O3 (Rust, по мере
освобождения слотов после POST-O1/O2) → W1-O5 (глифы). Волна C идёт параллельно, руками центральной сессии.
