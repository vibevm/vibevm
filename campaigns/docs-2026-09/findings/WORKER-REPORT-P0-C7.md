# WORKER-REPORT-P0-C7 (записан центральной сессией)

Воркер (Sonnet, `general-purpose`) выполнил чтение и анализ полностью, но **не создал** файлы находки и отчёта: сослался на инструкцию харнесса «не писать report/findings .md файлы, отдавать находки текстом в финальном сообщении» и вернул полный текст находки в ответе. Центральная сессия сохранила текст дословно в `A0.21-index-card-fields.md`.

Решения воркера: сравнение вариантов (а) сайт по `primary.jsonl` и (б) маршрут индекса по образцу `by-cap` сделано по коду; открытые вопросы по `lang`, `translates`/`translations` вынесены, а не додуманы.

Отклонения: файлы не созданы (см. выше). Самопроверка `ls findings/...` не выполнена по той же причине; `git status --short` выполнен: ` M .claude/agents/opus5.md`, `?? vibevm/vibedeps/.gitignore` (второе — не воркером).

Расход: 148 282 токена, 25 вызовов инструментов, ~10 минут.

Урок для регламента (→ JOURNAL J-024): клауза «деливерабл — только артефакты» конфликтует с харнесс-инструкцией некоторых агентов «результат — текст ответа»; центральная сессия принимает оба формата и сохраняет текст сама; в пакетах для `general-purpose` с `model: sonnet` стоит явно писать «если твой харнесс запрещает файлы отчёта — верни находку текстом в требуемой форме».
