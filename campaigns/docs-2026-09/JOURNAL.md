# Журнал кампании документации {#root}

<status stage="spec" state="plan" comment="журнал успехов, неудач, находок и наблюдений; ведётся с 2026-09-10 по MAINTENANCE.md §4; в зоне кампании с 2026-09-11 (фаза 1), после кампании — в пакет vibevm-docs; только дописывается"/>

Правила: запись в том же атоме, где случилось событие; тип — `успех`,
`неудача`, `находка`, `наблюдение`; поле «→ регламент» пусто не дольше
месячной петли; правило регламента без ссылки на запись — гипотеза.

| Id | Дата | Тип | Что случилось | Свидетельство | → регламент |
|---|---|---|---|---|---|
| J-001 | 2026-09-09 | находка | Установленный `vibe` 1.0.0 из PATH не знает команд lifecycle (`build`, `package`, `deploy`, `publish`, `scrape`, `extensions`); отладочная сборка из исходников знает | `vibe --help` против `target/debug/vibe.exe --help` | справочники `derived` берут `--help` из той сборки, которую документируют; полная сверка собирает документацию против релизного бинарника (MAINTENANCE §2.4) |
| J-002 | 2026-09-10 | находка | У oleg.guru номера абзацев считаются на клиенте и не попадают в текстовые проекции; для документации нумерация перенесена на сборку | `article.html.tpl`, скрипт «Paragraph anchors»; VISION D-22 п. 1 | номера стабильны в пределах версии; ссылка «для агента» всегда с версией; полная сверка проверяет, что номера не сдвинулись без смены версии |
| J-003 | 2026-09-10 | находка | Документ инфраструктуры приватен и несёт директиву о VPN: любая правка хостового nginx, портов, сертификатов — стоп до действия | `infra/main/main.md`, начало; план R-24 | деплой документации в любой петле идёт только через контейнеры сайта; чеклист сверки содержит «ничего вне контейнеров» |
| J-004 | 2026-09-10 | находка | Корневые файлы домена (`robots.txt`, `llms.txt`, `sitemap.xml`) принадлежат репозиторию лендинга; документация не может править их сама | `vibevm-org/public/`; VISION D-06 | полная сверка проверяет `curl`-ом, что корневые ссылки на `/doc/sitemap.xml` и `/doc/llms.txt` живы (MAINTENANCE §2.4) |
| J-005 | 2026-09-10 | неудача | Дерево vibevm изменилось другой центральной сессией во время работы: 3663 удаления, 97 правок; кастодия стюарда у codex-сессии; чистое дерево в начале сессии | `git status --short` до и после; `custody.toml` | петля коммита начинается с `git status` и проверки живого конфликта писателей (MAINTENANCE §2.1) |
| J-006 | 2026-09-10 | находка | В `oleg-guru` генерируемый `llms-full.txt` и его скрипт не закоммичены с 17 августа: генерат в git расходится с источником незаметно | `git -C oleg-guru status`, mtime файлов | `llms*.txt` у нас — генерат (R-03): не коммитится в пакет руками, сборка сайта регенерирует; панель проверяет совпадение генерата с источником |
| J-007 | 2026-09-10 | успех | Семантические токены с двумя картами значений: смена всей палитры — один файл, компоненты не трогаются | VISION D-21; план R-27 | месячная петля может менять дизайн без правок компонентов; дизайн-ревью дёшево повторять |
| J-008 | 2026-09-10 | находка | Главная беда модельных доков по слову владельца — ложное допущение о прочитанных спеках; сформулировано как «контейнеры и коридоры» и частично механизировано линтером | VISION D-25; `STYLE.md` §2, §11 | линтер стиля в петле коммита; списки тиков пополняются из месячного ревью: каждый проскочивший тик — в список (MAINTENANCE §2.3 п. 6) |
| J-009 | 2026-09-10 | находка | PowerShell 5.1 портит UTF-8 без BOM при round-trip | `vibevm/vibespecs/boot/90-user.xml`; план §2.3 | правки страниц только редакторскими инструментами; проверка кодировки страниц в панели |
| J-010 | 2026-09-10 | находка | У агента `opus5` нет `effort: high` во frontmatter; режим High не задаётся пакетом | `.claude/agents/opus5.md` | перед делегированием проверять определение агента; правка — рукой владельца |
| J-011 | 2026-09-10 | наблюдение | Предсказание 10: APCA провалит `--faint #8E8D85` на `#FAF9F5` в светлой теме | план §4 п. 10; ждёт A0.23 | предсказания — часть журнала; опровержение или подтверждение — запись |
| J-012 | 2026-09-10 | находка | Три источника стиля и дизайна у владельца уже согласованы между собой сильнее, чем казалось: одна терракота в дизайн-системе и на лендинге; ридер oleg.guru переносится фичами, не цветами | `anthropic.css` `--terracotta-bright`, `global.css` `--accent` | правило P-14 «сшивать, не рисовать заново»; месячная петля ищет такие совпадения раньше, чем проектирует новое |
| J-013 | 2026-09-10 | неудача | Первый черновик регламента поставил технический гейт «продукт не выходит без зелёной документации»; владелец отверг: релизов до десяти в день, pull request'ов до ста, документация неизбежно дрейфует, риск принят; полная проверка — обещание команды по календарю, не условие релиза | слово владельца 2026-09-10; MAINTENANCE §2.4 до и после правки | замков между релизом продукта и документацией нет; вместо них измеритель дрейфа в панели (число, не ratchet), метки «проверено против версии» на страницах и **полная сверка** по обещанию команды (раз в квартал и перед вехой); правило родилось без записи-основания и потому было гипотезой — подтверждение закона §4 п. 3 |
| J-014 | 2026-09-10 | находка | Номер версии не идентичность: `vibe --version` печатает только `vibe 1.0.0`, без отпечатка сборки; в репозитории хоста единственный тег `pre-cultural-refactor`, релизных тегов нет; `vibe self install latest` собирает «последнюю in-tree версию» из исходников; владелец: десять релизов в день под одним номером, история переписывается, `vibe self update --force` перекачивает «ту же» версию | `target/debug/vibe.exe --version`, `vibe self --help`, `git tag`; слово владельца 2026-09-10 | сначала D-27 «ключи по содержимому» (шестая редакция) — отвергнуто владельцем, см. J-016; итог: рендер хоста по текущему состоянию `main`, не по тегам; вторая–четвёртая редакции писали «версия» и «тег» по привычке — привычка, а не находка, снова оказалась гипотезой |
| J-016 | 2026-09-10 | неудача | Шестая редакция заменила номера версий отпечатками содержимого (хэш дерева, хэш текста факта, отпечаток поверхности, `#p12-a3f9`, постоянные ссылки по хэшу, состояния хоста). Владелец отверг: неразличимость версий — фича (пользователи обновляются сто раз в день, их пайплайны на воспроизводимости не замечают), amend-версии неотличимы по определению, митигация — не строить фичи, требующие истории, а удалить их | слово владельца 2026-09-10, четыре сообщения; VISION D-27 до и после | D-27 переписано в «ничего, что требует истории»; удалены пины, хэши, отпечатки, дрейф «с тех пор», постоянные ссылки, состояния хоста, отпечатки блоков, отставание адаптаций; остался всегда текущий рендер и чтение прозы человеком на сверке. Урок для регламента: при столкновении с намеренной особенностью продукта («это фича») сначала удалять свои механизмы, а не изобретать обход |
| J-017 | 2026-09-10 | находка | Владелец уточнил, что именно было ценным в отвергнутом механизме: версия — контракт на поведение, а не набор файлов; смена номера — его осознанное решение, единственное событие «разницы»; разница считается только между номерами версий; данные нужны разработчикам документации, чтобы алгоритмически знать, какие страницы обновлять, а не перечитывать всё; читатели кухни не видят | слово владельца 2026-09-10, шесть сообщений; VISION D-27 восьмой редакции | псевдоистория версий: снимки поверхности по объявленным версиям (`vibe doc surface --record`) и `vibe doc diff <старая> <новая>` → список страниц с причинами; петля «смена версии» в MAINTENANCE §2.5; наружу — только номер и человеческий changelog. Урок: седьмая редакция удалила вместе с ошибочным и полезное — перед удалением спрашивать, что в механизме нравилось |
| J-018 | 2026-09-10 | находка | Владелец: лендинг `vibevm-org` тоже переделать на Qwik в ходе кампании, чтобы было однообразно и композировалось. Третья редакция держала два стека на домене (Astro-лендинг + Qwik-документация) с прокси `/doc/` и контрактом трёх строк между репозиториями — лишние швы, которые один сайт убирает | слово владельца 2026-09-10; VISION D-28, D-23 девятой редакции | один сайт на домене из одного пакета: `design/` + `site/`; корневые файлы генерирует одна сборка; переключение на месте контейнера лендинга без правок хостового nginx; тест паритета как гейт переключения; `vibevm-org` снимается с эксплуатации. Урок для регламента: композиция через один пакет дешевле, чем контракт между двумя |
| J-019 | 2026-09-10 | находка | Владелец: сначала вся документация на английском, русский — следующей волной после проверки; и сдвинуть план так, чтобы Fable написала все тексты первой, а разработку целиком вести в Opus. Прежний план чередовал прозу и механику внутри фазы 3 и звал сильную модель на каждую страницу | слово владельца 2026-09-10; VISION D-29; план §6.0, фаза P | три волны: A — находки, контракт, проза (Fable); B — разработка (Opus, Fable в четырёх точках возврата); C — адаптация (Fable). Проза пишется до инструментов, проверяется скриптами вместо линтера, принимается владельцем до передачи. Урок для регламента: сильная модель работает непрерывным куском, а не вкраплениями; очередь «нужен автор» вместо правок по одной |
| J-020 | 2026-09-10 | находка | Владелец: любое действие теперь делается не только руками, но и агентом; сценарии — промпт сначала, механика потом, ручные шаги только если нужны; люди приходят за «как это работает» и «какой промпт запустить»; это отличие от документации прошлого. Прежний скелет страницы был ручным: команда, вывод, шаги | слово владельца 2026-09-10; VISION P-16, D-30; STYLE §7 | седьмой элемент словаря `prompt` с `needs`, `outcome`, `assert`; страница сценария — что это, промпт, что произойдёт, руками (если нужно); прогон промптов агентом вне панели (P.7b, A2.29, месячная петля выборкой, сверка целиком). Урок для регламента: непроверяемый промпт устаревает молча — ассерт обязателен |
| J-015 | 2026-09-10 | наблюдение | Рендер `derived` для каждого состояния хоста требует сборки бинарника из этого дерева на сервере; при десяти релизах в день это десять сборок Rust | D-27, A0.28 | дебаунс и лимиты в конфигурации сайта; спайк A0.28 меряет цену сборки; альтернатива через артефакт сборки продукта — только с согласия владельца |
| J-021 | 2026-09-11 | находка | Кампания переехала в worktree `vibevm-docs` на ветку `research-preview-1-docs` (тег `research-preview-1-implementation` на `b1291b06` разослан `cargo xtask mirror` на оба зеркала). По закону привязки контекста стюарда (`user-local-state.xml` §3: другой worktree или ветка — новый контекст) заведён контекст `8bc32a15-…` с планом `vibevm-docs-2026-09`; узлы `DOCS-*` главного плана перенесены детьми фаз с теми же критериями приёмки; в главном плане — мандат M-030 и evidence-указатель на новый контекст | `~/.vibe/steward/contexts/8bc32a15-5e0e-4511-ab6d-e125722c4375/`, `render_goal.py --check` → `ok: true`; главный план r153 | A1.11 сводится к правке узлов по находкам фазы 0, а не к созданию; регламент: смена ветки кампании = новый контекст стюарда, старый получает указатель, не редактируется дальше |
| J-022 | 2026-09-11 | находка | Определение агента `opus5` не несло `effort: high` (J-010); в worktree строка добавлена центральной сессией как незакоммиченная правка `.claude/agents/opus5.md` — файл отслеживается git, значит в фазе 1 нужен коммит `chore(agents): run the coder-tier executor at high effort`; владельцу сообщено. Клауза «правка рукой владельца» из плана §2.7 не сработала: владелец не правил файл, а директива «Opus 5 в режиме High» стоит | `git -C vibevm-docs status --short` → ` M .claude/agents/opus5.md`; harness: «reasoning effort comes from the agent definition frontmatter» | конфигурация исполнителя — часть зоны кампании, коммитится как `chore`, не ждёт руки владельца; в пакетах фазы 0 воркерам сказано не трогать эту строку статуса |
| J-024 | 2026-09-11 | неудача | Воркер `general-purpose` на Sonnet (пакет P0-C7) не создал файлы находки и отчёта: сослался на инструкцию своего харнесса «не писать report/findings-файлы, отдавать результат текстом» и вернул полную находку в ответе; соседний воркер того же типа (P0-C3) файлы создал. Клауза «деливерабл — только артефакты» и инструкция харнесса расходятся | ответ агента P0-C7; `findings/A0.21-index-card-fields.md` записан центральной сессией | пакет для `general-purpose`/Sonnet несёт фразу «если харнесс запрещает файлы отчёта — верни находку текстом в требуемой форме»; центральная сессия сохраняет текст сама; расход не вырос (148k токенов) |
| J-025 | 2026-09-11 | находка | В worktree появился untracked `vibevm/vibedeps/.gitignore` («Managed by vibe», `**/target/`, `**/node_modules/`): его пишет `vibe-workspace` (`build_ignore.rs`) при `vibe install`/`bin build`, то есть запуск `vibe` из корня хоста пачкает дерево файлом, которого нет в истории. Три воркера отметили его в самопроверке как «не моё» | `git status --short` → `?? vibevm/vibedeps/.gitignore`; `crates/vibe-workspace/src/vibedeps/build_ignore.rs` | гейт «дерево чистое» в пакетах называет этот файл ожидаемым; рендерер сайта (D-07, D-23) запускает `vibe` в копии дерева или чистит файл — иначе `git pull` на сервере споткнётся; вопрос в BACKLOG: должен ли этот файл быть в истории |
| J-026 | 2026-09-11 | находка | `cargo xtask specmap --check` красный на `b1291b06` без наших правок: карта не сведена (20 новых узлов, 3 новых ребра, 8 `unbumped-hash`) — долг предыдущей кампании, зашедший в `main` | `WORKER-REPORT-P0-O2.md`, вывод команды | фаза 1 начинается атомом «пересобрать карту» с чтением диффа как данных; гейт фазы 1 требует зелёный `--check`; в регламент: полная сверка начинается с зелёной панели, иначе дрейф документации неотличим от чужого долга |
| J-027 | 2026-09-11 | находка | Четыре имени словаря документации D-10 — `example`, `rule`, `derived`, `run` — уже заняты в живом корпусе как именованные секции (`<example title="…">`, 8 канонических файлов); литеральное плечо диспетчера стоит выше именованной формы, и открытый словарь молча переопределил бы их смысл. Снято дискриминатором по образцу `fact="true"`: блок жанра никогда не несёт `title=`, секция несёт всегда | `findings/A0.4-pivot-extension-points.md`, свидетельства 1 и 3 (проба `vibe refactor convert-source`) | VISION D-10 уточнено на месте; регламент: новое имя элемента диалекта проверяется grep'ом по корпусу до записи в решение — «имя свободно» есть проверяемый факт, не допущение |
| J-028 | 2026-09-11 | находка | Субагент инструмента `Agent` получает `CLAUDE.md` проекта (и память пользователя) автоматически, до текста пакета — в отличие от codexrunner/fractality, для которых писалась клауза «boot-лейн не читать». Полный лейн (145k) при этом не грузится; воркер отметил это как несоответствие пакета харнессу, не как нарушение | ответ агента P0-C2; `WORKER-REPORT-P0-C2.md` | в PACKET-COMMON.md записать: «`CLAUDE.md` в контекст попадёт сам — это не приглашение читать лейн»; при оценке стоимости воркера считать ~5k токенов на `CLAUDE.md`; в фазе 2 подумать о `.claude/agents/*` с `memory: none` (если поддерживается) |
| J-029 | 2026-09-11 | находка | Корень хоста — `[project]`, не `[package]`: «хост как пакет» не выбирается ни git-источником, ни store, ни реестром; проба git-источника по `branch` на bare-клоне хоста упала на манифесте. Зато выкладка на диске читается как проект и как project-local реестр in-tree пакетов без клона и без `cache add` | `findings/A0.20-host-from-source.md`, пробы A, B1–B5, C2 | D-07 и D-16 уточнены: канал хоста — выкладка на диске; регламент: «прочитать хост» в любой петле значит `git pull` в каталоге рендерера, не выборку пакета |
| J-030 | 2026-09-11 | находка | In-tree пакеты — мутабельные слоты одной версии; store обновляет запись по sidecar-хэшу, а `vibe cache add` при этом печатает «already present — bytes untouched». Строка отчёта врёт; признак свежести — хэш | `findings/A0.8-store-and-sources.md`, раздел «Расхождения»; DEFERRALS X-001 | ключ кэша рендера — хэш содержимого, не версия; в BACKLOG продукта — дефект вывода; регламент: перерендер решается по хэшу выкладки |
| J-031 | 2026-09-11 | находка | Валидатора JTD в продукте нет: `schemas/*.jtd.json` — вход кодогенерации, serde молча игнорирует лишние поля; `vibe install --json` печатает поток из трёх документов, у первого схема расходится с эмиттером, второй без схемы. План A0.12 предполагал `wire::load` — предположение, не факт | `findings/A0.12-example-runner.md` §4; макет `jtd_validate` | валидатор JTD — часть `vibe-doc` (A2.9); документ без схемы — «не проверен», не «прошёл»; три дефекта — в BACKLOG (X-008…X-010); регламент: «есть ли функция X» проверяется grep'ом до записи в план |
| J-032 | 2026-09-11 | находка | Значение `VIBE_SETTINGS` должно быть в нативном написании: Git Bash конвертирует POSIX-путь сам, а Python/PowerShell/CI — нет, и состояние тихо уезжает «от корня текущего диска» | `findings/A0.12-example-runner.md` §1, проба msys-probe | раннер и все скрипты панели выставляют переменные изоляции нативными путями; tripwire на дом и дерево — обязателен |
| J-033 | 2026-09-11 | находка | `min_vibe_version` нигде не проверяется — поле-транзит в индекс и post-publish-хук; настоящая защита старого `vibe` от нового манифеста — `deny_unknown_fields` на `ManifestWire`, то есть любой старый `vibe` откажет пакету с новыми полями по «неизвестному ключу», что бы ни стояло в `min_vibe_version`. Вопрос F-05 переформулирован: поднимать ли версию `vibe`, чтобы поле говорило правду | `findings/A0.3-manifest-strict-fields.md` §2, §5 | вопрос владельцу на гейте фазы 0 с рекомендацией «поднять до 1.1.0»; в регламент версий: новое поле манифеста = осознанный bump, потому что старый клиент его отвергнет |
| J-034 | 2026-09-11 | находка | `research/ts-demo`, названный в плане образцом web-пакета под дисциплиной, на `b1291b06` красный на трёх шагах floor, несёт lock схемы 6 и `tsconfig` ниже пола GUIDE; при этом объединённый с Qwik 2.0.0-beta.43 строгий `tsconfig` проходит `tsc --noEmit`, а структурный гейт понимает `.tsx` | `findings/A0.14-ts-floor.md` С-2, С-4…С-8, С-11 | web-пакет генерируется `typescript-ai-native init`, не копированием `ts-demo`; юнит-тесты — `.test.ts` без JSX, все семь шагов floor остаются; долг `ts-demo` — в BACKLOG (X-013); регламент: «образец» в плане проверяется прогоном до того, как на него ссылаются |
| J-035 | 2026-09-11 | находка | `formats/EPOCHS.toml`: `public = false`, `break_window_open = true` — до публикации ломать можно без break-заметок и миграций, `wire-diff` только сообщает; заметки обязательны с `public = true`, и этот флаг переключает только владелец. План (A0.2, A2.2) предполагал break-заметки как гейт — предположение | `formats/EPOCHS.toml`, PROP-044 §4.7; `findings/A0.2-wire-kind-vocabulary.md` §3 | заметки для `doc`/`app` и ключей документации — по желанию, одна короткая; регламент: перед заявлением «гейт X требует Y» проверять состояние флагов формата |
| J-036 | 2026-09-11 | наблюдение | Механический инвентарь legacy-документации (1184 утверждения в 48 файлах) дал 90.6 % «живо», 7.1 % «устарело», 2.3 % «неизвестно» — предсказание PHASE-G-SPEC §9 п. 2 («существенная доля файлов описывает несуществующее поведение») по механической проверке не подтвердилось; проверялись команды по `--help`, пути по дереву, поля по манифесту, не смысл абзацев | `findings/legacy-inventory.md`, сводка; `WORKER-REPORT-P0-C4.md` | P.5 проходит 111 неживых строк руками, а «живые» — выборкой в 10 % на смысл; регламент полной сверки: механический инвентарь — первый шаг, а не вердикт |
| J-037 | 2026-09-11 | успех | Предсказание 10 (план §4) подтверждено числом: `--faint #8E8D85` на `#FAF9F5` — Lc 57.15 при пороге 60. Сверх него аудит показал системную дыру тёмной палитры лендинга: между `--dim`/`--faint` и `--cream` нет тона, закрывающего 60 для вторичного текста — на лендинге это незаметно (мало текста), в документации недопустимо | `findings/A0.23-tokens/apca-report.md`, 64 пары, 23 ниже порога | D-21 уточнено: пороги по ролям, разделители вне гейта, новые тона `-docs` чеканятся по аудиту; регламент: палитра для чтения проверяется аудитом до дизайн-ревью, а не глазом на ревью |
| J-038 | 2026-09-11 | находка | Образцовый скрипт аудита контраста зависит от `apca-w3` под AGPL v3 — копилефт, запрещённый законом лицензий проекта; воркер отказался вносить его в дерево и прогнал самопроверку из scratch. Формула APCA опубликована, реализуется своей функцией | `findings/A0.23-tokens-apca.md`, «Открытые вопросы» | пакеты с npm-зависимостями называют лицензии в самопроверке; аудит web-пакета — собственная реализация; в регламент: новая зависимость = проверка лицензии до `npm i`, даже dev |
| J-039 | 2026-09-11 | находка | Проба сервера обнаружила в `vibe-index` вероятный обход путей: `data_dir.join(<захват>)` после percent-декодирования `Path` пропускает `..%2F`; на идентичном маршруте пробный сервер отдал файл вне корня. Побочная находка спайка, не цель кампании; на живом бинарнике не подтверждена | `findings/A0.7-http-server.md`, матрица обхода; DEFERRALS X-017 | владельцу — на гейте фазы 0 как P1; регламент: локальный читатель не повторяет форму `join(<захват>)`, статика — только `ServeDir` или лексический join с проверкой `starts_with(root)` |
| J-040 | 2026-09-11 | находка | Qwik SSG (2.0.0-beta.43) при `trailingSlash: false` генерирует одну страницу из семи, при неверном `base` — ноль и пустой sitemap, и оба раза завершается кодом 0 | `findings/A0.10-qwik-probe.md` §6, угол 1 | гейт сборки сайта считает страницы и сверяет с манифестом; адреса страниц — только со слэшем; регламент: «код выхода 0» для генераторов — не свидетельство |
| J-041 | 2026-09-11 | находка | `rust-embed` без фичи `debug-embed` в отладочной сборке читает файлы с диска по абсолютному пути (измерено: файл, добавленный после сборки, отдался; удалённый — пропал); `include_dir` встраивает всегда; оба молчат на пустом каталоге и не заказывают пересборку | `findings/A0.11-shell-embedding.md`, замер debug/release | выбран `include_dir` + обязательный `build.rs` с `rerun-if-changed` и проверкой `index.html`; регламент: автономность читателя проверяется на debug-сборке отдельным тестом |
| J-023 | 2026-09-11 | наблюдение | Холодная сборка `cargo build -p vibe-cli` в новом worktree заняла меньше десяти минут (фоновая задача, код выхода 0) — цена нового worktree ниже, чем ожидалось по заметке F19 о `MAX_PATH`; `git worktree add` с `-c core.longpaths=true` прошёл без ошибок | фоновая задача сборки; `target/debug/vibe.exe --version` → `vibe 1.0.0` | предсказание для A0.28: release-сборка от чистого `target/` — минуты, не десятки; проверить числом |
| J-042 | 2026-09-11 | находка | Замер A0.28: инкрементальная release-сборка `vibe-cli` стоит 57 % холодной (3m 50s против 6m 42s) из-за `lto = "thin"` и `codegen-units = 1`; профиль dev даёт побайтово тот же `vibe --help` при 22 с инкрементальной и 1m 42s холодной | `findings/A0.28-render-cost.md`, таблица замеров, `diff` вывода rc=0 | рендерер собирает dev-профилем, дебаунс 5 мин; регламент: derived-блоки снимаются с dev-бинарника, паритет dev/release для `--help` — тест рендерера |
| J-043 | 2026-09-11 | наблюдение | Воркер P0-O8 заметил параллельные правки центральной сессии в дереве (PROP-057 и поправки фазы 1) и сообщил как аномалию — верно по своему пакету, который обещал чистое дерево | `findings/WORKER-REPORT-P0-O8.md` | пакеты с фазы 1 говорят явно: центральная сессия правит дерево параллельно, чужие изменения вне периметра — не аномалия (внесено в `PACKET-P1-O1.md`) |
| J-044 | 2026-09-11 | находка | `cargo xtask specmap --check` на дереве фазы 1: 10 `unbumped-hash` — 8 унаследованных от кампании packages-2026-09 (PROP-018 ×2, PROP-020, PROP-021 ×2, PROP-023 ×2, PROP-024) и 2 наших (PROP-028 `roles`, `versioning`), 318 новых юнитов, 3 ребра | scratch `specmap-check.txt` | унаследованные помечаются `spec-editorial:` в теле коммита регенерации (их авторы ревизию не бампали — читаем как редакционные), свои секции бампаются до `req r2`; регламент: атом со спекой заканчивается `specmap --check`, не гейт фазы |
| J-045 | 2026-09-11 | находка | Импорт вижена в `design/` упирается в закон `--exhaustive`: каждый абзац design-документа обязан быть фактом (`facts.toml` включает `design/**`), 2 400 строк без разметки — красный гейт; авто-разметка детерминированной схемой идентификаторов отдана воркеру P1-O1 со скриптом | `facts.toml` include-globs; образец `design/host-as-package.xml` | регламент: любой документ, идущий в `vibevm/vibespecs/**`, размечается в том же атоме; импорт большого текста — воркер со скриптом, не рука центральной сессии |
| J-046 | 2026-09-11 | наблюдение | Гейт R-25 перед импортом зоны дал шесть строк, все прочитаны: `0.0.0.0` (отвергнутый bind), `~/.vibe/opt/…` (store vibe, три раза), `/opt/vibevm-mirror` (пример из старой документации), текст самого правила; scratch-пути `AppData\Local\Temp\…` в 21 находке вычищены до `<scratch>/` скриптом импорта, провенанс-абзацы и врезка для владельца удалены | `import_zone.py` (scratch); `grep -rn AppData campaigns/docs-2026-09` пусто | регламент: импорт делает скрипт с проверкой на утечки (scratch-пути, провенанс-маркеры), grep R-25 — его шаг, а не рук |
| J-047 | 2026-09-11 | успех | Фаза 0 закрыта: 26 спайков приняты (A0.24 снят), ни одно решение владельца не переписано, три внешних каталога не тронуты, коммитов ноль; PROP-057 (263 факта, 17 секций) написан за одну сессию из вижена и вердиктов, все поправки фазы 1 (PROP-028, PROP-045, PROP-043, PROP-047, PROP-003) — в том же заходе | `PHASE-0-FINDINGS.md`; `git log` — HEAD `b1291b06` до первого коммита фазы 1 | наблюдение без действия |
| J-048 | 2026-09-12 | находка | Панель `tools/self-check.sh` красная на дереве без единого изменённого `.rs`: шаг «wire-derive ratchet» — рукописных derive-файлов больше, чем заморожено в `wire-derive-baseline.json` (`vibe-registry` 8/7, `vibe-workspace` 4/3, `xtask` 14/12, `vibe-publish`); долг кампании bridge-пакетов, унаследованный с `b1291b06` | scratch `self-check.txt`, шаг 3/47, EXIT=1; `git diff b1291b06 --stat` — только спеки и документы | пакет P1-O2: классифицировать типы, поднять базовую линию только для не-wire с строкой на тип; регламент: панель гоняется на входе в кампанию, а не только на гейте — красное наследство видно до первого атома |
| J-049 | 2026-09-12 | находка | `cargo xtask specmap --check` после регенерации: пять сирот в гейтуемых крейтах (`embedded_source.rs`, `submodules.rs`) — код bridge-кампании без меток спеки; регенерация записала 8 унаследованных `unbumped-hash` как данные | вывод `cargo xtask specmap --check` | пакет P1-O2: метки на существующие якоря PROP-023/PROP-021, без дисposition-обхода, если якорь есть; регламент: атом с кодом заканчивается `specmap --check` в том же пакете |
| J-050 | 2026-09-12 | успех | Панель до шага 3 и все ручные гейты фазы 1 зелёные на наших правках: `facts check --exhaustive` clean (324 файла), `vibe check` 0 ошибок (2 предупреждения — легаси `WAL.xml`), `specmap --check` clean по юнитам и рёбрам; 12 атомарных коммитов `edd7e3ae…8f956eeb` | `git log --oneline b1291b06..HEAD` | наблюдение без действия |
| J-051 | 2026-09-12 | находка | Первые восемь страниц несли inline-элементы `<code>` и `<em>`, которых в диалекте PROP-045 нет: inline остаётся Markdown (`` `код` ``, `*курсив*`); замечено при самопроверке, исправлено по всем страницам одной заменой | страницы `start`, `model`; PROP-045 `XML-DIALECT-IS-THE-MD-SUBSET` | правило письма страниц: inline — только Markdown; перед коммитом раздела — `grep -l "<code>\|<em>"` по страницам (кандидат в линтер A2.25) |
| J-052 | 2026-09-12 | неудача | 21 из 44 страниц оказались некорректным XML: литеральные плейсхолдеры `<version>`, `<coordinate>`, `</vibevm>` в прозе и в инлайн-коде парсер читал как теги; `vibe check` этого не видит — страницы doc-пакета он пока не разбирает | разбор `xml.etree` по 44 файлам; скрипт `fix_pages_xml.py` (scratch, 21 файл исправлен) | перед каждым коммитом раздела — разбор всех страниц стандартным XML-парсером, пока нет `vibe doc check` (A2.5); в прозе и коде только `&lt;…&gt;`, голые скобки — никогда |
| J-053 | 2026-09-12 | находка | Автоправка не различает текст и тег, когда имя плейсхолдера совпадает с именем секции той же страницы (`<family>` в глоссарии, где есть секция `family`) или несёт `title=` (пример элемента в прозе): два места правились руками после повторного разбора | `glossary/index.xml` §family; `authoring/specs-agents-can-cite.xml` §the-dialect | после любой автоправки — повторный разбор; правило линтера: имя секции не должно совпадать с плейсхолдером, который страница цитирует в скобках |
| J-054 | 2026-09-12 | наблюдение | Опись по страницам дала 59 примеров, 19 промптов, 72 `derived`, 0 `figure` против оценки карты (29 примеров, 7 промптов); скрипт описи стал генератором очереди `EXAMPLES-TODO.md` и индекса якорей для инвентаря legacy | `scratchpad/page_index.py`; `EXAMPLES-TODO.md` §queue | оценки карты — не источник счёта; счёт снимается со страниц скриптом (кандидат в `vibe doc report`) |
| J-055 | 2026-09-12 | находка | Два примера называли фикстуру `none` при команде `cat vibe.toml` / `cat packages/…/vibe.toml`, которая читает дерево фикстуры: в пустой песочнице они бы упали | `howto/set-up-a-workspace#workspace-table`, `authoring/write-a-flow#manifest` — исправлены на `workspace-root`/`workspace` | `fixture` примера — состояние, которое команда читает, а не «ничего не нужно»; `none` только для команд, не читающих файлов |
| J-056 | 2026-09-12 | находка | Первая редакция `EXAMPLES-TODO.md` предписывала фикстуре объявлять `VIBE_OFFLINE=1` для команд установки — вразрез с решением A0.12 (окружение изоляции не меняет поведение); исправлено: `--offline` стоит в команде или команда идёт в сеть; герметичность онлайн-примеров отложена в A2.9 (X-025) | `EXAMPLES-TODO.md` §sandbox-rules; `findings/A0.12-example-runner.md` §2 | решения находок фазы 0 перечитываются перед каждым пакетом воркеру, не восстанавливаются по памяти |
| J-057 | 2026-09-12 | находка | Первая механическая проверка правила «первый абзац без единого термина глоссария» дала 32 страницы из 43: почти всюду через слова `package` и `project`, которые в глоссарии есть, но читаются без него; настоящих vibe-терминов (`lock file`, `manifest`, `registry`, `store`, `index`, `anchor`, `skill`, `contribution`, `fingerprint`, `receipt`) — 19 страниц. Решение: правило уточнено в PROP-057 `STYLE-PAGE-SKELETON` датированной оговоркой — обычное английское слово в обычном смысле не считается; 19 абзацев переписаны без vibe-терминов. Отвергнуто: переписывать 32 абзаца вокруг `package`/`project` — проза делается кривой, а `llms.txt` теряет ключевые слова. Пересмотреть: когда линтер A2.25 реализует проверку — его список исключений и есть эта оговорка | скрипт проверки (scratch); PROP-057 §16; коммиты этого атома | правило письма: первый абзац проверяется скриптом по глоссарию до коммита раздела, список обычных слов — из PROP-057, не из головы |
| J-058 | 2026-09-12 | успех | Механическая проверка 72 блоков `derived` по отладочному бинарнику и дереву: 60 команд `--help` отвечают с кодом 0 и непустым выводом, 11 схем JTD лежат в `schemas/`, единственный красный — `vibe doc --help` (команда появляется в фазе 2, A2.20); ссылки `derived` на страницах справочника согласованы с продуктом до появления генератора | скрипт `check_derived.py` (scratch); вывод `derived=72 bad=1` | скрипт — первая фикстура генератора `derived` (A2.13): проверка «ссылка разрешается» отделяется от «текст совпадает» |
| J-059 | 2026-09-12 | находка | Проверки прозы без инструментов (PP-C1, Sonnet, 1166 строк отчёта): 385 цитат, из них 3 на переименованных якорях (`PROP-054#WHY-C-ABI` → `why-c-abi`, `#WASM` → `WASM-DEFERRED`), 11 запрещённых слов (5 из них — точный термин `capabilities`), 195 фраз длиннее 25 слов, 3 абзаца длиннее 6 фраз (все — нумерованные процедуры в одном `<p>`), 12 заголовков-вопросов (все на странице вопросов, по жанру), 308 терминов до введения, 0 битых ссылок и id | `findings/PP-C1-prose-checks.md`, `findings/WORKER-REPORT-PP-C1.md`; коммиты `435d3aef`, следующий | линтер A2.25: `capabilities` вне списка; заголовки-вопросы разрешены на странице вопросов; процедура — по абзацу на шаг; лимит длины — по виду блока (X-028) |
| J-060 | 2026-09-12 | находка | Правило «термин введён до употребления» механизировано: скрипт ставит ссылку на глоссарий на первое употребление каждого vibe-термина в повествовательном абзаце (212 ссылок на 44 страницах); из них 10 оказались не тем смыслом — `index` как `INDEX.md` boot-лейна, `store` как хранилище версий и файл настроек, `fingerprint` как содержимое пакета при глоссарной статье про свежесть фаз. Исправлено словами и глоссарием: одно слово — одно значение, `fingerprint` — отпечаток содержимого, `freshness fingerprint` — штамп прогона фазы | скрипты `link_terms.py`, `fix_links.py` (scratch); коммит `435d3aef` | автоссылка — инструмент автора, не сборки: результат читается глазами; линтер A2.25 проверяет наличие ссылки, а не ставит её; глоссарий проверяет «одно слово — одно значение» до автоссылки |
| J-061 | 2026-09-12 | неудача | Первое снятие `expect` (PP-C2) дало 15 из 56: фикстура `hello-vibe` не построилась — оффлайн-установка из прогретого store не резолвит `latest` без клона реестра (B-131), а сетевой клон в глубокой песочнице рвётся на длине пути (B-132); 41 пример стоял за этой фикстурой | `findings/WORKER-REPORT-PP-C2.md` §3, §5a, §5b; пробы центральной сессии 2026-09-12 | фикстуры второй редакции: локальный `file://` реестр из in-tree `vibevm/vibepacks` в машинном `registry.toml` песочницы, короткий корень песочницы; правило A0.12 «окружение не меняет поведение» сохранено — реестр не флаг |
| J-062 | 2026-09-12 | находка | `vibe init package <координата>` добавляет слот `vibevm/vibepacks/<группа>/<имя>/v0.1.0/` в проект cwd, а с путём создаёт целый проект с таким же слотом внутри; члена workspace (`packages/<имя>` с `[package]` в корне) команда не умеет; `--kind` не действует (B-134). Семь страниц описывали несуществующую раскладку и исправлены (`c79325e7`) | PP-C2 §5d; пробы `vdocs-probe3`; коммит `c79325e7` | каждая команда, названная в «что произойдёт», прогоняется до написания страницы, а не после: следующие страницы пишутся после пробы, не по памяти о `--help` |
| J-063 | 2026-09-12 | неудача | Скрипт зачистки scratch-путей прошёлся по `findings/PP-O1-runs/`, куда в этот момент писал воркер PP-O1 — правки в чужие файлы во время его работы; вреда нет (файлы и должны быть без путей), но это нарушение границы пакета | вывод `strip_scratch_zone.py`; зона-гейт | зачистка и зона-гейт запускаются по каталогам завершённых воркеров; каталоги работающих исключаются явным списком |
| J-064 | 2026-09-12 | успех | Инвентарь legacy закрыт без пустых строк: воркер PP-C3 (Sonnet, скрипт `PP-C3-dispose.py`) разложил 1184 утверждения — 957 «перенесён» на страницы, 93 «перенесён (справочник)» через `derived`, 80 «снят: устарело», 54 «нужен автор»; из 54 центральная сессия 14 отнесла к редиректам реестра (новая секция `howto/use-a-private-registry#redirects`), 16 — к генерируемому справочнику манифеста и форматов, 9 — к путям исходников (снят), 4 — к устаревшим таблицам, которых нет в схеме манифеста, остальные точечно | `LEGACY-INVENTORY.md`, `findings/WORKER-REPORT-PP-C3.md`, скрипт `dispose_author.py` (scratch) | «нужен автор» решается правилом, записанным в скрипт, а не строка за строкой; проверка «таблица жива» — по структурам `vibe-core`, не по grep |
| J-065 | 2026-09-12 | находка | Прогон 19 промптов агентом (PP-O1, opus5): 5 зелёных, 1 условный, 1 вне песочницы, 12 красных — 5 багов продукта (B-135…B-138, B-133), 3 неверных ассерта, 1 неясный промпт (профиль деплоя, B-139), 3 ждут фазы 2. Страницы поправлены: контракт scrape (`init`/`check`, `modified = "delete"`, health по инструменту проекта), таблицы деплой-профиля в `by-hand`, `vibe bin`/`vibe tools` без `--path`, `reinstall --force --offline` как доказательство оффлайна, `vibe outdated` и реестры проекта | `findings/WORKER-REPORT-PP-O1.md`, `findings/PP-O1-runs/`; коммит этого атома | промпт пишется после пробы команд в песочнице, а не после чтения `--help`: три из пяти «багов» были бы найдены при написании |
| J-066 | 2026-09-12 | неудача | Во время прогона PP-O1 центральная сессия пересобрала `target/debug/vibe.exe` (панель, clippy) и правила промпты четырёх страниц; воркер заметил и два промпта перепрогнал по новому тексту; трипвайр показал ещё и чужую активность в `~/.vibe` (другая сессия владельца в контексте `d2ab5a82-…`) | `WORKER-REPORT-PP-O1.md` §1 | пока идёт прогон промптов или снятие примеров, бинарник не пересобирается и страницы сценариев не правятся; изменения копятся и уходят следующим прогоном |
| J-067 | 2026-09-12 | наблюдение | Второе снятие (PP-C2b) дало 54 из 54 на фикстурах с локальным реестром; 39 `expect` вставлены (`700db4b8`), 15 примеров после правок страниц ждали третьего снятия. Владелец: «минимизируй снятия только абсолютно необходимым» — третье снятие и повторный прогон промптов не запускаются; остаток снимает раннер A2.9 и гарнитура A2.29 фазы 2 механически | `EXAMPLES-TODO.md` §queue (состояния «снято»/«ждёт раннера A2.9»), `findings/PACKET-PP-C2c.md`, `findings/PACKET-PP-O1b.md` с пометками | ручные снятия — только до появления раннера и только для того, что нужно писать прозу; всё остальное — работа машины |
| J-068 | 2026-09-12 | наблюдение | Фаза 2 открыта той же сессией под целью владельца, без отдельной центральной сессии Opus, как предполагал план: атомы уходят `opus5` пакетами, воркер коммитит атом сам по subject'у плана, центральная сессия читает дифф после посадки и одна делает push. Первые два пакета (P2-O1: A2.1, A2.2, A2.4; P2-O2: A2.8) запущены параллельно в одном дереве, потому что не делят крейтов | `LEDGER.md` §phase-2; `findings/PACKET-P2-O1.md`, `PACKET-P2-O2.md` | пакет фазы 2 называет: атомы, порядок, subject'ы коммитов, гейты атома, крейты соседних воркеров, которые нельзя трогать |
| J-069 | 2026-09-12 | находка | Третий унаследованный красный шаг панели — не дрейф голденов, а перелом провода: `formats/corpora/index/e1` зависит от того, какой бэкенд deflate включила унификация фич Cargo (`zip` с `deflate-flate2-zlib-rs` в корне против `miniz_oxide`, который просит `vibe-index`); изолированная сборка и сборка workspace дают разные байты, и значения, зелёного в обеих, не существует | `findings/WORKER-REPORT-P1-O4.md` §3; проба воркера: `rebuild --check` зелёный при 1364 байтах, `golden_corpus` красный при 1368 | «same input, same bytes» держится только реализацией, прибитой внутри крейта, не фичей соседа по графу; вопрос владельцу — review-маркер |
| J-070 | 2026-09-12 | находка | Второй красный того же класса, что фикстура `upstream_authors`: 11 фикстур lock-файлов в дереве пишут `schema_version = 6` при `CURRENT_SCHEMA_VERSION = 7` (с `72d866d2`), каскад роняет ~8 целей `cargo test --workspace`, включая `golden_corpus` и `vibe-workspace bins::tests` | `WORKER-REPORT-P1-O4.md` §4.1; `WORKER-REPORT-P2-O2.md` «не сделано» п. 3 | фикстуры lock-файлов берут номер схемы из константы, не из литерала; пакет P1-O5 |
| J-071 | 2026-09-12 | успех | A2.8 сел (`a7bff252`): словарь документации в пивоте — параметр читателя (`Vocabulary::{Spec,Doc}`), `when` в слоте блока по `DOC-VOCAB-WHEN-SLOT`, писатель словарно-слепой, 132 теста; первое машинное чтение корпуса нашло два дефекта страниц (зарезервированное имя `fact` в глоссарии, `derived` в ячейке таблицы) — исправлены той же сессией с регистром карантина | `findings/WORKER-REPORT-P2-O2.md`; коммит правки страниц | страница проверяется читателем пивота до коммита раздела, как только читатель есть: `cargo test -p vibe-specdoc --test docs_corpus` — гейт страниц до `vibe doc check` |
| J-072 | 2026-09-12 | неудача | Общий индекс git при параллельных воркерах: центральная сессия сделала `git add <свои пути> && git commit`, а `git commit` без путей забирает весь индекс — и 17 застейдженных файлов атома A2.2 (wire-словарь) уехали внутрь коммита правки страниц `005ee978`; отдельного `feat(wire): widen package_kind vocabulary for doc and app` в истории нет. Историю не переписываем (красная линия, ветка живая): атом A2.2 учитывается по `005ee978`, список его файлов — в `WORKER-REPORT-P2-O1.md` | `git show --stat 005ee978`; отчёт P2-O1 | коммит всегда явной формой `git commit -m … -- <пути>`; никогда `git commit` без путей и `git add -A` в дереве с воркерами; правило внесено в пакеты и в сообщения работающим воркерам |
| J-073 | 2026-09-12 | успех | A2.1 (`b18453f2`), A2.2 (внутри `005ee978`), A2.4 (`29351a68`) сели: восемь видов в ядре с `is_read_only()` и отказом `vibe install` doc-пакету, wire-словарь, поля связи/локализации/карточки; `vibe check` на манифесте руководства — clean (было 2 ошибки). Хвост: `vocabulary_parity` красный на четырёх прозаических копиях набора видов (`VIBEVM-SPEC.md` ×3, `boot/00-core.xml`) — правит центральная сессия под той же поправкой A1.1; корпуса без образцов `doc`/`app` — A2.3 | `findings/WORKER-REPORT-P2-O1.md`; `cargo test -p vibe-wire --test vocabulary_parity` | тест паритета словаря сторожит и прозу спеки: правка реестра видов — это и `VIBEVM-SPEC.md`, и `00-core.xml`, и словарь wire одним атомом |
| J-074 | 2026-09-12 | успех | Раннер примеров сел (A2.9 `5c472381`, A2.10 `e2a05ce9`): 17 фикстур в `examples/` пакета, локальный реестр-копия, нормализация по фикстуре, `deferred.toml` вместо пустого `expect`; первый прогон по руководству — 38 из 39 ранее снятых совпали с первого раза, 10 из 15 пустых сняты `--accept`, 3 ждали правки страницы деплоя (коллизия id артефакта) и сняты центральной сессией одной командой раннера, 2 (scrape) заблокированы окружением и продуктом (B-143); `derived` 72/72 детерминированы | `findings/WORKER-REPORT-P2-O4.md`; `vibe doc check --examples --derived` exit 0 | «минимизировать снятия» выполняется буквально: снятие — команда раннера, не пакет воркеру; пустой `expect` запрещён, отложенное объявляется в `deferred.toml` с причиной |
| J-075 | 2026-09-12 | успех | P1-O5 (`b73195fd`): 10 из 11 фикстур lock-файлов берут номер схемы из `CURRENT_SCHEMA_VERSION` (одиннадцатая намеренно тестирует отказ старой схемы); каскад из 7 целей, приписанный схеме, зелёный. Остались красными: `vibe-index golden_corpus` (перелом провода J-069, владельцу), два теста `cli_search` (поля конверта `search`; причина не установлена, красные и у P2-O1), и ещё 16 фикстур с литералом `7` — тот же дефект при следующем подъёме схемы | `findings/WORKER-REPORT-P1-O5.md` §7 | хвост панели — отдельный пакет после посадки всех атомов фазы 2 (P1-O6): `cli_search`, литералы `7`, полный прогон `tools/self-check.sh` |
| J-076 | 2026-09-12 | успех | P2-O6 сел: цитаты `rule` резолвятся по текущим спекам (381 из 381 плюс 8 адресов в прозе; правила X-029 в классификаторе), HTML-остров без скриптов и стилей, номера блоков `pNN` на сборке во всех проекциях; карта doc-пакета несёт 381 ребро `documents` без пинов. Два расхождения: PROP-057 `READER-NUMBERED-BLOCKS` числил заголовок среди нумеруемых, `PIPE-NUMBERING` исключал — реализовано по `PIPE-NUMBERING`, спека сведена датированной оговоркой; движок карты не чеканит юнит для факта внутри `td`, отсюда 4 «висячих» ребра на существующие якоря (B-144) | `findings/WORKER-REPORT-P2-O6.md`; коммиты `51695bdd`, `fcc32b96`, `6aa578df` | два факта об одном механизме в одной спеке проверяются друг против друга при написании: реализация — первый читатель, который их сталкивает |
| J-077 | 2026-09-12 | успех | P2-O3 сел четырьмя атомами: индекс знает `doc`/`app` и поля карточки (`cb041f5d`), гейт `vibe check` для doc-пакетов, переводов и картинок (`9993148f`), `vibe init --kind doc` и `--translates` (`09afc33a`, попутно B-134), прогрев предметов и источника (`93b0d7ff`), хвост P2-O5 — читатель `facts check` выбирает словарь по виду пакета и руководство наблюдаемо (`153e68d9`) | `findings/WORKER-REPORT-P2-O3.md` | — |
| J-078 | 2026-09-12 | находка | Параллельные воркеры с приватными `CARGO_TARGET_DIR` (по 28–33 ГБ каждый) забили диск машины до нуля: контрольный прогон доктестов P2-O7 упал 25 ошибками линковки `link.exe 1108/1140/1201`, повтор после освобождения места — зелёный; три брошенных каталога завершённых воркеров (~90 ГБ) удалены центральной сессией; хостовый `target/` — 221 ГБ | `findings/WORKER-REPORT-P2-O7.md`; `df -h` до/после | пакет воркера обязывает удалять приватный каталог сборки в конце и трактовать ошибки линковки при полном диске как окружение; не больше двух воркеров с приватной сборкой одновременно |
| J-079 | 2026-09-12 | успех | P2-O7 сел четырьмя атомами: манифест страниц со статусами по D-19 и `llms.txt` четырёх тиров на язык (`55dfd646`), время чтения и провенанс без версий и отпечатков (`27de558e`), проверка переводов по страницам (`ff71092a`) и по блокам с первым разошедшимся `pNN` (`5ad8c7a8`); `LOC-DOCUMENTS-MATCH` живёт в `vibe check` (A2.5) — сверить ячейку поручено P2-O8 | `findings/WORKER-REPORT-P2-O7.md` | — |
| J-080 | 2026-09-12 | успех | A3.13 пятью воркерами по темам: 496 пометок за один проход при коридоре 5–15 % фактов `done` на документ (по пакетам 7,6–10,8 %); каждая пометка — строка в воспроизводимом скрипте `P3-M<n>-mark.py`, дифф проверялся побайтово; сомнения и факты с уже стоящим `action=` вынесены в отчёты для центральной сессии | `findings/WORKER-REPORT-P3-M1…M5.md` | разметка обязательств — скриптом с перечнем, не правкой руками: перечень и есть ревью |
| J-081 | 2026-09-12 | находка | Статусный барьер: документы объявлены BUILT, а их факты носят `spec/work` или `impl/plan` (PROP-050 — восемь узловых фактов о `friends`, `E(R)`, семвере `access`; PROP-053 — весь `vibe clean`; PROP-010 `USER-LEVEL-REGISTRIES`, `HASH-INTEGRITY-GATE`; ещё семь цитируемых руководством фактов не-`done` у M3); руководство их уже цитирует, но обязательством они стать не могут | отчёты M1, M2, M3, M5 | X-032: актуализация статусов — атом кампании фактов, не документации; второй проход разметки после неё |
| J-082 | 2026-09-12 | находка | Гейт зоны дважды сработал ложно на легальных именах: `\bREALITY\b` на якоре `REALITY-WINS`, шаблон адреса на ссылке `§2.3.1.4`; воркеры не прятали имена, а доложили | `9db1cc33`, `7b102c59` | шаблоны гейта сужены (дефис после слова, знак параграфа перед числами); правило — ложное срабатывание чинится в гейте, не обходом в тексте |
| J-083 | 2026-09-12 | успех | Первая страница по итогам разметки — `model/dependency-visibility`: PROP-050 давал 24 обязательства при нуле цитат; страница написана по текстам самих фактов (скрипт `fact_text` печатает факты по перечню пометок), одно снятие раннером `--only`, регистр корпуса 45/60/408 | коммит страницы | новая страница под непокрытый документ пишется по перечню его обязательств, а не по пересказу спеки |
| J-084 | 2026-09-12 | успех | Покрытие обязательств доведено с 34/25/0/7 % до 100 % по четырём аудиториям за одну сессию без ожидания гейта: скрипт-предпросмотр повторил правило A2.16 (обязательство покрыто, если его цитирует страница той же аудитории) и печатал пробелы по документам с текстами фактов; сначала расширены аудитории страниц, которые служат нескольким читателям (44 «цитат не той аудитории» закрылись без правок текста), затем правила добавлены в секции, которые их уже пересказывали, затем написаны три страницы под документы без единой цитаты | `6bd8272d`, `coverage_preview.py`, `gap_text.py` | порядок закрытия пробелов: аудитория страницы → правило в готовую секцию → новая страница; текст факта читается по перечню пробелов, а не спека целиком |
| J-085 | 2026-09-12 | находка | P1-O6: красные `cli_search` — устаревший тест (лестница индекса выросла третьей ступенью в `764cf517`, «нет индекса» теперь пишется `index_url = "none"`); панель мертва на шаге 3 из 49 всю сессию — рэтчет wire-derive, `vibe-core` 35 против замороженных 34 после `29351a68` (таблица `documentation` манифеста); два приватных каталога сборки воркера весили 84 ГБ и дважды роняли линковку | `findings/WORKER-REPORT-P1-O6.md` | база рэтчета поднимается тем же атомом, что и новая ручная структура wire, с объяснением в теле коммита; панель гоняется на чистом дереве после посадки фазы |
| J-086 | 2026-09-12 | успех | Второй проход разметки центральной сессии — два факта тем же скриптовым форматом (`P3-M-central-mark.py`), процитированы там, где страницы уже рассказывали их (`e61adfdc`); база рэтчета wire-derive поднята для таблицы `documentation` манифеста с объяснением в теле коммита (`e2732d79`) — панель снова измеряет с шага 3; очередь примеров пополнена тремя снятиями (`80ffb250`) | коммиты | пометки, снятые по не-критериальной причине, добираются вторым проходом в том же формате, что и первый |
| J-087 | 2026-09-12 | успех | P4-O1 сел четырьмя атомами: пакет сайта под дисциплиной TypeScript, токены с собственным аудитом APCA (40 пар зелёные после минта), тринадцать компонентов, контракт острова и типы из JTD; половина времени A4.2 ушла на баг беты Qwik (`<RouterOutlet />` в layout — X-034), найденный прямым запуском воркера SSG и замером CPU | `findings/WORKER-REPORT-P4-O1.md` | в пакетах фазы 4 называть `<Slot />` в layout и порядок сборок явно; минт токенов по одному порогу схлопывает уровни текста — на дизайн-ревью нести таблицу «было → стало» |
| J-088 | 2026-09-12 | успех | Настоящий гейт покрытия совпал с предпросмотром: 516 из 516 обязательств, 100 % по 571 паре аудиторий; цитаты 759/759; примеры 55 совпали при 7 отложенных — три golden'а сдвинулись законно, потому что новые цитаты дали карте рёбра `documents` в `KIND-SET`, и `vibe explain`/`vibe select` стали показывать их; корневые гайды переписаны под руководство; три страницы `.md` ушли владельцу на стиль-ревью | `458ac8a8`, `85de0edc`, `7fbe4345` | golden примера, который показывает карту, меняется вместе с цитатами руководства — переблагословлять после каждого прохода цитат, не раньше |
| J-089 | 2026-09-12 | успех | P2-O8 сел шестью атомами и хвостом: гейт покрытия при первом запуске показал 13 % (8 обязательств из 68 до разметки A3.13) и 100 % на сдаче; `vibe doc build/check/manifest/serve` — тонкие адаптеры над библиотекой, сервер отдельным крейтом на loopback; MCP `read_doc`; скилл — строка во встроенном шаблоне; `projection.rs` разбит по бюджету длины | `findings/WORKER-REPORT-P2-O8.md` | воркер, чьи атомы зависят от разметки соседа, пишет в отчёт оба числа — до и после — иначе предсказание не проверить |
| J-090 | 2026-09-12 | находка | Два воркера в одном web-пакете: периметры по файлам держались, но два общих файла (`design/src/index.ts`, `tools/build.mjs`) не собирались без строк соседа, и P4-O5 закоммитил их со смешанным содержимым; сборка дерева на момент сдачи красная в периметре P4-O2 (незакоммиченный `view.ts`) — не по вине P4-O5 | `findings/WORKER-REPORT-P4-O5.md` | параллельные пакеты в одном пакете кода получают общие файлы-точки входа (индекс экспортов, скрипт сборки) заранее разведёнными: экспорт-барреля на пакет, шаг сборки на пакет; иначе — последовательно |
| J-091 | 2026-09-12 | успех | Тест паритета лендинга поймал два настоящих дефекта до сдачи (`/ru/` с `lang="en"` — контейнерный атрибут; двойная шапка) и зафиксировал 35 осознанных отличий по 14 правилам; имена краулеров проверены по девяти провайдерам с датой, два имени сняты как недокументированные, шесть добавлены | `131bdd1d`, отчёт | список осознанных отличий живёт в самом тесте с причинами — отличие без правила красит прогон |
