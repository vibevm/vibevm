# Пакет передачи в волну B {#root}

<status stage="spec" state="work" comment="собирается Fable в конце фазы P (P.9, PLAN §12.1); Opus читает его первым документом волны B, до boot-лейна репозитория; черновик 2026-09-12 — разделы «Что принято» и «Открытые вопросы» дописываются по отчётам PP-C1…PP-C3, PP-O1 и стиль-ревью P.8"/>

Ты — центральная сессия волны B, Opus 5 в режиме High. Этот документ —
первое, что ты читаешь; boot-лейн репозитория — после него. Он говорит,
что заморожено, что принято, чего не трогать, где тебя остановят и с чего
начать. Всё, чего здесь нет, — в `PLAN.md` этой зоны.

## 1. Что заморожено {#frozen}

| Документ | Где | Ревизия | Статус |
|---|---|---|---|
| Вижен | `vibevm/vibespecs/design/documentation-vision.xml` | `82620654` | заморожен; решения D-01…D-29 не пересуждаются (PLAN §0); единственная оговорка — D-21 (дизайн), пересматривается на A4.14 рукой владельца |
| План | `campaigns/docs-2026-09/PLAN.md` | `a6dbe253` | заморожен; правки плана — только маркер `<!-- REVIEW: … -->` и вопрос владельцу |
| Закон стиля | `vibevm/vibepacks/org.vibevm.core/vibevm-docs/v0.1.0/AUTHORING.md`; норма — PROP-057 §16 | `8f956eeb`, `82620654` | заморожен; линтер A2.25 реализует его, не переписывает |
| Регламент сопровождения | `campaigns/docs-2026-09/MAINTENANCE.md` | `a6dbe253` | черновик до фазы 6 (F4); пополняется только из журнала |
| Контракт | PROP-057 `vibevm/vibespecs/common/PROP-057-documentation-packages-and-site.xml` | `c049789c` | норма; `spec/work` только у SHELL-*, STACK-SHELL-EMBEDDING-PROPOSED, REL-FIELD-PLACEMENT, SITE-HOST-POLL, OBS-EXAMPLES-GOLDEN, READER-DESIGN-PROVISIONAL |
| Поправки фазы 1 | PROP-028 `4df9ced4`; PROP-045 §7 `0d31a877`; PROP-043 + PROP-047 `390c7b1d`; PROP-003 §2.7.6 `f46195bb`; PROP-000 + `VIBEVM-SPEC.md` §4.1 `ed75ce00` | — | сели; `ed75ce00` несёт пометку «pending ratification» — см. §6 |
| Жанр и зона | `vibevm/vibespecs/design/README.md` `ee94b72a`; `campaigns/packages-2026-09/PHASE-G-SPEC.md` `void` `b93af0b3`; `DEV-GUIDE.md` §8 `18da96a4`; BACKLOG B-123…B-130 `eb803e29` | — | сели |

Полная карта коммитов с хэшами — `LEDGER.md` §«Фаза 1» и §«Фаза P».

## 2. Что принято {#accepted}

**Дерево прозы** `vibevm/vibepacks/org.vibevm.core/vibevm-docs/v0.1.0/` —
44 страницы в 11 разделах, список страниц и якорей — `PAGE-INDEX.md`
(генерируется `tasks/page-index.py`; после любой правки страниц —
перегенерировать). Коммиты разделов: `7faef288` start, `4fcc71b2` model,
`d2a71051` howto, `73f24039` agent, `c604e671` lifecycle, `8ca8d5ba`
reference, `cc36532d` authoring, `8487c6a0` architecture, `818a474d`
glossary + faq + diagnostics; каркас `0df597b3`; правка фикстур `e78811cc`.

| Что | Состояние на передачу | Свидетельство |
|---|---|---|
| Страницы: XML корректен, inline — Markdown, `<`/`&` экранированы | принято | J-051…J-053; разбор 44 файлов стандартным парсером чист; `vibe check` 0 ошибок |
| `expect` сняты с бинарника | 39 из 59 сняты и вставлены (`700db4b8`, сборка и HEAD — в `findings/WORKER-REPORT-PP-C2b.md`); 15 ждут раннера A2.9 (список — `findings/PACKET-PP-C2c.md`), 5 — релиза, фазы 2 или починки B-133; снятия минимизированы словом владельца (J-067) | `EXAMPLES-TODO.md` §queue; `findings/PP-C2-expects/` |
| Инвентарь legacy без пустых строк | принято: 1184 строки с решением (J-064) | `LEGACY-INVENTORY.md`, колонка «решение»; `findings/WORKER-REPORT-PP-C3.md`, `findings/PP-C3-dispose.py` |
| P.6 стиль и P.7 цитаты с нулём замечаний | принято: замечания отработаны `435d3aef`, `2f48c888`; повторная проверка терминов и XML — 0 (J-059, J-060); открытые калибровки линтера — X-028, X-029 | `findings/PP-C1-prose-checks.md`, `findings/WORKER-REPORT-PP-C1.md` (скрипты `analyze.py`, `render_report.py`, `validate_md.py` — первые фикстуры A2.25) |
| Каждый промпт прошёл прогон P.7b | частично: 7 зелёных из 19 в первом прогоне; семь исправленных промптов перепрогоняет гарнитура A2.29 (список — `findings/PACKET-PP-O1b.md`); красные по багам продукта (B-133, B-135…B-138) остаются красными до починки | `findings/WORKER-REPORT-PP-O1.md`, `findings/PP-O1-runs/`; промпты, ждущие фазы 2: `read-docs-locally`, `write-documentation`, `translate-documentation`; не прогоняемые в песочнице: `install-vibe`, `give-your-agent-the-skill` (только `--dry-run`) |
| Стиль-ревью владельца P.8 | ⏳ OPEN | review-маркер в `LEDGER.md`; под целью сессии владельца волна B не ждёт его; замечания — в точке F1 |

## 3. Чего не трогать {#do-not-touch}

1. **Прозу** (R-29, PLAN §6.0). Правка в одну-две строки по линтеру или
   раннеру — на месте (R-32); всё крупнее — строка в `AUTHOR-QUEUE.md` с
   причиной, страницей и якорем, для точки F1/F3. Проза, написанная тобой
   вне очереди, откатывается.
2. **Чужие каталоги** (R-28): `talks/2026.08.08-agents-talk/`, `oleg-guru/`,
   `vibevm-org/` — только чтение.
3. **Хостовый nginx, порты, сертификаты, compose соседей, VPN** (R-24):
   ничего в кампании их не касается; любой шаг, который хотя бы
   потенциально их задевает, — стоп до действия и три пункта владельцу
   (что, зачем, что сломается), см. `PLAN.md` §13.
4. **Секреты** (R-12): токены не читаются в контекст и не печатаются;
   `infra/` и `secrets.local.md` не открываются.
5. **Генерируемое** (R-03, R-08): `STATIC.xml`, `INDEX.md`, `specmap.json`,
   `derived` — только через генератор.
6. **Якоря** (R-06): переименование — tombstone, никогда удаление.

## 4. Точки возврата Fable {#return-points}

Зовёт владелец. Ты останавливаешься, собираешь очередь и не пишешь прозу
сам.

| Точка | Когда | Что должно быть готово к ней | В каком виде передаётся |
|---|---|---|---|
| F1 | после A2.25 (линтер) и A2.9 (раннер) прогнаны по прозе фазы P | линтер и раннер зелёные по всему корпусу, кроме строк очереди; замечания владельца по P.8 (если ревью состоялось) | `AUTHOR-QUEUE.md`: страница#якорь, причина, точка F1 |
| F2 | дизайн-ревью A4.14 | три страницы в двух темах в ридере, скриншоты, отчёт APCA (по образцу A0.23) | очередь по смыслу токенов и компонентов + решение владельца по D-21 |
| F3 | стиль-ревью A3.16 после механики, если понадобилось | страницы в `.md`-проекции и в ридере; `reviews.toml` | очередь с замечаниями владельца |
| F4 | фаза 6: A6.1, A6.4 | репетиции A6.2–A6.3 проведены, метрики сняты, журнал полон | журнал + черновик регламента; Fable консолидирует, пишет changelog и страницу «How this manual is maintained» |

## 5. С чего начать {#start}

На 2026-09-12 фаза 2 начата этой же центральной сессией под целью
владельца: пакеты P2-O1…P2-O4 в `findings/`, карта коммитов — `LEDGER.md`
§«Фаза 2». При передаче другой сессии — продолжать с этой карты, не с A2.0.


1. Прочитать этот пакет, затем boot-лейн репозитория, затем `PLAN.md` §0 и
   §2 буквально, затем фазу 2 целиком (`PLAN.md` §«Фаза 2»).
2. Первый атом — A2.0 (крейты по чеклисту §2.8), первый крейт — в A2.9.
   Порядок фазы 2: A2.1–A2.7 виды, манифест, индекс, гейт; A2.8–A2.12 пивот
   и проверки; A2.13–A2.21 конвейер и поверхности.
3. Findings фазы 0 — `PHASE-0-FINDINGS.md` и `findings/`; леджер —
   `LEDGER.md`; отложенное — `DEFERRALS.md` (X-001…X-029, каждое с
   триггером и атомом); журнал — `JOURNAL.md`, ведётся по R-33 в том же
   атоме, где случилось событие.
4. Первые фикстуры: раннер A2.9 — `EXAMPLES-TODO.md` §fixtures и корпус
   PP-C2; линтер A2.25 — скрипты PP-C1; прогон промптов A2.29 — корпус
   PP-O1; гейт doc-пакетов A2.5 — X-024.
5. Панель `tools/self-check.sh`: шаги 1–4 зелёные (`bb6320b1`, `03f01007`);
   на шаге 5 (`cargo test --workspace`) после `b73195fd` красными остаются
   `golden_corpus` (решение владельца, J-069) и два теста `cli_search`
   (пакет P1-O6 после фазы 2); шаг 6d (`wire-diff`) — тот же корпус `index/e1`.

## 6. Открытые вопросы владельцу на дату передачи {#open-questions}

Каждый — с рекомендацией; не изобретай ответы, работай по рекомендации и
помечай атом «под ратификацию», пока владелец не сказал иначе.

| Вопрос | Рекомендация | Где ждёт |
|---|---|---|
| A1.1/A1.2: поправка `VIBEVM-SPEC.md` §4.1 и PROP-000 (виды `doc`, `app`) закоммичена `ed75ce00` под целью сессии | ратифицировать при слиянии; при отказе — откатить `ed75ce00` и PROP-057 остаётся предложением | `LEDGER.md` §review |
| F-05: версия `vibe` для `min_vibe_version` doc-пакетов | поднять `vibe` до 1.1.0 в A2.1; в манифестах doc-пакетов уже стоит `"1.1.0"` | A2.1 |
| F-35: адреса сайта, хоста и реестра | `https://vibevm.org/doc/`; хост `github.com/vibevm/vibevm` (`main`); реестр `github.com/vibespecs` — так написаны страницы | фаза 4 |
| D-23 / F-41: кто выполняет серверные шаги переключения | владелец по чеклисту A5.6; агент готовит чеклист и compose, не заходит на сервер | A5.6 |
| D-16: частота опроса хоста | раз в час, дебаунс 5 мин, dev-профиль рендерера (A0.28) | A5.1 |
| X-017: P1 в `vibe-index` (обход путей) | чинить вне кампании отдельным PR до публикации сайта; сайт не использует `vibe-index` | владелец |
| PROP-057 `REL-FIELD-PLACEMENT` | таблицы связи и `[media]` — верхний уровень манифеста; `title`, `abstract` — в `[package]`; так написан `vibe.toml` doc-пакета | A2.2 |
| PROP-057 §12: механизм встраивания оболочки | восемь пунктов A0.11 принять как есть | A4.6 |
| X-024: `--exhaustive` и страницы doc-пакета | `[judging] exempt` освобождает и от `--exhaustive`; страницы — не факты | A2.5 |
| X-025: герметичность примеров установки без `--offline` | реестр-каталог внутри фикстуры, подстановка `${REGISTRY}` в файлы фикстуры раннером; команда страницы не меняется | A2.9 |
| X-026: примеры, снимаемые на релизе | атрибут `captured="release"` у `example`; раннер пропускает с причиной, панель релиза снимает | A2.9, фаза 5 |
| X-027: карточка предмета `org.vibevm.core/vibevm` | сайт берёт карточку предмета из doc-пакета, который его документирует, пока корень хоста — `[project]` | A2.3 |
| X-005, X-015 | мелкое: `vibevm/vibedeps/.gitignore` в истории; фирменное `og.png` | фаза 4 |
| корпус `index/e1` (J-069): байты gzip зависят от унификации фич Cargo | прибить реализацию deflate внутри `vibe-index` (вариант C отчёта P1-O4 §3.4); до решения шаг `wire-diff` панели красный по унаследованной причине | владелец, `findings/WORKER-REPORT-P1-O4.md` §3 |
| P.8: стиль-ревью трёх страниц | `start/what-vibevm-is`, `howto/work-offline`, `authoring/write-a-flow`; замечания — в F1 | владелец |
