# Кто исполняет делегированные задачи фаз E и T этой кампании.
#
# Переключатель принадлежит ВЛАДЕЛЬЦУ: правьте эту строку в любой момент
# (или скажите в чате — босс обновит файл). Босс перечитывает файл перед
# КАЖДЫМ fan-out'ом воркеров, поэтому переключение действует немедленно.
# Исторические launcher-механики: SUBAGENT-LAUNCHERS.md рядом; они не являются
# текущей execution authority.
#
#   "native"  — нативные Codex collaboration workers; текущий и единственный
#               live mode этой кампании
#
# Subscription-backed external GLM launcher отменён владельцем 2026-09-08;
# это семейство MUST NOT запускаться и не является допустимым mode.
#
# Ревью, вердикты, роутинг якорей и коммиты в ЛЮБОМ режиме остаются
# за боссом — режим меняет только исполнителя черновой работы.
# История прежних launcher assignments сохранена в SUBAGENT-LAUNCHERS.md;
# она не является текущей execution authority.
mode = "native"
