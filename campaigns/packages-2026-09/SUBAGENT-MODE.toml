# Кто исполняет делегированные задачи фаз E и T этой кампании.
#
# Переключатель принадлежит ВЛАДЕЛЬЦУ: правьте эту строку в любой момент
# (или скажите в чате — босс обновит файл). Босс перечитывает файл перед
# КАЖДЫМ fan-out'ом воркеров, поэтому переключение действует немедленно.
# Инструкция и все механики: SUBAGENT-LAUNCHERS.md рядом.
#
#   "codexrunner" — воркеры = OpenAI Codex CLI (gpt-5.6-sol, xhigh)
#               через запускалку codexrunner (лейн добавлен владельцем
#               2026-08-20)
#   "claudez" — воркеры = Claude Code на GLM через запускаторы
#               claudez / claudez2 (две параллельные линии, worktrees)
#   "native"  — воркеры = нативные субагенты хэрнесса (opus5),
#               как исполнялась фаза D
#
# Приоритет ЛЕЙНОВ внутри режима тоже выбирает владелец — действующий
# порядок: SUBAGENT-LAUNCHERS.md ##launchers-priority-is-the-owners.
#
# Ревью, вердикты, роутинг якорей и коммиты в ЛЮБОМ режиме остаются
# за боссом — режим меняет только исполнителя черновой работы.
mode = "codexrunner"
