[package]
name = "router-win"
group = "org.vibevm.probe"
kind = "flow"
version = "1.0.0"
epoch = 1
authors = ["Oleg Chirukhin"]
license = "UPL-1.0"
description = "Windows-gated router measurement probe"

[compatibility]
min_vibe_version = "0.1.0"
requires_kinds = []

[boot_snippet]
source = "spec/boot/40-flow-router-win.md"
category = "flow"
when = "os:windows"
