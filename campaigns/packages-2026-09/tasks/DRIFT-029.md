# DRIFT-029 — a content-minimal aggregator stops contributing a boot lane it does not have {#root}

```
<status stage="impl" state="plan" ref="DRIFT-029"/>
```

**Status:** queued
**Executor:** Opus. **Reviewer:** Fable, against §6 verbatim.
**Cluster:** workspace
**Finding:** F-078 (campaign LOG, 2026-07-26).

## 1. Goal {#goal}

`vibe install` / `vibe update` must stop generating boot artifacts inside a
materialised `vibedeps/` slot, so that a content-minimal aggregator cannot
contribute a *generated* static lane that duplicates the members the host has
already compiled directly.

## 2. Contract {#contract}

```
> Content-minimal (PROP-028): no boot snippet of its own; each member ships theirs.
> — packages/org.vibevm.world/git-practices/v0.1.0/vibe.toml, [package].description
```

```
> The boot snippets of the transitive deps still land in spec/boot/INDEX.md
> via the resolver's BFS closure (PROP-009).
> — vibe.toml, [requires.packages] comment
```

The host already compiles every member of the closure exactly once. An
aggregator that also contributes a pre-compiled lane containing those same
members makes the host read them twice.

## 3. Current state {#current}

Measured 2026-07-26, immediately after `vibe update --all` on a clean tree.
**Do not re-discover these — they are verified.**

- `packages/org.vibevm.world/git-practices/v0.1.0/` contains **exactly three
  files**: `LICENSE`, `README.md`, `vibe.toml`. There is **no `spec/`**.
- `.vibe/cache/org.vibevm.world/git-practices/v0.1.0/` contains **the same
  three files**. There is **no `spec/`**. The cache is clean; cache poisoning
  is ruled out.
- After the run, `vibedeps/flow-git-practices/0.1.0/spec/boot/STATIC.md` (192
  lines) and `.../INDEX.md` **exist** and are untracked. Both carry the
  `generated by vibe, do not edit` banner; the `INDEX.md` carries a
  `# vibe:fp <hash>` fingerprint line.
- By elimination (source clean + cache clean + slot populated), **vibe wrote
  those two files during materialisation**.
- The host's `spec/boot/STATIC.md` grew by **194 lines, 0 deletions**, and now
  carries each of the four git-\* snippets **twice**:
  - first copy, from the members' own snippets — lines 363, 421, 486, 514;
  - second copy, under
    `<!-- vibe:static org.vibevm.world/git-practices — vibedeps/flow-git-practices/0.1.0/spec/boot/STATIC.md -->`
    at line 551, with the four members repeated at 557, 615, 680, 708.
- `git-practices`' `vibe.toml` declares **no `[boot_snippet]`**. The
  contribution therefore came from a path-based fallback that found the file
  vibe had just generated.
- Only `flow-git-practices` is affected among the 36 materialised slots. The
  one other slot carrying a generated `INDEX.md`
  (`vibedeps/flow-delegation-rules/0.1.0/`) ships it **from source** —
  `packages/org.vibevm.fractality/delegation-rules/v0.1.0/spec/boot/INDEX.md`
  exists and is tracked. That one is **not** this task's subject.

## 4. Required behavior {#behavior}

The mechanism is for you to locate; the outcome is fixed. Start at
`crates/vibe-workspace/src/install/bootgen.rs` and the node enumeration the
installer walks (`iter_nodes`) — the reporter line
`"Materialised {} package{} into vibedeps/; regenerated boot artifacts for {} node{}"`
(`crates/vibe-cli/src/commands/install/report.rs:96`) said **1 node**, so the
count and the write are worth reconciling before changing anything.

```
1. Find the code path that writes spec/boot/{INDEX,STATIC}.md into a
   vibedeps/ slot. Name it in §9 with file:line before you change it.
2. A materialised dependency slot is a consumer copy, not a project node:
   it must not have boot artifacts compiled into it. Suppress the write.
3. Verify the host-side duplication disappears as a consequence. If it does
   NOT — i.e. the aggregator still contributes a lane from some other path —
   stop and report; do not add a second, independent de-duplication hack to
   paper over it.
4. Materialisation must stay byte-idempotent: a second `vibe update --all`
   on an already-updated tree changes nothing.
```

Edge cases: a package that genuinely **ships** `spec/boot/STATIC.md` from
source (i.e. the file is present in the source package directory) must keep
contributing it exactly as before — the fix distinguishes *generated here* from
*shipped by the package*, not "has a STATIC.md". `delegation-rules` is the live
regression case for this: its `INDEX.md` comes from source and must survive.

Error paths: none new. This removes a write; it introduces no failure mode.

## 5. Boundaries {#boundaries}

- **Do not touch** `spec/**` — any of it. If a spec unit turns out to specify
  the current behaviour, that is a §8 stop, not an edit. The reviewer lands
  every spec change in this campaign.
- **Do not touch** `packages/**`. In particular, do not "fix" this by adding a
  `spec/boot/` to `git-practices` or by editing its manifest — the package is
  content-minimal by design and that is correct.
- **Do not hand-edit** `spec/boot/STATIC.md` or `spec/boot/INDEX.md`. They are
  generated; the fix is upstream of them and is verified by regenerating.
- **Do not touch** `campaigns/**` except §9 of this file.
- Never edit golden tests to make them pass.

## 6. Acceptance {#acceptance}

```bash
cargo fmt --all
cargo test -p vibe-workspace
bash tools/self-check.sh ; echo "EXIT=$?"
```

The floor's real exit code must be `0` — read the number, never a piped tail.

Then, from a clean tree (`git status` empty):

```bash
cargo run -q -p vibe-cli --bin vibe -- update --all --assume-yes
git status --short
```

- `vibedeps/flow-git-practices/0.1.0/spec/` **does not exist**.
- `grep -c "^# Flow: Atomic Commits" spec/boot/STATIC.md` → **1** (it is 2 today).
- `grep -c "vibe:static org.vibevm.world/git-practices" spec/boot/STATIC.md` → **0**.
- `vibedeps/flow-delegation-rules/0.1.0/spec/boot/INDEX.md` **still exists** —
  the shipped-from-source case is untouched.
- A second identical `update --all` run leaves `git status` unchanged
  (idempotence).

New test: `vibe-workspace` gains a test asserting that materialising a package
whose source directory has no `spec/boot/` produces a slot with no
`spec/boot/`. Name it for the behaviour, not for this bug.

Discipline: `#[spec(implements = "spec://…#anchor")]` on new items where an
anchor governs, `cargo fmt --all`, clippy clean, one atomic commit, Conventional
Commits, **no AI attribution anywhere** (repo Rule 1).

## 7. Analogies {#analogies}

`crates/vibe-workspace/src/publish/staging.rs:164` — `regenerate_published_boot`
is the *sanctioned* case of compiling a boot lane into a copy (a staged publish
needs it, PROP-009 §2.11). Read it to see the shape of a deliberate
regeneration, then confirm the `vibedeps/` write is **not** that.

## 8. Stop rule {#stop}

- If a spec unit specifies that a materialised slot carries compiled boot
  artifacts: **STOP.** Mark `<!-- REVIEW: … -->` at the code point, write the
  question here, set status `returned`. Do not invent semantics and do not
  edit the spec.
- If any `spec/**` file enumerates where boot contributions may come from and
  your fix would contradict it: same stop.
- **Budget signal:** past **6 files / 150 lines**, stop and return with
  findings — the task was mis-scoped and the defect is bigger than diagnosed.

## 9. Log {#log}

*(appended by executor / reviewer)*
