# Чеклист ручной инспекции владельца — релиз 1.0.0 (закрытая альфа)

Составлен на релизном гейте С10 (2026-08-20). Порядок — от быстрого к
глубокому; каждая строка самодостаточна. Отметки — владельцу; агентские
пре-прогоны записаны и никого не подписывают. После инспекции — акты вне
марафона: новый PAT для С6-волны, тег `v1.0.0`, `cargo xtask mirror`
с тегами, слово тестерам.

## 1. Дистрибутив Windows (10 минут)

- [ ] `dist/vibe-1.0.0-windows-x86_64.zip` — на диске, **вне git**
      (`dist/` в `.gitignore`; больших блобов в истории нет). SHA256:
      `dbc34fddc1081ed19389867f66477d2f5616d51fb473e633e70cbab7264061b9`.
- [ ] Состав: `vibe.exe` (static CRT — импорты без VCRUNTIME/MSVCP,
      проверено настоящим dumpbin), `install.ps1`, `uninstall.ps1`,
      `README-INSTALL.md`, `LICENSE.md`, `ALPHA-NOTES.md`,
      `SHA256SUMS.txt` (первая строка честно говорит «corruption check
      only, not authenticity» — подписи пакетов B-015 паркованы).
- [ ] Авторские исходники скриптов + рецепт сборки:
      `distribution/windows/{install.ps1,uninstall.ps1,README-INSTALL.md,BUILD.md}`.
- [ ] **Реальная установка на живую машину — владельческий акт** (агент
      её по закону не делал: живой `~/.vibe` и PATH неприкосновенны).
      Рецепт: распаковать zip → `powershell -ExecutionPolicy Bypass -File
      .\install.ps1` → новый терминал → `vibe --version`. Два песочных
      smoke уже записаны (C9-сборка и С10-пре-прогон, оба PASS:
      install → shim-проверки → reuse → uninstall; живой PATH нетронут).

## 2. Документация (15 минут)

- [ ] `README.md` — правда 1.0.0: секция дистрибутива, quick-start c
      `vibe self import ./vibe.exe --tag 1.0.0 --use` (команда
      воспроизводит smoke-проверенную дословно).
- [ ] `docs/ALPHA-NOTES.md` — обещание совместимости альфы + **Known
      alpha limitations** (три честные строки: B-096 prefs-схема вне TUI,
      B-097 `tree --quiet`, B-098 форма origins).
- [ ] `CHANGELOG.md` — `## [1.0.0] — 2026-08-20`, дуга релиза одним
      экраном; `Unreleased` пуст.
- [ ] `docs/SITE-MANIFEST.toml` — 22 страницы, release 1.0.0;
      `docs/commands/` — 4 новых + 8 обновлённых.

## 3. Аудит-секция (10 минут) — АПРУВ ВЛАДЕЛЬЦА

- [ ] `AUDIT.md`, секция «Audit run — 2026-08-20 … DRAFT» — прочитать и
      апрувнуть/править. 5 новых строк (-01 токен-C4/P1-filed,
      -02 скрипты вне гейта, -03 запись реестра только pure-core-тесты,
      -04 новый advisory `lru`, -05 ignore-ценз 7) + пере-суд переносов
      (строка -09 закрыта; -10 сужена).
- [ ] **Владельческие решения, которые аудит эскалировал:**
      - [ ] 2026-08-03-04 (D3): тень `#[allow]` — один рулинг о
            TUI-подсистеме (39 сайтов) + 15 поименованных немых
            `dead_code`-сайтов. Эскалировано после трёх прогонов.
      - [ ] Райдер 2026-06-12-01: подтвердить, что history-rewrite
            2026-06-11 был намеренным (четвёртый прогон носит).

## 4. Credibility report (10 минут)

- [ ] `campaigns/packages-2026-09/CREDIBILITY-REPORT.md` — первый абзац
      честно называет предел (фаза T не прогнана); 26 практик:
      19 держатся / 5 с оговоркой / 2 неисследованы.

## 5. Ручные тесты — САЙН-ОФФ ВЛАДЕЛЬЦА (30–40 минут)

Закон manual-tests: агентский пре-прогон записан
(`campaigns/packages-2026-09/MT-PRERUN-2026-08-20.md`), подпись — только
владельца. В отчёте — **готовые launch-блоки** (изолируют живой `~/.vibe`
через `VIBE_SETTINGS=temp`) для обоих тестов.

- [ ] MT-02 `vibe tree` TUI — 10 шагов глазами (glyphs, footer, карта,
      режимы, сорт/шейп, поиск, копи, палитры, персистентность).
- [ ] MT-03 `vibe prefs ui` — 8 шагов глазами (дерево страниц, формы,
      apply/reset, provenance, валидация/линт, поиск, actions, тема).
- [ ] MT-01 (`vibe tree` статический) — по желанию: не-TUI половина уже
      зелёная в пре-прогоне (plain/json/help; см. отчёт).
- [ ] **Расхождения пре-прогона — диспозиция владельца** (все уже в
      BACKLOG + ALPHA-NOTES, ничего не чинилось молча на гейте):
      B-096 (P2, prefs без схемы вне TUI), B-097 (P3, `--quiet`),
      B-098 (P3, флаг vs сабкоманда origins), B-099 (P3, stale help
      «phase 2.7»).

## 6. Перекличка гейта — агентская, перепроверяемая одной командой

| Строка | Состояние на 2026-08-20 |
|---|---|
| `bash tools/self-check.sh` | **зелёная, EXIT=0** (после законного raise ratchet 41→42, 99cbdcd3) |
| `vibe --version` | `vibe 1.0.0` |
| Судейский долг | **0/0** (`judging-debt.py`: no debt) |
| `vibe check` | **0 errors / 1 warning** — только `wal_wellformed`; закрывается перезаписью WAL на wind-down; `manifest_epoch` info = 0, freshness = 0 |
| `cargo xtask mirror --check` | все цели в sync (строки читаны — B-090) |
| `formats/EPOCHS.toml` | `public = false` — переключатель не тронут |
| `formats/REGISTRY.toml` | 37 форматов (≥20 + конверты vibe-index) |
| Дистрибутив | zip на диске, два smoke PASS |
| **E2E С6 (реальный реестр)** | **ЖДЁТ ТОКЕНА** — блокер §10: PAT в `~/.vibe/github.publish.token` мёртв (Bad credentials); рецепт нового — в §10. После токена: канарейка → 42 публикации → E2E чистого каталога → EOL-стабильность |

## 7. Открытые владельческие развилки (не блокеры инспекции)

- [ ] B-095 — граница класса «шире 32 бит — строкой» vs count-поля;
      там же пустой axum-404 vs `ErrorResponse`.
- [ ] Лестница конфига vibe-index: диалект booleans + имя `--limit`
      (отчёт R3-LADDER).
- [ ] Nullable-enum в постпроцессоре (`list.kind`).
- [ ] Минт схемы конверта `vibe-index config --json` (ratchet-строка
      vibe-index 26 записана именно под него).
- [ ] `vibe-publish` post_hook: env-only копия.

## 8. После инспекции (вне марафона, по слову владельца)

1. Новый PAT (repo-create в org `vibespecs`) одной строкой в
   `~/.vibe/github.publish.token` → сказать слово — агент прогонит
   С6-волну (канарейка → 42 → E2E → EOL) и закроет перекличку целиком.
2. Тег `v1.0.0` + `cargo xtask mirror` (fast-forward, с тегами).
3. Объявление тестерам закрытой альфы.
4. Дистрибутивы Linux/Mac — другая машина (рулинг §0.9).
