{
 "schema": 1,
 "updated_at": "2026-07-26T19:45:00Z",
 "findings": [
  {
   "id": "F-068",
   "from": "packages/org.vibevm.ai-native/rust-ai-native/v0.7.0/README.md",
   "to": "packages/org.vibevm.ai-native/rust-ai-native/v0.7.0/vibe.toml",
   "type": "reality-mismatch",
   "status": "open",
   "wave": 2,
   "note": "THE DOCUMENTED INSTALL INSTRUCTION CANNOT RESOLVE THE PACKAGE IT IS PRINTED IN, and it is family-wide rather than a typo. rust-ai-native README:15 says: Require `stack:org.vibevm.ai-native/rust-ai-native` (`^0.6`) to get the whole family - while that package's own vibe.toml is version = \"0.7.0\". Under caret semantics for 0.x, ^0.6 means >=0.6.0 <0.7.0, so it EXCLUDES the very version shipping the sentence. typescript-ai-native has the identical defect one minor down: README:16 says ^0.5, vibe.toml says 0.6.0. go-ai-native (v0.1.0) carries no such line. Both offenders are EXACTLY one minor behind their own version, which is the signature of a mechanical version bump that did not touch the prose - the same class as wave 1's stale status lines, but worse, because a status line misleads a reader while this one misconfigures a consumer. Found in the FIRST aggregator file opened during the Phase A pilot. Fix direction: the caret in an aggregator README is derived data - it should be generated from, or gate-checked against, the manifest version, because a family aggregator's whole job is to name a version set and it bumps on every release."
  },
  {
   "id": "F-069",
   "from": "packages/org.vibevm.ai-native/rust-ai-native/v0.7.0/README.md",
   "to": "spec/modules/vibe-progress/PROP-043-progress-markup.md",
   "type": "missing-support",
   "status": "open",
   "wave": 2,
   "note": "PREDICTION 2 CONFIRMED AT THE PILOT, and concretely. The plan predicted the aggregator genre would need at least one grammar amendment to PROP-043 3.8. The umbrella packages are content-minimal by design (PROP-028): rust-ai-native carries a 19-line README and a manifest, and THREE OF ITS FOUR SUBSTANTIVE FACTS ARE ABOUT OTHER PACKAGES - what rust-ai-native-lang ships (GUIDE, cards, toolchain), what rust-ai-native-mcp serves, that core-ai-native arrives transitively. Marking those `@impl/done` here asserts, in this document, something this document cannot be the source of truth for: the truth conditions live in another package, and when that package changes nothing here notices. Wave 1 never hit this because a host PROP owns its own subject matter. The grammar needs a way to say `this fact is ABOUT <other unit>` - a borrowed or delegating anchor whose verdict is derived from the owning unit's rather than asserted independently, so a stale aggregator is detected by the owner moving rather than by someone re-reading the aggregator. Until that exists, the honest fallback is to mark such facts `unverifiable` in the aggregator and verify them where they live."
  },
  {
   "id": "F-070",
   "from": "progress.toml",
   "to": "crates/progress-core/src/scope.rs",
   "type": "missing-support",
   "status": "open",
   "wave": 2,
   "note": "THE EXHAUSTIVE COUNTER DEMANDS FACT MARKUP ON VERBATIM LICENCE BOILERPLATE. Measured at the Phase A pilot 2026-07-26: `progress check --exhaustive` reports 8 992 unmarked paragraphs across the widened scope, and 264 of them (3%) live in 33 LICENSE.md files - the same UPL-1.0 text copied once per package version slot. Marking them would mint ~264 fact anchors on someone else's words, 33 times over, and every one of them would be a fact this project is not the author or the source of truth for. Wave 1 never hit this because spec/ carries no LICENSE files. The precedent already exists and points the right way: DEFAULT_EXCLUDES (scope.rs:11-20) already drops `refs` as third-party and `fixtures` as not-a-contract, always-on and even under an explicit include. LICENSE.md is the same category. FIX DIRECTION: add it to DEFAULT_EXCLUDES rather than to progress.toml - section 4 is include-only BY DESIGN ('exclusions are expressed by enumeration'), so there is no config-side way to express this and it needs a small code change plus a test. Until then the Phase B exit gate (`check --exhaustive` green over both corpora) cannot be reached honestly: either 264 boilerplate paragraphs get marked, or the gate stays red for a reason nobody should have to re-derive."
  },
  {
   "id": "F-071",
   "from": "progress.toml",
   "to": "packages/org.vibevm.ai-native/rust-ai-native-lang/v0.7.0/spec/cards/INDEX.md",
   "type": "missing-support",
   "status": "open",
   "wave": 2,
   "note": "THREE DERIVED FILES ARE IN THE OBSERVED SCOPE AND MUST NOT BE. Found 2026-07-26 on an owner challenge to prove nothing generated had been let in. `spec/cards/INDEX.md` in all three language stacks (rust v0.7.0, typescript v0.6.0, go v0.1.0) says of ITSELF, verbatim: 'Generated/maintained as a derived index (A2/R-030); hand edits are a defect.' Marking them is by their own words a defect, and any markup written there dies on the next regeneration - exactly the reason wave 1 put spec/boot/STATIC.md and spec/boot/INDEX.md out of scope (progress.toml, owner ruling 2026-07-24). Wave 1 expressed that exclusion through its include glob `spec/boot/[0-9]*.md`, which drops unnumbered files naturally; `packages/**/*.md` has no such trick and section 4 is include-only by design, so this needs the same code-side fix surface as F-070. VERIFIED CLEAN in the same sweep, so nobody re-audits it: across all 344 observed files there are exactly two top-level roots (`packages`, `spec`) and NO path segment named target, vibedeps, node_modules, vendor, .vibe, refs, campaigns, fixtures, dist, build, generated or .git - DEFAULT_EXCLUDES (scope.rs:11-20) holds those, always-on and even under an explicit include. A first sweep for 'generated/do not edit' phrasing returned 11 files and ALL ELEVEN WERE FALSE POSITIVES - documents ABOUT generated code (the Go/Java/Kotlin guides' generated-code rule, scaffold-a-generators describing the `// Code generated ... DO NOT EDIT.` marker, git-attribution-policy discussing 'generated by' trailers, managed-blocks describing do-not-edit notices, wal-status instructing a reader not to edit the WAL). The three real ones use different wording entirely ('derived index'), which is why a phrase-matching sweep alone is not a safe audit for this."
  },
  {
   "id": "F-072",
   "from": "crates/vibe-cli/src/commands/progress.rs",
   "to": "conform.toml",
   "type": "missing-support",
   "status": "open",
   "wave": 2,
   "note": "progress.rs sits at 599/600 lines after DRIFT-024 - ONE line under the budget conform gates on. The executor hit 614 on its first version, watched `conform check` fail on file-length, and rewrote the adapter edit to its true minimum to land at 599; it then declined to take the split, correctly, because that is a structural decision. So the next change to that file, however small, does not fit. The split the rule prescribes is already visible in the file's own shape: Ground / ground() / refresh_state() plus the campaign-path helpers are one cell, the same shape as the existing `mod baseline` and `mod rescan` submodules. Cheap now, forced and rushed later - and it will be discovered by whoever is mid-way through an unrelated task."
  },
  {
   "id": "F-073",
   "from": "campaigns/progress-2026-08/run",
   "to": "campaigns/packages-2026-09/run",
   "type": "contradiction",
   "status": "open",
   "wave": 2,
   "note": "A NEW HOST-CORPUS ANCHOR NOW HAS NO CAMPAIGN TO JUDGE IT. Wave 1 is closed out and its run/ is disposable by ZONE-LIFETIMES; wave 2's subject is packages/, and the host's 58 files are in its scope only to keep the shared `progress check` gate at 0 - wave 2 does not verify them. So when a host spec gains an anchor (as PROP-043 just did, twice today, via sync-from-code), there is no live zone that owns its verdict: wave 1's cache is the only home and it can no longer be refreshed by any tool, because every parsing subcommand rescans the GLOBAL scope and drags 286 package files into it. Worked around today by hand-editing wave 1's cache and setting content_hash to the file's real sha256 so the staleness flag fires honestly. That is a workaround, not a design. Options for the owner: (a) let wave 2 own the host corpus's verdicts too, since it already observes it; (b) give a campaign its own scope rather than sharing progress.toml; (c) accept that the host corpus is frozen at wave 1's close and re-open it only at the next recurrence run."
  },
  {
   "id": "F-074",
   "from": "spec/boot/00-core.md",
   "to": "spec/WAL.md",
   "type": "contradiction",
   "status": "open",
   "wave": 2,
   "note": "THE NOTOUCH LIST LOST THREE OF ITS FOUR ENTRIES ON OWNER INSTRUCTION (2026-07-26), and the guard that remains is weaker than the one removed. `spec/boot/00-core.md`, `spec/boot/90-user.md` and `VIBEVM-SPEC.md` are no longer listed under 'Files you MUST NOT touch without explicit instruction'; only `refs/book/` is. The immediate effect was good and intended - it unblocked F-063's second half, which had sat on the owner's desk purely because no session could edit 90-user.md. The standing effect is that the two boot files carry the OWNER'S OWN machine facts, credentials layout and personal preferences, and any session may now re-form that prose wholesale. What is left is a WAL constraint saying 'mark additively, do not re-form without an instruction' - a convention, enforced by nothing, in a repository whose whole campaign exists because conventions were not being kept (F-055/056/057 were one forgotten discipline caught three times by accident). Worth an explicit decision rather than drift: (a) leave as-is and rely on the convention; (b) restore 00-core/90-user to NOTOUCH and keep VIBEVM-SPEC out, since the boot pair is personal and the spec is merely frozen; (c) replace the list with a gate - a floor check that fails if those files changed without a marker recording an owner instruction, which is the only option that does not rely on memory."
  }
 ]
}
