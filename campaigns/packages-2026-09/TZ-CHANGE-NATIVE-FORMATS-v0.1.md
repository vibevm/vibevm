# ТЗ: change-native форматы — первая стройка (v0.1)

**Жанр:** план кампании (временный документ; после исполнения содержимое
переезжает в спеки, план удаляется). **Контракт, который этот план строит:**
[`spec/common/PROP-044-change-native-formats.md`](../../spec/common/PROP-044-change-native-formats.md)
— читать ЦЕЛИКОМ до первого шага. **Статус:** ждёт слова владельца; ни одна
фаза не начинается без него.

**Мандат владельца (2026-08-09, дословно, сокращено):** «Все пакеты будут
опубликованы наружу на миллионы людей. Эволюция максимально быстрая — мы ещё
10 раз сменим формат и манифеста, и пакетов, и чего угодно. Разрушения БУДУТ.
Нам нужна система, которая переживает такой процесс. Формат пакетов и парсер
пакетов — это то, на чём базируется внешний контракт. Сейчас там просто serde…
Подход с serde ужасен и не похож на фундаментальное решение для вечноменяющейся
среды. Нужно: а) документ с идеологией; б) конкретное решение по пакетам и
индексам, оформленное как супер-подробное ТЗ.» Пункт (а) — PROP-044. Пункт (б)
— этот документ.

**Кто исполняет.** Рассчитано на агента слабее автора: каждая фаза несёт точные
файлы, точные команды и машинную приёмку. Исполнителю ЗАПРЕЩЕНО принимать
решения, помеченные «РЕШЕНО» (они приняты здесь), и ОБЯЗАТЕЛЬНО остановиться на
всём, что помечено «СТОП-ВЛАДЕЛЕЦ».

---

## 0. Базовая линия — измерено 2026-08-09, перед написанием плана

Каждое число проверено в этой сессии чтением дерева (третья независимая
проверка после замера и ревьюера исследования). Исполнитель НЕ перемеряет —
но если его наблюдение расходится с этой таблицей, он останавливается и
докладывает (закон: неверные факты о текущем состоянии — самый дорогой класс
ошибок плана).

| # | факт | где |
|---|---|---|
| B1 | `deny_unknown_fields` на каталоге — 15 мест | `crates/vibe-index/src/types/`: repomd.rs:15, entry/mod.rs:38, entry/content.rs ×5, entry/relations.rs ×6, entry/aggregate.rs ×2 |
| B2 | Полей «пусто ≡ нет поля» — 21 (14 коллекций + 7 вложенных структур); вся поверхность пропуска — 35 | `types/entry/**`, гистограмма: `Option::is_none` ×14, `Vec::is_empty` ×12, `BTreeMap::is_empty` ×2, `*Entry::is_empty` ×7 |
| B3 | Каталог перечитывается ради перезаписи на 6 путях | `cli/add.rs:51→122`, `cli/remove.rs:35→57`, `cli/reindex.rs:218→281`, `server/routes/packages.rs:256`, `:304`, `:333` (загрузка `cli/serve.rs:77`); седьмой `write_to` — `cli/init.rs:48`, только пишет |
| B4 | Объединение полутегированное | `types/repomd.rs:42` `#[serde(untagged)]`; тег у `Directory` (`:44-50`), у `File` нет; тест `repomd.rs:132` утверждает `!json.contains("kind")` |
| B5 | Версия каталога нигде не сравнивается; при записи затирается константой писателя | чтение: `index/memory.rs:273`; затирание: `memory.rs:245` (`Repomd::SCHEMA_VERSION`) |
| B6 | Запись недетерминирована: `generated_at: Utc::now()` на каждой записи | `index/memory.rs:249`; при этом gzip намеренно детерминирован (`index/primary.rs:61-65`, тесты `:172`, `:200`) |
| B7 | Словарь видов записан ~14 раз; `BindingSite` — дважды с разными правилами | enum: `vibe-core/src/package_ref/kind.rs:31`, `vibe-index/src/types/kinds.rs:21`; схемы: `schemas/list_report.jtd.json`, `schemas/registry_sync_report.jtd.json`, `crates/vibe-cli/resources/package-tree.schema.v1.json`; `BindingSite`: писатель `index/inverted.rs:88` (`kebab-case`) против читателя `vibe-registry/src/index_client/wire.rs:93` (`lowercase`) — совпадают по случайности однословности, паритет-теста нет |
| B8 | `content_hash` — рецепт зашит константой, идентичности алгоритма нет | `crates/vibe-index/src/content_hash.rs:29` `SHIPPABLE_EXCLUDES`; паритет-тест `tests/content_hash_parity.rs` сторожит две копии, не время |
| B9 | Yank/надгробия нет; must-understand нет; места под подпись нет | grep `yank|revoke|tombstone|signature|must_understand` по `crates/vibe-index/src`, `crates/vibe-core/src/manifest` — 0 |
| B10 | Схемы каталога не существует; `schemas/` — 7 отчётов CLI | `schemas/*.jtd.json`; маршрутизация генератора — одна ветка match `xtask/src/codegen.rs:66` (`generated_dir_for`) |
| B11 | Спайк jtd-codegen 0.4.1 (2026-08-09): discriminator → `#[serde(tag)]` РАБОТАЕТ; enum генерится ЗАКРЫТЫМ (без catch-all); optional → `Option<Box<T>>` + `skip_serializing_if="Option::is_none"`; deny_unknown_fields НЕ эмитится; поля camelCase с `rename` | спайк в scratchpad сессии 2026-08-09; свойства подтверждают заметку `crates/vibe-wire/src/lib.rs:17-43` |
| B12 | Golden-корпусов исторических версий нет; каталожных фикстур нет | `crates/vibe-index/fixtures/` — только `golden-flow-wal-0.1.0` (для паритета хэша); каталога `fixtures/golden-index/` из PROP-005 §3.1 НЕ существует |
| B13 | `vibe.toml` версии/эпохи не несёт; парсер строгий; манифестов в дереве 172 (44 авторских) | `vibe-core/src/manifest/document.rs:66` (deny), поля эпохи нет; счёт манифестов — G4-замер 2026-08-09 |
| B14 | `vibe.lock`: версия 5, `!=` → отказ, перегенерируется каждым install | `vibe-core/src/manifest/lockfile.rs:50`, `:430` |
| B15 | Клиент индекса терпим к полям, закрыт по значениям словарей | `vibe-registry/src/index_client/wire.rs:14-16` (view), `:57`, `:94` (закрытые enum) |

**Внешних потребителей каталога сегодня ноль** (знание владельца, перепроверке
не подлежит) — ломающие изменения провода индекса сейчас бесплатны и никогда
больше такими не будут.

---

## 1. Решения (РЕШЕНО здесь; исполнитель не переоткрывает)

Каждое — с отвергнутыми вариантами и условием пересмотра.

**D1. Архитектура — «одноразовые миры» (PROP-044).** Истина = исходники
пакетов + журнал фактов реестра; всё опубликованное производно и
восстановимо; вечен один хэндшейк. *Отвергнуто:* «вечная линия под машинным
надзором» (Protobuf/Buf-модель) — делает совместимость вечным налогом на
дизайн и слепа к смысловым переломам; «неизменяемый гербарий» (Nix-модель) —
решает 10% задачи и выталкивает агрегаты в неуправляемый слой; «формат как
программа» (данные несут ридер) — поверхность атаки и нестатическая
проверяемость. *Пересмотр:* если тест восстановления невозможно удержать
зелёным (см. риск в PROP-044 §9), архитектура деградирует — остановка,
доклад владельцу.

**D2. Объединение `RepomdFileEntry` становится симметрично тегированным
СЕЙЧАС.** Форма A: `#[serde(tag = "kind", rename_all = "lowercase")]`, оба
плеча несут `kind`. Ломающее изменение провода — бесплатно при нуле
потребителей; разблокирует генерацию (B11). *Отвергнуто:* оставить untagged
(вечно блокирует схему; молча глотает добавленные поля — единственный тип
каталога с этим свойством); adjacently-tagged форма D (`content`-обёртка) —
дороже байтами, а рост числа плеч не ожидается. *Пересмотр:* не предусмотрен.

**D3. Read-modify-write умирает: журнал + проекция.** Мутации сервера и CLI
— дозапись события в журнал фактов, затем перепроекция каталога целиком.
Писатель не принимает на вход типы, прочитанные из собственного каталога.
*Отвергнуто:* «терпимость + захват незнакомого» (`flatten extra`) — лечит
симптом и делает возврат RMW комфортным; статус-кво (строгий RMW) — это
источник половины дефектов базовой линии (B3, B5, B6). *Гейт:* фаза 0
меряет объём переделки (M-RMW ниже); если > бюджета — СТОП-ВЛАДЕЛЕЦ с
задокументированным запасным путём (временный захват на трёх RMW-типах,
явно помеченный как компромисс). *Пересмотр:* по результату M-RMW.

**D4. Свой слой генерации `vibe-wire-gen`.** Постпроцессор/замена
jtd-codegen, эмитящий политику: открытые словари `Unknown(String)`,
детерминизм-дружественные типы, политику пропуска пустого, строгость по
роли, snake_case. *Отвергнуто:* голый jtd-codegen (B11 — не умеет политику);
ручной serde (запрещён PROP-044 законом 5); смена языка схем на JSON
Schema/CUE (JTD выбран за бедность — агент не может выразить опасное; вся
выразительность — в нашем генераторе). *Пересмотр:* если PoC генератора
(фаза 0) обнаружит непроходимое — доклад.

**D5. Эпоха в `vibe.toml` — немедленно.** Поле `epoch = 1` в `[package]`
(u32). Отсутствие = состояние «до эпох» (НЕ «эпоха 1»): читается как
сегодняшний формат, помечается в диагностике. Новые манифесты получают поле
через `vibe init`/шаблоны; массовой правки 172 файлов НЕ делается (кодмод —
поздняя волна). *Отвергнуто:* «отсутствие = 1» (стирает различие «старый»
против «чужой/битый» — довод G4-разбора); обязательность на чтении
(уронит все существующие манифесты). *Пересмотр:* не предусмотрен.

**D6. Идентичность рецепта у `content_hash`.** Формат значения:
`sha256-tree/1:<hex>`; рецепт №1 (список исключений, нормализация путей,
порядок обхода) — данные в `formats/hash_recipes/1.toml`; обе копии
алгоритма читают рецепт из данных; golden-тест на образцовом дереве
замораживает пару (рецепт → хэш). Старые значения `sha256:<hex>` читаются
как рецепт 0 (легаси, замороженный). *Отвергнуто:* оставить константу
(смена списка тихо меняет каждый хэш в мире — запрещено законом «не врать»);
отдельное поле `hash_algo` рядом (две графы там, где хватает префикса).
*Пересмотр:* не предусмотрен.

**D7. Слоты must-understand / подписи / yank — в схему записи сейчас,
криптография — НЕТ.** Поля: `must_understand: []` (список строк-возможностей),
`yanked: bool` (+ `tombstone: {reason, superseded_by?}` на уровне имени),
резерв под `signature`. Безопасность запаркована владельцем (B-015) — только
слоты. *Отвергнуто:* отложить слоты «до надобности» — их окно закрывается
первой публикацией (терпимый читатель, молча глотающий будущее поле подписи,
= downgrade-атака). *Пересмотр:* уведомление владельца по B-015.

**D8. Хэндшейк `hello.json`.** Корень индекса; ключи:
`{vibe: <строка-версия формата хэндшейка>, worlds: [{epoch, path, sunset?}],
min_client?, notice?, successor?}`. Вечный файл, читается максимально
терпимо. Первая реализация вырожденная: один мир, без параллельной
публикации. *Отвергнуто:* версия-в-пути без хэндшейка (клиент без
входной точки не узнаёт о переезде — транспорт без редиректов); HTTP-заголовки
(транспорт — сырые файлы). *Пересмотр:* первый настоящий поезд эпох.

**D9. `vibe.lock` — молчаливая перегенерация.** Несовпадение (эпоха, хэш
генератора, id рецептов) → пересобрать без вопросов; `--frozen` → громкий
отказ (CI). *Отвергнуто:* сегодняшний отказ по `!=` (лишний вопрос
пользователю про черновик). *Пересмотр:* не предусмотрен.

**D10. Конфиги: `fn load() -> Config` без `Result`.** Незнакомый ключ —
предупреждение; кривое значение — умолчание + предупреждение. *Отвергнуто:*
строгий отказ (блокировать работу из-за устаревшей настройки при 10 релизах
в день). *Пересмотр:* не предусмотрен. (Отдельная фаза не выделяется —
применяется при первом же касании `vibe-settings` любой волной.)

**D11. Наблюдаемость сервера: подписчик трассировки ставится ВСЕГДА**
(уровень по умолчанию warn, `--log-level` для операторов). Причина: карантин
записей (D3/PROP-044 §4.5) обязан быть виден оператору; лог, существующий
только под флагом автопубликации, — случайная связь (вопрос владельца №3,
`BACKLOG.md` B-071). СТОП-ВЛАДЕЛЕЦ: это меняет stderr каждого оператора —
подтвердить уровень по умолчанию при чтении этого ТЗ. *Отвергнуто:* статус-кво
(события формируются и выбрасываются). 

**D12. Узкая публичная проекция, сгенерированные клиенты, кодмод-бот,
репетиции заката — НЕ в этом ТЗ.** Это волна 3 PROP-044; отложено поимённо
(закон: отложенное живёт в плане, не в воздухе). *Пересмотр:* после приёмки
этого ТЗ.

**Карта на пять вопросов владельца:** №1 (формат каталога) — решается этим
ТЗ целиком; №2 (недетерминированная запись) — растворяется фазами 2–3 (B6);
№3 (логи сервера) — D11; №4 (планка доказательства) и №5 (соединение
движков) — НЕ трогаются, остаются владельцу.

---

## 2. Правила исполнения (для каждого исполнителя, каждой фазы)

1. Прочитать ЦЕЛИКОМ: PROP-044, этот документ, `CLAUDE.md` (четыре правила).
   Воркеру, запускаемому через fractality: транспортный закон
   `campaigns/packages-2026-09/SUBAGENT-LAUNCHERS.md` читает БОСС, пакеты
   собирает босс; воркеры не запускают git.
2. Вендоренные копии (`vibedeps/**`, `packages/**/vibedeps/**`) НЕ редактируются
   никогда. Движки дисциплины (`packages/org.vibevm.ai-native/**/crates/**`)
   в этом ТЗ не трогаются вовсе — если кажется, что надо, СТОП и доклад.
3. Каждая фаза заканчивается зелёной панелью: `bash tools/self-check.sh`
   (Git Bash, читать РЕАЛЬНЫЙ код выхода `; echo "EXIT=$?"`, вердикт из
   хвоста). Любая правка текста `spec/**` → `cargo xtask specmap` в той же
   посадке; `cargo xtask specmap --check` зелёный.
4. Бюджет длины файла: 600 строк ПОСЛЕ `cargo fmt --all`. Новые ячейки —
   маленькие файлы, а не дописывание больших.
5. Коммиты: атомарные, Conventional Commits, subject — «почему», тело
   цитирует `spec://…` якоря PROP-044. Стейджить явные пути, никогда
   `git add -A`. Сообщения через heredoc `git commit -F - <<'MSG'`.
6. Ошибки нового кода цитируют REQ и поверхность починки (дом-стиль);
   `unwrap/expect` в доменной логике запрещены.
7. Ничего не публикуется наружу: никаких пушей в `vibespecs`, никаких
   реальных реестров. Все тесты герметичны.
8. Тексты отказов/карантина — генерируются из данных (реестр форматов,
   записки), не пишутся в строковых литералах по месту.

---

## 3. Фаза 0 — спайки и замеры (БЕЗ коммитов; гейтует всё дальнейшее)

**Ф0.1 — M-RMW: объём переделки мутаций.** Посчитать по дереву: строки и
структуру трёх серверных мутаций (`server/routes/packages.rs` — upsert,
delete_version, delete_package), `cli/add.rs`, `cli/remove.rs`,
`cli/reindex.rs` (инкрементальный путь), их тестов (`server_writes.rs`,
`auto_publish.rs`, `cli_write.rs`, части `cli_lifecycle.rs`). Выход: таблица
«файл → строк затронуто → сложность (переписать/подвинуть/оставить)» и
вердикт: укладывается ли переделка в бюджет **двух рабочих фаз** (Ф3 + хвост).
Если НЕТ — СТОП-ВЛАДЕЛЕЦ с запасным путём из D3.

**Ф0.2 — PoC генератора.** В scratchpad (не в дереве): взять выход
jtd-codegen для схемы с закрытым enum (спайк B11 воспроизводим) и написать
постпроцессор-набросок (~100 строк), который переписывает сгенерированный
закрытый enum в открытый:
```rust
pub enum PkgKind { Flow, …, Lang, Unknown(String) }
// + ручные impl Serialize/Deserialize c веткой Unknown, сохраняющей строку
```
Приёмка PoC: round-trip тест — `"plugin"` читается в `Unknown("plugin")` и
пишется обратно как `"plugin"`; известные значения без изменений; порядок
байтов детерминирован. Если постпроцессинг непроходим — вердикт «пишем
генератор с нуля по JTD» (это НЕ стоп, это выбор ветки в Ф4).

**Ф0.3 — Инвентарь форматов.** Составить список для реестра (Ф1): каталог
(three-way по PROP-044 §6.1), `vibe.toml`, `vibe.lock`, 7 CLI-отчётов
(`schemas/*.jtd.json`), `package-tree.schema.v1.json` (`vibe load --json`),
MCP-схемы инструментов (`crates/vibe-mcp/src/tools.rs` — перечислить),
subskill-манифест, журнал (новый), хэндшейк (новый). По каждому: восстановим?
сколько парсеров? где схема/её нет.

**Выход фазы 0:** три находки в `campaigns/packages-2026-09/harvest/`
(`f0-rmw-volume.md`, `f0-gen-poc.md`, `f0-format-inventory.md`). Спайки не
оставляют изменений дерева.

---

## 4. Фаза 1 — необратимое (окно закрывается первой публикацией)

Порядок внутри фазы жёсткий. Каждый шаг — отдельный коммит.

**Ф1.1 — Реестр форматов.** Новый файл `formats/REGISTRY.toml` (корень):
по записи на формат из Ф0.3: `id`, `epoch`, `schema` (путь или `none`),
`recoverable` (bool), `foreign_parsers` (`none|ours|many`), `corpus` (путь
или `none`), `sunset` (дата или `none`). Плюс генерация: новый маленький
крейт-модуль в `vibe-core` (или `vibe-wire`) с `enum FormatId` — генерируется
`cargo xtask codegen` из реестра (расширить `xtask/src/codegen.rs`; там
уже есть маршрутизация по stem — добавить ветку). Приёмка: `FormatId`
перечисляет все записи; `cargo xtask check-codegen` ловит ручную правку;
тест «формат без записи в реестре не собирается» (компиляционный тест
использования `wire::publish` — появится в Ф4; здесь — unit-тест на полноту
enum против TOML).

**Ф1.2 — Эпоха в `vibe.toml` (D5).** `vibe-core/src/manifest/document.rs`:
поле `epoch: Option<u32>` в `[package]` (serde default None; НЕ обязательное).
`vibe init` и шаблоны пишут `epoch = 1`. Диагностика: `vibe check` сообщает
`epoch absent (pre-epoch manifest)` как info, не ошибку. Приёмка: старые
манифесты читаются без изменений поведения; новый `vibe init` даёт манифест с
эпохой; round-trip сохраняет поле; `cargo test -p vibe-core` зелёный.

**Ф1.3 — Рецепт хэша (D6).** `formats/hash_recipes/1.toml`: `excludes`,
`path_normalisation = "backslash-to-slash"`, `sort = "by-component"`.
`crates/vibe-index/src/content_hash.rs` и его двойник в `vibe-registry`:
читают рецепт (включённый в бинарь через `include_str!` + парс один раз);
эмитят `sha256-tree/1:<hex>`; читатели принимают `sha256:<hex>` как рецепт 0
(две константы эквивалентны сегодня — зафиксировать тестом). Golden:
`fixtures/golden-flow-wal-0.1.0` → замороженное значение для рецепта 1 в
`tests/content_hash_parity.rs` (расширить, не заменить). Приёмка: паритет
двух копий сохранён; golden — точное равенство строки; смена
`hash_recipes/1.toml` роняет golden (доказать временной правкой, откатить).
СТОП-ВЛАДЕЛЕЦ перед сменой формата значения в существующем локфайле:
`vibe.lock` перегенерируется (D9 ещё не построен) — в этой фазе локфайл
продолжает нести старый вид строки, новый вид пишет только индекс.

**Ф1.4 — Слоты (D7).** В типы записи каталога: `must_understand: Vec<String>`
(skip если пуст), `yanked: bool` (skip если false), `tombstone` на уровне
`NameEntry` (`Option<Tombstone>{reason, superseded_by}`). Писатели пишут
пусто/false; читатели каталога учат ОДНО поведение уже сейчас: запись с
непустым `must_understand`, содержащим незнакомую строку, — карантин
(в этой фазе = пропуск с warn-логом; полноценный карантин — Ф6). Приёмка:
round-trip; тест «запись с must_understand=["x"] пропускается с
предупреждением, остальные читаются»; панель зелёная.

**Ф1.5 — Тегированное объединение (D2).** `types/repomd.rs`:
`#[serde(tag = "kind", rename_all = "lowercase")]`, `DirectoryTag` умирает,
`File` получает тег на проводе. Обновить golden-тест `repomd.rs:119-133`
(теперь `"kind":"file"` ОБЯЗАН присутствовать); `parses_real_world_shape`
переписать под новый провод. Это первый настоящий перелом — оформить его
эталонно: записка `formats/breaks/001.md` (что/почему/эпоха не нужна —
потребителей ноль/рецепт: «пересоздай индекс: vibe-index reindex»). Приёмка:
workspace-тесты зелёные; в записке стоит дата и рецепт.

---

## 5. Фаза 2 — детерминизм (закон 6; растворяет вопрос №2 владельца)

**Ф2.1 — Часы как вход.** Убрать `Utc::now()` из писателей каталога:
`Index::write_to(&self, data_dir, at: DateTime<Utc>)` (или `WriteCtx`);
`generated_at` записей — из аргумента; вызывающие передают: CLI — момент
команды, сервер — момент события мутации. `NameEntry.indexed_at` — максимум
по кандидатам (уже так, `finalise`). Механический гейт: в модулях-писателях
(`index/`, `types/`) появление `Utc::now` запрещено — шаг в
`tools/self-check.sh` (grep-гейт с точным перечнем модулей; сообщение
называет PROP-044 §4.3 и команду починки). Приёмка: двойная сборка одного
состояния с одним `at` → байт-в-байт; grep-гейт красный на пробной вставке
(доказать, откатить).

**Ф2.2 — Версию не затирать.** `memory.rs:245`: писатель штампует
СВОЮ версию только в артефакты, которые сам порождает с нуля; прочитанная
версия не перезаписывается молча (при перепроекции из журнала это
растворится само — здесь закрыть только явное затирание + тест). Приёмка:
тест «load(эпоха N) → write → эпоха N сохранена ИЛИ перелом объявлен».

**Ф2.3 — Идемпотентный upsert.** Повторная идентичная мутация → пустой
diff → авто-коммит не создаётся (`AUTO-PUBLISH-AN-EMPTY-DIFF-IS-SUCCESS`
уже есть — теперь он срабатывает и на редундантном upsert'е, не только на
гонке). Приёмка: тест в `auto_publish.rs`: дважды один upsert → один коммит.

---

## 6. Фаза 3 — журнал и проекция (D3; гейтована Ф0.1)

**Ф3.1 — Журнал фактов.** Новый модуль `crates/vibe-index/src/journal/`:
NDJSON, только дозапись, шардирование по месяцу (`journal/2026-08.ndjson`),
запись = `{at, actor, event}` где `event` ∈
`published{entry} | yanked{group,name,version,reason} | removed{…} |
renamed{from,to} | notice{…}`. Формат журнала — запись в реестре форматов,
схема в `schemas/journal/e1/`. Контрольная точка: `journal/checkpoint.json`
(последнее спроецированное смещение). Приёмка: append+replay тесты;
детерминированная сериализация.

**Ф3.2 — Проектор.** `Index::project(events) -> Index` — чистая функция;
`write_to` вызывается только над результатом проекции. Серверные мутации:
валидация входа → append в журнал → replay хвоста → write_to(at=событие).
`add`/`remove` CLI — те же события. `reindex`/`rescan-org` — источник
событий `published` из сканера (сканер остаётся как есть). Правило G4
механически: типы из `index_client`/чтения каталога не появляются в
сигнатурах писателей (arch-тест: грубый grep-гейт по импортам модулей
`journal|projector`, шаг self-check). Приёмка: `xtask rebuild --check` —
новый xtask: снести каталог, спроецировать из журнала, сравнить байты
(на герметичной фикстуре); шесть RMW-путей из B3 не читают каталог
(грep-доказательство: `load_from` остаётся только в путях чтения/serve-старте
для ОБСЛУЖИВАНИЯ чтения, не как вход мутации).

**Ф3.3 — Строгость читателя падает бесплатно.** Снять
`deny_unknown_fields` с типов каталога (15 мест B1) — теперь это безопасно:
прочитанное никогда не переписывается. Клиентские view (`wire.rs`) не
меняются. Приёмка: тест «каталог с незнакомым полем читается; запись из
журнала его не уносит, потому что запись не из чтения»; панель зелёная.

*(Если Ф0.1 сказал «не укладываемся» — вместо Ф3 исполняется задокументированный
запасной путь D3 и ТЗ возвращается владельцу на пересмотр объёма.)*

---

## 7. Фаза 4 — схема и генератор (D4)

**Ф4.1 — Схема каталога.** `schemas/index/e1/`: `entry.jtd.json`
(VersionEntry + вложенные), `repomd.jtd.json` (с тегированным объединением),
`by_name.jtd.json`, `journal.jtd.json` (из Ф3.1). Словари — ТОЛЬКО здесь
(enum в JTD); это единственный источник (G9). Приёмка: схемы валидны для
jtd-codegen; `cargo xtask codegen` строит без ошибок.

**Ф4.2 — `vibe-wire-gen`.** По ветке из Ф0.2: постпроцессор выхода
jtd-codegen (или свой эмиттер) в `xtask/src/wiregen/` (developer tooling,
вне продуктовых крейтов): открытые словари `Unknown(String)`; snake_case;
политика пропуска пустого ПО СХЕМЕ (metadata-аннотация
`"x-empty": "omit"|"emit"` в JTD metadata — генератор читает); строгость по
роли из реестра форматов (`foreign_parsers=none` → deny; иначе терпимо).
Выход — в `crates/vibe-wire/src/generated/index_e1/` и замена рукописных
типов `vibe-index/src/types/**` на реэкспорт сгенерированных (методы
(`finalise`, `sort_key`, конструкторы) живут в отдельных impl-файлах РЯДОМ,
не в generated). Приёмка: `cargo xtask check-codegen` расширен на новые
цели; workspace зелёный; паритет-тест словаря: `PackageKind` в
`vibe-core` == сгенерированному (существующий тест переезжает на новый
источник); `BindingSite` становится ОДНИМ определением с двумя реэкспортами —
B7 закрыт структурно.

**Ф4.3 — Запрет рукописного провода.** Шаг self-check: `#[derive(Serialize`
/ `Deserialize` вне `**/generated/**` и вне белого списка (манифест/локфайл
до их очереди; список перечислен в шаге поимённо, с датой пересмотра) —
красный. Сообщение гейта: правило, одна фраза «почему» (PROP-044 §2, закон 5),
команда починки («опиши в схеме; запусти cargo xtask codegen»). Приёмка:
пробная вставка derive в `vibe-index/src/` роняет панель (доказать, откатить).

---

## 8. Фаза 5 — корпуса, wire-diff, окно перелома

**Ф5.1 — Золотые корпуса.** `formats/corpora/index/e1/`: полный маленький
каталог (3 пакета, 5 версий, все словари задействованы, must_understand
образец, yanked образец), собранный детерминированно фикстурным журналом.
Тест: пересборка корпуса из его журнала → байт-в-байт (это тот же
`rebuild --check`, направленный на фикстуру, — работает в CI без сети).

**Ф5.2 — `wire-diff`.** `cargo xtask wire-diff`: пересобрать корпуса,
`git diff --exit-code -- formats/corpora/` ИЛИ непустой diff покрыт запиской
`formats/breaks/NNN.md`, на которую в том же коммите есть ссылка (проверка:
изменённые файлы корпуса + наличие новой записки). Шаг в self-check.
Приёмка: правка схемы без записки — красный; с запиской — зелёный
(доказать оба, откатить пробу).

**Ф5.3 — Окно перелома.** `formats/EPOCHS.toml`:
`break_window_open = true` (сейчас открыто — мы в стройке). Шаг self-check:
при `false` любые изменения под `schemas/**|formats/**` — красный. Приёмка:
флаг в false + пробная правка схемы → красный (доказать, откатить).

---

## 9. Фаза 6 — хэндшейк и карантин

**Ф6.1 — `hello.json` (D8).** Пишется `write_to` в корень данных:
`{"vibe":"hello/1","worlds":[{"epoch":1,"path":"."}]}` (+ `min_client`,
`notice`, `successor` — опционально, absent-если-нет). Схема в
`schemas/hello/e1/`; запись в реестре (`recoverable=true`,
`foreign_parsers=many`). Клиент (`index_client`): читать `hello.json` ПЕРЕД
`repomd.json`; нет файла → сегодняшний путь (совместимость с существующими
индексами); есть → выбрать мир своей эпохи; своей нет → отказ С РЕЦЕПТОМ
(«миры: …; твой клиент понимает эпоху N; обнови vibe / возьми мир N»).
Приёмка: e2e — клиент против фикстур с/без hello; отказной текст содержит
рецепт и перечень миров.

**Ф6.2 — Карантин по-настоящему (доводит Ф1.4).** Пропуск записи с
незнакомым must_understand: причина сохраняется в память индекса; `vibe`
и сервер отвечают на запрос ИМЕНИ с карантинными версиями явным
`unavailable {name, version, missing: [...], recipe}` (не молчанием — закон
«не отвечать молчанием»). D11 здесь же: подписчик всегда, warn по умолчанию.
Приёмка: тест «запись в карантине → поиск/由 имени отвечает unavailable с
причиной»; лог несёт warn без флага автопубликации.

---

## 10. Приёмка всего ТЗ (прогоняется целиком, по порядку)

```bash
# 0. чистое дерево, панель зелёная до старта
bash tools/self-check.sh ; echo "EXIT=$?"          # EXIT=0
# 1. реестр форматов согласован с генерацией
cargo xtask check-codegen                           # exit 0
# 2. детерминизм: двойная сборка
cargo test -p vibe-index deterministic              # тесты Ф2 зелёные
# 3. восстановимость
cargo xtask rebuild --check                         # exit 0
# 4. корпуса и wire-diff
cargo xtask wire-diff                               # exit 0 (без незакрытых diff)
# 5. карантин и hello — e2e
cargo test -p vibe-index quarantine hello           # зелёные
cargo test -p vibe-registry index_client            # клиент: hello + fallback
# 6. рукописный провод запрещён
#    (шаг внутри self-check; пробная вставка обязана уронить — см. Ф4.3)
# 7. вся панель
bash tools/self-check.sh ; echo "EXIT=$?"          # EXIT=0
```

**Предсказания (фальсифицируемы; проверяются в отчёте кампании):**
P1 — повторный идентичный upsert не создаёт коммита (Ф2.3). P2 — каталог с
незнакомым полем и незнакомой записью читается, сервер стартует, карантин
виден (Ф3.3+Ф6.2). P3 — `rebuild --check` зелёный на фикстуре и на живом
data-dir (Ф3.2). P4 — правка схемы без записки не проходит панель (Ф5.2).
P5 — словарь видов существует в одном месте; добавление 7-го вида = правка
одной схемы + перегенерация + ветки Unknown не ломаются (G9; можно доказать
пробой в ветке). P6 — старые манифесты без эпохи читаются без изменений
поведения (Ф1.2).

**Что НЕ делать (запреты этого ТЗ):** не вводить `flatten`-захват; не писать
мигратор каталога (производное не мигрируют — пересобирают); не трогать
движки дисциплины и вендоренные копии; не описывать схемами манифест/локфайл
(их очередь — следующее ТЗ; здесь только эпоха-поле D5); не публиковать
ничего в реальные реестры; не бампать версии 38 пакетов (B-067 ждёт
публикации); не строить параллельную публикацию миров (D12).

**СТОП-ВЛАДЕЛЕЦ (сводно):** вердикт Ф0.1 если превышен бюджет (D3);
уровень лога по умолчанию (D11); смена вида `content_hash` в локфайле
(Ф1.3); ратификация PROP-044 до старта фазы 1.
